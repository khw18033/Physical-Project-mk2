# 360° 카메라 실시간 영상 파이프라인 — 총정리 v2

**작성일**: 2026-09-21
**작성자**: 이대규
**목적**: 360° 카메라(Insta360 Pro2) 파이프라인을 **사진 촬영 방식에서 실시간 영상 스트림
방식으로 전환**한 작업 전체 기록. 전환 근거, 새로 확정한 파라미터, 실측 수치, 검증 실험,
미해결 항목을 모두 포함한다.

**v1(`260915_360도카메라_파이프라인_총정리.md`)과의 관계**
- v1은 **사진 촬영 방식**의 기록이다. 이 문서가 그것을 **대체**한다.
- 다만 v1의 §7(검증실험) · §8(yawOffsetDeg) · §9(검출품질) · §10(폐기항목) ·
  §11(트러블슈팅) · §12(함정) 상당수는 **여전히 유효**하다. 이 문서는 그 위에 얹는다.
- 어느 쪽이 현행인지 헷갈리지 않도록 v1에 절을 덧붙이지 않고 별도 문서로 분리했다.
- **v1을 폐기하지 말 것.** 사진 경로는 논문(AsiaSim 2026)의 측정 기준 구성이며,
  앵커 검출 정확도 11.2cm는 그 구성에서 줄자로 검증된 값이다.

**이 문서의 용도**
- 영상 파이프라인의 "왜 이렇게 했는가"를 되짚는 근거 기록
- 미해결 항목과 그 위에 얹을 업그레이드의 출발점
- 실행 절차서는 별도: `260921 카메라 최종정리2.txt`

**권위 순서**: 실측 로그 > 이 문서 > v1 문서 > 과거 채팅 기록 > 기억

---

# 1. 결과 요약

## 1.1 무엇을 바꿨나

**PC의 `pro2_auto_bg.py`(촬영+stitch) 자리를 `stream_to_server.py`(스트림 프레임 공급)로
교체한 것이 전부다. 서버와 유니티는 한 줄도 바꾸지 않았다**(`anchor360_live.py`의
2줄 추가 제외, §3.2).

```
[구] Insta360 Pro2 ──Farsight──> [PC]
         촬영 20.6s              다운로드 → ProStitcher stitch 52.2s
                                 → equirect.jpg 업로드
                                      │ HTTP POST :7866
                                      ▼
                                 [서버] pano_receiver → anchor360_live
                                      │ HTTP GET (PC가 당겨감)
                                      ▼
                                 [PC] anchor360_bridge ──UDP :5010──> [Unity]

[신] Insta360 Pro2 ──Farsight──> [PC]
      인카메라 실시간 스티칭       RTMP 프레임 수신 → JPEG → 업로드
      equirect 3840x1920            (stitch 단계 자체가 없음)
      RTMP :1935                         │ HTTP POST :7866
                                         ▼
                                    [서버] pano_receiver → anchor360_live
                                         │ HTTP GET
                                         ▼
                                    [PC] anchor360_bridge ──UDP :5010──> [Unity]
```

**핵심은 stitch 단계의 소멸이다.** 카메라가 6개 렌즈를 내부에서 실시간 합성해
equirect 3840×1920을 직접 내보낸다. 이것은 ProStitcher가 52초 걸려 만들던 것과
**같은 규격**이다.

## 1.2 무엇이 좋아졌나

| 항목 | 사진 방식 (실측) | 영상 방식 (실측) | 배수 |
|---|---|---|---|
| 카메라 → 서버 도착 | **73초** (촬영 20.6 + stitch 52.2) | **0.23초** | 317× |
| 서버 비전처리 | 0.19초 | **0.05초** | 3.8× |
| 유니티 갱신 주기 | **58초** | **0.44초** (2.29 Hz) | 132× |
| **종단 지연** | **73초 ~ 13분** | **약 0.5초** (최악 1.3초) | **150×+** |
| 유실률 | **61%** (62장 중 38장 적체) | **0.2%** (614장 중 1장) | — |
| PC 디스크 | 시간당 3GB | **0** | — |
| 서버 디스크 | 장당 3MB 누적 | **0** (한 폴더 덮어쓰기) | — |

## 1.3 검증 증거

| 구간 | 증거 |
|---|---|
| 카메라 → PC | `[스트림] 열림 4.18s` → 리더 스레드 **1909프레임 수신, read 실패 0** |
| PC → 서버 | **614장 업로드, 실패 0건**, 각 780KB |
| 서버 수신 | `pano_receiver` 로그에 `POST /upload_pano 200 OK` 연속 |
| 서버 검출 | `[live] #1 ~ #613`, 총 **613장 처리** |
| 서버 → PC | 브릿지 `NEW ts=...` **399건**, ts 간격 중앙 0.371초 |
| PC → Unity | UDP 5010 송출 539회 |
| Unity 반영 | `Anchor360_Obstacles` 아래 큐브 실시간 갱신 확인 |

**업로드 614 → 처리 613. 차이는 마지막 한 장뿐이다.** 사진 방식이 62장 중 38장을
큐에 쌓아둔 것과 구조적으로 다르다.

## 1.4 v1 대비 변경/해소된 항목

| v1 항목 | 상태 |
|---|---|
| §3.1 `pro2_auto_bg.py` / `stitch_prostitcher.py` / TRT 캐시 | **영상 경로에서 폐기** |
| §4.2 슬라이스 각도 u+50/150/230 | **180도 이동** → u+230/330/50 (§4.2) |
| §4.1 하향각 −35° | **−36.35°로 슬라이스, 바닥계산은 −35° 유지** (§4.3) |
| §6.2 stitch 벤치마크(`ai + topfixer=1` 12.7초) | **해당 없음** (인카메라 스티칭) |
| §12 "TRT 캐시 삭제 / `gyro.mp4` / stitch vs 촬영 여유" | **소멸** |
| §12 "디스크 `captures/` 시간당 3GB" | **소멸** (PC에 저장 안 함) |
| §13 C-2 "stitch vs 촬영 여유 확보" | **규명 없이 해소** (단계 자체가 없어짐) |
| §8 `yawOffsetDeg` 미해결 | **그대로 유지** (§8) |
| §9.1 소화기 미검출 | 영상에서는 conf 0.25~0.45로 검출됨 (§9.3) |
| §10.1 MASt3R 자세추출 폐기 | **그대로 유지** |
| §10.2 세 슬라이스 통합 보류 | **그대로 유지** (§10.4) |

---

# 2. 환경 정보

## 2.1 장비 및 주소

| 장비 | 주소 | 비고 |
|---|---|---|
| Insta360 Pro2 | `192.168.100.124` | Farsight DHCP. 실제로는 잘 안 바뀌나 전원마다 확인 |
| Pro2 명령 API | 포트 `20000` | `/osc/commands/execute`, `/osc/state` |
| Pro2 파일 다운로드 | 포트 `8000` | **영상 경로에서는 미사용** |
| **Pro2 RTMP 서버** | 포트 **`1935`** | **신규.** `/live/preview` |
| 서버 sysai-server2 | `210.110.250.33` | 학교망. SSH 8522. RTX A6000 |
| 촬영 PC | `192.168.50.203` | 서버에는 `203.230.104.168`(NAT 공인)로 보임 |
| Unity PC (팀원) | `192.168.50.244` | 랩실 사설망 |

**카메라 펌웨어**: `1.4.042621` / moduleVersion `3.3.181130.181229E1` / S/N `IP2E51CNJ7N88T`

## 2.2 네트워크 제약 — v1 §2.2가 그대로 유효하다

**서버(210.110.250.33) → Unity PC(192.168.50.244) UDP 직송은 여전히 불가능하다.**
서버에 `192.168.50.x` 인터페이스가 없고, UDP `sendto()`는 그 상황에서도 성공을 반환한다.
`anchor360_live.py`의 `UDP OK` 로그는 **도달 증거가 아니다.**

실제 송출은 `anchor360_bridge.py`가 한다. 영상 전환은 이 구조를 바꾸지 않았다.

## 2.3 새 함정 — `_previewUrl`은 카메라 자기 자신 기준이다

`camera._startPreview`의 응답은 이렇게 온다.

```json
{"name": "camera._startPreview", "sequence": 21, "state": "done",
 "results": {"_previewUrl": "rtmp://127.0.0.1/live/preview"}}
```

**`127.0.0.1`은 카메라 내부 주소다.** RTMP 서버가 카메라 안에서 돌고 있고, 카메라가
자기 기준으로 알려준 것이다. PC에서 그대로 열면 PC 자신을 찾으므로 아무것도 안 나온다.

PC에서 열 주소는 **`rtmp://192.168.100.124:1935/live/preview`**이다.
공식 API 문서도 `rtmp://{camera_ip}:1935/live/preview`로 적고 있다.
`stream_to_server.py`와 `preview_probe.py`가 자동 치환한다.

## 2.4 카메라 제어 세션은 하나뿐이다

Pro2는 제어 클라이언트를 **하나만** 붙인다. 두 번째가 `camera._connect`를 시도하면:

```json
{"state": "exception", "error": {"code": "connect error",
 "description": "already connected by another"}}
```

**이것의 실무적 의미:**
- `pro2_auto_bg.py`(사진)와 `stream_to_server.py`(영상)를 **동시에 돌릴 수 없다.** 택일이다.
- 두 경로를 비교 실험할 때는 번갈아 돌려야 한다.
- `preview_probe.py` 창이 살아 있으면 다른 스크립트가 막힌다.
- Insta360 Pro 앱(윈도우 클라이언트/폰 앱)이 열려 있어도 같다.

## 2.5 Fingerprint와 하트비트

`camera._connect`가 발급하는 `Fingerprint`는 **토큰일 뿐이고**, 카메라는 그것과 별개로
`/osc/state` 폴링(1초 주기)이 끊기면 세션을 "연결 끊김"으로 본다.

```python
def heartbeat():
    while not hb_stop.is_set():
        requests.post(STATE_URL, json={}, headers=headers(), timeout=3)
        time.sleep(1)
```

**그래서 curl 두 줄로는 명령이 안 통한다.** `_connect`로 Fingerprint를 받고 사람이
복사·붙여넣기 하는 10여 초 사이에 세션이 죽어서 두 번째 명령이
`disabledCommand / camera not connected`로 떨어진다. 실제로 겪었다(§11).

## 2.6 소프트웨어 환경

**서버**
- venv: `venv_image` (변경 없음)
- `py360convert`, `ultralytics(YOLOWorld)`, `opencv`, `yaml`, `fastapi`, `uvicorn`
- CLIP 캐시: `/home/dg/capstone-db/docx2026/weights/clip/ViT-B-32.pt` (약 338MB)

**PC**
- Python 3.12.10
- `requests`
- **`opencv-python` (신규 필수)** — RTMP 수신 + JPEG 인코딩. wheel에 FFmpeg 포함
- ProStitcher는 **영상 경로에서 불필요**

**Unity**
- Unity 6.3 LTS (6000.3.5f2), 프로젝트 `Go1_Unity`, 씬 `lab` — **변경 없음**

---

# 3. 파일 명세

## 3.1 PC 측

### `stream_to_server.py` — 스트림 프레임 공급 (신규, 2026-09-21)

**위치**: `C:\Users\asdfa\physical mk2\360camera_work`
**역할**: `pro2_auto_bg.py`를 대체한다. 같은 `/upload_pano`에 같은 `equirect.jpg`를 올리므로
서버 감시 루프가 입력이 무엇이었는지 알 필요가 없다.

**구조**
- 메인 스레드: 카메라 connect → 하트비트 시작 → `_startPreview` → RTMP 열기 →
  목표 fps로 최신 프레임 집어 JPEG 인코딩 → POST
- 리더 스레드 1개: `cap.read()`를 쉬지 않고 돌려 **최신 한 장만** 보관
- 하트비트 스레드 1개: `/osc/state` 1초 주기

**지연 누적 방지 — 이 스크립트의 핵심 설계**

`cv2.VideoCapture`는 RTMP 프레임을 내부 버퍼에 쌓는다. 메인 루프가 5fps로 `read()`를
부르면 13~15fps로 들어오는 프레임이 계속 밀려 **오래된 장면이 나온다.**
사진 방식에서 stitch 큐가 적체된 것과 **정확히 같은 현상이 버퍼 안에서 재현된다.**

```python
class Latest:
    def put(self, f, t):
        with self.lock:
            if self.frame is not None:
                self.dropped += 1      # 메인이 안 집어간 것 = 버린 프레임
            self.frame, self.t, self.seq = f, t, self.seq + 1
```

리더가 최신 한 장만 들고, 메인이 집어가지 않은 것은 즉시 버린다.
**실측에서 1909프레임 중 1294장을 버렸고, 프레임 나이가 29~46ms로 고정됐다.**
이 값이 커지지 않는 것이 적체가 없다는 직접 증거다.

**옵션**

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--ip` | `192.168.100.124` | 카메라 IP |
| `--upload` | `http://210.110.250.33:7866/upload_pano` | 서버 업로드 엔드포인트 |
| `--capture-id` | `LIVE_STREAM` | 서버에 덮어쓸 폴더명. 고정이라 디스크가 늘지 않음 |
| `--camera-id` | `pro2_anchor` | |
| `--fps` | `5.0` | 서버로 올릴 목표 fps |
| `--quality` | `92` | JPEG 품질. **낮추면 인코딩이 빨라진다**(§6.3) |
| `--width` / `--height` | `3840` / `1920` | 스티칭 출력 해상도 |
| `--bitrate` | `10240` | kbps. **올리면 fps가 떨어진다**(§7.5) |
| `--rotate` | `0` | capture_id를 N개 폴더로 돌려씀. `--no-wait-stable` 미적용 시 8 권장 |
| `--count` | `0` | N장 올리고 종료 (0=무한). 측정용 |
| `--dry-run` | off | 업로드 없이 스트림 성능만 측정 |
| `--url` | — | 스트림 주소 직접 지정 |

**출력** — 프레임마다 구간별 시간을 찍고, 종료 시 중앙/p90/최대를 낸다.

```
  #12   나이   87ms  인코딩  112ms  업로드   196ms  | PC구간   395ms   987KB  saved
```

**종료**: `Ctrl+C` → `_stopPreview` 전송 → 리더 join → 통계 출력

**주의 — 해결된 버그 이력**: 초기 버전은 종료 시 메인이 `cap.release()`를 부르는데
리더가 `cap.read()` 중이라 `Assertion fctx->async_lock failed at
libavcodec/pthread_frame.c:178`로 프로세스가 죽었다. 통계도 못 찍고 `_stopPreview`도
못 보냈다. **현재는 리더 스레드가 `cap`의 수명을 전적으로 소유하고 `finally`에서만
해제한다.** 메인은 `_stopPreview`를 먼저 보내 스트림을 끊고 join한다.

### `preview_probe.py` — 스트림 확인용 (신규, 2026-09-21)

**역할**: 파이프라인을 붙이기 전에 "카메라가 정말 equirect를 내보내는가"를 확인하는 도구.
`--measure N`으로 N초간 실제 해상도·종횡비·fps를 재고 첫 프레임을 저장한다.

| 옵션 | 설명 |
|---|---|
| `--measure N` | N초간 계측 후 첫 프레임 저장 |
| `--fps` / `--bitrate` / `--width` / `--height` | `_startPreview` 파라미터 |
| `--no-stab` | 자이로 안정화를 끈다. **쓰지 말 것**(§7.6) |
| `--restart` | 껐다 다시 켜기 |
| `--stop` | 프리뷰만 끄고 종료 |
| `--url` | 스트림 주소 직접 지정 |

운영에는 쓰지 않는다. 카메라를 옮기거나 펌웨어가 바뀐 뒤 기하를 재확인할 때 쓴다.

### 폐기된 PC 파일 (영상 경로 한정)

| 파일 | 상태 |
|---|---|
| `pro2_auto_bg.py` | 영상 경로에서 미사용. **사진 경로용으로 보존**(논문 기준 구성) |
| `stitch_prostitcher.py` | 동상 |
| `trt_cache/` | 동상. 영상 경로에서는 TRT 콜드빌드 개념 자체가 없음 |
| `pro2_auto.py`, `pro2_pipeline.py` | 구버전. 참고용 |

## 3.2 서버 측

### `pano_receiver.py` — **수정하지 않음**

포트 7866. `/upload_pano`(POST), `/pano_health`(GET), `/anchor360_latest`(GET).
원자적 쓰기(`.part` → `os.replace`)를 이미 쓰고 있고, 이것이 §3.2의 수정 근거가 된다.

**초당 4~5회 덮어쓰기에도 문제없이 동작했다** — 614회 POST 전부 `200 OK`.

### `anchor360_live.py` — **2줄 추가**

검출·필터·NMS·`pixel_to_ground`·JSON 스키마는 **한 글자도 건드리지 않았다.**
추가한 것은 `--no-wait-stable` 플래그뿐이다.

```
437a438,439
>     ap.add_argument("--no-wait-stable", action="store_true",
>                     help="원자적 쓰기를 신뢰하고 크기 안정화 대기를 건너뛴다")
546c548
<                     if wait_stable(latest[0]):
---
>                     if args.no_wait_stable or wait_stable(latest[0]):
```

**왜 필요했나 — `wait_stable`이 프레임당 1.5초를 먹었다.**

```python
def wait_stable(path, tries=5, delay=0.3):
    """
    파일 크기가 연속 2회 같으면 쓰기 완료로 본다.
    pano_receiver 가 원자적 교체(.part → os.replace)를 쓰면 사실 불필요하지만,
    안전망으로 둔다.
    """
    last = -1
    for _ in range(tries):
        cur = os.path.getsize(path)
        if cur > 0 and cur == last:
            return True
        last = cur
        time.sleep(delay)
    return last > 0        # ← 5번 다 실패해도 여기서 True
```

`LIVE_STREAM/equirect.jpg`를 초당 5회 덮어쓰므로 **크기가 매번 달라져 5회 전부
"같지 않음"으로 떨어진다.** 5 × 0.3 = 1.5초를 버리고 마지막에 True를 반환한다.

```
1.5(wait_stable) + 0.08(처리) + 0.1(poll) = 1.68초
```

브릿지가 관측한 JSON `ts` 간격이 정확히 **1.677 ~ 1.713초**였다.
PC가 60장을 올리는 동안 서버는 8장만 처리했다(**0.6 fps**).

**근거**: 함수 자신의 주석이 "원자적 교체를 쓰면 사실 불필요"라고 적고 있고,
`pano_receiver`는 실제로 원자적 교체를 쓴다. 기본값이 `False`라 **사진 경로에는
영향이 없다.**

**대안** — 서버를 건드리지 않겠다면 PC에서 `--rotate 8`을 쓴다. 폴더 8개를 돌려쓰면
각 파일이 한 번만 쓰이고 안 바뀌므로 `wait_stable`이 0.3초에 통과한다.
`0.3 + 0.08 + 0.1 = 0.48초` → **약 2fps**. 5fps가 필요하면 위 수정이 맞다.

### 확인 방법 — `grep -c`로 세면 안 된다

argparse는 하이픈(`--no-wait-stable`), 코드는 밑줄(`args.no_wait_stable`)이라
**표기가 서로 다르다.** `grep -c no_wait_stable`은 1이 나오는 게 정상이다.

```bash
grep -n -- "no.wait.stable" ~/capstone-db/docx2026/anchor360_live.py
# → 두 줄이 나와야 한다
```

### 그 외 서버 파일

| 파일 | 상태 |
|---|---|
| `e2p.py` | 변경 없음. `anchor360_live.py`가 내부에서 메모리 슬라이스로 대체 |
| `relay_loop.py` | 변경 없음. 정렬 전용, 실시간 갱신용 아님 |
| `detect_ground_3b1.py` / `detect_send_3b2*.py` | 변경 없음. 눈 확인·폴백용 |
| MASt3R 계열 | 논문용. 이 파이프라인에 없음 |

## 3.3 Unity 측 — **수정하지 않음**

`Anchor360ObstacleReceiver.cs`는 v1 §3.4 그대로다. UDP 5010, 스냅샷 방식,
`yawOffsetDeg`/`invertX`/`distanceScale`/`floorWorldY`.

**스냅샷 방식이 영상 전환에서 갖는 의미**: 패킷마다 기존 큐브를 전멸시키고 재생성한다.
초당 4회 이상 보내면 깜빡임이 보이기 시작한다. 그래서 브릿지 `--poll`로 유니티 송출률을
서버 처리율과 **분리**한다(§4.5). 팀원 코드 무수정 원칙이 그대로 유지된다.

---

# 4. 확정 파라미터

## 4.1 카메라 기하 — v1과 동일

| 항목 | 값 | 비고 |
|---|---|---|
| 카메라 높이 `CAM_H` | **1.85 m** | Unity Anchor360 Position Y와 일치해야 함 |
| 바닥계산 하향각 `PITCH` | **−35°** | **슬라이스 각도와 분리됨**(§4.3) |
| 슬라이스 FOV | 90° × 90° | |
| 슬라이스 해상도 | 720 × 720 | **절대 건드리지 말 것** |
| 합성 핀홀 | `fx = fy = 360`, `cx = cy = 360` | `W/(2·tan45°)` |
| 좌표 규약 | +x 오른쪽, +y 위(바닥 y=0), +z 슬라이스 정면 | |

**720px 고정이 중요한 이유가 하나 더 생겼다.** `e2p`의 출력 크기는 입력 equirect
해상도와 무관하다. 즉 **입력이 ProStitcher 것이든 인카메라 것이든 슬라이스는 항상
720×720이고, 기하 전제(`fx=fy=360`)가 그대로 성립한다.** 달라지는 것은 픽셀의
선명도뿐이다(§9.1). 이것이 스티처를 바꿔도 좌표계를 다시 잡지 않아도 되는 근거다.

## 4.2 슬라이스 각도 — **사진 방식과 정확히 180도 다르다**

| 대응 영역 | 사진 (ProStitcher) | **영상 (인카메라)** |
|---|---|---|
| 왼쪽 복도 | u+50 | **u+230** |
| 중앙 사각지대 | u+150 | **u+330** |
| 오른쪽 복도 | u+230 | **u+50** |

**두 스티처가 `u=0`을 서로 반대 방향에 놓는다.** 렌즈 배치를 어느 쪽으로 펼치느냐의
규약 차이일 뿐이고, 측정값이 정확히 180.00도로 떨어지는 것이 그 증거다(§7.2).

**이 값을 안 바꾸면** 왼쪽 복도를 보려는데 오른쪽 사무공간이 잡힌다. 실제로 처음
비교했을 때 검출 물체 목록이 하나도 안 겹쳤고, 한동안 "스티칭 품질 차이"로 오진했다(§11).

## 4.3 피치 보정 — `--v -36.35 --pitch -35`

**인카메라 스티칭이 장면을 ProStitcher보다 1.35도 아래에 놓는다.**

| 인자 | 값 | 역할 |
|---|---|---|
| `--v` | **−36.35** | 슬라이스를 자르는 고저각. 1.35도 더 내려 자른다 |
| `--pitch` | **−35** | 바닥평면 계산용 하향각. **물리값이므로 그대로 둔다** |

`anchor360_live.py`의 `--pitch` 기본값이 `--v`와 같게 되어 있어서, 이 둘을 **반드시
따로 줘야 한다.** `--v`만 바꾸면 바닥 계산까지 틀어진다.

기동 로그에서 이 두 줄이 다르게 찍혀야 한다.
```
  슬라이스   : u=+230.0deg, v=-36.4deg, 720x720
  카메라     : 높이 1.85m, 하향 -35.0deg
```

**효과**: ProStitcher 대비 좌표 오차 **22.9cm → 9.6cm** (§7.4).

**실측 재현성**: 두 세션에서 −1.30° / −1.40°. 편차 0.1도는 D로 환산하면 **4mm** 수준이라
무시할 수 있다. 롤 편차는 **0.00°**로, 보정 상수는 2개(u, v)면 충분하다.

**어느 쪽이 참인가는 아직 모른다.** 우리는 ProStitcher를 기준으로 놓고 맞췄다.
논문의 11.2cm가 그 구성에서 줄자로 검증된 값이기 때문이다. 다만 안정화 실험(§7.6)을
보면 **인카메라 쪽이 더 정확히 수평일 가능성**도 있다. 그렇다면 현행 파이프라인에
1.35도 기울기가 들어가 있다는 뜻이 된다. 확인하려면 줄자 실측이 필요하다(§13 B-3).

## 4.4 `yawOffsetDeg` — **미결 유지**

Unity Anchor360 Inspector 값. 2026-09-15 기준 **−50** 근처.

**영상 전환은 이 문제를 건드리지 않는다.** 물리적으로 같은 방향을 자르면 `e2p` 출력
이미지가 같고, 따라서 슬라이스 로컬 좌표계도 같고, Unity로 가는 `pos3d`의 의미도 같다.
**u 번호만 바뀔 뿐 `yawOffsetDeg`는 그대로다.** 실제로 −50으로 두고 영상 경로를
돌렸을 때 큐브가 정상적으로 갱신됐다.

원인 규명은 §8 참조. v1 §8의 분석이 그대로 유효하다.

## 4.5 스트림 설정

| 항목 | 값 | 근거 |
|---|---|---|
| `mode` | `pano` | 스티칭 출력 |
| `mime` | `h264` | |
| 해상도 | `3840 × 1920` | ProStitcher 출력과 동일 규격 |
| `framerate`(요청) | `30` | **5를 요청하면 `-411 invalid message param`**(§7.5) |
| `bitrate` | **`10240`** kbps | 올려도 선명도 이득이 거의 없고 fps만 깎인다(§7.5) |
| `stabilization` | **`true`** | 끄면 정합이 무너진다(§7.6) |
| 목표 업로드 fps | `5` | 실측 4.31 (§6.3) |
| JPEG 품질 | `92` | 5fps가 필요하면 85로 낮출 것 |

**송출률 분리**: 서버는 4.3fps로 검출하고, 브릿지 `--poll 0.3`이 유니티로 2.29Hz만
내보낸다. Unity 수신기가 스냅샷 방식이라 이 분리가 필요하다(§3.3).

## 4.6 검출 설정 (config) — v1과 동일

경로: `/home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml`
**소유자 `jny`, 권한 777** — 팀원 공유 파일이라 언제든 바뀔 수 있다.

```yaml
models.zeroshot_detector.weight: yolov8s-world.pt
models.zeroshot_detector.conf:   0.12
fusion.iou:                      0.45
classes.zeroshot:                59개
```

주요 `class_conf`: `person 0.35 | chair 0.22 | table 0.22 | box 0.12 |
cardboard box 0.15 | storage box 0.15 | fire extinguisher 0.12 |
robot / quadruped robot / robot dog 0.25`

## 4.7 포트 정리

| 포트 | 용도 |
|---|---|
| 7864 | `server_stream_depth_v2` (GO1 스트리밍) |
| 7865 | 로봇 B 점유 |
| **7866** | `pano_receiver` (Pro2 수신 + JSON 제공) |
| 5009 | `Go1ObstacleJsonReceiver` 기본값 |
| **5010** | `Anchor360ObstacleReceiver` |
| 5011 | `Go1ObstacleJsonReceiver` 실제 운용 |
| 15100/15101/15103/15104 | UTM 로봇 제어 (별개 계통) |
| 20000 | Pro2 명령 API |
| 8000 | Pro2 파일 다운로드 — **영상 경로 미사용** |
| **1935** | **Pro2 내장 RTMP 서버** — 신규 |

---

# 5. 구동 절차

**실행 절차서는 `260921 카메라 최종정리2.txt`가 정본이다.** 여기서는 요약만 둔다.

터미널 4개 + Unity 1개. 서버 2 / PC 2.

```
0. 최초 1회 : pip install opencv-python
              anchor360_live.py 에 --no-wait-stable 2줄 추가 (§3.2)

1. 카메라   : 전원 → 송신기 15초 → 수신기 파란불
              ※ pro2_auto_bg / preview_probe 창을 전부 닫을 것 (§2.4)

2-1 서버    : pano_receiver.py            (오래 켤 거면 nohup)
2-2 서버    : anchor360_live.py --u 230 --v -36.35 --pitch -35
                --poll 0.1 --no-wait-stable --drop-clipped --max-dist 8 --debug-img

3-1 PC      : anchor360_bridge.py --unity-ip 192.168.50.244 --poll 0.3
3-2 PC      : stream_to_server.py --fps 5

4. Unity    : 재생 → Anchor360 → Yaw Offset Deg = -50
```

**순서 주의**: `anchor360_live.py`는 기동 시 "기존 최신 사진은 건너뜁니다"를 하므로
`stream_to_server.py`보다 먼저 띄운다. 다만 영상에서는 5fps로 계속 들어오므로
사진 방식만큼 치명적이지 않다(한 장이 아니라 0.2초를 놓칠 뿐).

**종료는 역순**: 스트림 → 브릿지 → live → Unity 정지 → receiver → 카메라 전원.

---

# 6. 실측 성능

## 6.1 사진 방식이 실패한 이유 — stitch 큐 적체

**62장 연속 운전 실측 (2026-09-21)**

| 구간 | 실측 | v1 §6 기록 |
|---|---|---|
| 촬영계열 | 중앙 **20.6초** (13.5~30.3) | 16.6~19.9초 |
| **stitch** | 중앙 **52.2초**, 평균 56.9, 최대 81.9, 최소 42.8 | **15.1~19.1초** |
| 서버 도착 | **24건** | — |

**촬영은 20.6초마다 들어오는데 stitch가 52.2초 걸린다. 매 사이클 31.6초씩 밀린다.**

```
촬영 #1   큐=0      촬영 #33  큐=20
촬영 #9   큐=4      촬영 #41  큐=25
촬영 #17  큐=9      촬영 #49  큐=30
촬영 #25  큐=14     촬영 #57  큐=35
                    촬영 #62  큐=38
```

적체 38장 × 20.6초 = **약 13분.** 유니티가 보던 것은 13분 전 장면이었다.

브릿지 로그가 이를 확인한다. `NEW` 갱신 간격이 47~77초, 평균 약 58초로
**stitch 시간에 묶여 있었다.** 촬영 주기 20초와 무관했다.

> **이것은 "느려진 것"이 아니라 구조적으로 발산하는 상태다.**
> stitch가 촬영보다 느린 한 지연은 시간이 갈수록 무한히 커진다.
>
> stitch가 v1 기록의 3배로 느려진 원인은 **규명하지 않았다.** 영상 경로가 이 단계를
> 통째로 없애므로 규명할 필요가 없어졌다. (v1 §13 C-2 항목이 이렇게 해소됐다.)

## 6.2 영상 방식 — 2분 연속 운전 실측

**조건**: `--fps 5`, `--quality 92`, `--bitrate 10240`, 142.6초, `--no-wait-stable` 적용

| 계측 | 값 |
|---|---|
| **업로드** | **614장 / 142.6초 = 4.31 fps** |
| 업로드 실패 | **0건** |
| 리더 스레드 수신 | **1909프레임 = 13.4 fps**, read 실패 **0** |
| 버린 프레임 | 1294장 (목표 fps로 솎아낸 정상 동작) |
| **서버 처리** | **613장** |
| 브릿지 신규 JSON | **399건 / 174초 = 2.29 Hz** |
| 업로드 파일 크기 | 780~787 KB |

## 6.3 구간별 시간 분해

| 구간 | 중앙 | p90 | 최대 |
|---|---|---|---|
| 프레임 나이 | **38 ms** | 82 | 260 |
| JPEG 인코딩 | **137 ms** | 207 | 480 |
| 업로드 | **49 ms** | 58 | 166 |
| **PC 구간 합계** | **228 ms** | 323 | 811 |
| 서버 `read` | 0.020 s | 0.030 | 0.040 |
| 서버 `slice` | 0.010 s | 0.010 | 0.020 |
| 서버 `infer` | 0.010 s | 0.010 | 0.010 |
| **서버 총계** | **0.050 s** | 0.070 | 0.080 |

**4.31 fps인 이유는 JPEG 인코딩이다.** PC 구간 228ms 중 137ms(60%)를 차지하고,
목표 주기 200ms를 넘어서 4.31로 수렴한다.

**서버는 놀고 있다.** 총 0.050초면 이론상 **20 fps 처리 가능**한데 4.3 fps만 들어온다.
**4.6배 여유**다. 이것이 §13 A(모델 업그레이드)의 예산 근거다.

## 6.4 발산 여부 — 2분 동안 평평했다

2분을 5구간으로 쪼갠 중앙값이다.

| 구간 | 프레임 나이 | JPEG 인코딩 | 업로드 | PC 구간 | 이론 상한 |
|---|---|---|---|---|---|
| 1/5 | 29 ms | 92 ms | 43 ms | **167 ms** | 5.99 fps |
| 2/5 | 46 | 138 | 50 | 236 | 4.25 |
| 3/5 | 44 | 142 | 49 | 242 | 4.12 |
| 4/5 | 44 | 156 | 50 | 258 | 3.88 |
| 5/5 | 35 | 148 | 49 | **244 ms** | 4.10 |

**첫 30초에 한 번 올라간 뒤 평평하다.** 236 → 242 → 258 → 244는 증가 추세가 아니라
진동이다. 워밍업으로 보인다(원인 미규명, §13 C-1).

**프레임 나이가 29~46ms로 고정된 것이 결정적이다.** 버퍼에 적체가 생기면 이 값이
계속 커진다. 안 커졌다는 것은 리더 스레드가 오래된 프레임을 제때 버리고 있다는
직접 증거다. 사진 방식의 큐가 0→38로 발산한 것과 구조가 다르다.

> **2분은 검증된 구간이다. 10분·1시간은 아직 미검증이다**(§13 C-2).

## 6.5 종단 지연

| 구간 | 중앙 | 최악 |
|---|---|---|
| PC 구간 | 228 ms | 811 ms |
| 서버 감시 주기(`--poll 0.1`) | ~50 ms | 100 ms |
| 서버 처리 | 50 ms | 80 ms |
| 브릿지 주기(`--poll 0.3`) | ~150 ms | 300 ms |
| **합계** | **약 0.5초** | **약 1.3초** |

브릿지가 관측한 JSON `ts` 간격은 중앙 **0.371초**, p90 0.504초.
서버는 0.23초마다 쓰는데 브릿지가 0.3초마다 보므로 일부를 건너뛴다. 정상이다.

> 383개 간격 중 **한 번 4.351초**가 있었다. 1/383이므로 무시 가능하나 기록해둔다.
> 초기 구간의 일시적 지연으로 추정된다.

---

# 7. 검증 실험 기록

## 7.1 인카메라 스티칭이 equirect를 내보내는가 — 확인

`camera._startPreview`에 `stiching: {mode: "pano", width: 3840, height: 1920}`를 주고
받은 프레임을 직접 열었다.

```
[계측] 첫 프레임 3840x1920  종횡비 2.00  ← equirect(2:1) 맞음
```

육안으로도 연구실 전체가 펼쳐진 정상 equirect였고, **ProStitcher 출력과 규격이 같다**
(`STITCH_CFG = {"width": 3840, "height": 1920, ...}`). `e2p.py`가 그대로 붙는다.

콜드스타트(스트림 열기)는 1.90 ~ 4.99초.

## 7.2 yaw 규약 차이 — 정확히 180.00도

**방법**: ProStitcher의 `u+50 / v-35` 슬라이스를 기준으로 두고, 인카메라 equirect를
u=0~358까지 2도 간격으로 잘라 정규화 상호상관(NCC)으로 맞췄다.

```
상위 5 (2도 간격):
  u=+230.0  NCC=0.8266   ← 피크
  u=+228.0  NCC=0.7931
  u=+232.0  NCC=0.7704

미세조정 (0.25도):
  u=+230.00  NCC=0.8266
```

**0.25도 해상도에서 정확히 230.00.** `u+230`으로 다시 잘라 눈으로 확인한 결과
포스터·책장·사람·복합기·소화기·바닥 ㄱ자 테이프 마커까지 기준과 같은 프레이밍이었다.

**독립 측정 3회 전부 편차 0.00~0.10도**였다(§7.3 표). 드리프트가 아니라 규약 차이다.

## 7.3 피치 −1.35도 · 롤 0도 — 3축 정밀 적합

`py360convert.e2p`의 `in_rot_deg`(롤)까지 포함해 3축으로 탐색했다.

| | 쌍 1 (시간차 7.6분) | 쌍 2 (시간차 **57초**) |
|---|---|---|
| **u 편차** | −0.00° | −0.10° |
| **v 편차** | **−1.30°** | **−1.40°** |
| **롤 편차** | **0.00°** | **0.00°** |
| NCC | 0.8645 | 0.8803 |

**롤 차이가 0이므로 보정 상수는 2개면 충분하다.** v 편차의 세션 간 0.1도 차이는
D로 환산해 4mm 수준이다.

## 7.4 좌표 정확도 대조 — 22.9cm → 9.6cm

같은 장면을 두 경로로 찍어 물체별 `pos3d`를 대조했다.

**① 보정 없음 (`--u 230 --v -35`)**

| 물체 | 인카메라 | ProStitcher | 3D 오차 |
|---|---|---|---|
| chair1 | (−0.18, 0.57) | (−0.20, 0.72) | 0.15 |
| chair2 | (−0.91, 0.65) | (−0.98, 0.77) | 0.14 |
| chair3 | (−1.57, 1.82) | (−1.72, 1.97) | 0.21 |
| fire extinguisher | (+1.15, 0.65) | (+1.30, 0.73) | 0.17 |
| 먼 박스 | (−1.49, 4.78) | (−1.56, 5.25) | 0.48 |
| | | **평균** | **22.9 cm** |

**② 보정 적용 (`--v -36.35 --pitch -35`), 시간차 57초**

| 물체 | 인카메라 | ProStitcher | 3D 오차 |
|---|---|---|---|
| chair1 | (−0.19, 0.62) | (−0.20, 0.72) | 0.10 |
| chair2 | (−0.93, 0.70) | (−0.99, 0.77) | 0.09 |
| chair3 | (−1.64, 1.91) | (−1.70, 1.96) | **0.08** |
| fire extinguisher | (+1.18, 0.70) | (+1.30, 0.73) | 0.12 |
| cardboard box | (−1.60, 5.13) | (−1.57, 5.26) | 0.13 |
| box | (+2.19, 5.95) | (+2.19, 6.00) | **0.05** |
| | | **평균** | **9.6 cm** |

**9.6cm는 논문의 앵커 검출 정확도 평균 11.2cm보다도 작다.** 즉 두 스티처의 차이가
파이프라인 자체의 측정 오차와 같은 수준까지 내려왔다.

검출 구조도 일치했다: 보정 후 **전송 7개 + 제외 1개**로 기준과 동일. 먼 박스의 분류까지
`box D=5.00` → `cardboard box D=5.32`로 바뀌어 기준의 `cardboard box D=5.48`에 붙었다.

## 7.5 비트레이트 — 올리면 선명도 대신 fps를 잃는다

| | 라플라시안 분산 | 고주파 비율 | 달성 fps |
|---|---|---|---|
| 인카메라 10 Mbps | 190.5 | 0.2737 | **15.1** |
| 인카메라 40 Mbps | 208.2 (**+9.3%**) | 0.2760 (+0.8%) | **5.4** |
| ProStitcher 기준 | 302.2 / 318.3 | 0.3567 | — |

**4배를 부어 선명도는 9% 오르고 fps는 3분의 1이 됐다.** 검출 conf는 평균 0.354 → 0.360
(+1.7%, 측정 노이즈 수준)으로 **변화 없음**.

**부수 소득 — 병목이 Farsight임이 확정됐다.** 비트레이트를 올렸을 때 fps가 떨어졌다는
사실 자체가 링크 한계를 가리킨다. PC 디코드가 한계였다면 이렇게 안 떨어진다.

| 요청 비트레이트 | 달성 fps | 실효 처리량 |
|---|---|---|
| 10,240 kbps | 15.1 | 5.15 Mbps |
| 10,240 kbps | 14.5 | 4.95 Mbps |
| 10,240 kbps | 13.4 (2분 실측) | 4.57 Mbps |
| 40,960 kbps | 5.4 | 7.37 Mbps |

**Farsight 실효 천장은 약 5~7 Mbps**다. 스펙의 "1080p 30fps 실시간 프리뷰"와 맞아떨어진다.
**비트레이트 × fps가 거의 일정하다.** 공짜 점심이 없다.

**`framerate: 5` 요청은 거부된다** — `-411 invalid message param`.
(`framerate`와 `bitrate`를 동시에 바꿔서 어느 쪽이 원인인지는 미분리. 서버에서
솎아내면 되므로 규명하지 않았다.)

## 7.6 자이로 안정화 — 끄면 안 된다

**가설**: ProStitcher가 자이로 비활성으로 스티칭하니(v1 §12 `gyro.mp4 enable=0`),
프리뷰도 끄면 두 경로의 피치가 맞을 것이다.

**결과 — 가설은 반증됐다. 반대 방향으로.**

| | u 편차 | v 편차 | NCC |
|---|---|---|---|
| 안정화 **ON** | −0.10° | −1.40° | **0.8803** |
| 안정화 **OFF** | +5.50° | +0.50° | **0.3879** |

정합이 0.88에서 0.39로 붕괴했다. **ProStitcher의 출력이 오히려 "안정화된 프레임"에
가깝다.** 안정화를 끄면 카메라의 물리적 거치 기울기가 드러나면서 롤까지 틀어진다.

> **결론: `stabilization: true` 유지. `--no-stab`은 쓰지 않는다.**
> (OFF 상태의 3축 정밀 측정은 계산 시간 초과로 미완. 실무 결론에는 영향 없음.)

## 7.7 검출 재현성 — 87프레임 연속

| 물체 | 건수 | D 중앙 | **표준편차** | 범위 | conf 중앙 |
|---|---|---|---|---|---|
| person | 87 | 3.08 | **2.6 cm** | 2.95~3.10 | 0.60 |
| cardboard box | 67 | 2.31 | **0.1 cm** | 2.31~2.32 | 0.20 |
| storage box | 39 | 2.00 | 14.8 cm | 1.98~2.32 | 0.16 |
| box | 52 | 5.48 | 12.2 cm | 4.61~5.54 | 0.13 |
| chair | 260 | — | — | (의자 3개 혼재) | 0.52 |

**정적 물체가 mm~cm 단위로 고정된다.** v1 §7.2의 "정적 물체는 센티미터 단위로 고정"과
일치한다. 87프레임 전부 `person`을 놓치지 않았다(conf 0.90~0.92까지 나온 구간도 있음).

제외 사유: `접지잘림` 160건, `>8m` 81건. 둘 다 의도한 필터가 작동한 것이다.

---

# 8. 미해결 — `yawOffsetDeg` 불일치 (v1 §8 승계)

**영상 전환은 이 문제를 해결하지도, 악화시키지도 않았다.** v1 §8의 분석이 그대로
유효하므로 여기서는 요지와 "영상 전환이 무엇을 바꿨고 무엇을 안 바꿨는가"만 정리한다.

## 8.1 현상

과거 `-35`로 맞았던 u+50 슬라이스가 2026-09-15에는 `-45 ~ -65`(그중 `-50`이 최적)를
줘야 현실과 근사했다. 카메라 구도는 거의 동일했다.

## 8.2 과거 기록 3값의 내부 모순

`yaw = -u + C` 형태로 **하나의 공통 상수 C**로 설명돼야 한다.

| 슬라이스 | u | 기록 yaw | 역산 C |
|---|---|---|---|
| 왼쪽 | 50 | −35 | **+15** |
| 가운데 | 150 | +150 | **−60** |
| 오른쪽 | 230 | −45 | **−175** |

**C가 190도 범위로 흩어진다. 하나의 카메라 방향에서 나올 수 없다.**
세 값은 각 슬라이스를 따로따로 눈대중으로 맞춘 결과이며, 카메라 방향에서 유도된 값이 아니다.

## 8.3 이론값과 가설 현황

`R_anchor = 0`이면 `yaw = -u`. u=50의 이론값이 **−50**이고, 2026-09-15에 가장 잘 맞았던
값이 정확히 −50이었다. 즉 질문은 "왜 −35가 안 되나"가 아니라 **"−35가 애초에 맞았나"**다.

| # | 가설 | 상태 |
|---|---|---|
| 1 | 앵커 Position이 실제 카메라 위치와 어긋남 | **배제** (줄자 확인, Y=1.85) |
| 2 | 앵커 Rotation Y ≠ 0 | **미확인** ← 최우선 |
| 3 | 과거 −35가 애초에 부정확 | **가능성 높음** |
| 4 | 기준 물체 빈약으로 회전이 결정 안 됨 | **관측됨** (−45~−65로 20도 흔들림) |
| 5 | 카메라 물리 각도 변화 | **배제** (픽셀 대조 1~2도 이내) |
| 6 | `live.py` 계산 오차 | **배제** (최대 7cm, 방향 무관) |
| **7** | **인카메라/ProStitcher 수평 기준 차이(1.35도)** | **신규 · 미확인** ← §8.5 |

## 8.4 영상 전환이 이 문제에 주는 영향 — 없다

`e2p`가 만드는 슬라이스는 **입력 equirect가 무엇이든 같은 물리 방향을 보면 같은 이미지**다.
따라서 `pixel_to_ground`가 내는 `pos3d`도 같고, Unity가 그것을 세계 좌표로 옮기는
`world = R_anchor × (Yaw(yawOffsetDeg) × local) + P_anchor`도 그대로다.

**u 번호만 180도 바뀌고 `yawOffsetDeg`는 안 바뀐다.** 실제로 −50으로 두고 영상 경로를
돌렸을 때 큐브가 정상 갱신됐다. 다만 v1에서 지적된 "일부 물체의 방향 오차"는
**그대로 남아 있다** — 복사기(`storage box`), 소화기 등이 실제 위치와 어긋난다.

## 8.5 신규 가설 7 — 수평 기준 차이

§7.6에서 나온 사실: **인카메라 스티칭이 ProStitcher보다 더 정확히 수평일 가능성이 있다.**
안정화를 끄면 정합이 0.88 → 0.39로 붕괴하는데, 이는 ProStitcher 출력이 "자이로로
수평 보정된 프레임"에 가깝다는 뜻이다. 그런데 두 경로 사이엔 여전히 1.35도가 남는다.

**만약 인카메라 쪽이 참 수평이라면**, 현행 파이프라인(ProStitcher 기준)에 1.35도
기울기가 계속 들어가 있었다는 뜻이고, 이것이 `yawOffsetDeg`를 눈대중으로 맞출 때
보상 대상으로 끌려들어갔을 수 있다.

**확인 방법**: 줄자로 실측한 물체 2개를 시야에 두고, 인카메라 경로 / ProStitcher 경로
각각에서 D를 재어 GT와 비교한다. 어느 쪽이 GT에 가까운지가 답이다.

## 8.6 근본 해결 방향 — 계산으로 유도 (v1 §8.7 그대로)

대응점 **1개**면 회전이 풀린다.
```
yaw = atan2(맵에서 앵커→물체 방향) − atan2(슬라이스 로컬 x, z)
```
**2개 이상**이면 최소제곱으로 풀리고 **스케일 오차까지 같이 나온다.** 잔차가 크면
"yaw 문제가 아니라 거리 문제"라는 것이 드러난다 — 눈대중으로는 절대 갈라지지 않는 구분이다.

**결정적 실험(v1 §8.8) — 합성 JSON 2점 프로브**는 여전히 유효하고, **영상 전환 후
오히려 하기 쉬워졌다.** `relay_loop.py`로 고정 JSON을 쏘면서 Inspector 다이얼을 돌리면
되는데, 이제 실시간 피드백이 5fps로 들어오므로 실제 장면으로도 즉시 확인이 가능하다.
사진 방식에서는 20초를 기다려야 했다.

---

# 9. 검출 품질 관찰

## 9.1 선명도 격차 — ProStitcher 대비 0.65배

| | 라플라시안 분산 | 고주파 비율 | 대비(std) |
|---|---|---|---|
| 인카메라 (H.264 10Mbps) | **190.5** | 0.2737 | 52.0 |
| ProStitcher (JPEG) | **302.2 / 318.3** | 0.3567 | 48.9 |

**ProStitcher가 1.6배 선명하다.** 포스터 글씨, 바닥 테이프 마커, 사람 윤곽이 더 또렷하다.

**원인은 압축이 아니다.** 비트레이트를 4배로 올려도 +9.3%에 그쳤다(§7.5).
30fps를 임베디드에서 내야 하니 **카메라가 내부적으로 낮은 해상도로 합성한 뒤
3840×1920으로 올리는 것**으로 보인다.

압축 예산 비교: ProStitcher equirect 1장 ≈ 3,200KB ≈ 26 Mbit/프레임,
H.264 10Mbps @30fps = 0.33 Mbit/프레임. **약 78배 차이**인데 선명도는 1.6배 차이에
그친다는 것이 "압축이 원인이 아니다"의 방증이다.

**→ 대응은 검출 모델 업그레이드 쪽이다**(§13 A).

## 9.2 임계값 경계 깜빡임 — v1 §9.3 그대로

`box 0.12`, `storage box 0.15` 등 임계값에 걸친 클래스는 프레임마다 들어왔다 나갔다 한다.
스냅샷 방식이라 큐브가 생겼다 사라지는 것으로 보인다.

영상 전환으로 **빈도가 올라갔다** — 초당 4~5회 판정하므로 사진 방식(20초에 1회)보다
깜빡임이 눈에 띈다. 다만 **브릿지 `--poll 0.3`이 샘플링하므로 유니티에서는 완화된다.**

conf가 전반적으로 낮은 것(§9.1의 결과)이 이 문제를 키운다.

## 9.3 AGENT 클래스 — 위험 구간

정적 장애물은 안전하다. 임계값이 0.12~0.22인데 conf가 0.3~0.65로 나온다.
**소화기도 영상 경로에서 conf 0.25~0.45로 검출된다** (v1 §9.1의 미검출 우려는
이번엔 나타나지 않았다).

문제는 임계값이 높은 클래스다.

| 클래스 | 임계값 | 관찰 |
|---|---|---|
| `person` | 0.35 | 정지 프레임 대조에서 **3회 중 2회 놓침**. 다만 연속 스트림 87프레임에서는 **전부 검출**(conf 0.60~0.92) |
| `robot` / `quadruped robot` / `robot dog` | 0.25 | ⚠️ **미검증. GO1을 못 잡으면 치명적** |

`person`이 정지 대조에서는 놓치고 연속 스트림에서는 다 잡힌 이유는, 그 사람이 스트림
실험에서 더 가깝고(D=2.5~3.1) 자세가 좋았기 때문으로 보인다. **거리·자세에 따라 경계에
걸린다는 뜻이고, 안정적이라고 단정할 수 없다.**

**로봇 검출은 반드시 확인해야 한다**(§13 A-0). v1 §10.1의 MASt3R Plan B —
"360°가 로봇 자체를 검출하고 `pixel_to_ground`로 앵커 좌표를 잡는다" — 가 이 검출에
의존하기 때문이다.

---

# 10. 폐기·비적용 항목

## 10.1 ProStitcher 경로 — 영상 파이프라인에서 폐기, 단 보존

`pro2_auto_bg.py` / `stitch_prostitcher.py` / TRT 캐시 / `captures/` 는 영상 경로에서
쓰지 않는다. **그러나 삭제하지 않는다.**

- **논문(AsiaSim 2026)의 측정 기준 구성**이다. 앵커 검출 11.2cm는 이 구성의 값이다.
- 영상 경로에 문제가 생겼을 때의 폴백이다.
- 두 경로를 대조할 때 기준으로 쓴다(§7.4의 방식).

**동시 실행은 불가능하다**(§2.4). 번갈아 돌려야 한다.

## 10.2 유선 직결 — 선택지에서 제외 (2026-09-21 결정)

Pro2는 유선 직결 모드를 지원한다(카메라 `192.168.1.188`, PC `192.168.1.100/24`).
Farsight의 5~7Mbps 천장이 사라지므로 40Mbps를 30fps로 받을 수 있게 된다.

**그러나 선택지에서 제외한다.** 근거:
- 선명도 격차의 원인이 링크가 아니라 인카메라 스티처였다(§7.5). 대역을 늘려도
  선명도는 거의 안 오른다.
- 현재 4.31 fps는 **PC의 JPEG 인코딩**이 정하고 있지 링크가 정하고 있지 않다(§6.3).
- 실측에서 read 실패 0건. 링크가 불안정하다는 징후가 없다.

얻을 것이 fps 여유뿐인데 그 여유가 지금 병목이 아니다.

## 10.3 MASt3R 자세추출 — 완전 폐기 (v1 §10.1 그대로)

`sparse_global_alignment` 기반 좌표 정합은 이 셋업에서 비가용으로 결론났다.
근본 원인은 어안-핀홀 모델 불일치. 현재 MASt3R는 **"매칭 판별자"로만** 쓰이며
**이 파이프라인에는 포함되지 않는다.**

360°는 자기 자신이 절대 기준이므로 정합이 필요 없다. Unity로 가는 장애물 JSON은
zeroshot + `pixel_to_ground`만 쓴다.

## 10.4 세 슬라이스 통합 — 보류 유지 (v1 §10.2 그대로)

현재 u+230 한 장만 운용한다. 커버 범위는 90°(360°의 1/4).

**영상 전환으로 시간 예산은 더 넉넉해졌다** — 서버 총 0.050초 중 `slice`가 0.010초이므로
3장을 해도 0.07초다. 20fps 처리 능력 대비 여유가 크다.

**그러나 §8이 먼저 풀려야 한다.** 슬라이스가 하나면 `yawOffsetDeg` 하나만 맞추면 되지만,
셋이면 회전 합성이 필요하고 그 오프셋을 계산으로 유도해야 한다. 지금처럼 눈대중이면
v1 §8.2의 모순된 3값이 재현된다.

## 10.5 비트레이트 상향 — 비권장

§7.5 참조. 4배를 부어 선명도 +9.3%, 검출 변화 없음, fps 3분의 1.

---

# 11. 트러블슈팅 이력 (2026-09-21)

| # | 증상 | 진단 | 해결 |
|---|---|---|---|
| 1 | bash 문법 curl이 cmd에서 줄마다 실행됨 | `\` 줄이음 / 작은따옴표가 cmd에서 안 먹음 | 한 줄 + `\"` 이스케이프 |
| 2 | `disabledCommand / camera not connected` | **하트비트 없이 curl 두 줄**. 세션 만료 | 하트비트를 도는 스크립트로 전환 |
| 3 | `already connected by another` | 다른 스크립트 창이 세션 점유 | 창 정리. Pro2는 제어 세션 1개(§2.4) |
| 4 | `rtmp://127.0.0.1/live/preview`로 열기 실패 | **카메라 자기 자신 기준 주소** | 호스트를 카메라 IP로 치환(§2.3) |
| 5 | 두 경로 검출 물체가 하나도 안 겹침 | **u 규약 180도 차이** | `--u 230`(§7.2) |
| 6 | 보정 후에도 D가 전부 12~21cm 작게 나옴 | **피치 1.35도 차이** | `--v -36.35 --pitch -35`(§7.3) |
| 7 | `framerate: 5` 거부 (`-411`) | 카메라가 안 받음 | 서버에서 솎아냄 |
| 8 | 비트레이트 40960에서 fps 15→5.4 | **Farsight 대역 천장 5~7Mbps** | 10240 유지(§7.5) |
| 9 | 안정화 OFF에서 NCC 0.88→0.39 | ProStitcher가 안정화 프레임에 가까움 | `stabilization: true` 유지(§7.6) |
| 10 | `Assertion fctx->async_lock failed` | 종료 시 `cap.release()`와 `cap.read()` 경합 | 리더 스레드가 `cap` 소유(§3.1) |
| 11 | 5fps로 올렸는데 서버는 0.6fps | **`wait_stable`이 1.5초 소비** | `--no-wait-stable`(§3.2) |
| 12 | `grep -c no_wait_stable`가 1 | argparse는 하이픈, 코드는 밑줄 | `grep -n -- "no.wait.stable"` |

## 11.1 이번에 드러난 판단 오류 (반복 방지용)

- **검출 결과가 다르면 먼저 "같은 곳을 보고 있는가"부터 확인한다.**
  §7.2를 하기 전까지 검출 물체 목록이 안 겹치는 것을 "스티칭 품질 차이"로 오진했다.
  실제로는 180도 반대를 보고 있었다. **비교 대상이 정렬돼 있는지가 모든 비교의 전제다.**

- **`grep -c`로 패치 적용 여부를 확인하는 명령을 잘못 만들었다.**
  argparse의 하이픈/밑줄 변환을 놓치고 "2가 나와야 한다"고 안내했다.
  실제로는 1이 정상이었고, 사용자가 "꼭 2여야 하냐"고 되물어서 발견됐다.
  **확인 명령을 줄 때는 그 명령을 실제로 돌려보고 기대값을 확인해야 한다.**

- **없는 사실을 만들었다.** "카메라 IP가 바뀌어서 `camera connect failed`가 났다"고
  말했으나, **이 세션 내내 IP는 `192.168.100.124` 그대로였다.** 실제 원인은 직전 창을
  닫은 직후 세션이 안 풀린 것이었다. 문서에 기록된 위험(v1 §12 "카메라 IP 변동")과
  그날 실제로 일어난 일을 섞어버린 것이다.

- **파일을 쓰기 모드로 열면서 잘못된 인자를 줘서 0바이트로 만들었다.**
  `io.open(p, "w", newline="\\n")` — 잘못된 newline 값이 `TextIOWrapper`에서 예외를
  내는데, 그 전에 **파일은 이미 truncate된다.** 이어서 넣은 `ast.parse` 검사는
  **빈 파일도 통과**하므로 "원본 무사"라고 잘못 보고했다.
  **검사는 "파싱되는가"가 아니라 "길이와 내용이 살아 있는가"까지 봐야 한다.**
  이후 모든 파일 수정에 길이·핵심문자열 존재 검사를 추가했다.

- **가설을 세울 때 근거 문서를 잘못 읽었다.** v1 §12의 `gyro.mp4 enable=0`을 보고
  "ProStitcher는 자이로를 끄고 스티칭한다"고 단정했으나, §7.6 실험이 반대를 가리켰다.
  **그 줄은 "enable=0이어도 파일이 존재해야 한다"는 뜻이지 현재 설정이 0이라는 뜻이 아니었다.**

---

# 12. 알려진 함정 모음

| 항목 | 내용 |
|---|---|
| **카메라 세션 1개** | `pro2_auto_bg` / `preview_probe` / `stream_to_server` / Insta360 Pro 앱은 **동시 사용 불가** |
| **하트비트 필수** | `/osc/state` 1초 주기가 끊기면 세션 만료. curl 두 줄로는 명령이 안 통함 |
| **`_previewUrl`은 127.0.0.1** | 카메라 자기 자신 기준. 호스트를 카메라 IP로 바꿔야 함 |
| **u 규약 180도** | 사진 u+50 = 영상 u+230. 안 바꾸면 반대편을 본다 |
| **`--v`와 `--pitch` 분리** | `--pitch` 기본값이 `--v`와 같게 되어 있음. 반드시 따로 줄 것 |
| **`--poll 0.1`** | 기본값 1.0이면 5fps로 올려도 초당 1장만 집음 |
| **`wait_stable` 1.5초** | 덮어쓰기 방식에서 프레임당 1.5초 소비. `--no-wait-stable` 또는 `--rotate 8` |
| **`--save-slice` 금지** | 4.3fps면 시간당 1.5GB |
| **RTMP 버퍼 적체** | `cap.read()`를 느리게 부르면 오래된 프레임이 나온다. 리더 스레드로 최신만 유지 |
| **`cap.release()` 경합** | 다른 스레드가 `read()` 중이면 libavcodec assertion으로 프로세스 사망 |
| **비트레이트 × fps 일정** | Farsight 천장 5~7Mbps. 비트레이트를 올리면 fps를 잃는다 |
| **`framerate: 5` 거부** | `-411`. 서버에서 솎아낼 것 |
| **안정화 끄지 말 것** | OFF면 정합 NCC 0.88 → 0.39 |
| **720px 고정** | `e2p`와 `fx/cx` 전제가 맞물림. fov 변경 시 거리 전체가 틀어짐 |
| **config 공유 파일** | `jny` 소유 777. 팀원이 바꾸면 검출이 말없이 달라짐 |
| **`UDP OK` 로그** | 서버→Unity 직송은 안 됨. 도달 증거가 아님(v1 §2.2) |
| **`pano_receiver` SSH 종속** | 세션 끊기면 죽음. 오래 켤 거면 `nohup` |
| **Unity 수신기 스냅샷** | 패킷마다 큐브 전멸/재생성. 초당 4회 이상이면 깜빡임 |
| **카메라 RTC 배터리 사망** | 전원마다 2016-02-12로 리셋 (영상 경로에서는 영향 없음) |

---

# 13. 남은 일

**파이프라인 검증은 끝났다(§1, §6). 아래는 그 위에 얹는 것들이다.**
순서에 의미가 있다 — A-0은 다른 모든 것보다 먼저다.

## A. 비전 모델 업그레이드

### A-0. 먼저 — 로봇 검출 확인 (최우선)

**어떤 업그레이드를 할지 정하기 전에, 지금 모델이 GO1을 잡는지부터 봐야 한다.**

- `robot` / `quadruped robot` / `robot dog` 임계값 0.25, **전부 미검증**
- `person`(임계값 0.35)이 정지 프레임 대조에서 3회 중 2회 빠졌다(§9.3)
- 로봇 검출은 v1 §10.1 MASt3R Plan B의 전제이기도 하다

**방법**: GO1을 앵커 시야(u+230 슬라이스)에 두고
① 영상 경로 ② ProStitcher 경로로 한 쌍을 찍어 conf와 `pos3d`를 대조한다.
§7.4와 같은 방식이다.

**판정**
- 양쪽 다 안정적으로 잡음 → A-1은 선택 사항
- **영상만 놓침 → 선명도가 원인. A-1이 선택이 아니라 필수**
- 양쪽 다 놓침 → 모델(`yolov8s-world`) 한계. A-1이 필수이고 사진 경로에도 이득

### A-1. `yolov8s-world` → `yolov8x-worldv2`

**근거 — 시간 예산이 충분하다.**

| 항목 | 현재 |
|---|---|
| 서버 총 처리 | 0.050초 (중앙) |
| 그중 추론 | **0.010초** |
| 이론 처리 능력 | 약 20 fps |
| 실제 유입 | 4.31 fps |
| **여유** | **4.6배** |

추론이 지금의 4~5배(0.04~0.05초)가 돼도 총 0.09~0.10초로 **10fps 처리가 가능하다.**
현재 유입 4.3fps를 감당하고도 남는다.

**기대 효과**
- 인카메라 스티칭의 선명도 열세(0.65배, §9.1)를 모델 성능으로 상쇄
- AGENT 클래스(`person`, `robot`)의 임계값 경계 탈락 완화(§9.3)
- v1 §9.1의 소화기 미검출 문제에도 이득 — **사진 경로에도 그대로 적용된다**

**주의 사항**
- 가중치 경로는 config(`unidepth_dual_yolo_config.yaml`)에 있고 **`jny` 소유 공유 파일**이다.
  config를 고치지 말고 `anchor360_live.py`의 **`--weight` 인자로 오버라이드**할 것.
  ```
  python3 anchor360_live.py --weight /path/to/yolov8x-worldv2.pt ...
  ```
- **CLIP 캐시**: `YOLOWorld.set_classes()`가 쓰는 ViT-B-32(338MB)는 CWD 아래
  `weights/clip/`에 캐시된다. 스크립트가 자기 디렉터리로 `chdir`하므로 문제없으나,
  다른 경로에서 띄우면 다시 받는다.
- 모델 로드 시간이 늘어난다(현재 8.5초). **상주 방식이라 1회성이므로 무관하다.**
- **바꾸기 전후로 §7.4 방식의 좌표 대조를 한 번 해둘 것.** 모델이 바뀌면 bbox가
  달라지고 `pos3d`도 미세하게 달라진다. 논문 수치와의 관계를 기록해둬야 한다.

### A-2. 대안 — 임계값 조정은 권장하지 않는다

`class_conf`를 낮추면 AGENT 탈락은 줄지만 **오검출이 같이 늘어난다.**
그리고 config는 공유 파일이라 팀원 작업에 영향을 준다(§12).
**모델을 키우는 쪽이 부작용이 적다.**

## B. `yawOffsetDeg` 원인 규명

§8 참조. 순서는 v1 §13 A와 같다.

- **B-1. Unity `Anchor360` Rotation Y 읽기** — Inspector 숫자 하나.
  `yaw = -u - R_anchor`의 미지수다. **아직 안 읽었다.**
- **B-2. 합성 JSON 2점 프로브**(v1 §8.8) — `R_anchor` 실측값 + 부호 + 스케일 확정.
  `relay_loop.py`로 고정 JSON을 쏘면서 Inspector를 돌린다.
- **B-3. 어느 스티처가 참 수평인가**(§8.5 신규) — 줄자 실측 물체 2개로 두 경로의 D를
  GT와 대조. 1.35도의 주인을 가린다. **A-0과 같이 하면 촬영 한 번으로 끝난다.**
- **B-4. 기준 물체를 갖춘 상태에서 yaw 재측정** — 소화기·상자를 1~2m 복도 바닥에,
  conf 0.5 이상 접지 선명한 것으로.
- **B-5. 결정된 값 박제** — 값 + Anchor360 Inspector 전체 스크린샷.
  v1에서 아무 데도 안 남겨서 처음부터 다시 찾았다.

> **영상 전환이 이 작업을 쉽게 만들었다.** 다이얼을 돌리면서 큐브가 움직이는 것을
> 5fps로 즉시 볼 수 있다. 사진 방식에서는 한 번 확인에 20초(실제로는 58초)를 기다렸다.

## C. 운영 안정성

- **C-1. 워밍업 구간 원인 규명** — PC 구간이 첫 30초 167ms → 이후 230ms대로
  올라간 뒤 평평하다(§6.4). JPEG 인코딩이 92ms → 140ms대로 늘어난 것이 주원인인데
  이유는 모른다(CPU 스로틀링? GC? 다른 프로세스?). **동작에 지장은 없다.**
- **C-2. 장시간 운전 검증** — **2분만 검증됐다.** 10분·1시간에서
  카메라 발열 / RTMP 끊김 후 재연결 / 메모리 증가를 봐야 한다.
  현재 `stream_to_server.py`에는 **재연결 로직이 없다** — `read` 실패가 계속되면
  그냥 멈춘다. 장시간 운전이 필요해지면 이것부터 넣어야 한다.
- **C-3. 5fps 달성** — `--quality 85`로 인코딩을 줄여볼 것. 선명도가 이미 0.65배라
  JPEG 품질 차이는 거의 영향이 없을 것으로 보인다. 재보고 기록할 것.
- **C-4. 브릿지 송출률 튜닝** — 현재 `--poll 0.3` → 2.29Hz. Unity 스냅샷 방식이라
  더 올리면 깜빡임이 보인다. 큐브 깜빡임이 거슬리면 오히려 0.5로 **낮추는** 쪽을 검토.
- **C-5. `pano_receiver.py` docstring 포트 7865 → 7866** (v1에서 이월, 미수정)

## D. 확장

- **D-1. 세 슬라이스 통합** (u+230 / u+330 / u+50 → 회전 적용 → 1패킷).
  시간 예산은 충분하다(slice 0.010초 × 3 = 0.03초).
  **B가 풀려야 회전 오프셋을 계산으로 넣을 수 있다**(§10.4).
- **D-2. 대응점 기반 yaw 자동 산출**(§8.6). 현재 시스템에서 **앵커 맵 등록이 유일한
  수동 단계**다.
- **D-3. 검출 결과 스무딩** — 임계값 경계 깜빡임(§9.2)을 프레임 간 평활로 줄이는 것.
  다만 Unity 수신기가 스냅샷 방식이라 서버 쪽에서 해야 한다.
  **검증된 수식을 건드리는 작업이므로 신중할 것.**

## E. 별개 계통 (v1에서 이월)

- **E-1.** Unity `[UTM:GO1_A/B]` 포트 15101/15104 충돌
- **E-2.** GO1 5011로 서버에서 쏘는 경로가 있다면 v1 §2.2와 같은 문제를 겪을 수 있음

---

# 14. 참고

## 14.1 관련 문서

| 문서 | 내용 |
|---|---|
| `260915_360도카메라_파이프라인_총정리.md` | **v1. 사진 방식.** 논문 기준 구성. §7~§12 상당수가 여전히 유효 |
| `260921 카메라 최종정리2.txt` | **영상 방식 실행 절차서 (정본)** |
| `260914 카메라 최종 정리.txt` | 사진 방식 실행 절차서 |
| `260712 카메라 사용법 올인원.txt` | 초기 수동 절차 |

## 14.2 이번 작업의 로그

| 파일 | 내용 |
|---|---|
| `260921 360도 카메라 실제 구동 로그.txt` | 사진 방식 62장 연속 운전. **stitch 큐 적체의 원본 증거**(§6.1) |
| `260921 360도 카메라 실제 구동 로그2.txt` | 비트레이트 / 좌표 대조 실험(§7.4, §7.5) |
| `260921 카메라 영상 로그3.txt` | 영상 파이프라인 첫 관통. `wait_stable` 병목 발견(§3.2) |
| `260921 카메라 영상 실측 로그.txt` | **2분 연속 운전 614프레임. §6의 근거** |

## 14.3 스크립트

| 파일 | 위치 | 역할 |
|---|---|---|
| `stream_to_server.py` | PC `360camera_work` | **영상 파이프라인 공급기** |
| `preview_probe.py` | PC `360camera_work` | 스트림 확인 도구 |
| `anchor360_live_2.py` | PC `360camera_work` | 서버 `anchor360_live.py`의 수정본 사본 |
| `anchor360_bridge.py` | PC `docx2026_code` | 서버 JSON → Unity UDP (변경 없음) |
| `pro2_auto_bg.py` | PC `docx2026_code` | 사진 방식. **보존** |

## 14.4 외부 참고

- Insta360 ProCameraApi — `camera._connect` / `_startPreview` / `_startLive` /
  `_stopPreview` 명세, `rtmp://{camera_ip}:1935/live/preview`
- Insta360 Pro2 사양 — 실시간 스티칭 3840×1920@30fps, Farsight 1080p30 프리뷰,
  지상 300m / 5.18GHz
- Pro2 매뉴얼 1.1.5(연결 방식) / 4.1.3(라이브 전 스티칭 캘리브레이션)

---

*문서 끝. 2026-09-21 작성.*
