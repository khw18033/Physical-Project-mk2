# 360° 카메라 관련 파일 — 전수 목록 + 폴더 보유 현황

**작성일**: 2026-09-22
**목적**: 팀원에게 "360도 카메라 관련 전부"를 넘기기 위한 체크리스트.
**점검 대상 폴더**: `C:\Users\asdfa\physical mk2\360camera_work` · `C:\Users\asdfa\docx2026_code`

**표기**
- ✅ **있음** — 폴더에 있고 현행본
- ⚠ **있음(주의)** — 있지만 사본/구버전/조건부
- ❌ **없음** — 서버나 다른 PC에만 있음. 받아와야 함
- 🔒 **불필요** — 360 경로와 무관. 참고용으로만

---

# 0. 방금 확인 — 폴더에 새로 들어온 2개

| 파일 | 상태 | 확인 결과 |
|---|---|---|
| `pano_receiver.py` (3,974 B, 09-22) | ✅ **서버 현행본 맞음** | 원자적 쓰기(`.part`→`os.replace`) **있음**, `/anchor360_latest` GET **있음**. 지난번 사본(3,464 B)은 둘 다 없던 구버전이었음 |
| `unidepth_dual_yolo_config.yaml` (6,037 B, 09-22) | ✅ **서버 현행본 맞음** | zeroshot 클래스 59개, `weight: /home/dg/.../yolov8s-world.pt` |

**그리고 이걸로 지난 분석의 §C-7 이 "추정"에서 "확정"으로 바뀜:**

| 클래스 | 260617 판 | **현행(확정)** | 결과 |
|---|---|---|---|
| `box` | 0.18 | **0.12** | conf 0.12~0.17 짜리가 새로 통과 → 5~6.5 m 유령 박스의 직접 원인 |
| `fire extinguisher` | 0.22 | **0.12** | 저신뢰 소화기 통과 |

나머지 항목(`chair 0.22`, `table 0.22`, `storage box 0.15`, `person 0.35`, `robot/quadruped robot/robot dog 0.25`, `default 0.25`)은 6월판과 동일.
⚠ 이 파일은 **서버에서는 `jny` 소유·권한 777** 이다. 팀원에게 줄 때는 **스냅샷 사본**으로 주고, 서버에서도 각자 사본을 쓰게 할 것.

---

# 1. 서버 파일 (`sysai-server2` / `~/capstone-db/`)

## 1-A. 현행 파이프라인 — 반드시 있어야 하는 것

| # | 서버 경로 | 역할 | 폴더 |
|---|---|---|---|
| S1 | `~/capstone-db/docx2026/anchor360_live.py` | **상주 데몬.** 감시→슬라이스→검출→필터→`pixel_to_ground`→JSON→UDP. **거리 계산식이 들어있는 파일** | ✅ `360camera_work` (23,625 B, `--no-wait-stable` 포함 = 현행본) |
| S2 | `~/capstone-db/image_server/pano_receiver.py` | FastAPI 수신 서버. 포트 7866. `/upload_pano`, `/pano_health`, `/anchor360_latest` | ✅ `360camera_work` (09-22 수령분) |
| S3 | `~/capstone-db/image_server/unidepth_dual_yolo_config.yaml` | 검출 설정. 가중치 경로·클래스 59개·`group_mapping`·`class_conf` | ✅ `360camera_work` (09-22 수령분) |
| S4 | `~/capstone-db/image_server/yolov8s-world.pt` | **YOLO-World 가중치.** 이게 없으면 아무것도 안 돎 | ❌ **없음** |
| S5 | `~/capstone-db/docx2026/weights/clip/ViT-B-32.pt` | CLIP 캐시 338 MB. `set_classes()` 가 씀. 없으면 실행마다 재다운로드 | ❌ **없음** |
| S6 | `~/capstone-db/venv_image/` | 서버 파이썬 환경 | ❌ **없음** (복사 방법은 배포가이드 §E-3) |

## 1-B. 구버전(수동 1세대) — 수식 대조·폴백용

| # | 서버 경로 | 역할 | 폴더 |
|---|---|---|---|
| S7 | `~/capstone-db/docx2026/e2p.py` | equirect → perspective 슬라이스 (파일 경유). 현행은 메모리로 대체 | ✅ `360camera_work` |
| S8 | `~/capstone-db/docx2026/detect_ground_3b1.py` | 검출 → 콘솔 표(X/Z/D/wy) + bbox jpg. **눈 확인용. `pixel_to_ground` 원본** | ✅ `360camera_work` |
| S9 | `~/capstone-db/docx2026/detect_send_3b2.py` | 검출 → JSON + UDP 1회 송출. **`anchor360_live.py` 의 직계 조상** | ✅ `360camera_work` |
| S10 | `~/capstone-db/docx2026/detect_send_3b2_timed.py` | 3b2 + `[TIMING]`. ⚠ `predict` 2회라 실시간 금지 | ✅ `360camera_work` |
| S11 | `~/capstone-db/docx2026/relay_loop.py` | 고정 JSON 반복 UDP. **yaw 정렬 전용.** 2점 프로브에 필요 | ✅ `360camera_work` + `docx2026_code` (동일 해시) |
| S12 | `~/capstone-db/docx2026/relay_to_unity.py` | — | 🔒 **서버에 존재하지 않음** (v1 §3.3 에서 `ls` 종료코드 2로 확인. `relay_loop.py` 로 대체됨) |

## 1-C. UniDepth 를 붙이려면 추가로 필요한 것 ★ 팀원이 요청한 작업의 핵심

| # | 서버 경로 | 역할 | 폴더 |
|---|---|---|---|
| S13 | `~/capstone-db/image_server/checkpoints/unidepth-v2-vitb14/` | **UniDepth V2 모델 본체.** 이게 없으면 시작 자체가 안 됨 | ❌ **없음** |
| S14 | `~/capstone-db/image_server/unidepth_dual_yolo.py` (현행본) | **UniDepth 호출 방법의 유일한 레퍼런스.** `UniDepthV2.from_pretrained` / `infer(rgb, camera=, normalize=)` / `extract_depth_from_outputs` / `get_object_depth` / `calibrate_depth_to_cm` | ⚠ **260617 판이 프로젝트 문서에 있음.** 서버 현행본은 ❌ |
| S15 | `~/capstone-db/image_server/server_stream_depth_v2.py` (현행본) | 포트 7864 GO1 스트리밍 서버. Redis 입출력 구조 참고 | ⚠ **260617 판이 프로젝트 문서에 있음.** 서버 현행본은 ❌ |
| S16 | **UniDepth 가 설치된 파이썬 환경** | `from unidepth.models import UniDepthV2` 가 되는 환경 | ❌ **미확인 — 반드시 먼저 확인할 것** |

> ⚠ **S16 이 제일 중요하다.** 7월 문서의 검출 명령은 전부
> `source /home/jny/miniconda3/etc/profile.d/conda.sh && conda activate unidepth` 로 시작한다.
> 즉 **UniDepth 는 `jny` 의 conda 환경에 있고**, 현행 360 경로가 쓰는 `venv_image` 에
> `unidepth` 패키지가 들어 있는지는 **확인된 바 없다**(v1 §2.3 목록에도 없음).
> 팀원이 `anchor360_live.py` 안에서 그냥 `import unidepth` 하면 **ModuleNotFoundError 가 날 가능성이 높다.**
> 먼저 이것부터:
> ```bash
> . ~/capstone-db/venv_image/bin/activate
> python -c "from unidepth.models import UniDepthV2; print('OK')"
> ```

## 1-D. 같은 config 를 공유하지만 360 과 무관한 것 (GO1 계통)

| 서버 경로 | 역할 | 폴더 |
|---|---|---|
| `~/capstone-db/image_server/best_finetune_train3.pt` | GO1 finetune 가중치. 360 경로는 `zeroshot_detector` 만 읽음 | 🔒 불필요 |
| Redis (포트 7860) | GO1 스트림 전용 | 🔒 불필요 |
| 포트 7864 `/command/go1` | GO1 제어 | 🔒 불필요 |

## 1-E. MASt3R 계열 (논문용, 파이프라인 외)

| 서버 경로 | 상태 |
|---|---|
| `crop_center.py`, `crop_go1.py` | 🔒 로봇 이미지 크롭 |
| `robot_bbox.py`, `mast3r/match_vote.py`, `rescore*.py`, `truth.json`, `out_m3/` | 🔒 매칭 판별자 (논문 현행) |
| `check_pose_sparse*.py`, `check_pose_diag_5ap.py` | 🔒 **자세추출 — 완전 폐기** (v1 §10.1) |

**→ 360 → Unity 장애물 파이프라인에 MASt3R 는 포함되지 않는다.** 팀원에게 줄 필요 없음.

---

# 2. PC 파일

## 2-A. 현행(영상) — `C:\Users\asdfa\physical mk2\360camera_work`

| # | 파일 | 역할 | 폴더 |
|---|---|---|---|
| P1 | `stream_to_server.py` | **카메라 RTMP → 최신 프레임 → JPEG → 서버 업로드.** `pro2_auto_bg.py` 대체 | ✅ (13,145 B) |
| P2 | `preview_probe.py` | 스트림 확인 도구. `--measure N` | ✅ (12,847 B) |
| P3 | `anchor360_bridge.py` | 서버 JSON GET → 유니티 UDP 5010 + 하트비트 | ✅ (양 폴더에 동일 해시) |

## 2-B. 구버전(사진) — `C:\Users\asdfa\docx2026_code`

| # | 파일 | 역할 | 폴더 |
|---|---|---|---|
| P4 | `pro2_auto_bg.py` | **촬영 + 백그라운드 stitch + 업로드.** 논문 기준 구성 | ✅ (양 폴더 동일 해시) |
| P5 | `stitch_prostitcher.py` | **ProStitcher CLI 호출.** `pro2_auto_bg.py` 가 `from stitch_prostitcher import stitch_folder` 로 **import 함** | ⚠ **`docx2026_code` 에만 있음. `360camera_work` 에는 없음** |
| P6 | `trt_cache/flownet_model_20230922_fp16.trt` (70 MB) | TensorRT 엔진 백업. 없으면 stitch 콜드빌드 **~19분** | ⚠ `docx2026_code\trt_cache` 에만 |
| P7 | `trt_cache/fusionnet_model_20230922_fp16.trt` (13 MB) | 동상 | ⚠ 동상 |
| P8 | `pro2_auto.py` | 1회 테스트(직렬, 단계별 시간 측정) | ⚠ `docx2026_code` 에만 |
| P9 | `pro2_pipeline.py` | 구버전(origin 6장 전송). 참고용 | ⚠ `docx2026_code` 에만 |
| P10 | `pro2_auto_old.py` | `pro2_auto.py` 와 동일 크기. 백업 | ⚠ `docx2026_code` 에만 |
| P11 | `trt_cache/pro2_auto_bg_oldbackup.py` | 더 오래된 백업 | ⚠ `docx2026_code\trt_cache` 에만 |

> ⚠ **P5 가 빠지면 `360camera_work` 의 `pro2_auto_bg.py` 는 import 에러로 실행조차 안 된다.**
> 사진 경로를 넘길 거면 P5~P7 을 반드시 같이 넣을 것.

## 2-C. 1세대 수동 도구 / JSON — `C:\Users\asdfa\docx2026_code`

| # | 파일 | 역할 | 폴더 |
|---|---|---|---|
| P12 | `anchor360_merged.json` | **7월 유니티가 실제로 본 5개짜리 고정 JSON.** "예전엔 정확했다"의 정체 (지난 분석 §C-1) | ⚠ `docx2026_code` 에만 |
| P13 | `obstacles_360.json` | `detect_send_3b2.py` 원시 출력 예시 (u−60, 7/10) | ⚠ `docx2026_code` 에만 |
| P14 | `send_to_unity.py` | `anchor360_merged.json` 1회 UDP 송출 | ⚠ `docx2026_code` 에만 |
| P15 | `camera_test.py`, `camera_test2.py` | 6/26 카메라 연결 초기 테스트 | ⚠ `docx2026_code` 에만 |
| P16 | `stitch_prostitcher_old1/old2/test.py` | stitch 구현 시행착오 3종 | ⚠ `docx2026_code` 에만 |
| P17 | `taskkill` (0 byte), `test_thumbnail.jpg`, `trt_cache.zip` (74 MB) | 잔여물 | 🔒 불필요 |

## 2-D. 서버 스크립트의 PC 사본 (`360camera_work` 안)

`anchor360_live.py` · `pano_receiver.py` · `unidepth_dual_yolo_config.yaml` · `e2p.py` ·
`detect_ground_3b1.py` · `detect_send_3b2.py` · `detect_send_3b2_timed.py` · `relay_loop.py`
→ 전부 ✅. **팀원에게는 이 사본을 그대로 주면 된다.**

## 2-E. `구버전(참고금지)` 폴더 안

| 파일 | 확인 결과 |
|---|---|
| `anchor360_live_2.py`, `anchor360_live_backup.py` | **루트의 `anchor360_live.py` 와 md5 완전 동일** (`5333e450…`). 셋 다 같은 파일이므로 **1개만 주면 됨** |
| `preview_probe.py.bak`, `stream_to_server.py.bak` | 루트본과 **다름** = 수정 전 구버전. 줄 필요 없음 |
| `360camera_work.zip` (499 KB) | 09-21 시점 스냅샷 |
| 로그 6종 · 문서 4종 | 아래 §4 |

---

# 3. Unity 파일 (팀원 PC)

| # | 파일 | 역할 | 폴더 |
|---|---|---|---|
| U1 | `Anchor360ObstacleReceiver.cs` | UDP 5010 수신 → 큐브 스폰. 스냅샷 방식. `yawOffsetDeg`/`invertX`/`distanceScale`/`floorWorldY` | ✅ `360camera_work` (8,147 B) |
| U2 | `Go1ObstacleJsonReceiver.cs` | GO1 쪽 수신기(5011). 360 과 **병합 로직 없음** | ❌ 없음 (참고용) |
| U3 | **`Anchor360` 오브젝트 Inspector 값** | Position(3.2, 1.85, 6.24) / **Rotation Y = 미확인** / yawOffsetDeg / invertX / distanceScale / floorWorldY | ❌ **없음 — 스크린샷 필요** |

---

# 4. 문서 · 로그 · 데이터

## 4-A. 문서 (전부 ✅ `360camera_work`)

| 파일 | 내용 |
|---|---|
| `260921 카메라 최종정리2.txt` | **영상 방식 실행 절차서 (정본)** |
| `260921_360도카메라_파이프라인_총정리_v2.md` | 영상 전환 근거·실측·미해결 |
| `260915_360도카메라_파이프라인_총정리.md` | **v1. 사진 방식. 논문 기준 구성. 폐기 금지** |
| `260712 카메라 사용법 올인원.txt` | 1세대 수동 절차 + 줄자 대조 로그 |
| `260922_구버전_현행_비전처리_비교_및_배포가이드.md` | 어제 작성 분석 |
| `구버전/260914 카메라 최종 정리.txt` | 2세대(사진+자동) 절차서 |
| `구버전/260701 질문+진행한것 (카메라 작동법).txt` | e2p/detect/MASt3R 초기 절차 |
| `구버전/260803 360도 카메라.txt` | 8월 수동 검출 로그 |
| `구버전/260921 360도 카메라 동영상 촬영으로 변경.txt` | Farsight 유지 결정 메모 |

## 4-B. 로그 (전부 ✅ `360camera_work/구버전`)

| 파일 | 내용 |
|---|---|
| `260921 360도 카메라 실제 구동 로그.txt` (72 KB) | 사진 62장 연속. **stitch 큐 0→38 발산 증거** |
| `260921 360도 카메라 실제 구동 로그2.txt` (10 KB) | **사진↔영상 좌표 대조 원본** + 안정화 OFF 실험 |
| `260921 카메라 영상 로그3.txt` (19 KB) | 영상 첫 관통 + **사용자 오차 관찰 원문** |
| `260921 카메라 영상 실측 로그.txt` (193 KB) | **2분 614프레임 실측** |

## 4-C. 이미지

| 항목 | 폴더 |
|---|---|
| `image backup/` 17장 (live_debug, proj_debug, p2_live_slice, p2_proj_slice, br40_* 등) | ✅ |
| `360도 카메라 반영범위/` 8장 (A/B × u0/90/180/270 등) | ✅ |
| **`anchor360_latest_260921.jpg`** (오차 화살표 표시본) | ❌ **없음** |
| **`KakaoTalk_20260921_174546411.jpg`** (실제 랩실 사진) | ❌ **없음** |
| 서버 `live_out/anchor360_latest.jpg` / `slices/` | ❌ (필요시) |
| 서버 `pano_captures/pro2_anchor/*/equirect.jpg` | ⚠ `docx2026_code\captures\` 에 158개 폴더 + `360도 카메라 사진.zip` (52 MB) |

---

# 5. 팀원에게 넘길 꾸러미 — 지금 부족한 것만

## 5-A. 지금 당장 받아야 하는 것 (서버에서)

```bash
# 1) 가중치 + CLIP 캐시
scp -P 8522 dg@210.110.250.33:~/capstone-db/image_server/yolov8s-world.pt .
scp -P 8522 dg@210.110.250.33:~/capstone-db/docx2026/weights/clip/ViT-B-32.pt .

# 2) UniDepth 작업용 — 현행본 3개
scp -P 8522 dg@210.110.250.33:~/capstone-db/image_server/unidepth_dual_yolo.py .
scp -P 8522 dg@210.110.250.33:~/capstone-db/image_server/server_stream_depth_v2.py .
scp -rP 8522 dg@210.110.250.33:~/capstone-db/image_server/checkpoints/unidepth-v2-vitb14 .

# 3) 환경 확인 (★ 제일 먼저)
ssh -p 8522 dg@210.110.250.33
. ~/capstone-db/venv_image/bin/activate
python -c "from unidepth.models import UniDepthV2; print('unidepth OK')"   # ← 이게 통과하는지
pip freeze > ~/capstone-db/requirements-server.txt
python -V; nvidia-smi | head -3

# 4) 서버 파일 목록 (빠진 것 확인용)
ls -la ~/capstone-db/docx2026 ~/capstone-db/image_server
```

## 5-B. PC 안에서 옮기기만 하면 되는 것

`docx2026_code` → `360camera_work` 로 복사 (또는 배포 꾸러미에 같이 넣기):

```
stitch_prostitcher.py          ← ★ 없으면 pro2_auto_bg.py 실행 불가
trt_cache\flownet_model_20230922_fp16.trt
trt_cache\fusionnet_model_20230922_fp16.trt
anchor360_merged.json          ← 7월 기준 데이터. 비교 근거
obstacles_360.json
send_to_unity.py
pro2_auto.py, pro2_pipeline.py ← 참고용
```

## 5-C. 유니티 쪽에서 받아야 할 것

- `Anchor360` 오브젝트 **Inspector 전체 스크린샷** (특히 **Rotation Y**)
- `Go1ObstacleJsonReceiver.cs` (참고용)
- `anchor360_latest_260921.jpg`, `KakaoTalk_20260921_174546411.jpg`

## 5-D. 권장 배포 구조

```
360camera_dist/
├─ README_팀원용.md
├─ server/
│   ├─ anchor360_live.py                    ✅  ← 거리 계산식이 여기
│   ├─ pano_receiver.py                     ✅
│   ├─ unidepth_dual_yolo_config.yaml       ✅  (스냅샷 사본)
│   ├─ e2p.py  relay_loop.py                ✅
│   ├─ detect_ground_3b1.py                 ✅  (구버전 수식 원본)
│   ├─ detect_send_3b2.py                   ✅
│   ├─ detect_send_3b2_timed.py             ✅
│   ├─ yolov8s-world.pt                     ❌  ← 받아야
│   ├─ weights/clip/ViT-B-32.pt             ❌  ← 받아야
│   └─ requirements-server.txt              ❌  ← 만들어야
├─ server_unidepth/                         ← ★ 팀원 작업용
│   ├─ unidepth_dual_yolo.py                ❌  ← 받아야 (호출법 레퍼런스)
│   ├─ server_stream_depth_v2.py            ❌  ← 받아야
│   └─ checkpoints/unidepth-v2-vitb14/      ❌  ← 받아야
├─ pc/
│   ├─ stream_to_server.py                  ✅
│   ├─ preview_probe.py                     ✅
│   ├─ anchor360_bridge.py                  ✅
│   └─ photo_mode/
│       ├─ pro2_auto_bg.py                  ✅
│       ├─ stitch_prostitcher.py            ⚠ docx2026_code 에서 복사
│       └─ trt_cache/*.trt                  ⚠ docx2026_code 에서 복사
├─ unity/
│   ├─ Anchor360ObstacleReceiver.cs         ✅
│   └─ Anchor360_Inspector.png              ❌  ← 캡처 필요
├─ data/
│   ├─ anchor360_merged.json                ⚠ docx2026_code 에서 복사
│   ├─ obstacles_360.json                   ⚠ 동상
│   └─ sample_equirect/                     ⚠ captures 에서 2~3장만
└─ docs/  (문서 9종 + 로그 4종)              ✅
```

**요약: 전체 32개 중 — 폴더에 있음 19, PC 안에서 복사만 하면 됨 7, 서버에서 받아야 함 6.**

---

# 6. 기술 메모 — UniDepth 를 더할 때 미리 알려줄 것

팀원 아이디어(기하식 + UniDepth)는 **방향은 맞다.** 다만 그냥 더하면 지금보다 나빠질 수 있어서
넘길 때 같이 전달할 것 5가지.

## 6-1. 두 값의 오차 구조가 정반대다

| | 기하식 (`pixel_to_ground`) | UniDepth |
|---|---|---|
| 전제 | **물체가 바닥에 닿아 있음** | 없음 |
| 근거리(<2 m) 정확도 | **매우 높음** (1 px = 0.7 cm) | 상대오차 일정 |
| 원거리(5 m+) | **D² 로 악화** (1 px = 4.3 cm) | 상대오차 일정 |
| 프레임 간 재현성 | **표준편차 0.1~2.6 cm** (87프레임 실측) | 더 흔들림 |
| 접지 깨질 때 | **체계적으로 틀림** (복사기가 캐비닛 위 → 훨씬 멀게) | **영향 없음** |

→ **단순 평균은 금물.** 근거리에서 0.7 cm/px 짜리를 일부러 망치는 짓이다.
→ 거리 구간별 가중이 맞다: `D<3 m` 이고 `ground_clipped=False` → 기하 신뢰 /
   `ground_clipped=True` 이거나 `D>4 m` → UniDepth 가중 상향.

## 6-2. 가장 싸고 안전한 1차 목표는 "융합"이 아니라 "검증자"

두 값이 X % 이상 어긋나면 **그 검출을 버린다.** 융합 수식을 안 만들어도
지금 유니티에 뜨는 유령 큐브(복사기·5 m 박스)를 바로 걷어낼 수 있고,
**검증된 기하 수식을 안 건드려도 된다**(논문 11.2 cm 수치가 이 수식 기준이다).

## 6-3. `calibrate_depth_to_cm()` 를 그대로 가져오면 안 된다

`unidepth_dual_yolo.py` 의 그 함수는 **GO1 전용**이다.
```python
distance_cm = -4.61*d**3 + 28.91*d**2 - 11.16*d + 15.21     # GO1 어안에 맞춘 3차 피팅
# + 이미지 중심 반지름 r 기반 center_gain / edge_gain (어안 왜곡 보정)
```
- 464×400 **어안** 프레임 기준, `camera=None` 으로 추론한 결과를 후보정한 것
- 360 슬라이스는 **720×720 정상 원근**이고 **내부 파라미터가 정확히 알려져 있다**
  (`fx=fy=360`, `cx=cy=360`)
→ 360 쪽은 **보정식 없이 `camera=K` 를 주는 게 정답**이다. 오히려 GO1 보다 유리한 조건이다.
→ 어안 반경 보정(`center_gain`/`edge_gain`)은 **반드시 빼야 한다.** 그대로 쓰면 틀린 게인이 곱해진다.

## 6-4. "깊이"와 "수평거리"는 다른 값이다

- 기하식 `D = √(X²+Z²)` 는 **접지점의 바닥 수평거리**
- UniDepth 는 **보이는 표면까지의 레인지(깊이)**

바로 비교하면 안 되고, `K` 로 역투영해서 카메라 좌표 3D 점을 만든 뒤
pitch 회전을 적용해 **수평 성분만** 취해야 같은 축이 된다.
(`anchor360_live.py` 의 `GroundProjector` 안에 이미 그 회전이 있으니 재활용 가능)

## 6-5. 예산과 한계

- **시간 예산은 있다.** 서버 총 처리 0.050 s(추론 0.010 s), 이론 20 fps 대비 유입 4.31 fps = **4.6배 여유.**
  GO1 쪽은 UniDepth 를 `DEPTH_INTERVAL=4` 로 **4프레임에 1번만** 돌리고 있으니
  360 도 매 프레임은 무리일 수 있다. **먼저 1장 재보고 결정할 것.**
- **단, UniDepth 는 방향(yaw) 문제를 1 mm 도 못 고친다.** 지금 유니티에서 가장 크게 어긋나 보이는 건
  거리보다 **방향**이고, 그건 `yawOffsetDeg` / `Anchor360` Rotation Y 문제다.
  깊이 작업과 **병행하되 별개로** 풀어야 한다.
- 바꾸기 **전후로 사진↔영상 좌표 대조(분석문서 §C-6 방식)를 한 번씩 해둘 것.**
  논문 수치(11.2 cm)와의 관계를 기록으로 남겨야 한다.

---

*문서 끝. 2026-09-22.*
