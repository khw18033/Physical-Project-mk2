# 가시화 파트 구현 현황 — 2026-08-21

> 기준: 교수님께 제출한 요구사항 시트(`피지컬팀 프로젝트 mk2 요구사항 정의서.xlsx`) 김현우 시트 **33건**
> 대조 방법: 요구사항 ID를 코드에서 역추적해 데이터 레이어·화면 양쪽에 실체가 있는지 확인

## 한눈에

| 구분 | 건수 | 비율 |
|---|---|---|
| **동작하는 것** | 21 | 64% |
| **아직 없는 것** | 12 | 36% |

핵심은 숫자보다 **어느 21건인가**입니다. 지금 구현된 21건은 **데이터가 들어와서 화면에 닿기까지의 뼈대 전부**입니다. 남은 12건은 그 뼈대 위에 붙는 화면들이라, 새 구조를 만들 필요 없이 얹으면 됩니다.

---

# 1. 동작하는 것 (21건)

## 1-1. 데이터가 들어오는 길 — 입구 4개 전부 열림

| ID | 내용 | 확인 방법 |
|---|---|---|
| `VZ-I-01` | 실시간 상태 구독 (상태 3층) | 현황판이 갱신되는 것 |
| `VZ-I-02` | 재접속 시 현재값 1회 확보 | 새로고침해도 **빈 카드가 없음** |
| `VZ-I-03` | 레지스트리 조회 | `robot-03`이 값 없이도 카드로 보임 |
| `VZ-I-04` | 지표 질의 (요약/원본 두 경로) | 지표 탭의 "원본 보기" 전환 |
| `VZ-I-05` | 감사 이력 조회 | 제어 패널의 마지막 조작자 |

**입구가 넷이라는 구조가 실제로 그렇게 구현돼 있습니다.** 브라우저가 브로커나 DB에 직접 붙는 경로는 코드에 없습니다.

## 1-2. 상태를 다루는 방식

| ID | 내용 | 왜 중요한가 |
|---|---|---|
| `VZ-U-01` | 상태 4종 구분 표시 | 3층(기기·서버·배포)을 각각 보관하고 표시값만 파생. **뭉쳐서 저장하지 않음** |
| `VZ-C-03` | 집약 계층 표기 | 요약값을 원본으로 오인해 재집약하는 것을 **차단**(경고가 아니라 계산 중단) |
| `VZ-C-04` | 권한 범위 | 담당 구역 밖 대상은 버튼 잠금 + 서버도 거부 |
| `VZ-C-01` | 권한·역할 확인 | 로그인 시 1회 조회, 주기 폴링 없음 |
| `VZ-C-02` | 외부 소스 부재 시 동작 | 레지스트리가 정적 파일이어도 화면이 돌아감 |
| `VZ-C-05` | 설계 규모 가정 | 상수로 명시 (구역 1 · 장치 20 · 사용자 1) |
| `VZ-I-11` | 구독 범위 한정 | **자리 확보 상태** — 필드가 실제로 왕복만 함 |

## 1-3. 명령을 보내고 결과를 받는 길

| ID | 내용 |
|---|---|
| `VZ-O-01` | 제어 명령 발행 — 요청 식별자는 가시화, **상관키는 백엔드 발급** |
| `VZ-O-02` | 명령 결과 4단계 수신 → 3종 표시 |
| `VZ-O-03` | 책임소재 필드 전달 (기록 작성은 백엔드) |
| `VZ-O-05` | 제어 잠금 상태와 사유 표시 |

## 1-4. 임무와 영상

| ID | 내용 |
|---|---|
| `VZ-U-07` | 계획 승인 — **승인 전에는 실행되지 않음**, 근거를 펼쳐 봄 |
| `VZ-U-05` | 서브태스크 진행 — 어느 구간에서 왜 실패했는지 |
| `VZ-I-06` | 영상 스트림 표시 (합성 영상으로 대체) |
| `VZ-I-07` | 탐지 오버레이 정합 — **프레임 참조로 맞춤** |
| `VZ-I-09` | 객체 추적·궤적, 신뢰도 낮은 탐지 구분 |

---

# 2. 아직 없는 것 (12건)

## 2-1. 트윈 화면 — 1건 (가장 큰 공백)

| ID | 내용 |
|---|---|
| `VZ-U-02` | 디지털 트윈 위치 렌더링 |

지금 현황판은 **카드 격자**이고, 공간에 배치된 그림이 하나도 없습니다. 좌표는 목 서버가 이미 50ms로 보내고 있고 `coordinate_frame` 표기도 카드에 뜨지만, **그 좌표를 실제로 배치하는 화면이 없습니다.**

**Unity 별도 앱으로 만들 예정**이며, 최종적으로는 시뮬레이터를 겸합니다. 다만 시뮬레이터를 먼저 만들면 뷰어가 시뮬레이터의 내부 상태를 직접 읽는 지름길이 생겨 **파이프라인을 한 번도 안 거치게** 되므로, 뷰어를 먼저 붙여 왕복을 강제한 뒤 시뮬로 갈아끼우는 순서로 갑니다.

## 2-2. 음성·LLM — 4건

| ID | 내용 |
|---|---|
| `VZ-L-01` | 음성 입력(STT) 및 텍스트 전달 |
| `VZ-L-02` | 해석 결과 표시 및 수락 |
| `VZ-L-03` | 실행 전 2단 확인 · 신뢰도 임계 |
| `VZ-L-04` | 화면 구성 보조 LLM |

STT를 가시화가 맡는 것으로 확정됐습니다. 목업은 있고 구현이 없습니다.

## 2-3. 노드 편집기 — 1건

| ID | 내용 |
|---|---|
| `VZ-U-04` | 노드 편집기 구성 반영 |

파이프라인·화면 구성을 노드 그래프로 편집하고, 시험 실행 후 **명시적 '반영' 조작으로만** 운영 적용. `VZ-L-04`(LLM이 노드 그래프 초안 생성)와 한 몸이라 함께 만드는 것이 맞습니다.

## 2-4. 잔여 표시 — 4건

| ID | 내용 |
|---|---|
| `VZ-I-08` | 위험도 상태 표시 (평시·관찰·경보·복구) |
| `VZ-I-10` | AI 실패 이벤트 알림 |
| `VZ-O-04` | 자체 관측 지표 발행 |
| `VZ-U-03` | 추상화 계층 뷰 (결심자·운영자·개발자) |

각각 작습니다. 넷을 묶어 한 번에 정리할 수 있는 규모입니다.

## 2-5. 다른 파트를 기다리는 것 — 2건

| ID | 내용 | 무엇을 기다리나 |
|---|---|---|
| `VZ-U-06` | 미확인 탐지 그룹 확인 | AI와 전달 형식을 맞춰야 함 |
| `VZ-C-06` | 원천 종류(실물·시뮬레이션) 표기 | **표기를 붙일 주체가 아직 없음** |

---

# 3. 남은 순서

| 순서 | 무엇 | 왜 이 순서인가 |
|---|---|---|
| **1** | `VZ-U-02` 트윈 뷰어 (Unity, 뷰어만) | 디지털 트윈 과제인데 트윈 화면이 없는 상태. 좌표는 이미 흐르고 있어 바로 붙음 |
| **2** | 잔여 표시 4건 | 작고 독립적. 큰 작업 사이에 끼워 정리 |
| **3** | 음성·LLM 4건 | 목업이 있고 경계도 확정됨(STT는 가시화) |
| **4** | 노드 편집기 + LLM 그래프 생성 | 둘이 한 몸. 앞의 것들이 안정된 뒤 |
| **5** | Unity 시뮬레이터 (2단계) | 뷰어로 왕복을 검증한 뒤에 원천을 갈아끼움 |
| 대기 | `VZ-U-06` · `VZ-C-06` | 다른 파트 합의 후 |

---

# 4. 회의에서 정해야 할 것 5건

| # | 안건 | 상대 | 정하지 않으면 |
|---|---|---|---|
| 1 | **영상 변환 지점** | 3자 | 실제 카메라를 붙이는 순간 막힘. **세 파트 통틀어 담당자가 없는 유일한 항목** |
| 2 | **명령 접수 응답에 요청 식별자를 에코하는가** | 이대규 | 두 키 매핑이 성립하지 않아 진행 표시가 엉뚱한 카드에 붙음 |
| 3 | **원천 종류 표기의 발행 주체·형식** (`VZ-C-06`) | 3자 | 시뮬레이션 조작과 실물 조작이 감사에서 구분되지 않음 |
| 4 | **좌표계 손잡이(handedness) 규약** | 이대규 | Unity는 왼손 좌표계. 오른손 관례로 값이 오면 **좌우가 뒤집힘** |
| 5 | **설계 규모 가정** (`VZ-C-05`) | 전 파트 | 모든 주기 산정의 전제인데 문서에 숫자가 없음 |

부수적으로, 백엔드 시트의 「브라우저 WS 게이트웨이」(`BE-T-03`) 등 **「브라우저」 표현**을 뷰어로 일반화하는 합의가 필요합니다. 가시화 시트는 이미 정리했고, 한쪽만 고치면 두 문서가 어긋납니다.
