# 백엔드 요구사항 반영 — 대조 결과

> 대조: `피지컬팀 프로젝트 mk2 요구사항 정의서 (2).xlsx` (이대규 40행 신규 · 진나영 ID 재편)
> 결론: **막혀 있던 6건 중 4건이 풀렸고**, 반대로 **내가 고쳐야 할 것이 2건** 나왔습니다. 영상 변환 지점만 여전히 공백입니다.

## 한눈에

| 구분 | 건수 | 내용 |
|---|---|---|
| ✅ **해소됨** | 4 | 초기 스냅샷 · 프레임 참조 형식 · 좌표 규약 · 상관키 |
| 🔴 **충돌 — 내 쪽 수정 필요** | 1 | `command_id` 발급 주체 |
| ⬆️ **자리 확보 → 실사용 승격** | 2 | 집약 계층 표기 · 권한 범위 |
| ⚠️ **성격이 바뀜** | 2 | 지표 질의 · 계획 승인 중계자 |
| ❓ **새로 확인할 것** | 2 | STT 주체 · 재난 모드 지연 상한 |
| ⛔ **여전히 공백** | 1 | 영상 변환 지점 |

---

# A. 연관점 빈칸 채움 (적용 완료)

## A-1. 비어 있던 3건

| 행 | 채운 값 | 근거 |
|---|---|---|
| `VZ-C-01` 권한·역할 확인 | **BE-Q-04** | 역할·범위 조회 API 제공 + 백엔드 강제 |
| `VZ-C-04` 권한 범위 | **BE-Q-04, BE-C-02** | 범위 기준은 Zone 계층 |
| `VZ-C-05` 설계 규모 가정 | **BE-S-03, BE-S-07** | "부하는 구역 수에 비례" 원칙과 재난 모드 SLA |

## A-2. 진나영 ID 재편 반영 (두 번째 교체)

`AI-*` → `OD/EG/PL/…` 로 바뀐 지 얼마 안 되어 **다시 `AI-N/E/D/S/R/B/O/L/C`** 로 재편됐습니다.

| 이전(구) | 그 전(더 구) | 지금 |
|---|---|---|
| OD-01 / OD-02 | AI-P-01 | **AI-N-01 / AI-N-02** |
| EG-02 / EG-03 | AI-P-02 | **AI-E-01 / AI-E-02** |
| PL-02·03·04 | AI-D-03 | **AI-D-01 / AI-D-02** |
| PL-05 | — | **BE-X-04** *(승인 중계는 백엔드로 이동)* |
| PL-06 | AI-D-04 | **AI-D-03** |
| PR-01 / PR-02 / PR-04 / PR-05 | — | **AI-S-01 / AI-S-02 / AI-S-03 / AI-S-04** |
| RK-01·02·03 | — | **AI-R-01 / AI-R-02 / AI-R-03** |
| OB-01 / OB-02 | AI-O-01 | **AI-O-01 / AI-O-02** |
| CL-01 / CL-02 | AI-L-02 | **AI-L-01 / AI-L-02** |
| GN-02 / GN-03 | AI-C-02 / AI-C-04 | **AI-C-02 / AI-C-03** |

> 세 번째 체계입니다. 상대 파트 ID를 연관점에 직접 적는 방식은 **재편될 때마다 전 행을 손봐야** 합니다. 통합 문서에서 ID 체계를 고정하지 않으면 이 작업이 반복됩니다.

---

# B. 해소된 것 (4건) — 문구에서 "미결" 표시를 뺄 수 있음

## B-1. ✅ 초기 스냅샷 — 회의 안건이 확정됨

> **BE-T-06** 재접속 시 현재값 즉시 제공 — "브라우저가 백엔드를 경유하는 구조상 **브로커 retained보다 백엔드 캐시**로 즉시 제공하는 것이 경로가 단일해 유리"

`retained` vs 백엔드 캐시 논쟁이 **캐시로 확정**됐습니다. `VZ-I-02` 정의의 "둘 중 하나" 표현을 확정 문구로 바꾸면 됩니다. 3자 회의 안건에서도 뺄 수 있습니다.

## B-2. ✅ 프레임 참조 형식 — 단일 규약으로 확정

> **BE-C-03** — `frame_ref(source_id, capture_timestamp, sequence_id)` 형식을 백엔드가 정의, 전 파트 준수. **AI-C-03**도 같은 필드로 정렬됨.

`VZ-I-07`의 최대 리스크였던 **AI ↔ 하드웨어 형식 불일치가 사라졌습니다.** B 작업에서 실측한 뒤처짐 숫자를 근거로 밀던 건인데, 백엔드가 규약으로 잡아줬습니다.

## B-3. ✅ 좌표 규약 — 다시 명문화됨

> **BE-C-04** — 좌표 변환은 백엔드 단독, `coordinate_frame` 동반, y=위 · x-z 바닥 · 미터
> **DT-03** — 이미지 좌표를 x-z 바닥으로 변환하고 전역 통합, 앵커 보정 포함

하드웨어 시트에서 사라졌던 좌표 규약이 **백엔드 쪽에 자리 잡았습니다.** `VZ-U-02`의 "확정 필요" 주석을 뺄 수 있습니다. 출처(자기추정/앵커보정/AI인지) 구분도 DT-03 연관점에 그대로 있습니다.

## B-4. ✅ 상관키 — 존재는 확정, 다만 주체가 다름

> **BE-X-01** — 백엔드가 `command_id`를 발급하고 `plan_id`와 매핑 보유

상관키 자체는 확정됐습니다. 그런데 **발급 주체가 제 가정과 다릅니다** → C-1로.

---

# C. 내가 고쳐야 할 것

## C-1. 🔴 `command_id` 발급 주체 충돌 — 가장 중요

**지금 내 시트 (`VZ-O-01`)**
> "명령 식별자(전 파트 단일 상관 키)와 만료 시각을 **포함**하며" — 가시화가 붙여 보내는 것으로 서술

**백엔드 (`BE-X-01`)**
> "백엔드가 **명령 조립 단계에서** 전 파트 단일 상관키(`command_id`)를 **발급**하고, 가시화 요청부터 디바이스 실행·결과·감사까지 이 키로 사슬을 잇는다"

**충돌 지점** — 둘 다 발급하면 키가 두 개가 되고, 가시화가 안 붙이면 **요청과 응답을 짝지을 수단이 발행 순간에 없습니다.**

**제안하는 수정**
> 가시화는 요청에 **클라이언트 요청 식별자**(`client_request_id`)를 붙여 보내고, 백엔드가 발급한 `command_id`를 응답으로 받아 그 시점부터 결과·감사 추적에 사용한다. 두 키의 매핑은 백엔드가 보유한다.

이러면 발행 직후 낙관적 UI(버튼 비활성·스피너)를 요청 식별자로 걸고, `command_id`가 오면 그때부터 사슬을 잇습니다. **B 작업에서 만든 제어 패널 코드도 이 부분을 고쳐야 합니다.**

## C-2. ⬆️ `VZ-C-03` 집약 계층 표기 — 자리 확보에서 **실사용으로 승격**

> **BE-S-06** — 값에 원본/집약 여부와 집약 계층을 **표기해 전달** (상시)
> **BE-S-03** — 평시에는 엣지가 raw를 보관하고 **요약 시계열만** 백엔드로 페더레이션

"나중에 집약이 서버로 옮겨가면"이라고 썼는데, **이미 그렇게 설계돼 있습니다.** 평시 지표는 요약값으로 올라옵니다. 지금부터 표기를 읽어 재집약을 막아야 합니다.

→ 제안 주기를 "해당 없음(자리만 확보)" → **"값 수신 시 상시"** 로 바꾸고 자리 확보 표시를 뗍니다.

## C-3. ⬆️ `VZ-C-04` 권한 범위 — 자리 확보에서 **실사용으로 승격**

> **BE-Q-04** — 역할과 **그 역할이 적용되는 범위(도메인·구역 집합)** 를 조회하는 API 제공, 백엔드가 강제

범위가 이미 API에 들어 있습니다. → **"로그인 시 1회 + 토큰 갱신 시"** 로 주기를 넣고 자리 확보 표시를 뗍니다.

## C-4. ⚠️ `VZ-I-04` 지표 질의 — 성격이 바뀜

제 근거는 "하드웨어가 15초마다 수집하니 그보다 자주 물어도 새 점이 없다"였습니다. 그런데 백엔드 설계에서는 —

> **BE-S-03** — 평시 요약 pull **15초 주기**. raw는 엣지에 남고 백엔드에는 **구역 요약만** 올라온다

15초의 의미가 **수집 주기 → 페더레이션 주기**로 바뀌었고, 내가 평시에 받는 건 **원본이 아니라 요약**입니다. raw가 필요하면 `BE-Q-01`이 `BE-T-05`를 통해 엣지로 중계합니다.

→ 정의에 **"평시 조회는 구역 요약, 원본은 프록시가 엣지로 중계"** 를 명시하고, 근거 문장을 페더레이션 주기로 고칩니다. C-2와 짝입니다.

## C-5. ⚠️ `VZ-U-07` 계획 승인 — 중계자가 백엔드

> **BE-X-04** — 백엔드가 AI 계획과 근거를 가시화에 전달하고, **승인·거부를 받아 승인된 계획만** 엣지·로봇으로 발행

제 연관점은 AI 쪽(`PL-04/05/07`)을 보고 있었는데, **실제 상대는 백엔드**입니다. AI는 계획 생성(`AI-D-01`)과 검증(`AI-D-02`)까지고, 승인 왕복은 백엔드가 중계합니다.

---

# D. 새로 확인해야 할 것

## D-1. ❓ STT를 누가 하는가 — 다시 모호해짐

**내 시트 (`VZ-L-01`)** — "발화를 텍스트로 변환해 … **텍스트 형태로 AI에 전달**"
**진나영 (`AI-D-03`)** — "관리자의 음성 또는 텍스트 명령에서 … 추출. **음성 입력은 STT로 텍스트화하며**"

이전 판(`PL-06`)에는 *"가시화가 STT로 변환해 전달한 텍스트를 입력받아"* 라고 명시돼 있었는데, 이번 개정에서 그 문장이 빠지고 **AI가 STT를 하는 것처럼** 읽힙니다.

STT를 양쪽이 다 만들면 낭비고, 아무도 안 만들면 경로가 끊깁니다. **나영에게 한 줄 확인**이 필요합니다.

## D-2. ❓ 재난 모드 지연 상한 (`BE-S-07`)

백엔드에 "재난 모드 지연 상한(SLA)" 항목이 생겼습니다. 제 화면 갱신 주기(이벤트 모드 1초)와 이 상한이 맞물리는지 확인해야 합니다. 상한이 제 갱신보다 느슨하면 화면이 SLA를 못 지키는 구간이 생깁니다.

---

# E. 여전히 공백 — 영상 변환 지점

`BE-T-05`가 *"VZ-I-06(영상)이 이 중계를 경유"* 라고만 하고, **카메라 원본 규격을 브라우저 재생 가능 형식으로 바꾸는 주체**는 어느 시트에도 없습니다.

- 하드웨어 — `HW-R-07`은 "고해상도 스트림을 엣지노드로 전송"까지
- 백엔드 — `BE-T-05`는 경로 중계만, 변환 언급 없음
- 가시화 — 브라우저는 변환할 수 없음

**세 파트를 통틀어 유일하게 담당자가 없는 항목입니다.** B-3에서 canvas 합성으로 우회해 뒀지만, 실제 카메라를 붙이는 순간 막힙니다. 이건 계속 물어야 합니다.

---

# 정리

**적용됨** — 연관점 32행 전면 갱신 (빈칸 3건 + 진나영 ID 재편 + BE-* 추가)

**승인해 주시면 바로 반영할 것** — C-1(상관키 발급 주체) · C-2(집약 표기 승격) · C-3(권한 범위 승격) · C-4(지표 질의 성격) · C-5(승인 중계자)

**물어볼 것** — D-1(STT 주체, 나영) · D-2(재난 SLA, 대규) · E(영상 변환 지점, 3자)
