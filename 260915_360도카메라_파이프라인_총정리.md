# 360° 카메라 이미지 처리 파이프라인 통합 — 총정리

**작성일**: 2026-09-15
**작성자**: 이대규
**목적**: 360° 카메라(Insta360 Pro2) 촬영 → 서버 비전 처리 → Unity 장애물 큐브까지의
실시간 파이프라인을 구축·통합한 작업 전체 기록. 구동 절차, 파일 명세, 확정 파라미터,
실측 수치, 트러블슈팅 이력, 미해결 항목을 모두 포함한다.

**이 문서의 용도**
- 다음에 이 시스템을 구동할 때 보는 절차서
- "예전에 왜 이렇게 했는가"를 되짚는 근거 기록
- 미해결 항목을 이어받아 발전시키기 위한 출발점

**권위 순서**: 실측 로그 > 이 문서 > 과거 채팅 기록 > 기억

---

# 1. 결과 요약

## 1.1 달성한 것

**카메라 전원 → Unity 큐브까지 전 구간 자동화 완료.**

```
[Insta360 Pro2] ──Farsight무선──> [Windows PC]
      촬영                         다운로드 → ProStitcher stitch
                                   → equirect.jpg 업로드
                                        │ HTTP POST :7866
                                        ▼
                                   [서버 sysai-server2]
                                   pano_receiver 수신 (디스크 저장)
                                        │ 폴더 감시
                                        ▼
                                   anchor360_live.py (상주)
                                   e2p 슬라이스(메모리) → YOLO-World 검출
                                   → 필터 → NMS → 바닥평면 pos3d
                                   → JSON 저장
                                        │ HTTP GET (PC가 당겨감)
                                        ▼
                                   [Windows PC]
                                   anchor360_bridge.py
                                        │ UDP :5010
                                        ▼
                                   [Unity PC 192.168.50.244]
                                   Anchor360ObstacleReceiver → 큐브 스폰
```

**Unity 코드는 한 줄도 수정하지 않았다.** 팀원(조병현) 소유 코드를 건드리지 않는 것을
설계 제약으로 두고, 접점을 "UDP 5010 + 기존 JSON 스키마" 하나로 고정했다.

## 1.2 검증 증거

| 구간 | 증거 |
|---|---|
| PC → 서버 | `[worker] ✅ uploaded PIC_...` ↔ `[pano] saved: .../equirect.jpg (3208KB)` |
| 서버 검출 | `[live] #N ... 전송 N개 ... 총 0.14s` |
| 서버 → PC | 브릿지 `NEW ts=1789447281.457 검출 6개 2723B` |
| PC → Unity | Unity Console `[Anchor360] RX: { "timestamp": "1789447281.457", ...}` |
| Unity 반영 | Hierarchy `Anchor360_Obstacles` 아래 큐브 6개, JSON과 개수·이름·순서 일치 |
| 로거 | `[CubeLogger] 로봇 큐브 0개, 360 큐브 6개` |

**브릿지의 `ts`와 Unity `RX`의 `timestamp`가 정확히 일치** → 관통 확인.

## 1.3 7월 미결 항목 해소

2026-07-11 인수인계 문서에 체크되지 않은 채 남아 있던 항목:

> `□ 릴레이 확보 (서버→노트북→유니티, relay_to_unity/relay_loop). 5010 릴레이 미적용 상태`

이 항목이 `anchor360_bridge.py`로 닫혔다. 과거에는 서버에서 JSON을 뽑아 랩실 PC로
수동 복사한 뒤 `relay_loop.py`로 쏘는 방식("내부망 PC 직송")이었다.

---

# 2. 환경 정보

## 2.1 장비 및 주소

| 장비 | 주소 | 비고 |
|---|---|---|
| Insta360 Pro2 | `192.168.100.124` | Farsight DHCP. **전원 켤 때마다 바뀔 수 있음** |
| Pro2 명령 API | 포트 `20000` | `/osc/commands/execute`, `/osc/state` |
| Pro2 파일 다운로드 | 포트 `8000` | `_picUrl` 기반 개별 GET |
| 서버 sysai-server2 | `210.110.250.33` | 학교망. SSH 8522. RTX A6000 |
| 촬영 PC (내 PC) | `192.168.50.203` (Wi-Fi 2) | 서버에는 `203.230.104.168`(NAT 공인)로 보임 |
| Unity PC (팀원) | `192.168.50.244` | 랩실 사설망 |

**Farsight ↔ 노트북은 LAN 케이블.** USB-C는 폰/태블릿용 공식 방법이다.

## 2.2 네트워크 제약 — 가장 중요한 구조적 사실

**서버(210.110.250.33) → Unity PC(192.168.50.244) UDP 직송은 불가능하다.**

```
$ ip -4 addr show | grep inet
    inet 127.0.0.1/8 scope host lo
    inet 210.110.250.33/24 brd 210.110.250.255 scope global enp193s0f0
    inet 172.17.0.1/16 ... docker0
    inet 172.18.0.1/16 ... br-8da169f67baa
    inet 10.42.0.0/32 ... flannel.1
    inet 10.42.0.1/24 ... cni0

$ ip route get 192.168.50.244
192.168.50.244 via 210.110.250.1 dev enp193s0f0 src 210.110.250.33

$ ping -c 2 192.168.50.244
2 packets transmitted, 0 received, 100% packet loss
```

서버에 `192.168.50.x` 인터페이스가 없다. 커널이 "모르는 주소 → 기본 게이트웨이"로
처리하고, 학교망 게이트웨이는 사설 대역을 어디로 보낼지 모르므로 폐기한다.

**함정: UDP `sendto()`는 이 상황에서도 계속 성공을 반환한다.**
"커널 버퍼에 넣었다"에서 성공이므로 도달 여부와 무관하다. 그래서 `UDP OK`,
`sent 2723 bytes` 같은 로그를 근거로 "전송 성공"이라 판단하면 안 된다.
**이것 때문에 원인 파악이 지연됐다.**

반대 방향(PC → 서버)은 NAT를 통과하므로 정상 동작한다. 그래서 **PC가 서버에서
당겨오는 구조**로 뒤집었다. 이것이 `anchor360_bridge.py`의 존재 이유다.

이 제약은 2026-07-11 기록에 이미 명시되어 있었다:
> "서버(210.110.250.33) ↔ Unity PC가 다른 망이라 직송 불가."

즉 **오늘 새로 생긴 문제가 아니며, 서버 재설치 탓도 아니다.**

## 2.3 소프트웨어 환경

**서버**
- venv: `venv_image` (카메라/비전 관련 venv를 통합한 것. 과거 `venv_stitch` 등을 흡수)
- `py360convert`, `ultralytics(YOLOWorld)`, `opencv`, `yaml`, `fastapi`, `uvicorn` 모두 설치 확인
- CLIP 캐시: `/home/dg/capstone-db/docx2026/weights/clip/ViT-B-32.pt` (약 338MB)

**PC**
- Python 3.12.10 (`C:\Users\asdfa\AppData\Local\Programs\Python\Python312`)
  - Python 3.8도 남아 있으나 `where python` 우선순위는 3.12
- `requests 2.34.2` (2026-09-15 설치)
- ProStitcher: `C:\Program Files (x86)\Insta360Stitcher\tools\prostitcher\ProStitcher.exe`

**Unity**
- Unity 6.3 LTS (6000.3.5f2), 프로젝트 `Go1_Unity`, 씬 `lab`

---

# 3. 파일 명세

## 3.1 PC 측 — `C:\Users\asdfa\docx2026_code`

### `pro2_auto_bg.py` — 촬영 자동화 (최종본)

**구조**
- 메인 스레드: 촬영 → 완료대기 → 다운로드(RAM) → 디스크 저장 → 큐에 투입 → 즉시 다음 촬영
- 워커 스레드 1개: 큐에서 꺼내 stitch → 서버 업로드 (순차)
- 워커 1개인 이유: GTX 1080 메모리 보호 + ProStitcher 동시 실행 위험 회피

**옵션**

| 옵션 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `--count` | int | `0` | 촬영 장수. 0 = 무한 |
| `--interval` | float | `0` | 촬영 **시작 시각 기준** 주기(초). 0 = 연속 |
| `--width` | int | `3840` | equirect 가로 |
| `--height` | int | `1920` | equirect 세로 |
| `--mode` | str | `ai` | `ai` / `optical` / `template` 블렌딩 |
| `--topfixer` | int | `1` | 천장 최적화 0/1 |
| `--blender` | str | `auto` | 블렌더 백엔드 |

기본값이 이미 무한·연속이므로 `python pro2_auto_bg.py` 한 줄이면 된다.

**주요 상수 (코드 내부)**
```python
CAMERA_IP   = "192.168.100.124"
SERVER_IP   = "210.110.250.33"
SERVER_PORT = 7866
CAMERA_ID   = "pro2_anchor"
DOWNLOAD_FILES = [origin_1~6.jpg, pro.prj, gyro.mp4]
LOCAL_BASE  = "captures"
_TRT_BACKUP_DIR = r"C:\Users\asdfa\docx2026_code\trt_cache"
```

**내장 기능**
- `sync_trt_cache()`: 기동 시 `%TEMP%`의 TensorRT 엔진 캐시를 백업 폴더와 동기화.
  `%TEMP%`가 비면 복원(콜드 빌드 ~19분 방지), 최신 엔진이 있으면 백업 갱신
- `capture_id`에 PC 실시각 덧붙임: `PIC_20160212_013624__20260915_125551`
  (카메라 RTC 배터리 사망으로 전원마다 2016-02-12로 리셋 → 폴더명 충돌 회피)
- 하트비트 스레드 정지/재개: `_idRes` 폴링 race 회피
- 큐 3개 이상 적체 시 경고

**종료**: `Ctrl+C` → 진행 중 촬영 마치고 큐 비운 뒤 종료

### `anchor360_bridge.py` — 서버 JSON → Unity UDP 중계 (신규, 2026-09-15)

**왜 필요한가**: §2.2 참조. 서버→Unity 직송 불가. 방향을 뒤집어 PC가 당겨온다.

**동작**
1. `--poll` 주기마다 서버 `/anchor360_latest`를 HTTP GET
2. JSON 파싱 → `timestamp` 비교로 신규 여부 판단
3. UDP로 `--unity-ip:--port`에 송출
4. 새 내용이 없어도 계속 재송출 (**하트비트 겸용**)

**옵션**

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--server` | `http://210.110.250.33:7866/anchor360_latest` | JSON 엔드포인트 |
| `--unity-ip` | `192.168.50.244` | Unity PC IP (Unity PC에서 돌리면 `127.0.0.1`) |
| `--port` | `5010` | |
| `--poll` | `2.0` | 당겨오기/재송출 주기(초) |
| `--only-new` | off | 내용이 바뀐 경우에만 송출(하트비트 끄기) |
| `--quiet` | off | 새 패킷일 때만 로그 |

**하트비트가 중요한 이유**: 서버 `anchor360_live.py`는 사진 1장당 UDP 1회만 쏜다.
Unity를 늦게 켜면 다음 사진(19초 뒤)까지 화면이 빈다. 실제로 이 상황을 겪었다
(사진 13:02~13:03 전송, Unity 13:09:49 기동 → 큐브 0개).
브릿지가 2초마다 재송출하므로 Unity를 언제 켜도 몇 초 안에 채워진다.

**설치 위치 판단**
- Unity PC에서 실행하면 홉 1개 (`--unity-ip 127.0.0.1`)
- 내 PC에서 실행하면 홉 2개지만 **팀원 PC를 전혀 건드리지 않음**
- → **내 PC 실행을 채택.** 팀원 PC에 파이썬/패키지 설치·터미널 점유·장애 시 대응
  요청이 모두 불필요해지는 것이 홉 1개보다 이득. 사진 주기 19초 대비 2초 폴링과
  2~3KB JSON은 비용이 사실상 0.

### 기타 PC 파일

| 파일 | 역할 |
|---|---|
| `pro2_auto.py` | 1회 테스트(직렬, 단계별 시간 측정) |
| `stitch_prostitcher.py` | ProStitcher CLI 호출. `stitch_folder()` import용. `--benchmark` |
| `pro2_pipeline.py` | 구버전(origin 6장 전송). 참고용 |
| `trt_cache/` | TensorRT 엔진 백업 (`*_fp16.trt`) |

---

## 3.2 서버 측 — `~/capstone-db/image_server`

### `pano_receiver.py` — 파노라마 수신 서버

**포트 7866** (원래 7865였으나 로봇 B가 점유하여 이전)

| 엔드포인트 | 메서드 | 설명 |
|---|---|---|
| `/upload_pano` | POST | `capture_id`, `camera_id`, `file` multipart 수신 |
| `/pano_health` | GET | 생존 확인 |
| `/anchor360_latest` | GET | **2026-09-15 추가.** 검출 JSON 반환 (브릿지용) |

**저장 경로**: `pano_captures/{camera_id}/{capture_id}/{원본파일명}`
→ 실제로는 `pano_captures/pro2_anchor/PIC_..._.../equirect.jpg`

**2026-09-15 적용한 수정 2건**

1. **원자적 쓰기** — 3MB를 쓰는 도중 감시기가 집으면 깨진 JPEG을 읽는다.
```python
data = await file.read()
tmp_path = save_path + ".part"
with open(tmp_path, "wb") as f:
    f.write(data)
os.replace(tmp_path, save_path)      # 같은 FS 내에서 원자적
```

2. **GET 엔드포인트 추가**
```python
from fastapi.responses import FileResponse

LATEST_JSON = os.path.expanduser(
    "~/capstone-db/docx2026/live_out/anchor360_latest.json")

@app.get("/anchor360_latest")
async def anchor360_latest():
    if not os.path.exists(LATEST_JSON):
        return {"status": "empty"}
    return FileResponse(LATEST_JSON, media_type="application/json")
```

**함정**: SSH 세션이 끊기면 같이 죽는다. `nohup` 또는 tmux 필수.
**미수정**: 상단 docstring에 포트가 아직 7865로 적혀 있다(코드는 7866).

---

## 3.3 서버 측 — `~/capstone-db/docx2026`

### `anchor360_live.py` — 실시간 파이프라인 데몬 (신규, 2026-09-15)

`detect_send_3b2.py`를 "상주 + 감시 루프" 형태로 재구성한 것.
**검출 필터 / NMS / `pixel_to_ground` 수식 / JSON 스키마는 `detect_send_3b2.py`와 동일**
(검증된 수식이라 한 줄도 바꾸지 않음).

**핵심 3가지**

1. **모델 상주** — `YOLOWorld` 로드(+워밍업)를 기동 시 1회만.
   기존 수동 방식은 사진마다 8~9초가 들었다. 이제 기동 시 1회로 빠진다.
2. **메모리 슬라이스** — `e2p` 결과를 파일로 안 쓰고 바로 검출에 넘긴다.
   equirect를 `cv2.imread`(BGR)로 읽으므로 슬라이스도 BGR → 채널 변환 불필요.
   JPEG 재인코딩 손실 단계가 하나 적어 **원본 equirect에 더 충실하다**.
   (단 "더 정확하다"는 아니다 — 줄자 GT와 대조한 것이 아니라 두 경로를
   서로 비교한 것뿐이며, 차이 구간에서 어느 쪽이 실제에 가까운지는 미검증. §7.1)
3. **감시 루프** — `watch_dir/*/equirect.jpg` 중 mtime 최신을 집어 처리.
   같은 파일은 두 번 처리하지 않는다.

**클래스 구조**

| 클래스/함수 | 역할 |
|---|---|
| `Config` | config yaml에서 weight/classes/conf/group/class_conf/iou 로드 |
| `GroundProjector` | `pixel_to_ground` — 슬라이스 픽셀 → 바닥평면(y=0) 교차 |
| `Detector` | YOLOWorld 상주, `warmup()`, `detect()`(필터+NMS 포함) |
| `slice_equirect()` | `py360convert.e2p` 메모리 슬라이스 |
| `build_packet()` | pos3d/size 계산 + 품질 필터 + JSON 조립, `dropped` 목록 반환 |
| `draw_debug()` | 전송한 검출만 그린 시각화 |
| `find_latest()` | watch_dir에서 mtime 최신 equirect 탐색 |
| `wait_stable()` | 파일 크기 안정화 대기(원자적 쓰기의 안전망) |

**옵션 전체**

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--watch-dir` | `~/capstone-db/image_server/pano_captures/pro2_anchor` | 감시 루트 |
| `--cfg` | `~/capstone-db/image_server/unidepth_dual_yolo_config.yaml` | zeroshot config |
| `--weight` | (config 값) | YOLOWorld 가중치 경로 오버라이드 |
| `--out-dir` | `~/capstone-db/docx2026/live_out` | JSON/디버그 저장 |
| `--u` | `50.0` | 슬라이스 방위각(deg) |
| `--v` | `-35.0` | 슬라이스 고저각(deg) |
| `--slice-size` | `720` | 슬라이스 한 변 픽셀. **720 고정 권장(기하 전제)** |
| `--cam-h` | `1.85` | 카메라 높이(m) |
| `--pitch` | (=`--v`) | 바닥평면 계산용 하향각 |
| `--unity-ip` | `192.168.50.246` | UDP 대상 (**현재 무의미 — 브릿지가 실제 송신**) |
| `--port` | `5010` | |
| `--imgsz` | `640` | YOLO 추론 해상도 |
| `--poll` | `1.0` | 감시 주기(초) |
| `--drop-clipped` | off | 접지점이 프레임 밖으로 잘린 검출 제외 |
| `--max-dist` | (없음) | 이 거리(m) 초과 검출 제외. 랩실이면 6~8 권장 |
| `--debug-img` | off | 전송한 검출을 그린 슬라이스를 덮어쓰며 저장 |
| `--save-slice` | off | 검출에 쓴 슬라이스 원본을 타임스탬프 이름으로 백업 |
| `--slice-dir` | `live_out/slices` | `--save-slice` 저장 위치 |
| `--once` | off | 최신 사진 1장만 처리하고 종료 |
| `--replay <path>` | — | 지정 equirect 1장만 처리 (**카메라 없이 테스트**) |

**품질 필터 (2026-09-15 추가)**

- `--drop-clipped`: bbox 아랫변이 화면 밑변에 붙은 검출 제외.
  접지점이 프레임 밖이라 바닥평면 거리가 **원리적으로** 안 맞는다.
  실측 사례: 화면 아래로 잘린 책상이 `D=0.58m`로 검출됨.
- `--max-dist`: 지평선에 가까운 얕은 광선은 바닥과 거의 평행해서 픽셀 1~2개
  오차가 미터 단위로 증폭된다. 실측 사례: `box conf=0.13 D=11.88m wy=-0.346`.

걸러낸 항목도 이유와 함께 콘솔에 출력한다:
```
[제외:    접지잘림] table           D= 0.58  conf=0.23
[제외:     >8m] storage box     D=11.23  conf=0.17
```

**출력물**
- `live_out/anchor360_latest.json` (원자적 쓰기)
- `live_out/anchor360_latest.jpg` (`--debug-img`, 덮어쓰기)
- `live_out/slices/{UTC타임스탬프}__{capture_id}__u+50_v-35.jpg` (`--save-slice`)

**JSON 스키마** (Unity `Anchor360ObstacleReceiver` 무수정 호환)
```json
{
  "timestamp": "1789447281.457",
  "camera_id": "anchor360",
  "frame": "anchor360",
  "anchor_dir": { "u_deg": 50.0, "v_deg": -35.0, "roll_deg": 0 },
  "detections": [
    {
      "id": 1, "name": "person", "group": "AGENT",
      "pos3d": { "x": -0.144, "y": 0.0, "z": 1.071 },
      "dist_m": 1.08,
      "size": { "w": 0.456, "h": 1.014, "d": 0.456 },
      "bbox_xyxy": [257, 189, ...],
      "ground_clipped": false,
      "conf": 0.91,
      "wy": -0.955
    }
  ]
}
```
`conf`, `wy`는 2026-09-15 추가(진단용). Unity `JsonUtility`는 모르는 필드를 무시하므로
수신기에 영향 없다. `id`는 **필터 후에** 매겨 번호에 구멍이 없다.

**CLIP 캐시 주의**: `YOLOWorld.set_classes()`가 쓰는 CLIP(ViT-B-32, 338MB)은
**CWD 아래 `weights/clip/`에 캐시**된다. 다른 폴더에서 띄우면 다시 받는다.
그래서 이 스크립트는 기동 시 자기 파일이 있는 디렉터리로 `chdir`한다
(사용자 인자는 그 전에 절대경로로 변환하므로 안전).

**검출 0개여도 빈 패킷을 보낸다** — Unity 수신기가 스냅샷 방식이라
사라진 장애물의 큐브가 자동으로 지워진다.

### 기존 detect 3종 — 차이

세 파일의 **검출·필터·NMS·`pixel_to_ground`는 완전히 동일**하다. 뒷단만 다르다.

| | `detect_ground_3b1` | `detect_send_3b2` | `detect_send_3b2_timed` |
|---|---|---|---|
| 결과물 | bbox 그린 **jpg 저장** | JSON + **UDP 1회 송출** | 3b2와 동일 |
| `pos3d`·`size` | 없음 | 있음 | 있음 |
| 콘솔 | X·Z·D·접지잘림·`wy` 표 | 검출 요약 | 검출 요약 + `[TIMING]` |
| `predict` 호출 | 1회 | 1회 | **2회**(워밍업 전/후 분리) |
| 3번째 인자 | `출력이미지` | `imgsz` | `imgsz` |

- `3b1` = **눈으로 확인용**. 카메라 새로 세우고 "잡히나, D가 줄자와 맞나" 볼 때.
- `3b2` = 실전 수동 송출. 폴백용으로 유지.
- `3b2_timed` = 성능 측정 전용. `predict` 2회라 **실시간 루프에 쓰면 안 된다.**

**인자 순서가 3번째부터 어긋나니 주의.**

### `e2p.py`

```python
persp = py360convert.e2p(equ, fov_deg=(90,90), u_deg=u, v_deg=v, out_hw=(720,720))
```
`fov 90×90`, `720×720` 고정. 이것이 `detect_*`의 `fx=fy=360, cx=cy=360` 전제와
맞물려 있어 **셋 다 720을 건드리면 안 된다.** fov를 바꾸면 거리가 통째로 틀어진다.

### `relay_loop.py`

```
python relay_loop.py [json파일] [유니티IP] [포트] [주기초]
기본:  relay_loop.py obstacles_360.json 192.168.50.246 5010 1.0
```

**파일을 루프 바깥에서 1회만 읽는다.** 실행 중 JSON이 바뀌어도 처음 읽은 내용만
계속 보낸다. 이것은 버그가 아니라 의도된 설계로, 용도는
**"고정 JSON을 계속 쏘는 동안 Unity Inspector에서 `yawOffsetDeg`/`invertX`/
`distanceScale`을 돌려가며 정렬을 맞추는 것"**이다. 데이터가 고정돼야 파라미터
효과만 분리해서 볼 수 있다.

**실시간 갱신용으로 쓰면 안 된다.** 또한 **서버에서 돌리면 §2.2 때문에 막힌다** —
랩실 내부망 PC에서 돌려야 한다. 이것이 과거 "내부망 PC 직송" 방식의 실체다.

### 기타 서버 파일

| 파일 | 상태 |
|---|---|
| `crop_center.py`, `crop_go1.py` | 로봇 이미지 크롭 (MASt3R 계열) |
| `robot_bbox.py`, `mast3r/match_vote.py`, `rescore*.py`, `truth.json`, `out_m3/` | MASt3R 매칭 판별자 (논문용, 현행) |
| `check_pose_sparse*.py`, `check_pose_diag_5ap.py` | MASt3R 자세추출 — **폐기** |
| `relay_to_unity.py` | **서버에 부재** (`ls` 종료코드 2). `relay_loop.py`로 대체된 것으로 보임 |

---

## 3.4 Unity 측

### `Anchor360ObstacleReceiver.cs` — **수정하지 않음**

| 항목 | 값 |
|---|---|
| 수신 포트 | UDP **5010** |
| 방식 | 스냅샷 (패킷마다 기존 큐브 전멸 후 재생성) |
| 루트 오브젝트 | `Anchor360_Obstacles` (Start에서 생성) |
| 조정 파라미터 | `yawOffsetDeg`, `invertX`, `distanceScale`, `floorWorldY` |
| 크기 | `minCubeSize 0.15`, `maxCubeSize 3.0`, `defaultCubeSize 0.4`, `sizeMultiplier 1.0` |
| Tag/Layer | `Obstacle` / `Obstacle` |
| 디버그 | `printReceivedJson` |

**좌표 변환 (코드 실제 동작)**
```csharp
float lx = det.pos3d.x * (invertX ? -1f : 1f) * distanceScale;
float lz = det.pos3d.z * distanceScale;
Vector3 local = yawFix * new Vector3(lx, 0f, lz);   // yawFix = Euler(0, yawOffsetDeg, 0)
Vector3 world = anchorTransform.TransformPoint(local);
Vector3 pos   = new Vector3(world.x, floorWorldY + scale.y * 0.5f, world.z);
```

즉 전체 변환은
```
world = R_anchor × (Yaw(yawOffsetDeg) × local) + P_anchor
```

**중요 정정**: 2026-07-11 대화에서 "유니티 Transform이 회전·이동을 같이 처리해서
재현 불가"라고 결론 내렸는데, **이는 사실이 아니다.** `TransformPoint`는 위 식대로
회전 후 이동을 순서대로 적용할 뿐이며 완전히 재현 가능하다. 당시 재현 실패의
원인은 `P_anchor`를 빼지 않았거나 `R_anchor`를 0으로 가정한 것으로 추정된다.
**이 잘못된 진단 때문에 검산을 포기하고 눈대중 방식으로 이어졌다.**

### `Go1ObstacleJsonReceiver.cs` — 참고

- 기본값 `listenPort = 5009`, **실제 운용은 5011** (Inspector에서 변경)
- 두 수신기 사이 **병합 로직 없음.** 각자 자기 컬렉션만 관리
- Go1 내부 병합은 공격적: 같은 클래스 0.45m, 다른 클래스 0.35m, 근접 흡수 0.55m

### Unity 씬 오브젝트

```
lab
├── go1, go1 (1)
├── CubeLogger
├── Anchor360                 ← Position (3.2, 1.85, 6.24) / Rotation Y 미확인
├── GO1_Detected_Obstacles
└── Anchor360_Obstacles       ← 런타임 생성
    ├── Obs360_person_1
    ├── Obs360_chair_2
    ├── Obs360_chair_3
    ├── Obs360_person_4
    ├── Obs360_cardboard box_5
    └── Obs360_box_6
```

---

# 4. 확정 파라미터

## 4.1 카메라 기하

| 항목 | 값 | 비고 |
|---|---|---|
| 카메라 높이 `CAM_H` | **1.85 m** | 렌즈중심 높이. Unity Anchor360 Position Y와 일치해야 함 |
| 하향각 `PITCH` | **−35°** | roll 0 |
| 슬라이스 FOV | 90° × 90° | |
| 슬라이스 해상도 | 720 × 720 | |
| 합성 핀홀 | `fx = fy = 360`, `cx = cy = 360` | `W/(2·tan45°) = W/2`로 계산 확정, 캘리브레이션 불필요 |
| 좌표 규약 | +x 오른쪽, +y 위(바닥 y=0), +z 슬라이스 정면 | 원점 = 카메라를 바닥에 수직 투영한 점 |

## 4.2 슬라이스 각도

| 슬라이스 | `u_deg` | 대응 영역 |
|---|---|---|
| f1 | **+50** | 왼쪽 복도 |
| f2 | **+150** | 중앙 사각지대 |
| f3 | **+230** | 오른쪽 복도 |

`u = 0`은 카메라 정면(전원버튼 쪽), **+u는 반시계 방향** (py360convert 규약).

## 4.3 `yawOffsetDeg` — ⚠️ 불일치 발생

| 슬라이스 | 과거 기록 (~2026-08) | 2026-09-15 관측 |
|---|---|---|
| u+50 (왼쪽) | **−35** | **−45 ~ −65**, 그중 **−50**이 가장 근사 |
| u+150 (중앙) | +150 | 미측정 |
| u+230 (오른쪽) | −45 | 미측정 |

**이 사실을 반드시 기록으로 남긴다.** 다음에 `-35`를 그대로 넣으면 큐브가 실제보다
오른쪽으로 치우친다. 원인 규명은 §8 참조.

## 4.4 검출 설정 (config)

경로: `/home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml`
**소유자 `jny`, 권한 777, 최종수정 2026-07-12** — 팀원 공유 파일이므로 언제든 바뀔 수 있다.

```yaml
models.zeroshot_detector.weight: /home/dg/capstone-db/image_server/yolov8s-world.pt
models.zeroshot_detector.conf:   0.12
models.zeroshot_detector.imgsz:  320        # 실행 시 640으로 오버라이드 중
fusion.iou:                      0.45
classes.zeroshot:                59개
```

**주요 `class_conf.zeroshot`**
```
default 0.25 | person 0.35 | chair 0.22 | table 0.22
box 0.12 | cardboard box 0.15 | storage box 0.15 | luggage 0.15
fire extinguisher 0.12 | trash bin 0.22
robot 0.25 | quadruped robot 0.25 | robot dog 0.25
```

**필터**: `KEEP_GROUPS = {HARD_OBSTACLE, AGENT}` + 바닥접지. 경로 교차 판정은
Unity/NavMesh에 위임.

## 4.5 포트 정리

| 포트 | 용도 |
|---|---|
| 7864 | `server_stream_depth_v2` (GO1 스트리밍) |
| 7865 | 로봇 B 점유 |
| **7866** | **`pano_receiver`** (Pro2 수신 + JSON 제공) |
| 5009 | `Go1ObstacleJsonReceiver` 기본값 |
| **5010** | **`Anchor360ObstacleReceiver`** |
| 5011 | `Go1ObstacleJsonReceiver` 실제 운용 |
| 15100/15101/15103/15104 | UTM 로봇 제어 (별개 계통) |
| 20000 / 8000 | Pro2 명령 / 파일 다운로드 |

---

# 5. 구동 절차

터미널 4개 + Unity 1개. 서버 2 / 내 PC 2.

## 5.1 카메라 켜기

1. **360° 카메라 전원** → **송신기 켜고 15초 대기** → **수신기(Farsight) 켜고 파란불 대기**
2. 카메라 화면에서 **IP 확인** (Farsight DHCP라 바뀔 수 있음)
3. 내 PC에서 도달 확인
```
ping -n 2 192.168.100.124
```
> IP가 다르면 `pro2_auto_bg.py`의 `CAMERA_IP` 상수 수정

## 5.2 서버 기동 (터미널 2개)

**(1) 사진 수신 서버** — 이게 꺼져 있으면 업로드도 브릿지도 다 막힌다
```bash
cd ~/capstone-db && . venv_image/bin/activate
cd ~/capstone-db/image_server
nohup python3 pano_receiver.py > pano.log 2>&1 &
tail -f pano.log
```

**(2) 자르기 + 검출 + JSON** (새 터미널)
```bash
cd ~/capstone-db && . venv_image/bin/activate
cd ~/capstone-db/docx2026
python3 anchor360_live.py --drop-clipped --max-dist 8 --debug-img --save-slice
```
> `새 사진 대기 중...`이 뜨면 준비 완료

## 5.3 내 PC 기동 (터미널 2개)

**(3) Unity 중계기**
```
cd C:\Users\asdfa\docx2026_code
python anchor360_bridge.py --unity-ip 192.168.50.244
```

**(4) 촬영 자동화** (새 터미널) — 기본값이 무한·연속
```
cd C:\Users\asdfa\docx2026_code
python pro2_auto_bg.py
```

## 5.4 Unity

1. **재생(Play)**
2. Hierarchy `Anchor360` 선택 → Inspector `Yaw Offset Deg` 설정
   (2026-09-15 기준 **−50** 근처. §4.3 참조)

> 브릿지가 2초마다 재송출하므로 Unity는 언제 켜도 몇 초 안에 큐브가 채워진다.

## 5.5 ⚠️ 순서 주의

**`anchor360_live.py`는 촬영보다 먼저 띄워야 한다.** 기동 시 "기존 최신 사진은
건너뜁니다"를 하고 새 사진만 기다리므로, 나중에 띄우면 이미 올라온 한 장을 놓치고
다음 장(19초 뒤)부터 처리한다.

## 5.6 정상 동작 확인 — 5곳

| 위치 | 떠야 하는 것 |
|---|---|
| PC 촬영 | `촬영 #N 완료` → `[worker] stitched` → `✅ uploaded` |
| 서버 `pano.log` | `[pano] saved: .../equirect.jpg (~3200KB)` |
| 서버 live | `[live] #N ... 전송 N개 ... UDP OK` |
| PC 브릿지 | `NEW ts=...` (타임스탬프가 바뀜) |
| Unity | `[Anchor360] RX:` + `Anchor360_Obstacles` 아래 큐브 |

**브릿지의 `ts`와 Unity `RX`의 `timestamp`가 같으면 관통 확인.**

## 5.7 종료 (역순)

```
(4) 촬영       Ctrl+C   (큐에 남은 stitch 마치고 종료)
(3) 브릿지     Ctrl+C
(2) live       Ctrl+C
Unity          재생 정지
(1) receiver   그대로 두거나  pkill -f pano_receiver
카메라·송수신기 전원
```

## 5.8 카메라 없이 테스트

```bash
EQ=~/capstone-db/image_server/pano_captures/pro2_anchor/PIC_.../equirect.jpg
python3 anchor360_live.py --replay $EQ --debug-img --drop-clipped --max-dist 8
```

---

# 6. 실측 시간

## 6.1 촬영계열 (PC)

| 구간 | 시간 | 비고 |
|---|---|---|
| `_takePicture` | ~10.5 s | **카메라 하드웨어 한계.** 해상도·파라미터 무관 |
| 저장 대기 | ~6 s | 하드웨어 한계 |
| 다운로드 | ~2 s | `ThreadPoolExecutor` 병렬화로 4→2초 |
| **촬영계열 소계** | **16.6 ~ 19.9 s** | 2026-09-15 실측 |

> 과거 기록 18.6초와 일치. RAM(`io.BytesIO`) vs 디스크 저장은 실측 차이 0.1초 미만
> 이었으므로 "RAM이 빠를 것"이라는 가정은 기각됨.

## 6.2 Stitch (PC, GTX 1080)

| 회차 | 시간 |
|---|---|
| 첫 회 (TRT 콜드 스타트) | **25.7 s** |
| 2회차 | 15.1 s |
| 3회차 | 19.1 s |
| 4회차 | 18.6 s |

> 과거 기록은 11.7~12.7초. 2026-09-15는 다소 느려졌으나 촬영계열과 비슷한 수준.
> **`큐 대기 0` 유지됐으나 여유가 적다.**
> 실제 stitch 연산은 0.5초이고 나머지는 AI flownet 모델 GPU 로딩(약 6.5초)이다.
> ProStitcher가 Windows 전용이라 서버 A6000을 못 쓰는 것은 구조적 제약.

**벤치마크 (4K 3840×1920)**

| 순위 | 설정 | 시간 |
|---|---|---|
| 1 | template, tf0 | 6.6 s |
| 2 | optical, tf0 | 7.1 s |
| 3 | ai, tf0 | 8.2 s |
| 4 | **ai, tf1** | **12.7 s** ← 채택 |
| 5 | template, tf1 | 18.3 s |
| 6 | optical, tf1 | 18.8 s |

**품질 우선으로 `ai + topfixer=1` 채택.** template tf0이 가장 빠르지만 렌즈 경계의
근거리 물체에 ghosting/블러가 생기고, 이는 perspective로 잘라 검출할 때 정확도
저하로 직결된다. 백그라운드로 숨겨지므로 체감 0.

## 6.3 서버 처리 (`anchor360_live.py`)

| 구간 | 시간 |
|---|---|
| 모델 로드 (1회성) | 8.74 ~ 9.33 s |
| 워밍업 (1회성) | 0.73 ~ 0.88 s |
| equirect 읽기 | 0.05 ~ 0.09 s |
| 슬라이스 (메모리) | 0.01 ~ 0.10 s |
| **추론 (warm)** | **0.02 ~ 0.05 s** |
| **사진 1장당 총계** | **0.11 ~ 0.23 s** |

**촬영 주기 대비 약 1%.** 병목은 전적으로 카메라 하드웨어이며 서버는 놀고 있다.

> 이 단계별 시간이 매 사진마다 자동으로 로그에 남는다. 2026-07-24 회의에서 교수님이
> 지적하신 "20초 중 어느 단계에서 걸리는지 분리 측정하라"에 대한 답이 기록으로 쌓인다.

## 6.4 전체 주기

- **첫 사진**: 약 30 s (stitch를 겹칠 대상이 없어 전부 노출)
- **2장째부터**: 약 **19 s** (stitch가 다음 촬영 시간 안에 은닉)
- 브릿지 재송출: 2 s

---

# 7. 검증 실험 기록

## 7.1 메모리 슬라이스 vs 파일 경유 — 동일성 확인

같은 equirect(`PIC_20160212_013016__20260803_224150`)로 두 경로를 비교.

| 물체 | 파일 경유 (X, Z, D) | 메모리 (X, Z, D) | 차이 |
|---|---|---|---|
| chair | 0.24, 1.33, 1.35 | 0.25, 1.33, 1.35 | 1 cm |
| cardboard box | −1.22, 0.43, 1.29 | −1.22, 0.43, 1.29 | 0 |
| cardboard box | −0.56, 1.58, 1.68 | −0.55, 1.58, 1.67 | 1 cm |
| chair | −1.67, 1.87, 2.51 | −1.67, 1.88, 2.51 | 1 cm |
| cardboard box | −0.23, 2.52, 2.53 | −0.22, 2.51, 2.52 | 1 cm |
| cardboard box | 0.24, 2.41, 2.42 | 0.24, 2.40, 2.41 | 1 cm |
| cardboard box | −0.95, 2.57, 2.74 | −0.94, 2.56, 2.72 | 2 cm |
| person | −1.80, 2.51, 3.09 | −1.76, 2.47, 3.04 | 5 cm |
| chair | −2.71, 2.31, 3.56 | −2.70, 2.31, 3.56 | 1 cm |
| cardboard box | −1.65, 4.79, 5.07 | −1.63, 4.72, 5.00 | 7 cm |

**차이가 거리에 비례한다** (가까운 것 0~1cm, 5m짜리 7cm). 원인은 파일 경유의
**JPEG 왕복 손실**로, bbox가 픽셀 한두 개 움직이면 원근 투영 때문에 먼 물체일수록
증폭된다. 기하 계산(`fx/fy/cx/cy`, `CAM_H`, `PITCH`, `pixel_to_ground`)은 두 경로가
완전히 동일하며, 달라지는 것은 입력 픽셀뿐이다.

**어느 쪽이 더 정확한지는 이 실험으로 알 수 없다.** 줄자 GT와 대조한 것이 아니라
두 경로를 서로 비교한 것이기 때문이다. 말할 수 있는 것은 **메모리 쪽이 손실 단계가
하나 적어 원본 equirect에 더 충실하다**는 것까지다.

**이 결과가 §8의 가설 6(`live.py` 계산 오차)을 기각하는 근거다.** 두 가지 이유:
- **크기**: 최대 7cm인데, `-35`↔`-50`의 15° 차이는 2.7m 물체에서 약 70cm의
  횡방향 변위를 만든다. 한 자릿수 cm로는 15°를 설명할 수 없다.
- **성질**: 오차가 거리에 비례하는 것은 bbox 픽셀 흔들림의 패턴이다.
  회전 오차라면 각도가 일정한 호 방향 변위로 나타나야 한다. 모양이 다르다.

(채널 순서 BGR/RGB가 틀렸다면 검출이 통째로 무너졌을 것이므로, 10개가 cm 단위로
일치한 것은 채널 처리도 정상임을 함께 보여준다.)

**또한 이 실험이 "메모리 처리 때문에 오검출이 늘었다"는 가설을 기각한다.**
파일 경유도 `box conf 0.12 D=6.88m`(랩실에 있을 수 없는 값, `wy=-0.339`)라는
같은 종류의 쓰레기를 냈다. 차이는 **클래스별 conf 임계값 경계**(`box 0.12`,
`table 0.22`)에서 어느 것이 들어오고 나가느냐일 뿐이다.

## 7.2 실시간 좌표 재현성

연속 촬영 3장의 검출 좌표 (D, m):

| 물체 | #2 | #3 | #4 |
|---|---|---|---|
| chair (가까운) | 1.36 | 1.36 | 1.36 |
| chair (먼) | 2.64 | 2.63 | 2.66 |
| box | 5.42 | 5.45 | 5.43 |
| person | 3.50 | 2.10 | 3.59 |

**정적 물체는 센티미터 단위로 고정, 사람만 움직인 만큼 변한다.**
실시간 동작의 직접 증거다.

## 7.3 카메라 물리 각도 변화 여부 — 배제

`20260712_t2_u+50_v-35_test.jpg`(7월)와
`20260915_074055__PIC_..._u+50_v-35.jpg`(9월) 픽셀 대조.

| 지형지물 | 2026-07-12 | 2026-09-15 |
|---|---|---|
| 벽 포스터(빨간 원 그림) 중심 | x≈285 | x≈280 |
| 책장 왼쪽 모서리 | x≈383 | x≈386 |
| 책장 오른쪽 모서리 | x≈540 | x≈540 |
| 우측 벽 파란 종이 | x≈607 | x≈607 |
| 바닥 ㄱ자 테이프 마커 | 같은 자리 | 같은 자리 |

**어긋남 한 자릿수 픽셀 = 1~2° 이내.**
720px/90° 기준 **중앙에서 1° ≈ 6px, 가장자리에서 1° ≈ 13px**이며,
`-35`와 `-50`의 차이인 15°는 **약 94px(프레임 폭 13%)**여야 한다.
→ **카메라 물리 각도 변화는 원인이 아니다.**

달라진 것은 장면 내용뿐이다(중앙 박스·로봇 케이스 더미 → 옷걸이·삼각대,
소화기 사라짐, 사람 위치 변경).

---

# 8. 미해결 — `yawOffsetDeg` 불일치 고찰

## 8.1 현상

과거 `-35`로 맞았던 u+50 슬라이스가, 2026-09-15에는 `-45 ~ -65`(그중 `-50`이 최적)를
줘야 현실과 근사했다. 카메라 사진의 구도는 거의 동일하다.

## 8.2 과거 기록 3값의 내부 모순

`yawOffsetDeg`는 슬라이스 로컬 좌표를 앵커 프레임으로 되돌리는 각도다.
카메라가 한 대면 `yaw = -u + C` 형태로 **하나의 공통 상수 C**로 설명돼야 한다.

| 슬라이스 | u | 기록 yaw | 역산 C |
|---|---|---|---|
| 왼쪽 | 50 | −35 | **+15** |
| 가운데 | 150 | +150 | **−60** |
| 오른쪽 | 230 | −45 | **−175** |

**C가 190° 범위로 흩어진다. 하나의 카메라 방향에서 나올 수 없다.**

특히 u+50과 u+230은 실제로 180° 반대 방향인데 yaw가 10°밖에 차이 나지 않는다.
이 값들을 그대로 쓰면 좌·우 복도 큐브가 거의 같은 자리에 겹친다.

→ **세 값은 각 슬라이스를 따로따로 눈대중으로 맞춘 결과**이며,
카메라 방향에서 유도된 값이 아니다.

## 8.3 이론값

`R_anchor = 0`이라면 `yaw = -u`:

| 슬라이스 | u | 이론 yaw |
|---|---|---|
| 왼쪽 | 50 | **−50** |
| 가운데 | 150 | −150 |
| 오른쪽 | 230 | −230 ≡ **+130** |

**2026-09-15에 가장 잘 맞았던 −50이 정확히 u=50의 이론값이다.**
과거 −35는 여기서 15° 벗어난 값이다.

→ 질문을 뒤집어야 한다. "왜 −35가 안 되나"가 아니라 **"−35가 애초에 맞았나".**

## 8.4 왜 이 지경이 됐나 — 2026-07-11의 잘못된 진단

당시 다음과 같이 결론 내리고 검산을 포기했다.

> receiver가 실제로 하는 변환(`yawFix * local` 후 `anchorTransform.TransformPoint`)이
> 내 단순 2D 회전이랑 달라 — 유니티 Transform이 회전·이동을 같이 처리하거든.
> 그래서 부호를 뭘로 하든 내 계산으론 재현이 안 돼.
> → "유니티 화면이 진실이다"

**이것은 사실이 아니다.** §3.4에서 보듯 변환은
`world = R_anchor × (Yaw × local) + P_anchor`로 완전히 재현 가능하다.
재현 실패의 원인은 `P_anchor` 미차감 또는 `R_anchor` 0 가정으로 추정된다.

이 오진 때문에 **"계산으로 유도"가 폐기되고 "눈대중"이 표준이 되었다.**
그 결과가 §8.2의 모순된 3값이다.

## 8.5 가설 검증 현황

| # | 가설 | 상태 |
|---|---|---|
| 1 | 앵커 Position이 실제 카메라 위치와 어긋남 | **배제.** 예전 표시 자리 그대로, 줄자 확인, Y=1.85 확인 |
| 2 | 앵커 Rotation Y ≠ 0 | **미확인** ← 다음 할 일 |
| 3 | 과거 −35가 애초에 부정확 | **가능성 높음.** §8.2 모순 + §8.3 이론값 일치 |
| 4 | 기준 물체 빈약으로 회전이 결정 안 됨 | **관측됨.** −45~−65로 20° 범위 흔들림 |
| 5 | 카메라 물리 각도 변화 | **배제.** §7.3 픽셀 대조 1~2° 이내 |
| 6 | `live.py` 계산 오차 | **배제.** §7.1 최대 7cm, 방향 무관 |

## 8.6 가설 4 보충 — 눈대중의 구조적 한계

2026-09-15 yaw 조정 시 화면에 있던 기준 물체:
- `cardboard box conf 0.23 D=2.73` (임계값 0.15 근처)
- `box conf 0.13 D=5.50` (임계값 0.12 경계, `wy=-0.343` 얕은 광선)
- 사람 2명 (움직임)

과거에는 소화기·상자·의자가 1.2~1.7m에 접지 선명하게 있었다. **기준 자체가 달라졌다.**

**결정적 구분: 회전은 반경 방향 오차를 고칠 수 없다.**
박스가 실제 3m인데 5.5m로 잡히면 아무리 돌려도 제자리에 안 온다. 그런데 눈으로
"제일 비슷한" 각도를 찾다 보면 그 반경 오차를 보상하려고 yaw가 끌려간다.
**−45~−65로 20° 범위가 나온 것 자체가 이 증상이다.**
회전이 관측으로 제대로 결정되면 그렇게 흔들리지 않는다.

## 8.7 근본 해결 방향 — 계산으로 유도

대응점 **1개**면 회전이 풀린다.
```
yaw = atan2(맵에서 앵커→물체 방향) − atan2(슬라이스 로컬 x, z)
```
대응점 **2개 이상**이면 최소제곱으로 풀리고, **스케일 오차까지 같이 나온다.**
잔차가 크면 "yaw 문제가 아니라 거리 문제"라는 것이 드러난다 —
눈대중으로는 절대 갈라지지 않는 구분이다.

**절차 (카메라를 옮겨도 재현 가능)**
1. 카메라를 새 자리에 세운다
2. 맵 좌표를 아는 물체 2개를 시야에 둔다 (줄자로 벽 기준 실측)
3. 한 번 검출 → yaw와 스케일 계산
4. 그 값을 Inspector에 고정

## 8.8 결정적 실험 — 합성 JSON 2점 프로브

검출 오차와 눈대중을 **완전히 제거하고** receiver의 실제 변환을 확정하는 방법.
2026-07-11에 "재현 불가"로 닫힌 지점을 여는 실험이다.

```bash
python3 -c "
import json
pkt = {
  'timestamp':'0','camera_id':'anchor360','frame':'anchor360',
  'detections':[
    {'id':1,'name':'PROBE_FWD','group':'HARD_OBSTACLE',
     'pos3d':{'x':0.0,'y':0.0,'z':2.0},'dist_m':2.0,
     'size':{'w':0.3,'h':0.3,'d':0.3},'bbox_xyxy':[0,0,0,0],'ground_clipped':False},
    {'id':2,'name':'PROBE_RIGHT','group':'HARD_OBSTACLE',
     'pos3d':{'x':2.0,'y':0.0,'z':0.0},'dist_m':2.0,
     'size':{'w':0.3,'h':0.3,'d':0.3},'bbox_xyxy':[0,0,0,0],'ground_clipped':False}]}
open('probe.json','w').write(json.dumps(pkt))
"
```

`yawOffsetDeg = 0`으로 두고 송출한 뒤, Unity에서 `Obs360_PROBE_FWD_1`과
`Obs360_PROBE_RIGHT_2`의 Transform Position을 읽는다.

```
world_FWD − P_anchor = R_anchor × (0, 0, 2)
```

이 벡터의 방향이 곧 **`R_anchor`의 실제 값**이다. Inspector 숫자를 믿을 필요 없이
실측으로 나오며, `PROBE_RIGHT`까지 보면 **부호(좌우 뒤집힘)와 스케일**까지 확정된다.

결과가 나오면 `yaw = -u - R_anchor`로 **세 슬라이스 값을 계산으로 뽑을 수 있다.**

---

# 9. 검출 품질 관찰

## 9.1 소화기 미검출

`fire extinguisher` 임계값이 config에서 이미 **0.12(최저 등급)**인데도 검출되지 않았다.
`yolov8s-world.pt`(**small 모델**)의 한계로 보인다.

촬영 주기 19초 중 서버가 0.18초만 쓰므로 `yolov8x-worldv2`로 올려도 시간 예산에
여유가 충분하다. 앵커로 소화기를 다시 쓸 계획이 생기면 검토.

> 처음에는 "팀원이 임계값을 되돌렸을 것"으로 의심했으나, config 확인 결과 틀렸다.
> `jny` 소유·7월 12일 수정이라는 정황만으로 넘겨짚은 오진이었다.

## 9.2 불안정 검출 유형 2종

**(가) 접지잘림** — bbox 아랫변이 프레임 밑변에 붙어 접지점이 화면 밖.
바닥평면 거리가 원리적으로 안 맞는다. 예: `table D=0.58m`.
→ `--drop-clipped`

**(나) 얕은 광선** — 지평선 근처라 `wy`가 0에 가까움. 픽셀 오차가 미터로 증폭.
예: `box D=11.88m wy=-0.346`, `storage box D=11.2m`(값은 일정하나 부정확).
→ `--max-dist`

`wy` 참고값: 가까운 물체 −0.6 ~ −1.3 / 5m 물체 −0.375 / 11m 물체 −0.2대.
**`wy`가 0에 가까울수록 거리를 믿을 수 없다.**

## 9.3 임계값 경계 깜빡임

`box 0.12`, `table 0.22` 등 임계값에 딱 걸친 클래스는 프레임마다 들어왔다 나갔다 한다.
스냅샷 방식이라 큐브가 생겼다 사라지는 것으로 보인다.
필터로 거르는 것이 현실적이며, config 수정은 공유 파일이라 권장하지 않는다.

---

# 10. 폐기·비적용 항목

## 10.1 MASt3R 자세추출 — 완전 폐기 (2026-07)

`sparse_global_alignment` 기반 좌표 정합은 이 셋업에서 **비가용**으로 결론.

**폐기 근거 (5a′ 사다리 검증)**: 로봇을 5개 테이프 위치로 옮겨가며 측정했을 때
**5쌍 전부 dy > 0** — 로봇이 360°보다 1.55m 아래 있는 것이 물리적 사실인데
MASt3R는 5번 모두 로봇을 앵커보다 **위**에 놓았다. 거리도 GT 대비 사방팔방
(003 과소 1.44 vs 2.15, 001/005 과대 3.46/3.44 vs 1.80/3.00)이라
**스케일 보정으로 구제 불가**.

**근본 원인**: 어안-핀홀 모델 불일치. GO1 좌안은 어안 미보정 + 검은 테두리,
360° 쪽은 equirect에서 잘라낸 합성 핀홀인데 **가장자리 물체일수록 왜곡이 커서
focal 추정이 폭주**한다(중심 물체인 의자는 1.67로 안정).

**현재 MASt3R의 역할**: 자세는 안 뽑고 **"매칭 판별자"로만** 사용.
"360°가 본 물체 #i와 로봇이 본 물체 #j가 같은 물체인가"만 픽셀 대응으로 판정.
결과 16/16 (소화기 8/8, 상자 8/8, 오매칭 0). **논문용이며 이 파이프라인에는 없다.**

**→ 본 파이프라인에 MASt3R는 포함되지 않는다.** 360°는 자기 자신이 절대 기준이므로
정합이 필요 없다. 유니티로 가는 장애물 JSON은 zeroshot + `pixel_to_ground`만 쓴다.

## 10.2 세 슬라이스 통합 — 보류

현재 u+50 한 장만 운용. 슬라이스 하나면 좌표가 그 슬라이스 로컬이라
Unity `yawOffsetDeg` 하나만 맞추면 되고 회전 합성이 불필요하다.
대가는 커버 범위가 90°(360°의 1/4)로 줄어드는 것.

과거 `anchor360_zeroshot_v3.json`은 **손으로 합친 결과물**이다. 근거:
- 좌표가 이미 공통 좌표계 (u50은 x≈−1.19~−1.62, u230은 x≈+0.95~+1.39로 갈라짐)
  — 원시 출력이라면 양쪽 모두 z≈+1.x로 나온다
- `size`가 전부 `0.3/0.3/0.3` (실제 계산은 bbox×거리라 물체마다 다름)
- `bbox_xyxy`가 전부 `[0,0,0,0]`, 스키마에 없는 `slice`·`role`·`note` 필드 존재

**그때 쓴 회전 오프셋이 어느 파일에도 남아 있지 않다.**
세 슬라이스 통합으로 가려면 §8이 먼저 풀려야 한다.

---

# 11. 트러블슈팅 이력 (2026-09-15)

시간순. **"예전에 무슨 일이 있었나"를 되짚기 위한 기록.**

| # | 증상 | 진단 | 해결 |
|---|---|---|---|
| 1 | PC `ModuleNotFoundError: requests` | Python 3.12 업그레이드로 site-packages 초기화 | `python -m pip install requests` |
| 2 | `[trt] 캐시 복원` 발생 | `%TEMP%`가 또 비어 있었음 (Storage Sense 껐는데도). 원인 미확인 | `sync_trt_cache()`가 자동 복원 |
| 3 | `camera connect failed` | 카메라 충전 중 | 정상 동작 (3회 재시도 후 종료) |
| 4 | 338MB 다운로드 | **오진 2회.** ①"YOLO 가중치" ②"~/.cache/clip이라 재발 안 함" | 실제는 CLIP ViT-B-32, **CWD/weights/clip**에 캐시. `chdir`로 해결 |
| 5 | `relay_to_unity.py` 없음 | 서버에서 삭제됨 | `relay_loop.py`로 대체 확인 |
| 6 | Unity에 `[Anchor360] UDP 5010 수신 시작` 안 보임 | **실제로는 로그에 있었음** (로그 폭주로 묻힘). `Anchor360_Obstacles`도 존재 | — |
| 7 | 큐브가 안 뜸 (1차) | 사진 13:02~13:03 전송, Unity 13:09:49 기동. **1장당 1패킷이라 놓침** | 브릿지 하트비트로 구조적 해결 |
| 8 | 큐브가 안 뜸 (2차, 진짜) | **서버→Unity PC 경로 없음.** `ping` 100% 손실, `ip route`가 기본 GW로 나감 | `anchor360_bridge.py` (방향 역전) |
| 9 | 리스너 테스트가 무의미했음 | **서버에서 돌림.** Unity PC에서 돌려야 했음 | — |
| 10 | 브릿지 `연결 거부 (10061)` | `pano_receiver`가 꺼져 있었음 | 먼저 기동 |
| 11 | `[live] 종료. 처리한 사진 0장` | `--replay`/`--once`에서 `seq` 미증가 | 수정 |
| 12 | `table D=0.58`, `box D=11.88` 유령 큐브 | 접지잘림 + 얕은 광선. **메모리 처리 탓이 아님** (§7.1) | `--drop-clipped`, `--max-dist` |
| 13 | UTM 포트 15101/15104 충돌 | GO1_A와 GO1_B가 같은 포트 점유 시도 | **별개 계통, 미해결** |

## 11.1 이번에 드러난 판단 오류 (반복 방지용)

- **`UDP OK` / `sent N bytes`를 도달 근거로 삼았다.** UDP `sendto`는 커널 버퍼 투입에서
  성공을 반환한다. 이 때문에 8번 원인 파악이 지연됐다.
- **"로그가 안 보인다"를 "로그가 없다"로 받아들였다** (6번). 로그가 폭주하는 환경에서는
  검색·필터를 먼저 쓰거나, `Start()`가 만드는 부산물(`Anchor360_Obstacles`)을 봐야 한다.
- **정황만으로 원인을 넘겨짚었다** (9.1 소화기 임계값, 4번 CLIP 위치).
  파일을 실제로 열어 확인하기 전까지는 가설로만 다뤄야 한다.
- **테스트를 엉뚱한 기계에서 돌렸다** (9번). "어디서 실행하는가"가 결과의 의미를 바꾼다.

---

# 12. 알려진 함정 모음

| 항목 | 내용 |
|---|---|
| `pano_receiver` SSH 종속 | 세션 끊기면 죽음. `nohup`/tmux 필수 |
| 포트 방화벽 | 7866 `ufw allow` 필요했던 전례 있음 |
| TRT 캐시 삭제 | 콜드 빌드 ~19분. `trt_cache/` 백업 필수 |
| Pro2 RTC 배터리 사망 | 전원마다 2016-02-12로 리셋. `capture_id`에 PC 실시각 덧붙임으로 회피 |
| `Fingerprint` 헤더 | 철자 정확히. 틀리면 "error fingerprint none" |
| `gyro.mp4` | `enable=0`이어도 디스크에 존재해야 stitch XML 통과 |
| 카메라 IP 변동 | Farsight DHCP. 전원마다 확인 |
| 720px 고정 | `e2p`와 `fx/cx` 전제가 맞물림. fov 변경 시 거리 전체가 틀어짐 |
| `live.py` 기동 순서 | 촬영보다 먼저. 늦으면 1장 놓침 |
| 슬라이스 백업 파일명 UTC | `capture_id`는 KST. 한 파일명에 9시간 차이 혼재 |
| config 공유 파일 | `jny` 소유 777. 팀원이 바꾸면 검출이 말없이 달라짐 |
| 디스크 | `captures/` 시간당 ~3GB, `live_out/slices/` 시간당 ~13MB. 자동 정리 없음 |
| stitch vs 촬영 여유 | 촬영 16.6~19.9s, stitch 15.1~19.1s. 적체 시 `--interval 30` 또는 `--topfixer 0` |
| `detect_*` 인자 순서 | 3번째 인자가 3b1은 출력이미지, 3b2는 imgsz |
| `3b2_timed` | `predict` 2회. 실시간 루프에 쓰면 안 됨 |
| `relay_loop.py` | 파일을 1회만 읽음. 실시간 갱신 안 됨. 정렬 전용 |
| Unity 수신기 `OnDisable` 없음 | 컴포넌트만 껐다 켜면 소켓이 안 닫힘. Unity 재시작이 확실 |

---

# 13. 남은 일

## A. `yawOffsetDeg` 원인 규명 (최우선)

- **A-1. Anchor360 Rotation Y 읽기** — Inspector 숫자 하나. `yaw = -u - R_anchor`의 미지수
- **A-2. 합성 JSON 2점 프로브** (§8.8) — `R_anchor` 실측값 + 부호 + 스케일 확정
- **A-3. 기준 물체를 갖춘 상태에서 yaw 재측정** — 소화기·상자를 1~2m 복도 바닥에,
  conf 0.5 이상 접지 선명한 것으로
- **A-4. 결정된 값 박제** — 값 + Anchor360 Inspector 전체 스크린샷.
  이번에 아무 데도 안 남아서 처음부터 다시 찾았다

## B. 검출 품질

- **B-1.** 소화기 미검출 → `yolov8x-worldv2` 검토 (시간 예산 충분)
- **B-2.** `--max-dist` 확정 (현재 8, 6도 검토)
- **B-3.** config 공유 리스크 → 사본 또는 변경 시점 기록

## C. 운영 안정성

- **C-1.** 디스크 자동 정리
- **C-2.** stitch vs 촬영 여유 확보
- **C-3.** TRT 캐시가 또 비워진 원인 규명
- **C-4.** 슬라이스 백업 파일명 타임존 통일
- **C-5.** `pano_receiver.py` docstring 포트 7865 → 7866

## D. 확장

- **D-1.** 세 슬라이스 통합 (u+50/150/230 → 회전 적용 → 1패킷).
  검출 warm 0.02초라 3장 해도 0.06초. **A가 풀려야 회전 오프셋을 계산으로 넣을 수 있다**
- **D-2.** 대응점 기반 yaw 자동 산출 (§8.7).
  현재 시스템에서 **앵커 맵 등록이 유일한 수동 단계**다

## E. 별개 계통

- **E-1.** Unity `[UTM:GO1_A/B]` 포트 15101/15104 충돌
- **E-2.** GO1 5011로 서버에서 쏘는 경로가 있다면 §2.2와 같은 문제를 겪을 수 있음

---

# 14. 참고 — 관련 채팅방

| 채팅방 | 내용 |
|---|---|
| mast3r 처리 프로세스 상세 분석 | 촬영→stitch→전송→MASt3R 전 과정 전수조사. 시간·최적화·산출물 |
| Insta360 Pro2 Farsight를 통한 실시간 파일 전송 | `pano_receiver.py` 원본, Farsight API 함정, stitch 벤치마크 |
| Pro2 사진 촬영은 되는데 stitch 실패 | TRT 캐시 사고, `sync_trt_cache()`, RTC 배터리 |
| Insta360 Pro2 카메라 삼각대 고정 작업 계획 | `detect_send_3b2.py`, `Anchor360ObstacleReceiver.cs`, UDP 5010 결정 |
| 로봇 2대 협력 주행 시스템 설계 및 실행 전략 | u_deg 회전 설계, 3슬라이스 concat, **§8.4 오진의 출처** |
| Claude 제품 간 메모리 공유 및 협업 방안 | 유니티 receiver 분석, **"서버↔Unity 다른 망이라 직송 불가" 최초 기록** |
| Insta360 Pro2 디지털 트윈 논문 구현 계획 | MASt3R 거리 이상 최초 발견 |

---

*문서 끝. 2026-09-15 작성.*
