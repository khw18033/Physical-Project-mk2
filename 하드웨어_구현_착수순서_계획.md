# 피지컬팀 프로젝트 mk2 — 하드웨어 파트 구현 착수 순서 계획

> 근거 문서: `피지컬팀_프로젝트_mk2_요구사항_정의서(하드웨어)` — HW-R-01~10, HW-S-01~08, HW-C-01~07, HW-A-01~05 (총 30건)
> 담당: 조병현 / 작성일: 2026-08-25

## 우선순위 결정 원칙

1. **규격이 코드보다 먼저** — 프로토콜·스키마·ID 체계(HW-C 계열)가 흔들리면 이후 모든 구현을 재작업하게 되므로 최우선으로 확정한다.
2. **얇은 파이프라인을 끝까지 먼저 뚫는다** — "센서 1개 → 라즈베리파이 → 엣지노드"의 최소 경로를 먼저 완성하고, 그 위에 기능을 얹는다.
3. **주기 데이터 → 이벤트/명령 → 영상·AI → 장애 대응** 순서로 확장한다. 명령 체계(ACK)는 되돌리기 어려운 액추에이터 구동 전에 반드시 갖춘다.
4. 외부 파트(백엔드 프로토콜 확정, AI 모델) 의존 항목은 **인터페이스만 먼저 합의**하고 구현은 뒤로 미룬다.

---

## Phase 0 — 규격·합의 (코드 작성 전 선행) 🔴 최우선

**목표: 모든 통신의 "계약"을 백엔드와 합의해 문서로 고정한다.**

> **전제 확정(『데이터 전송 및 아키텍처 v4』, 2026-08-25):** 말단↔엣지 전송 프로토콜은 **MQTT로 확정**. 브로커(Mosquitto)는 엣지에 두고 말단(라즈베리파이)은 클라이언트 라이브러리(paho-mqtt)만 설치한다. 업무 데이터·명령·하트비트는 MQTT, 관측(노드 CPU·메모리·전송 성공률·지연)은 OpenTelemetry(OTLP), 영상은 별도 미디어 경로로 분리. 엣지↔서버는 Kafka이며, 엣지의 MQTT→Kafka 브릿지가 원본 메시지 구조를 유지한 채 중계한다.

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 0-1 | HW-C-01 | 하드웨어↔엣지노드 전송 인터페이스 | 프로토콜은 MQTT로 확정(아키텍처 v4)되어 "무엇으로 보낼지"는 종결. 남은 것은 접속 파라미터 합의 — 브로커 접속 정보, 데이터별 QoS 등급, 유언 메시지(LWT) 규칙. **백엔드 파트와의 파라미터 협의가 첫 번째 행동** |
| 0-2 | HW-C-02 | 표준 메시지 스키마·토픽 체계 | BE-C-01 공통 스키마(source_id, timestamp, schema_version 등)와 정렬. 브릿지가 MQTT 토픽을 원본 유지로 Kafka 주제에 중계하므로, 토픽 설계(구역·장치·데이터종류 기준)는 Kafka 주제 매핑까지 함께 확정. 모든 메시지 구현의 기준 문서 |
| 0-3 | HW-C-07 | device_id 기반 장치 식별·매핑 | 스키마의 필수 필드이므로 스키마 확정과 동시에 채번 규칙 결정. 토픽 경로·LWT 식별에도 그대로 쓰인다 |
| 0-4 | HW-S-08 | NTP 시각 동기화·타임스탬프 | 모든 계측 데이터에 타임스탬프가 들어가므로 장치 초기 셋업 단계에서 바로 적용 |

**완료 기준(DoD):** 백엔드와 합의된 토픽 구조·QoS/LWT 규칙·메시지 스키마 v1 문서 공유(프로토콜 MQTT 확정 반영), 라즈베리파이 공통 이미지에 NTP 활성화 + paho-mqtt·opentelemetry-sdk(+OTLP exporter) 설치.

### 하드웨어 파트 단독 결정사항 (백엔드 협의 전 초안 확정)

| 항목 | 결정 | 근거 |
|---|---|---|
| device_id 채번 규칙 | **`type-serial` 형식**(예: 수위센서 `wl-001`, 로봇 `rb-01`), **수동 채번**(채번 대장 시트 + 셋업 스크립트가 `/etc/device_id`에 기록). MAC 주소는 등록(Birth) 메시지의 참고 필드로만 사용 | zone을 ID에 넣으면 장치 이동·재배치 시 ID가 바뀌어 이력이 끊김 → 구역 소속은 장치 등록(HW-C-04)과 토픽 계층에서 관리. 통합정립본 v3의 정체성 체계(Entity/Node/Zone, 토픽 `{domain}/{type}/{id}/{channel}`)와 정합. 단 정체성·라벨 규약 소유자는 백엔드이므로 **이 채번 형식은 초안으로 대규에게 확정받는다**. MAC 기반 자동 채번은 보드 교체 시 ID가 바뀌고 가독성이 나쁨 |
| NTP 구성 | **엣지노드를 NTP 서버로**(chrony). 엣지는 상위 공용 NTP와 동기화하고, 말단은 chrony 클라이언트로 엣지만 바라봄 | 핵심은 절대 시각보다 **장치 간 시각 정합** — HW-R-09(재전송 후 타임스탬프 순서 복원)의 전제. 각자 공용 NTP를 보면 인터넷 상태에 따라 편차 발생. 국가 인프라 전제(v4)상 현장망은 인터넷 단절 가능 → 엣지가 로컬 기준시계로 계속 서비스. 통합정립본 v3에는 시각 동기 규정 없음(하드웨어 재량) |
| 말단 관측 방식·계측 범위 | **말단에 opentelemetry-sdk 설치, 관측은 OTLP 직접 발신으로 확정**(아키텍처 v4 방식). 업무 데이터·하트비트·명령 응답은 MQTT 발행. metric 5종으로 시작: CPU, 메모리, 디스크 여유 공간, MQTT 발행 성공/실패 카운트, 발행 지연. export 주기 15초. log는 에러 발생 시에만, **trace(명령 span)는 Phase 3에서 ACK 구현과 함께 합류** | 업무(MQTT)와 관측(OTLP)의 평면 분리는 두 문서 공통 원칙. 디스크는 HW-R-09 버퍼링 도입 시 저사양 말단에서 가장 먼저 고갈되는 자원이라 포함. ⚠️ **통합정립본 v3과 상충**: v3은 "디바이스는 표준 MQTT만 발행, OTel 미러는 백엔드 브리지" [확정]로 기재 → **말단 OTLP 직접 발신 채택을 백엔드(대규)에 통보하고 v3 문서 갱신 합의 필요** |

**백엔드 확인 대기 항목(통합정립본 v3 §5, 병현 회신 필요):** [G2] 로봇 cmd/ack에 command_id 필드 추가(스키마 변경 가능 여부 회신), [G3] 로봇 state에 device_status(ok/degraded/fault) 추가 — 하드웨어가 직접 낼지, 백엔드가 metric/log에서 파생할지 회신.

---

## Phase 1 — 최소 연결·생존 확인 (뼈대 파이프라인)

**목표: 장치가 엣지노드에 붙고, 살아있는지 확인되는 상태.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 1-1 | HW-C-04 | 장치 등록(Birth/Death)·구역 관리 | 접속 즉시 등록 → 이후 모든 데이터의 소속 판별 기반 |
| 1-2 | HW-S-05 | 센서노드 하트비트 (1 Hz) | 가장 단순한 주기 메시지로 파이프라인 검증에 최적 |
| 1-3 | HW-R-02 | 로봇 온보드 하트비트 | HW-S-05와 동일 로직 재사용 (대기 시에만 전송 규칙 추가) |
| 1-4 | HW-S-07 | 오프라인 감지 (4회 미수신 판정) | 하트비트가 돌면 바로 이어서 타임아웃 판정 구현 |
| 1-5 | HW-C-06 | 제어 명령 수신·ACK 공통 체계 | 하향(엣지→장치) 채널 검증. 이후 임무·액추에이터 명령의 공통 기반 |

**완료 기준:** 장치 전원 on→등록→온라인 표시, 전원 off→약 4초 내 오프라인 반영, 테스트 명령에 ACK 회신.

---

## Phase 2 — 핵심 데이터 파이프라인 (상향 데이터)

**목표: 실제 계측·상태 데이터가 주기대로 흐른다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 2-1 | HW-S-01 | 센서 계측값 수집 (센서→라즈베리파이) | 센서 데이터의 원천. 선정 센서 데이터시트로 최대 샘플링 확정 |
| 2-2 | HW-S-02 | 평시 계측 전달 (1분 주기 + 임계값 즉시 발행) | 수집이 되면 바로 보고 로직. report-by-exception 포함 |
| 2-3 | HW-R-01 | 로봇 내부 데이터 전달 (제어기→온보드, 50 Hz) | 로봇 측 데이터의 원천. HW-R-03의 선행 조건 |
| 2-4 | HW-R-03 | 로봇 상태 데이터 전달 (임무 중 20 Hz / 대기 1 Hz) | 관제·가시화가 소비하는 대표 데이터. 여기까지 되면 데모 가능 |

**완료 기준:** 대시보드(또는 로그)에서 센서값·로봇 상태가 정의된 주기로 갱신 확인.

---

## Phase 3 — 명령·임무 체계 (하향 제어)

**목표: 엣지노드가 내린 명령이 실제 동작으로 이어진다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 3-1 | HW-R-05 | 임무(서브태스크) 수신·검증 | Phase 1의 ACK 체계 위에 임무 메시지 정의 |
| 3-2 | HW-R-06 | 제어 명령 내부 전달 (온보드→제어기 변환) | 수신한 임무를 구동 인터페이스로 변환하는 마지막 구간 |
| 3-3 | HW-A-01 | 액추에이터 상태 수집 (20 Hz) | 구동 전에 상태 파악부터. 안전한 제어의 전제 |
| 3-4 | HW-A-02 | 액추에이터 제어 명령 수신 | 상태 수집이 되는 상태에서 구동 명령 연결 |
| 3-5 | HW-A-03 | 제어 명령 ACK | HW-C-06 공통 체계를 액추에이터에 적용 |
| 3-6 | HW-A-04 | 실제 수행 결과 확인 (진행 20 Hz + 완료 즉시) | ACK와 물리적 완료를 구분하는 마무리 단계 |

**완료 기준:** "명령 하달→ACK→수행 중→완료/실패" 전 과정이 상태로 구분되어 보고됨.

---

## Phase 4 — 컨테이너 오케스트레이션·적응형 운영

**목표: K3s 기반으로 기능을 컨테이너 단위로 올리고 내린다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 4-1 | HW-C-03 | K3s 컨테이너 기반 구조 | Phase 2~3에서 검증된 기능을 컨테이너로 패키징, 워커 노드 편입. ※ K3s 클러스터 구축 자체는 Phase 1부터 병행 가능 |
| 4-2 | HW-S-04 | 적응형 주기 전환 명령 수신 | 컨테이너 배포/제거가 전환의 실체이므로 4-1 이후 |
| 4-3 | HW-S-03 | 이벤트 모드 고주기 보고 (1 Hz) | 고주기 모니터링 컨테이너 배포로 완성 |

**완료 기준:** 강우 시작 이벤트 → 고주기 컨테이너 자동 배포 → 1초 보고 전환 → 종료 시 평시 복귀 시연.

---

## Phase 5 — 영상·AI 연동

**목표: 영상 스트림과 온디바이스 AI를 파이프라인에 연결한다. (AI 파트 일정과 동기화)**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 5-1 | HW-S-06 | 고정 비전센서(CCTV) 스트림 (15 fps) | 고정 카메라가 로봇 카메라보다 구성 단순 → 영상 경로 먼저 검증 |
| 5-2 | HW-R-04 | 환경인식용 이미지·온디바이스 인식 | benchncnn 실측으로 추론 FPS 확정 필요 → AI 파트 협의 병행 |
| 5-3 | HW-R-07 | 관제용 고해상도 스트림 (온디맨드 15 fps) | 세션 열고 닫는 온디맨드 제어는 상시 스트림(5-1) 검증 후 |
| 5-4 | HW-R-10 | 온디바이스 AI 모델 수신·적용 | 모델 버전 관리·배포는 K3s(Phase 4)와 AI 파트 모델 준비가 전제 |
| 5-5 | HW-R-08 | 구역 사전 정보(맵·경로) 수신 (전체+델타) | 엣지노드의 구역 정보 관리 기능(백엔드)과 AI 경로 계획 결과가 필요 |

**완료 기준:** CCTV·로봇 영상이 엣지노드 도달, 온디바이스 추론 FPS 실측치 문서화, 모델 교체 시연.

---

## Phase 6 — 장애 대응·관측 (신뢰성 강화)

**목표: 끊겨도 데이터가 안 사라지고, 문제 원인을 추적할 수 있다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 6-1 | HW-R-09 | 통신 두절 시 버퍼링·재전송 | 정상 경로가 안정된 뒤 장애 주입 테스트로 검증 |
| 6-2 | HW-A-05 | 액추에이터 통신 두절 안전 처리 | 원격 제어 잠금·복구 후 재동기화. 액추에이터 전 기능(Phase 3) 완성 후 |
| 6-3 | HW-C-05 | OpenTelemetry 관측 계측 (60초 export) | 통합시험 단계에서 지연·실패율 추적에 필요. 백엔드 Collector 구축과 병행 |

**완료 기준:** 네트워크 차단 시험에서 데이터 무손실(타임스탬프 순서 복원), 두절 중 원격 명령 차단 확인, 노드 지표 대시보드 표시.

---

## 요약: 지금 당장 할 일 Top 3

- [ ] **① 백엔드 파트와 전송 프로토콜·메시지 스키마 확정 회의** (HW-C-01, HW-C-02, HW-C-07) — 이게 결정되어야 코드를 쓸 수 있음
- [ ] **② 라즈베리파이 공통 베이스 셋업** — OS 이미지, NTP(HW-S-08), MQTT 클라이언트, device_id 채번 스크립트
- [ ] **③ 하트비트 + 등록 최소 파이프라인 구현** (HW-C-04, HW-S-05) — 스키마 v1을 실제 트래픽으로 검증

## 외부 의존성 체크리스트

| 의존 대상 | 관련 요구사항 | 필요한 것 |
|---|---|---|
| 백엔드 | HW-C-01, C-02, S-07, R-08 | 최종 프로토콜 확정, 공통 스키마(BE-C-01), 엣지노드 브로커 |
| AI | HW-R-04, R-10, R-08 | 경량 모델 요구 FPS, 모델 배포 형식·버전 규칙 |
| 가시화 | HW-R-03, R-07, S-06 | 대시보드 소비 형식, 영상 표시 방식(RTSP/WebRTC 등) |
| 조달(구매) | HW-S-01, S-03 | 센서 모델 최종 선정 → 데이터시트 기준 샘플링 주기 확정 |

---

## 진행 기록

### 2026-08-25 — Phase 0 파이 셋업 완료 + Phase 1 하트비트·LWT 검증

**완료:**

- [x] 라즈베리파이(pi7, Pi 5·64bit) 공통 셋업: chrony 동기화 확인(오차 µs 수준), `/etc/device_id` = `wl-001`, cgroup memory 활성화(cmdline.txt, `cgroup.controllers`로 확인), venv에 paho-mqtt·opentelemetry-sdk·otlp-exporter·psutil 설치
- [x] 임시 브로커: 노트북(Windows) Mosquitto 2.1.2, `listener 1883 0.0.0.0` + `allow_anonymous true` (테스트용)
- [x] `heartbeat.py`: `zoneA/sensor/wl-001/heartbeat` 5초 주기 발행(seq·ISO8601 timestamp), `/status` online(retained, QoS1) + LWT offline 등록
- [x] 검증: 구독자 수신 정상, keepalive PING 정상, **Ctrl+C 급사 시 브로커가 LWT로 offline 자동 발행 확인** → HW-S-05 + 생사 감지 기반 동작

**트러블슈팅 메모:** Mosquitto Windows 서비스가 자동 실행되어 브로커가 2개 뜸 → 구독자(localhost)와 발행자(IP)가 서로 다른 브로커에 붙는 문제. `net stop mosquitto` + `sc config mosquitto start= demand`로 서비스 비활성화. 구독 시 `-h`에 localhost 대신 IP 명시 권장.

**임시값(협의 후 교체):** 토픽 `zoneA` prefix(도메인 체계 확정 대기), 하트비트 5초(계획서 1Hz vs v4 5초 — 협의 안건), 브로커 위치(노트북 → 엣지노드로 이전 예정), 인증 없음(최종 MQTT 5.0/TLS).

**다음 할 일:** 등록 Birth 메시지(HW-C-04), 엣지 오프라인 판정 로직(HW-S-07), 명령 수신·ACK(HW-C-06), OTLP metric 5종 발신 코드.

### 2026-08-25 (2차) — Birth 등록 + 명령·ACK 검증 (HW-C-04, HW-C-06)

**완료:**

- [x] `node_client.py`: heartbeat.py 확장 — Birth(online status에 device_type·fw_version·MAC 포함, retained), `.../cmd` 구독 + `.../cmd/ack` QoS 1 회신(command_id 상관키 에코, command_id 없으면 rejected)
- [x] 검증: Birth 장치정보 수신 ✅ / 하트비트 seq 연속 ✅ / cmd→ack 왕복(command_id `cmd-test-001` 일치, applied, 응답 지연 1초 미만) ✅ / Ctrl+C 급사 시 LWT offline ✅
- [ ] 불량 명령 거부 테스트(command_id 누락 → rejected) — 코드는 있음, 실측 미실시

**Phase 1 잔여:** HW-S-07 오프라인 판정(하트비트 4회 미수신) — 엣지 역할이므로 노트북 감시 스크립트로 대행 예정. HW-R-02(로봇 하트비트)는 로봇 온보드 장비 확보 후 동일 로직 재사용.

### 2026-08-25 (3차) — HW-S-07 오프라인 판정 검증 → **Phase 1 완료** ✅

**완료:**

- [x] `monitor.py`(엣지 대역, 파이 별도 세션에서 실행): `zoneA/+/+/heartbeat`·`status` 와일드카드 구독, 장치별 last_seen 추적 + watchdog 스레드가 4회(20초) 미수신 시 OFFLINE 판정
- [x] 검증: retained Birth 즉시 수신 ✅ / 하트비트 수신 → ONLINE 판정 ✅ / 급사 시 LWT offline(즉시) + 4회 미수신 OFFLINE 판정(20초) **이중 감지 동작** ✅ / 재실행 시 ONLINE 복귀 판정 ✅

**Phase 1 완료 기준 충족:** 전원 on→등록(Birth)→온라인 표시 ✅ / 끊김→LWT 즉시 + 타임아웃 20초 내 오프라인 반영 ✅ / 테스트 명령에 ACK 회신 ✅
(잔여: HW-R-02 로봇 하트비트는 로봇 장비 확보 후 동일 로직 재사용)

**다음(Phase 2 착수 방식):** 센서 미확보 상태이므로 가짜 수위값 생성기로 HW-S-02(1분 주기 + 임계값 초과 시 즉시 발행, report-by-exception) 파이프라인을 먼저 구현 → 센서 선정·입고 후 값 읽기 함수만 교체(HW-S-01). 병행: node_client.py systemd 서비스 등록, OTLP metric 5종 발신 코드.

### 2026-08-25 (4차) — Phase 2 착수: HW-S-02 평시 보고 + 임계값 즉시 발행 검증

**완료:**

- [x] `sensor_node.py`(node_client.py 확장): 가짜 수위 생성기(평시 2.5m 평균회귀 잔물결, `/tmp/rain` 존재 시 상승) + report-by-exception 로직 — 매초 수집, 발행은 ①평시 60초 주기 ②임계(3.0m) 돌파 즉시 ③해제(2.9m 미만, 히스테리시스 0.1m) 즉시. state 메시지는 통합정립본 A-2 스키마 정렬(schema_version·seq·water_level_m·alert·reason·health), QoS 1
- [x] 검증: 평시 60초 주기 periodic 보고 ✅ / `touch /tmp/rain` → 3.049m `threshold_exceeded` **즉시 발행** ✅ / `rm /tmp/rain` → 2.877m `threshold_cleared` 즉시 발행 ✅ / 히스테리시스 동작(2.9 미만에서 해제) ✅ / 하트비트·Birth·LWT·cmd/ack 기존 기능 유지

**HW-S-01(실센서 수집) 대비:** `read_water_level()` 함수만 실제 센서 코드로 교체하면 나머지 파이프라인 그대로 사용 가능. 센서 모델 선정(조달) 대기.

**다음 할 일:** node_client → sensor_node systemd 서비스 등록(부팅 자동 시작), OTLP metric 5종 발신 코드, HW-S-03(이벤트 모드 1Hz 고주기 보고)은 Phase 4 컨테이너 배포와 연계.
