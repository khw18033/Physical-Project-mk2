# 피지컬팀 프로젝트 mk2 — 하드웨어 파트 구현 착수 순서 계획

> 근거 문서: `피지컬팀_프로젝트_mk2_요구사항_정의서(하드웨어)` — HW-R-01~10, HW-S-01~08, HW-C-01~07, HW-A-01~05 (총 30건)
> 담당: 조병현 / 작성일: 2026-08-25

## 우선순위 결정 원칙

1. **규격이 코드보다 먼저** — 프로토콜·스키마·ID 체계(HW-C 계열)가 흔들리면 이후 모든 구현을 재작업하게 되므로 최우선으로 확정한다.
2. **얇은 파이프라인을 끝까지 먼저 뚫는다** — "센서 1개 → 라즈베리파이 → 엣지노드"의 최소 경로를 먼저 완성하고, 그 위에 기능을 얹는다.
3. **주기 데이터 → 이벤트/명령 → 영상·AI → 장애 대응** 순서로 확장한다. 명령 체계(ACK)는 되돌리기 어려운 액추에이터 구동 전에 반드시 갖춘다.
4. 외부 파트(백엔드 프로토콜 확정, AI 모델) 의존 항목은 **인터페이스만 먼저 합의**하고 구현은 뒤로 미룬다.

---

## Phase 0 — 규격·합의 (코드 작성 전 선행) 🔴 최우선

**목표: 모든 통신의 "계약"을 백엔드와 합의해 문서로 고정한다.**

> **전제 확정(『데이터 전송 및 아키텍처 v4』, 2026-08-25):** 말단↔엣지 전송 프로토콜은 **MQTT로 확정**. 브로커(Mosquitto)는 엣지에 두고 말단(라즈베리파이)은 클라이언트 라이브러리(paho-mqtt)만 설치한다. 업무 데이터·명령·하트비트는 MQTT, 관측(노드 CPU·메모리·전송 성공률·지연)은 OpenTelemetry(OTLP), 영상은 별도 미디어 경로로 분리. 엣지↔서버는 Kafka이며, 엣지의 MQTT→Kafka 브릿지가 원본 메시지 구조를 유지한 채 중계한다.

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 0-1 | HW-C-01 | 하드웨어↔엣지노드 전송 인터페이스 | 프로토콜은 MQTT로 확정(아키텍처 v4)되어 "무엇으로 보낼지"는 종결. 남은 것은 접속 파라미터 합의 — 브로커 접속 정보, 데이터별 QoS 등급, 유언 메시지(LWT) 규칙. **백엔드 파트와의 파라미터 협의가 첫 번째 행동** |
| 0-2 | HW-C-02 | 표준 메시지 스키마·토픽 체계 | BE-C-01 공통 스키마(source_id, timestamp, schema_version 등)와 정렬. 브릿지가 MQTT 토픽을 원본 유지로 Kafka 주제에 중계하므로, 토픽 설계(구역·장치·데이터종류 기준)는 Kafka 주제 매핑까지 함께 확정. 모든 메시지 구현의 기준 문서 |
| 0-3 | HW-C-07 | device_id 기반 장치 식별·매핑 | 스키마의 필수 필드이므로 스키마 확정과 동시에 채번 규칙 결정. 토픽 경로·LWT 식별에도 그대로 쓰인다 |
| 0-4 | HW-S-08 | NTP 시각 동기화·타임스탬프 | 모든 계측 데이터에 타임스탬프가 들어가므로 장치 초기 셋업 단계에서 바로 적용 |

**완료 기준(DoD):** 백엔드와 합의된 토픽 구조·QoS/LWT 규칙·메시지 스키마 v1 문서 공유(프로토콜 MQTT 확정 반영), 라즈베리파이 공통 이미지에 NTP 활성화 + paho-mqtt·opentelemetry-sdk(+OTLP exporter) 설치.

### 하드웨어 파트 단독 결정사항 (백엔드 협의 전 초안 확정)

| 항목 | 결정 | 근거 |
|---|---|---|
| device_id 채번 규칙 | **`type-serial` 형식**(예: 수위센서 `wl-001`, 로봇 `rb-01`), **수동 채번**(채번 대장 시트 + 셋업 스크립트가 `/etc/device_id`에 기록). MAC 주소는 등록(Birth) 메시지의 참고 필드로만 사용 | zone을 ID에 넣으면 장치 이동·재배치 시 ID가 바뀌어 이력이 끊김 → 구역 소속은 장치 등록(HW-C-04)과 토픽 계층에서 관리. 통합정립본 v3의 정체성 체계(Entity/Node/Zone, 토픽 `{domain}/{type}/{id}/{channel}`)와 정합. 단 정체성·라벨 규약 소유자는 백엔드이므로 **이 채번 형식은 초안으로 대규에게 확정받는다**. MAC 기반 자동 채번은 보드 교체 시 ID가 바뀌고 가독성이 나쁨 |
| NTP 구성 | **엣지노드를 NTP 서버로**(chrony). 엣지는 상위 공용 NTP와 동기화하고, 말단은 chrony 클라이언트로 엣지만 바라봄 | 핵심은 절대 시각보다 **장치 간 시각 정합** — HW-R-09(재전송 후 타임스탬프 순서 복원)의 전제. 각자 공용 NTP를 보면 인터넷 상태에 따라 편차 발생. 국가 인프라 전제(v4)상 현장망은 인터넷 단절 가능 → 엣지가 로컬 기준시계로 계속 서비스. 통합정립본 v3에는 시각 동기 규정 없음(하드웨어 재량) |
| 말단 관측 방식·계측 범위 | **말단에 opentelemetry-sdk 설치, 관측은 OTLP 직접 발신으로 확정**(아키텍처 v4 방식). 업무 데이터·하트비트·명령 응답은 MQTT 발행. metric 5종으로 시작: CPU, 메모리, 디스크 여유 공간, MQTT 발행 성공/실패 카운트, 발행 지연. export 주기 15초. log는 에러 발생 시에만, **trace(명령 span)는 Phase 3에서 ACK 구현과 함께 합류** | 업무(MQTT)와 관측(OTLP)의 평면 분리는 두 문서 공통 원칙. 디스크는 HW-R-09 버퍼링 도입 시 저사양 말단에서 가장 먼저 고갈되는 자원이라 포함. ⚠️ **통합정립본 v3과 상충**: v3은 "디바이스는 표준 MQTT만 발행, OTel 미러는 백엔드 브리지" [확정]로 기재 → **말단 OTLP 직접 발신 채택을 백엔드(대규)에 통보하고 v3 문서 갱신 합의 필요** |

**백엔드 확인 대기 항목(통합정립본 v3 §5, 병현 회신 필요):** [G2] 로봇 cmd/ack에 command_id 필드 추가(스키마 변경 가능 여부 회신), [G3] 로봇 state에 device_status(ok/degraded/fault) 추가 — 하드웨어가 직접 낼지, 백엔드가 metric/log에서 파생할지 회신.

---

## Phase 1 — 최소 연결·생존 확인 (뼈대 파이프라인)

**목표: 장치가 엣지노드에 붙고, 살아있는지 확인되는 상태.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 1-1 | HW-C-04 | 장치 등록(Birth/Death)·구역 관리 | 접속 즉시 등록 → 이후 모든 데이터의 소속 판별 기반 |
| 1-2 | HW-S-05 | 센서노드 하트비트 (1 Hz) | 가장 단순한 주기 메시지로 파이프라인 검증에 최적 |
| 1-3 | HW-R-02 | 로봇 온보드 하트비트 | HW-S-05와 동일 로직 재사용 (대기 시에만 전송 규칙 추가) |
| 1-4 | HW-S-07 | 오프라인 감지 (4회 미수신 판정) | 하트비트가 돌면 바로 이어서 타임아웃 판정 구현 |
| 1-5 | HW-C-06 | 제어 명령 수신·ACK 공통 체계 | 하향(엣지→장치) 채널 검증. 이후 임무·액추에이터 명령의 공통 기반 |

**완료 기준:** 장치 전원 on→등록→온라인 표시, 전원 off→약 4초 내 오프라인 반영, 테스트 명령에 ACK 회신.

---

## Phase 2 — 핵심 데이터 파이프라인 (상향 데이터)

**목표: 실제 계측·상태 데이터가 주기대로 흐른다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 2-1 | HW-S-01 | 센서 계측값 수집 (센서→라즈베리파이) | 센서 데이터의 원천. 선정 센서 데이터시트로 최대 샘플링 확정 |
| 2-2 | HW-S-02 | 평시 계측 전달 (1분 주기 + 임계값 즉시 발행) | 수집이 되면 바로 보고 로직. report-by-exception 포함 |
| 2-3 | HW-R-01 | 로봇 내부 데이터 전달 (제어기→온보드, 50 Hz) | 로봇 측 데이터의 원천. HW-R-03의 선행 조건 |
| 2-4 | HW-R-03 | 로봇 상태 데이터 전달 (임무 중 20 Hz / 대기 1 Hz) | 관제·가시화가 소비하는 대표 데이터. 여기까지 되면 데모 가능 |

**완료 기준:** 대시보드(또는 로그)에서 센서값·로봇 상태가 정의된 주기로 갱신 확인.

---

## Phase 3 — 명령·임무 체계 (하향 제어)

**목표: 엣지노드가 내린 명령이 실제 동작으로 이어진다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 3-1 | HW-R-05 | 임무(서브태스크) 수신·검증 | Phase 1의 ACK 체계 위에 임무 메시지 정의 |
| 3-2 | HW-R-06 | 제어 명령 내부 전달 (온보드→제어기 변환) | 수신한 임무를 구동 인터페이스로 변환하는 마지막 구간 |
| 3-3 | HW-A-01 | 액추에이터 상태 수집 (20 Hz) | 구동 전에 상태 파악부터. 안전한 제어의 전제 |
| 3-4 | HW-A-02 | 액추에이터 제어 명령 수신 | 상태 수집이 되는 상태에서 구동 명령 연결 |
| 3-5 | HW-A-03 | 제어 명령 ACK | HW-C-06 공통 체계를 액추에이터에 적용 |
| 3-6 | HW-A-04 | 실제 수행 결과 확인 (진행 20 Hz + 완료 즉시) | ACK와 물리적 완료를 구분하는 마무리 단계 |

**완료 기준:** "명령 하달→ACK→수행 중→완료/실패" 전 과정이 상태로 구분되어 보고됨.

---

## Phase 4 — 컨테이너 오케스트레이션·적응형 운영

**목표: K3s 기반으로 기능을 컨테이너 단위로 올리고 내린다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 4-1 | HW-C-03 | K3s 컨테이너 기반 구조 | Phase 2~3에서 검증된 기능을 컨테이너로 패키징, 워커 노드 편입. ※ K3s 클러스터 구축 자체는 Phase 1부터 병행 가능 |
| 4-2 | HW-S-04 | 적응형 주기 전환 명령 수신 | 컨테이너 배포/제거가 전환의 실체이므로 4-1 이후 |
| 4-3 | HW-S-03 | 이벤트 모드 고주기 보고 (1 Hz) | 고주기 모니터링 컨테이너 배포로 완성 |

**완료 기준:** 강우 시작 이벤트 → 고주기 컨테이너 자동 배포 → 1초 보고 전환 → 종료 시 평시 복귀 시연.

---

## Phase 5 — 영상·AI 연동

**목표: 영상 스트림과 온디바이스 AI를 파이프라인에 연결한다. (AI 파트 일정과 동기화)**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 5-1 | HW-S-06 | 고정 비전센서(CCTV) 스트림 (15 fps) | 고정 카메라가 로봇 카메라보다 구성 단순 → 영상 경로 먼저 검증 |
| 5-2 | HW-R-04 | 환경인식용 이미지·온디바이스 인식 | benchncnn 실측으로 추론 FPS 확정 필요 → AI 파트 협의 병행 |
| 5-3 | HW-R-07 | 관제용 고해상도 스트림 (온디맨드 15 fps) | 세션 열고 닫는 온디맨드 제어는 상시 스트림(5-1) 검증 후 |
| 5-4 | HW-R-10 | 온디바이스 AI 모델 수신·적용 | 모델 버전 관리·배포는 K3s(Phase 4)와 AI 파트 모델 준비가 전제 |
| 5-5 | HW-R-08 | 구역 사전 정보(맵·경로) 수신 (전체+델타) | 엣지노드의 구역 정보 관리 기능(백엔드)과 AI 경로 계획 결과가 필요 |

**완료 기준:** CCTV·로봇 영상이 엣지노드 도달, 온디바이스 추론 FPS 실측치 문서화, 모델 교체 시연.

---

## Phase 6 — 장애 대응·관측 (신뢰성 강화)

**목표: 끊겨도 데이터가 안 사라지고, 문제 원인을 추적할 수 있다.**

| 순서 | ID | 항목 | 이 시점인 이유 |
|---|---|---|---|
| 6-1 | HW-R-09 | 통신 두절 시 버퍼링·재전송 | 정상 경로가 안정된 뒤 장애 주입 테스트로 검증 |
| 6-2 | HW-A-05 | 액추에이터 통신 두절 안전 처리 | 원격 제어 잠금·복구 후 재동기화. 액추에이터 전 기능(Phase 3) 완성 후 |
| 6-3 | HW-C-05 | OpenTelemetry 관측 계측 (60초 export) | 통합시험 단계에서 지연·실패율 추적에 필요. 백엔드 Collector 구축과 병행 |

**완료 기준:** 네트워크 차단 시험에서 데이터 무손실(타임스탬프 순서 복원), 두절 중 원격 명령 차단 확인, 노드 지표 대시보드 표시.

---

## 요약: 지금 당장 할 일 Top 3

- [ ] **① 백엔드 파트와 전송 프로토콜·메시지 스키마 확정 회의** (HW-C-01, HW-C-02, HW-C-07) — 이게 결정되어야 코드를 쓸 수 있음
- [ ] **② 라즈베리파이 공통 베이스 셋업** — OS 이미지, NTP(HW-S-08), MQTT 클라이언트, device_id 채번 스크립트
- [ ] **③ 하트비트 + 등록 최소 파이프라인 구현** (HW-C-04, HW-S-05) — 스키마 v1을 실제 트래픽으로 검증

## 외부 의존성 체크리스트

| 의존 대상 | 관련 요구사항 | 필요한 것 |
|---|---|---|
| 백엔드 | HW-C-01, C-02, S-07, R-08 | 최종 프로토콜 확정, 공통 스키마(BE-C-01), 엣지노드 브로커 |
| AI | HW-R-04, R-10, R-08 | 경량 모델 요구 FPS, 모델 배포 형식·버전 규칙 |
| 가시화 | HW-R-03, R-07, S-06 | 대시보드 소비 형식, 영상 표시 방식(RTSP/WebRTC 등) |
| 조달(구매) | HW-S-01, S-03 | 센서 모델 최종 선정 → 데이터시트 기준 샘플링 주기 확정 |

---

## 진행 기록

### 2026-08-25 — Phase 0 파이 셋업 완료 + Phase 1 하트비트·LWT 검증

**완료:**

- [x] 라즈베리파이(pi7, Pi 5·64bit) 공통 셋업: chrony 동기화 확인(오차 µs 수준), `/etc/device_id` = `wl-001`, cgroup memory 활성화(cmdline.txt, `cgroup.controllers`로 확인), venv에 paho-mqtt·opentelemetry-sdk·otlp-exporter·psutil 설치
- [x] 임시 브로커: 노트북(Windows) Mosquitto 2.1.2, `listener 1883 0.0.0.0` + `allow_anonymous true` (테스트용)
- [x] `heartbeat.py`: `zoneA/sensor/wl-001/heartbeat` 5초 주기 발행(seq·ISO8601 timestamp), `/status` online(retained, QoS1) + LWT offline 등록
- [x] 검증: 구독자 수신 정상, keepalive PING 정상, **Ctrl+C 급사 시 브로커가 LWT로 offline 자동 발행 확인** → HW-S-05 + 생사 감지 기반 동작

**트러블슈팅 메모:** Mosquitto Windows 서비스가 자동 실행되어 브로커가 2개 뜸 → 구독자(localhost)와 발행자(IP)가 서로 다른 브로커에 붙는 문제. `net stop mosquitto` + `sc config mosquitto start= demand`로 서비스 비활성화. 구독 시 `-h`에 localhost 대신 IP 명시 권장.

**임시값(협의 후 교체):** 토픽 `zoneA` prefix(도메인 체계 확정 대기), 하트비트 5초(계획서 1Hz vs v4 5초 — 협의 안건), 브로커 위치(노트북 → 엣지노드로 이전 예정), 인증 없음(최종 MQTT 5.0/TLS).

**다음 할 일:** 등록 Birth 메시지(HW-C-04), 엣지 오프라인 판정 로직(HW-S-07), 명령 수신·ACK(HW-C-06), OTLP metric 5종 발신 코드.

### 2026-08-25 (2차) — Birth 등록 + 명령·ACK 검증 (HW-C-04, HW-C-06)

**완료:**

- [x] `node_client.py`: heartbeat.py 확장 — Birth(online status에 device_type·fw_version·MAC 포함, retained), `.../cmd` 구독 + `.../cmd/ack` QoS 1 회신(command_id 상관키 에코, command_id 없으면 rejected)
- [x] 검증: Birth 장치정보 수신 ✅ / 하트비트 seq 연속 ✅ / cmd→ack 왕복(command_id `cmd-test-001` 일치, applied, 응답 지연 1초 미만) ✅ / Ctrl+C 급사 시 LWT offline ✅
- [ ] 불량 명령 거부 테스트(command_id 누락 → rejected) — 코드는 있음, 실측 미실시

**Phase 1 잔여:** HW-S-07 오프라인 판정(하트비트 4회 미수신) — 엣지 역할이므로 노트북 감시 스크립트로 대행 예정. HW-R-02(로봇 하트비트)는 로봇 온보드 장비 확보 후 동일 로직 재사용.

### 2026-08-25 (3차) — HW-S-07 오프라인 판정 검증 → **Phase 1 완료** ✅

**완료:**

- [x] `monitor.py`(엣지 대역, 파이 별도 세션에서 실행): `zoneA/+/+/heartbeat`·`status` 와일드카드 구독, 장치별 last_seen 추적 + watchdog 스레드가 4회(20초) 미수신 시 OFFLINE 판정
- [x] 검증: retained Birth 즉시 수신 ✅ / 하트비트 수신 → ONLINE 판정 ✅ / 급사 시 LWT offline(즉시) + 4회 미수신 OFFLINE 판정(20초) **이중 감지 동작** ✅ / 재실행 시 ONLINE 복귀 판정 ✅

**Phase 1 완료 기준 충족:** 전원 on→등록(Birth)→온라인 표시 ✅ / 끊김→LWT 즉시 + 타임아웃 20초 내 오프라인 반영 ✅ / 테스트 명령에 ACK 회신 ✅
(잔여: HW-R-02 로봇 하트비트는 로봇 장비 확보 후 동일 로직 재사용)

**다음(Phase 2 착수 방식):** 센서 미확보 상태이므로 가짜 수위값 생성기로 HW-S-02(1분 주기 + 임계값 초과 시 즉시 발행, report-by-exception) 파이프라인을 먼저 구현 → 센서 선정·입고 후 값 읽기 함수만 교체(HW-S-01). 병행: node_client.py systemd 서비스 등록, OTLP metric 5종 발신 코드.

### 2026-08-25 (4차) — Phase 2 착수: HW-S-02 평시 보고 + 임계값 즉시 발행 검증

**완료:**

- [x] `sensor_node.py`(node_client.py 확장): 가짜 수위 생성기(평시 2.5m 평균회귀 잔물결, `/tmp/rain` 존재 시 상승) + report-by-exception 로직 — 매초 수집, 발행은 ①평시 60초 주기 ②임계(3.0m) 돌파 즉시 ③해제(2.9m 미만, 히스테리시스 0.1m) 즉시. state 메시지는 통합정립본 A-2 스키마 정렬(schema_version·seq·water_level_m·alert·reason·health), QoS 1
- [x] 검증: 평시 60초 주기 periodic 보고 ✅ / `touch /tmp/rain` → 3.049m `threshold_exceeded` **즉시 발행** ✅ / `rm /tmp/rain` → 2.877m `threshold_cleared` 즉시 발행 ✅ / 히스테리시스 동작(2.9 미만에서 해제) ✅ / 하트비트·Birth·LWT·cmd/ack 기존 기능 유지

**HW-S-01(실센서 수집) 대비:** `read_water_level()` 함수만 실제 센서 코드로 교체하면 나머지 파이프라인 그대로 사용 가능. 센서 모델 선정(조달) 대기.

**다음 할 일:** node_client → sensor_node systemd 서비스 등록(부팅 자동 시작), OTLP metric 5종 발신 코드, HW-S-03(이벤트 모드 1Hz 고주기 보고)은 Phase 4 컨테이너 배포와 연계.

### 2026-08-31 — 요구사항 정의서(xlsx) 대조 후 미달 항목 보완 + 장비 무관 항목 착수

**배경:** 요구사항 정의서 30건과 구현 코드를 대조한 결과, "완료"로 기록한 항목 중
정의서 기준 미달이 5건, 백엔드 규약(BE-*)과의 정렬 누락이 3건 확인되어 먼저 메꿨다.

**미달 보완 (기존 완료 항목의 결함)**

| ID | 미달 내용 | 조치 |
|---|---|---|
| HW-C-04 | 등록 1회만 있고 **상태 요약 10초 갱신**이 없었음 | `status` 채널에 10초 주기 summary 발행. 등록 정보를 요약에도 실어 retained가 덮여도 장치 정보 보존 |
| HW-S-05 / S-07 | 하트비트 5초 → 판정 20초 (정의서는 1Hz·약 4초) | **1 Hz로 변경**, 판정 4초. `HW_HB_INTERVAL`로 협의 결과 즉시 반영 가능 |
| HW-S-02 | 임계 돌파/해제만 있고 **급변 감지**가 없었음 | 10초 창 양끝 차이 0.3m 이상이면 `rapid_change` 즉시 발행 + 보고 최소 간격 10초 |
| HW-C-06 | ACK 1단계만 (BE-X-03은 4단계 요구) | `cmd/result` 채널 신설: executing → state_changed → completed/failed. 설정 명령은 `physical:false`로 물리 단계 없음을 명시 |
| HW-C-07 | 최초 등록만, 변경 갱신 없음 | 10초마다 zone·MAC·IP 지문 비교 → 변경 시 재등록. 구역 변경이면 옛 토픽 retained를 빈 payload로 삭제 후 새 토픽으로 재접속 |

**백엔드 규약 정렬**

- BE-C-01/C-02: 모든 메시지를 `source_id`·`node_id`·`zone_id`·`timestamp`·`schema_version`·
  `correlation_id` 봉투로 통일. **`schema.py` 어댑터 한 곳**에 모아 확정 시 한 파일만 고치면 되게 했다.
  확정 전까지 `device_id` 별칭 병기(`LEGACY_DEVICE_ID` 플래그로 한 줄 제거).
- BE-T-04 / [G3]: `device_status`(ok/degraded/fault) 자기보고 층을 분리 발행. 서버 판정
  가용성(availability)은 monitor가 별도로 매긴다. 기존 `health.status` 대체.
- BE-X-01: `command_id`는 백엔드 발급·말단 에코 방침 확인 → 기존 구현 유지. QoS 1 중복
  배달에 대비해 같은 command_id는 재실행하지 않고 이전 ACK만 재송신(되돌리기 어려운 명령 보호).
- VZ-O-01: 명령 형식을 추상 `action`+`params`로 전환(기존 평평한 형태도 당분간 수용),
  `expires_at` 만료 시 실행 거부.
- VZ-U-01: 정상 종료(`event:shutdown`)와 급사(`event:death`, LWT)를 구분 발행 —
  "장애"와 "의도적 미배포"를 가시화가 나눌 수 있어야 하기 때문.
- BE-T-01: 말단↔엣지 **MQTT 5.0 적용 완료**. TLS는 `HW_TLS_CA` 등 설정 자리만 두고
  엣지 브로커 인증서 준비 시 활성.

**장비 없이 착수 가능한 항목 구현**

- **HW-S-03 / S-04** 이벤트 모드 1 Hz + 적응형 주기 전환 명령. 임계 돌파 시 자동 전환하되,
  엣지가 명령으로 주기를 지정해 두면 자동 전환이 이를 덮지 않는다(`mode_source`로 우선순위 표현).
- **HW-R-09** 두절 시 버퍼링·재전송(`spool.py`). 디스크 JSON Lines에 기록 순서대로 적재 →
  재연결 시 그 순서대로 재전송하므로 타임스탬프 순서가 자연히 복원된다. 재전송분에 `replayed`
  표식. 상한 초과 시 오래된 것부터 폐기하고 폐기 건수를 `degraded` 판정 근거로 보고.
- **HW-C-05** metric 5종 OTLP(`otel_metrics.py`). 발행 지연은 publish 호출 반환이 아니라
  **PUBACK 수신 시점**으로 측정(호출 반환은 큐 적재일 뿐이라 항상 0에 가깝다).
  SDK·엔드포인트가 없으면 no-op으로 떨어져 노드는 정상 동작한다.
- **systemd 서비스**(`sensor-node.service`). `StateDirectory=hw-node`로 버퍼 경로 권한 확보,
  `After=chrony.service`로 시각 동기 후 기동, `Restart=always`.
- **설정 외부화**(`config.py`). 주기·임계값·브로커 주소를 전부 `HW_*` 환경변수로 노출 —
  협의 결과가 나와도 코드 수정 없이 systemd unit만 고치면 된다.

**검증(노트북 Mosquitto + paho 2.1.0, 전 항목 실측):**
명령 4단계 왕복 ✅ / 설정 명령 2단계(physical:false) ✅ / 중복 command_id 재실행 억제 ✅ /
거부 4종(누락·미지원·만료·파라미터 오류) ✅ / `rapid_change`가 임계 도달 **전에** 발행(2.937m) ✅ /
임계 돌파 시 자동 event 전환 → 해제 시 normal 복귀 ✅ / 명령 지정이 자동 전환보다 우선 ✅ /
브로커 15초 두절 중 21건 버퍼 적재 → 복구 시 seq 순서대로 전량 재전송, 버퍼 0 ✅ /
하트비트 4회(4초) 미수신 OFFLINE 판정 ✅ / `kill -9` 시 LWT 즉시(device_status:fault) ✅ /
정상 종료 시 `shutdown` 구분 발행 ✅ / OTel 미설치 시 무중단 fallback ✅

**검증 중 발견·수정한 결함:** `monitor.py`가 구독을 접속 전에 한 번만 걸어, 브로커가 한 번
재시작하면 재접속은 되지만 구독이 사라져 조용히 눈이 머는 상태였다(clean session).
구독을 `on_connect` 안으로 옮겨 해결.

**미검증(환경 부재):** OTLP 실제 export — 엣지 Collector가 없어 초기화·fallback 경로만 확인했다.
Collector 구축 후 실측 필요. 로봇·액추에이터·CCTV·K3s는 장비/인프라 확보가 선행.

**다음 할 일:** ① 백엔드 협의 — 토픽 도메인 체계, 스키마 필드 확정(그 뒤 `schema.py` 한 곳 수정),
하트비트 1Hz vs 5초, [G2]/[G3] 회신 ② 엣지노드 구축 후 브로커·OTel Collector 이전 + TLS 활성화
③ K3s 클러스터 구축(말단은 워커 노드만 — AI-B-10이 말단에 브로커·수집기 설치를 금지)
④ 장비 입고 시 HW-S-01(`read_water_level()` 교체) → HW-R / HW-A 계열.

### 2026-08-31 (2차) — 라즈베리파이 pi7 실기 검증 (Process 1)

노트북 모의가 아니라 **실제 말단(pi7)에 systemd 서비스로 배포해** 전 시나리오를 재검증했다.
안동 하천실험센터 UN 발표 준비 일정에 맞춰, 이후 모든 개발은 실기 검증을 거친다.

**실기 환경 실측:** Raspberry Pi 5 / Debian 13 trixie / Python 3.13.5 / 192.168.50.172,
chrony 동기 오차 77µs, `/etc/device_id`=`wl-001`, venv에 paho-mqtt 2.1.0 ·
opentelemetry 1.44.0(sdk·otlp grpc/http) · psutil 7.2.2 · grpcio 1.83.0,
cgroup memory 활성, **k3s·kubectl 설치되어 있으나 서비스 비활성**(HW-C-03 착수 가능 상태),
메모리 7.9Gi / 루트 여유 90G. `physical` 계정은 passwordless sudo 및 gpio·i2c·spi 그룹 소속.

**실기에서만 드러난 결함 3건과 수정**

| 결함 | 증상 | 원인·수정 |
|---|---|---|
| **SIGTERM 미처리** | `systemctl stop`·재부팅이 전부 LWT `death`/`fault`로 보고 | systemd는 SIGTERM으로 종료시키는데 핸들러가 없어 프로세스가 그냥 죽었다. 계획된 재시작이 장애로 보이면 VZ-U-01의 '장애 vs 의도적 미배포' 구분이 무너진다. `SIGTERM → KeyboardInterrupt`로 기존 정상 종료 경로에 합류 |
| **systemd unit 경로 오류** | `User=pi`, `/home/pi/...` — 실기 계정은 `physical` | 계정·경로 수정. 더불어 브로커 IP·구역 같은 **현장값을 unit에서 분리해 `/etc/hw-node.env`** 로 뺐다(`hw-node.env.example` 추가). 저장소에 현장 IP를 박으면 안동 현장과 실험실 설정이 서로를 덮어쓴다 |
| **device_id 중복 무경보** | 같은 client_id 두 노드가 서로를 밀어내며 초당 재접속. 로그만 보면 '네트워크가 불안한' 것으로 보임 | Mosquitto는 `session taken over`로 끊지만 그 사유가 클라이언트에 전달되지 않는 것을 실측 확인(paho `rc=0`). MQTT 5.0 사유코드 0x8E 검사에 더해 **20초 내 3회 재접속 빈도 판정**을 추가해 양쪽 노드 모두 경보 |

**실기 검증 결과(전 항목 pi7에서 재확인):** systemd 기동·자동 재시작 ✅ /
Birth→하트비트 1Hz→10초 요약 ✅ / 명령 4단계 왕복 ✅ / 중복 command_id 재실행 억제 ✅ /
거부 3종 ✅ / HW-S-04 주기 전환·auto 복귀 ✅ / 급변이 임계 도달 전 발행(2.92m) ✅ /
임계 돌파 시 자동 event 전환 → 해제 시 normal 복귀 ✅ / **실제 네트워크 두절 20초 중 31건
버퍼 적재 → 복구 시 seq 순서대로 전량 재전송, 잔량 0** ✅ / `kill -9` → LWT 즉시 +
systemd 3초 내 자동 재기동 ✅ / `systemctl stop` → `graceful_shutdown` 구분 ✅ /
device_id 중복 경보 양방향 ✅ / 중복 제거 후 30초간 재접속 0회 안정 ✅

**미검증:** OTLP 실제 export — 엣지 Collector가 없어 초기화·fallback 경로만 확인.
다음 프로세스에서 수신단을 세워 실측한다.

### 2026-08-31 (3차) — SRS/SDD 수립 + 구조 재편(센서/로봇 분할) (Process 2~4)

안동 하천실험센터 UN 발표 준비에 맞춰, 요구사항 정의서(xlsx) 30건을 기준으로
`docs/SRS.md`·`docs/SDD.md`를 세우고 그 설계대로 코드를 재편했다.

**타당성 검토를 서로 다른 모델 3종에 독립 의뢰하고, 상충·근거 부족 항목은 실기 확인**

| 쟁점 | 결론 | 확인 방법 |
|---|---|---|
| 말단 K3s | agent 상주는 타당(RAM 268MB, etcd/apiserver 없음). **server는 말단 금지** — AI-B-10 위반 | K3s 공식 자원 프로파일 |
| 주기 전환의 실체 | **컨테이너 배포에서 분리.** agent는 제어평면 미연결 시 기동 불가(k3s#1686), 이미지 pull 시 30~60초. 재난 골든타임 경로에 둘 수 없다 → 로컬 자율(주)/MQTT 명령(오버라이드)/컨테이너(증강)의 3층 | 공식 이슈·문서 |
| Pi 5 H.264 인코더 | **없음.** 비디오 장치가 전부 pispbe ISP, bcm2835-codec 인코더 노드 부재 | **pi7 실기 확인** — 모델 간 주장이 엇갈려 직접 검증 |
| 로봇 20Hz QoS | 상태 스트림 QoS 0, 이산 사건만 QoS 1 | 무선 왕복 실측 자료 |
| 로봇 버퍼링 | 전량 재전송은 복구 직후 링크를 막는다 → 이산 전량 / 연속 다운샘플 | 설계 판단 + 실측 검증 |

**구조 재편 (요구사항 8: 로봇과 센서 분할)**

공통 코어 `common/`(BaseNode·명령엔진·스키마·버퍼·관측)을 추출하고 `sensor/`·`robot/`이
이를 상속한다. 등록·명령 4단계·버퍼링·관측·식별은 공유하고, 계측 원천·보고 정책·명령
어휘만 갈린다. 하위는 `on_sample`/`on_tick`/`heartbeat_enabled`/`ACTIONS` 훅만 채운다.
로봇 제어기 링크는 `ControllerLink` 추상으로 두어 실물 확정 전까지 `SimLink`로 검증한다.

**재편 중 발견·수정한 결함 3건**

| 결함 | 증상 | 수정 |
|---|---|---|
| entity_type 미전달 | 로봇이 `zoneA/sensor/rb-01` 로 발행 | Identity 가 타입을 들고 다니게 하고 노드 클래스가 주입 |
| 발행 주기 양자화 | 20Hz 목표인데 **16.5Hz** — 정확히 1/0.06s. 상태 발행이 20ms 수집 틱 안에 있어 60ms 간격이 됨 | 발행을 `on_tick` 훅으로 분리 + `next_wakeup`으로 루프를 마감시각에 깨움 → **19.9Hz** |
| 재접속 백오프 | 25초 두절 뒤 복구까지 60초 초과. paho 기본 상한 120초는 두절이 길수록 복구가 늦어져 재난 대응과 방향이 반대 | `reconnect_delay_set` 상한 10초 → 20초 내 회복 |

**실기 검증 (pi7, 전 항목)**

센서 회귀 전 항목 통과(명령 4단계·중복 억제·거부 4종·급변 2.852m·임계·자동 전환·해제).
로봇 신규: 임무 4단계 ✅ / **임무 중 20Hz·하트비트 0Hz, 대기 1Hz·하트비트 1Hz 실측** ✅ /
**63초 두절 중 1257건 적재 → 다운샘플 후 64건 재전송, 잔량 0**(95% 감축) ✅ /
정상 종료 `event=shutdown, reason=graceful_shutdown` 과 급사 `event=death, reason=lwt`
구분 발행 ✅. 노드 자원: 메모리 피크 18M, 전체 CPU 2.2% — k3s agent(268MB) 여유 충분.

**다음:** 엣지노드 하드웨어 확보 후 ①OTLP 실 export 검증 ②K3s server 구성 + 말단 agent
편입 + **MQTT 1883 회귀 검증**(flannel/kube-proxy가 Debian 13 nftables 위에서 업무 경로에
간섭하지 않는지 — 현재 검증은 전부 k3s 비활성 상태 결과) ③증강 기능 컨테이너 배포/제거 시연.

### 2026-08-31 (4차) — HW-C-05 OTLP 실 export 검증 (Process 5)

그동안 초기화·fallback 경로만 확인하고 **실제로 무엇이 나가는지는 미검증**이던 관측 평면을
닫았다. 엣지 Collector 실물이 없으므로 노트북에 최소 OTLP/gRPC 수신단을 세워 대체했다.

**검증 결과:** metric 5종(CPU·메모리·디스크 여유·발행 성공/실패·발행 지연)이 실제로 도착.
resource 속성에 `service.instance.id`·`hw.entity_id`·`hw.node_id`·`hw.zone_id`가 실려
어느 노드의 지표인지 구분된다. 발행 지연은 PUBACK 왕복으로 **실측 2.2~52 ms**.

**수신단에서만 드러난 결함 3건**

| 결함 | 증상 | 수정 |
|---|---|---|
| service.name 고정 | 로봇 지표까지 `hw-sensor-node` 로 들어와 Collector 에서 노드 종류를 구분할 수 없었다 | `hw-{entity_type}-node` 로 노드 종류 반영 |
| QoS 0 지연을 0ms 로 기록 | QoS 0 은 PUBACK 이 없어 왕복이 없는데 0을 넣어 히스토그램이 0 쪽으로 쏠렸다 — "빠르다"는 거짓 신호 | 측정된 것만 기록. 로봇 314건 발행 중 QoS 1 인 15건만 지연 표본 |
| CPU 첫 표본 0.0 | `psutil.cpu_percent(interval=None)` 첫 호출은 항상 0.0 | 초기화 때 한 번 미리 호출 |

**남은 한계:** 검증에 쓴 것은 최소 수신단이며 BE-S-02 가 요구하는 엣지 Collector(Agent) →
백엔드 Gateway 구조의 Agent 실물은 여전히 부재. 엣지노드 확보 후 재검증 대상.

### 2026-08-31 (5차) — HW-C-03 K3s 실검증: 엣지 제어평면 구성 + 말단 워커 편입 (Process 6)

엣지노드 전용 장비가 없어 **개발 PC의 WSL2 Ubuntu 26.04를 임시 엣지노드**로 세우고
HW-C-03 전 과정을 실기 검증했다. 절차는 `docs/EDGE_SETUP.md`.

**구성:** WSL2 미러 네트워킹 → K3s server(`edge-wsl`, v1.36.4) → pi7을 agent로 편입
(`pi7 Ready <none>`, arm64, containerd 2.2.3). 설계대로 **제어평면은 엣지에만** 두고
말단은 워커로만 넣었다.

**막혔던 두 지점**

| 증상 | 원인 | 해결 |
|---|---|---|
| 파이에서 6443 TCP는 붙는데 `/cacerts` 가 connection reset | WSL2 미러 모드의 **Hyper-V 방화벽이 인바운드 기본 차단**(`DefaultInboundAction: Block`). 포트별 허용 규칙은 관리자 권한 필요 | `.wslconfig` 에 `firewall=false` — 권한 없이 되고 되돌리기도 쉽다 |
| 환경파일을 바꿨는데 계속 옛 서버(210.110.250.33)를 바라봄 | 이미 active 인 유닛에 `systemctl start` 는 무동작 | `restart` 사용. 한동안 조인 실패로 오진했다 |

**검증 결과**

| 항목 | 결과 |
|---|---|
| 컨테이너 배포 | pi7 에 스케줄·기동 (파드 IP 10.42.1.2) |
| **셀프힐링** | `crictl stop` → Exit Code 137 → K3s 자동 재기동(RESTARTS 1) |
| 컨테이너 제거 | 파드 정리, 노드 Ready 유지 |
| **MQTT 1883 회귀** | k3s agent 활성 상태에서 계측·하트비트 정상(1.0 Hz). **flannel·kube-proxy 가 Debian 13 nftables 위에서 업무 경로에 간섭하지 않음** — SRS §9.6 미결 항목 해소 |
| **층 분리(HW-S-05)** | 컨테이너가 죽는 동안에도 노드 하트비트는 무중단. "컨테이너 장애"와 "노드 장애"가 실제로 구분됨 |

**남은 한계:** WSL2 는 검증용이다. 개발 PC 가 꺼지면 구역이 제어평면을 잃으므로
현장에는 전용 리눅스 장비(+SSD, Mosquitto·OTel Collector·미디어 게이트웨이 동거)가 필요하다.

### 2026-08-31 (6차) — 액추에이터 제어노드 구현 (Process 7)

HW-A 계열 5건을 구현하고 pi7에서 검증했다. 액추에이터 실물이 없으므로 `ActuatorLink`
추상 뒤에 `SimActuator`를 두고, 실물 확보 시 링크 구현체만 갈아끼운다(로봇의
`ControllerLink`와 같은 방식).

**설계에서 가장 신경 쓴 두 가지**

1. **`unknown`(상태 확인 불가)을 따로 둔다.** HW-A-01이 5종 구분을 요구하는데,
   "닫혀 있다"와 "닫혔는지 모른다"를 같은 값으로 뭉치면 피드백 선이 끊긴 수문을
   시스템이 '닫힘'으로 오인한다. 재난 대응에서 이 오인은 사람을 위험에 빠뜨린다.
2. **거부를 ACK 이전에 한다.** HW-A-03은 "수행할 수 없는 경우 거부 사유 반환"을
   요구한다. 받아 놓고 나중에 failed로 돌려주면, 되돌리기 어려운 명령을 다루는 관제가
   "일단 갔다"고 오해하는 시간이 생긴다. 공통 명령 엔진에 `validate()` 훅을 추가해
   잠금·미지원 장치·잘못된 목표·구동 중(busy) 4종을 ACK 단계에서 거부한다.

**검증 중 수정한 결함:** 잠금 해제 경로가 통신 두절에만 걸려 있어, 피드백이 복구돼도
잠금이 풀리지 않았다. 해제 조건을 사유와 무관하게 통일했다 — 어느 사유였든
"피드백이 살아 있고 상태가 확정적(대기·완료)일 때만" 푼다.

**실기 검증(pi7):** 수문 개방 4단계(ACK→수행중→**피드백으로 확인한 물리 도달**→완료) ✅ /
ACK 이전 거부 4종 ✅ / 피드백 상실 → 잠금 + `unknown` 보고 → 명령 거부 → 복구 시
실제 위치 재확인 후 자동 해제 ✅ / 통신 두절 → 잠금 + **수문이 open→close(안전 상태)로
자동 복귀** → 복구 후 해제 ✅ / 센서·로봇·액추에이터 3종 동시 가동 ✅

### 2026-08-31 (7차) — Pi 5 영상 인코딩 실측 (Process 8)

HW-S-06·HW-R-07 착수 전 선행 조건이던 인코딩 부하를 pi7에서 실측했다.
스크립트는 `pi/bench/encode_bench.sh`(재현 가능).

**측정 조건:** 4코어, 세 노드(sensor·robot·actuator) + k3s agent 상주 중, 각 20초,
libx264 `-tune zerolatency`, 소스를 `-re`로 실시간 공급(이게 없으면 인코더가 최대 속도로
돌아 항상 100%가 나와 실제 운용 부하를 알 수 없다).

| 용도 | 해상도 | preset | 코어 | 4코어 대비 |
|---|---|---|---|---|
| AI 입력(HW-R-04) | 640x480@15 | veryfast | 0.13 | 3% |
| 관제(HW-R-07) | 1280x720@15 | veryfast | 0.33 | 8% |
| 관제(HW-R-07) | 1920x1080@15 | veryfast | **0.70** | 18% |
| CCTV 상시(HW-S-06) | 1920x1080@15 | ultrafast | 0.41 | 10% |
| CCTV 상시(HW-S-06) | 1280x720@15 | ultrafast | 0.19 | 5% |

**안전 루프 간섭 검증(가장 중요):** 1080p15 + 720p15 를 **동시에** 인코딩하는 동안
(전체 CPU 23.8%, 70.3°C, 스로틀 없음) 로봇 상태 보고가 **19.9 Hz 유지**. 인코딩이
안전 루프를 굶기지 않는다 — SDD 원칙 P1이 실측으로 지지됨.

**결론 정정:** 착수 전 우려와 달리 하드웨어 인코더 부재가 영상 경로를 막지 않는다.
상시 CCTV + 온디맨드 관제 스트림 동시 운용이 CPU 상 가능하다. 다만 ①합성 패턴 기준이라
실제 카메라 영상에는 1.5배 여유를 둔다 ②남는 예산은 온디바이스 추론이 가져가므로
지배 제약은 인코딩이 아니라 추론이다 ③HW-R-07 온디맨드는 CPU 가 아니라 **무선 대역폭**
때문에 유지한다.

**설치:** 벤치마크를 위해 파이에 `ffmpeg` 를 설치했다(`rpicam-vid` 는 카메라 필요).

### 2026-08-31 (8차) — 증강 기능 컨테이너 구현·배포 (Process 9)

HW-C-03의 증강 층에 자리표시자(busybox) 대신 **실제로 의미 있는 워크로드**를 넣었다.

**`pi/augment/analyzer.py` — 추세 분석기.** 센서 상태 채널을 구독해 5분 창의 선형 회귀로
수위 상승 기울기(m/분)와 임계 도달 예상 시간을 계산해 `{zone}/analysis/{id}/state`로 발행한다.
말단 노드는 "지금 3.0m를 넘었다"만 말하고(그게 골든타임 경로다), 이 컨테이너는 "이 속도면
n분 뒤 넘는다"를 더한다 — **있으면 더 빨리 대응하지만 없다고 경보가 사라지지는 않는**
정보다. 증강 기능의 성질이 정확히 그렇다.

**검증 중 수정한 결함 2건**

| 결함 | 증상 | 수정 |
|---|---|---|
| 표본 개수만으로 추세 판정 | 3초 주기 표본 5개(15초)의 잔물결에 회귀선을 그어 "3.3분 뒤 임계 도달" 오경보 | 시간 폭도 본다. 창이 60초 미만이면 `판단보류` |
| 임계 초과 시 ETA=0.0 | 이미 넘은 상태에서 "0.0분 후 도달"은 소비자가 '방금 넘었다'로 오해 | ETA는 `null`, `above_threshold: true` 플래그로 분리 |

**배포 산출물:** `pi/augment/Dockerfile`(arm64, 공통 코어 포함, 비루트), `pi/augment/build.sh`
(digest 출력 + 레지스트리 없을 때 `k3s ctr images import` 경로), 매니페스트 2종 —
운영본(digest 고정)과 검증본(레지스트리 부재 시 hostPath+pip).

**엣지 구성에서 발견한 한계:** 파이 파드에서 **엣지 파드로 가는 flannel VXLAN 경로가
통하지 않는다**(외부 인터넷은 도달 OK, 엣지 CoreDNS 파드는 TimeoutError). 클러스터 DNS가
말단 파드에서 죽으므로 증상이 "pypi.org 이름 해석 실패"로 나타나 원인을 네트워크가 아닌
곳에서 찾기 쉽다. **안전 루프는 영향 없다**(호스트 네트워크 + 브로커 IP 직결).
증강 워크로드는 `dnsPolicy: Default`로 노드 리졸버를 쓰게 해 해결했다 —
어차피 서비스 탐색을 쓰지 않으므로 실무적으로도 더 튼튼한 선택이다.
전용 엣지 장비가 필요한 또 하나의 이유로 `docs/EDGE_SETUP.md` §4-1에 기록했다.

**실기 검증:** 컨테이너 기동 → 센서 구독 → 창 60초 미만 `판단보류` → 이후 +5.69m/분
추세 산출 → 임계 초과 표시 → 제거 → 안전 루프 4개 서비스 전부 무영향 ✅

### 2026-08-31 (9차) — 백엔드 협의 안건 정리 (Process 10)

미결 항목을 흩어진 상태로 두지 않고 `docs/BACKEND_AGENDA.md` 한 장으로 모았다.
받는 쪽이 바로 판단할 수 있도록 항목마다 **배경 / 하드웨어 현재 처리 / 필요한 결정 /
미확정 시 영향**을 붙이고 우선순위(🔴 발표 전 필수 · 🟡 권장 · ⚪ 이후)를 매겼다.

**🔴 발표 전 필수 6건:** 스키마 필드 확정, 토픽 도메인 체계, 하트비트 주기 상충
(정의서 1Hz vs 아키텍처 v4 5초 — 1Hz라야 4초 오프라인 판정이 성립), [G2] command_id
에코 해석, [G3] device_status 발행 주체, **엣지노드 전용 장비**.

**핵심 메시지:** 말단↔엣지 구간은 이미 동작한다. 남은 것은 "안 되는 것"이 아니라
**결정권이 백엔드에 있어 하드웨어가 임시값으로 두고 있는 것**이다. 미확정 항목은
`pi/common/schema.py` 한 곳에 가둬 두었으므로 확정이 오면 그 파일만 고치면 전 노드에
반영된다 — 지금 확정될수록 재작업이 없다는 점을 문서 서두에 명시했다.

### 2026-08-31 (10차) — 임시 엣지 철수, pi7 원래 클러스터 복귀

pi7은 이 프로젝트 전용 장비가 아니라 **다른 클러스터(`210.110.250.33`)의 워커**였다.
HW-C-03 검증을 위해 임시로 개발 PC의 WSL 제어평면에 붙였다가, 검증이 끝나 원복했다.

| 항목 | 원복 |
|---|---|
| pi7 k3s-agent | 원래 서버로 복원 → **원래 워크로드(`armc`: `twin-w-ys-pi7`·`twin-w-fastsam-pi7`·`metrics-pusher`) 복귀 확인** |
| 임시 엣지 | `kubectl delete node pi7`, WSL k3s `stop/disable`(설치는 유지) |
| `.wslconfig` | 원복 — 미러 네트워킹·`firewall=false` 해제(WSL 트래픽 방화벽 필터링 복구) |

**하드웨어 서비스 4개는 이 과정에서 무영향이었다.** sensor/robot/actuator 노드는 systemd로
호스트 네트워크에서 돌고 브로커를 IP로 직접 잡으므로 오케스트레이터와 무관하다 —
설계 원칙 P1이 실제 철수 과정에서 다시 확인됐다.

HW-C-03 검증 결과 자체는 유효하다(배포·셀프힐링·제거·MQTT 무간섭 전부 실측 완료).
재현 절차는 `docs/EDGE_SETUP.md` §6에 남겼다. 다만 **pi7을 다시 붙이면 원래 클러스터에서
빠지므로**, 전용 엣지 장비 확보 전까지는 검증이 필요할 때만 잠깐 붙였다 되돌린다.

### 2026-08-31 (11차) — 데이터 전송 및 아키텍처 v8 정합 검토

`docs/260827_데이터_전송_및_아키텍처v8.docx`를 받아 SRS·SDD·구현과 대조했다.
상세는 SRS §9.7·9.8.

**해소 4건** — v8이 확정해 준 것

| 미결 | v8 |
|---|---|
| O-3 관측 발신 방식 | §5-3·5-8 말단 OTLP 직접 발신 명시 확정 |
| O-7 [G2] command_id | §5-6 백엔드 발급·MySQL 감사. **에코 구현이 맞았다** |
| O-8 frame_ref | §6-9 `{source_id, capture_timestamp(epoch ms), sequence_id}` |
| O-9 미디어 규격 | §5-10·6-8 말단↔엣지 **RTP/UDP + JPEG**, 엣지 로컬 소비, 온디맨드 WebSocket 중계 |

**상충 3건** — v8과 요구사항 정의서가 어긋난다. 결정 필요

| 항목 | 정의서 | v8 | 쟁점 |
|---|---|---|---|
| 하트비트 | 1 Hz | 5초 | **HW-S-07이 "1Hz 기준 4회 = 약 4초"를 규정한다.** 5초면 판정이 20초가 되어 정의서를 못 지킨다. v8은 HW-S-07을 언급하지 않아 충돌이 해소되지 않았다 |
| OTel export | 60초 | 15초 | 정의서만 60초 |
| 로봇 대기 상태 | 1초 | 5초 | 대기 중 하트비트(HW-R-02)와의 관계도 함께 정리 필요 |

셋 다 환경변수라 **확정 값만 오면 코드 수정 없이 반영**된다.

**신규 설계 긴장 1건 (O-15).** v8 표5는 "엣지 Prometheus가 각 노드를 확인해 up=0"으로
장치 생사를 본다는데, §5-8은 말단이 **OTLP push 전용**이라고 한다. Prometheus의 `up`은
scrape 대상에만 생기므로 push 전용 노드에는 존재하지 않는다. 하드웨어는 "엣지 Collector를
scrape하고 말단별 라벨의 신선도로 판정"을 제안하고 백엔드 확인을 요청했다.

**신규 작업 4건.** ①**JPEG 기준 인코딩 재측정**(기존 벤치는 H.264 기준이라 이 설계에
맞지 않는다. Pi 5의 JPEG 하드웨어 인코딩 여부도 함께 확인) ②RTP/UDP 송출 구현
③말단 trace(span) 계측 범위 결정(v8이 하드웨어에 위임) ④frame_ref 시각 표기 정합
(업무 봉투는 ISO 8601, frame_ref는 epoch ms).

**v8이 현 설계를 확인해 준 것:** 말단에 서버 미설치(P4), 업무·관측 평면 분리(P2),
LWT+하트비트 이중 감지, 명령 MQTT+QoS+ACK, 엣지에 Mosquitto·OTel Collector·K3s.
특히 §6-5의 "두 평면이 갈릴 때 최종 가용성은 업무 평면(MQTT 세션) 우선"은 상태 3층
구현과 정확히 정합한다.

### 2026-08-31 (12차) — 아키텍처 v8 반영 + 영상 송출 구현 (HW-R-07)

v6가 v8로 교체되어 재대조하고, v8이 확정해 준 만큼 **영상 송출을 실제로 구현·검증**했다.

**v6 → v8 변경 중 하드웨어 영향 3건**

| 변경 | 영향 |
|---|---|
| frame_ref를 **엣지가 발급**, 재생성 금지 (§6-9) | **작업이 줄었다.** 말단은 RTP 순번·타임스탬프만 실으면 되고 자체 식별자를 안 만든다. 대신 아래 구멍이 생겼다 |
| JPEG 확정 강화 — "무선 부족 시 압축 코덱 전환" 단서 **삭제** | 대역폭 탈출구가 문서상 사라짐 |
| "연결은 pull 기본" 명시 | 로봇은 같은 절에서 push라 해석 확인 필요 |

주기 3건(하트비트 5초·OTel 15초·로봇 대기 5초)은 v8에서도 그대로라 상충이 유지된다.

**JPEG 실측 — H.264 기준 벤치를 대체**

| 해상도 | q | 코어 | 파일 | 실제 전송(RTP) |
|---|---|---|---|---|
| 640x480@15 | 5 | 0.09 (2%) | 2.6 Mbps | — |
| 1280x720@15 | 5 | 0.24 (6%) | 5.8 Mbps | **6.8 Mbps** |
| 1920x1080@15 | 5 | 0.48 (12%) | 9.7 Mbps | — |
| 1920x1080@15 | 12 | 0.46 (12%) | **5.5 Mbps** | — |

**JPEG가 H.264보다 CPU가 싸다**(1080p15에서 0.48 vs 0.70 코어). 대신 대역폭 2.4배.
**품질(q)이 대역폭의 지렛대**이고 CPU는 거의 안 변한다 — 무선 홉의 로봇은 해상도를
낮추기 전에 q를 올리는 편이 손해가 적다.

**HW-R-07 구현·검증 완료.** `robot/media.py`에 `MediaSender` 추상 + `RtpJpegSender`.
명령 전 0패킷 → MQTT `stream start` → **6.8 Mbps, RTP payload type 26(JPEG), 결손 0%**
→ 중복 개시 ACK 이전 거부 → `stop` 후 0패킷, ffmpeg 잔여 0.

**구현에서만 드러난 함정 — RFC 2435.** `ffmpeg -c:v mjpeg -f rtp`를 그대로 쓰면
**한 바이트도 전송되지 않는다.** ffmpeg의 mjpeg 인코더는 기본이 최적화 허프만 테이블인데
RFC 2435는 표준 테이블만 허용해 RTP 먹서가 매 프레임을 버린다. 프로세스는 살아 있고
CPU도 10% 쓰며 인코딩을 계속하므로 증상이 보이지 않는다. `-huffman default`로 해결.
이 경험 때문에 구현에 **"실제로 바이트가 나가는지 확인한 뒤에야 state_changed를 낸다"**
는 확인 단계를 넣었다.

**v8이 새로 만든 구멍 (O-18, 협의 안건 10-4).** ①v8 §6-9는 frame_ref를 엣지가 단일
발급하고 재생성을 금지한다 ②v8 §5-10은 온디바이스 프레임이 엣지를 거치지 않는다고 한다
③VZ-I-07은 온디바이스 결과를 오버레이하라고 한다. 셋이 함께 서지 않는다 —
**온디바이스 결과에 붙일 frame_ref가 없다.** 하드웨어는 `source_id`를 `rb-01_ondevice`로
분리해 말단이 별개 발급하는 안을 제안했다(다른 프레임이므로 재생성이 아니다).

**신규 미결:** O-17 무선 대역폭(로봇 홉1은 무선인데 v8은 로컬 LAN을 전제), O-18 위 구멍.

### 2026-08-31 (13차) — 주기 값을 백엔드 기준으로 통일

정의서(xlsx)와 아키텍처 v8이 갈리던 주기를 **백엔드(v8) 기준으로 채택**하고 정의서를
그에 맞춰 개정했다. 하드웨어가 문서 간 상충을 붙들고 있기보다 전송 아키텍처의 소유자를
따르는 편이 맞다고 판단했다.

| 항목 | 기존(정의서) | 채택(v8) |
|---|---|---|
| 하트비트 (HW-S-05·R-02·A-05) | 1 Hz | **5초** |
| OTel export (HW-C-05) | 60초 | **15초** |
| 로봇 대기 상태 (HW-R-03) | 1초 | **5초** |

HW-S-03(이벤트 모드 1 Hz)은 v8도 "재난 1초"라 그대로 두었다.

**파급과 실측.** 하트비트가 5초가 되면 HW-S-07의 "4회 미수신" 규칙상 판정이 4초→20초가
된다. 그런데 **실질 악화는 그보다 작다** — 대부분 LWT가 먼저 잡기 때문이다.

| 끊김 유형 | 이전 | 현재 |
|---|---|---|
| 프로세스 급사 | 즉시 | **즉시** (변화 없음) |
| 침묵형 링크 끊김 | 7.5초 | **15.0초** (nftables로 주입해 실측) |
| 하트비트 타임아웃(백스톱) | 4초 | 20초 |

즉 **감지 속도의 지렛대는 하트비트 주기가 아니라 `keepalive`** 다. v8이 정하지 않은 값이라
하드웨어가 고르며, 하트비트(5초)보다 충분히 커야 정상 트래픽을 끊김으로 오인하지 않으므로
10초(LWT 15초)로 두었다.

**정의서 개정 중 발견·수정한 실수.** `1초 1회 (1 Hz)` 문자열이 HW-S-05와 HW-S-03에 모두
있어 첫 치환이 **HW-S-03(이벤트 모드)을 잘못 바꿨다.** 확인 과정에서 발견해 되돌리고,
`<t>` 요소 전체를 앵커로 삼아 정확히 지정했다. 이후 치환은 출현 횟수가 1인지 먼저 확인한다.

### 2026-08-31 (14차) — Unitree Go1 실기 연결 (HW-R-01)

pi7의 eth0를 Go1 등판 이더넷에 연결해 실제 로봇 텔레메트리를 받았다.

**연결 확인:** pi7 eth0 1 Gbps / `192.168.123.162`, Go1 내부 노드 5개 전부 응답
(`.161` Pi CM4 · `.10` 컨트롤러 · `.13/.14/.15` Nano). Nano 3대에 카메라 노드가 있어
**Edu 모델**로 확인. sportMode **1.38.0**(SDK 호환 하한 1.32 상회), SDK 기설치.

**핵심 판단 — SDK(UDP)를 쓰지 않았다.** Go1의 고수준 SDK 경로(8082)는 요청/응답 구조라
상태를 받으려면 `HighCmd`를 보내야 하는데, 그 순간 sport mode에서 **제어권을 가져와
서 있던 로봇이 주저앉을 수 있다.** 대신 Go1이 자체 MQTT 브로커(`.161:1883`)로 이미
발행 중인 텔레메트리를 **구독만** 했다. 구독은 로봇을 움직일 수 없다.

**역공학 결과** (`robot/state` 84바이트, 12.5 Hz — 공개 규격 없음)

| 오프셋 | 해석 | 확신도 |
|---|---|---|
| 0~5 | 몸통 roll·pitch·yaw (도) | 높음 — 기립 시 (0,0,-25) |
| 6~29 | 12관절 각도 (도) | **높음** — FR/FL (0,46,-92), Go1 기립 자세와 일치 |
| 60~67 | 몸통 높이 (m) | **높음** — 0.295, Go1 기본 0.28과 일치 |
| 52~59 | 위치 x·y (추정) | 낮음 — 정지 중이라 구분 불가 |
| 68~75 | 속도 x·y (m/s) | 중간 |

`bms/state`는 SDK `comm.h`의 `BmsState`와 **정확히 일치**했다 —
SOC·전류·사이클·온도가 전부 물리적으로 타당한 값으로 디코드됐다.

**수정한 결함:** `JOINTS` 상수를 int16 **인덱스**로 두고 `struct.unpack_from`에
**바이트 오프셋**으로 넘겨 관절이 쓰레기값(-6400, 255, 11776…)으로 나왔다. 6으로 고쳐
정상 각도 확인. 상수에 "전부 바이트 오프셋"이라고 주석을 박았다.

**명령(HW-R-06)은 의도적으로 막아 두었다.** `send_command()`가 `NotImplementedError`를
낸다. 로봇이 엎드린 상태에서 사람이 지켜보는 가운데 별도로 붙인다.

**미확정:** 위치·속도 매핑은 로봇이 정지 중이라 확정할 수 없다. 앱·리모컨으로 주행시키며
스트림을 관찰하면 **명령을 보내지 않고** 확정할 수 있다. `mode`도 실제 필드를 못 찾아
높이·속도로 추정 중이다.

### 2026-08-31 (15차) — Go1 카메라 경로 조사 (HW-R-07)

Go1 영상을 pi7으로 받아보려 시도했다. **경로는 뚫렸고 디코딩에서 막혔다.**

**가장 큰 소득 — Go1은 이미 RTP로 보낸다**

```
Go1 카메라(Nano) ──RTP/H.264 over UDP──► 지정 IP
포트 9201 전방 · 9202 턱 · 9203 좌 · 9204 우 · 9205 배
대상: UnitreeCameraSDK 설정의 IpLastSegment → 192.168.123.<값>
```

v8이 정한 전송 계층(RTP/UDP)과 **이미 같다.** 코덱만 H.264로 다르다(v8은 JPEG).
설계 함의가 크다 — 로봇 영상은 `media.py`로 재인코딩할 필요 없이 엣지가 그대로
받으면 되고, 말단의 디코드·재인코딩 왕복과 CPU가 사라진다. 신규 미결 O-20.

**막힌 지점.** 캘리브레이션된 카메라는 전부 로봇의 `camerarosnode`가 점유 중이다.
비어 있던 턱 카메라로 시도하니 RTP 패킷은 나갔으나(700패킷, PT 96 = H.264)
SDK가 그 카메라의 캘리브레이션을 못 읽어(`load tmp file failed, temp file permission
error`) SPS/PPS 없는 스트림이라 ffmpeg이 해상도를 못 알아냈다.

Unitree 문서는 자체 영상 전송 시 `camerarosnode`·`imageai`·point cloud를 **죽이라고**
명시한다. 그러면 **로봇의 장애물 인식이 멈춘다.** 배터리도 31%까지 떨어져 오늘은
여기서 중단하고 충전 후 정지 상태에서 진행하기로 했다. 신규 미결 O-19.

**로봇은 원상복구했다.** 송신기 종료, 자체 스택 전부 정상, 설정 파일 무변경.
턱 카메라만 USB 재열거로 `video2→video3`가 됐는데 아무도 쓰지 않는 장치라 무해하다.

**수정한 결함 — eth0 IP가 재부팅에 날아갔다.** `ip addr add`로 임시 설정했더니 파이가
재부팅되며 Go1 연결이 통째로 끊겼다. NetworkManager 프로파일(`go1-link`)로 영구
등록하고 `pi_base_setup.sh --go1 <IP>` 옵션으로 재현 가능하게 했다.
**기본 경로는 절대 eth0로 넘기지 않는다**(`ipv4.never-default`) — Go1 내부망에는 우리가
쓸 게이트웨이가 없어 넘어가면 엣지·브로커 연결이 전부 끊긴다. 스크립트가 이것도 검사한다.
