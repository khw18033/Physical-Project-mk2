# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 0. 코드베이스 작업 가이드

§1 이후는 요구사항 정의서(53개 AI-* 요구사항)이며 구현 범위의 기준이다.
이 절은 실제 저장소에서 작업할 때 필요한 정보만 요약한다.

### 명령

Python 패키지는 `perception-framework/`에 있다. 모든 명령은 그 디렉터리에서 실행한다.

```bash
pip install -e ".[dev]"      # 개발 설치 (Python >= 3.10, jsonschema/numpy/opencv/protobuf)
pytest -q                    # 전체 테스트
# 시스템 pytest 6.2.5 + anyio 플러그인 충돌로 collection이 깨지면:
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -q
pytest tests/test_selector.py::test_name   # 단일 테스트

pip install -e ".[sim]"                       # 시뮬레이터 전용 (pyjevsim, 최초 1회)
PYTHONPATH=. python simulator/simulation.py   # 하드웨어 없이 프레임워크 성질(선택 기능 축소,
                                               # provider 교체, 물리 명령 lifecycle 등) 확인하는
                                               # DEVS 기반 시뮬레이터 — simulator/README.md 참고
pytest tests/test_scenarios.py                # 같은 시나리오(simulator/scenarios/*.json)를 자동 검증으로
```

빌드·lint 도구는 아직 설정되어 있지 않다.

### 구조와 흐름

핵심은 "무엇을 실행할 수 있는가"를 런타임에 정하는 3단 구조다. 상위 로직
(perception/decision/risk/reference)은 구체 기술을 절대 import하지 않는다.

1. `contracts/capability.py` — `CapabilityRequirement.evaluate(available)`이
   required/optional 선언을 `ACTIVE / DEGRADED / DISABLED`로 환산한다.
   optional 결손은 DEGRADED까지만 내려가고 DISABLED가 되지 않는다는 규칙이
   프레임워크 전체의 장애 격리 근거다(AI-C-05).
2. `contracts/profile.py` — `CompatibilityProfile`(required/preferred 태그 +
   `ResourceCost` + `priority`), `ResourceBudget`, `DeploymentProfile`.
   하드웨어·런타임 조건은 벤더 enum이 아니라 자유 문자열 태그다. 새 하드웨어는
   태그+프로파일 등록으로 추가하고 이 모듈을 수정하지 않는다.
3. `registry/capability_registry.py` — capability kind별 provider 등록부.
   `_local`(자기 노드, 항상 authoritative)과 `_remote`(중앙 레지스트리의 마지막
   스냅샷)를 분리해 중앙이 끊겨도 마지막 known-good로 계속 동작한다. 장치 최종
   가용성은 백엔드 판정을 **입력으로 소비**할 뿐 여기서 재판정하지 않는다.
4. `selection/selector.py` — 호환 후보 중 `(priority, compute_units)` 최소를
   고른다. 후보가 없으면 예외가 아니라 `SelectionResult(provider=None, reason=...)`을
   반환한다. 호출자는 이 None을 정상적인 축소 경로로 처리해야 한다.

`providers/adapters.py`의 Protocol들(Transport/Serializer/MediaSource/AIRuntime/
Control/Observability/NetworkOverlay/ModelDeployment)이 인프라 경계다. `providers/fakes.py`가
테스트·데모용 구현이며, MQTT/Kafka/OTel/K3s/Tailscale 같은 현재 배포 기술은
각 provider 뒤에 숨긴다. 물리 명령의 의미 계약(AI-C-20)은 `contracts/physical_command.py` +
`execution/command_execution.py`가 따로 소유하며, ROS 2 Action/AIP-155를 설계 근거로만
참고하고 해당 런타임에는 의존하지 않는다.

기능 모듈은 요구사항 대분류를 그대로 따른다: `ondevice/`(AI-N), `edge/`(AI-E),
`perception/`(AI-S, AI-E-01/04), `risk/`(AI-R), `execution/`(AI-B, AI-C-20),
`observability/`(AI-O), `continual/`(AI-L), `reference/`(AI-N-01 수직 슬라이스).
구 AI-D(서브태스크 생성·검증) 요구사항은 최신 요구사항 시트에서 제거되어 가시화 파트로
이관되었다 — 관련 코드·계약은 이 저장소에서 삭제되었다(git 이력 참고).

### 코드 작성 규약

- 모든 모듈·테스트 docstring 상단에 `implements: AI-X-NN` 형식으로 요구사항 ID를
  남긴다(§7). 기존 파일들이 이 형식을 지키고 있으니 그대로 따른다.
- 주석·docstring은 영문 설명 + 요구사항 인용은 원문 한국어를 섞어 쓰는 기존
  스타일을 유지한다.
- 변수·스키마 이름은 [perception-framework/perception_framework/contracts/data_dictionary.py](perception-framework/perception_framework/contracts/data_dictionary.py)의
  `AI-C-01` 공통 데이터 사전에 등록한 의미 단위로 통일한다. 새 이름이 필요하면
  먼저 사전에 의미·생산자·소비자·데이터 평면을 추가한다.
- 구현 상태는 [docs/ai/requirement-traceability.md](docs/ai/requirement-traceability.md)에
  요구사항 ID별로 기록되어 있다. 코드를 추가하면 이 표도 갱신한다.
  구조 설명은 [docs/ai/architecture.md](docs/ai/architecture.md).
- 그 밖의 설계 결정·가이드·검증 계획 문서는 목적별로 분류되어 있다 —
  새 문서를 찾거나 추가하기 전에 [docs/ai/README.md](docs/ai/README.md)에서
  위치를 먼저 확인한다(설계 결정은 `docs/ai/design/`, 확장 절차는 `docs/ai/guides/`,
  검증 계획은 `docs/ai/validation/`).
- 작업 단위별 보고서는 `reports/YYYY-MM-DD_HHMM_주제.md` 형식으로 남긴다.

## 1. 문서 목적

이 파일은 AI·엣지 실행 프레임워크를 구현하는 AI 개발 에이전트를 위한 최상위 요구사항 문서다.
구현의 목표는 특정 AI 모델의 성능을 높이거나 특정 하드웨어·프로토콜·런타임에 최적화된 단일 애플리케이션을 만드는 것이 아니다.
목표는 서로 다른 센서, 카메라, 연산 하드웨어, AI 기능, 실행 위치, 전송 기술, 적용 도메인이 바뀌어도 핵심 업무 로직을 수정하지 않고 확장·교체·축소 운용할 수 있는 범용 프레임워크를 구현하는 것이다.

이 문서의 요구사항 ID가 구현 범위의 기준이다. 요구사항에 없는 기능을 임의로 추가하지 말고, 구현상 필수라고 판단되는 추가 기능은 먼저 `추가 요구사항 필요`로 보고한다.

## 2. 절대 준수 원칙

1. 특정 센서 제품, 카메라 종류, GPU/NPU 벤더, AI 모델 구조, 모델 파일 형식, 추론 런타임, 메시징 프로토콜, 직렬화 형식, 저장소, 오케스트레이터를 프레임워크 핵심 코드에 하드코딩하지 않는다.
2. 구체 기술은 현재 배포에서 선택된 provider 또는 adapter 구현일 뿐이다. 상위 인지·판단·위험 분석·실행관리 로직은 목적 수준의 공통 인터페이스만 사용한다.
3. `로봇`, `시설 감시`, `하천` 등 도메인명을 기준으로 핵심 코드에 분기문을 추가하지 않는다. 도메인 차이는 배포 프로파일, 입력 adapter, 활성 capability, 규칙, 실행 위치와 외부 제어 연결로 표현한다.
4. 각 기능은 필수 capability와 선택 capability를 구분한다. 선택 기능의 추가·제거·장애가 관련 없는 기능의 시작·실행·복구를 방해해서는 안 된다.
5. 현재 사용 가능한 기능과 자원을 런타임에 탐색하고, 요구 기능·안전·지연 조건을 만족하는 범위에서 필요한 자원만 사용한다.
6. 자원이 부족하거나 기능 제공자가 사라지면 우선순위가 낮은 선택 기능부터 축소하고 가능한 구성으로 재구성한다. 전체 시스템을 불필요하게 중단하지 않는다.
7. 새 센서·하드웨어·런타임·외부 서비스·AI 기능은 핵심 코드 수정이 아니라 adapter/provider/capability 등록으로 확장할 수 있어야 한다.
8. 요구사항 단계에서는 구체 변수명을 임의 확정하지 않는다. 각 요구사항이 필요로 하는 정보의 의미와 생산자·소비자를 먼저 구현한 뒤 전체 인터페이스 설계 단계에서 공통 데이터 사전으로 변수명을 통일한다.
9. 업무 데이터, 시스템 관측 데이터, 미디어를 구분한다. 물리 제어 명령은 업무 데이터에 포함하되 전달 보장·순서·결과 회신·책임 추적이 필요한 제어 데이터로 별도 정책을 적용하며, 그 의미 계약은 AI-C-20을 따른다.
10. 현재 배포에서 말단↔엣지 업무·제어·하트비트는 MQTT, 엣지↔서버 업무와 서버→엣지 제어는 서버 Kafka, metric·log·trace는 OpenTelemetry 기반 관측 경로, 영상 픽셀은 별도 미디어 경로를 사용한다. 구역 엣지와 서버 사이의 모든 구간은 공개 인터넷을 경유하지 않고 보안 오버레이 네트워크(현재 Tailscale) 위에서만 연결한다. 그러나 상위 AI 코드가 이 구현 기술의 API에 직접 의존해서는 안 된다.
11. 엣지에는 Kafka 서버를 설치하지 않는다. 엣지는 MQTT Broker와 서버 Kafka를 연결하는 양방향 Bridge를 둔다. 서버에는 MQTT Broker를 두지 않는다. 오케스트레이션이 존재하는 실행영역(서버·구역 엣지·기타 연산 영역)은 동일한 실행 제어·배치·health·롤백 계약(AI-B-11)으로 관리하되, 모든 실행영역이 동일한 오케스트레이터(K3s 등)를 설치해야 한다는 전제는 두지 않는다 — 실제 오케스트레이터 선택은 실행영역별 provider 구성이다. 말단은 오케스트레이터 제어면을 필수 설치 대상으로 요구하지 않는다.
12. 말단은 경량 실행을 기본으로 한다. 브로커·수집기·중앙 저장소·오케스트레이터 제어면 같은 서버형 미들웨어를 기본 설치 대상으로 요구하지 않는다.
13. 카메라 보정(자동 캘리브레이션 포함)은 말단에서 무거운 연산으로 수행하지 않는다. 필요한 영상은 미디어 경로로 엣지에 전달하고, 엣지에서 추정·검증한 보정 프로파일을 적용 대상에 배포한다.
14. 일반 metric은 엣지에 상세값을 유지하고 서버에는 요약할 수 있지만 장치 생사·치명 오류처럼 집계하면 안 되는 상태는 개별 상태로 유지한다.
15. 장치 최종 가용성은 백엔드가 업무 전송 상태와 관측 상태를 통합해 판정한다. AI Capability Registry가 이를 중복 구현하지 않는다.
16. 물리 명령은 전달만으로 완료되지 않는다. 수락·거부는 별도 결과로, 실행 lifecycle(ACCEPTED/EXECUTING/CANCELING/SUCCEEDED/ABORTED/CANCELED)과 최종 결과는 AI-C-20의 의미 계약으로 표현하며, 처리 경로와 지연은 별도의 기술 trace(OTel)로 추적한다. 동일 command_id 재전달은 물리 동작을 다시 실행하지 않아야 한다.
17. Kafka는 장기 저장소가 아니다. 단기 버퍼·다중 소비자·단기 replay 용도로 사용하고, 장기 재현 데이터는 업무 저장소 또는 별도 아카이브 참조를 사용한다.
18. 폐쇄망 배포 프로파일에서는 실행에 필요한 이미지·모델·패키지·규칙을 내부 저장소에서 조달하고, 공개 인터넷에 대한 나가는 연결을 핵심 기능 동작의 전제로 두지 않는다. 외부 서비스 provider는 항상 선택 기능이며 사용할 수 없을 때 해당 기능만 비활성화한다. 폐쇄망이 모든 배포의 필수조건은 아니며, 해당 프로파일에서만 배치 전 검증 대상이다.
19. AI는 인지·추적·객체 상태 해석, 추가 정보 필요성 판단, capability 선택, 학습 후보 선정·검증에 필요한 품질 정보를 담당하되, 장치의 최종 가용성 판정, 전역 좌표·최종 디지털 트윈 상태의 authoritative 관리, 물리 명령 발급과 실제 센서·로봇·액추에이터 제어를 직접 소유하지 않는다(AI-C-19). 가시화는 AI 판단·학습 정보를 표현하고 사용자 입력을 전달할 뿐 AI 판단 로직을 중복 구현하지 않는다.

## 3. 구현 시 금지 사항

- 요구사항에 없는 센서, 서버, GPU/NPU, 외부 API가 항상 존재한다고 가정하지 않는다.
- 특정 모델이 없으면 시스템을 시작하지 못하게 만들지 않는다.
- 특정 protocol client를 인지·판단 코드 안에서 직접 생성하지 않는다.
- 특정 도메인 전용 클래스를 복제하여 별도 프레임워크를 만들지 않는다.
- 선택 기능 장애를 전체 process liveness 실패로 취급하지 않는다.
- 장치 최종 availability를 AI 내부에서 독자적으로 재판정하지 않는다.
- 영상 픽셀을 MQTT/Kafka/OTLP 업무·관측 메시지에 직접 싣지 않는다.
- 일반 metric 요약에 장치 급사·치명 오류를 섞어 개별 의미를 잃게 하지 않는다.
- Kafka를 장기 데이터베이스처럼 사용하지 않는다.
- 현재 요구사항에서 정하지 않은 변수명·스키마명을 편의상 임의 확정하지 않는다.
- AI가 물리 명령을 직접 발급하거나 실제 액추에이터 제어·전역 좌표·최종 가용성을 authoritative하게 소유하지 않는다(AI-C-19).
- ROS 2 Action, AIP-155, W3C WoT, KubeEdge 등에서 차용한 설계 개념을 이유로 해당 middleware·wire protocol·runtime을 core dependency로 설치하지 않는다.

## 4. 구현 결과물의 기본 구조

구현은 가능한 한 다음 경계를 유지한다.

```text
Domain / Application Logic
        │
        ├─ Perception / Decision / Risk / Safety
        │
Common Capability & Provider Interfaces
        │
        ├─ Input / Media Adapter
        ├─ AI Runtime Provider
        ├─ Transport Provider
        ├─ Serializer Provider
        ├─ Control / Orchestrator Provider
        ├─ Observability Provider
        ├─ Network Overlay / Security Provider
        └─ Model Deployment Provider
        │
Capability Registry + Resource/Execution/Validation Profile + Deployment Profile
        │
Concrete Hardware / Sensor / Runtime / Protocol / Infrastructure
```

구현 상세가 이 구조와 다르더라도 요구사항의 의존성 분리, 기능 격리, provider 교체 가능성, 자원 최소화 원칙을 만족해야 한다.

## 5. 전체 요구사항

아래에는 요구사항 정의서(Google Sheets `진나영` 탭, 2026-09-02 기준)의 53개 요구사항을
12개 필드 모두 포함하여 수록한다. 이 시트가 최신 기준이며 로컬 스냅샷보다 우선한다.
이전 버전(51개, 서브태스크 생성·검증을 다루는 AI-D 포함) 대비 주요 변경:

- **제거·이관**: AI-D-01~04(서브태스크 생성·검증·재생성 판단)는 가시화 파트로 이관되어
  이 문서에서 삭제. AI-R-03/04는 AI-R-02와 신규 AI-C-19로 흡수. AI-B-07(장애 복구·롤백)은
  AI-B-05로 흡수. AI-C-09/11/12/14/17은 각각 AI-C-04/05/06/18로 흡수(보안 오버레이는
  AI-C-04의 Security Provider 항목으로 일반화).
- **신규**: AI-N-03(링크 품질 기반 실행 전환), AI-S-06~08(이종 모델 결과의 점진적 객체
  레코드 구성, 업무 목적별 요구 근거 정의, 환경·사전정보 관리), AI-C-18(구성요소 Capability
  선언 규약), AI-C-19(AI 판단·권한·책임 경계), AI-C-20(물리 명령 의미 계약).
- **범위 확장**: AI-B-01은 "검증" 프로파일까지, AI-B-11은 서버·엣지 한정에서 "복수
  실행영역"으로 일반화.

### 안전·말단운용

#### AI-N-01 — 로컬 안전 판단

**ID**

AI-N-01

**대분류**

안전·말단운용

**소분류**

로컬 안전 판단

**요구사항 정의**

이동형 에이전트는 외부 네트워크나 엣지 기능이 사용할 수 없는 경우에도 로컬 카메라와 현재 실행 가능한 영상 처리 기능만으로 접근·충돌 위험을 판단하고 최소 안전 상태를 제어계에 제공해야 한다. 객체 분류, 추적, 거리 추정 등 개별 기능은 안전 판단을 보조하는 선택 기능으로 취급하고 특정 모델이나 런타임을 필수조건으로 두어서는 안 된다. 사용 가능한 기능이 줄어들면 남은 영상 정보로 판단 수준을 단계적으로 낮추고, 영상 입력 자체가 사라지면 사전에 정의된 보수적 안전 상태로 전이해야 한다.

**전달 경로**

온보드 카메라 → 온디바이스

**제안 주기**

이동·작업 중 연속. 초기 5Hz 이상을 기준으로 시작하고 최종 최소 주기는 안전 응답시간 시험으로 확정

**주기 선정 이유**

최소 처리주기는 이동속도·제동거리·센서 취득·추론·제어 지연을 합산해 정해야 한다.

**1차 선정 기술**

로컬 영상 안전 판단 인터페이스 + 선택형 인지·추적·거리 기능 + 안전 fallback

**선정 이유**

로컬 안전은 특정 객체탐지 모델의 성능이 아니라 네트워크 단절과 선택 기능 부재에도 유지되는 실행 구조가 핵심이다. 현재 장비에 맞는 모델·런타임은 실행 프로파일에서 선택하고 요구사항에는 고정하지 않는다.

**연관분야**

하드웨어

**연관점**

HW-R-04, HW-C-06, BE-C-03, AI-C-03

**담당**

진나영

---

#### AI-N-02 — 외부 구성 적용

**ID**

AI-N-02

**대분류**

안전·말단운용

**소분류**

외부 구성 적용

**요구사항 정의**

온디바이스는 구역 엣지에서 제공하는 실행 설정, 환경정보, 보정 프로파일 등 외부 구성을 버전과 적용 대상으로 검증한 뒤 적용해야 한다. 최초 적용은 필요한 전체 구성을 받고 이후에는 변경된 항목만 갱신할 수 있어야 하며, 갱신 또는 검증 실패 시 마지막 정상 구성을 유지해야 한다. 환경 사전정보의 의미·유효성·관측과의 충돌 처리는 AI-S-08의 공통 규약을 따르고, 외부 구성이 없어도 로컬 안전 기능은 계속 동작해야 한다.

**전달 경로**

구역 엣지 환경·구성 제공 → 말단 온디바이스 적용

**제안 주기**

구역 진입·임무·환경·보정·실행 구성 변경 시

**주기 선정 이유**

실행 구성은 변경될 때만 갱신하고 검증 실패 시 기존 정상 상태를 보존해야 잘못된 설정 전파와 불필요한 전체 재배포를 줄일 수 있다.

**1차 선정 기술**

버전 기반 구성 적용 인터페이스 + last-known-good + 전송 provider

**선정 이유**

환경 사전정보 자체의 출처·유효성·충돌 규칙은 AI-S-08에서 관리하고 이 요구사항은 말단의 구성 적용과 복구 책임에 한정한다.

**연관분야**

하드웨어, 백엔드

**연관점**

BE-A-05, HW-R-08, AI-E-02, AI-N-01, AI-S-08

**담당**

진나영

---

#### AI-N-03 — 링크 품질 기반 실행 전환

**ID**

AI-N-03

**대분류**

안전·말단운용

**소분류**

링크 품질 기반 실행 전환

**요구사항 정의**

이동형 말단은 현재 연결 품질이 원격 기능의 실행 가능성에 미치는 영향을 판단하고, 품질 저하 시 원격 선택 기능을 축소하거나 다른 실행 위치 사용을 요청할 수 있어야 한다. 링크 품질 지표의 종류와 측정 방식은 고정하지 않고 공통 인터페이스로 조회하며 특정 무선 규격이나 측정값이 항상 존재한다고 가정해서는 안 된다. 순간 변동으로 불필요한 실행 전환이 반복되지 않도록 이력과 상태 변화를 활용할 수 있어야 한다. 실제 무선 연결 전환과 피어 선택은 네트워크·실행 provider의 책임으로 두고, 연결 중단 동안에도 로컬 안전 기능과 이미 실행 중인 로컬 기능은 계속 동작해야 한다.

**전달 경로**

말단 링크 상태 관측 → AI 원격 기능 사용 가능성 판단 → 네트워크·실행 provider 전환 / 로컬 축소

**제안 주기**

링크 품질은 연속 관측하되 실행 전환 판단은 품질 저하·상태 변화 시 이벤트 기반

**주기 선정 이유**

순간 신호 변동마다 기능을 전환하면 반복 전환이 발생하고, 완전 단절 후 대응하면 이미 기능이 중단될 수 있으므로 상태 변화와 이력을 구분해 판단해야 한다.

**1차 선정 기술**

공통 링크 품질 인터페이스 + 원격 기능 축소 판단 + 네트워크/실행 provider 연계

**선정 이유**

AI는 원격 기능 사용 여부와 축소 필요성을 판단하지만 실제 무선 핸드오버를 직접 구현하지 않는다. 구체적인 전환 알고리즘은 배포환경에서 비교·검증해 선택한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-N-01, AI-B-06, AI-C-05, AI-O-04, AI-C-19

**담당**

진나영

---

### 감시·인지

#### AI-E-01 — 인지

**ID**

AI-E-01

**대분류**

감시·인지

**소분류**

인지

**요구사항 정의**

인지 기능은 카메라 종류, 고정·이동 여부, 실행 하드웨어, 모델 구조와 런타임에 따라 별도 업무 로직으로 나누지 않고 공통 인지 인터페이스를 사용해야 한다. 입력 영상 특성, 보정 수준, 사용 가능한 연산 자원과 필요한 출력 수준을 기준으로 현재 실행 가능한 인지 제공자를 선택해야 한다. 보정이나 추가 분석 기능이 없더라도 가능한 범위의 영상 좌표 기반 결과를 계속 제공하고, 새로운 카메라·모델·하드웨어가 추가되어도 상위 인지·판단 코드는 변경하지 않아야 한다.

**전달 경로**

로컬 카메라 또는 별도 미디어 경로 → 실행 가능한 인지 노드 → 후속 판단·백엔드

**제안 주기**

영상 도착 시 처리하되 실행 프로파일이 정한 최소 필요 주기로 샘플링

**주기 선정 이유**

입력 FPS와 요구 지연이 소스마다 달라 고정 주기보다 현재 실행 프로파일과 SLO에 맞추는 편이 불필요한 연산을 줄인다.

**1차 선정 기술**

공통 인지 인터페이스 + 교체 가능한 인지 provider + 실행 프로파일 선택

**선정 이유**

입력·카메라·하드웨어·실행환경 차이를 공통 인터페이스와 실행 프로파일에서 흡수해 동일 인지 로직을 재사용하는 것이 핵심이다. 현재 배포의 영상 픽셀은 별도 미디어 경로를 사용한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

HW-R-04, HW-S-06, AI-E-02, AI-S-01, VZ-I-07

**담당**

진나영

---

#### AI-E-02 — 카메라 보정 lifecycle

**ID**

AI-E-02

**대분류**

감시·인지

**소분류**

카메라 보정 lifecycle

**요구사항 정의**

새로운 카메라가 등록되거나 해상도·crop·zoom·장착 상태 등 입력 조건이 바뀌거나 기존 보정의 유효성이 낮아진 경우 필요한 보정 상태를 산출하고 품질을 검증할 수 있어야 한다. 보정 방식은 사전 보정, 보정판 기반, 영상 시퀀스 self-calibration, 학습 기반 추정 등 특정 방법을 프레임워크의 필수 방식으로 고정하지 않아야 한다. 영상 샘플이 필요한 방법은 별도 미디어 경로를 사용하고, 검증을 통과한 결과만 적용 조건·품질·생성 시점·버전을 포함한 보정 프로파일로 관리·배포해야 한다. 새 보정이 실패하면 마지막 정상 프로파일 또는 영상 좌표 기반 인지를 유지해야 한다.

**전달 경로**

카메라/영상 관측 → 선택된 보정 provider → 검증된 보정 프로파일 → 엣지 인지·말단 적용

**제안 주기**

새 카메라 등록, 입력 조건 변경, 장착 변화 의심, 보정 품질 저하 시

**주기 선정 이유**

보정은 카메라 조건이 바뀔 때만 수행하고 검증된 결과만 적용해야 저사양 말단의 불필요한 연산과 잘못된 보정 전파를 줄일 수 있다.

**1차 선정 기술**

교체 가능한 Calibration Provider + 품질 검증 + 버전 보정 프로파일 + 현재 엣지 실행/설정 전달

**선정 이유**

AnyCalib, DroidCalib, GeoCalib 및 전통적 보정 방식은 비교할 구현 후보이며 요구사항에서 특정 알고리즘을 정답으로 고정하지 않는다. 현재 배포에서 원본 영상은 별도 미디어 경로, 작은 보정 프로파일은 업무·설정 경로를 사용한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-N-02, AI-E-01, AI-C-02, AI-C-08

**담당**

진나영

---

#### AI-E-04 — 선택형 보조 기능 실행

**ID**

AI-E-04

**대분류**

감시·인지

**소분류**

선택형 보조 기능 실행

**요구사항 정의**

기본 기능만으로 판단에 필요한 정보가 부족한 경우 현재 등록되어 실제 실행 가능한 로컬·엣지·서버·외부 서비스 capability 중 필요한 보조 기능만 선택적으로 실행할 수 있어야 한다. 특정 모델 종류나 실행 위치를 고비용·저비용으로 고정하지 않고 현재 실행 프로파일에서 측정 가능한 품질·지연·자원·네트워크·외부 호출 비용을 사용할 수 있어야 한다. 보조 기능의 종류·실행 순서·선택 방식은 특정 도메인·센서·모델에 고정하지 않고 교체 가능해야 하며, 제공자가 없거나 실패해도 기본 기능은 유지하고 필요한 근거가 확보되면 추가 실행을 중단해야 한다.

**전달 경로**

기본 인지·객체 상태 → 근거 선택 → 로컬·엣지·서버·외부 provider → 후속 판단

**제안 주기**

기본 기능만으로 필요한 정보가 부족한 경우

**주기 선정 이유**

추가 분석의 가치와 비용은 실행 위치·하드웨어·네트워크에 따라 달라지므로 상시 실행이나 모델 이름 기반 고정 분류를 피해야 한다.

**1차 선정 기술**

가용 capability 탐색 + 실행 프로파일 + 교체 가능한 선택 정책 + 선택형 provider 실행

**선정 이유**

VLM, open-vocabulary 인지, 고정밀 비전, 외부 AI 서비스 등은 provider 예시이며 실제 채택은 기존 연구와 동일 조건 실험으로 판단 품질·지연·자원·비용을 비교해 결정한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-E-01, AI-S-03, AI-S-05, AI-S-07, AI-B-01, AI-B-06

**담당**

진나영

---

#### AI-S-01 — 객체 추적

**ID**

AI-S-01

**대분류**

감시·인지

**소분류**

객체 추적

**요구사항 정의**

연속된 인지 결과가 제공되면 동일 객체의 식별 상태와 위치 변화, 이동 방향, 최근 관측 이력을 유지할 수 있어야 한다. 추적 기능은 특정 알고리즘에 의존하지 않고 인지 결과와 교체 가능한 추적 기능 사이의 공통 인터페이스를 사용해야 한다. 추적 기능이 없거나 실패하면 프레임별 인지 결과만 유지해야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

인지 결과 갱신 시

**주기 선정 이유**

기본 추적은 가볍게 유지하고 필요한 상황에만 추가 특징 추출을 사용해 처리비용을 제한한다.

**1차 선정 기술**

교체 가능한 객체 추적 provider + 공통 추적 인터페이스

**선정 이유**

추적 알고리즘 자체를 프레임워크에 고정하지 않고 입력·출력 계약과 요구 지연을 만족하는 provider를 실행 프로파일에서 선택한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-E-01, AI-E-04, AI-S-02, VZ-I-09

**담당**

진나영

---

#### AI-S-02 — 다중 관측 객체 연계

**ID**

AI-S-02

**대분류**

감시·인지

**소분류**

다중 관측 객체 연계

**요구사항 정의**

서로 다른 관측 소스에서 동일 객체가 보이는 경우 시간·공간·외형 등 사용 가능한 근거를 이용해 동일 객체 후보를 연계할 수 있어야 한다. 복수 카메라, ReID, 전역 좌표, 정밀 시간 동기화는 모두 선택 기능이며 하나라도 없다고 전체 추적을 중단해서는 안 된다. 필요한 근거가 부족하면 관측 소스별 추적을 독립적으로 유지해야 한다.

**전달 경로**

카메라별 track → 선택적 연계 모듈 → 서버

**제안 주기**

복수 카메라·연계 capability가 있을 때 track 갱신/경계 이동 시

**주기 선정 이유**

다중 카메라가 있는 환경에서는 동일 객체 중복을 줄이되 단일 카메라 환경에서도 전체 인지 시스템이 그대로 동작하도록 한다.

**1차 선정 기술**

시간·공간·외형 근거를 이용한 선택적 다중 관측 연계

**선정 이유**

ReID와 복수 카메라는 품질 향상용 선택 기능으로 두고 단일 카메라 환경을 정상 구성으로 허용한다. 참고 근거는 FastReID (https://github.com/JDAI-CV/fast-reid); Assigning Pods to Nodes (https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)이다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, DT-01, VZ-I-09

**담당**

진나영

---

#### AI-S-03 — 불확실성·근거 충분도 평가

**ID**

AI-S-03

**대분류**

감시·인지

**소분류**

불확실성·근거 충분도 평가

**요구사항 정의**

인지·추적·판단 결과는 모델이 제공하는 신뢰도와 현재 업무·태스크가 요구하는 근거가 실제로 확보되었는지를 구분해 평가할 수 있어야 한다. 근거 충분도는 단일 confidence 값만으로 확정하지 않고 현재 확보된 관측의 출처, 최신성, 일관성, 누락 상태 등 사용 가능한 정보를 고려할 수 있어야 하며, 구체적인 판정 규칙이나 알고리즘을 프레임워크의 필수 방식으로 고정해서는 안 된다. 충분한 근거가 없는 결과는 확정 상태로 승격하지 않고 추가 정보 요청이나 미확인 상태로 연결할 수 있어야 한다.

**전달 경로**

인지·추적·객체 레코드 → 근거 충분도 평가 → 후속 판단·추가 정보 선택

**제안 주기**

인지·추적·객체 상태 생성 또는 필요한 근거 조건이 변경될 때

**주기 선정 이유**

필요한 근거 수준은 도메인·시나리오·하드웨어가 아니라 최종 업무 목적과 사용 가능한 정보에 따라 달라질 수 있으므로 하나의 임계값이나 규칙으로 고정하면 일반화가 어렵다.

**1차 선정 기술**

공통 불확실성·근거 충분도 인터페이스 + 교체 가능한 평가 provider

**선정 이유**

근거 충분도 판정 방법과 임계값은 구현 후보로 관리하고 검증 데이터와 동일 조건 비교 실험을 통해 선정·갱신해야 한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, AI-S-05, AI-S-06, AI-S-07, AI-R-02, VZ-I-09

**담당**

진나영

---

#### AI-S-04 — 미확인 객체 후보 관리

**ID**

AI-S-04

**대분류**

감시·인지

**소분류**

미확인 객체 후보 관리

**요구사항 정의**

기존 인지 결과만으로 분류를 확정하기 어려운 객체는 기존 클래스에 강제 매핑하지 않고 미확인 상태의 지속 객체 또는 track으로 유지해야 한다. 동일 물리 객체로 판단되는 동안 관측 이력과 객체 식별 상태를 유지하고, 사용 가능한 다른 관측 소스, open-vocabulary 인지, VLM, 환경·인프라 정보 등에서 추가 의미 근거를 얻을 수 있어야 한다. 추가 의미 근거는 객체명 후보뿐 아니라 속성, 행동, 공간 관계, 기능 등 객체의 의미 판단을 보조하는 부분 정보일 수 있으며 서로 다른 분석 결과를 하나의 확정 label로 즉시 합치지 않고 개별 근거와 가설 상태로 누적할 수 있어야 한다. 이러한 결과는 확정 객체명이 아니라 가설 또는 후보 근거로 구분해야 하며 사용자 확인이나 충분히 검증된 근거가 확보된 경우에만 분류 상태를 갱신해야 한다. 장기간 해결되지 않거나 반복되는 미확인 객체는 학습 후보로 연결할 수 있어야 한다.

**전달 경로**

미확인 객체 track/record → 선택형 의미·관측 분석 → 사용자 확인·학습 후보

**제안 주기**

미확인 조건 발생 시 즉시, 동일 객체가 유지되는 동안 관측·근거 갱신

**주기 선정 이유**

객체 이름을 모르더라도 물리 객체의 지속성을 유지해야 추가 관측과 사용자 확인을 같은 대상에 누적할 수 있고 잘못된 기존 클래스 강제 배정을 줄일 수 있다.

**1차 선정 기술**

지속 미확인 객체 상태 + 선택형 의미 근거 provider + 사용자 확인 연계

**선정 이유**

VLM·open-vocabulary 모델·환경정보는 미확인 객체의 의미를 추론하기 위한 선택 근거이며 자체 결과를 Ground Truth로 간주하지 않는다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, AI-S-03, AI-S-05, AI-S-06, AI-S-07, AI-L-01, AI-L-03

**담당**

진나영

---

#### AI-S-05 — 추가 정보 판단·선택·요청

**ID**

AI-S-05

**대분류**

감시·인지

**소분류**

추가 정보 판단·선택·요청

**요구사항 정의**

미확인, 근거 부족·충돌 또는 위험 분석 등에서 현재 업무가 요구하는 정보와 객체·환경 레코드에 확보된 근거를 비교해 부족한 정보의 종류를 식별하고, 현재 사용 가능한 관측·분석 capability 중 적합한 후보를 선택·요청할 수 있어야 한다. 선택 방식은 특정 도메인·센서·모델·점수식에 고정하지 않고 기능 가용성, 자원·지연 조건과 과거 실행 결과 등 사용 가능한 상태를 입력으로 활용하는 교체 가능한 방식이어야 한다. 카메라 방향 전환, 이동형 센서 투입, 재관측처럼 물리 행동이 필요한 경우 AI는 대상 객체, 필요한 정보, 관측 조건과 요청 사유만 제공하고 실제 장치 선택·명령·실행은 백엔드·하드웨어 제어 경로를 통해 수행해야 한다. 필요한 근거가 확보되면 추가 실행을 중단하고, 이용 가능한 후보가 없으면 확인 가능한 범위까지만 판단하면서 부족한 근거를 남겨야 한다.

**전달 경로**

업무 요구·객체/환경 근거 상태 → 부족 정보 식별 → 가용 capability 선택·요청 → 근거 재평가

**제안 주기**

근거 부족·충돌·미확인·위험 상태가 발생하거나 새 근거가 수신될 때

**주기 선정 이유**

추가 정보 획득은 인지와 위험 분석에서 공통으로 재사용하고, AI가 정보 필요성을 판단하되 물리 장치의 authoritative 제어는 담당 파트에 남겨야 한다.

**1차 선정 기술**

교체 가능한 근거 획득 정책 + capability 필터 + 관측 요청 계약 + 실행 결과·비용 기록

**선정 이유**

규칙 기반, 정보이득·비용 기반, 학습형 정책 등은 동등한 구현·연구 후보이며 외부 기존 방법과 동일 조건에서 비교해 채택한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-04, AI-S-03/04/07/08, AI-R-01/02, AI-B-01, AI-C-18, AI-C-19

**담당**

진나영

---

#### AI-S-06 — 이종 모델 결과의 점진적 객체 레코드 구성

**ID**

AI-S-06

**대분류**

감시·인지

**소분류**

이종 모델 결과의 점진적 객체 레코드 구성

**요구사항 정의**

수행 태스크와 출력 형식이 서로 다른 인지 기능들의 결과가 서로 다른 시점에 도착하는 경우 모든 결과를 기다리거나 도착 순서대로 덮어쓰지 않고 동일 물리 객체의 지속 레코드에 점진적으로 통합할 수 있어야 한다. 새 근거가 기존 객체와 연계되지 않더라도 객체 존재에 대한 근거가 충분한 경우에는 새로운 임시 객체 레코드를 생성할 수 있어야 한다. 지속 객체 레코드는 특정 프레임의 탐지 결과와 구분하여 관리하고, 객체 생성 이후 여러 관측에서 생성된 근거와 의미 가설, 최신 상태와 객체의 생성·활성·관측 소실·종료 등 lifecycle을 동일 객체에 누적할 수 있어야 한다. 각 근거는 생산자, 원본 관측, 생성·완료 시점과 필요한 출처 정보를 추적할 수 있어야 하며 서로 중복되거나 의존적인 근거를 구분할 수 있는 여지를 유지해야 한다. 구체적인 객체 연계·상태 해석 알고리즘은 교체 가능해야 하며 제공자 부재를 반대 근거로 해석하거나 관측 변화가 없는데 레코드를 임의 변경해서는 안 된다. AI가 산출하는 위치는 로컬 좌표에 한정하고 전역 좌표 변환과 최종 디지털 트윈 상태 통합은 백엔드 책임으로 둔다.

**전달 경로**

인지 제공자들의 비동기 결과 → 객체 연계·신규 생성 → 점진적 객체 레코드 → 업무 데이터 경로 → 백엔드 디지털 트윈

**제안 주기**

제공자 결과가 도착하거나 객체 상태가 실제로 변할 때

**주기 선정 이유**

비동기 결과를 지속 객체 단위로 관리하면 빠른 초기 상태 제공과 후속 근거 보강을 함께 지원할 수 있고, 신규 객체도 기존 객체와 동일한 lifecycle 안에서 관리할 수 있다.

**1차 선정 기술**

Observation/Evidence/Object 관계를 유지하는 공통 근거 표현 + 지속 객체 연계·신규 생성 + 교체 가능한 상태 해석 provider + 상태 변화 노출

**선정 이유**

AsiaSim에서 사용한 연계·해석 방식은 현재 구현 후보일 뿐 프레임워크 요구사항에서 유일한 알고리즘으로 고정하지 않고 다른 방법과 비교 실험이 가능해야 한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-E-01, AI-S-01, AI-S-03/04/05/07/08, AI-C-01/02/03/05, AI-L-08, DT-01, DT-03, BE-C-04

**담당**

진나영

---

#### AI-S-07 — 업무 목적별 요구 근거 정의

**ID**

AI-S-07

**대분류**

감시·인지

**소분류**

업무 목적별 요구 근거 정의

**요구사항 정의**

객체·환경 판단을 소비하는 업무나 태스크는 판단에 반드시 필요한 정보와 있으면 품질을 높이는 선택 정보를 의미 수준에서 정의할 수 있어야 한다. 동일 객체라도 충돌 회피, 자산 식별, 위험 분석 등 사용 목적에 따라 필요한 의미·공간·시간·행동 정보가 다를 수 있으므로 모든 객체에 동일한 완성 상태를 요구해서는 안 된다. 요구 정보는 특정 센서·모델·도메인 구현에 종속되지 않아야 하며 현재 객체 레코드에 확보된 정보와 비교해 추가 정보 필요 여부를 평가할 수 있어야 한다.

**전달 경로**

업무·태스크 요구 정보 → 객체·환경 레코드 근거 상태 비교 → AI-S-03/05

**제안 주기**

업무·태스크 정의·변경 시, 객체 상태 평가 시

**주기 선정 이유**

필요한 정보 수준을 목적과 분리하지 않으면 불필요한 고비용 분석을 반복하거나 반대로 안전·업무에 필요한 정보를 누락할 수 있다.

**1차 선정 기술**

목적 수준 요구 근거 계약 + 현재 근거 상태 비교 인터페이스

**선정 이유**

구체적인 근거 표현과 판정 알고리즘은 별도 구현·연구 대상으로 두고 요구사항에서는 업무 목적과 현재 확보 정보의 분리를 보장한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-03, AI-S-05, AI-S-06, AI-R-02

**담당**

진나영

---

#### AI-S-08 — 환경·사전정보 관리

**ID**

AI-S-08

**대분류**

감시·인지

**소분류**

환경·사전정보 관리

**요구사항 정의**

AI가 사용하는 환경 사전정보와 장기 관측 정보는 실제 센서 관측과 구분하여 관리하고 출처, 대상 구역·객체, 생성·갱신 시점, 버전, 유효 범위와 현재 사용 가능 여부를 확인할 수 있어야 한다. 공간·시설·과거 관측·임무·환경 이벤트 등 서로 다른 종류의 사전정보를 특정 도메인에 고정하지 않고 선택 정보로 사용할 수 있어야 하며, 실제 관측과 충돌하는 사전정보만으로 객체·위험 상태를 확정해서는 안 된다. 반복적인 불일치나 오래된 사전정보는 재평가·갱신 대상으로 식별할 수 있어야 한다.

**전달 경로**

백엔드/엣지 환경·이력 정보 → 환경 사전정보 인터페이스 → AI 판단·추가 정보 선택

**제안 주기**

구역·임무·환경 상태·사전정보 버전 변경 시, 실제 관측과 반복 충돌 시

**주기 선정 이유**

사전정보는 탐색과 판단을 효율화할 수 있지만 실제 관측과 같은 확정 근거로 사용하면 오래되거나 잘못된 정보가 판단을 오염시킬 수 있다.

**1차 선정 기술**

환경 사전정보 계약 + 출처·유효범위·버전 관리 + 관측과 분리

**선정 이유**

어떤 사전정보가 실제로 유용한지는 도메인별 비교 실험으로 검증하되 프레임워크는 다양한 정보 종류를 교체·추가할 수 있게 한다.

**연관분야**

백엔드

**연관점**

AI-N-02, AI-S-05, AI-S-07, AI-L-01

**담당**

진나영

---

### 판단·위험

#### AI-R-01 — 위험 분석 상태 관리

**ID**

AI-R-01

**대분류**

판단·위험

**소분류**

위험 분석 상태 관리

**요구사항 정의**

위험 관련 관측이나 이벤트가 존재하는 배치에서만 위험 분석 상태를 활성화하고 평시·관찰·경보·복구 등 상태 전이를 관리해야 한다. 특정 센서 종류나 하천 도메인을 프레임워크의 필수 입력으로 고정하지 않고 현재 등록된 관측과 규칙만 사용해야 한다. 위험 입력이 없으면 위험 분석 기능만 비활성화하고 다른 기능은 계속 동작해야 한다.

**전달 경로**

말단 업무 관측 → 구역 엣지 위험 분석

**제안 주기**

환경 이벤트 발생 시 상태 전이

**주기 선정 이유**

센서의 1차 이벤트 검출과 AI 분석 수준 관리를 분리해 기능 중복을 줄인다.

**1차 선정 기술**

가용 sensor/event 기반 FSM

**선정 이유**

위험 분석에 쓰는 센서 값·상태는 시스템 자기관측 데이터가 아니라 업무 데이터이므로 현재 말단↔엣지 MQTT 업무 경로에서 소비한다. 특정 센서 종류는 프레임워크 필수조건으로 두지 않는다. 참고: 프로젝트 문서 260827_데이터_전송_및_아키텍처v8.docx §4, §5-1~§5-3.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-S-02, HW-S-03, BE-C-01, VZ-I-08

**담당**

진나영

---

#### AI-R-02 — 위험도·근거·권고 산정

**ID**

AI-R-02

**대분류**

판단·위험

**소분류**

위험도·근거·권고 산정

**요구사항 정의**

위험 관련 관측이나 이벤트가 존재하는 배치에서 현재 사용 가능한 센서·영상·외부 이벤트를 이용해 위험 수준과 판단 근거의 충분도를 별도로 산정하고, 결과에는 위험 상태·위험 정도·사용 근거·근거 충분도·적용한 모델 또는 규칙 버전과 권고 조치를 포함할 수 있어야 한다. 존재하지 않는 입력을 임의 생성하거나 보간해 확정 판단에 사용해서는 안 되며 구체적인 위험 산정 알고리즘은 센서·도메인에 따라 교체 가능해야 한다. 실제 액추에이터 명령 생성과 물리 동작 확인은 백엔드·하드웨어가 담당하고, 근거가 부족하면 AI-S-05의 공통 추가 정보 요청을 사용해야 한다.

**전달 경로**

업무 관측·이벤트 → 위험 분석 → 위험·근거·권고 결과 → 백엔드

**제안 주기**

위험 분석 활성 상태에서 관측 갱신 시 수행하고 주요 상태 변화 시 즉시

**주기 선정 이유**

실제 위험 수준과 정보 부족을 구분하면서 판단 결과와 제어 책임을 분리해야 잘못된 경보·직접 제어와 중복 로직을 줄일 수 있다.

**1차 선정 기술**

교체 가능한 위험 분석 provider + 근거 충분도 + 위험 Domain Object

**선정 이유**

위험 분석 결과는 업무 데이터로 전달하고 실제 물리 명령은 백엔드가 별도 생성한다. 위험 산정 방식은 외부 기존 연구와 프로젝트 후보를 동일 조건으로 비교해 선택한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-R-01, AI-S-03, AI-S-05, BE-A-04, BE-X-01, VZ-I-08

**담당**

진나영

---

### 실행·자원

#### AI-B-01 — 실행 호환성·자원·검증 프로파일 관리

**ID**

AI-B-01

**대분류**

실행·자원

**소분류**

실행 호환성·자원·검증 프로파일 관리

**요구사항 정의**

동일 capability를 제공하는 서로 다른 모델·알고리즘, 런타임, 하드웨어, 실행 위치, 입력 조건, 전처리·후처리와 필요한 보정의 조합을 독립적인 실행 구성으로 식별하고, 실행 가능 조건과 검증된 적용 범위를 공통 실행 프로파일로 관리할 수 있어야 한다. 프로파일에는 현재 측정 가능한 처리지연, 처리량, 메모리·연산 자원, 품질, 실패 특성, 네트워크·외부 호출 비용 등을 기록하고 미측정 항목은 구분해야 한다. 다른 구성에서 측정한 성능·비용을 자동으로 동일하다고 가정해서는 안 되며 새 구성은 재측정·재검증해야 한다. 사람에게 확인된 의미 정보, 검증 데이터와 업무 요구처럼 구성과 독립적인 지식은 재사용할 수 있어야 한다.

**전달 경로**

실행 구성 → 호환성·성능 검증 → 적용 범위가 명시된 실행 프로파일 → 기능 선택·배치

**제안 주기**

모델·알고리즘·런타임·하드웨어·입력·전후처리·보정 구성 변경 또는 재측정 필요 시

**주기 선정 이유**

같은 기능도 실제 실행 조합에 따라 품질과 비용이 달라지므로 모델 이름이 아니라 검증된 조합별 측정값과 적용 범위를 사용해야 한다.

**1차 선정 기술**

실행 구성 식별 + Provider 실행 프로파일 + 호환성·품질·비용 측정 + 검증 Scope

**선정 이유**

고비용·저비용과 provider 우열은 기술 이름이 아니라 동일 조건 실험에서 얻은 실행 프로파일로 판단한다.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-R-10, AI-B-06, AI-B-09, AI-C-18, AI-L-05/07

**담당**

진나영

---

#### AI-B-02 — 실행환경 패키징·재현성 관리

**ID**

AI-B-02

**대분류**

실행·자원

**소분류**

실행환경 패키징·재현성 관리

**요구사항 정의**

각 기능은 동일한 시작·설정·상태 확인 규약으로 독립 실행할 수 있어야 하며, 운영 배포에서는 로봇 SDK·모델·런타임 의존성을 다른 기능과 격리한 재현 가능한 실행 단위로 패키징할 수 있어야 한다. 현재 배포는 OCI 호환 컨테이너 이미지를 사용하되 상위 기능이 Docker·containerd·K3s 등 특정 실행 기술의 API를 직접 요구해서는 안 된다. 실행 이미지와 외부 구성은 버전과 식별 가능한 무결성 정보를 유지하고 자격증명·비밀정보를 이미지에 포함해서는 안 된다.

**전달 경로**

개발환경 → 패키징 저장소 → 실행 노드

**제안 주기**

기능·의존성·모델 버전 변경 시 이미지 생성

**주기 선정 이유**

실행 환경과 의존성을 컨테이너에 고정하면 노드별 환경 차이로 인한 오류를 줄이고 동일 기능을 여러 장치에 반복 배포할 수 있다.

**1차 선정 기술**

독립 실행 계약 + OCI 호환 이미지 패키징 + 이미지·구성 버전/무결성 식별

**선정 이유**

OCI Image Specification은 상호운용 가능한 컨테이너 이미지 형식과 content-addressable descriptor를 제공한다. Docker는 현재 이미지 생성 도구, K3s는 배포·관리 provider로만 사용하고 기능 의미 계약의 필수 의존성으로 두지 않는다. 참고: https://specs.opencontainers.org/image-spec/ ; https://docs.k3s.io

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-03, AI-C-04, 실행 이미지 버전, 설정 버전

**담당**

진나영

---

#### AI-B-03 — 실행 제어

**ID**

AI-B-03

**대분류**

실행·자원

**소분류**

실행 제어

**요구사항 정의**

기능의 시작·중지·재시작·설정 변경과 실행 상태 확인은 공통 실행 제어 인터페이스로 요청해야 한다. 이러한 실행환경 제어와 로봇·센서·액추에이터의 물리 행동 명령은 서로 다른 계약으로 구분하고, 물리 명령은 AI-C-20의 수락·실행·결과 규약을 따라야 한다. 업무상 command_id 기반 책임 추적과 OTel trace 기반 기술 처리 경로·지연 추적은 서로 다른 목적으로 관리해야 하며, AI 기능은 현재 전송·오케스트레이션 기술의 API를 직접 호출하지 않고 provider 경계를 통해 동작해야 한다.

**전달 경로**

백엔드 → 명령 경로 → 구역 엣지 실행관리 → 필요 시 말단 → 실행 결과 회신

**제안 주기**

제어 명령 발생 시 이벤트 기반

**주기 선정 이유**

물리 시스템 제어는 명령 전달 여부뿐 아니라 실제 실행 성공·거부·실패 원인을 확인할 수 있어야 하며, 책임 추적과 기술 성능 추적은 목적이 달라 분리해야 한다.

**1차 선정 기술**

공통 실행 제어 인터페이스 + AI-C-20 물리 명령 계약 + 현재 Kafka→Bridge→MQTT 경로 + OTel trace

**선정 이유**

현재 명령 경로는 서버→엣지 Kafka, 엣지 Bridge, 엣지→말단 MQTT로 유지한다. 이 경로의 전달 성공은 물리 명령의 수락이나 완료와 동일하게 해석하지 않고, 물리 실행 상태와 결과는 AI-C-20의 의미 계약으로 구분한다. 참고: 변경 데이터 전송 아키텍처 5-6~5-7, 6-6.

**연관분야**

백엔드

**연관점**

BE-X-01, BE-X-06, AI-B-05, AI-C-20

**담당**

진나영

---

#### AI-B-04 — 실행 노드 자원 등록·배치

**ID**

AI-B-04

**대분류**

실행·자원

**소분류**

실행 노드 자원 등록·배치

**요구사항 정의**

각 실행 노드는 현재 제공 가능한 연산 자원, 아키텍처, 가속기, 센서·장치 연결, 런타임과 네트워크 특성을 공통 자원 규약으로 등록할 수 있어야 한다. 기능 배치 시 필수 자원과 선호 자원을 구분하고 필요한 장치와 권한만 요청해야 한다. 선호 자원이 없으면 호환 가능한 일반 자원으로 대체하고 필수 자원이 없으면 해당 기능만 미배치 상태로 유지해야 한다. 자원 발견·노출·할당 방식은 K3s, NFD, Device Plugin 등 특정 구현의 API에 상위 기능이 직접 의존하지 않도록 provider 경계로 분리해야 한다.

**전달 경로**

구역 엣지 실행관리 → 등록된 실행 대상

**제안 주기**

노드 등록·구역 변경·하드웨어 변경 시

**주기 선정 이유**

논리적인 노드 특성과 실제 자원을 기준으로 배치해야 서로 다른 장치에서도 동일 배포 규칙을 사용할 수 있다.

**1차 선정 기술**

공통 Resource Contract + required/preferred 조건 + 최소 장치 접근 + K3s 자원 provider

**선정 이유**

현재 K3s 배포에서는 Kubernetes 표준 자원 요청·배치 기능을 사용하고, 자동 node feature 발견에는 Node Feature Discovery, 특수 장치 자원 노출에는 Device Plugin을 사용할 수 있다. 이는 K3s 배포 provider 구현이며 프레임워크 공통 자원 계약의 필수 의존성으로 두지 않는다. 참고: https://kubernetes-sigs.github.io/node-feature-discovery/ ; https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/device-plugins/

**연관분야**

하드웨어, 백엔드

**연관점**

BE-C-01, BE-C-02, BE-T-04, VZ-I-01, AI-C-04, AI-C-18

**담당**

진나영

---

#### AI-B-05 — 실행 기능 생명주기·장애 복구

**ID**

AI-B-05

**대분류**

실행·자원

**소분류**

실행 기능 생명주기·장애 복구

**요구사항 정의**

배포 관리 기능이 있는 환경에서는 각 기능을 독립 실행 단위로 배포·기동·중지·업데이트·상태 확인·복구할 수 있어야 한다. 프로세스의 시작·liveness·readiness와 실제 장치의 운용 상태·안전 상태를 구분해야 하며 하나를 다른 상태의 근거로 대체해서는 안 된다. 비정상 종료, 준비 상태 실패, 모델 로드 실패와 선택 자원 소실을 기능 단위로 격리하고 새 버전 적용 실패 시 마지막 정상 버전으로 복구할 수 있어야 한다. 오케스트레이터나 상위 연결이 일시적으로 사용할 수 없어도 이미 실행 중인 핵심 기능과 로컬 안전은 유지되어야 하며, 재연결 후에는 현재 실행 상태·최근 결과·적용 구성 버전을 확인해 상위 상태와 정합할 수 있어야 한다.

**전달 경로**

실행관리 → 독립 실행 단위·외부 구성 배포/상태 확인 → 복구·rollback

**제안 주기**

기능·구성 배포/변경, 상태 이상, 장애 또는 배포 실패 시

**주기 선정 이유**

배포와 장애 복구를 같은 기능 lifecycle 안에서 관리해야 부분 실패를 격리하고 새 버전 실패 시 검증된 상태로 복구할 수 있다.

**1차 선정 기술**

독립 실행 lifecycle + health 상태 분리 + 버전 구성 배포 + last-known-good rollback + 로컬 상태 정합 + 현재 K3s/Helm provider

**선정 이유**

Kubernetes의 startup/liveness/readiness probe는 컨테이너 시작·재시작·트래픽 수용 상태를 구분하는 공식 선례이며 실제 로봇 운용·안전 상태와 동일시하지 않는다. 네트워크 단절 후 edge-local 상태를 보존하고 상위와 재정합하는 구조는 KubeEdge DeviceTwin을 설계 참고로만 사용하며 KubeEdge runtime에 의존하지 않는다. 참고: https://kubernetes.io/docs/concepts/workloads/pods/probes/ ; https://kubeedge.io/docs/architecture/edge/devicetwin/

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-03, BE-X-06, AI-B-02, AI-O-02, AI-L-07, AI-C-20

**담당**

진나영

---

#### AI-B-06 — 자원 상태 기반 실행 재구성

**ID**

AI-B-06

**대분류**

실행·자원

**소분류**

자원 상태 기반 실행 재구성

**요구사항 정의**

운영 중 자원 부족, 과열, 지연 증가, 실행 노드 또는 기능 제공자 소실이 발생하면 현재 필요한 기능과 근거 요구, 사용 가능한 provider와 검증된 실행 프로파일을 다시 평가해 실행 위치·처리주기·구성 수준을 조정할 수 있어야 한다. 구체적인 재구성 알고리즘을 특정 규칙이나 오케스트레이터에 고정하지 않고 교체·검증 가능하게 유지해야 한다. 대체 후보가 없으면 우선순위가 낮은 선택 기능부터 축소하고 핵심 기능은 가능한 범위에서 유지해야 한다.

**전달 경로**

AI 관측·근거 요구·실행 프로파일 → 실행 관리환경·AI 실행환경

**제안 주기**

운영 중 주기 평가, 자원 포화·지연 증가·provider 상태 변화 시 즉시 재평가

**주기 선정 이유**

자원 상태뿐 아니라 현재 필요한 기능과 사용 가능한 실행 후보가 함께 바뀌므로 고정 배치보다 검증된 범위에서 재구성할 수 있어야 한다.

**1차 선정 기술**

capability/resource/evidence-aware 재구성 인터페이스 + 교체 가능한 선택 정책

**선정 이유**

어떤 재구성 알고리즘이 최적인지는 하드웨어·도메인·부하에 따라 달라질 수 있으므로 요구사항에서는 고정하지 않고 비교 실험으로 선정한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-04, AI-B-05, AI-O-01, AI-S-05

**담당**

진나영

---

#### AI-B-08 — AI 실행 제공자 추상화

**ID**

AI-B-08

**대분류**

실행·자원

**소분류**

AI 실행 제공자 추상화

**요구사항 정의**

AI 기능은 특정 모델 파일 형식, 추론 엔진, 가속기 API나 로컬·원격 실행 방식에 직접 의존하지 않고 공통 capability 실행 인터페이스를 통해 기능을 요청해야 한다. 각 실행 제공자는 자신이 제공하는 기능의 의미, 지원 입력·출력, 필요한 하드웨어·런타임 조건, 현재 상태, 검증된 적용 범위와 사용 가능한 실행 프로파일을 등록할 수 있어야 한다. 특정 제공자가 사라져도 같은 capability를 제공하는 다른 제공자로 교체할 수 있어야 하며 대체 제공자가 없으면 해당 선택 기능만 비활성화해야 한다.

**전달 경로**

AI 모듈 → 공통 capability 실행 인터페이스 → runtime/provider adapter → 실행 노드

**제안 주기**

AI 기능 실행 요청 시

**주기 선정 이유**

모델 형식과 하드웨어 API를 상위 기능에서 분리하고 capability 의미와 검증 범위를 기준으로 요청해야 새 모델·장치 교체가 핵심 코드 변경으로 확산되지 않는다.

**1차 선정 기술**

공통 AI Capability 실행 인터페이스 + 교체 가능한 로컬·원격 provider

**선정 이유**

상위 기능은 구체 모델·런타임을 모르고 목적 수준 capability만 요청하며, 실제 provider는 등록된 조건과 검증 범위 안에서 사용한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-06, AI-B-09, AI-C-04, AI-C-18

**담당**

진나영

---

#### AI-B-09 — 신규 확장 등록·호환성 검증

**ID**

AI-B-09

**대분류**

실행·자원

**소분류**

신규 확장 등록·호환성 검증

**요구사항 정의**

새로운 센서, 하드웨어, AI 기능, 모델, 런타임 또는 외부 서비스 제공자가 추가되면 기존 핵심 코드를 수정하지 않고 어댑터와 capability 등록 정보만으로 연결할 수 있어야 한다. 운영 선택 후보가 되기 전에는 지원 입력·출력, 실행 조건, 품질·자원 특성, 상태 확인, 장애 시 동작과 적용 가능한 범위를 검증하고 AI-B-01의 독립 실행 구성으로 프로파일을 생성해야 한다. 모델·하드웨어·런타임·입력 구성 등이 달라진 경우 다른 조합의 품질·비용 프로파일을 자동으로 재사용해서는 안 된다.

**전달 경로**

신규 구성 → 검증 환경 → capability·실행 프로파일 등록 → 운영 선택 후보

**제안 주기**

새 센서·하드웨어·모델·기능·런타임·외부 서비스 추가 또는 조합 변경 시

**주기 선정 이유**

핵심 코드 수정 없는 확장과 새 조합의 독립 검증을 함께 보장해야 범용성과 성능 보장을 동시에 유지할 수 있다.

**1차 선정 기술**

Adapter + Capability 등록 + 신규 조합 호환성·품질·자원·health 검증

**선정 이유**

새 provider 조합은 검증되기 전에는 정상 선택 후보로 간주하지 않고 AI-B-01 실행 프로파일을 새로 생성한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-06, AI-B-08, AI-C-10, AI-C-18

**담당**

진나영

---

#### AI-B-10 — 말단 경량 실행 경계

**ID**

AI-B-10

**대분류**

실행·자원

**소분류**

말단 경량 실행 경계

**요구사항 정의**

자원 제약이 큰 말단은 로컬 안전·센서 처리·필요한 AI 실행과 전송 클라이언트만 유지하고, 브로커·수집기·백본 메시지 서버·중앙 관측 저장소 같은 서버형 미들웨어를 기본 설치 대상으로 요구해서는 안 된다. 현재 배포에서는 말단이 업무 전송 클라이언트와 관측 SDK, 미디어 송신 기능만 사용하고 MQTT 브로커, OpenTelemetry Collector, Prometheus, MQTT-Kafka 브릿지와 K3s 제어 기능은 구역 엣지에 둔다. 향후 더 강한 말단 하드웨어를 사용하더라도 추가 서비스 배치는 실행 프로파일에서 선택적으로 결정해야 한다.

**전달 경로**

말단 경량 클라이언트 → 구역 엣지 서버형 서비스

**제안 주기**

배포 프로파일 정의·말단 하드웨어 변경 시

**주기 선정 이유**

말단에 서버형 미들웨어를 중복 배치하면 메모리·CPU·저장공간·운영 복잡도가 증가하므로 구역 단위 서비스를 엣지에 집약하는 편이 현재 구조와 자원 최소화 원칙에 맞는다.

**1차 선정 기술**

현재 프로파일: 말단 Paho MQTT·OTel SDK·미디어 client / 엣지 Mosquitto·OTel Collector·Prometheus·Bridge·K3s

**선정 이유**

프로젝트 문서 260827_데이터_전송_및_아키텍처v8.docx는 말단에는 무거운 서버를 두지 않고 클라이언트 라이브러리만 두며, 받아주는 서버와 수집기는 엣지에 둔다고 명시한다. 참고: §3, §5-8~§5-9.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-05, BE-T-01, BE-S-02, AI-B-02~05, AI-O-01

**담당**

진나영

---

#### AI-B-11 — 복수 실행영역 통합 실행관리

**ID**

AI-B-11

**대분류**

실행·자원

**소분류**

복수 실행영역 통합 실행관리

**요구사항 정의**

배포 관리 기능이 존재하는 서버·구역 엣지·추가 연산 영역은 동일한 실행 제어·배치·health·롤백 계약으로 관리할 수 있어야 한다. 각 실행영역에서 실제 사용하는 오케스트레이터와 실행 방식은 provider로 분리하고, 중앙 서버와 모든 엣지가 동일한 오케스트레이터를 설치해야 한다는 전제를 두어서는 안 된다. 한 실행영역의 제어면이 사용할 수 없어도 다른 영역에서 이미 실행 중인 기능과 로컬 핵심 기능은 계속 동작해야 하며 말단은 오케스트레이터 제어면을 필수 설치 대상으로 요구하지 않아야 한다.

**전달 경로**

배포 요청 → 공통 실행관리 provider → 현재 사용 가능한 서버·엣지·추가 연산 영역

**제안 주기**

배포·재배치·장애·자원 변화 시 이벤트 기반

**주기 선정 이유**

실행영역마다 운영환경이 다를 수 있으므로 실제 오케스트레이터를 통일하기보다 상위 실행관리 계약을 통일하는 것이 범용성과 장애 격리에 유리하다.

**1차 선정 기술**

복수 실행영역 공통 실행관리 provider + 현재 엣지 K3s provider

**선정 이유**

현재 배포에서 구역 엣지는 K3s를 사용할 수 있지만 중앙 서버나 다른 실행영역에 동일한 K3s 제어면을 두는 것을 프레임워크 필수조건으로 하지 않는다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-03, AI-B-04, AI-B-05, AI-C-04

**담당**

진나영

---

### 관측·운영

#### AI-O-01 — 실행 성능·자원 관측

**ID**

AI-O-01

**대분류**

관측·운영

**소분류**

실행 성능·자원 관측

**요구사항 정의**

각 실행 기능은 처리지연, 처리량, 대기열, 누락·오류, CPU·가속기·메모리 등 현재 측정 가능한 실행 상태를 로컬에서 확인할 수 있어야 한다. 근거 선택, 실행 재구성, capability 탐색 등 AI 관리 기능 자체의 처리지연과 자원 오버헤드도 측정 가능해야 하며, 관리 기능이 절감하는 자원보다 더 큰 비용을 유발하는지 평가할 수 있어야 한다. 일반 수치 지표는 구역 엣지에서 상세값을 유지하고 서버에는 운영에 필요한 요약을 전달할 수 있어야 한다. 개별 장치의 생사와 치명적 오류처럼 집계 과정에서 사라지면 안 되는 상태는 일반 수치 요약과 분리하고 외부 관측 기능 장애가 실제 기능 실행을 중단시키지 않아야 한다.

**전달 경로**

실행 노드·AI 관리기능 → 관측 수집 → 엣지 상세 지표 / 서버 요약 지표

**제안 주기**

로컬 자원 조정은 1~2초 수준, 중앙 운영 요약은 현재 15초, 임계·오류 사건은 즉시

**주기 선정 이유**

범용 자원 최적화 기능의 효과를 증명하려면 AI workload뿐 아니라 선택·재구성 로직 자체의 비용까지 포함한 순이익을 측정해야 한다.

**1차 선정 기술**

Local metrics + AI 관리 오버헤드 metrics + OTel/OTLP + 엣지 상세 보관 + 서버 metric 요약

**선정 이유**

측정 가능한 실행·관리 비용을 같은 기준으로 관측하여 모델·provider·선택 정책 비교 실험과 운영 재구성의 근거로 사용한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

HW-C-05, BE-S-02, VZ-I-04, AI-B-01, AI-B-06, AI-S-05

**담당**

진나영

---

#### AI-O-02 — AI 오류·이상 사건 기록

**ID**

AI-O-02

**대분류**

관측·운영

**소분류**

AI 오류·이상 사건 기록

**요구사항 정의**

입력·추론·계획·검증·실행 실패, 목표 지연 초과, 선택 기능 비가용과 실행 상태 변화를 구조화된 오류·이상 사건으로 기록해야 한다. 치명적 오류와 기능 불가 상태는 일반 metric 집계 주기를 기다리지 않고 개별 사건으로 즉시 전달할 수 있어야 한다. 선택 기능이 없는 정상적인 축소 운용과 핵심 기능 실패를 구분하고, 외부 수집기가 일시적으로 사용할 수 없어도 로컬 오류 기록은 남아야 한다.

**전달 경로**

AI 실행 노드 → 로컬 사건 기록 → 관측 경로 → 서버 오류·알림

**제안 주기**

오류·이상 발생 즉시

**주기 선정 이유**

치명적 오류를 평균·요약 지표에 포함하면 개별 장애가 가려질 수 있으므로 사건 단위로 보존하고 즉시 전달해야 한다.

**1차 선정 기술**

Local structured event + OTel log/trace + 즉시 오류/알림 경로

**선정 이유**

변경 아키텍처에서는 log와 trace를 요약하지 않고 원본 전달하며, 장치 생사와 치명 오류는 metric 요약과 별도로 취급한다. 참고: 변경 데이터 전송 아키텍처 6-3, 6-5.

**연관분야**

백엔드, 가시화

**연관점**

BE-X-05, VZ-I-10, BE-C-01

**담당**

진나영

---

#### AI-O-03 — 오류 재현 정보 관리

**ID**

AI-O-03

**대분류**

관측·운영

**소분류**

오류 재현 정보 관리

**요구사항 정의**

중요 오류나 이상 판단은 당시 사용한 원본 입력, 실행 구성, 버전과 사용 가능한 업무 데이터·관측 참조를 연결해 다시 분석할 수 있어야 한다. 적용된 모델·판단 정책·환경 지식 버전에 관련 학습·검증 계보가 존재하는 경우 해당 참조를 함께 남겨야 한다. 업무·책임 추적과 기술 성능 추적은 서로 다른 목적으로 유지해야 한다. 업무 스트림이 단기 보존 구간 안에 있는 경우에는 전송 로그의 위치를 이용해 재현할 수 있고, 보존 기간이 지난 데이터는 업무 저장소나 별도 아카이브가 존재하는 경우 그 참조를 사용해야 한다. 특정 전송·관측 기술이 없더라도 확보된 참조 범위에서 재현 정보를 남겨야 한다.

**전달 경로**

AI 실행환경 → 업무 저장/단기 전송로그 참조 + 관측 trace 참조 → 재현

**제안 주기**

중요 오류·이상 발생 시 기록, 신규 버전 검증 시 재사용

**주기 선정 이유**

전송 로그는 장기 저장소가 아니므로 단기 재현과 장기 재현의 데이터 원천을 분리해야 한다.

**1차 선정 기술**

업무/감사 상관 정보 + 단기 Kafka replay 참조 + 장기 저장/아카이브 참조 + OTel trace

**선정 이유**

현재 Kafka는 다중 소비자 버퍼와 단기 재생을 위한 계층이며 장기 저장소가 아니다. 참고: 변경 데이터 전송 아키텍처 6-1.

**연관분야**

백엔드, 가시화

**연관점**

BE-T-02, BE-S-01, BE-S-02, BE-S-04, AI-L-08

**담당**

진나영

---

#### AI-O-04 — 실행 가용성 신호 연계

**ID**

AI-O-04

**대분류**

관측·운영

**소분류**

실행 가용성 신호 연계

**요구사항 정의**

AI 실행 기능은 자신의 실행 가능 여부와 치명적 오류를 개별 상태로 제공해야 하며, 이 상태가 일반 자원 지표의 요약 과정에서 소실되어서는 안 된다. 말단·노드의 최종 가용성은 백엔드가 업무 전송 상태와 관측 상태를 통합해 판정하고, AI는 이 통합 결과를 원격 기능 선택과 실행 재구성에 사용해야 한다. 업무 전송은 가능하지만 관측 지표가 누락된 경우와 관측 지표는 존재하지만 업무 세션이 끊긴 경우를 구분할 수 있어야 한다.

**전달 경로**

실행 기능·장치 상태 → 업무/관측 경로 → 백엔드 통합 가용성 → AI 기능 선택

**제안 주기**

생사·연결·치명 오류 변화 시 즉시

**주기 선정 이유**

일반 metric 요약만으로는 특정 장치의 급사나 통신 불가를 놓칠 수 있으므로 개별 신호를 유지하고, 최종 가용성 판정은 한 곳에서 통합해야 한다.

**1차 선정 기술**

기능 health + MQTT LWT/heartbeat + Prometheus up/alert + 백엔드 availability

**선정 이유**

변경 데이터 전송 아키텍처는 장치 생사를 업무 평면과 관측 평면에서 각각 감지한 뒤 백엔드 단일 지점에서 통합 판정하고, 두 신호가 다르면 업무 세션을 1차 기준으로 사용하도록 정의한다. 참고: 6-5.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-O-01/02, AI-C-10, AI-C-05, BE-T-04

**담당**

진나영

---

### 공통 아키텍처

#### AI-C-01 — 공통 데이터 규약

**ID**

AI-C-01

**대분류**

공통 아키텍처

**소분류**

공통 데이터 규약

**요구사항 정의**

AI가 생산·소비하는 데이터는 백엔드가 정의한 공통 식별·시간·버전 규약과 의미 체계를 따라야 한다. 원본 센서 관측, 관측·AI에서 생성된 개별 근거, 동일 물리 객체의 지속 상태, 미확정 의미 가설, 환경 사전정보, 추가 관측 요청, 사용자 확인 정보, 학습 후보·검증 결과, capability와 실행 프로파일처럼 성격이 다른 정보는 의미 수준에서 구분할 수 있어야 한다. 서로 다른 정보는 원본 관측, 대상 객체, 생산자와 버전 등 필요한 참조를 통해 관계를 추적할 수 있어야 한다. 요구사항 단계에서는 구체 변수명과 직렬화 형식을 확정하지 않고 전체 파트 요구사항 확정 후 공통 데이터 사전에서 통일해야 한다.

**전달 경로**

온디바이스 ↔ 엣지 노드 ↔ 서버

**제안 주기**

스키마 확정·버전 변경 시

**주기 선정 이유**

공통 데이터 계약을 사용해야 장치·모델·전송 방식이 바뀌어도 파트 간 인터페이스를 유지할 수 있다.

**1차 선정 기술**

공통 Domain Schema + Serializer provider

**선정 이유**

전송 아키텍처는 같은 시스템 안에서도 데이터 성격에 따라 MQTT, Kafka, OTLP, 별도 미디어 경로를 사용하므로 의미 규약과 전송 구현을 분리해야 한다. 참고: 프로젝트 문서 260827_데이터_전송_및_아키텍처v8.docx §4.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-C-01, HW-C-02, VZ-I-01, AI-C-03, AI-S-06, AI-L-08

**담당**

진나영

---

#### AI-C-02 — 좌표계 경계

**ID**

AI-C-02

**대분류**

공통 아키텍처

**소분류**

좌표계 경계

**요구사항 정의**

AI가 산출하는 위치·방향·영상 좌표에는 어떤 기준 좌표계에서 나온 값인지 알 수 있는 정보가 포함되어야 한다. 카메라 보정 프로파일이 있는 경우 영상과 광선·로컬 공간 사이의 변환에 활용하고, 전역 좌표 변환은 백엔드 디지털 트윈이 담당해야 한다. 보정 정보나 전역 변환 기능이 없으면 로컬 영상 좌표 결과를 유지해야 한다.

**전달 경로**

온디바이스·엣지 노드 → 서버

**제안 주기**

공간 정보 생성 시

**주기 선정 이유**

좌표 변환 책임을 한 곳에 두어 AI·디지털 트윈·가시화 간 좌표 불일치를 방지한다.

**1차 선정 기술**

로컬 좌표 기준 유지 + 전역 좌표 변환은 선택 기능으로 사용

**선정 이유**

전역 좌표·캘리브레이션을 필수 전제로 두지 않아 Vision 자체는 단독으로 동작할 수 있다. 참고 근거는 팀 공통 규약: BE-C-04이다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-02, BE-C-04, DT-03

**담당**

진나영

---

#### AI-C-03 — 프레임 참조·시간 규약

**ID**

AI-C-03

**대분류**

공통 아키텍처

**소분류**

프레임 참조·시간 규약

**요구사항 정의**

영상·센서 입력과 파생 결과는 원본 관측을 다시 찾을 수 있는 참조와 측정 시각, 처리 순서를 유지해야 한다. 원본 프레임·센서 관측을 식별하는 참조와 여러 관측에 걸쳐 유지되는 물리 객체 식별 상태는 서로 다른 목적으로 관리하고, 하나의 지속 객체가 어떤 원본 관측들에서 관찰되었는지 추적할 수 있어야 한다. 현재 프로젝트에서 노드 간 결과를 융합하거나 프레임을 정합하는 기능은 백엔드 공통 시간 기준을 따라야 하며 NTP를 현재 시간 동기화 방식으로 사용한다. 시간 동기화가 일시적으로 깨져도 단일 노드의 로컬 처리와 순서는 유지하되 노드 간 정합이 필요한 기능은 정확도가 저하된 상태로 처리해야 한다.

**전달 경로**

온디바이스·엣지 노드 → 서버

**제안 주기**

모든 관측·파생 결과 생성 시

**주기 선정 이유**

영상 오버레이, 지연 측정, 오류 재현에서 동일한 원본 입력을 식별할 수 있어야 한다.

**1차 선정 기술**

공통 프레임 참조·시간 계약 + 현재 NTP 동기 + 로컬 순서 fallback

**선정 이유**

프레임 정합과 여러 노드 결과 병합에는 공통 시간 기준이 필요하지만, 일시적 동기화 장애를 전체 AI 중단으로 확대하지 않는다. 현재 공통 규약 BE-C-03과 정합한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-C-03, HW-S-08, VZ-I-07, AI-S-06, AI-L-08

**담당**

진나영

---

#### AI-C-04 — 외부 기술·제공자 어댑터

**ID**

AI-C-04

**대분류**

공통 아키텍처

**소분류**

외부 기술·제공자 어댑터

**요구사항 정의**

센서 입력, 미디어, 메시지 전송, 데이터 직렬화, AI 실행, 실행관리, 제어, 저장·관측, 보안과 디지털 트윈·시뮬레이션 소비자 등 외부 기술은 기능별 공통 인터페이스와 provider/adapter 경계 뒤에 두어야 한다. 외부 기술은 공통 의미 계약, 직렬화 binding, 현재 배포 provider, 설계 참고 표준을 구분해 사용하고, 상위 인지·판단·위험·학습 기능은 특정 센서 제품, 통신 프로토콜, GPU 벤더, 모델 형식, 런타임, 저장소, 오케스트레이터 또는 보안 제품의 API와 자료구조를 직접 참조해서는 안 된다. ROS 2 Action, AIP-155, W3C WoT, KubeEdge 등에서 차용한 개념은 해당 middleware·wire protocol·runtime의 설치를 요구하지 않아야 한다.

**전달 경로**

외부 장치·서비스·인프라·DT 소비자 ↔ 공통 provider/adapter ↔ AI 핵심 모듈

**제안 주기**

새 외부 기술·센서·모델·실행·DT 소비자 추가 또는 provider 변경 시

**주기 선정 이유**

현재 배포 기술과 프레임워크 핵심 계약을 분리해야 MQTT, Kafka, OTel, K3s, 특정 DT 엔진 등이 바뀌어도 업무 로직을 재사용할 수 있다.

**1차 선정 기술**

Sensor/Media/Transport/Serializer/Runtime/Control/Observability/Security/Orchestrator/Digital-Twin Provider Interface

**선정 이유**

현재 MQTT·Kafka·OTel·OCI·K3s·Tailscale 등은 배포 profile의 구현 기술로, ROS 2 Action·AIP-155·W3C WoT·KubeEdge 등은 설계 참고 근거로 구분한다. 새 기술이 추가되어도 상위 의미 계약은 유지해야 한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-B-02/03/04/05/08/11, AI-C-01/07/15/18/20, DT-01

**담당**

진나영

---

#### AI-C-05 — 핵심·선택 기능 및 의존성 격리

**ID**

AI-C-05

**대분류**

공통 아키텍처

**소분류**

핵심·선택 기능 및 의존성 격리

**요구사항 정의**

배포 환경마다 반드시 유지해야 하는 최소 핵심 기능과 품질·효율을 높이는 선택 기능·조건을 구분해 선언해야 한다. 필수 조건이 충족되지 않으면 해당 기능만 비활성화하고, 선택 조건이나 선택 센서·하드웨어·서비스가 없어지면 가능한 기본 또는 축소 모드로 전환해야 한다. 원격 장치가 백엔드의 최종 가용성 판정에서 사용할 수 없는 상태라면 그 장치가 제공하는 선택 capability는 선택 후보에서 제외해야 하며, 하나의 선택 구성요소나 통신 경로 장애가 관련 없는 기능의 시작·실행·복구를 차단해서는 안 된다.

**전달 경로**

Capability Registry·가용성 → 각 기능의 required/optional 조건 → ACTIVE/DEGRADED/DISABLED

**제안 주기**

시작·capability/가용성 변화·장애 발생 시

**주기 선정 이유**

선택 구성요소의 결손을 정상적인 구성 변형으로 처리해야 새로운 장치 추가·제거와 부분 장애에 강한 시스템을 만들 수 있다.

**1차 선정 기술**

Core/Optional capability 선언 + required/optional dependency + 단계적 기능 축소

**선정 이유**

기능 자체 health와 장치의 authoritative availability를 구분하면서 해당 의존 기능만 격리한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-N-01, AI-E-01/02, AI-B-02~06/08, AI-C-10, AI-O-02/04, BE-A-03

**담당**

진나영

---

#### AI-C-06 — 데이터 유형별 전송 경계

**ID**

AI-C-06

**대분류**

공통 아키텍처

**소분류**

데이터 유형별 전송 경계

**요구사항 정의**

AI가 생산·소비하는 데이터는 업무 데이터, 시스템 관측 데이터와 영상 등 대용량 미디어 데이터의 의미를 구분하고 각 유형에 맞는 공통 전송·관측·미디어 인터페이스를 사용해야 한다. 물리 제어 요청은 업무 데이터의 특수 유형으로 다루되 전달·순서·실행 결과·책임 추적 요구를 별도 정책으로 적용해야 한다. 상위 AI 로직은 데이터의 의미만 생산·소비하고 실제 구간별 전송 기술이나 경로를 직접 호출해서는 안 된다.

**전달 경로**

AI 데이터 → 업무/제어·관측·미디어 유형 분류 → 유형별 provider → 대상

**제안 주기**

데이터 생성·전송 시 항상 적용

**주기 선정 이유**

업무·관측·대용량 미디어와 제어 요구는 신뢰성·대역폭·보존 특성이 다르므로 의미와 실제 전송 구현을 분리해야 한다.

**1차 선정 기술**

Transport/Observability/Media Interface + 현재 MQTT(말단↔엣지), Kafka(엣지↔서버), OTLP, 별도 미디어 경로

**선정 이유**

현재 배포의 MQTT·Kafka·OTLP·RTP/RTSP/WebSocket 등은 provider 구현이며 상위 AI 요구사항은 특정 프로토콜을 필수 기술로 고정하지 않는다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-T-01/02, BE-S-02/03, AI-B-03, AI-C-01/04/08, AI-O-01/02

**담당**

진나영

---

#### AI-C-07 — 데이터 직렬화

**ID**

AI-C-07

**대분류**

공통 아키텍처

**소분류**

데이터 직렬화

**요구사항 정의**

업무 데이터의 의미 계약과 네트워크 직렬화 형식을 분리하고 공통 직렬화 인터페이스를 통해 변환해야 한다. 현재 machine-to-machine 계약의 canonical serialization은 Protobuf를 사용하되 상위 인지·판단·제어 로직이 Protobuf API나 생성 코드 자료구조를 업무 의미 모델로 직접 사용해서는 안 된다. 스키마 변경 시 기존 필드 식별자를 재사용하지 않는 등 호환 가능한 진화 규칙을 유지하고, 사람이 확인하는 경계에서는 필요 시 JSON 표현을 제공할 수 있어야 한다.

**전달 경로**

온디바이스 ↔ 엣지 노드 ↔ 서버

**제안 주기**

스키마 변경 시

**주기 선정 이유**

Protobuf는 schema-first typed serialization과 다중 언어 코드 생성을 제공하며 field number 기반 호환성 규칙으로 계약 진화를 관리할 수 있다.

**1차 선정 기술**

Serializer Interface + 현재 canonical Protobuf binding + 필요 시 JSON 표현

**선정 이유**

Protobuf를 현재 기계 간 직렬화 binding으로 사용하되 의미 계약과 직렬화 구현은 분리한다. 참고: Protocol Buffers Overview 및 schema evolution guidance (https://protobuf.dev/overview ; https://protobuf.dev/programming-guides/proto3/)

**연관분야**

백엔드, 가시화

**연관점**

BE-C-01, AI-C-01, AI-C-04, AI-C-08, AI-C-20

**담당**

진나영

---

#### AI-C-08 — 미디어 입력 관리

**ID**

AI-C-08

**대분류**

공통 아키텍처

**소분류**

미디어 입력 관리

**요구사항 정의**

네트워크 카메라, 로봇 로컬 카메라, 파일 재생 등 서로 다른 영상 소스를 공통 미디어 입력으로 제공해야 한다. 영상 픽셀은 대용량이므로 업무 데이터 MQTT/Kafka나 관측 OTLP에 실어 보내지 않고 별도 미디어 경로를 사용해야 한다. 카메라 URL, 장치 경로, codec, 전송 방식은 미디어 어댑터 내부에 숨기고 인지 기능은 공통 영상 입력만 소비해야 한다. 영상 소스가 없는 배치에서는 영상 기반 기능만 비활성화해야 한다.

**전달 경로**

카메라·로봇 → 별도 미디어 경로 → 인지/가시화 소비자

**제안 주기**

영상 스트림 연결 동안 지속

**주기 선정 이유**

RTSP는 실시간 미디어 전달의 표준 제어 프로토콜이고 GStreamer는 RTP/RTSP, jitter, transport 선택과 다양한 디코더를 제공해 카메라별 차이를 어댑터에서 흡수할 수 있다.

**1차 선정 기술**

Media Source Interface + 현재 RTP/RTSP·JPEG 수신 provider + FFmpeg/OpenCV/GStreamer decode + local-camera adapter + 필요 시 WebSocket 중계 adapter

**선정 이유**

현재 배포의 말단↔엣지 영상은 RTP/UDP 기반 별도 미디어 경로를 사용하고 고정 IP 카메라는 필요 시 RTSP 세션을 사용한다. 엣지에서 디코드한 원본 영상의 서버 중계가 필요한 경우에는 frame_ref를 유지한 WebSocket 온디맨드 중계를 사용한다. 상위 인지 기능은 이 구체 전송·코덱 차이를 미디어 어댑터 뒤에서 공통 입력으로 소비한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-01, AI-E-02, HW-R-04, HW-S-06

**담당**

진나영

---

#### AI-C-10 — 기능·자원 등록·탐색

**ID**

AI-C-10

**대분류**

공통 아키텍처

**소분류**

기능·자원 등록·탐색

**요구사항 정의**

실행 노드, 센서·미디어 입력, 로봇·장치 기능, AI 기능, 런타임, 전송·관측·외부 서비스는 현재 제공 가능한 capability와 자원, 상태, 버전, 입력·출력 조건을 등록할 수 있어야 한다. capability의 의미와 선언 항목은 공통 규약을 사용하고 상위 기능은 특정 장치나 모델 이름이 아니라 필요한 capability를 기준으로 탐색할 수 있어야 한다. AI 기능 등록은 백엔드가 관리하는 장치·구역·최종 가용성 정보를 보완하는 역할로 한정하고 장치의 최종 가용성을 별도로 중복 판정해서는 안 된다. 중앙 등록 기능이 일시적으로 없어도 각 노드는 마지막으로 확인된 로컬 정보를 이용해 기존 기능을 계속 실행해야 한다.

**전달 경로**

구성요소 capability 등록 ↔ 구역 엣지/백엔드 레지스트리·가용성 판정 → AI 기능 탐색

**제안 주기**

시작·등록·상태/구성/capability 변경 시 즉시 갱신

**주기 선정 이유**

서로 다른 로봇·센서·AI가 서로 다른 기능을 제공하므로 장치명 대신 공통 capability 의미로 등록·탐색해야 상위 코드를 유지할 수 있다.

**1차 선정 기술**

Capability Registry + 공통 Capability Contract + 백엔드 장치 가용성 연계 + 로컬 snapshot

**선정 이유**

AI capability 등록은 장치의 최종 availability 판정을 대체하지 않고 C-18의 선언 규약에 따라 현재 제공 가능한 기능을 보완한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-04/05/06/08/09, AI-C-04/05/18/19

**담당**

진나영

---

#### AI-C-13 — 기능 선택·자원 최소화

**ID**

AI-C-13

**대분류**

공통 아키텍처

**소분류**

기능 선택·자원 최소화

**요구사항 정의**

기능 선택과 실행 구성은 현재 업무·안전·필요 정보·지연 조건을 만족하는 범위에서 사용 가능한 provider의 실제 품질·자원·비용 프로파일을 이용해 불필요한 실행을 줄일 수 있어야 한다. 선택 방법은 특정 점수식, 규칙 또는 학습 알고리즘을 프레임워크의 필수 방식으로 고정하지 않고 교체 가능하게 유지해야 하며, 후보 방법은 동일 조건에서 판단 품질, 자원 사용, 지연, 실패 안정성과 선택 로직 자체의 오버헤드를 포함해 비교할 수 있어야 한다. 자원이 부족하거나 구성요소가 바뀌면 검증된 범위 안에서 가능한 실행 후보로 재구성하고 핵심 기능을 우선 유지해야 한다.

**전달 경로**

전체 기능·근거 요구·실행 프로파일 → 교체 가능한 구성 선택 → 실행관리

**제안 주기**

기능 실행·상태·자원·필요 정보 조건 변화 시

**주기 선정 이유**

범용 프레임워크에서 최적 선택 방법은 도메인과 장비에 따라 달라질 수 있으므로 요구사항은 목적과 검증 조건만 정의하고 알고리즘은 실험으로 선정해야 한다.

**1차 선정 기술**

제약조건 기반 구성 선택 인터페이스 + 교체 가능한 선택 policy + 실측 실행 프로파일

**선정 이유**

고정 가중치나 특정 알고리즘을 요구사항으로 두지 않고 기존 연구·신규 제안 방법을 객관적 비교실험으로 검증한 뒤 구현 프로파일에서 선택한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01/06/09, AI-S-05, AI-C-18, AI-O-01

**담당**

진나영

---

#### AI-C-15 — 도메인·배포 프로파일 분리

**ID**

AI-C-15

**대분류**

공통 아키텍처

**소분류**

도메인·배포 프로파일 분리

**요구사항 정의**

로봇 자율주행 지원, 시설 감시, 하천 위험 관리 등 서로 다른 적용 분야는 별도 핵심 프로그램으로 분기하지 않고 동일 프레임워크 위에서 입력 어댑터, 활성 capability, 판단 기준, 실행 위치와 외부 제어 연결을 배포 프로파일로 구성해야 한다. 새 도메인을 추가할 때 기존 인지·판단·실행관리 핵심 코드를 수정하지 않고 필요한 어댑터·기능 제공자·설정과 검증된 도메인별 요구 조건을 추가하는 방식으로 확장할 수 있어야 한다. Unity를 포함한 특정 디지털 트윈 엔진·시뮬레이터·가시화 기술도 도메인 핵심 코드가 아니라 선택 가능한 소비자 또는 배포 구성으로 취급해야 하며, 특정 도메인에서 사용하지 않는 기능은 설치·실행을 요구하지 않아야 한다.

**전달 경로**

배포 프로파일 → 기능·어댑터·실행 위치·DT 소비자 구성

**제안 주기**

새 도메인·현장·장비·디지털 트윈 조합을 배포하거나 구성이 변경될 때

**주기 선정 이유**

도메인명이나 특정 DT 엔진을 기준으로 핵심 코드에 분기문을 추가하면 적용 분야가 늘어날수록 결합도가 증가하므로 차이는 구성과 확장점으로 격리해야 한다.

**1차 선정 기술**

공통 프레임워크 + Domain/Deployment Profile + Adapter/Capability/Consumer 조합

**선정 이유**

로봇·시설·하천·군사 시나리오와 Unity·기타 DT 구현은 동일 프레임워크의 서로 다른 배포 프로파일 또는 소비자 구성으로 다룬다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-C-04/05/10/13/18/19, AI-B-04/06/09, DT-01

**담당**

진나영

---

#### AI-C-16 — 폐쇄망 배포 경계

**ID**

AI-C-16

**대분류**

공통 아키텍처

**소분류**

폐쇄망 배포 경계

**요구사항 정의**

폐쇄망 배포 프로파일에서는 공개 인터넷에 대한 나가는 연결 없이 핵심 기능을 기동·복구·재구성할 수 있어야 한다. 실행에 필요한 이미지, 모델, 패키지, 규칙과 보정정보는 배포 프로파일이 지정한 내부 조달 경로에서 확보할 수 있어야 하고, 외부 AI 서비스나 외부 API는 선택 기능으로 선언해 사용할 수 없을 때 해당 기능만 비활성화해야 한다. 어떤 실행 단위가 외부 연결을 요구하는지는 등록 정보로 드러나야 하며 폐쇄망 프로파일에서는 배치 전에 이를 검증할 수 있어야 한다.

**전달 경로**

폐쇄망 배포 프로파일 → 내부 자산 조달·외부 연결 요구 검증 → 실행 노드

**제안 주기**

폐쇄망 프로파일 배포·검증 시, 외부 연결 요구 변경 시

**주기 선정 이유**

폐쇄망 요구는 모든 배포의 필수조건이 아니라 해당 보안 프로파일에서 검증해야 할 실행 제약이다.

**1차 선정 기술**

내부 자산 저장소 참조 + 외부 연결 요구 선언 + 폐쇄망 프로파일 검증

**선정 이유**

특정 저장소·네트워크 제품을 고정하지 않고 폐쇄망 프로파일에서 핵심 기능이 외부 인터넷 의존 없이 유지되는지만 규정한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-02, AI-B-05, AI-B-09, AI-E-04

**담당**

진나영

---

#### AI-C-18 — 구성요소 Capability 선언 규약

**ID**

AI-C-18

**대분류**

공통 아키텍처

**소분류**

구성요소 Capability 선언 규약

**요구사항 정의**

로봇, 센서, 카메라, 연산 노드, AI 기능과 외부 서비스는 자신이 제공할 수 있는 capability를 공통 의미 규약으로 선언할 수 있어야 한다. 선언에는 기능의 의미, 입력·출력 정보와 schema 참조, 단위·좌표·시간 기준, 필수·선택 실행 조건, 취소 가능 여부, 동일 입력 반복 호출 특성, 동기·비동기 특성, 수행·관측 범위, 현재 상태, 버전과 확인 가능한 품질·지연·자원 특성을 표현할 수 있어야 하며 모든 항목이 항상 존재한다고 가정해서는 안 된다. 상위 기능은 특정 장치·모델 이름이 아니라 필요한 capability와 조건을 기준으로 구성요소를 탐색·사용해야 한다.

**전달 경로**

장치·센서·AI·서비스 capability 선언 → Registry → 상위 기능 탐색·선택

**제안 주기**

구성요소 등록·기능·상태·버전 변경 시

**주기 선정 이유**

서로 다른 로봇과 센서가 제공하는 기능·범위가 달라도 공통 의미 계약을 사용해야 새 구성요소 추가 시 핵심 코드 변경과 잘못된 기능 조합을 줄일 수 있다.

**1차 선정 기술**

공통 Capability Contract + Registry 연계 + action input/output/behavior metadata

**선정 이유**

W3C WoT Thing Description 1.1의 ActionAffordance는 input/output, idempotent, synchronous 등 capability 표현의 공개 선례로 참고하되 Thing Description JSON-LD나 WoT runtime은 필수 의존성으로 사용하지 않는다. 참고: https://www.w3.org/TR/wot-thing-description11/

**연관분야**

하드웨어, 백엔드

**연관점**

AI-C-04/05/10/20, AI-B-01/04/08/09

**담당**

진나영

---

#### AI-C-19 — AI 판단·권한·책임 경계

**ID**

AI-C-19

**대분류**

공통 아키텍처

**소분류**

AI 판단·권한·책임 경계

**요구사항 정의**

AI는 인지·추적·객체 상태 해석, 추가 정보 필요성 판단, AI capability 선택, 학습 후보 선정·학습·성능 검증과 관리자 판단에 필요한 품질 정보를 담당해야 하며 장치의 최종 가용성 판정, 전역 좌표·최종 디지털 트윈 상태의 권위 있는 관리, 물리 명령 발급과 실제 센서·로봇·액추에이터 제어를 직접 소유해서는 안 된다. 추가 관측이 필요하다고 판단한 경우 필요한 정보·관측 조건을 요청하고 실제 장치 선택과 물리 행동은 백엔드·하드웨어의 제어 경계를 거쳐야 한다. 가시화 파트는 AI가 제공한 판단·학습 정보를 사용자에게 표현하고 사용자 입력을 전달하며 AI 판단 로직 자체를 중복 구현하지 않아야 한다.

**전달 경로**

AI 판단·정보 요구 ↔ 백엔드·하드웨어 제어 / AI 판단정보 ↔ 가시화 사용자 인터페이스

**제안 주기**

모든 AI 판단·관측 요청·학습·배포 승인 흐름에 상시 적용

**주기 선정 이유**

판단, 권한, 물리 실행과 사용자 표현의 책임을 분리해야 잘못된 직접 제어, 중복 구현, 오류 원인 불명확성과 안전 책임 혼선을 줄일 수 있다.

**1차 선정 기술**

파트 책임 경계 계약 + 정보/제어 요청 분리

**선정 이유**

AI는 판단 근거와 필요한 요청을 산출하고 실제 제어·최종 가용성·전역 상태·화면 표현은 각 담당 파트의 authoritative 책임으로 유지한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-S-05, AI-R-02, AI-O-04, AI-L-03/06/07, AI-C-02

**담당**

진나영

---

#### AI-C-20 — 물리 명령 의미 계약

**ID**

AI-C-20

**대분류**

공통 아키텍처

**소분류**

물리 명령 의미 계약

**요구사항 정의**

로봇·센서·액추에이터의 물리 행동 명령은 MQTT·Kafka·로봇 SDK와 독립된 공통 의미 계약을 사용해야 한다. 명령은 백엔드가 발급한 고유 command_id로 식별하고 논리 대상, 수행 action, action별 입력과 실행 유효기한을 표현할 수 있어야 한다. 전달 성공, 명령 수락·거부, 실행 중 상태와 최종 완료·실패·취소 결과를 구분해야 하며, 수락된 명령의 실행 lifecycle은 ACCEPTED, EXECUTING, CANCELING과 SUCCEEDED, ABORTED, CANCELED 상태를 구분할 수 있어야 한다. 거부는 실행 상태에 포함하지 않고 별도 수락 결과로 표현해야 한다. 동일 command_id가 재전달되면 동일 명령을 다시 물리 실행하지 않아야 하고, 취소 요청 수락과 실제 CANCELED 도달도 구분해야 한다. 긴급정지는 일반 취소와 구분하고 상위 네트워크 명령만을 안전 기능의 유일한 경로로 사용해서는 안 된다.

**전달 경로**

백엔드 명령 발급 → 서버 Kafka → 엣지 Bridge → 말단 MQTT → 로봇 어댑터 → SDK / 상태·결과 역방향 회신

**제안 주기**

명령 발생·상태 변화·취소·종료 시 이벤트 기반

**주기 선정 이유**

전달 ACK만으로 물리 동작의 수락·완료를 판단하면 중복 실행과 상태 오판이 발생할 수 있으므로 실행 lifecycle과 idempotency identity를 전송 기술과 분리해야 한다.

**1차 선정 기술**

독립 Physical Command Contract + 현재 Protobuf binding + Robot Adapter

**선정 이유**

ROS 2 Action의 goal lifecycle은 수락·실행·취소·종료 의미를 분리하는 공개 선례로, AIP-155는 request ID 기반 idempotency의 근거로 참고한다. ROS 2/DDS/Google API runtime은 사용하지 않는다. 참고: https://design.ros2.org/articles/actions.html ; https://google.aip.dev/155 ; https://protobuf.dev/

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-03, AI-B-05, AI-C-04, AI-C-06/07/18/19, BE-X-01/06

**담당**

진나영

---

### 학습·MLOps

#### AI-L-01 — 학습 필요성·후보 데이터 선별

**ID**

AI-L-01

**대분류**

학습·MLOps

**소분류**

학습 필요성·후보 데이터 선별

**요구사항 정의**

운영 중 미확인 객체, 반복 오류·불일치, 사용자 수정, 환경 변화나 성능 저하 등 기존 AI 또는 판단 정책을 재검토할 필요가 있는 사건을 학습 후보 발생 사유로 기록하고 관련 관측을 선택적으로 학습 후보로 표시할 수 있어야 한다. 모든 입력 데이터를 학습용으로 저장하는 것을 기본으로 하지 않고 후보가 된 이유, 관련 객체·환경·모델·버전과 원본 관측 참조를 함께 추적할 수 있어야 한다. 학습 후보 생성 기능이 없어지거나 실패해도 운영 추론과 로컬 안전 기능은 계속 동작해야 한다.

**전달 경로**

운영 관측·오류·사용자 수정·미확인 객체 → AI 학습 후보 판정 → 단기 후보 보존·백엔드 저장 요청

**제안 주기**

학습 후보 조건 발생 시 이벤트 기반

**주기 선정 이유**

전체 스트림을 무조건 저장하지 않고 실제로 재검토 가치가 있는 사례를 이유와 함께 선별해야 저장·검수·학습 비용과 데이터 오염 위험을 줄일 수 있다.

**1차 선정 기술**

학습 후보 판정 인터페이스 + 원본 관측·객체·환경·버전 참조

**선정 이유**

어떤 사건과 데이터를 학습 가치가 있다고 판단할지는 비교 실험과 운영 결과로 검증 가능한 정책으로 두고 요구사항에서는 후보 사유와 추적 가능성을 보장한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-04, AI-S-08, AI-O-02/03, AI-L-02, AI-C-19

**담당**

진나영

---

#### AI-L-02 — 학습 후보 격리·품질 관리

**ID**

AI-L-02

**대분류**

학습·MLOps

**소분류**

학습 후보 격리·품질 관리

**요구사항 정의**

자동 수집되거나 AI가 생성한 후보 데이터와 사람이 확인했거나 검증 절차를 통과한 학습 데이터를 구분해 관리해야 한다. 검증되지 않은 VLM·open-vocabulary·기존 모델의 추론 결과나 환경 사전정보를 확정 정답으로 간주해 학습 데이터에 자동 승격해서는 안 된다. 후보 데이터는 원본 출처와 확인 상태를 유지하고 중복·품질·유효성 검토를 거쳐 학습에 사용할 수 있는 상태로 승격하거나 제외할 수 있어야 한다. 데이터 정제·저장 기능의 장애가 현재 운영 AI의 실행을 중단시켜서는 안 된다.

**전달 경로**

학습 후보 → 격리 상태 → 품질·중복·확인 상태 검토 → 검증된 학습 데이터 / 제외

**제안 주기**

후보 생성 후 학습 데이터 승격 전

**주기 선정 이유**

자동 추론 결과와 미검증 데이터를 바로 학습하면 오류가 반복·확대될 수 있으므로 운영 데이터와 검증된 학습 데이터 사이에 격리·검수 경계가 필요하다.

**1차 선정 기술**

후보/검증 데이터 상태 분리 + 데이터 provenance + 품질 검증 인터페이스

**선정 이유**

실제 데이터 저장소와 장기 보존 인프라는 백엔드 책임으로 두고 AI는 학습 사용 가능 상태와 필요한 품질 정보를 산출한다.

**연관분야**

백엔드

**연관점**

AI-L-01, AI-L-03/04, AI-O-03, AI-C-19

**담당**

진나영

---

#### AI-L-03 — 사용자 확인·의미 정보 반영

**ID**

AI-L-03

**대분류**

학습·MLOps

**소분류**

사용자 확인·의미 정보 반영

**요구사항 정의**

미확인 객체와 학습 후보에 대해 AI가 확보한 관측 이력, 추가 분석 결과와 의미 후보를 사용자 확인이 가능한 정보로 제공하고, 사용자가 확정·수정한 객체명, 속성, 관계 또는 기타 의미 정보를 원래 객체·관측·후보 데이터와 연결해 보존할 수 있어야 한다. AI·VLM이 제시한 후보와 사용자가 확인한 정보를 명확히 구분해야 하며 사용자 확인 결과는 현재 객체 상태에 반영할 수 있으나 모델 파라미터 변경과는 분리해야 한다. 실제 화면 구성과 사용자 상호작용 방식은 가시화 파트가 담당해야 한다.

**전달 경로**

미확인 객체·AI 의미 후보 → 가시화 사용자 확인 → 확인 결과 → 객체 상태·학습 후보

**제안 주기**

사용자 확인이 필요한 객체·학습 후보가 발생할 때

**주기 선정 이유**

사용자 피드백을 즉시 객체 의미 상태에는 반영할 수 있게 하되 모델 재학습과 분리해야 잘못된 즉시 학습 위험을 줄이고 확인된 지식을 빠르게 활용할 수 있다.

**1차 선정 기술**

AI 판단정보/후보 의미 제공 + 사용자 확인 결과 연계 인터페이스

**선정 이유**

AI 파트는 어떤 근거와 후보 의미를 제공할지 책임지고 가시화 파트는 이를 어떤 UI로 보여주고 입력받을지 결정한다.

**연관분야**

가시화, 백엔드

**연관점**

AI-S-04, AI-L-01/02/04/08, AI-C-19

**담당**

진나영

---

#### AI-L-04 — 학습 실행·전략 분리

**ID**

AI-L-04

**대분류**

학습·MLOps

**소분류**

학습 실행·전략 분리

**요구사항 정의**

검증된 학습 데이터로 모델·판단 정책·환경 지식 등 갱신 대상의 후보 버전을 생성할 수 있어야 하며 학습 방법과 실행 위치를 특정 알고리즘이나 특정 온디바이스·엣지·서버에 고정해서는 안 된다. 학습 대상과 전략은 독립된 기능으로 교체 가능해야 하고 실제 실행 위치는 데이터 접근성, 자원, 보안·통신 조건과 검증 가능한 실행환경을 기준으로 선택할 수 있어야 한다. 자원 제약이 큰 말단에서는 대규모 학습을 기본 기능으로 요구하지 않아야 하며 학습 기능의 장애가 운영 실행 기능에 전파되어서는 안 된다.

**전달 경로**

검증된 학습 데이터 → 교체 가능한 학습 기능 → 후보 모델·정책·지식 버전

**제안 주기**

학습 데이터가 준비되고 갱신 필요가 승인된 경우

**주기 선정 이유**

continual learning 방법과 적합한 실행 위치는 대상·데이터·자원에 따라 달라지므로 프레임워크가 하나의 학습 알고리즘이나 장비를 필수로 요구하면 확장성이 떨어진다.

**1차 선정 기술**

교체 가능한 Learning Provider + 실행 위치 선택 + 운영 실행과 격리

**선정 이유**

온디바이스는 기본적으로 추론·로컬 안전·짧은 후보 보존을 우선하고, 더 큰 학습은 엣지·서버 capability가 있을 때 선택적으로 수행할 수 있게 한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-L-02, AI-L-05, AI-B-01/04/08, AI-C-05/19

**담당**

진나영

---

#### AI-L-05 — 학습 결과 검증·기존 지식 보존

**ID**

AI-L-05

**대분류**

학습·MLOps

**소분류**

학습 결과 검증·기존 지식 보존

**요구사항 정의**

새 모델·판단 정책·환경 지식 후보를 운영에 적용하기 전에 새 데이터·환경에 대한 개선 여부와 기존에 검증된 객체·환경·업무에서의 성능 저하 여부를 함께 평가해야 한다. 품질뿐 아니라 지연, 메모리·연산 자원, 실행 안정성, 호환 가능한 모델·하드웨어·런타임 범위와 알려진 실패 조건을 검증하고 결과를 기존 정상 버전과 비교할 수 있어야 한다. 이전 지식 유지 방법이나 continual learning 알고리즘을 하나로 고정하지 않고 후보 전략을 동일 검증 조건에서 비교할 수 있어야 하며 검증 기준을 만족하지 못한 후보는 운영 적용 대상으로 승격해서는 안 된다.

**전달 경로**

후보 모델·정책·지식 → 신규/기존 검증 데이터·실행환경 평가 → 승인 가능 후보 / 거부

**제안 주기**

모든 학습 결과 생성 후 운영 적용 전

**주기 선정 이유**

새 지식만 개선하고 기존 지식을 잊거나 자원·지연 요구를 위반하는 학습 결과가 물리 시스템에 적용되는 것을 방지하려면 회귀·호환성 검증이 필수다.

**1차 선정 기술**

신규 성능 + 회귀/forgetting + 자원·호환성 검증 + 적용 Scope

**선정 이유**

어떤 continual learning 방법이 우수한지는 검증 결과로 선정하며 요구사항은 기존 지식 보존과 운영 조건 검증을 통과하도록 강제한다.

**연관분야**

백엔드, 하드웨어

**연관점**

AI-L-04, AI-L-06/07, AI-B-01/05, AI-O-01/03

**담당**

진나영

---

#### AI-L-06 — 관리자 판단 정보·적용 승인 요청

**ID**

AI-L-06

**대분류**

학습·MLOps

**소분류**

관리자 판단 정보·적용 승인 요청

**요구사항 정의**

자동 검증을 통과한 학습 결과를 실제 운영에 적용하기 전에 관리자가 변경의 필요성과 위험·효과를 판단할 수 있도록 학습 발생 이유, 사용 데이터의 출처·확인 상태와 대표 사례, 학습 대상·기존 버전, 검증 과정, 신규·기존 성능 변화, 자원·지연 변화, 알려진 실패 사례, 검증된 적용 범위와 미검증 범위, 복구 가능한 이전 정상 버전을 구조화된 승인 정보로 제공해야 한다. 검증을 통과하지 못한 후보는 승인 대상으로 올려서는 안 되며 실제 화면 구성과 승인 UI는 가시화 파트가 담당해야 한다.

**전달 경로**

자동 검증 결과·학습 lineage → 관리자 판단 정보 → 가시화 승인 UI → 승인 결과

**제안 주기**

자동 검증 PASS 후 운영 적용 전

**주기 선정 이유**

물리 시스템에서는 학습 결과를 자동 적용하기보다 객관적인 성능·위험·적용 범위를 관리자에게 제공해 책임 있는 최종 적용 판단을 가능하게 해야 한다.

**1차 선정 기술**

학습·검증·적용 Scope 요약 인터페이스 + 관리자 승인 상태 연계

**선정 이유**

AI는 판단에 필요한 정보를 산출하고 가시화는 표현·입력, 백엔드는 승인 주체·시간·대상 버전과 적용 범위의 authoritative 기록을 담당한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-L-05, AI-L-07, AI-O-03, AI-C-19

**담당**

진나영

---

#### AI-L-07 — 단계적 적용·운영 검증·롤백

**ID**

AI-L-07

**대분류**

학습·MLOps

**소분류**

단계적 적용·운영 검증·롤백

**요구사항 정의**

관리자 승인을 받은 새 모델·정책·지식은 검증된 적용 범위를 벗어나 바로 전체 운영에 적용하지 않고 운영 판단에 영향을 주지 않는 검증 모드 또는 제한된 대상에서 기존 정상 버전과 결과·자원·오류 상태를 비교할 수 있어야 한다. 운영 검증 기준을 충족한 경우에만 적용 범위를 확대할 수 있어야 하며 승인 조건을 벗어나거나 성능 회귀·호환성 문제가 확인되면 이전 정상 버전으로 복구할 수 있어야 한다. 학습·검증·배포 관리 기능의 장애가 이미 실행 중인 정상 AI 기능을 중단시켜서는 안 된다.

**전달 경로**

관리자 승인 후보 → 검증/제한 적용 → 운영 비교 → 승격 또는 last-known-good rollback

**제안 주기**

관리자 승인 후 새 버전 적용·승격·이상 발생 시

**주기 선정 이유**

오프라인 검증만으로 발견하지 못한 실제 입력·자원 조건의 회귀가 물리 시스템 전체에 즉시 전파되지 않도록 적용 범위를 단계적으로 확대해야 한다.

**1차 선정 기술**

단계적 적용 계약 + 기존 버전 병행 검증 + rollback 연계

**선정 이유**

Shadow·canary 등 구체 배포 방식은 실행환경의 provider 선택으로 두고 요구사항에서는 운영 영향 격리와 단계적 승격·복구를 보장한다.

**연관분야**

백엔드, 하드웨어, 가시화

**연관점**

AI-L-05/06, AI-B-01/05, AI-O-01/02/03, AI-C-19

**담당**

진나영

---

#### AI-L-08 — 학습·적용 계보 추적

**ID**

AI-L-08

**대분류**

학습·MLOps

**소분류**

학습·적용 계보 추적

**요구사항 정의**

학습에 사용된 데이터와 생성된 모델·판단 정책·환경 지식의 각 버전은 해당 학습이 필요해진 운영 사건, 관련 지속 객체와 원본 관측, 사용된 학습 데이터와 사용자 확인 상태, 학습 방법·설정, 생성된 후보 버전, 검증 결과, 관리자 승인 결과와 실제 적용 범위를 서로 연결해 추적할 수 있어야 한다. 운영 중 문제가 발생하면 현재 적용된 버전에서 학습·검증·승인 과정과 원본 데이터까지 가능한 범위에서 역추적할 수 있어야 하며, 저장소나 전송 기술 자체를 이 계보 규약의 필수 구현으로 고정해서는 안 된다.

**전달 경로**

운영 사건·객체·관측 → 학습 후보·데이터 → 학습 실행 → 검증·승인 → 적용 버전

**제안 주기**

학습 후보 생성부터 검증·승인·배포·롤백까지 각 상태 변화 시

**주기 선정 이유**

물리 시스템에서 적용 중인 모델·정책이 어떤 운영 사건과 데이터, 사용자 확인, 학습·검증·승인 과정을 거쳐 생성됐는지 역추적할 수 있어야 오류 원인 분석과 관리자 판단, 재현이 가능하다.

**1차 선정 기술**

버전·참조 기반 learning lineage 계약 + 데이터/모델/검증/승인 상관 정보

**선정 이유**

특정 DB·레지스트리·전송 기술을 고정하지 않고 각 단계의 식별자와 참조 관계를 유지해 기존 백엔드 저장·감사 인프라와 연결한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-L-01~07, AI-O-03, AI-C-01/03, AI-S-06

**담당**

진나영

---

## 6. AI 개발 에이전트의 구현 순서

1. 전체 요구사항을 먼저 읽고 공통 경계와 책임을 추출한다.
2. 요구사항 간 생산자·소비자 관계를 정리하되 변수명은 아직 확정하지 않는다.
3. Core/Optional capability와 provider/adapter 경계를 먼저 설계한다.
4. Capability Registry, 실행 호환성·자원·검증 프로파일, 배포 프로파일 구조를 설계한다.
5. 기능별 공통 인터페이스를 만든 뒤 현재 배포 기술을 provider 구현으로 연결한다.
6. 로컬 실행, 엣지 실행, 원격 실행 중 어느 위치에서도 동일한 상위 기능 계약을 사용할 수 있게 한다.
7. 정상 경로보다 먼저 선택 기능 부재, 네트워크 단절, provider 장애, 자원 부족, 갱신 실패의 동작을 명시한다.
8. 전체 기능이 구현된 뒤 실제 생산자·소비자 정보를 기반으로 공통 데이터 사전과 변수명을 한 번에 통일한다.
9. 각 요구사항 ID별로 구현 위치, 코드 모듈, 테스트 케이스와 충족 여부를 추적할 수 있게 한다.

## 7. 요구사항 추적 규칙

각 구현 단위와 테스트에는 관련 요구사항 ID를 남긴다.

예시:

```text
implements: AI-C-04, AI-C-10, AI-C-05
tests: adapter replacement, optional provider loss, registry fallback
```
