# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 0. 코드베이스 작업 가이드

§1 이후는 요구사항 정의서(51개 AI-* 요구사항)이며 구현 범위의 기준이다.
이 절은 실제 저장소에서 작업할 때 필요한 정보만 요약한다.

### 명령

Python 패키지는 `ai-framework/`에 있다. 모든 명령은 그 디렉터리에서 실행한다.

```bash
pip install -e ".[dev]"      # 개발 설치 (Python >= 3.10, jsonschema/numpy/opencv)
pytest -q                    # 전체 테스트
# 시스템 pytest 6.2.5 + anyio 플러그인 충돌로 collection이 깨지면:
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -q
pytest tests/test_selector.py::test_name   # 단일 테스트
PYTHONPATH=. python examples/demo.py          # 구현된 기능들의 런너블 데모
PYTHONPATH=. python examples/scenario_demo.py # 최종 시나리오 데모
```

빌드·lint 도구는 아직 설정되어 있지 않다.

### 구조와 흐름

핵심은 "무엇을 실행할 수 있는가"를 런타임에 정하는 3단 구조다. 상위 로직
(perception/decision/risk/reference)은 구체 기술을 절대 import하지 않는다.

1. `contracts/capability.py` — `CapabilityRequirement.evaluate(available)`이
   required/optional 선언을 `ACTIVE / DEGRADED / DISABLED`로 환산한다.
   optional 결손은 DEGRADED까지만 내려가고 DISABLED가 되지 않는다는 규칙이
   프레임워크 전체의 장애 격리 근거다(AI-C-11).
2. `contracts/profile.py` — `CompatibilityProfile`(required/preferred 태그 +
   `ResourceCost` + `priority`), `ResourceBudget`, `DeploymentProfile`.
   하드웨어·런타임 조건은 벤더 enum이 아니라 자유 문자열 태그다. 새 하드웨어는
   태그+프로파일 등록으로 추가하고 이 모듈을 수정하지 않는다.
3. `registry/capability_registry.py` — capability kind별 provider 등록부.
   `_local`(자기 노드, 항상 authoritative)과 `_remote`(중앙 레지스트리의 마지막
   스냅샷)를 분리해 중앙이 끊겨도 마지막 known-good로 계속 동작한다. 장치 최종
   가용성은 백엔드 판정을 **입력으로 소비**할 뿐 여기서 재판정하지 않는다.
4. `selection/selector.py` — 호환 후보 중 `(priority, compute_units)` 최소를
   고른다. 후보가 없으면 예외가 아니라 `SelectionResult(provider=None, reason=...)`을
   반환한다. 호출자는 이 None을 정상적인 축소 경로로 처리해야 한다.

`providers/adapters.py`의 Protocol들(Transport/Serializer/MediaSource/AIRuntime/
Control/Observability/NetworkOverlay/ModelDeployment)이 인프라 경계다. `providers/fakes.py`가
테스트·데모용 구현이며, MQTT/Kafka/OTel/K3s/Tailscale 같은 현재 배포 기술은
각 provider 뒤에 숨긴다.

기능 모듈은 요구사항 대분류를 그대로 따른다: `ondevice/`(AI-N), `edge/`(AI-E),
`decision/`(AI-D), `perception/`(AI-S, AI-E-01/04), `risk/`(AI-R),
`execution/`(AI-B), `observability/`(AI-O), `reference/`(AI-N-01 수직 슬라이스).

### 코드 작성 규약

- 모든 모듈·테스트 docstring 상단에 `implements: AI-X-NN` 형식으로 요구사항 ID를
  남긴다(§7). 기존 파일들이 이 형식을 지키고 있으니 그대로 따른다.
- 주석·docstring은 영문 설명 + 요구사항 인용은 원문 한국어를 섞어 쓰는 기존
  스타일을 유지한다.
- 변수·스키마 이름은 [ai-framework/ai_framework/contracts/data_dictionary.py](ai-framework/ai_framework/contracts/data_dictionary.py)의
  `AI-C-01` 공통 데이터 사전에 등록한 의미 단위로 통일한다. 새 이름이 필요하면
  먼저 사전에 의미·생산자·소비자·데이터 평면을 추가한다.
- 구현 상태는 [docs/ai/requirement-traceability.md](docs/ai/requirement-traceability.md)에
  요구사항 ID별로 기록되어 있다. 코드를 추가하면 이 표도 갱신한다.
  구조 설명은 [docs/ai/00-architecture.md](docs/ai/00-architecture.md).
- 작업 단위별 보고서는 `reports/YYYY-MM-DD_HHMM_주제.md` 형식으로 남긴다.

## 1. 문서 목적

이 파일은 AI·엣지 실행 프레임워크를 구현하는 AI 개발 에이전트를 위한 최상위 요구사항 문서다.
구현의 목표는 특정 AI 모델의 성능을 높이거나 특정 하드웨어·프로토콜·런타임에 최적화된 단일 애플리케이션을 만드는 것이 아니다.
목표는 서로 다른 센서, 카메라, 연산 하드웨어, AI 기능, 실행 위치, 전송 기술, 적용 도메인이 바뀌어도 핵심 업무 로직을 수정하지 않고 확장·교체·축소 운용할 수 있는 범용 프레임워크를 구현하는 것이다.

이 문서의 요구사항 ID가 구현 범위의 기준이다. 요구사항에 없는 기능을 임의로 추가하지 말고, 구현상 필수라고 판단되는 추가 기능은 먼저 `추가 요구사항 필요`로 보고한다.

## 2. 절대 준수 원칙

1. 특정 센서 제품, 카메라 종류, GPU/NPU 벤더, AI 모델 구조, 모델 파일 형식, 추론 런타임, 메시징 프로토콜, 직렬화 형식, 저장소, 오케스트레이터를 프레임워크 핵심 코드에 하드코딩하지 않는다.
2. 구체 기술은 현재 배포에서 선택된 provider 또는 adapter 구현일 뿐이다. 상위 인지·판단·위험 분석·실행관리 로직은 목적 수준의 공통 인터페이스만 사용한다.
3. `로봇`, `시설 감시`, `하천` 등 도메인명을 기준으로 핵심 코드에 분기문을 추가하지 않는다. 도메인 차이는 배포 프로파일, 입력 adapter, 활성 capability, 규칙, 실행 위치와 외부 제어 연결로 표현한다.
4. 각 기능은 필수 capability와 선택 capability를 구분한다. 선택 기능의 추가·제거·장애가 관련 없는 기능의 시작·실행·복구를 방해해서는 안 된다.
5. 현재 사용 가능한 기능과 자원을 런타임에 탐색하고, 요구 기능·안전·지연 조건을 만족하는 범위에서 필요한 자원만 사용한다.
6. 자원이 부족하거나 기능 제공자가 사라지면 우선순위가 낮은 선택 기능부터 축소하고 가능한 구성으로 재구성한다. 전체 시스템을 불필요하게 중단하지 않는다.
7. 새 센서·하드웨어·런타임·외부 서비스·AI 기능은 핵심 코드 수정이 아니라 adapter/provider/capability 등록으로 확장할 수 있어야 한다.
8. 요구사항 단계에서는 구체 변수명을 임의 확정하지 않는다. 각 요구사항이 필요로 하는 정보의 의미와 생산자·소비자를 먼저 구현한 뒤 전체 인터페이스 설계 단계에서 공통 데이터 사전으로 변수명을 통일한다.
9. 업무 데이터, 시스템 관측 데이터, 미디어를 구분한다. 물리 제어 명령은 업무 데이터에 포함하되 전달 보장·순서·결과 회신·책임 추적이 필요한 제어 데이터로 별도 정책을 적용한다.
10. 현재 배포에서 말단↔엣지 업무·제어·하트비트는 MQTT, 엣지↔서버 업무와 서버→엣지 제어는 서버 Kafka, metric·log·trace는 OpenTelemetry 기반 관측 경로, 영상 픽셀은 별도 미디어 경로를 사용한다. 구역 엣지와 서버 사이의 모든 구간은 공개 인터넷을 경유하지 않고 보안 오버레이 네트워크(현재 Tailscale) 위에서만 연결한다. 그러나 상위 AI 코드가 이 구현 기술의 API에 직접 의존해서는 안 된다.
11. 엣지에는 Kafka 서버를 설치하지 않는다. 엣지는 MQTT Broker와 서버 Kafka를 연결하는 양방향 Bridge를 둔다. 서버에는 MQTT Broker를 두지 않는다. 오케스트레이션은 예외로, 서버와 구역 엣지 **양쪽 모두** K3s를 두어 서버 자원·보안 경계도 동일한 실행관리 계약으로 운용한다. 서버 클러스터와 엣지 클러스터는 별개 클러스터이며 상위 코드는 어느 클러스터를 쓰는지 알 필요가 없다.
12. 말단은 경량 실행을 기본으로 한다. 브로커·수집기·중앙 저장소·오케스트레이터 제어면 같은 서버형 미들웨어를 기본 설치 대상으로 요구하지 않는다.
13. 카메라 자동 캘리브레이션은 말단에서 무거운 연산으로 수행하지 않는다. 필요한 영상은 미디어 경로로 엣지에 전달하고, 엣지에서 추정·검증한 보정 프로파일을 적용 대상에 배포한다.
14. 일반 metric은 엣지에 상세값을 유지하고 서버에는 요약할 수 있지만 장치 생사·치명 오류처럼 집계하면 안 되는 상태는 개별 상태로 유지한다.
15. 장치 최종 가용성은 백엔드가 업무 전송 상태와 관측 상태를 통합해 판정한다. AI Capability Registry가 이를 중복 구현하지 않는다.
16. 명령은 전달만으로 완료되지 않는다. 수신 여부, 최종 실행 성공·거부·실패 원인을 업무 결과로 회신하고, 처리 경로와 지연은 별도의 기술 trace로 추적한다.
17. Kafka는 장기 저장소가 아니다. 단기 버퍼·다중 소비자·단기 replay 용도로 사용하고, 장기 재현 데이터는 업무 저장소 또는 별도 아카이브 참조를 사용한다.
18. 운영 시스템은 폐쇄망을 전제로 한다. 실행에 필요한 이미지·모델·패키지·규칙은 내부 저장소에서 조달하고, 공개 인터넷에 대한 나가는 연결을 기능 동작의 전제로 두지 않는다. 외부 서비스 provider는 항상 선택 기능이며 사용할 수 없을 때 해당 기능만 비활성화한다.

## 3. 구현 시 금지 사항

- 요구사항에 없는 센서, 서버, GPU/NPU, 외부 API가 항상 존재한다고 가정하지 않는다.
- 특정 모델이 없으면 시스템을 시작하지 못하게 만들지 않는다.
- 특정 protocol client를 인지·판단 코드 안에서 직접 생성하지 않는다.
- 특정 도메인 전용 클래스를 복제하여 별도 프레임워크를 만들지 않는다.
- 선택 기능 장애를 전체 process liveness 실패로 취급하지 않는다.
- 장치 최종 availability를 AI 내부에서 독자적으로 재판정하지 않는다.
- 영상 픽셀을 MQTT/Kafka/OTLP 업무·관측 메시지에 직접 싣지 않는다.
- 일반 metric 요약에 장치 급사·치명 오류를 섞어 개별 의미를 잃게 하지 않는다.
- Kafka를 장기 데이터베이스처럼 사용하지 않는다.
- 현재 요구사항에서 정하지 않은 변수명·스키마명을 편의상 임의 확정하지 않는다.

## 4. 구현 결과물의 기본 구조

구현은 가능한 한 다음 경계를 유지한다.

```text
Domain / Application Logic
        │
        ├─ Perception / Decision / Risk / Safety
        │
Common Capability & Provider Interfaces
        │
        ├─ Input / Media Adapter
        ├─ AI Runtime Provider
        ├─ Transport Provider
        ├─ Serializer Provider
        ├─ Control / Orchestrator Provider
        ├─ Observability Provider
        ├─ Network Overlay Provider
        └─ Model Deployment Provider
        │
Capability Registry + Resource Profile + Deployment Profile
        │
Concrete Hardware / Sensor / Runtime / Protocol / Infrastructure
```

구현 상세가 이 구조와 다르더라도 요구사항의 의존성 분리, 기능 격리, provider 교체 가능성, 자원 최소화 원칙을 만족해야 한다.

## 5. 전체 요구사항

아래에는 요구사항 정의서의 51개 요구사항을 12개 필드 모두 포함하여 수록한다.
(원본 48개 + 서버·엣지 통합 실행관리, 폐쇄망 운용, 보안 오버레이 네트워크 3개 추가)

### 온디바이스

#### AI-N-01 — 로컬 안전 판단

**ID**

AI-N-01

**대분류**

온디바이스

**소분류**

로컬 안전 판단

**요구사항 정의**

이동형 에이전트는 외부 네트워크나 엣지 기능이 사용할 수 없는 경우에도 로컬 카메라와 현재 실행 가능한 영상 처리 기능만으로 접근·충돌 위험을 판단하고 최소 안전 상태를 제어계에 제공해야 한다. 객체 분류, 추적, 거리 추정 등 개별 기능은 안전 판단을 보조하는 선택 기능으로 취급하고 특정 모델이나 런타임을 필수조건으로 두어서는 안 된다. 사용 가능한 기능이 줄어들면 남은 영상 정보로 판단 수준을 단계적으로 낮추고, 영상 입력 자체가 사라지면 사전에 정의된 보수적 안전 상태로 전이해야 한다.

**전달 경로**

온보드 카메라 → 온디바이스

**제안 주기**

이동·작업 중 연속. 초기 5Hz 이상을 기준으로 시작하고 최종 최소 주기는 안전 응답시간 시험으로 확정

**주기 선정 이유**

최소 처리주기는 이동속도·제동거리·센서 취득·추론·제어 지연을 합산해 정해야 한다.

**1차 선정 기술**

로컬 영상 안전 판단 인터페이스 + 선택형 인지·추적·거리 기능 + 안전 fallback

**선정 이유**

로컬 안전은 특정 객체탐지 모델의 성능이 아니라 네트워크 단절과 선택 기능 부재에도 유지되는 실행 구조가 핵심이다. 현재 장비에 맞는 모델·런타임은 실행 프로파일에서 선택하고 요구사항에는 고정하지 않는다.

**연관분야**

하드웨어

**연관점**

HW-R-04, HW-C-06, BE-C-03, AI-C-03

**담당**

진나영

---

#### AI-N-02 — 환경·보정 설정 적용

**ID**

AI-N-02

**대분류**

온디바이스

**소분류**

환경·보정 설정 적용

**요구사항 정의**

온디바이스는 구역 엣지에서 제공하는 환경정보, 실행 설정, 보정정보와 실행에 필요한 외부 구성요소를 검증한 뒤 적용해야 한다. 최초 적용이나 기준 버전이 없는 경우에는 필요한 전체 구성을 받고, 이후에는 변경된 항목만 갱신할 수 있어야 한다. 갱신 실패나 검증 실패 시 기존 정상 구성을 유지하고, 외부 구성이 없어도 로컬 안전 기능은 계속 동작해야 한다. 구성의 크기나 종류에 따라 전송 방식이 달라질 수 있으므로 상위 기능이 특정 전송기술에 직접 의존해서는 안 된다.

**전달 경로**

구역 엣지 → 말단 온디바이스

**제안 주기**

구역 진입·임무·환경·보정·실행 구성 변경 시

**주기 선정 이유**

처음에는 실행에 필요한 전체 기준 구성을 제공하고 이후에는 변경분만 갱신해야 전송량과 재기동 비용을 줄일 수 있다.

**1차 선정 기술**

버전 기반 전체/변경분 구성 적용 인터페이스 + 전송 provider

**선정 이유**

현재 로봇 배포에서는 임무·서브태스크와 작은 설정은 말단↔엣지 업무 경로를 사용하고, 맵·경로·모델 등 큰 구성요소는 크기와 배포환경에 적합한 별도 배포 provider를 사용할 수 있게 둔다. 참고: 변경 데이터 전송 아키텍처 5-1.

**연관분야**

하드웨어, 백엔드

**연관점**

BE-A-05, HW-R-08, AI-E-03, AI-N-01

**담당**

진나영

---

### 감시·인지

#### AI-E-01 — 인지

**ID**

AI-E-01

**대분류**

감시·인지

**소분류**

인지

**요구사항 정의**

인지 기능은 카메라 종류, 고정·이동 여부, 실행 하드웨어, 모델 구조와 런타임에 따라 별도 업무 로직으로 나누지 않고 공통 인지 인터페이스를 사용해야 한다. 입력 영상 특성, 보정 수준, 사용 가능한 연산 자원과 필요한 출력 수준을 기준으로 현재 실행 가능한 인지 제공자를 선택해야 한다. 보정이나 추가 분석 기능이 없더라도 가능한 범위의 영상 좌표 기반 결과를 계속 제공하고, 새로운 카메라·모델·하드웨어가 추가되어도 상위 인지·판단 코드는 변경하지 않아야 한다.

**전달 경로**

로컬 카메라 또는 별도 미디어 경로 → 실행 가능한 인지 노드 → 후속 판단·백엔드

**제안 주기**

영상 도착 시 처리하되 실행 프로파일이 정한 최소 필요 주기로 샘플링

**주기 선정 이유**

입력 FPS와 요구 지연이 소스마다 달라 고정 주기보다 현재 실행 프로파일과 SLO에 맞추는 편이 불필요한 연산을 줄인다.

**1차 선정 기술**

공통 인지 인터페이스 + 교체 가능한 인지 provider + 실행 프로파일 선택

**선정 이유**

입력·카메라·하드웨어·실행환경 차이를 공통 인터페이스와 실행 프로파일에서 흡수해 동일 인지 로직을 재사용하는 것이 핵심이다. 현재 배포의 영상 픽셀은 별도 미디어 경로를 사용한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

HW-R-04, HW-S-06, AI-E-02, AI-E-03, AI-S-01, VZ-I-07

**담당**

진나영

---

### 엣지 노드

#### AI-E-02 — 카메라 자동 캘리브레이션

**ID**

AI-E-02

**대분류**

엣지 노드

**소분류**

카메라 자동 캘리브레이션

**요구사항 정의**

새로운 카메라가 등록되거나 기존 보정의 유효성이 낮아지면 말단은 캘리브레이션을 계산하지 않고 필요한 영상 샘플 또는 짧은 영상 구간을 엣지의 제어·AI 기능으로 전달해야 한다. 영상 샘플은 업무 메시지 경로가 아니라 기존 미디어 경로를 사용해야 한다. 엣지는 사전 보정 프로파일이나 특정 보정판을 필수로 요구하지 않고 영상 자체에서 카메라 내부 기하와 렌즈 왜곡을 자동 추정해야 한다. 자동 추정이 불안정하면 보정값을 배포하지 않고 영상 좌표 기반 인지를 유지해야 한다.

**전달 경로**

말단 카메라 → 별도 미디어 경로 → 구역 엣지 자동 보정 기능

**제안 주기**

새 카메라 등록, 영상 해상도·crop·zoom 변경, 장착 변화 의심, 보정 품질 저하 시

**주기 선정 이유**

캘리브레이션은 매 프레임 수행할 필요가 없고 카메라 기하가 바뀌거나 기존 보정의 일관성이 떨어질 때만 다시 수행하는 것이 자원 효율적이다.

**1차 선정 기술**

엣지 자동 보정 서비스 + 단일 영상 초기 추정 + 영상 시퀀스 보완 + 품질 검증

**선정 이유**

무거운 보정 연산을 저사양 말단에 두지 않고 구역 엣지에서 수행하며, 영상 데이터는 전송 아키텍처의 별도 미디어 경로를 사용한다. 자동 보정 자체는 AnyCalib의 단일 영상 model-agnostic 추정, DroidCalib의 영상 시퀀스 self-calibration, GeoCalib의 불확실도 검증 개념만 참고한다. 전송 구조 참고: 프로젝트 문서 data_transport_architecture_v2.docx §3~§5.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-E-01, AI-E-03, AI-C-02, AI-C-08

**담당**

진나영

---

#### AI-E-03 — 보정 프로파일 생성·배포

**ID**

AI-E-03

**대분류**

엣지 노드

**소분류**

보정 프로파일 생성·배포

**요구사항 정의**

엣지는 자동 캘리브레이션 결과를 인지 기능이 재사용할 수 있는 보정 프로파일로 생성하고 적용 가능한 영상 조건, 추정 품질, 유효 여부, 생성 시점과 버전을 함께 관리해야 한다. 새 프로파일은 검증을 통과한 경우에만 적용 대상에 배포하고, 검증 실패 시 기존 정상 프로파일을 유지해야 한다. 말단에 전달되는 프로파일은 작은 설정 데이터이므로 현재 배포에서는 말단↔엣지 업무·제어 경로를 사용하고, 엣지 내부 인지는 로컬 설정 경로로 적용할 수 있어야 한다.

**전달 경로**

구역 엣지 보정 기능 → 엣지 인지 / 말단 온디바이스

**제안 주기**

새 보정 결과가 검증을 통과했을 때 1회 배포

**주기 선정 이유**

온디바이스에서 매번 보정을 계산하지 않고 검증된 결과만 교체해야 연산량과 잘못된 보정 적용 위험을 줄일 수 있다.

**1차 선정 기술**

버전 관리 보정 프로파일 + 엣지 로컬 적용 + 현재 말단 MQTT 설정 전달

**선정 이유**

영상 원본은 미디어 경로, 생성된 작은 프로파일은 업무·제어 경로로 분리하면 대용량 영상과 설정 메시지가 같은 채널을 경쟁하지 않는다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §4, §5-1.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-N-02, AI-E-01, AI-E-02, AI-C-02

**담당**

진나영

---

#### AI-E-04 — 선택형 보조 기능 실행

**ID**

AI-E-04

**대분류**

엣지 노드

**소분류**

선택형 보조 기능 실행

**요구사항 정의**

기본 기능만으로 판단에 필요한 정보가 부족한 경우 현재 등록되어 실제 실행 가능한 보조 기능 중 필요한 기능만 선택적으로 실행해야 한다. 추가 인지, 거리·기하 추정, 미확인 대상 분석, 외부 AI 서비스 등 보조 기능의 종류와 구현 방식은 고정하지 않고 기능 등록 정보를 통해 탐색해야 한다. 보조 기능이 없거나 실패해도 기본 기능은 유지하고, 필요한 근거가 확보되면 고비용 보조 기능은 즉시 중단해야 한다.

**전달 경로**

기본 인지 결과 → 제어·엣지 노드 추가 분석 → 후속 판단

**제안 주기**

기본 인지에서 근거 부족이 발생한 경우에만

**주기 선정 이유**

고비용 분석은 상시 실행보다 필요한 경우에만 활성화해야 지연과 자원 사용을 줄일 수 있다.

**1차 선정 기술**

가용 기능 탐색 + 선택형 보조 provider 실행 + 필요 충족 시 즉시 해제

**선정 이유**

특정 고성능 모델을 상시 실행하는 대신 필요한 시점에 사용 가능한 기능만 호출해 자원 사용과 기능 종속성을 줄인다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-E-01, AI-S-05, AI-S-03, AI-B-06

**담당**

진나영

---

### 의사결정

#### AI-D-01 — 구역별 서브태스크 생성

**ID**

AI-D-01

**대분류**

의사결정

**소분류**

구역별 서브태스크 생성

**요구사항 정의**

백엔드가 하달한 메인 임무와 현재 구역에서 허용되는 행동·금지조건·로봇 상태를 이용해 수행 가능한 서브태스크를 생성해야 한다. 계획 생성 방식은 특정 LLM이나 계획 라이브러리를 필수로 하지 않고 검증 가능한 행동 단위와 조건 표현을 사용해야 한다. 환경 사전정보나 추가 인지 결과가 없으면 현재 확인 가능한 정보 범위에서만 계획을 생성해야 한다.

**전달 경로**

백엔드 → 구역 엣지 AI

**제안 주기**

메인 임무 수신 또는 해당 구역 재생성 요청 시

**주기 선정 이유**

전역 임무는 백엔드가 결정하고 엣지 AI는 자기 구역의 실행 가능한 세부 행동만 생성한다.

**1차 선정 기술**

제약 기반 서브태스크 생성 인터페이스 + 행동 템플릿·상태관리 provider

**선정 이유**

서브태스크 생성 방식은 교체 가능하게 두고, 출력 행동과 사전조건을 별도 검증할 수 있어야 한다.

**연관분야**

백엔드

**연관점**

BE-A-03, HW-R-03, AI-D-02, AI-D-04

**담당**

진나영

---

#### AI-D-02 — 서브태스크 검증

**ID**

AI-D-02

**대분류**

의사결정

**소분류**

서브태스크 검증

**요구사항 정의**

생성된 서브태스크는 구조, 구역 경계, 금지조건, 행동 순서, 자원 조건, 물리적 수행 가능성, 사전조건과 필요한 근거를 실행 전에 검증해야 한다. 검증 기능은 계획 생성 기능과 분리해 동일 조건에서 재현 가능한 결과를 제공해야 한다. 확인할 수 없는 필수 조건이 있으면 해당 행동만 실행 불가로 표시하고 필요한 추가 근거를 요청해야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

서브태스크 생성·변경 직후

**주기 선정 이유**

생성 결과를 바로 실행하지 않고 동일 입력에 같은 결과를 내는 검증 절차를 거쳐 실행 오류를 줄인다.

**1차 선정 기술**

JSON Schema + rule/constraint validator

**선정 이유**

구조 검증과 실행 조건 검증을 생성 모델에서 분리해 결과를 재현 가능하게 한다. 참고 근거는 JSON Schema Draft 2020-12 (https://json-schema.org/draft/2020-12); BehaviorTree.CPP (https://github.com/BehaviorTree/BehaviorTree.CPP)이다.

**연관분야**

백엔드

**연관점**

AI-D-01, AI-D-03, BE-X-04, VZ-U-07

**담당**

진나영

---

#### AI-D-03 — 의사결정 보조 정보 요청

**ID**

AI-D-03

**대분류**

의사결정

**소분류**

의사결정 보조 정보 요청

**요구사항 정의**

의사결정에 필요한 근거가 부족하거나 서로 충돌하면 현재 실제로 사용할 수 있는 정보원 중 필요한 항목만 선택해 요청해야 한다. 최신 추적 결과, 다른 관측 소스, 환경 사전정보, 추가 인지 결과 등은 모두 선택 정보로 취급하고 특정 정보원이 항상 존재한다고 가정해서는 안 된다. 추가 정보가 없으면 확인 가능한 범위까지만 판단하고 부족한 근거를 명시해야 한다.

**전달 경로**

엣지 의사결정 → AI-S-05·백엔드/DT → 엣지 의사결정

**제안 주기**

AI-D-02에서 근거 부족·충돌 발생 시 이벤트 기반

**주기 선정 이유**

인지·환경 인프라가 이미 가진 정보를 계획 검증에도 재사용하면 불충분한 관측을 전제로 한 잘못된 서브태스크 생성을 줄일 수 있다.

**1차 선정 기술**

가용 정보원 기반 의사결정 보조 요청

**선정 이유**

계획 품질을 높이기 위해 인프라 정보를 활용하되 특정 카메라·AI 기능·DT 정보가 항상 존재한다는 전제를 두지 않는다. 참고 근거는 BehaviorTree.CPP (https://github.com/BehaviorTree/BehaviorTree.CPP); 팀 공통 규약: BE-A-05이다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-05, AI-E-02, BE-C-01

**담당**

진나영

---

#### AI-D-04 — 서브태스크 재생성 판단

**ID**

AI-D-04

**대분류**

의사결정

**소분류**

서브태스크 재생성 판단

**요구사항 정의**

현재 서브태스크의 사전조건이 실제로 깨졌을 때만 해당 구역의 서브태스크를 다시 생성해야 한다. 경로 차단, 대상 소실, 근거 만료처럼 실행 가능성에 영향을 주는 변화와 단순 관측 변화는 구분해야 한다. 긴급 충돌 회피는 온디바이스 안전 기능이 담당하고 메인 임무 자체의 변경은 백엔드에 요청해야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

현재 서브태스크의 전제조건을 무효화하는 이벤트 발생 시

**주기 선정 이유**

모든 환경 변화에 재계획하지 않고 실제 수행 조건이 깨진 경우에만 재생성한다.

**1차 선정 기술**

사전조건·유효성 기반 재생성 규칙 + 상태관리

**선정 이유**

환경 변화가 실제 실행 가능성에 영향을 줄 때만 재생성해 불필요한 계획 반복을 줄이고, 재생성 규칙은 교체 가능한 정책으로 둔다.

**연관분야**

백엔드

**연관점**

AI-N-01, AI-D-01, AI-D-02, AI-D-03, BE-A-03

**담당**

진나영

---

### 감시·인지

#### AI-S-01 — 객체 추적

**ID**

AI-S-01

**대분류**

감시·인지

**소분류**

객체 추적

**요구사항 정의**

연속된 인지 결과가 제공되면 동일 객체의 식별 상태와 위치 변화, 이동 방향, 최근 관측 이력을 유지할 수 있어야 한다. 추적 기능은 특정 알고리즘에 의존하지 않고 인지 결과와 교체 가능한 추적 기능 사이의 공통 인터페이스를 사용해야 한다. 추적 기능이 없거나 실패하면 프레임별 인지 결과만 유지해야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

인지 결과 갱신 시

**주기 선정 이유**

기본 추적은 가볍게 유지하고 필요한 상황에만 추가 특징 추출을 사용해 처리비용을 제한한다.

**1차 선정 기술**

교체 가능한 객체 추적 provider + 공통 추적 인터페이스

**선정 이유**

추적 알고리즘 자체를 프레임워크에 고정하지 않고 입력·출력 계약과 요구 지연을 만족하는 provider를 실행 프로파일에서 선택한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-E-01, AI-E-04, AI-S-02, VZ-I-09

**담당**

진나영

---

#### AI-S-02 — 다중 관측 객체 연계

**ID**

AI-S-02

**대분류**

감시·인지

**소분류**

다중 관측 객체 연계

**요구사항 정의**

서로 다른 관측 소스에서 동일 객체가 보이는 경우 시간·공간·외형 등 사용 가능한 근거를 이용해 동일 객체 후보를 연계할 수 있어야 한다. 복수 카메라, ReID, 전역 좌표, 정밀 시간 동기화는 모두 선택 기능이며 하나라도 없다고 전체 추적을 중단해서는 안 된다. 필요한 근거가 부족하면 관측 소스별 추적을 독립적으로 유지해야 한다.

**전달 경로**

카메라별 track → 선택적 연계 모듈 → 서버

**제안 주기**

복수 카메라·연계 capability가 있을 때 track 갱신/경계 이동 시

**주기 선정 이유**

다중 카메라가 있는 환경에서는 동일 객체 중복을 줄이되 단일 카메라 환경에서도 전체 인지 시스템이 그대로 동작하도록 한다.

**1차 선정 기술**

시간·공간·외형 근거를 이용한 선택적 다중 관측 연계

**선정 이유**

ReID와 복수 카메라는 품질 향상용 선택 기능으로 두고 단일 카메라 환경을 정상 구성으로 허용한다. 참고 근거는 FastReID (https://github.com/JDAI-CV/fast-reid); Assigning Pods to Nodes (https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)이다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, DT-01, VZ-I-09

**담당**

진나영

---

#### AI-S-03 — 불확실성·근거 충분도 평가

**ID**

AI-S-03

**대분류**

감시·인지

**소분류**

불확실성·근거 충분도 평가

**요구사항 정의**

인지·추적·판단 결과는 모델의 신뢰도와 현재 판단에 사용할 수 있는 근거의 충분도를 구분해 제공해야 한다. 신뢰도 보정이나 다중 소스 일치도 평가 기능이 있을 때만 추가로 적용하고, 없으면 원본 신뢰도와 근거 부족 상태를 명확히 구분해야 한다. 충분한 근거가 없는 결과를 확정 상태로 승격해서는 안 된다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

인지·추적 결과 생성 시, 보정 기준은 모델·검증 데이터 변경 시 갱신

**주기 선정 이유**

모델 자체의 불확실성과 관측 정보 부족을 구분해야 후속 조치를 다르게 적용할 수 있다.

**1차 선정 기술**

공통 불확실성·근거 평가 인터페이스 + 선택형 보정 provider

**선정 이유**

특정 calibration 알고리즘을 필수로 하지 않고 원본 신뢰도와 근거 충분도를 공통적으로 표현할 수 있게 하는 것이 핵심이다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, AI-R-02, AI-R-03, VZ-I-09

**담당**

진나영

---

#### AI-S-04 — 미확인 객체 후보 관리

**ID**

AI-S-04

**대분류**

감시·인지

**소분류**

미확인 객체 후보 관리

**요구사항 정의**

기존 인지 결과만으로 분류를 확정하기 어려운 객체는 미확인 상태로 유지하고 이후 추가 분석과 사용자 확인에 필요한 원본 관측과 이력을 보존해야 한다. open-vocabulary 모델이나 VLM이 없으면 기존 클래스에 강제 매핑하지 않아야 한다. 사용자 확인이나 추가 근거가 확보된 경우에만 분류 상태를 갱신해야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

미확인 조건 발생 시 즉시, 동일 track의 반복 후보는 누적·병합

**주기 선정 이유**

미확인 객체를 기존 클래스에 강제 배정하지 않고 후속 분석이 가능한 상태로 유지한다.

**1차 선정 기술**

미확인 상태 관리 + 선택형 추가 분류 provider

**선정 이유**

특정 open-vocabulary 모델이 없어도 미확인 상태를 유지할 수 있어야 하며 추가 분류 기능은 선택 provider로만 사용한다.

**연관분야**

백엔드, 가시화

**연관점**

AI-S-01, AI-S-03, AI-S-05

**담당**

진나영

---

#### AI-S-05 — 추가 정보 선택

**ID**

AI-S-05

**대분류**

감시·인지

**소분류**

추가 정보 선택

**요구사항 정의**

미확인 또는 근거 부족 상황에서는 현재 사용 가능한 정보·분석 기능 가운데 판단 개선 효과가 큰 항목만 선택해 실행해야 한다. 다른 관측 소스, 추적 이력, 깊이 추정, ReID, open-vocabulary 인지, VLM, 재관측 등은 모두 선택 후보이며 없는 기능을 요청해서는 안 된다. 요구 근거 수준을 만족하면 추가 분석을 중단해 연산과 네트워크 사용을 줄여야 한다.

**전달 경로**

엣지 노드 ↔ 서버

**제안 주기**

AI-S-03/04의 정보 부족 발생 시, 추가 정보 수신 후 재평가

**주기 선정 이유**

모든 센서와 모델을 동시에 실행하지 않고 필요한 분석만 단계적으로 수행해 처리비용과 지연을 줄인다.

**1차 선정 기술**

가용 capability 필터 + 규칙 기반 추가정보 점수

**선정 이유**

다른 관측 소스, 추가 분류, 거리·기하 추정 등은 모두 선택 자원으로 취급하고 현재 사용할 수 있는 기능만 조합한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-04, AI-D-03, AI-S-03/04

**담당**

진나영

---

### 위험도

#### AI-R-01 — 위험 분석 상태 관리

**ID**

AI-R-01

**대분류**

위험도

**소분류**

위험 분석 상태 관리

**요구사항 정의**

위험 관련 관측이나 이벤트가 존재하는 배치에서만 위험 분석 상태를 활성화하고 평시·관찰·경보·복구 등 상태 전이를 관리해야 한다. 특정 센서 종류나 하천 도메인을 프레임워크의 필수 입력으로 고정하지 않고 현재 등록된 관측과 규칙만 사용해야 한다. 위험 입력이 없으면 위험 분석 기능만 비활성화하고 다른 기능은 계속 동작해야 한다.

**전달 경로**

말단 업무 관측 → 구역 엣지 위험 분석

**제안 주기**

환경 이벤트 발생 시 상태 전이

**주기 선정 이유**

센서의 1차 이벤트 검출과 AI 분석 수준 관리를 분리해 기능 중복을 줄인다.

**1차 선정 기술**

가용 sensor/event 기반 FSM

**선정 이유**

위험 분석에 쓰는 센서 값·상태는 시스템 자기관측 데이터가 아니라 업무 데이터이므로 현재 말단↔엣지 MQTT 업무 경로에서 소비한다. 특정 센서 종류는 프레임워크 필수조건으로 두지 않는다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §4, §5-1~§5-3.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-S-02, HW-S-03, BE-C-01, VZ-I-08

**담당**

진나영

---

#### AI-R-02 — 위험도·근거 상태 산정

**ID**

AI-R-02

**대분류**

위험도

**소분류**

위험도·근거 상태 산정

**요구사항 정의**

현재 사용 가능한 센서·영상·외부 이벤트만 이용해 위험 수준과 판단 근거의 충분도를 별도로 산정해야 한다. 존재하지 않는 입력을 임의 생성하거나 보간해 확정 판단에 사용해서는 안 된다. 저비용 규칙·통계로 요구 성능을 만족하면 더 복잡한 시계열 모델을 실행하지 않아야 한다.

**전달 경로**

엣지 노드 → 서버

**제안 주기**

WATCH/ALERT에서 센서 갱신 주기에 맞춰 수행, 주요 상태 변화 시 즉시

**주기 선정 이유**

실제 위험과 정보 부족을 분리해야 잘못된 경보와 추가 관측 필요를 구분할 수 있다.

**1차 선정 기술**

교체 가능한 위험 규칙·시계열 분석 provider + 근거 충분도 평가

**선정 이유**

위험 산정 방식은 센서 종류와 도메인에 따라 교체할 수 있어야 하며, 없는 입력을 전제로 한 고정 알고리즘을 두지 않는다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

HW-S-03, BE-S-07, VZ-I-08, AI-R-01, AI-R-04

**담당**

진나영

---

#### AI-R-03 — 위험 판단 결과·권고 출력

**ID**

AI-R-03

**대분류**

위험도

**소분류**

위험 판단 결과·권고 출력

**요구사항 정의**

위험 판단 결과에는 위험 상태, 위험 정도, 근거 충분도, 사용한 근거, 적용한 모델·규칙 버전과 권고 조치를 포함해야 한다. 판단 로직은 특정 메시징·직렬화·저장 기술에 직접 의존하지 않아야 한다. 실제 액추에이터 명령 생성과 물리 동작 확인은 백엔드·하드웨어 파트가 담당해야 한다.

**전달 경로**

위험 분석 결과 → 업무 데이터 경로 → 백엔드

**제안 주기**

위험 판단 또는 권고 변화 시

**주기 선정 이유**

AI 판단과 실제 시설 제어를 분리해 제어 책임과 감사 흐름을 명확히 한다.

**1차 선정 기술**

위험 Domain Object + Serializer/Transport provider

**선정 이유**

위험 판정 결과는 CPU·지연 같은 시스템 관측치가 아니라 실제 업무 판단 결과이므로 업무 데이터 평면으로 전달한다. 엣지에서 서버로 올라가는 현재 업무 데이터 간선은 Kafka이며, 실제 물리 제어 명령은 백엔드가 별도로 생성한다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §4, §6-1, §6-6.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-A-04, BE-X-01, VZ-I-08

**담당**

진나영

---

#### AI-R-04 — 관측·분석 수준 조정 요청

**ID**

AI-R-04

**대분류**

위험도

**소분류**

관측·분석 수준 조정 요청

**요구사항 정의**

위험 수준과 근거 충분도에 따라 현재 사용할 수 있는 관측·분석 기능 가운데 필요한 수준만 선택적으로 활성화하도록 요청해야 한다. 존재하지 않는 센서나 실행할 수 없는 모델을 요청해서는 안 된다. 위험이 낮아지거나 근거가 충분해지면 고주기 관측과 고비용 분석을 다시 낮춰야 한다.

**전달 경로**

AI 위험 모듈 → 엣지 실행환경

**제안 주기**

위험·근거 상태 변화 시 이벤트 기반

**주기 선정 이유**

위험하거나 정보가 부족한 경우에만 고주기 수집·추가 분석을 활성화해 평상시 자원 사용을 줄인다.

**1차 선정 기술**

capability-aware FSM + optional observation request

**선정 이유**

적응형 관측 역시 존재하는 기능만 선택해 특정 센서 추가를 시스템 전제로 만들지 않는다. 참고 근거는 MQTT Version 5.0 (https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.html); K3s Documentation (https://docs.k3s.io)이다.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-S-04, AI-B-05, AI-R-01, AI-R-02, AI-S-05

**담당**

진나영

---

### 자원·배포

#### AI-B-01 — 실행 호환성·자원 프로파일 관리

**ID**

AI-B-01

**대분류**

자원·배포

**소분류**

실행 호환성·자원 프로파일 관리

**요구사항 정의**

동일 기능을 제공하는 서로 다른 하드웨어, 실행 위치, 모델·알고리즘, 런타임 조합이 어떤 환경에서 실행 가능한지와 필요한 연산·메모리·지연 특성을 공통 프로파일로 관리해야 한다. 이 프로파일은 현재 노드에서 실행 가능한 구성을 판단하고 자원 예산을 넘지 않는 조합을 선택하기 위해 사용해야 한다. 새로운 하드웨어나 실행 방식이 추가되면 동일한 형식으로 프로파일을 등록할 수 있어야 한다.

**전달 경로**

온디바이스·엣지 노드 → 엣지 관리환경

**제안 주기**

하드웨어·런타임·구성 또는 실행 환경이 변경될 때 갱신

**주기 선정 이유**

같은 기능도 실행 하드웨어와 런타임에 따라 자원 사용과 지연이 달라지므로 현재 환경에서 실행 가능한 구성을 자동 선택하려면 호환성과 자원 특성이 필요하다.

**1차 선정 기술**

기능별 호환성·자원 프로파일 + 실행 가능 조건

**선정 이유**

모델 architecture나 특정 가속기를 고정하는 것이 아니라 기능 제공자가 자신의 실행 조건과 자원 특성을 등록하도록 한다.

**연관분야**

하드웨어

**연관점**

HW-R-10, HW-C-03, AI-B-06

**담당**

진나영

---

#### AI-B-02 — 실행환경 패키징·재현성 관리

**ID**

AI-B-02

**대분류**

자원·배포

**소분류**

실행환경 패키징·재현성 관리

**요구사항 정의**

각 기능은 동일한 시작·설정·상태 확인 규약으로 독립 실행할 수 있어야 하며 운영 배포를 위해 필요하면 컨테이너 등 선택한 방식으로 패키징할 수 있어야 한다. 애플리케이션 기능이 특정 컨테이너 엔진이나 오케스트레이터를 직접 요구해서는 안 된다. 개발·복구 환경에서도 동일 기능을 단독 실행할 수 있어야 한다.

**전달 경로**

개발환경 → 패키징 저장소 → 실행 노드

**제안 주기**

기능·의존성·모델 버전 변경 시 이미지 생성

**주기 선정 이유**

실행 환경과 의존성을 컨테이너에 고정하면 노드별 환경 차이로 인한 오류를 줄이고 동일 기능을 여러 장치에 반복 배포할 수 있다.

**1차 선정 기술**

독립 실행 계약 + OCI 컨테이너 패키징을 현재 운영 후보로 사용

**선정 이유**

K3s는 배포·관리 provider로 사용하되 실제 기능 로직은 K3s 없이도 독립 실행 가능하게 유지한다. 참고 근거는 K3s Documentation (https://docs.k3s.io)이다.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-03, AI-C-04, 실행 이미지 버전, 설정 버전

**담당**

진나영

---

#### AI-B-03 — 실행 제어

**ID**

AI-B-03

**대분류**

자원·배포

**소분류**

실행 제어

**요구사항 정의**

기능의 시작·중지·재시작·설정 변경과 실행 상태 확인은 공통 실행 제어 인터페이스로 요청해야 한다. 실행 제어 요청을 받은 기능은 수신 여부와 최종 실행 결과를 반환하고, 실행할 수 없는 경우에는 거부 또는 실패 원인을 함께 반환해야 한다. 업무상 명령 이력·책임 추적과 기술적 처리 경로·지연 추적은 서로 다른 목적으로 관리해야 하며 하나의 기록으로 합쳐서는 안 된다. AI 기능은 현재 전송·오케스트레이션 기술의 API를 직접 호출하지 않고 제어 provider를 통해 동작해야 한다.

**전달 경로**

백엔드 → 명령 경로 → 구역 엣지 실행관리 → 필요 시 말단 → 실행 결과 회신

**제안 주기**

제어 명령 발생 시 이벤트 기반

**주기 선정 이유**

물리 시스템 제어는 명령 전달 여부뿐 아니라 실제 실행 성공·거부·실패 원인을 확인할 수 있어야 하며, 책임 추적과 기술 성능 추적은 목적이 달라 분리해야 한다.

**1차 선정 기술**

공통 실행 제어·결과 인터페이스 + 현재 Kafka→Bridge→MQTT 경로 + OTel trace

**선정 이유**

현재 명령 경로는 서버→엣지 Kafka(보안 오버레이 위), 엣지에서 브릿지 변환, 엣지→말단 MQTT이다. 거부·실패 원인은 업무 결과로 회신하고, 어디서 지연·단절됐는지는 관측 trace에서 확인한다. 참고: 변경 데이터 전송 아키텍처 5-6~5-7, 6-6.

**연관분야**

백엔드

**연관점**

BE-X-01, BE-X-06, AI-B-05

**담당**

진나영

---

#### AI-B-04 — 실행 노드 자원 등록·배치

**ID**

AI-B-04

**대분류**

자원·배포

**소분류**

실행 노드 자원 등록·배치

**요구사항 정의**

각 실행 노드는 현재 제공 가능한 연산 자원, 아키텍처, 가속기, 센서 연결, 런타임과 네트워크 특성을 등록해야 한다. 기능 배치 시 반드시 필요한 조건과 있으면 성능을 높이는 선호 조건을 구분해 현재 환경에 맞는 노드를 선택해야 한다. 선호 자원이 없으면 호환 가능한 일반 자원으로 대체하고 필수 조건을 만족하는 노드가 없으면 해당 기능만 미배치 상태로 유지해야 한다.

**전달 경로**

구역 엣지 실행관리 → 등록된 실행 대상

**제안 주기**

노드 등록·구역 변경·하드웨어 변경 시

**주기 선정 이유**

논리적인 노드 특성과 실제 자원을 기준으로 배치해야 서로 다른 장치에서도 동일 배포 규칙을 사용할 수 있다.

**1차 선정 기술**

기능·자원 등록 + required/preferred 배치 조건

**선정 이유**

현재 아키텍처에서 K3s 제어 기능은 구역 엣지와 서버 양쪽에 위치하며, 두 클러스터는 보안 오버레이 네트워크로만 연결된다. 어떤 말단이나 엣지 프로세스가 실제 worker가 되는지는 배포 구성으로 결정하고 요구사항에서 특정 하드웨어를 고정하지 않는다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §3.

**연관분야**

하드웨어, 백엔드

**연관점**

BE-C-01, BE-C-02, BE-T-04, VZ-I-01

**담당**

진나영

---

#### AI-B-05 — 실행 기능 배포·생명주기 관리

**ID**

AI-B-05

**대분류**

자원·배포

**소분류**

실행 기능 배포·생명주기 관리

**요구사항 정의**

배포 관리 기능이 있는 환경에서는 각 기능을 독립 실행 단위로 배포·기동·중지·업데이트·복구할 수 있어야 한다. 실행에 필요한 설정·규칙·맵·모델·보정정보 등 외부 구성요소도 버전과 적용 대상을 관리하고, 최초 전체 배포 후 변경분만 갱신할 수 있어야 한다. 선택 기능의 배치·갱신 실패가 핵심 기능을 종료시키거나 연쇄 재시작하게 해서는 안 되며, 오케스트레이터가 일시적으로 사용할 수 없어도 이미 실행 중인 핵심 기능은 계속 동작해야 한다.

**전달 경로**

구역 엣지 실행관리 → 등록된 실행 대상

**제안 주기**

기능·설정·외부 구성요소 배포/변경 및 장애 발생 시

**주기 선정 이유**

실행 코드뿐 아니라 맵·규칙·보정·모델 등 실행에 필요한 구성도 일관된 생명주기와 rollback 경계를 가져야 확장이 쉬워진다.

**1차 선정 기술**

독립 실행 lifecycle + 버전 기반 구성 배포 + rollback + 현재 K3s/Helm provider

**선정 이유**

구성요소의 크기와 종류에 따라 실제 배포 경로는 달라질 수 있으므로 K3s·MQTT 등 구체 방식은 provider에서 선택한다.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-03, BE-X-06, AI-B-02, AI-B-07

**담당**

진나영

---

#### AI-B-06 — 자원 상태 기반 실행 재구성

**ID**

AI-B-06

**대분류**

자원·배포

**소분류**

자원 상태 기반 실행 재구성

**요구사항 정의**

운영 중 자원 부족, 과열, 지연 증가, 실행 노드 또는 기능 제공자 소실이 발생하면 현재 사용 가능한 실행 후보를 다시 평가해 기능의 실행 위치·처리주기·구성 수준을 조정해야 한다. 요구 기능을 유지하면서 현재 환경에서 필요한 자원만 사용하도록 재구성해야 하며, 대체 후보가 없으면 우선순위가 낮은 선택 기능부터 축소하고 핵심 기능은 유지해야 한다.

**전달 경로**

AI 관측 → 실행 관리환경·AI 실행환경

**제안 주기**

운영 중 주기 평가, 자원 포화·지연 증가 시 즉시 재평가

**주기 선정 이유**

자원 상태가 변해도 고정 설정을 유지하면 지연 증가와 처리 누락이 발생할 수 있어 검증된 범위 안에서 실행 구성을 조정해야 한다.

**1차 선정 기술**

capability/resource-aware 재구성 + 단계적 기능 축소

**선정 이유**

자원 최적화뿐 아니라 하드웨어·런타임 소실 시 기능 단위의 단계적 축소를 제공한다. 참고 근거는 Assigning Pods to Nodes (https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/); OpenTelemetry Documentation (https://opentelemetry.io/docs)이다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-04, AI-B-05, AI-O-01

**담당**

진나영

---

#### AI-B-07 — 장애 복구·버전 롤백

**ID**

AI-B-07

**대분류**

자원·배포

**소분류**

장애 복구·버전 롤백

**요구사항 정의**

구성요소의 비정상 종료, 준비 상태 실패, 모델 로드 실패, 선택 센서·서비스 소실을 기능 단위로 격리하고 복구해야 한다. 외부 선택 기능의 장애를 프로세스 자체 장애와 구분해 정상 기능을 불필요하게 재시작해서는 안 된다. 새 버전 적용이 실패하면 마지막 정상 버전으로 복구할 수 있어야 한다.

**전달 경로**

실행 관리환경 ↔ AI 실행 단위

**제안 주기**

장애·배포 실패 발생 시 즉시

**주기 선정 이유**

물리 시스템에서는 새 버전 실패가 전체 기능 중단으로 이어지지 않도록 검증된 이전 상태로 복구할 수 있어야 한다.

**1차 선정 기술**

공통 health interface + K3s probe/standalone supervisor + rollback

**선정 이유**

health 계약을 오케스트레이터와 분리해 K3s가 없는 실행환경에서도 동일한 장애 판단을 사용할 수 있다. 참고 근거는 Liveness, Readiness, and Startup Probes (https://kubernetes.io/docs/concepts/workloads/pods/probes/); K3s Helm (https://docs.k3s.io/add-ons/helm)이다.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-03, AI-B-05, AI-O-02, 실행 이미지 버전, 모델 버전

**담당**

진나영

---

#### AI-B-08 — AI 실행 제공자 추상화

**ID**

AI-B-08

**대분류**

자원·배포

**소분류**

AI 실행 제공자 추상화

**요구사항 정의**

AI 기능은 특정 모델 파일 형식, 추론 엔진, 가속기 API나 로컬·원격 실행 방식에 직접 의존하지 않고 공통 실행 인터페이스를 통해 기능을 요청해야 한다. 각 실행 제공자는 자신이 제공하는 기능, 지원 입력·출력, 필요한 하드웨어·런타임 조건과 자원 특성을 등록해야 한다. 특정 제공자가 사라져도 같은 기능을 제공하는 다른 제공자로 교체할 수 있어야 하며, 대체 제공자가 없으면 해당 선택 기능만 비활성화해야 한다.

**전달 경로**

AI 모듈 → 공통 실행 인터페이스 → runtime adapter → 노드

**제안 주기**

모델 실행 요청 시

**주기 선정 이유**

모델 형식과 하드웨어 API를 상위 기능에서 분리해야 특정 GPU·벤더·파일 형식 변경이 전체 시스템 코드 변경으로 확산되지 않는다.

**1차 선정 기술**

공통 AI 실행 인터페이스 + 교체 가능한 로컬·원격 실행 provider

**선정 이유**

어떤 모델·런타임을 쓰는지가 아니라 상위 기능이 구체 실행 기술을 모르게 하는 것이 핵심이다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-06, AI-C-04, AI-C-09

**담당**

진나영

---

#### AI-B-09 — 신규 확장 등록·호환성 검증

**ID**

AI-B-09

**대분류**

자원·배포

**소분류**

신규 확장 등록·호환성 검증

**요구사항 정의**

새로운 센서, 하드웨어, AI 기능, 런타임 또는 외부 서비스 제공자가 추가되면 기존 핵심 코드를 수정하지 않고 어댑터와 기능 등록 정보만으로 연결할 수 있어야 한다. 운영 등록 전에는 지원 입력·출력, 실행 조건, 자원 사용, 상태 확인과 장애 시 동작을 검증해야 한다. 검증 목적은 기존 모델보다 정확도를 높이는 것이 아니라 새 구성요소가 공통 인터페이스와 자원·장애 격리 규칙을 만족하는지 확인하는 것이다.

**전달 경로**

신규 구성 → 검증 환경 → 실행 프로파일 등록

**제안 주기**

새 센서·하드웨어·기능·런타임·외부 서비스 추가 또는 변경 시

**주기 선정 이유**

범용 프레임워크의 확장성은 새 구성요소를 추가할 때 핵심 코드 수정 없이 연결되고 기존 기능에 장애가 전파되지 않는지로 검증해야 한다.

**1차 선정 기술**

Adapter + Capability 등록 + 호환성·자원·health 검증

**선정 이유**

새 구성요소의 핵심 검증 항목은 공통 인터페이스 준수, 실행 가능 조건, 자원 한도, 상태 확인, 장애 격리와 기존 기능 비영향성이다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-06, AI-B-08, AI-C-10/13

**담당**

진나영

---

#### AI-B-10 — 말단 경량 실행 경계

**ID**

AI-B-10

**대분류**

자원·배포

**소분류**

말단 경량 실행 경계

**요구사항 정의**

자원 제약이 큰 말단은 로컬 안전·센서 처리·필요한 AI 실행과 전송 클라이언트만 유지하고, 브로커·수집기·백본 메시지 서버·중앙 관측 저장소·오케스트레이터 제어면 같은 서버형 미들웨어를 기본 설치 대상으로 요구해서는 안 된다. 현재 배포에서는 말단이 업무 전송 클라이언트와 관측 SDK, 미디어 송신 기능만 사용하고 MQTT 브로커, OpenTelemetry Collector, Prometheus, MQTT-Kafka 브릿지와 엣지측 K3s 제어 기능은 구역 엣지에, 서버측 K3s 제어 기능과 Kafka·중앙 저장소는 서버에 둔다. 향후 더 강한 말단 하드웨어를 사용하더라도 추가 서비스 배치는 실행 프로파일에서 선택적으로 결정해야 한다.

**전달 경로**

말단 경량 클라이언트 → 구역 엣지 서버형 서비스

**제안 주기**

배포 프로파일 정의·말단 하드웨어 변경 시

**주기 선정 이유**

말단에 서버형 미들웨어를 중복 배치하면 메모리·CPU·저장공간·운영 복잡도가 증가하므로 구역 단위 서비스를 엣지에 집약하는 편이 현재 구조와 자원 최소화 원칙에 맞는다.

**1차 선정 기술**

현재 프로파일: 말단 Paho MQTT·OTel SDK·미디어 client / 엣지 Mosquitto·OTel Collector·Prometheus·Bridge·K3s·오버레이 노드 / 서버 Kafka·중앙 저장소·K3s·오버레이 노드

**선정 이유**

프로젝트 문서 data_transport_architecture_v2.docx는 말단에는 무거운 서버를 두지 않고 클라이언트 라이브러리만 두며, 받아주는 서버와 수집기는 엣지에 둔다고 명시한다. 참고: §3, §5-8~§5-9.

**연관분야**

하드웨어, 백엔드

**연관점**

HW-C-05, BE-T-01, BE-S-02, AI-B-02~05, AI-O-01

**담당**

진나영

---

#### AI-B-11 — 서버·엣지 통합 실행관리 경계

**ID**

AI-B-11

**대분류**

자원·배포

**소분류**

서버·엣지 통합 실행관리

**요구사항 정의**

서버와 구역 엣지는 각각 자체 오케스트레이터 제어면을 갖고, 동일한 실행 제어·배치·health·롤백 계약으로 운용되어야 한다. 상위 기능은 어떤 실행 대상이 서버 클러스터에 있는지 엣지 클러스터에 있는지 알 필요가 없어야 하며, 실행 위치는 배포 프로파일과 자원·배치 조건으로만 결정해야 한다. 한쪽 클러스터의 제어면이 사용 불가능해도 다른 쪽에서 이미 실행 중인 기능과 로컬 실행 기능은 계속 동작해야 한다. 말단은 제어면을 설치 대상으로 요구하지 않는다.

**전달 경로**

배포 요청 → 실행관리 provider → 서버 클러스터 / 엣지 클러스터

**제안 주기**

배포·재배치·장애·자원 변화 시 이벤트 기반

**주기 선정 이유**

서버 자원도 구역 엣지와 같은 방식으로 관리해야 자원 회수·격리·재배치 규칙을 이중으로 만들지 않고, 클러스터가 늘어나도 상위 계약이 유지된다.

**1차 선정 기술**

클러스터 대상 선택이 가능한 공통 실행관리 provider + 서버·엣지 각각의 K3s 제어면

**선정 이유**

서버에도 K3s를 두면 서버 자원 배분, 격리, 정책 적용, 롤백을 엣지와 동일한 계약으로 처리할 수 있다. 어느 클러스터를 쓰는지는 provider 설정이며 상위 코드에 노출하지 않는다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-03, AI-B-04, AI-B-05, AI-B-07, AI-C-12, AI-C-17

**담당**

진나영

---

### 관측

#### AI-O-01 — 실행 성능·자원 관측

**ID**

AI-O-01

**대분류**

관측

**소분류**

실행 성능·자원 관측

**요구사항 정의**

각 실행 기능은 처리지연, 처리량, 대기열, 누락·오류, CPU·가속기·메모리 등 현재 측정 가능한 실행 상태를 로컬에서 확인할 수 있어야 한다. 일반 수치 지표는 구역 엣지에서 상세값을 유지하고 서버에는 운영에 필요한 요약을 전달할 수 있어야 한다. 다만 개별 장치의 생사와 치명적 오류처럼 집계 과정에서 사라지면 안 되는 상태는 일반 수치 요약과 분리해 개별 상태로 유지해야 한다. 외부 관측 기능 장애가 실제 기능 실행을 중단시키지 않아야 한다.

**전달 경로**

실행 노드 → 관측 수집 → 엣지 상세 지표 / 서버 요약 지표

**제안 주기**

로컬 자원 조정은 1~2초 수준, 중앙 운영 요약은 현재 15초, 임계·오류 사건은 즉시

**주기 선정 이유**

국가 규모에서는 일반 자원지표를 전부 중앙으로 올리기보다 구역에서 상세를 유지하고 중앙에는 요약해야 하지만, 장치 생사처럼 개별 의미가 중요한 값은 집계 대상에서 제외해야 한다.

**1차 선정 기술**

Local metrics + OTel/OTLP + 엣지 상세 보관 + 서버 metric 요약

**선정 이유**

현재 아키텍처는 metric만 엣지에서 요약해 서버로 올리고 log·trace는 원본을 전달한다. 장치 생사 신호는 요약에서 제외한다. 참고: 변경 데이터 전송 아키텍처 6-3~6-5.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

HW-C-05, BE-S-02, VZ-I-04, AI-B-06

**담당**

진나영

---

#### AI-O-02 — AI 오류·이상 사건 기록

**ID**

AI-O-02

**대분류**

관측

**소분류**

AI 오류·이상 사건 기록

**요구사항 정의**

입력·추론·계획·검증·실행 실패, 목표 지연 초과, 선택 기능 비가용과 실행 상태 변화를 구조화된 오류·이상 사건으로 기록해야 한다. 치명적 오류와 기능 불가 상태는 일반 metric 집계 주기를 기다리지 않고 개별 사건으로 즉시 전달할 수 있어야 한다. 선택 기능이 없는 정상적인 축소 운용과 핵심 기능 실패를 구분하고, 외부 수집기가 일시적으로 사용할 수 없어도 로컬 오류 기록은 남아야 한다.

**전달 경로**

AI 실행 노드 → 로컬 사건 기록 → 관측 경로 → 서버 오류·알림

**제안 주기**

오류·이상 발생 즉시

**주기 선정 이유**

치명적 오류를 평균·요약 지표에 포함하면 개별 장애가 가려질 수 있으므로 사건 단위로 보존하고 즉시 전달해야 한다.

**1차 선정 기술**

Local structured event + OTel log/trace + 즉시 오류/알림 경로

**선정 이유**

변경 아키텍처에서는 log와 trace를 요약하지 않고 원본 전달하며, 장치 생사와 치명 오류는 metric 요약과 별도로 취급한다. 참고: 변경 데이터 전송 아키텍처 6-3, 6-5.

**연관분야**

백엔드, 가시화

**연관점**

BE-X-05, VZ-I-10, BE-C-01

**담당**

진나영

---

#### AI-O-03 — 오류 재현 정보 관리

**ID**

AI-O-03

**대분류**

관측

**소분류**

오류 재현 정보 관리

**요구사항 정의**

중요 오류나 이상 판단은 당시 사용한 원본 입력, 실행 구성, 버전과 사용 가능한 업무 데이터·관측 참조를 연결해 다시 분석할 수 있어야 한다. 업무·책임 추적과 기술 성능 추적은 서로 다른 목적으로 유지해야 한다. 업무 스트림이 단기 보존 구간 안에 있는 경우에는 전송 로그의 위치를 이용해 재현할 수 있고, 보존 기간이 지난 데이터는 업무 저장소나 별도 아카이브가 존재하는 경우 그 참조를 사용해야 한다. 특정 전송·관측 기술이 없더라도 확보된 참조 범위에서 재현 정보를 남겨야 한다.

**전달 경로**

AI 실행환경 → 업무 저장/단기 전송로그 참조 + 관측 trace 참조 → 재현

**제안 주기**

중요 오류·이상 발생 시 기록, 신규 버전 검증 시 재사용

**주기 선정 이유**

전송 로그는 장기 저장소가 아니므로 단기 재현과 장기 재현의 데이터 원천을 분리해야 한다.

**1차 선정 기술**

업무/감사 상관 정보 + 단기 Kafka replay 참조 + 장기 저장/아카이브 참조 + OTel trace

**선정 이유**

현재 Kafka는 다중 소비자 버퍼와 단기 재생을 위한 계층이며 장기 저장소가 아니다. 참고: 변경 데이터 전송 아키텍처 6-1.

**연관분야**

백엔드, 가시화

**연관점**

BE-T-02, BE-S-01, BE-S-02, BE-S-04

**담당**

진나영

---

#### AI-O-04 — 실행 가용성 신호 연계

**ID**

AI-O-04

**대분류**

관측

**소분류**

실행 가용성 신호 연계

**요구사항 정의**

AI 실행 기능은 자신의 실행 가능 여부와 치명적 오류를 개별 상태로 제공해야 하며, 이 상태가 일반 자원 지표의 요약 과정에서 소실되어서는 안 된다. 말단·노드의 최종 가용성은 백엔드가 업무 전송 상태와 관측 상태를 통합해 판정하고, AI는 이 통합 결과를 원격 기능 선택과 실행 재구성에 사용해야 한다. 업무 전송은 가능하지만 관측 지표가 누락된 경우와 관측 지표는 존재하지만 업무 세션이 끊긴 경우를 구분할 수 있어야 한다.

**전달 경로**

실행 기능·장치 상태 → 업무/관측 경로 → 백엔드 통합 가용성 → AI 기능 선택

**제안 주기**

생사·연결·치명 오류 변화 시 즉시

**주기 선정 이유**

일반 metric 요약만으로는 특정 장치의 급사나 통신 불가를 놓칠 수 있으므로 개별 신호를 유지하고, 최종 가용성 판정은 한 곳에서 통합해야 한다.

**1차 선정 기술**

기능 health + MQTT LWT/heartbeat + Prometheus up/alert + 백엔드 availability

**선정 이유**

변경 데이터 전송 아키텍처는 장치 생사를 업무 평면과 관측 평면에서 각각 감지한 뒤 백엔드 단일 지점에서 통합 판정하고, 두 신호가 다르면 업무 세션을 1차 기준으로 사용하도록 정의한다. 참고: 6-5.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-O-01/02, AI-C-10/11, BE-T-04

**담당**

진나영

---

### 공통

#### AI-C-01 — 공통 데이터 규약

**ID**

AI-C-01

**대분류**

공통

**소분류**

공통 데이터 규약

**요구사항 정의**

AI가 생산·소비하는 데이터는 백엔드가 정의한 공통 식별·시간·버전 규약과 의미 체계를 따라야 한다. 요구사항 단계에서는 구체 변수명을 확정하지 않고 필요한 정보의 의미만 정의하며, 하드웨어·백엔드·AI·가시화 요구사항이 확정된 뒤 데이터 사전에서 동일 의미 항목의 변수명을 통일해야 한다. 업무 데이터, 관측 데이터, 명령, 미디어가 서로 다른 전송 경로를 사용하더라도 공통 의미 규약은 유지되어야 한다.

**전달 경로**

온디바이스 ↔ 엣지 노드 ↔ 서버

**제안 주기**

스키마 확정·버전 변경 시

**주기 선정 이유**

공통 데이터 계약을 사용해야 장치·모델·전송 방식이 바뀌어도 파트 간 인터페이스를 유지할 수 있다.

**1차 선정 기술**

공통 Domain Schema + Serializer provider

**선정 이유**

전송 아키텍처는 같은 시스템 안에서도 데이터 성격에 따라 MQTT, Kafka, OTLP, 별도 미디어 경로를 사용하므로 의미 규약과 전송 구현을 분리해야 한다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §4.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-C-01, HW-C-02, VZ-I-01

**담당**

진나영

---

#### AI-C-02 — 좌표계 경계

**ID**

AI-C-02

**대분류**

공통

**소분류**

좌표계 경계

**요구사항 정의**

AI가 산출하는 위치·방향·영상 좌표에는 어떤 기준 좌표계에서 나온 값인지 알 수 있는 정보가 포함되어야 한다. 카메라 보정 프로파일이 있는 경우 영상과 광선·로컬 공간 사이의 변환에 활용하고, 전역 좌표 변환은 백엔드 디지털 트윈이 담당해야 한다. 보정 정보나 전역 변환 기능이 없으면 로컬 영상 좌표 결과를 유지해야 한다.

**전달 경로**

온디바이스·엣지 노드 → 서버

**제안 주기**

공간 정보 생성 시

**주기 선정 이유**

좌표 변환 책임을 한 곳에 두어 AI·디지털 트윈·가시화 간 좌표 불일치를 방지한다.

**1차 선정 기술**

로컬 좌표 기준 유지 + 전역 좌표 변환은 선택 기능으로 사용

**선정 이유**

전역 좌표·캘리브레이션을 필수 전제로 두지 않아 Vision 자체는 단독으로 동작할 수 있다. 참고 근거는 팀 공통 규약: BE-C-04이다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-02, AI-E-03, BE-C-04, DT-03

**담당**

진나영

---

#### AI-C-03 — 프레임 참조·시간 규약

**ID**

AI-C-03

**대분류**

공통

**소분류**

프레임 참조·시간 규약

**요구사항 정의**

영상·센서 입력과 파생 결과는 원본 관측을 다시 찾을 수 있는 참조와 측정 시각, 처리 순서를 유지해야 한다. 현재 프로젝트에서 노드 간 결과를 융합하거나 프레임을 정합하는 기능은 백엔드 공통 시간 기준을 따라야 하며 NTP를 현재 시간 동기화 방식으로 사용한다. 시간 동기화가 일시적으로 깨져도 단일 노드의 로컬 처리와 순서는 유지하되 노드 간 정합이 필요한 기능은 정확도가 저하된 상태로 처리해야 한다.

**전달 경로**

온디바이스·엣지 노드 → 서버

**제안 주기**

모든 관측·파생 결과 생성 시

**주기 선정 이유**

영상 오버레이, 지연 측정, 오류 재현에서 동일한 원본 입력을 식별할 수 있어야 한다.

**1차 선정 기술**

공통 프레임 참조·시간 계약 + 현재 NTP 동기 + 로컬 순서 fallback

**선정 이유**

프레임 정합과 여러 노드 결과 병합에는 공통 시간 기준이 필요하지만, 일시적 동기화 장애를 전체 AI 중단으로 확대하지 않는다. 현재 공통 규약 BE-C-03과 정합한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-C-03, HW-S-08, VZ-I-07

**담당**

진나영

---

#### AI-C-04 — 외부 입력·실행 어댑터

**ID**

AI-C-04

**대분류**

공통

**소분류**

외부 입력·실행 어댑터

**요구사항 정의**

센서 입력, 미디어, 메시지 전송, 데이터 직렬화, AI 실행, 제어, 저장·관측 등 외부 기술과 업무 로직 사이에는 공통 인터페이스와 어댑터를 둬야 한다. 상위 기능은 특정 센서 제품, 카메라 방식, 통신 프로토콜, GPU 벤더, 모델 형식, 런타임 또는 저장소를 직접 참조해서는 안 된다. 새 구성요소는 어댑터와 기능 등록 정보만 추가해 연결할 수 있어야 한다.

**전달 경로**

센서·장치 → 입력 어댑터 → AI 모듈 → 서버

**제안 주기**

새 센서·모델·출력 형식 추가 시

**주기 선정 이유**

장치·모델별 처리 코드를 핵심 AI 로직과 분리해야 여러 도메인과 하드웨어에 같은 실행 구조를 재사용할 수 있다.

**1차 선정 기술**

Sensor/Media/Transport/Serializer/Runtime/Control/Observability Adapter Interface

**선정 이유**

센서·하드웨어·전송·런타임 종속성을 어댑터 경계 뒤로 숨기고 상위 기능이 목적 수준의 인터페이스만 사용하도록 한다. 참고 근거는 Hexagonal Architecture (Ports & Adapters) (https://alistair.cockburn.us/hexagonal-architecture)이다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-02, AI-C-01, 입력/출력 스키마

**담당**

진나영

---

#### AI-C-05 — 핵심·선택 기능 경계

**ID**

AI-C-05

**대분류**

공통

**소분류**

핵심·선택 기능 경계

**요구사항 정의**

배포 환경마다 반드시 유지해야 하는 최소 핵심 기능과 설치 환경에 따라 추가되는 선택 기능을 구분해야 한다. 추가 센서, 카메라, GPU·NPU, 엣지 서버, 다중 관측, 고비용 AI, 위험 분석, 서브태스크 기능 중 일부가 없다는 이유로 관련 없는 기능까지 시작하지 못하는 구조로 구현해서는 안 된다. 선택 기능이 추가·제거·고장 나면 해당 기능만 활성·축소·비활성화하고 나머지 기능은 계속 실행해야 한다.

**전달 경로**

온디바이스 ↔ 엣지 노드 ↔ 서버

**제안 주기**

기능·인터페이스 설계 변경 시

**주기 선정 이유**

기능 책임과 실행 위치를 명확히 해 파트 간 중복 구현과 네트워크 의존성 문제를 줄인다.

**1차 선정 기술**

Core/Optional capability 분리 + 단계적 기능 축소

**선정 이유**

범용 프레임워크가 배치 환경별 센서·하드웨어·AI 구성 차이를 허용하면서 사용 가능한 수준에서 계속 동작하게 한다. 참고 근거는 Liveness, Readiness, and Startup Probes (https://kubernetes.io/docs/concepts/workloads/pods/probes/); Assigning Pods to Nodes (https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)이다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-N-01, AI-E-01/02, AI-D-01/02, AI-B-02~07, BE-A-03

**담당**

진나영

---

#### AI-C-06 — 메시지 전송

**ID**

AI-C-06

**대분류**

공통

**소분류**

메시지 전송

**요구사항 정의**

AI 기능은 업무 데이터와 제어 요청을 공통 전송 인터페이스로 송수신하고, 구간별 실제 전송 기술은 어댑터에서 선택해야 한다. 현재 배포에서는 말단과 구역 엣지 사이의 업무·명령·하트비트는 MQTT를 사용하고, 구역 엣지와 서버 사이의 업무 데이터와 서버에서 엣지로 내려오는 명령은 서버의 Kafka를 사용한다. 엣지는 Kafka 서버를 운영하지 않고 MQTT 브로커와 서버 Kafka 사이의 양방향 브릿지 역할만 수행해야 한다. AI 로직은 MQTT·Kafka API를 직접 호출하지 않고 동일한 업무 메시지 계약을 사용해야 한다.

**전달 경로**

말단 ↔ 엣지 MQTT Broker ↔ 엣지 Bridge ↔ (보안 오버레이) ↔ 서버 Kafka

**제안 주기**

메시지·제어 이벤트 발생 시

**주기 선정 이유**

말단 구간과 백본 구간의 물리 조건이 다르므로 구간별 전송 기술을 나누되, 엣지에 두 종류의 서버를 중복 설치하지 않고 브릿지로 연결해야 한다.

**1차 선정 기술**

Transport Interface + 현재 MQTT Broker(엣지) + 양방향 Bridge(엣지) + Kafka(서버)

**선정 이유**

현재 구조에서는 엣지에 Kafka가 없고 서버에 MQTT 브로커가 없다. 브릿지가 엣지 MQTT와 서버 Kafka를 직접 연결한다. 참고: 변경 데이터 전송 아키텍처 3, 6-2, 6-6.

**연관분야**

하드웨어, 백엔드

**연관점**

BE-T-01, AI-B-03, AI-C-01

**담당**

진나영

---

#### AI-C-07 — 데이터 직렬화

**ID**

AI-C-07

**대분류**

공통

**소분류**

데이터 직렬화

**요구사항 정의**

업무 데이터의 내부 구조와 네트워크 전송용 직렬화 형식을 분리하고 공통 직렬화 인터페이스를 통해 변환해야 한다. 특정 바이너리 또는 텍스트 형식이 프레임워크의 필수조건이 되어서는 안 된다. 기계 간 통신에서는 크기·처리속도·스키마 안정성을, 사람이 확인하는 경계에서는 가독성을 우선해야 한다.

**전달 경로**

온디바이스 ↔ 엣지 노드 ↔ 서버

**제안 주기**

스키마 변경 시

**주기 선정 이유**

Protobuf는 compact typed schema와 코드 생성·하위호환 확장에 유리하고, JSON은 가시성과 디버깅에 유리하므로 사용 경계를 분리한다.

**1차 선정 기술**

Serializer Interface + Protobuf/JSON을 현재 후보로 검증

**선정 이유**

Protobuf의 typed schema 장점을 사용하면서 직렬화 형식 교체가 업무 코드 변경으로 이어지지 않도록 한다. 참고 근거는 Protocol Buffers Overview (https://protobuf.dev/overview); MessagePack (https://msgpack.org); Hexagonal Architecture (Ports & Adapters) (https://alistair.cockburn.us/hexagonal-architecture)이다.

**연관분야**

백엔드, 가시화

**연관점**

BE-C-01, AI-C-01, AI-C-08

**담당**

진나영

---

#### AI-C-08 — 미디어 입력 관리

**ID**

AI-C-08

**대분류**

공통

**소분류**

미디어 입력 관리

**요구사항 정의**

네트워크 카메라, 로봇 로컬 카메라, 파일 재생 등 서로 다른 영상 소스를 공통 미디어 입력으로 제공해야 한다. 영상 픽셀은 대용량이므로 업무 데이터 MQTT/Kafka나 관측 OTLP에 실어 보내지 않고 별도 미디어 경로를 사용해야 한다. 카메라 URL, 장치 경로, codec, 전송 방식은 미디어 어댑터 내부에 숨기고 인지 기능은 공통 영상 입력만 소비해야 한다. 영상 소스가 없는 배치에서는 영상 기반 기능만 비활성화해야 한다.

**전달 경로**

카메라·로봇 → 별도 미디어 경로 → 인지/가시화 소비자

**제안 주기**

영상 스트림 연결 동안 지속

**주기 선정 이유**

RTSP는 실시간 미디어 전달의 표준 제어 프로토콜이고 GStreamer는 RTP/RTSP, jitter, transport 선택과 다양한 디코더를 제공해 카메라별 차이를 어댑터에서 흡수할 수 있다.

**1차 선정 기술**

Media Source Interface + 별도 미디어 provider(RTSP/GStreamer/local-camera/Redis 등)

**선정 이유**

전송 아키텍처는 영상 픽셀을 업무·관측 경로와 분리한다고 명시하며 Redis는 구현 예시일 뿐 고정 요구사항이 아니다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §4, §5-1, §5-9.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-E-01, AI-E-02, HW-R-04, HW-S-06

**담당**

진나영

---

#### AI-C-09 — 선택형 외부·고비용 기능 연동

**ID**

AI-C-09

**대분류**

공통

**소분류**

선택형 외부·고비용 기능 연동

**요구사항 정의**

로컬 기본 기능만으로 처리하기 어려운 작업은 엣지·서버·외부 AI 서비스 등 선택형 기능으로 위임할 수 있어야 한다. 상위 업무 로직은 해당 기능이 로컬 모델인지 원격 서비스인지, 어떤 모델·가속기를 사용하는지 알 필요가 없어야 하며 공통 기능 호출 인터페이스만 사용해야 한다. 호환 제공자가 없거나 연결이 끊기면 해당 보조 기능만 비활성화하고 기본 기능은 계속 실행해야 한다.

**전달 경로**

기본 기능 → 선택형 로컬·엣지·서버 기능 provider

**제안 주기**

기본 기능만으로 요구 정보를 확보하지 못한 경우

**주기 선정 이유**

고비용 기능을 특정 GenAI 기술로 고정하지 않고 필요할 때만 외부 또는 상위 자원으로 위임해야 다양한 하드웨어와 도메인에 같은 구조를 적용할 수 있다.

**1차 선정 기술**

공통 선택 기능 호출 인터페이스 + 로컬/엣지/서버 provider

**선정 이유**

생성형 AI, 고정밀 비전, 기하 추정 등 구체 기능은 provider 구현 예가 될 수 있지만 프레임워크의 필수 구성요소는 아니다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-08, AI-C-04

**담당**

진나영

---

#### AI-C-10 — 기능·자원 등록·탐색

**ID**

AI-C-10

**대분류**

공통

**소분류**

기능·자원 등록·탐색

**요구사항 정의**

실행 노드, 센서·미디어 입력, AI 기능, 런타임, 전송·관측·외부 서비스는 현재 제공 가능한 기능과 자원, 상태, 버전, 입력·출력 조건을 등록할 수 있어야 한다. AI 기능 등록은 백엔드가 관리하는 장치·구역·최종 가용성 정보를 보완하는 역할로 한정하고, 장치의 최종 가용성을 별도로 중복 판정해서는 안 된다. 원격 기능을 선택할 때에는 백엔드가 통합한 장치 가용 상태와 해당 기능의 로컬 상태를 함께 확인해야 한다. 중앙 등록 기능이 일시적으로 없어도 각 노드는 마지막으로 확인된 로컬 기능 정보를 이용해 기존 기능을 계속 실행해야 한다.

**전달 경로**

로컬 기능 등록 ↔ 구역 엣지/백엔드 레지스트리·가용성 판정

**제안 주기**

시작·등록·상태/구성 변경 시 즉시 갱신

**주기 선정 이유**

업무 전송 상태와 관측 상태를 통합한 장치 가용성 판정은 백엔드 단일 책임으로 두고, AI는 그 결과를 기능 선택에 활용해야 중복 판정을 피할 수 있다.

**1차 선정 기술**

Capability Registry + 백엔드 장치 가용성 연계 + 로컬 snapshot

**선정 이유**

현재 아키텍처는 MQTT 세션·heartbeat와 Prometheus up을 백엔드에서 통합해 최종 장치 가용성을 판정한다. AI capability는 이 판정을 대체하지 않는다. 참고: 변경 데이터 전송 아키텍처 6-5.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01, AI-B-04~08, AI-C-04/05/11

**담당**

진나영

---

#### AI-C-11 — 선택 기능·의존성 격리

**ID**

AI-C-11

**대분류**

공통

**소분류**

선택 기능·의존성 격리

**요구사항 정의**

각 기능은 반드시 필요한 최소 조건과 있으면 품질·효율을 높이는 선택 조건을 구분해 선언해야 한다. 필수 조건이 충족되지 않으면 해당 기능만 비활성화하고, 선택 조건이 없어지면 가능한 기본 또는 축소 모드로 전환해야 한다. 원격 장치가 백엔드의 최종 가용성 판정에서 사용할 수 없는 상태라면 그 장치가 제공하는 선택 기능은 즉시 선택 후보에서 제외해야 한다. 하나의 센서, 하드웨어, 서비스, 통신 경로 장애가 관련 없는 기능의 시작·실행·복구를 차단해서는 안 된다.

**전달 경로**

Capability Registry → 각 기능

**제안 주기**

시작·capability 변화·장애 발생 시

**주기 선정 이유**

선택 구성요소의 결손을 정상적인 구성 변형으로 처리하면 기능 추가·제거와 부분 장애에 강한 시스템을 만들 수 있다.

**1차 선정 기술**

required/optional capability 선언 + ACTIVE/DEGRADED/DISABLED 상태

**선정 이유**

기능 자체의 health와 장치의 최종 availability를 구분하고 둘 중 하나라도 사용할 수 없으면 해당 원격 기능만 격리한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-C-10, AI-O-02, AI-B-05~08

**담당**

진나영

---

#### AI-C-12 — 인프라 제공자 추상화

**ID**

AI-C-12

**대분류**

공통

**소분류**

인프라 제공자 추상화

**요구사항 정의**

메시지 전송, 직렬화, 미디어 입력, AI 실행, 실행관리, 네트워크 오버레이, 관측·저장 등 외부 인프라는 기능별 제공자 인터페이스로 분리해야 한다. 현재 프로젝트에서 사용하는 MQTT, Kafka, OpenTelemetry, K3s, Tailscale과 향후 추가될 기술은 현재 배포의 구현 선택일 뿐 프레임워크 핵심 코드의 필수 기술로 하드코딩해서는 안 된다. 제공자 추가·제거·교체는 등록과 설정 변경으로 처리하고 상위 인지·판단·위험 분석 코드는 유지해야 한다.

**전달 경로**

상위 기능 → 공통 provider interface → 선택 구현

**제안 주기**

provider 등록·변경 시

**주기 선정 이유**

도구 선택의 근거와 운영 표준은 유지하면서 특정 벤더·프로토콜·runtime이 없어도 다른 provider 또는 축소 기능으로 동작할 수 있게 한다.

**1차 선정 기술**

Transport/Serializer/Media/Runtime/Orchestrator Provider Interface

**선정 이유**

현재 배포에서는 MQTT, Kafka, OTel, 별도 미디어 경로, K3s, Tailscale 등 구체 구현을 사용하지만 상위 AI 계약에서는 provider로 감춘다. 즉 현재 아키텍처와 범용 프레임워크의 비종속성은 충돌하지 않고 '현재 배포 프로파일'과 '공통 인터페이스'로 분리한다. 참고: 프로젝트 문서 data_transport_architecture_v2.docx §3~§6.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-B-02/03/08, AI-C-04/06~11

**담당**

진나영

---

#### AI-C-13 — 기능 선택·자원 최소화

**ID**

AI-C-13

**대분류**

공통

**소분류**

기능 선택·자원 최소화

**요구사항 정의**

기능 선택과 실행 구성은 요구 기능·안전·지연 조건을 만족하는 범위에서 전체 자원 사용과 운영 부담을 최소화하는 것을 기본 원칙으로 해야 한다. 사용 가능한 기능을 모두 상시 실행하지 않고 실제로 필요한 기능만 활성화해야 한다. 자원이 부족하거나 하드웨어가 바뀌면 우선순위가 낮은 선택 기능부터 축소하고 가능한 실행 후보로 자동 재구성해야 한다.

**전달 경로**

전체 기능·자원 상태 → 구성 선택

**제안 주기**

기능 실행·상태·자원 변화 시

**주기 선정 이유**

범용 프레임워크의 목표는 특정 모델의 최고 성능이 아니라 서로 다른 장비에서도 필요한 기능을 가장 적은 자원으로 지속 제공하는 것이다.

**1차 선정 기술**

제약조건 기반 최소비용 구성 선택

**선정 이유**

최대 성능보다 요구조건 충족 후 최소 자원 사용을 기본 정책으로 둔다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-01/06/09, AI-S-05, AI-R-04

**담당**

진나영

---

#### AI-C-14 — 데이터 유형별 경로 분리

**ID**

AI-C-14

**대분류**

공통

**소분류**

데이터 유형별 경로 분리

**요구사항 정의**

AI에서 발생하는 데이터는 업무 데이터와 시스템 관측 데이터로 구분하고, 영상 등 대용량 데이터는 별도 미디어 경로로 처리해야 한다. 물리 제어 명령은 업무 데이터에 포함하되 전달 보장, 순서, 실행 결과와 책임 추적이 필요한 제어 데이터로 별도 정책을 적용해야 한다. 현재 배포에서는 말단↔엣지 업무·제어·하트비트는 MQTT, 엣지↔서버 업무와 서버→엣지 제어는 Kafka, metric·log·trace는 OpenTelemetry 기반 관측 경로, 영상 픽셀은 별도 미디어 경로를 사용한다. AI 기능은 데이터의 의미만 생산하고 실제 경로 선택은 전송·관측·미디어 어댑터가 담당해야 한다.

**전달 경로**

AI 생산 데이터 → 업무/관측/미디어 분류 → 유형별 전용 어댑터

**제안 주기**

데이터 생성·전송 시 항상 적용

**주기 선정 이유**

명령은 업무 의미를 가지지만 일반 상태 데이터보다 전달·순서·결과 회신·책임 추적 요구가 강하므로 같은 업무 평면 안에서 별도 정책이 필요하다.

**1차 선정 기술**

업무/관측/미디어 데이터 분리 + 제어 데이터 전용 정책

**선정 이유**

변경 아키텍처는 업무 데이터와 관측 데이터를 분리하고, 명령을 업무 데이터의 특수 유형으로 다루며 영상은 별도 경로로 분리한다. 참고: 변경 데이터 전송 아키텍처 4, 5-6, 8.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

BE-T-01/02, BE-S-02/03, AI-C-06/08, AI-O-01/02

**담당**

진나영

---

#### AI-C-15 — 도메인·배포 프로파일 분리

**ID**

AI-C-15

**대분류**

공통

**소분류**

도메인·배포 프로파일 분리

**요구사항 정의**

로봇 자율주행 지원, 시설 감시, 하천 위험 관리 등 서로 다른 적용 분야는 별도 핵심 프로그램으로 분기하지 않고 동일 프레임워크 위에서 입력 어댑터, 활성 기능, 판단 규칙, 실행 위치와 외부 제어 연결을 배포 프로파일로 구성해야 한다. 새 도메인을 추가할 때 기존 인지·의사결정·실행관리 핵심 코드를 수정하지 않고 필요한 어댑터·규칙·기능 제공자와 설정을 추가하는 방식으로 확장할 수 있어야 한다. 특정 도메인에서 사용하지 않는 기능은 설치·실행을 요구하지 않아야 한다.

**전달 경로**

배포 프로파일 → 기능·어댑터·규칙·실행 위치 구성

**제안 주기**

새 도메인·현장·장비 조합을 배포하거나 구성이 변경될 때

**주기 선정 이유**

도메인명을 기준으로 핵심 코드에 분기문을 추가하면 적용 분야가 늘어날수록 결합도가 증가하므로, 차이는 구성과 확장점으로 격리해야 한다.

**1차 선정 기술**

공통 프레임워크 + Domain/Deployment Profile + Adapter/Rule/Capability 조합

**선정 이유**

로봇·군사 감시·하천 시나리오는 서로 다른 프로그램이 아니라 동일 프레임워크의 다른 배포 프로파일로 구현한다. 현재 전송 기술 역시 배포 프로파일의 구현 선택으로 취급한다.

**연관분야**

하드웨어, 백엔드, 가시화

**연관점**

AI-C-04/05/10/11/12/13, AI-B-04/06/09

**담당**

진나영

---

#### AI-C-16 — 폐쇄망 운용 경계

**ID**

AI-C-16

**대분류**

공통

**소분류**

폐쇄망 운용 경계

**요구사항 정의**

시스템은 공개 인터넷에 대한 나가는 연결 없이 기동·복구·재구성할 수 있어야 한다. 실행에 필요한 이미지, 모델, 패키지, 규칙, 보정 정보는 내부 저장소에서 조달해야 하며 외부 저장소를 기본 조달 경로로 두어서는 안 된다. 외부 AI 서비스나 외부 API를 사용하는 기능은 항상 선택 기능으로 선언하고, 사용할 수 없으면 해당 기능만 비활성화하고 나머지는 계속 실행해야 한다. 어떤 실행 단위가 외부 연결을 요구하는지는 등록 정보로 드러나야 하며, 폐쇄망 프로파일에서는 그 요구가 배치 전에 검출되어야 한다.

**전달 경로**

내부 저장소 → 실행 노드 / 외부 연결 요구 선언 → 배치 검증

**제안 주기**

배포·검증 시점, 외부 연결 요구가 선언·변경될 때

**주기 선정 이유**

보안 경계는 실행 중 우회되면 의미가 없으므로 배치 전에 요구 조건으로 검증해야 하고, 조달 실패가 런타임 장애로 나타나지 않아야 한다.

**1차 선정 기술**

내부 자산 저장소 참조 + 외부 연결 요구 선언 + 폐쇄망 프로파일 검증

**선정 이유**

폐쇄망은 특정 네트워크 제품의 문제가 아니라 조달 경로와 의존성 선언의 문제다. 따라서 요구사항은 저장소·프로토콜을 고정하지 않고 "외부 연결을 전제로 하지 않는다"는 경계만 규정한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-B-02, AI-B-05, AI-B-09, AI-C-09, AI-C-17

**담당**

진나영

---

#### AI-C-17 — 보안 오버레이 네트워크 추상화

**ID**

AI-C-17

**대분류**

공통

**소분류**

보안 오버레이 네트워크

**요구사항 정의**

구역 엣지와 서버 사이의 업무·제어·관측·배포 통신은 공개 인터넷을 직접 경유하지 않고 인증된 보안 오버레이 네트워크 위에서 이루어져야 한다. 전송·관측·실행관리 provider는 오버레이의 존재를 전제하되 특정 오버레이 제품의 API에 의존해서는 안 되며, 연결 가능 여부와 피어 상태는 공통 인터페이스로 조회할 수 있어야 한다. 오버레이가 끊기면 원격 기능과 서버 연동만 축소하고, 구역 내부 통신과 말단 로컬 기능은 계속 동작해야 한다. 오버레이 상태는 업무 전송 상태·관측 상태와 구분해 보고해야 한다.

**전달 경로**

엣지 노드 ↔ 보안 오버레이 ↔ 서버

**제안 주기**

연결 상태 변화 시 즉시, 그 외에는 주기적 상태 확인

**주기 선정 이유**

오버레이 단절은 구역 내부 기능 장애와 원인이 다르므로 별도 신호로 유지해야 원격 기능 축소와 로컬 장애 대응을 구분할 수 있다.

**1차 선정 기술**

네트워크 오버레이 provider 인터페이스 + 현재 Tailscale 구현

**선정 이유**

현재 배포는 인터넷 노출 없이 서버·엣지를 연결하기 위해 Tailscale을 사용한다. 그러나 오버레이 제품은 배포 프로파일의 구현 선택이므로 상위 코드는 연결 가능 여부·피어 상태라는 목적 수준 인터페이스만 사용한다.

**연관분야**

하드웨어, 백엔드

**연관점**

AI-C-06, AI-C-12, AI-C-14, AI-B-11, AI-O-04

**담당**

진나영

---

## 6. AI 개발 에이전트의 구현 순서

1. 전체 요구사항을 먼저 읽고 공통 경계와 책임을 추출한다.
2. 요구사항 간 생산자·소비자 관계를 정리하되 변수명은 아직 확정하지 않는다.
3. Core/Optional capability와 provider/adapter 경계를 먼저 설계한다.
4. Capability Registry, 실행 호환성·자원 프로파일, 배포 프로파일 구조를 설계한다.
5. 기능별 공통 인터페이스를 만든 뒤 현재 배포 기술을 provider 구현으로 연결한다.
6. 로컬 실행, 엣지 실행, 원격 실행 중 어느 위치에서도 동일한 상위 기능 계약을 사용할 수 있게 한다.
7. 정상 경로보다 먼저 선택 기능 부재, 네트워크 단절, provider 장애, 자원 부족, 갱신 실패의 동작을 명시한다.
8. 전체 기능이 구현된 뒤 실제 생산자·소비자 정보를 기반으로 공통 데이터 사전과 변수명을 한 번에 통일한다.
9. 각 요구사항 ID별로 구현 위치, 코드 모듈, 테스트 케이스와 충족 여부를 추적할 수 있게 한다.

## 7. 요구사항 추적 규칙

각 구현 단위와 테스트에는 관련 요구사항 ID를 남긴다.

예시:

```text
implements: AI-C-04, AI-C-10, AI-C-11
tests: adapter replacement, optional provider loss, registry fallback
```

완료 판정은 특정 모델 정확도나 특정 기술 사용 여부가 아니라 해당 요구사항이 정의한 시스템 동작과 경계가 실제로 보장되는지로 판단한다.
