# 360° 카메라 — 구버전(2607) vs 현행(2609) 비전처리 전면 비교 + 팀 배포(이식) 가이드

**작성일**: 2026-09-22
**대상 폴더**: `C:\Users\asdfa\physical mk2\360camera_work` (전 파일 정독), `C:\Users\asdfa\docx2026_code`
**근거**: 폴더 내 스크립트 전문, 실측 로그 5종, 총정리 v1(260915)/v2(260921), 프로젝트 문서
`unidepth_dual_yolo_config_260617.yaml` · `unidepth_dual_yolo_260617.py`
**권위 순서**: 실측 로그·코드 원문 > 총정리 문서 > 채팅 기록 > 기억

---

# 0. 결론 요약 (먼저 읽을 것)

## 0.1 질문 2 — "예전엔 정확했는데 지금은 왜 오차가 심한가"

**비전 처리(거리 계산) 코드는 7월과 9월이 한 글자도 다르지 않다.** 그리고 **360° 경로에
UniDepth는 지금도, 과거에도 쓰인 적이 없다.** 팀원이 "unidepth 안 쓰고 그냥 기하로 구한다"고
한 지적은 **사실이지만, 그것은 변경 사항이 아니라 처음부터 그랬던 것**이다.

| 확인 대상 | 7월 (`detect_ground_3b1.py` / `detect_send_3b2.py`) | 9월 (`anchor360_live.py`) |
|---|---|---|
| 거리 산출 방식 | `pixel_to_ground` — bbox 하단중앙 픽셀 광선 ↔ 바닥평면(y=0) 교차 | **완전 동일** |
| `fx=fy` | `W/(2·tan45°)` = 360 | **완전 동일** |
| 회전 | pitch 1축 (roll 0 고정) | **완전 동일** |
| 크기 산출 | `(x2−x1)/fx·D`, `(y2−y1)/fy·D` | **완전 동일** |
| NMS | greedy IoU 0.45, 그룹 무관 | **완전 동일** |
| 필터 | `KEEP_GROUPS = {HARD_OBSTACLE, AGENT}` | **완전 동일** + `--drop-clipped`/`--max-dist` **추가** |
| 검출기 | `YOLOWorld(yolov8s-world.pt)` + config 클래스별 conf | **완전 동일** |
| 깊이추정 모델 | **없음** | **없음** |

그러면 왜 체감이 달라졌는가 — **5가지가 겹쳤고, 그중 가장 큰 것은 코드가 아니라 "7월에 유니티가
보던 데이터가 무엇이었는가"다.**

1. **7월 유니티 입력은 실시간 검출 결과가 아니라 손으로 만든 5개짜리 고정 JSON이었다**
   (`anchor360_merged.json`. §C-1에 증거). 전부 0.86~1.68 m, 전부 바닥에 놓인 소화기·상자,
   크기는 전부 `0.3/0.3/0.3` 고정, `bbox_xyxy` 전부 `[0,0,0,0]`, 슬라이스별 회전을 **손으로** 넣음.
   → "잘 맞았다"는 기억은 **가장 정확한 거리대의, 가장 좋은 물체만, 손으로 정렬한 결과**다.
2. **거리대가 바뀌었다.** 이 방식은 D ≈ 카메라 높이(1.85 m) 근처에서 오차가 최소이고
   멀어질수록 제곱으로 커진다. 1 픽셀 오차가 D=1 m 에서 0.66 cm, D=5 m 에서 4.3 cm,
   D=8 m 에서 10.1 cm 다(§B-4 표). 지금 유니티에 뜨는 `box`/`cardboard box` 는 5~6.5 m 짜리다.
3. **`yawOffsetDeg` 미해결** — 이건 거리 문제가 아니라 **방향 문제**이고 v1 §8 이후 손도 못 댔다.
   기록된 과거 3값(−35 / +150 / −45)은 **수학적으로 서로 모순**이라 애초에 신뢰할 수 없다.
   "각도가 중구난방"은 거의 전부 여기서 온다.
4. **인카메라 스티칭 선명도가 ProStitcher의 0.65배** → bbox 하단변이 흔들림 →
   거리가 광선 방향으로 미끄러짐. 다만 이건 실측상 **평균 9.6 cm** 수준이다(§C-6).
5. **공유 config 의 임계값이 6월 이후 낮아졌다.** `box: 0.18 → 0.12`,
   `fire extinguisher: 0.22 → 0.12`. 지금 유령처럼 뜨는 `box conf=0.12~0.17, D=5~6.5 m` 들은
   **6월 설정이었으면 전부 걸러졌을 것들**이다(§C-7). 소유자가 `jny`, 권한 777.

**그리고 결정적으로 — 영상 전환은 정확도 악화의 원인이 아니다.**
2026-09-21 같은 날 같은 장면을 사진(ProStitcher) 경로와 영상(인카메라) 경로로 각각 돌린
대조 실험이 있고, 두 경로의 좌표 차이는 **평균 9.6 cm** 로 논문 앵커 정확도(11.2 cm)보다 작다.
즉 **오늘 사진 방식으로 돌려도 지금과 거의 같은 값이 나온다**(§C-6).

## 0.2 질문 1 — "팀원이 각자 돌릴 수 있게"

- **venv 는 그냥 복사해도 안 된다.** `pyvenv.cfg` 의 절대경로, `bin/activate` 의 `VIRTUAL_ENV`,
  `bin/*` 스크립트의 절대 shebang, 그리고 `.so` 확장모듈의 파이썬/CUDA 버전 결합 때문이다.
  **다만 방법은 3가지 있다**(§E-3). 이 프로젝트에는 **"서버 venv 1개를 공용 읽기전용으로 열어두고
  스크립트만 각자 복사"** 가 가장 싸고 안전하다.
- PC 쪽 하드코딩은 `pro2_auto_bg.py` 의 상수 3개와 `_TRT_BACKUP_DIR` 이 전부고,
  `stream_to_server.py` / `anchor360_bridge.py` / `preview_probe.py` 는 **이미 전부 CLI 인자로 뺐다.**
- 서버 쪽은 `anchor360_live.py` 가 이미 `~/` 기반이라 **계정만 바꾸면 그대로 돈다.**
  단 **config yaml 안의 가중치 경로가 `/home/dg/...` 절대경로**라 `--cfg` 또는 `--weight` 오버라이드가 필요하다.

---

# Part A. 전체 흐름 — 무엇이 언제 어떻게 바뀌었나

## A-1. 타임라인

| 시점 | 무슨 일 | 남긴 것 |
|---|---|---|
| 2026-06-26 ~ 06-30 | Pro2 촬영 자동화 + ProStitcher CLI stitch 구현. 촬영 18.6 s 주기 달성 | `pro2_auto.py`, `pro2_auto_bg.py`, `stitch_prostitcher.py` |
| 2026-06-17 | GO1 로봇용 `unidepth_dual_yolo.py` + 공용 config 확정 | `unidepth_dual_yolo_config.yaml` (이후 360 경로가 **빌려 씀**) |
| 2026-07-01 ~ 07-12 | 카메라 삼각대 고정. `e2p.py` → `detect_ground_3b1/3b2` **수동 1장씩** 실행. 줄자 대조 | `260701`, `260712` 문서 |
| 2026-07-11 | 3 슬라이스(u+50/150/230) 결과를 **손으로 합쳐** 5개짜리 JSON 제작 → 유니티 정렬 | **`anchor360_merged.json`** |
| 2026-07-12 | 공용 config 최종 수정(`jny`). `box 0.18→0.12`, `fire extinguisher 0.22→0.12` 로 추정 | config mtime |
| 2026-08-03 | 같은 수동 방식 재실행. 이때 이미 `box conf 0.13 D=6.88 m` 유령 발생 | `260803` 로그 |
| 2026-09-15 | **자동화**: `anchor360_live.py`(상주 데몬) + `anchor360_bridge.py`(UDP 중계) 신규.<br>이때 `yawOffsetDeg` 불일치가 처음 문서화됨 | 총정리 v1 |
| 2026-09-21 | **사진 → 영상 전환**: `pro2_auto_bg.py` 자리를 `stream_to_server.py` 로 교체.<br>u 규약 180° 차이, 피치 1.35° 차이 발견·보정 | 총정리 v2 |

## A-2. 3세대 파이프라인 비교

### 1세대 — 수동 (2026-07)

```
Pro2 촬영(사진) → PC ProStitcher stitch → 서버 업로드
  → [사람이] e2p.py 로 u+50 / u+150 / u+230 각각 1장씩 잘라 파일 저장
  → [사람이] detect_ground_3b1.py 를 슬라이스마다 실행, 콘솔 표를 눈으로 확인
  → [사람이] detect_send_3b2.py 로 JSON 뽑거나, 세 슬라이스 결과를 손으로 합침
  → relay_loop.py / send_to_unity.py 로 고정 JSON 반복 송출
  → 유니티에서 yawOffsetDeg 를 눈대중으로 맞춤
```
- 모델 로드가 **매 실행 8~9초**. 사진 1장 처리에 사람 개입 필수.
- 장점: 사람이 매 단계를 본다. 이상하면 그 자리에서 거른다.

### 2세대 — 자동 + 사진 (2026-09-15)

```
pro2_auto_bg.py (촬영 20 s + stitch) → 업로드
  → anchor360_live.py (상주, 폴더 감시 → 메모리 슬라이스 → 검출 → JSON)
  → anchor360_bridge.py (HTTP GET → UDP 5010)
  → Unity
```
- `detect_send_3b2.py` 를 상주형으로 재구성. **수식은 그대로.**
- 신규: `--drop-clipped`, `--max-dist`, 메모리 슬라이스, 모델 상주.
- 2026-09-21 실측에서 stitch 가 52 s 로 늘어 **큐가 0→38 로 발산**, 종단지연 13분.

### 3세대 — 자동 + 영상 (2026-09-21, 현행)

```
stream_to_server.py (카메라 인카메라 실시간 스티칭 RTMP → 프레임 → 업로드)
  → anchor360_live.py --u 230 --v -36.35 --pitch -35 --poll 0.1 --no-wait-stable
  → anchor360_bridge.py --poll 0.3
  → Unity
```
- stitch 단계 **소멸**. 종단지연 73 s → 0.5 s. 유실 61 % → 0.2 %.
- **서버 비전 코드는 `--no-wait-stable` 플래그 2줄 추가가 전부.**

---

# Part B. 비전 처리 — 구버전 vs 현행 (핵심)

## B-1. 거리 계산 코드는 동일하다 (코드 대조)

세 파일의 해당 함수를 나란히 놓으면 **연산 순서까지 같다.**

`detect_ground_3b1.py` / `detect_send_3b2.py`
```python
def pixel_to_ground(px, py):
    dx = (px - cx) / fx
    dy = -(py - cy) / fy
    dz = 1.0
    wy = cos_t * dy + sin_t * dz
    wz = -sin_t * dy + cos_t * dz
    wx = dx
    if wy >= -1e-6: return None
    t = -CAM_H / wy
    return wx * t, wz * t
```

`anchor360_live.py` (`GroundProjector.pixel_to_ground`)
```python
    dx = (px - self.cx) / self.fx
    dy = -(py - self.cy) / self.fy
    dz = 1.0
    wy = self.cos_t * dy + self.sin_t * dz
    wz = -self.sin_t * dy + self.cos_t * dz
    wx = dx
    if wy >= -1e-6: return None, wy
    t = -self.cam_h / wy
    return (wx * t, wz * t), wy
```

`anchor360_live.py` 의 docstring 도 이를 명시한다 —
*"검출 필터 / NMS / pixel_to_ground 수식 / JSON 스키마는 detect_send_3b2.py 와 동일하다.
(검증된 수식이라 한 줄도 바꾸지 않음)"*

**교차 검증**: 2026-09-21 `proj` 경로(사진, u+50, v−35) 로그의 3개 물체를 위 식으로 손계산하니
`fire extinguisher D=1.51 / chair D=0.75 / chair D=1.24` 가 나왔고, 로그값은
`1.49 / 0.75 / 1.25` 였다. **수식 재현 확인.**

## B-2. UniDepth 는 어디에 있나 — 이름이 만든 착각

착각의 출처는 **파일명과 conda 환경명**이다.

- 360 경로가 읽는 설정 파일 이름이 `unidepth_dual_yolo_config.yaml` 이다.
- 7월 명령어가 `conda activate unidepth` 로 시작한다.

그러나 **그 config 는 원래 GO1 로봇 앞 카메라용**이다. 프로젝트 문서
`unidepth_dual_yolo_config_260617.yaml` 을 열면 바로 드러난다.

```yaml
streams:
  camera_id: go1_front              # ← 로봇 앞 카메라
camera_intrinsics:                  # ← GO1 어안렌즈 캘리브레이션
  fx: 285.0   fy: 285.0   cx: 232.0   cy: 200.0
models:
  unidepth:
    model_name: .../unidepth-v2-vitb14
  finetune_detector: ...
  zeroshot_detector:
    weight: .../yolov8s-world.pt
```

그리고 `unidepth_dual_yolo_260617.py`(= 서버 포트 7864 `server_stream_depth_v2` 계통)만이
`UniDepthV2.from_pretrained(...)` 를 부르고, `calibrate_depth_to_cm()` 라는
**3차 다항식 + 어안 반경 보정**으로 cm 를 낸다. Redis 스트림 기반이고 로봇 전용이다.

**360 계열 3개 파일이 그 yaml 에서 읽는 항목은 5개뿐이다:**
`models.zeroshot_detector.weight` / `.conf`, `classes.zeroshot`, `group_mapping`,
`class_conf.zeroshot`, `fusion.iou`. `models.unidepth` 는 **읽지도 않는다.**

> **정리**: GO1 = 단안 깊이추정(UniDepth) + 보정식.
> 360 앵커 = 깊이추정 없음, 바닥평면 교차(단일 시점 기하).
> **둘은 전혀 다른 파이프라인이고, 360 쪽이 UniDepth 를 쓴 적은 한 번도 없다.**

## B-3. `pixel_to_ground` 가 실제로 하는 일

한 문장: **"물체는 바닥에 닿아 있다"는 가정 하나로 단안 이미지에서 거리를 뽑는다.**

1. equirect 를 `py360convert.e2p(fov 90×90, out 720×720)` 로 자른다.
   → 이 순간 슬라이스는 **합성 핀홀 카메라**가 되고, 내부 파라미터가 계산으로 확정된다.
   `fx = fy = 720/(2·tan45°) = 360`, `cx = cy = 360`. **캘리브레이션 불필요.**
2. bbox 의 **하단 중앙 픽셀** `((x1+x2)/2, y2)` 을 "물체가 바닥에 닿은 점"으로 본다.
3. 그 픽셀의 광선을 pitch(−35°)만큼 회전시켜 중력 좌표로 돌린다.
4. 카메라 높이 1.85 m 에서 그 광선이 바닥 y=0 과 만나는 지점을 구한다 → `(X, Z)`.
5. `D = √(X²+Z²)`.

**그래서 이 값이 맞으려면 5가지 전제가 전부 참이어야 한다.**

| # | 전제 | 깨지면 |
|---|---|---|
| 1 | 물체가 **바닥에 닿아** 있다 | 책상 위 물체·벽걸이 물체는 훨씬 멀게 나옴 |
| 2 | bbox 하단변이 **접지선과 일치** | 그림자·다리·가림에 따라 흔들림 |
| 3 | 카메라 높이 = **정확히 1.85 m** | 거리 전체가 선형으로 틀어짐 |
| 4 | 하향각 = **정확히 −35°**, roll = 0 | 방사방향으로 틀어짐 |
| 5 | 바닥이 **평면**이고 카메라 발밑과 같은 높이 | 단차·경사에서 틀어짐 |

**이 5개는 전부 손으로 정한 상수이고 매 프레임 측정되지 않는다.** 이것이 이 방식의
구조적 취약점이고, 7월이든 9월이든 똑같았다.

## B-4. 구조적 민감도 — 숫자로

`H`=1.85 m, `f`=360 px 일 때, 광선 하향각 θ 에 대한 거리 민감도는
`dD/dθ = (D² + H²)/H` 이고, 픽셀 1개는 `1/f` rad(=0.159°)에 해당한다.

| 실제 거리 D | bbox 하단 **1 px** 오차 | **5 px** 오차 | **1°** 오차 | 상대오차 /px |
|---|---|---|---|---|
| 0.75 m | 0.6 cm | 3.0 cm | 3.8 cm | 0.80 % |
| 1.00 m | 0.7 cm | 3.3 cm | 4.2 cm | 0.66 % |
| 1.50 m | 0.9 cm | 4.3 cm | 5.4 cm | 0.57 % |
| **1.85 m** | **1.0 cm** | **5.1 cm** | 6.5 cm | **0.56 %** ← 최소 |
| 2.50 m | 1.5 cm | 7.3 cm | 9.1 cm | 0.58 % |
| 3.00 m | 1.9 cm | 9.3 cm | 11.7 cm | 0.62 % |
| **5.00 m** | **4.3 cm** | **21.3 cm** | 26.8 cm | 0.85 % |
| 6.50 m | 6.9 cm | 34.3 cm | 43.1 cm | 1.06 % |
| **8.00 m** | **10.1 cm** | **50.6 cm** | 63.6 cm | 1.27 % |
| 11.00 m | 18.7 cm | 93.4 cm | 117.4 cm | 1.70 % |

**읽는 법 3가지.**

- **상대오차가 D = 카메라 높이(1.85 m) 에서 최소**다. `dD/D ∝ (D/H + H/D)`.
  7월에 줄자로 맞춰본 물체들이 정확히 1.2~1.7 m 에 있었다. **가장 잘 나오는 구간만 본 것이다.**
- **절대오차는 D² 로 커진다.** 5 m 짜리는 1 m 짜리보다 **6.5배** 민감하다.
  지금 유니티에서 어긋나 보이는 `box D=5.5`, `cardboard box D=5.3` 이 바로 그 구간이다.
- **`--max-dist 8` 은 너무 관대하다.** 5 px 흔들림을 15 cm 이내로 묶으려면 **D ≤ 4.1 m**,
  10 cm 이내로 묶으려면 **D ≤ 3.2 m** 여야 한다.

추가 민감도:

| 원인 | 영향 |
|---|---|
| 카메라 높이 2 cm 오차 | 모든 거리 **1.08 %** (D=5 m → 5.4 cm) |
| 카메라 높이 5 cm 오차 | 모든 거리 **2.70 %** (D=5 m → 13.5 cm) |
| 하향각 1° 오차 | D=2 m → 7 cm, D=5 m → 27 cm |
| **영상↔사진 피치 1.35° 차이** | D=1.25 m → 6.3 cm, D=2.5 m → 12.3 cm, **D=5 m → 36.2 cm** |

마지막 줄이 중요하다. `--v -36.35 --pitch -35` 보정을 **안 넣으면** 5 m 물체가 36 cm 틀어진다.
현행 실행 절차서에는 들어가 있다. **빠뜨리지 말 것.**

## B-5. 구버전 ↔ 현행 파라미터/입력 대조표

| 항목 | 7월 (수동) | 9월-15 (자동·사진) | 9월-21 (자동·영상, 현행) |
|---|---|---|---|
| 입력 equirect | ProStitcher `ai+topfixer=1`, 3840×1920, JPEG ~3.2 MB | 동일 | **인카메라 실시간 스티칭**, H.264 10 Mbps → JPEG q92, ~780 KB |
| 선명도 (라플라시안 분산) | 302~318 | 302~318 | **190** (0.65배) |
| 슬라이스 방법 | `e2p.py` → **JPEG 파일로 저장** → 다시 읽음 | **메모리** (JPEG 왕복 없음) | 메모리 |
| 슬라이스 수 | **3장** (u+50/150/230), 각각 따로 실행 | **1장** (u+50) | **1장** (u+230 = 사진 u+50) |
| `u` 규약 | ProStitcher 기준 | ProStitcher 기준 | **180° 이동** (인카메라 스티처 규약) |
| `v` (자르는 고저각) | −35 | −35 | **−36.35** (1.35° 보정) |
| `pitch` (바닥계산용) | −35 | −35 (= `--v`) | **−35** (`--v`와 분리) |
| `CAM_H` | 1.85 | 1.85 | 1.85 |
| 슬라이스 크기 | 720×720 | 720×720 | 720×720 |
| `imgsz` (추론) | 640 | 640 | 640 |
| 가중치 | `yolov8s-world.pt` | 동일 | 동일 |
| conf 임계값 | config (6월본: `box 0.18`, `FE 0.22`) → **7/12 변경** | config (`box 0.12`, `FE 0.12`) | 동일 |
| 접지잘림 필터 | **없음** | `--drop-clipped` | `--drop-clipped` |
| 최대거리 필터 | **없음** | `--max-dist 8` | `--max-dist 8` |
| 모델 로드 | **사진마다 8~9 s** | 기동 시 1회 | 기동 시 1회 |
| 유니티로 가는 것 | **손으로 합친 고정 JSON 5개** | 실시간 검출 6~8개 | 실시간 검출 7~8개, 초당 2.3회 갱신 |
| 유니티 갱신 주기 | 수동 (사실상 정지 화면) | 58 s (stitch 적체 시) | **0.44 s** |
| `yawOffsetDeg` | −35 / +150 / −45 (슬라이스별 눈대중) | −50 근처 | −50 근처 |

**JSON 스키마 변경**: 9월에 `conf`, `wy` 필드가 추가됐고 `id` 를 필터 **후에** 매기도록 바뀌었다.
유니티 `JsonUtility` 는 모르는 필드를 무시하므로 수신기는 무영향이다.

---

# Part C. "예전엔 정확했는데 지금은 왜?" — 원인 7가지

## C-1. (가장 큰 원인) 7월 유니티 입력은 실시간 검출이 아니었다

`C:\Users\asdfa\docx2026_code\anchor360_merged.json` (timestamp 1783844071 ≈ **2026-07-11**) 전문:

```json
"note": "u=0 front, +u=CCW. 5 objects (4 anchor + 1 center-cover). rotated to common frame.",
"detections": [
  {"name":"fire extinguisher", "pos3d":{"x":-1.107,"z":0.485}, "dist_m":1.209,
   "size":{"w":0.3,"h":0.3,"d":0.3}, "bbox_xyxy":[0,0,0,0], "u_deg":50,  "role":"anchor"},
  {"name":"box",               "pos3d":{"x":-1.582,"z":0.557}, "dist_m":1.677, ... "u_deg":50,  "role":"anchor"},
  {"name":"box",               "pos3d":{"x":-0.330,"z":-0.791},"dist_m":0.857, ... "u_deg":150, "role":"cover"},
  {"name":"fire extinguisher", "pos3d":{"x":0.615,"z":-0.869}, "dist_m":1.065, ... "u_deg":230, "role":"anchor"},
  {"name":"box",               "pos3d":{"x":0.602,"z":-1.367}, "dist_m":1.494, ... "u_deg":230, "role":"anchor"}
]
```

**이 파일이 말해주는 것 6가지:**

1. **물체가 5개뿐**이고 전부 **소화기·상자** — 바닥에 확실히 닿는, 윤곽이 뚜렷한 물체만.
2. **거리가 0.857 / 1.065 / 1.209 / 1.494 / 1.677 m** — **전부 §B-4 표의 최소오차 구간**.
   5 m 짜리 물체가 단 하나도 없다.
3. `size` 가 전부 `0.3/0.3/0.3` — **실제 계산값이 아니라 손으로 넣은 고정값**.
   (실제 `detect_send_3b2` 는 bbox×거리로 물체마다 다르게 낸다.)
4. `bbox_xyxy` 가 전부 `[0,0,0,0]` — **원시 출력이 아니다.**
5. `u_deg`, `role`, `note` 는 **스키마에 없는 필드** — 사람이 붙인 것.
6. `"rotated to common frame"` — 세 슬라이스 좌표를 **손으로 회전 합성**했다.
   그때 쓴 회전 오프셋은 **어느 파일에도 남아 있지 않다**(v1 §10.2 에도 같은 지적).

그리고 이 JSON 은 `send_to_unity.py` / `relay_loop.py` 로 **반복 송출**됐다.
`relay_loop.py` 는 **파일을 루프 바깥에서 1회만 읽는다** — 즉 유니티는 **정지된 5개 큐브**를 보고 있었다.

> **결론**: "예전엔 정확했다"는 **가장 좋은 거리대의, 가장 좋은 물체 5개를, 손으로 정렬해서,
> 정지 상태로 본 결과**다. 지금은 **자동 검출 전부를, 0.6~8 m 전 구간에서, 초당 2.3회 갱신하며**
> 본다. 같은 조건의 비교가 아니다.

## C-2. 거리대가 달라졌다

7월 검증 물체: **0.86 ~ 1.68 m** (§C-1) → 1 px 오차 = 0.6~0.9 cm
현행 전송 물체 (2026-09-21 로그): **0.69 / 1.21 / 1.36 / 2.00 / 2.31 / 2.49 / 5.54 m**
→ 5.54 m 짜리는 1 px 오차 = **5.2 cm**, 5 px 오차 = **26 cm**

가까운 것(사람·의자 0.69~2.49 m)은 지금도 잘 맞는다 — **사용자가 "사람 2개랑 의자 2개는 얼추
비슷하게 나온다"고 관찰한 것과 정확히 일치한다.** 어긋나는 것은 전부 먼 것이거나 §C-5 물체다.

## C-3. `yawOffsetDeg` — 방향 오차, 거리와 무관 (v1 §8 그대로 미해결)

카메라 1대면 `yaw = −u + C` 형태로 **하나의 공통 상수 C** 로 설명돼야 한다. 그런데:

| 슬라이스 | u | 과거 기록 yaw | 역산 C |
|---|---|---|---|
| 왼쪽 | 50 | −35 | **+15** |
| 가운데 | 150 | +150 | **−60** |
| 오른쪽 | 230 | −45 | **−175** |

**C 가 190° 범위로 흩어진다. 하나의 카메라 방향에서 나올 수 없는 값이다.**
특히 u+50 과 u+230 은 실제로 180° 반대인데 yaw 는 10° 밖에 차이가 안 난다.

이론값은 `R_anchor=0` 일 때 `yaw = −u` 이므로 u=50 → **−50**, 그리고 2026-09-15 에
실제로 가장 잘 맞았던 값이 **−50** 이었다. **질문은 "왜 −35 가 안 되나"가 아니라
"−35 가 애초에 맞았나"** 이고, 답은 거의 확실히 "아니다" 다.

또 하나: 2026-07-11 에 *"유니티 Transform 이 회전·이동을 같이 처리해서 재현 불가"* 라고
결론내고 검산을 포기했는데, **이건 사실이 아니다.** 수신기 코드는

```csharp
Vector3 local = yawFix * new Vector3(lx, 0f, lz);     // yawFix = Euler(0, yawOffsetDeg, 0)
Vector3 world = anchorTransform.TransformPoint(local);
```
즉 `world = R_anchor × (Yaw(yawOffsetDeg) × local) + P_anchor` 로 **완전히 재현 가능하다.**
이 오진 때문에 "계산으로 유도"가 폐기되고 "눈대중"이 표준이 되었고, 그 결과가 위 모순된 3값이다.

**눈대중의 구조적 함정**: 회전은 **반경 방향 오차를 고칠 수 없다.** 박스가 실제 3 m 인데
5.5 m 로 잡히면 아무리 돌려도 제자리에 안 온다. 그런데 눈으로 "제일 비슷한" 각도를 찾다 보면
그 반경 오차를 보상하려고 yaw 가 끌려간다. **2026-09-15 에 −45~−65 로 20° 범위가 흔들린 것
자체가 이 증상이다.**

## C-4. 인카메라 스티칭 선명도 0.65배

| | 라플라시안 분산 | 고주파 비율 | 검출 conf 평균 |
|---|---|---|---|
| 인카메라 (H.264 10 Mbps) | **190.5** | 0.2737 | 0.354 |
| ProStitcher (JPEG) | **302.2 / 318.3** | 0.3567 | — |

**원인은 압축이 아니다.** 비트레이트를 4배(40960 kbps)로 올려도 선명도는 **+9.3 %**,
검출 conf 는 +1.7 %(측정 노이즈 수준), 그런데 fps 는 15.1 → 5.4 로 떨어진다.
카메라가 30 fps 를 임베디드에서 내려고 **내부적으로 낮은 해상도로 합성한 뒤 업스케일**하는 것으로 보인다.

흐려지면 bbox 하단변이 몇 픽셀 흔들린다 → §B-4 표만큼 거리가 미끄러진다.
다만 **실측 영향은 평균 9.6 cm** 수준이다(§C-6). 즉 **이게 주범은 아니다.**

## C-5. 접지 가정이 깨지는 물체 — 복사기(`storage box`)가 대표

사용자가 "오차가 크다"고 화살표로 표시한 것이 `storage box`(= 복사기)와 소화기다.
`proj_debug.jpg` 를 열어 보면 **복사기는 흰색 캐비닛 위에 올라가 있다.** 바닥에 닿아 있지 않다.

- bbox 하단 = 복사기 바닥 = **캐비닛 상판 높이(≈0.7 m)** 의 픽셀
- `pixel_to_ground` 는 그 픽셀의 광선이 **y=0 바닥**과 만나는 점을 찾는다
- → 복사기는 **실제보다 훨씬 멀리** 찍힌다. 전제 1이 깨진 것이다.

같은 유형:
- 책상 위 상자, 선반 위 물건 → 멀게 나옴
- 벽에 걸린 물건 → 지평선 근처 광선이 되어 `--max-dist` 로 튕기거나 10 m 대로 나옴
- 큰 소파·테이블처럼 bbox 하단이 화면 밑변에 붙는 것 → `--drop-clipped` 가 걸러냄 (D=0.35~0.62 m 로 찍히던 것들)

로그를 보면 매 프레임 `[제외: 접지잘림] table D=0.62`, `open box D=0.58`, `cardboard box D=0.36`,
`[제외: >8m] storage box D=11.7` 이 반복된다. **필터는 정상 동작 중이고, 걸러지는 것들이
바로 이 유형이다.** 문제는 `storage box D=2.00`, `box D=5.5` 처럼 **필터를 통과하는데도
접지가 애매한 것들**이다.

## C-6. 영상 전환은 정확도 악화의 원인이 아니다 (결정적 증거)

2026-09-21, **같은 장면을 57초 간격으로** 두 경로로 찍어 물체별 `pos3d` 를 대조했다.

**① 보정 없음 (`--u 230 --v -35`)**

| 물체 | 인카메라 (x, z) | ProStitcher (x, z) | 3D 오차 |
|---|---|---|---|
| chair1 | (−0.18, 0.57) | (−0.20, 0.72) | 0.15 |
| chair2 | (−0.91, 0.65) | (−0.98, 0.77) | 0.14 |
| chair3 | (−1.57, 1.82) | (−1.72, 1.97) | 0.21 |
| fire extinguisher | (+1.15, 0.65) | (+1.30, 0.73) | 0.17 |
| 먼 박스 | (−1.49, 4.78) | (−1.56, 5.25) | 0.48 |
| | | **평균** | **22.9 cm** |

**② 보정 적용 (`--v -36.35 --pitch -35`)**

| 물체 | 인카메라 (x, z) | ProStitcher (x, z) | 3D 오차 |
|---|---|---|---|
| chair1 | (−0.19, 0.62) | (−0.20, 0.72) | 0.10 |
| chair2 | (−0.93, 0.70) | (−0.99, 0.77) | 0.09 |
| chair3 | (−1.64, 1.91) | (−1.70, 1.96) | **0.08** |
| fire extinguisher | (+1.18, 0.70) | (+1.30, 0.73) | 0.12 |
| cardboard box | (−1.60, 5.13) | (−1.57, 5.26) | 0.13 |
| box | (+2.19, 5.95) | (+2.19, 6.00) | **0.05** |
| | | **평균** | **9.6 cm** |

**9.6 cm 는 논문의 앵커 검출 정확도 11.2 cm 보다도 작다.**
검출 구조까지 일치했다 — 보정 후 **전송 7개 + 제외 1개**로 기준과 동일.

> **즉 오늘 사진(ProStitcher) 방식으로 되돌려도 유니티 화면은 거의 똑같이 어긋난다.**
> 되돌리는 것은 해결책이 아니고, 대신 종단지연 0.5 s → 73 s ~ 13분, 유실 0.2 % → 61 % 로
> 돌아간다. **영상 전환을 되돌리지 말 것.**

## C-7. 공유 config 임계값이 6월 이후 낮아졌다 (재확인 필요)

프로젝트에 보관된 **2026-06-17 판** config 와, 총정리 문서가 기록한 **현행 서버** config 를
비교하면 두 항목이 다르다.

| 클래스 | 260617 판 | 현행(문서 기록) | 영향 |
|---|---|---|---|
| `box` | **0.18** | **0.12** | conf 0.12~0.17 짜리가 새로 통과 |
| `fire extinguisher` | **0.22** | **0.12** | 저신뢰 소화기 통과 |

그리고 **현행 로그에 실제로 뜨는 문제 물체들의 conf 가 정확히 그 구간이다:**

```
box  D=5.54  conf=0.15        box  D=5.52  conf=0.13
box  D=5.57  conf=0.15        box  D=5.49  conf=0.17
box  D=6.29  conf=0.14        box  D=6.38  conf=0.12
storage box D=2.00 conf=0.16  storage box D=2.01 conf=0.17
```

**6월 설정(`box 0.18`)이었다면 이들 대부분이 아예 검출 목록에 없었다.**
2026-08-03 로그에도 이미 `box conf 0.13 D=6.88 m`, `box conf 0.17 D=7.12 m` 같은
쓰레기가 나오고 있다 — **7월 12일 config 수정 직후부터 시작된 현상**과 시점이 맞는다.

config 는 **소유자 `jny`, 권한 777** 인 팀원 공유 파일이다. 팀원이 바꾸면
**360 검출이 말없이 달라진다.** (v1/v2 두 문서 모두 이 위험을 명시하고 있다.)

> ⚠ 이 절은 "프로젝트에 보관된 6월 스냅샷 ↔ 문서에 옮겨적은 현행값" 비교다.
> **서버의 현재 yaml 원본을 받아 확인해야 확정**이다(§F 요청목록 2번).

---

# Part D. 지금 할 것 — 우선순위

> 원칙: **소프트웨어·설정 먼저, 하드웨어(카메라 각도·삼각대)는 맨 마지막.**
> 2026-09-15 픽셀 대조에서 카메라 물리각은 **1~2° 이내**로 확인됐고, 이는 −35↔−50 의
> 15° 차이(≈94 px, 프레임 폭 13 %)를 설명할 수 없다. 하드웨어는 이미 용의선상에서 빠져 있다.

### D-0. 10분 안에 끝나는 것 3개 — 이것부터

1. **`--max-dist 8` → `4` 로 낮춰서 한 번 돌려본다.**
   근거: 5 px bbox 흔들림이 15 cm 이내가 되는 경계가 D=4.1 m.
   5 m 대 `box` 유령이 전부 사라지고 남은 큐브만 보면 **방향(yaw) 문제가 단독으로 드러난다.**
   ```
   python3 anchor360_live.py --u 230 --v -36.35 --pitch -35 \
     --poll 0.1 --no-wait-stable --drop-clipped --max-dist 4 --debug-img
   ```
2. **유니티 `Anchor360` 오브젝트의 Inspector 값을 전부 읽어 적는다.**
   Position (x,y,z) / **Rotation Y** / yawOffsetDeg / invertX / distanceScale / floorWorldY.
   특히 **Rotation Y 는 v1 §13 A-1, v2 §13 B-1 에서 두 번 "아직 안 읽었다"로 남아 있다.**
   `yaw = −u − R_anchor` 의 마지막 미지수다. **스크린샷으로 박제할 것.**
3. **카메라 실제 높이를 줄자로 다시 잰다** (렌즈 중심 기준).
   5 cm 만 틀려도 모든 거리가 2.7 % 틀어진다. 유니티 `Anchor360` Position Y 와 일치해야 한다.

### D-1. 합성 JSON 2점 프로브 (v1 §8.8 — 여전히 유효, 지금이 하기 더 쉽다)

검출 오차와 눈대중을 **완전히 제거하고** 수신기의 실제 변환을 확정한다.

```bash
python3 -c "
import json
pkt={'timestamp':'0','camera_id':'anchor360','frame':'anchor360','detections':[
 {'id':1,'name':'PROBE_FWD','group':'HARD_OBSTACLE','pos3d':{'x':0.0,'y':0.0,'z':2.0},
  'dist_m':2.0,'size':{'w':0.3,'h':0.3,'d':0.3},'bbox_xyxy':[0,0,0,0],'ground_clipped':False},
 {'id':2,'name':'PROBE_RIGHT','group':'HARD_OBSTACLE','pos3d':{'x':2.0,'y':0.0,'z':0.0},
  'dist_m':2.0,'size':{'w':0.3,'h':0.3,'d':0.3},'bbox_xyxy':[0,0,0,0],'ground_clipped':False}]}
open('probe.json','w').write(json.dumps(pkt))"
python relay_loop.py probe.json 192.168.50.244 5010 1.0
```

`yawOffsetDeg = 0` 으로 두고 송출한 뒤 유니티에서 `Obs360_PROBE_FWD_1` 의 Transform Position 을 읽으면

```
world_FWD − P_anchor = R_anchor × (0, 0, 2)
```

이 벡터의 방향이 **`R_anchor` 의 실측값**이다. `PROBE_RIGHT` 까지 보면 **부호(좌우 뒤집힘)와
스케일(distanceScale)** 까지 한 번에 확정된다. 결과가 나오면 `yaw = −u − R_anchor` 로
**세 슬라이스 값을 계산으로 뽑을 수 있다.** ※ `relay_loop.py` 는 **랩실 내부망 PC**에서 돌려야 한다.

### D-2. 대응점 기반 yaw 산출 (근본 해결)

대응점 **1개**면 회전이 풀린다.
```
yaw = atan2(맵에서 앵커→물체 방향) − atan2(슬라이스 로컬 x, z)
```
**2개 이상**이면 최소제곱으로 풀리고 **스케일 오차까지 같이 나온다.**
잔차가 크면 *"yaw 문제가 아니라 거리 문제"* 라는 것이 드러난다 —
**눈대중으로는 절대 갈라지지 않는 구분이다.**

절차: ① 줄자로 벽 기준 좌표를 아는 물체 2개를 1~2 m 복도 바닥에 둔다(소화기·상자,
conf 0.5 이상, 접지 선명한 것) ② 한 번 검출 ③ yaw 와 스케일 계산 ④ Inspector 에 고정.

### D-3. GT 대조 — 어느 스티처가 참 수평인가 (v2 §8.5, 미해결)

현행 1.35° 보정은 **ProStitcher 를 기준으로 맞춘 것**이지 줄자 GT 에 맞춘 것이 아니다.
그런데 안정화 실험(v2 §7.6)은 **인카메라 쪽이 더 정확히 수평일 가능성**을 시사한다
(안정화 OFF 시 NCC 0.88 → 0.39 붕괴).

**만약 인카메라 쪽이 참이라면, 지금 파이프라인에 1.35° 기울기가 들어가 있다는 뜻이다**
— D=5 m 에서 36 cm. 확인 방법: 줄자로 잰 물체 2개를 시야에 두고 두 경로에서 D 를 재어
GT 와 대조. **D-2 와 같이 하면 촬영 한 번으로 끝난다.**

### D-4. 접지 가정을 안 지키는 물체 처리

- **복사기(`storage box`) 는 애초에 이 방식으로 못 잡는다.** 캐비닛 위에 있다.
  → ① 앵커/기준 물체에서 제외하거나 ② `class_conf.zeroshot.storage box` 를 올려 배제하거나
    ③ 맵에 정적 장애물로 직접 등록.
- 소화기는 바닥에 있으므로 유효하다. **단 "실제 위치"를 줄자로 확정해야** GT 로 쓸 수 있다.

### D-5. 검출 모델 업그레이드 — 그 다음

`yolov8s-world` → `yolov8x-worldv2`. **시간 예산은 충분하다** (서버 총 0.050 s 중 추론 0.010 s,
이론 20 fps 처리 가능한데 실제 유입 4.31 fps — **4.6배 여유**).

주의 3가지:
- config 는 공유 파일이므로 **고치지 말고** `anchor360_live.py --weight /경로/yolov8x-worldv2.pt` 로 오버라이드.
- CLIP 캐시(ViT-B-32, 338 MB)는 **CWD/weights/clip** 에 캐시된다. 스크립트가 자기 디렉터리로
  `chdir` 하므로 정상이나, 다른 경로에서 띄우면 다시 받는다.
- 바꾸기 **전후로 §C-6 방식의 좌표 대조를 한 번 해둘 것.** 모델이 바뀌면 bbox 가 달라지고
  `pos3d` 도 미세하게 달라진다. 논문 수치와의 관계를 기록해둬야 한다.

**단, A-0(로봇 검출 확인)이 먼저다.** `robot` / `quadruped robot` / `robot dog` 임계값 0.25 가
**전부 미검증**이고, GO1 을 못 잡으면 치명적이다.

### D-6. 하지 말 것

| 항목 | 이유 |
|---|---|
| 사진 방식으로 되돌리기 | §C-6. 정확도는 그대로고 지연만 146배 나빠진다 |
| 비트레이트 올리기 | 4배 부어서 선명도 +9.3 %, fps 1/3. Farsight 천장 5~7 Mbps |
| 안정화(`--no-stab`) 끄기 | 정합 NCC 0.88 → 0.39 붕괴 |
| 유선 직결 | 선명도 원인이 링크가 아니라 인카메라 스티처. 카메라-PC 거리 문제도 있음 |
| config 직접 수정 | `jny` 소유 공유 파일. 로봇 파이프라인에 영향 |
| 슬라이스 720px / fov 90 변경 | `fx=fy=360` 전제와 맞물림. 거리 전체가 틀어짐 |
| 카메라 각도/삼각대 먼저 만지기 | 픽셀 대조상 1~2° 이내. 소프트웨어 원인이 먼저다 |

---

# Part E. 팀원 각자 돌리기 — 이식(배포) 가이드

## E-1. 지금 고정되어 있는 것 — 전수 목록

### PC 쪽

| 파일 | 고정된 것 | 인자로 바꿀 수 있나 |
|---|---|---|
| `stream_to_server.py` | 카메라 IP, 업로드 URL, capture_id, camera_id | **✅ 전부 `--ip / --upload / --capture-id / --camera-id`** |
| `preview_probe.py` | 카메라 IP | **✅ `--ip`** |
| `anchor360_bridge.py` | 서버 URL, 유니티 IP, 포트 | **✅ `--server / --unity-ip / --port`** |
| `pro2_auto_bg.py` | `CAMERA_IP`, `SERVER_IP`, `SERVER_PORT`, `CAMERA_ID` (모듈 상수) | ❌ **코드 수정 필요** |
| `pro2_auto_bg.py` | `_TRT_BACKUP_DIR = r"C:\Users\asdfa\docx2026_code\trt_cache"` | ❌ **계정명 하드코딩** |
| `stitch_prostitcher.py` | `DEFAULT_STITCHER = r"C:\Program Files (x86)\Insta360Stitcher\tools\prostitcher\ProStitcher.exe"` | ✅ `--stitcher` (기본 설치면 동일) |
| `send_to_unity.py` | `anchor360_merged.json` 파일명 고정 | ❌ (하지만 일회용 도구) |
| 실행 디렉터리 | `cd C:\Users\asdfa\docx2026_code` / `cd "C:\Users\asdfa\physical mk2\360camera_work"` | 문서상 절대경로 — 상대경로로 안내 필요 |

### 서버 쪽

| 항목 | 고정된 것 | 비고 |
|---|---|---|
| `anchor360_live.py` 기본값 | `~/capstone-db/image_server/pano_captures/pro2_anchor`, `~/capstone-db/docx2026/live_out` | ✅ **`~` 기반이라 계정만 바꾸면 그대로 동작** |
| `anchor360_live.py --cfg` 기본값 | `~/capstone-db/image_server/unidepth_dual_yolo_config.yaml` | ✅ `~` 기반 |
| **config yaml 내부** | `weight: /home/dg/capstone-db/image_server/yolov8s-world.pt` | ❌ **절대경로. `dg` 고정** |
| CLIP 캐시 | `CWD/weights/clip/ViT-B-32.pt` (338 MB) | 스크립트가 `chdir` 함. 계정별로 1회 다운로드 |
| `pano_receiver.py` | `PANO_ROOT = "pano_captures"` (상대) | ✅. 단 **포트 7866 은 1명만 점유 가능** |
| venv | `~/capstone-db/venv_image` | `dg` 계정 소유 |
| 포트 | 7866(pano), 5010(Anchor360 UDP), 20000/1935(카메라) | **동시에 여러 명이 못 쓴다** |
| 카메라 | **제어 세션 1개만** | **근본적으로 동시 사용 불가** |

### ⚠ 먼저 짚고 갈 제약 — "각자 동시에" 는 불가능하다

| 자원 | 제약 |
|---|---|
| **Insta360 Pro2** | 제어 클라이언트 **1개만** 붙는다. 2번째는 `already connected by another`. `pro2_auto_bg` / `stream_to_server` / `preview_probe` / Insta360 Pro 앱 전부 **택일** |
| **서버 포트 7866** | `pano_receiver` 1 인스턴스. 2번째는 `Address already in use` |
| **`pano_captures/pro2_anchor/LIVE_STREAM/`** | 같은 폴더를 덮어쓴다. 2명이 올리면 섞인다 |
| **UDP 5010** | 유니티 수신기가 스냅샷 방식. 2명이 쏘면 큐브가 번갈아 뒤집힌다 |

→ **"각자 돌린다"의 현실적 의미는 두 가지 중 하나다.**
> **(가) 교대 운용** — 환경은 각자 갖되, 카메라·서버는 한 번에 한 명. (권장, 지금 필요한 것)
> **(나) 카메라 없는 오프라인 개발** — `anchor360_live.py --replay <equirect.jpg>` 로
> 저장된 사진 1장을 돌린다. 이건 **몇 명이든 동시에 가능**하고, 알고리즘·파라미터 실험에 충분하다.
> `capture_id` 와 `--out-dir` 만 사람마다 다르게 주면 서버에서도 안 부딪힌다.

## E-2. venv 를 복사할 수 있나 — 정확한 답

**"그대로 복사해서 다른 계정/경로에 붙이면 안 된다."** 이유 4가지:

1. **`pyvenv.cfg`** 에 base 파이썬의 **절대경로**(`home = /usr/bin`)와 버전이 박혀 있다.
2. **`bin/activate`** 에 `VIRTUAL_ENV="/home/dg/capstone-db/venv_image"` 가 **문자열로** 박혀 있다.
3. **`bin/` 의 콘솔 스크립트**(pip, uvicorn, yolo 등) 첫 줄이
   `#!/home/dg/capstone-db/venv_image/bin/python3` 절대 shebang 이다.
4. **컴파일된 확장모듈(`.so`)** 이 파이썬 마이너 버전·glibc·CUDA/torch 빌드에 묶여 있다.
   `torch`, `opencv`, `py360convert` 계열이 전부 여기 해당한다.

**단, 세부적으로는 이렇게 갈린다:**

| 상황 | 되나 |
|---|---|
| 같은 서버, 같은 절대경로, 다른 사용자가 **읽기만** | **✅ 된다.** venv 활성화에 쓰기 권한 불필요 |
| 같은 서버, **다른 경로**로 복사 | ❌ (1~3 때문에 깨짐. `sed` 로 고치면 되긴 함) |
| 다른 서버, 같은 OS/파이썬 버전 | △ (경로 수정 + 라이브러리 버전 운 좋으면) |
| 다른 OS / 다른 파이썬 마이너 버전 | ❌ |
| conda 환경 (`/home/jny/miniconda3/envs/unidepth`) | **`conda-pack` 쓰면 ✅** |

## E-3. 배포 3안 — 비교와 권장

### 【안 1】 공용 venv 읽기전용 개방 (★ 권장 — 가장 싸고 안전)

venv 를 복사하지 않는다. `dg` 것 하나를 **모두가 활성화**한다.

```bash
# dg 계정에서 1회
chmod o+x  /home/dg                                   # 통과 권한만
chmod -R o+rX /home/dg/capstone-db/venv_image         # 읽기+디렉터리 진입
chmod o+r  /home/dg/capstone-db/image_server/yolov8s-world.pt
chmod o+r  /home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml
```

```bash
# 팀원 각자
. /home/dg/capstone-db/venv_image/bin/activate        # 그대로 쓴다 (쓰기 불필요)
mkdir -p ~/capstone-db/docx2026 ~/capstone-db/image_server/pano_captures/pro2_anchor
cp /home/dg/capstone-db/docx2026/anchor360_live.py   ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/e2p.py              ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/relay_loop.py       ~/capstone-db/docx2026/
cp /home/dg/capstone-db/image_server/pano_receiver.py ~/capstone-db/image_server/
cp /home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml ~/capstone-db/image_server/   # 사본!
```

- 장점: 디스크 1벌, CUDA/torch 검증 1회, 버전 드리프트 0.
- 주의 1: **config 는 사본을 쓴다.** 그래야 `jny` 가 원본을 바꿔도 안 흔들린다(§C-7).
  단 사본 안의 `weight:` 경로를 `/home/dg/.../yolov8s-world.pt` 로 두거나 자기 사본을 가리키게 고친다.
- 주의 2: **CLIP 캐시**는 `CWD/weights/clip/` 이므로 `~/capstone-db/docx2026/weights/clip/` 로
  `dg` 것을 복사해두면 338 MB 재다운로드를 피한다.
  ```bash
  mkdir -p ~/capstone-db/docx2026/weights/clip
  cp /home/dg/capstone-db/docx2026/weights/clip/ViT-B-32.pt ~/capstone-db/docx2026/weights/clip/
  ```
- 주의 3: `dg` 가 venv 를 갱신하면 전원에게 즉시 반영된다. **누가 언제 바꿨는지 기록할 것.**

### 【안 2】 requirements 로 각자 재구성 (정석, 재현성 최고)

```bash
# dg 계정에서 1회 — 이 파일을 팀에 배포
. ~/capstone-db/venv_image/bin/activate
pip freeze > ~/capstone-db/requirements-server.txt
python -c "import sys; print(sys.version)"   # 파이썬 버전도 같이 적어둘 것
nvidia-smi | head -3                          # CUDA 드라이버 버전
```

```bash
# 팀원 각자
python3 -m venv ~/capstone-db/venv_image
. ~/capstone-db/venv_image/bin/activate
pip install --upgrade pip
pip install -r requirements-server.txt
```

- ⚠ **torch 는 `pip freeze` 만으로 부족하다.** CUDA 빌드가 인덱스 URL 에 묶여 있어서
  `pip install torch --index-url https://download.pytorch.org/whl/cu121` 처럼
  **정확한 인덱스**를 requirements 상단에 주석으로 적어둬야 한다.
  (현재 서버 CUDA 버전을 `nvidia-smi` 로 확인해서 적을 것.)
- ⚠ 디스크: torch + ultralytics + opencv 만 해도 **1인당 5~7 GB**. 3~4명이면 20 GB 넘는다.
  서버 여유 확인 필수(`df -h ~`).
- 속도를 원하면 `uv` 를 쓴다 (`uv venv && uv pip sync requirements-server.txt`). 10~30배 빠르다.

### 【안 3】 그대로 복사 + 경로 치환 (급할 때만)

```bash
cp -a /home/dg/capstone-db/venv_image ~/capstone-db/venv_image
cd ~/capstone-db/venv_image
# 1) pyvenv.cfg 는 보통 그대로 둬도 됨 (base 파이썬 경로라서)
# 2) activate 계열의 VIRTUAL_ENV 치환
sed -i "s#/home/dg/capstone-db/venv_image#$HOME/capstone-db/venv_image#g" bin/activate bin/activate.csh bin/activate.fish
# 3) bin/* 의 절대 shebang 치환 (텍스트 파일만)
grep -rIl "^#!/home/dg/capstone-db/venv_image" bin/ | \
  xargs sed -i "1s#^#!/home/dg/capstone-db/venv_image#\#!$HOME/capstone-db/venv_image#"
# 4) 검증
. bin/activate && python -c "import torch, cv2, py360convert, ultralytics, yaml; print('OK', torch.__version__, torch.cuda.is_available())"
```

- 4번 검증이 통과하면 쓸 만하다. 실패하면 **안 2로 간다.**
- `virtualenv --relocatable` 은 **폐기된 기능이다. 쓰지 말 것.**
- 디스크는 안 2와 똑같이 든다. **장점이 "빠르다" 하나뿐**이라 우선순위가 낮다.

### conda 환경(`unidepth`)이 필요한 경우

7월 문서의 `conda activate unidepth` 는 **GO1/UniDepth 계통**이다. 360 경로에는 필요 없다.
그래도 옮겨야 한다면 **`conda-pack`** 이 정답이다.
```bash
conda install -n base conda-pack
conda pack -n unidepth -o unidepth.tar.gz     # 경로 독립 아카이브
# 받는 쪽
mkdir -p ~/envs/unidepth && tar xzf unidepth.tar.gz -C ~/envs/unidepth
source ~/envs/unidepth/bin/activate && conda-unpack
```

## E-4. PC 쪽 체크리스트 (팀원용)

**1) 설치**
```
python --version                 # 3.12 권장 (현재 3.12.10)
pip install requests opencv-python
```
ProStitcher 는 **영상 경로에서는 불필요**. 사진 경로도 쓰려면 Insta360 Stitcher 를 기본 경로에 설치.

**2) 받을 파일 (영상 경로 최소 구성)**
```
stream_to_server.py      ← 스트림 공급
preview_probe.py         ← 스트림 확인 (선택)
anchor360_bridge.py      ← 서버 JSON → 유니티 UDP
260921 카메라 최종정리2.txt   ← 실행 절차서(정본)
```
사진 경로까지 쓰려면 추가: `pro2_auto_bg.py`, `stitch_prostitcher.py`, `trt_cache/*.trt`

**3) 각자 바꿔야 하는 값 — 딱 3개**

| 무엇 | 지금 값 | 어떻게 |
|---|---|---|
| 카메라 IP | `192.168.100.124` | `stream_to_server.py --ip <IP>` (또는 `DEF_IP` 수정) |
| 서버 주소 | `210.110.250.33:7866` | `--upload http://<IP>:7866/upload_pano`, `--server http://<IP>:7866/anchor360_latest` |
| 유니티 PC IP | `192.168.50.244` | `anchor360_bridge.py --unity-ip <IP>` (유니티 PC 본인이면 `127.0.0.1`) |

**4) `pro2_auto_bg.py` 를 쓸 경우 추가 수정 (코드 직접)**
```python
CAMERA_IP = "192.168.100.124"                                  # ← 각자
SERVER_IP = "210.110.250.33"                                   # ← 각자
_TRT_BACKUP_DIR = r"C:\Users\asdfa\docx2026_code\trt_cache"    # ← 자기 계정 경로로
```

**5) 방화벽**: 유니티 PC 에서 **UDP 5010 인바운드 허용**. 이거 빠지면 브릿지 `NEW` 는 뜨는데
유니티 `RX` 가 안 뜬다.

**6) 실행 (영상 경로)**
```
# 터미널 1 — 브릿지
python anchor360_bridge.py --unity-ip <유니티IP> --poll 0.3
# 터미널 2 — 스트림
python stream_to_server.py --fps 5
```

## E-5. 서버 쪽 체크리스트 (팀원용)

```bash
# 0) 환경 (안 1 기준)
. /home/dg/capstone-db/venv_image/bin/activate
python -c "import torch,cv2,py360convert,ultralytics,yaml,fastapi,uvicorn; print('OK', torch.cuda.is_available())"

# 1) 개인 작업 디렉터리
mkdir -p ~/capstone-db/{docx2026,image_server/pano_captures/pro2_anchor}
mkdir -p ~/capstone-db/docx2026/weights/clip

# 2) 스크립트/설정/가중치 사본
cp /home/dg/capstone-db/docx2026/{anchor360_live.py,e2p.py,relay_loop.py} ~/capstone-db/docx2026/
cp /home/dg/capstone-db/image_server/pano_receiver.py ~/capstone-db/image_server/
cp /home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml ~/capstone-db/image_server/
cp /home/dg/capstone-db/docx2026/weights/clip/ViT-B-32.pt ~/capstone-db/docx2026/weights/clip/

# 3) 사본 config 의 weight 경로 확인 — 절대경로라 그대로 두거나 자기 사본으로
grep -n "weight:" ~/capstone-db/image_server/unidepth_dual_yolo_config.yaml

# 4) 카메라 없이 동작 확인 (── 이게 핵심. 몇 명이든 동시 가능 ──)
cd ~/capstone-db/docx2026
python3 anchor360_live.py \
  --replay /home/dg/capstone-db/image_server/pano_captures/pro2_anchor/<어떤폴더>/equirect.jpg \
  --u 230 --v -36.35 --pitch -35 --drop-clipped --max-dist 8 --debug-img
```

기동 로그에서 **이 두 줄이 서로 다르게** 찍히는지 반드시 확인:
```
  슬라이스   : u=+230.0deg, v=-36.4deg, 720x720
  카메라     : 높이 1.85m, 하향 -35.0deg
```
같으면 `--pitch` 를 안 준 것이고, **바닥 계산까지 1.35° 틀어진다.**

**실시간(카메라 연동)까지 갈 때만** — 포트 충돌 때문에 **한 번에 한 명**:
```bash
# 터미널 1
cd ~/capstone-db/image_server && python3 pano_receiver.py      # 포트 7866 — 1명만
# 터미널 2
cd ~/capstone-db/docx2026
python3 anchor360_live.py --u 230 --v -36.35 --pitch -35 \
  --poll 0.1 --no-wait-stable --drop-clipped --max-dist 8 --debug-img
```

> ⚠ `--save-slice` 는 붙이지 말 것. 4.3 fps 면 **시간당 1.5 GB** 쌓인다.
> ⚠ `--poll 0.1` 과 `--no-wait-stable` 을 빠뜨리면 초당 0.6장밖에 처리 못 한다.

**패치 적용 확인** (argparse 는 하이픈, 코드는 밑줄이라 표기가 다름):
```bash
grep -n -- "no.wait.stable" ~/capstone-db/docx2026/anchor360_live.py   # 두 줄 나와야 정상
# grep -c no_wait_stable 은 1이 정상. 2 아님.
```

## E-6. 코드에 넣을 최소 수정안 (환경변수 1단계)

지금 구조에서 **20줄 미만**으로 개인화를 끝낼 수 있다. 각 스크립트 상단 상수만 바꾸면 된다.

```python
# stream_to_server.py / anchor360_bridge.py / preview_probe.py / pro2_auto_bg.py 공통
import os
DEF_IP       = os.getenv("PRO2_CAMERA_IP",  "192.168.100.124")
SERVER_IP    = os.getenv("CAPSTONE_SERVER", "210.110.250.33")
DEF_UPLOAD   = os.getenv("PANO_UPLOAD_URL", f"http://{SERVER_IP}:7866/upload_pano")
DEF_SERVER   = os.getenv("ANCHOR_JSON_URL", f"http://{SERVER_IP}:7866/anchor360_latest")
DEF_UNITY_IP = os.getenv("UNITY_IP",        "192.168.50.244")

# pro2_auto_bg.py 전용
_TRT_BACKUP_DIR = os.getenv(
    "TRT_BACKUP_DIR",
    os.path.join(os.path.expanduser("~"), "docx2026_code", "trt_cache"))
```

팀원은 파이썬을 안 건드리고 배치파일 하나만 갖는다.
```bat
:: run_env.bat
set PRO2_CAMERA_IP=192.168.100.124
set CAPSTONE_SERVER=210.110.250.33
set UNITY_IP=127.0.0.1
```

서버 쪽은 이미 `~` 기반이라 수정이 거의 필요 없다. 유일한 개선은
`anchor360_live.py` 의 `--out-dir` / `--capture-id` 를 **사람마다 다르게 주는 습관**이다
(동시 실험 시 서로 덮어쓰지 않게).

## E-7. 배포 꾸러미 권장 구성

```
360camera_dist/
├─ README_팀원용.md                      ← E-4, E-5 를 그대로
├─ pc/
│   ├─ stream_to_server.py
│   ├─ preview_probe.py
│   ├─ anchor360_bridge.py
│   ├─ run_env.bat.sample
│   ├─ requirements-pc.txt              ← requests, opencv-python
│   └─ photo_mode/                      ← 사진 경로 폴백용
│       ├─ pro2_auto_bg.py
│       ├─ stitch_prostitcher.py
│       └─ trt_cache/*.trt
├─ server/
│   ├─ anchor360_live.py
│   ├─ pano_receiver.py                 ← ★ 서버 현행본 (원자적 쓰기 + /anchor360_latest 포함)
│   ├─ e2p.py
│   ├─ relay_loop.py
│   ├─ unidepth_dual_yolo_config.yaml   ← ★ 스냅샷 사본 (jny 원본 변경 차단)
│   ├─ requirements-server.txt          ← ★ pip freeze 결과
│   └─ setup_server.sh                  ← E-5 절차 자동화
├─ unity/
│   └─ Anchor360ObstacleReceiver.cs     ← 참고용(무수정)
└─ docs/
    ├─ 260921 카메라 최종정리2.txt        ← 실행 절차서(정본)
    ├─ 260921_..._총정리_v2.md
    └─ 260915_..._총정리.md             ← 논문 기준 구성. 폐기 금지
```
★ 표시 3개가 **지금 폴더에 없거나 낡은 것**이다(§F).

---

# Part F. 필요한 파일 / 정보 (요청)

## F-1. 지금 폴더에 없어서 확인 못 한 것

| # | 무엇 | 왜 필요한가 | 어떻게 |
|---|---|---|---|
| 1 | **서버 현행 `pano_receiver.py`** | 폴더의 사본이 **낡았다**. 원자적 쓰기(`.part`→`os.replace`)도, `/anchor360_latest` GET 도 없다. 이대로 팀원에게 주면 브릿지가 안 돈다 | `scp -P 8522 dg@210.110.250.33:~/capstone-db/image_server/pano_receiver.py .` |
| 2 | **서버 현행 `unidepth_dual_yolo_config.yaml`** | §C-7 (`box 0.18→0.12`) 확정용. **현재 분석은 6월 스냅샷 vs 문서 기록 비교라 추정 단계** | `scp -P 8522 dg@210.110.250.33:~/capstone-db/image_server/unidepth_dual_yolo_config.yaml .` |
| 3 | **`pip freeze` 결과 + 파이썬/CUDA 버전** | 안 2 배포에 필수 | `. venv_image/bin/activate && pip freeze > req.txt; python -V; nvidia-smi \| head -3` |
| 4 | **`anchor360_latest_260921.jpg`, `KakaoTalk_20260921_174546411.jpg`** | 오차 화살표를 표시한 그 사진. 어떤 물체가 얼마나 어긋났는지 정량화 가능 | 폴더에 넣어주기 |
| 5 | **유니티 `Anchor360` Inspector 전체 스크린샷** | Position / **Rotation Y** / yawOffsetDeg / invertX / distanceScale / floorWorldY. **`yaw = −u − R_anchor` 의 마지막 미지수** | 유니티에서 캡처 |
| 6 | **줄자 실측 GT 2점 이상** | D-2/D-3 의 전제. 이게 없으면 "yaw 문제인지 거리 문제인지" 영원히 못 가른다 | 소화기·상자를 1~2 m 복도 바닥에 두고 벽 기준 실측 |
| 7 | 서버 `~/capstone-db/docx2026` 파일 목록 | `ls -la` 만이라도. 배포 꾸러미에 빠진 게 없는지 확인 | `ls -la ~/capstone-db/docx2026 ~/capstone-db/image_server` |
| 8 | (선택) `yolov8s-world.pt` 크기/해시 | 팀원 배포 시 같은 가중치인지 확인 | `ls -l`, `md5sum` |

## F-2. 이미 확인했고 **요청 불필요**한 것

| 파일 | 어디서 확인 |
|---|---|
| `unidepth_dual_yolo_260617.py`, `server_stream_depth_v2_260617.py` | **프로젝트 문서에 있음** — 읽었다. GO1 전용 UniDepth 경로 확정 |
| `unidepth_dual_yolo_config_260617.yaml` | **프로젝트 문서에 있음** — 읽었다 (§C-7 비교 근거) |
| `stitch_prostitcher.py`, `pro2_auto.py`, `pro2_pipeline.py` | `docx2026_code` 폴더에 있음 |
| `anchor360_merged.json`, `obstacles_360.json` | `docx2026_code` 폴더에 있음 — **§C-1 의 결정적 증거** |
| `anchor360_live.py` | `360camera_work` 폴더 사본이 **현행본**(`--no-wait-stable` 포함) |
| `detect_ground_3b1.py`, `detect_send_3b2.py`, `detect_send_3b2_timed.py` | `360camera_work` 폴더에 있음 — 수식 대조 완료 |
| `Anchor360ObstacleReceiver.cs` | `360camera_work` 폴더에 있음 — 좌표 변환 확인 완료 |

---

# Part G. 부록 — 파일별 1줄 요약 (360camera_work 전체)

## 현행 (루트)

| 파일 | 역할 | 세대 |
|---|---|---|
| `stream_to_server.py` | 카메라 RTMP → 최신 프레임 1장 → JPEG → 서버 업로드. **리더 스레드로 적체 방지** | 3세대 (현행) |
| `preview_probe.py` | 스트림 확인용 계측 도구. `--measure N` 으로 해상도/fps/첫 프레임 | 3세대 보조 |
| `anchor360_live.py` | **서버 상주 데몬.** 폴더 감시 → 메모리 슬라이스 → YOLO-World → 필터 → 바닥평면 → JSON | 2~3세대 공용 |
| `anchor360_bridge.py` | 서버 JSON GET → 유니티 UDP 5010. **하트비트 겸용** | 2~3세대 공용 |
| `pano_receiver.py` | FastAPI 수신 서버. ⚠ **폴더 사본은 낡음** | 1~3세대 공용 |
| `pro2_auto_bg.py` | 사진 촬영 + 백그라운드 stitch. **영상 경로에선 미사용, 논문 기준 구성이라 보존** | 1~2세대 |
| `detect_ground_3b1.py` | 검출 → 콘솔 표(X/Z/D/wy) + bbox 그린 jpg. **눈 확인용** | 1세대 |
| `detect_send_3b2.py` | 검출 → JSON + UDP **1회** 송출. 폴백용 | 1세대 |
| `detect_send_3b2_timed.py` | 3b2 + `[TIMING]`. **`predict` 2회라 실시간 루프 금지** | 1세대 |
| `e2p.py` | equirect → perspective 슬라이스 (파일 경유). `anchor360_live` 가 메모리로 대체 | 1세대 |
| `relay_loop.py` | 고정 JSON 반복 UDP. **파일을 1회만 읽음** — 정렬 조정 전용 | 1세대 / **D-1 에 필요** |
| `Anchor360ObstacleReceiver.cs` | 유니티 수신기. 스냅샷 방식, `yawOffsetDeg`/`invertX`/`distanceScale`/`floorWorldY` | 무수정 |

## 문서

| 파일 | 내용 |
|---|---|
| `260921 카메라 최종정리2.txt` | **영상 방식 실행 절차서 (정본)** |
| `260921_..._총정리_v2.md` | 영상 전환 근거·실측·미해결. **파라미터의 "왜"** |
| `260915_..._총정리.md` | **v1. 사진 방식. 논문(AsiaSim 2026) 기준 구성. §7~§12 여전히 유효. 폐기 금지** |
| `260712 카메라 사용법 올인원.txt` | 1세대 수동 절차 + 줄자 대조 로그 |
| `구버전/260914 카메라 최종 정리.txt` | 2세대(사진+자동) 절차서 |
| `구버전/260701 질문+진행한것.txt` | e2p/detect/MASt3R 초기 절차 |
| `구버전/260803 360도 카메라.txt` | 8월 수동 검출 로그 (`box conf 0.13 D=6.88m` 최초 관측) |

## 로그

| 파일 | 내용 |
|---|---|
| `구버전/260921 360도 카메라 실제 구동 로그.txt` | 사진 62장 연속. **stitch 큐 0→38 발산 원본 증거** |
| `구버전/260921 ... 로그2.txt` | **사진↔영상 좌표 대조 원본 (§C-6)** + 안정화 OFF 실험 |
| `구버전/260921 카메라 영상 로그3.txt` | 영상 첫 관통. `wait_stable` 병목 발견 + **사용자 오차 관찰 원문** |
| `구버전/260921 카메라 영상 실측 로그.txt` | **2분 614프레임 실측. 4.31 fps, PC구간 228 ms** |

---

*문서 끝. 2026-09-22 작성. 이 문서는 총정리 v1/v2 를 대체하지 않고 그 위에 얹는다.*
