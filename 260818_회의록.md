# 260818 회의록

> 음성 전사본 기반 정리. 인명·용어는 문맥으로 복원함
> (전사 오류: "대교/재규" → 대규, "환호/혀" → 현우, "변연/병영/다연" → 하드웨어 담당, 확인 필요)

---

## 1. 파트 재분담

파트 경계를 다시 설정. 한 사람이 한 덩어리를 통으로 갖는 방식이 아니라, **겹치는 영역을 둘이 나눠 갖는 구조**.

### 백엔드 (2분할)
- **대규**: OpenTelemetry 기반 관측 스택 백엔드 — 모든 데이터가 여기를 거침
- **나영**: 쿠버네티스/도커 기반 마이크로서비스 아키텍처, MLOps — 기존 쿠버네티스 경험 활용

### AI (세분화)
"AI 안에서도 가지치기가 많다"

| 구분 | 담당 |
|---|---|
| 음성인식 AI (STT) | - |
| 디지털 트윈 구성 AI | 대규 (360도 앵커 기반 정합을 해왔으므로 계속) |
| 예측 AI | 나영 |
| 오브젝트 디텍션 | (AI 영역에 포함) |

### 하드웨어 (2분할)
- **말단 센서**: 라즈베리파이/아두이노, PLC 등 타 체계에서 데이터를 끌어와야 하는 경우, 유선/무선/LTE 모듈, 엣지 노드 연결 방식
- **로봇·장비**: GO1, A2급 로봇, 드론 등

> 분할 이유: 센서 쪽만 해도 종류·회로·통신 프로토콜을 모두 알아야 하므로 로봇까지 겸하기엔 업무량 과다

### 가시화
- **현우** 담당. 현재 범위로는 분량·임팩트가 부족하다는 것이 이번 회의의 주요 논점

---

## 2. 가시화 파트 확장 방향

### 현재 진단
지금까지의 가시화는 **정적(static)** 이다. 셋업해두면 그 플로우대로 데이터가 흘러가는 형태.

- 개발 분량은 많은데 임팩트가 낮음
- "프론트엔드가 원래 그렇다" — 당연히 되어야 하는 것으로 인식되는 영역
- 공학적 난이도를 설명하기 어려운 구조

### 요구 방향: 동적 가시화
- 노드 에디터에서 **여러 데이터 소스를 조합·중첩**해서 구성 (드래그로 붙이고 겹쳐 보기)
- 참조 사례: DearPyGui 데모 / 일본 쪽 GitHub 프로젝트
  - 카메라 소스 → 컨트라스트·Canny 엣지 필터 → 오브젝트 디텍션 → 결과 저장까지 노드로 연결
- **조사 과제(현우)**: 그라파나에서 동적 데이터소스 조합·중첩이 가능한지 확인

### 언리얼 블루프린트 비유
로봇 동작 AI(PyTorch 기반)를 통짜로 만들지 않고 프로세스 노드 기반으로 구성

1. 가시화 도구에서 노드로 동작 로직 구성
2. 미리 돌려보고 테스트 (이미지 탐색 → 액션 결정 등)
3. "저장·반영" 시 실제 로봇에 반영
4. 역방향으로 현실 데이터를 가시화 도구로 가져와 확인

### 차별화 포인트: 로컬 LLM 기반 가시화 도구
- 음성 지시 → LLM이 태스크 분해 → **서브태스크가 노드로 자동 생성** → 더블클릭으로 결과 확인
- 로컬 LLM을 프롬프트 창이 아니라 **MCP 기반 서버로 띄워 API처럼 사용**, 데이터를 한 번 가공한 뒤 가시화
- 논리: "가시화 도구를 만들었습니다"는 임팩트가 없다. **"온디바이스 AI를 활용하는 로컬 LLM 기반 가시화 도구"** 라야 차별화됨
- 그라파나처럼 Prometheus에서 데이터를 가져와 가시화하는 것도 당연히 포함하되, 그 위에 LLM 가공 계층을 얹는 것이 핵심

### 대상 사용자 정의 수정
- 현우 제안: "관리자용"
- 수정: **개발자용에 더 가깝다.** 개발자용을 상위로 추상화하면 관리자용이 되는 구조

### 계층적 가시화 (군 지휘 비유)
같은 데이터라도 사용자 수준·목적에 따라 다르게 보여야 한다.

- **최상위 결심자**: "이길 수 있나?" → 빨강/초록 두 색이면 충분. 구체 데이터엔 관심 없음
- **더블클릭 → 하위 레이어**: 전차 몇 대인지, 상대가 보병인지 대전차 화기 보유인지 등 구체 데이터가 그래프 등으로 도출
- 아래로 갈수록 구체화, 위로 올라갈수록 판단만 집약
- → **설명가능성(explainable) 구조**

### 기술 스택 결정 포인트 (미결)

| 후보 | 평가 |
|---|---|
| 파이썬 (DearPyGui) | 취업 관점에서 좋은 선택지는 아님 |
| C#/.NET | 현장에서 .NET GUI로 전환 중. 유니크하지만 "운에 따라" 갈리는 베팅 |
| 웹 / Node.js 프론트엔드 | 수요 많지만 공급도 많고 노가다 비중 큼 |

> 결론: 스택 자체로는 승부하기 어렵다. **"로컬 LLM 기반"이라는 포인트를 얹어야 한다.**

---

## 3. 음성 인터페이스 (내추럴 UI)

- STT로 음성 → 텍스트 → 제어 로직 → **기존 클릭 이벤트를 대체하는 트리거**
- 기술적으로 어려운 단계는 아니나 **시연 효과가 큼** (지금까지 시연은 전부 수동 클릭)
- 활용 계획: 내년 총장 관련 행사, 100주년 기념 행사 등에서 시연
- 성격: "쇼"에 가깝지만, 우리가 만든 것이 여러 곳에 쓰인다는 **스토리를 만드는 것**이 목적

---

## 4. 요구사항 정립 프로세스 — 대규 중심

### 최대 리스크
각자 AI를 써서 따로 요구사항·인터페이스를 만들면 **통합하는 순간 안 맞는다.**
→ 전체 일관성이 깨지고 프로젝트 전체가 무너지는 케이스.

### 대규가 "시작"인 이유
OTel 기반으로 모든 데이터가 주고받아지므로, 여기서 중심을 잡고 나머지가 거기에 맞춰야 한다.

### 절차

1. 각자 요구사항 1차 정리
2. 각 파트 간 **오가는 정보 식별**
3. **공통 인터페이스 문서 1개** (엑셀 시트 형태) 작성 — 모듈별 데이터 수신부 / 송신부 명세
4. 이 문서를 기준으로 AI에 넣어 요구사항 생성
5. 전체 리뷰

### 인터페이스 명세 예시 (가시화 모듈)
- **받는 쪽**: OTel 기반 Grafana/Prometheus에서 어떤 요소를 받아야 하는지 정리
- **주는 쪽**: 제어 메시지도 OTel 기반으로 내보낸다

### 제어 명령까지 기록해야 하는 이유
공공 서비스이므로 **책임 소재 추적**이 필요.

> 수문 예시 — 수문 데이터는 Prometheus에 저장되지만, "수문을 열어라"는 명령 이벤트가 저장되지 않으면 문제 발생 시 누가·언제·어디서·어떤 방식으로 의사결정했는지 확인할 수 없다.

- 인바운드(센서 → 시스템) / 아웃바운드(제어 명령) 이벤트 **모두** 저장되어야 함
- 정리 항목: 누가 했다 / 어디서 했다 / 어떤 방식으로 했다

---

## 5. PM 역할 부여

- 캡스톤디자인2에서 각자 **PM 역할**을 맡김. 현 시점 PM은 대규 (시작점이므로)
- PM 업무: 전체를 어떻게 오거나이즈할지, 언제 어떤 산출물을 가지고 회의를 구성할지 결정
- 목적: 경험해보지 않으면 회사에서 팀장 역할을 할 수 없음

### AI 시대의 PM
1. 전체 큰 틀을 머릿속에 갖는다
2. 요구사항을 AI에 넣어 검증·시뮬레이션한다
3. 파생되는 결과물까지 주고받으며 정리한다
4. 그 상태로 회의를 주도한다

> 아무것도 없이 "이것도 생각해보고 저것도 생각해보다" 끝나면 아무것도 안 된다.

**핵심 주문**: 디테일은 AI가 처리할 테니 **의사결정을 빨리 하라.** 완벽하지 않아도 논의 가능한 안을 가져오면 코멘트를 받을 수 있다.
