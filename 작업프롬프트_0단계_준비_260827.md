# 작업 지시: 0단계 — 새 가시화 도구 골격과 코드 이식

**기간**: 2026-08-27 ~ 08-31
**대상 문서**: `구현진행_가시화도구_260826.md` §4-2 0단계 · `기술스택_가시화도구_260826.md` §3·§4
**기준 커밋**: `605eb73`

---

## 배경

이 저장소는 하이브리드 디지털 트윈 프레임워크의 **가시화 파트**다. 기존 `web-dashboard/`는
"지금 장치 상태를 보여주는 관제 화면"으로 이미 동작하고 검증 스크립트 5종이 통과한다.

이번에 만드는 것은 성격이 다른 도구다. **임무 실행 기록을 되짚어 실패를 찾는 디버깅 도구**이고,
마일스톤 → 태스크 → 액션 아이템의 3계층과 시점 되감기를 다룬다. 기존 구조 위에 얹으면 이미
동작하는 관제 화면까지 흔들리므로 **새 폴더 `viz-debugger/`에서 새로 만든다.**

**다만 새로 만든다는 것이 버린다는 뜻은 아니다.** 통신·상태 보관·명령 추적 코드는 이미 있고
다시 짤 이유가 없다. 0단계는 **골격을 세우고 무엇을 어떻게 가져올지 확정하는 단계**다.
화면은 1단계에서 만든다. **이번 단계에서 화면을 만들지 않는다.**

---

## 먼저 읽을 것

1. `기술스택_가시화도구_260826.md` — 특히 **§3 이식 판단표**(파일 단위)와 **§4 폴더 구조**.
   이 문서가 이번 작업의 명세다.
1-1. `요구사항정의서.md` **§3.1 통합 앱 구조** — 최종 모습은 탭 6개짜리 한 앱이고,
   지금 만드는 것은 **탭⑥과 공유 계층뿐**이다(18 + 16 = 34건).
   엑셀 김현우 시트의 `통합 앱 배치` 열이 `탭⑥` 또는 `공유 계층`인 것만 해당한다.
   **영상·인지·트윈·지표 패널·파이프라인 편집기는 3단계 이후 이식분이다.**
   목록에 있다고 지금 만들지 않는다. 다만 **탭으로 붙일 자리는 남겨 둔다** — 0단계의
   폴더 골격에 `src/shell/`과 `src/tabs/`를 빈 채로 만들어 두면 나중에 이식이 쉬워진다.
2. `구현진행_가시화도구_260826.md` §2 — 왜 새로 만드는지, 옮길 때 지키는 규칙 셋.
3. `web-dashboard/src/transport/Transport.ts` — 상위 코드가 보는 유일한 통신 면.
   주석에 이 인터페이스가 지켜야 하는 것이 적혀 있다.
4. `web-dashboard/src/data/store.ts` 앞부분 주석 — "상태를 단일 값으로 뭉쳐 저장하지 않는다"는
   원칙 넷. 새 저장소도 이 정신을 잇되 보관 대상이 바뀐다.
5. `web-dashboard/src/data/plans.ts` — `PlanSegment`가 태스크 계층의 원형이다.
6. `contracts/pipeline.schema.json` — `additionalProperties: false`로 계층 경계를 막는 방식.
   새 스키마도 같은 방식을 쓴다.
7. `web-dashboard/README.md`의 **"지켜야 하는 규칙"** 절 — 이 저장소의 코드 규율과 서술 톤.

---

## 제약 — 반드시 지킬 것

1. **기존 코드를 한 줄도 고치지 않는다.** `web-dashboard/` · `mock-gateway/` · `unity-twin/` ·
   `stt-lab/` · `contracts/`의 기존 스키마 5종은 **읽기 전용**이다. 열어서 읽되 저장하지 않는다.
2. **기존 검증이 계속 통과해야 한다.** 작업 전과 후에 `npm run verify:cache` ·
   `verify:pipeline` · `verify:mission-graph` · `verify:contract-roundtrip` ·
   `verify:catalog-derived` · `typecheck` · `build`를 모두 돌린다.
   **하나라도 깨지면 원본을 건드린 것이다.** 기대값을 낮춰 통과시키지 않는다.
3. **이식한 파일 맨 위에 출처를 남긴다.** 예외 없다.
   ```
   // 이식: web-dashboard/src/transport/Transport.ts @ 605eb73 — 무수정
   // 이식: web-dashboard/src/data/plans.ts @ 605eb73 — 8상태·DAG로 개조
   ```
4. **`contracts/`의 기존 스키마는 복사하지 않는다.** 상대 경로로 참조한다.
   계약이 두 벌이 되면 어느 쪽이 원본인지 관리해야 한다.
5. **화면을 만들지 않는다.** 빈 화면 하나가 뜨는 것으로 충분하다. 1단계 범위를 당겨오지 않는다.
6. **포트를 겹치지 않게 잡는다.** 기존 `web-dashboard`와 새 도구를 동시에 띄워 비교할 수 있어야 한다.
7. **목임을 감추지 않는다.** 게이트웨이가 목이라는 사실이 화면과 문서에 드러나야 한다.
8. **의존성을 늘리지 않는다.** 그래프 라이브러리·상태 관리 라이브러리·UI 프레임워크를 넣지 않는다.
   기술 문서 §2-3에 직접 구현하는 이유가 적혀 있다. 이견이 있으면 코드를 쓰기 전에 보고한다.

---

## 단계

### 1단계 — 기준선 확인 (먼저 한다)

1. 위 검증 7종을 돌리고 **결과를 기록**한다. 이게 작업 전 기준선이다.
2. `git status`가 깨끗한지 확인한다.

이걸 먼저 하지 않으면 나중에 깨졌을 때 내가 깬 것인지 원래 그랬는지 알 수 없다.

### 2단계 — 폴더 골격

기술 문서 §4의 구조대로 `viz-debugger/`를 만든다.

```
viz-debugger/
├── src/{transport,trace,model,data,graph,views}/
├── gateway/
├── adapters/
├── scenarios/
├── scripts/
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

- `package.json`은 기존 `web-dashboard/package.json`을 참고하되 **의존성은 최소로** 시작한다
  (react · react-dom · ws + 개발 의존성). 스크립트 이름은 기존 규칙을 따른다.
- `npm run dev`로 목 게이트웨이와 Vite가 함께 뜨고, 브라우저에 **빈 화면 + 연결 상태 배너**가
  보이면 이 단계는 끝이다.
- `README.md`에 실행 방법과 **"이 폴더는 web-dashboard를 대체하지 않는다"**는 문장을 적는다.

### 3단계 — 무수정 이식

기술 문서 §3-1 표의 파일을 **그대로 복사**한다. 내용을 고치지 않는다.
import 경로만 새 폴더 기준으로 맞춘다.

- `src/transport/` 전체 (Transport.ts · WsTransport.ts · types.ts · backoff.ts · index.ts)
- `src/data/mergeScheduler.ts` · `env.ts` · `constants.ts`

**판정 기준**: 이식한 `transport`로 목 게이트웨이에 붙어 연결 상태가 `connected`가 되고,
게이트웨이를 껐다 켜면 **재연결과 구독 복원이 자동으로 일어난다.** 이게 확인되면
이식이 제대로 된 것이다. 확인용 임시 화면은 만들어도 되고 콘솔 출력으로 대신해도 된다.

### 4단계 — 3계층 계약 스키마 초안

`contracts/`에 **새 파일로** 추가한다. 기존 파일을 고치지 않는다.

| 파일 | 담는 것 |
|---|---|
| `mission.schema.json` | `mission_id` · `utterance{audio_ref, text, engine, confidence}` · `milestones[]` |
| `milestone.schema.json` | `milestone_id` · `title` · `order` · `status` · `tasks[]` · `assigned_targets[]` |
| `task.schema.json` | `task_id` · `title` · `deps[]` · `status` · `attempt` · `derived_from` · `action_items[]` · `evaluation` |
| `action-item.schema.json` | `item_id` · `target` · `adapter` · `params{}` · `status` · `telemetry_ref` |
| `evaluation.schema.json` | `criteria[]` · `result` · `judged_by` · `evidence_refs[]` |
| `trace-event.schema.json` | `seq` · `ts` · `layer` · `node_id` · `kind` · `produced_by` · `payload` |

지켜야 하는 것.

1. **`milestone`과 `task`는 `additionalProperties: false`** 로 두고 하드웨어 어휘를 필드로
   갖지 못하게 한다. 기종 의존 값은 전부 `action-item`의 `params` 안에만 있다.
2. `status`는 **8상태 열거**다: `pending` · `running` · `done` · `failed` · `skipped` ·
   `awaiting_evaluation` · `not_executed` · `rerunning`.
   `skipped`(의도)와 `not_executed`(선행 실패)를 반드시 별개 값으로 둔다.
3. `trace-event`의 `kind`: `created` · `dispatched` · `acked` · `started` · `progress` ·
   `evaluated` · `failed` · `derived`. `produced_by`: `ai` · `backend` · `human`.
4. `task.deps[]`가 DAG를 만든다. `index`/`total` 같은 일렬 필드를 넣지 않는다.
5. 각 스키마마다 `contracts/examples/` 아래에 **유효 예시 1건**을 넣고
   `contracts/validate_examples.py`로 검증되게 한다.

### 5단계 — 어댑터 프로파일 1종

`viz-debugger/adapters/unitree_go1.json`을 만든다.

- 이 로봇이 받는 액션 종류(보행 모드 전환 · 직진 · 회전 · 정지)와 각 파라미터의 이름·단위·범위
- 상태 조회 항목(배터리 · RSSI · 지연 · IP · 펌웨어 · 관절 온도 · 하트비트)

**액션 아이템의 `params`를 해석하는 규칙이 이 파일에만 있어야 한다.** `.ts` 코드에
기종 이름이나 파라미터 표를 다시 적지 않는다.

### 6단계 — 검증 스크립트 두 개

기존 `web-dashboard/scripts/verify-*.mjs`와 같은 방식으로, **단독 실행 가능한 Node 스크립트**로 만든다.
테스트 프레임워크를 붙이지 않는다.

- **`verify:hierarchy`** — 6종 스키마가 서로 맞물리는지, `milestone`·`task` 예시에 하드웨어
  어휘를 넣으면 **실패로 잡히는지**까지 확인한다. 통과만 확인하고 끝내지 않는다.
- **`verify:adapter-swap`** — 어댑터 프로파일을 하나 더 추가했을 때
  `src/model/`과 `src/views/`의 마일스톤·태스크 관련 파일 변경이 **0인지** 확인한다.
  이번 단계에서는 두 번째 프로파일이 없으므로 **스켈레톤만 만들고 판정 기준을 주석으로 적어 둔다.**

### 7단계 — 이식 판단표 갱신

작업하면서 §3-1(무수정)과 §3-2(개조) 사이를 오간 파일이 나올 것이다.
**실제로 한 대로 `기술스택_가시화도구_260826.md` §3의 표를 고친다.** 문서가 코드를 따라가야 한다.

---

## 완료 판정

이 다섯 개가 전부 참이어야 0단계가 끝난 것이다.

1. `viz-debugger`에서 `npm run dev`가 뜨고 브라우저에 빈 화면과 연결 상태가 보인다.
2. 이식한 `transport`로 목 게이트웨이에 붙고, 게이트웨이 재시작 시 **자동 재연결·구독 복원**이 된다.
3. 새 스키마 6종의 예시가 `validate_examples.py`를 통과한다.
4. `verify:hierarchy`가 통과하고, **일부러 하드웨어 어휘를 넣은 예시는 실패로 잡힌다.**
5. `web-dashboard`의 기존 검증 7종이 **작업 전과 동일하게** 통과한다.

---

## 보고

`reports/` 아래에 기존 형식(`YYYY-MM-DD_HHMM_제목.md`)으로 남긴다. 담을 것.

- 기준선 검증 결과(작업 전)와 완료 후 결과
- 이식 판단표에서 **바뀐 항목과 그 이유**
- 스키마를 만들다 발견한 **회의 합의와 어긋나는 지점** — 있으면 고치지 말고 적는다
- 1단계로 넘길 미결 사항

**막히면 임의로 정하지 말고 보고서에 남기고 멈춘다.** 특히 계층 경계와 8상태의 의미는
회의 합의 사항이므로 혼자 바꾸지 않는다.
