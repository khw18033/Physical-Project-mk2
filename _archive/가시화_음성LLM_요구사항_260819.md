# 가시화 — 음성 명령 인터페이스(F13) · 로컬 LLM 계층(F12) 요구사항

> 작성: 김현우 2026.08.19 · 대상: `캡스톤MK2_요구사항_통합정립본_v1.docx` §2.3 보완
> 태그 [기존] 요구사항정의서에 이미 있음 / [신설] 이번에 추가 / [요청] 백엔드에 새로 필요한 것 / [추후 확정] Tier 2

---

## §0. 이 문서를 내는 이유

통합정립본 v1 §2.3(동적 가시화) 표에 **음성 인터페이스(F13)와 로컬 LLM 계층(F12)이 빠져 있습니다.**

현재 통합본에 남아 있는 흔적은 어휘와 필드뿐입니다.

- §1-4 `origin.path` 어휘에 `voice` · `llm_suggestion`
- 부록 A-4 감사 스키마에 `voice.transcript` · `llm.model`
- `viz.llm.request_latency_ms` · `viz.llm.failure_rate` 지표

**즉 "음성·LLM으로 명령이 들어온다"는 전제는 계약에 이미 반영돼 있는데, 그 기능 자체가 요구사항 표에 없습니다.** 원문(`요구사항정의서.md` F12: REQ-1201~1207, F13: REQ-1301~1305)에는 상세히 있으므로, 이 문서는 **새 요구가 아니라 누락분의 반영**입니다.

**왜 중요한가**: 프로젝트 포지셔닝이 "가시화 도구"가 아니라 **"로컬 LLM 기반 동적 가시화 도구"**입니다(회의록 §2, 요구사항정의서 §1). 통합본에서 이 층이 빠지면 차별화 축이 문서상 사라집니다.

---

## §1. 요구사항 표 (통합본 §2.3에 추가할 행)

| 대분류 | 소분류 | 요구사항 | 방향 | 태그 |
|---|---|---|---|---|
| 음성 | 입력 처리 | 발화를 텍스트로 변환해 기존 클릭과 **동일한 액션 트리거**로 처리. 음성은 추가 입력 수단이며 기존 조작을 대체 강제하지 않음 (REQ-1301) | 내부 | [기존] |
| 음성 | 엔진 추상화 | 특정 STT 엔진에 종속되지 않는 인터페이스. 구현체는 Phase A 스파이크 결과로 확정 (REQ-1302) | 내부 | [추후 확정] |
| 음성 | 인식 확인 | 인식된 발화를 화면에 표시하고 실행 전 확인 기회 제공. **제어 액션은 음성만으로 즉시 실행 금지** (REQ-1303) | 규약 | [기존] |
| 음성 | 신뢰도 임계 | STT·의도 해석 신뢰도가 임계 미만이면 실행하지 않고 재확인 요구 | 내부 | [신설] |
| 음성 | 호칭 해석 | "503 구역 로봇" 같은 자연어 호칭을 entity/node로 해석. **별칭 사전은 레지스트리에서 수신** | 백엔드→가시화 | [요청] |
| 음성 | 발화 감사 | 음성 발행 액션에 인식 발화 원문을 감사 로그에 기록 (REQ-1305) | 가시화→감사 | [기존] |
| LLM | 런타임 추상화 | 특정 모델·런타임 비종속. **온디바이스 로컬 모델 기본**, 별도 프로세스(MCP/HTTP)로 분리 (REQ-1201) | 내부 | [기존] |
| LLM | 도구 노출 | LLM이 호출 가능한 조작(노드 추가·질의·뷰 전환)을 도구로 선언·노출. 분야별 확장점(E7) (REQ-1202) | 내부 | [기존] |
| LLM | 액션 카탈로그 | **제어 명령을 발화에서 생성하려면 "가능한 액션·파라미터 스키마·필요 권한" 목록이 필요** | 백엔드→가시화 | [요청] |
| LLM | 태스크 분해 | 자연어 지시를 도구 호출 시퀀스로 분해해 노드 그래프·뷰 구성 생성 (REQ-1203) | 내부 | [기존] |
| LLM | 제안-검토-수락 | LLM 생성 결과는 **제안 상태**로 표시하고 사용자가 명시적으로 수락해야 반영. 검토 없이 운영 상태 적용 금지 (REQ-1204) | 규약 | [기존] |
| LLM | 관여 표시 | 화면상 어떤 내용이 LLM 생성·가공물인지 구별 표시 (REQ-1207, NFR-011) | 내부 | [기존] |
| LLM | 미가용 허용 | LLM 미연결·무응답 시 관련 기능만 비활성화, 나머지는 정상 동작. **LLM은 증강이지 전제가 아님** (REQ-1206) | 규약 | [기존] |
| 공통 | 출처 기록 | 명령의 **입력 수단**과 **판단 주체**를 감사에 각각 기록 → `origin.path` 어휘 확장 필요 | 가시화→감사 | [요청] |
| 공통 | 자체 지표 | `viz.stt.*` · `viz.nlu.*` 지표 추가 (기존 `viz.llm.*`에 더해) | 가시화→OTel | [신설] |
| 공통 | 로컬 처리 | LLM·STT 추론은 로컬 수행 기본. 관제 데이터가 외부 서비스로 전송되지 않아야 함 (NFR-010) | 규약 | [기존] |
| 추후 확정 | — | STT 엔진·모델 선정, 음성 원본 오디오 보존 정책, 관제실 음향 환경, 의도 해석 성공 판정 기준 | Tier 2 | [추후 확정] |

**음성 적용 범위**: 뷰 전환 · 데이터 조회 · **제어 명령** · 구성(노드 그래프) 생성 — 전체. 단 제어와 구성은 REQ-1204/1303의 **확인 절차를 반드시 거칩니다.**

---

## §2. 개념 설명

### 2-1. 음성은 별도 명령 체계가 아니라 **입력 수단**이다

REQ-1301의 핵심은 "음성 → 텍스트 → **기존 클릭과 같은 액션 트리거**"입니다. 음성 전용 명령 경로를 새로 만들지 않습니다. 그래서 §2.3에 이미 [확정]된 제어 명령 경로(가시화 → WS 게이트웨이 → 백엔드 → MQTT)를 그대로 탑니다. **백엔드 입장에서 새 채널이 생기지 않습니다.**

달라지는 것은 **감사에 실리는 메타데이터뿐**입니다.

### 2-2. 2단 확인 구조 — 오인식과 오해석을 사람이 막는다

```
발화 → STT → 텍스트 표시(1차 확인) → LLM 의도 해석 → 액션 제안 표시(2차 확인) → 사용자 수락 → 명령 발행
```

- **1차**: 인식된 문장이 맞는지 (REQ-1303)
- **2차**: 그 문장을 이 액션으로 해석한 게 맞는지 (REQ-1204 제안-검토-수락)

조회·뷰 전환은 되돌릴 수 있으므로 1차만으로 진행할 수 있지만, **제어 명령과 구성 반영은 2차까지 필수**입니다. 공공 서비스에서 오인식으로 수문이 열리면 안 되기 때문입니다.

여기에 v2에서 이미 요청한 **`expires_at`(지연 명령 실행 금지)**이 붙습니다. 음성은 발화와 실행 사이 지연이 길어지기 쉬워서 특히 필요합니다.

### 2-3. `origin.path` 4값으로는 이 경로가 표현되지 않는다 — 핵심 갭

현재 어휘는 `manual | voice | llm_suggestion | auto` **단일 필드 4값**입니다.

그런데 시연의 핵심 경로는 **"음성으로 말했고, LLM이 해석했고, 사람이 수락했다"**입니다. 이건 `voice`인가요 `llm_suggestion`인가요? **둘 다입니다.** 단일 필드로는 하나를 버려야 합니다.

무엇을 버리든 감사가 망가집니다.

- `voice`로 적으면 → **LLM이 해석에 개입한 사실이 사라집니다.** 사고 시 "LLM이 잘못 해석했다"를 추적할 수 없습니다.
- `llm_suggestion`으로 적으면 → **입력이 음성이었다는 사실이 사라집니다.** STT 오인식이 원인일 때 추적할 수 없습니다.

원문 REQ-908이 요구한 건 "수동 클릭 / 음성 지시 / LLM 제안 수락 / 자동 제어를 구분"인데, **이 넷은 서로 배타적이지 않습니다.** 음성 지시와 LLM 제안 수락은 동시에 성립합니다.

**제안: 두 축으로 분리**

| 필드 | 값 | 의미 |
|---|---|---|
| `input.mode` | `click` / `voice` / `api` | 사람이 **어떻게 입력**했나 |
| `decision.source` | `human` / `llm_suggestion` / `auto` | **누가 판단**했나 |

이러면 조합이 정확히 남습니다.

| 상황 | input.mode | decision.source | 기존 origin.path |
|---|---|---|---|
| 버튼 클릭 | click | human | manual |
| 음성으로 직접 지시 | voice | human | voice |
| 클릭 후 LLM 제안 수락 | click | llm_suggestion | llm_suggestion |
| **음성 → LLM 해석 → 수락** | **voice** | **llm_suggestion** | **표현 불가** |
| AI 자동 제어 | api | auto | auto |

**백엔드 부담을 줄이는 방법**: 기존 `origin.path`를 없애지 말고 **두 필드에서 파생되는 값으로 유지**하면 됩니다(위 표의 마지막 열이 그 규칙입니다). 기존 대시보드·쿼리가 안 깨지고, 새 조합만 추가로 구분됩니다. → **어휘를 바꾸는 게 아니라 두 필드를 추가하고 기존 값을 파생시키는 것**으로 제안합니다.

### 2-4. LLM이 명령을 만들려면 **무엇이 가능한지**를 알아야 한다

지금 통합본 부록 A-4 감사 스키마에는 `action.id`와 `action.params`가 있습니다. 그런데 **그 `action.id` 목록이 어디서 오는지가 문서 어디에도 없습니다.**

사람이 화면 버튼을 누를 때는 문제가 없습니다 — 버튼을 만들 때 개발자가 액션을 알고 있으니까요. 하지만 **LLM이 발화에서 명령을 생성하려면 런타임에 목록을 알아야 합니다.** "수문 열어줘"를 `action.id="levee.open"`으로 바꾸려면, 그런 액션이 존재하고 파라미터가 무엇인지를 모델이 알아야 합니다.

**필요한 것** (백엔드 소유 — 제어 액션은 백엔드가 MQTT로 발행하므로):

| 항목 | 왜 |
|---|---|
| `action.id` 목록 | LLM이 고를 후보 |
| 적용 대상 타입 | 로봇용/센서용 구분 |
| 파라미터 스키마 | 유효한 명령을 만들기 위해 |
| 필요 역할 | RBAC과 연동 — 권한 없는 액션은 제안조차 안 하도록 |
| 확인 필요 여부 | 위험 액션은 2차 확인 강제 |

**이건 F12만의 요구가 아닙니다.** 액션 카탈로그가 없으면 노드 편집기(F10)도, 권한별 UI 차단도 하드코딩이 됩니다. 음성·LLM은 그 필요를 앞당길 뿐입니다.

REQ-1202(LLM 도구 노출)는 **화면 조작 도구**라 제 소유가 맞지만, **제어 액션은 백엔드 소유**입니다. 이 둘을 구분해서 요청합니다.

### 2-5. 호칭 사전 — 레지스트리에 얹으면 된다

사람은 `go1-01`이라고 말하지 않고 **"503 구역 로봇"**이라고 말합니다. 확장점 E8(음성 명령 어휘)이 이 사전인데, 이건 **별도 시스템이 아니라 레지스트리에 필드 하나**면 됩니다.

entity·node·zone에 `display_name`과 `aliases[]`가 있으면 해결됩니다. 이미 요청 중인 **레지스트리 조회 인터페이스에 얹으면 되므로 새 요청이 아닙니다.**

### 2-6. LLM 관여를 화면에서도 구별한다

REQ-1207과 NFR-011은 **측정된 사실과 LLM이 생성·추정한 내용을 구별**하라고 요구합니다. 감사 로그에만 남기는 게 아니라 화면에서도 구별돼야 합니다.

- LLM 제안은 **제안 상태 스타일**로 (수락 전까지 운영 상태와 시각적으로 구분)
- LLM이 요약·라벨링한 텍스트는 **생성물 표시**
- 감사 패널에서 명령 이력에 입력 수단·판단 주체 배지 표시

여기서 §2.2의 부수 효과가 하나 붙습니다. 나영이 `ai.model.version`을 별도 resource attribute로 유지하기로 해서, **"어느 모델 버전이 판단했나"가 감사에 자동으로 딸려옵니다.** 로컬 LLM도 같은 방식으로 `llm.model`을 실으면 일관됩니다.

---

## §3. 감사 스키마 확장 (부록 A-4에 추가)

기존 필드에 더해 굵은 항목이 추가됩니다.

```jsonc
{
  "event.name": "control.command.issued",
  "command.id": "string",
  "actor.id": "string", "actor.role": "string",

  // ─── 출처: 단일 필드 → 두 축 ───
  "input.mode":       "click | voice | api",           // ★ 신설
  "decision.source":  "human | llm_suggestion | auto", // ★ 신설
  "origin.path":      "manual | voice | llm_suggestion | auto", // 위 둘에서 파생(호환 유지)

  // ─── 음성 (input.mode=voice일 때) ───
  "voice.transcript":  "string  — 인식된 발화 원문 (기존)",
  "voice.confidence":  "number 0~1 (기존)",
  "stt.engine":        "string  — 엔진·모델 식별자", // ★ 신설
  "nlu.intent":        "string  — 해석된 의도",       // ★ 신설
  "nlu.confidence":    "number 0~1",                  // ★ 신설

  // ─── LLM (decision.source=llm_suggestion일 때) ───
  "llm.model":         "string (기존)",
  "llm.suggestionId":  "string (기존)",
  "llm.userModified":  "bool — 제안을 수정 후 수락했는지 (기존)",

  "target.zone/node/entity": "string",
  "action.id": "string", "action.params": {},
  "expires_at": "ISO8601 — 지연 명령 실행 금지",
  "traceparent": "W3C Trace Context"
}
```

**음성 원본 오디오는 보존하지 않고 전사 텍스트만 남기는 것**을 기본으로 제안합니다(개인정보). 확정은 감사 보존 기간과 함께 Tier 2.

---

## §4. 자체 지표 추가 (`viz.*`)

`viz.*` 네임스페이스는 가시화 소유이므로 제가 정의합니다. 단위는 이름이 아니라 `unit` 필드에 둡니다.

```
viz.stt.latency              (histogram, unit=ms)   발화 종료 → 텍스트 확정
viz.stt.confidence           (histogram)            인식 신뢰도 분포
viz.nlu.latency              (histogram, unit=ms)   텍스트 → 의도 해석
viz.nlu.intent_unmatched     (counter)              해석 실패 — 시연 판정의 핵심 지표
viz.voice.command_issued     (counter)              라벨: decision.source
viz.llm.suggestion_accepted  (counter)              라벨: llm.userModified
```

기존 `viz.llm.request_latency_ms` · `viz.llm.failure_rate`는 그대로 두되, 단위는 이름에서 떼고 `unit` 필드로 옮깁니다(§1-4 규약).

`viz.nlu.intent_unmatched`가 중요합니다. 구현계획서 Phase A의 판정 기준("10건 중 7건")을 **실측으로 뒷받침하는 지표**가 됩니다.

---

## §5. 백엔드에 요청할 것 (신규 2건)

기존 질문 3건(감사 주체 · 상태 어휘 · 레지스트리)에 더해, 이 문서에서 두 건이 나옵니다.

### 요청 A. `origin.path`를 두 축으로 분리

> 통합본 §1-4의 `origin.path` 4값으로는 **"음성으로 말했고 LLM이 해석했다"**가 표현되지 않습니다. 둘 중 하나를 버려야 하는데, 어느 쪽을 버려도 사고 추적에서 원인(STT 오인식 / LLM 오해석)을 구분할 수 없게 됩니다.
>
> `input.mode`(click/voice/api)와 `decision.source`(human/llm_suggestion/auto) **두 필드로 나누고, 기존 `origin.path`는 이 둘에서 파생되는 값으로 유지**하는 건 어떨까요? 그러면 기존 쿼리·대시보드는 그대로 두고 새 조합만 추가로 구분됩니다.

### 요청 B. 액션 카탈로그 (제어 액션 목록·파라미터 스키마·필요 권한)

> 부록 A-4에 `action.id`·`action.params`가 있는데, 그 **목록의 출처가 문서에 없습니다.** 사람이 버튼을 누를 때는 개발자가 알고 있어서 문제가 없지만, LLM이 발화에서 명령을 만들려면 런타임에 "무엇이 가능한지"를 알아야 합니다.
>
> 제어 액션은 백엔드가 MQTT로 발행하니 백엔드 소유가 맞다고 봅니다. **액션 카탈로그를 계약 항목으로만 추가**해 주시면(형태는 Tier 2여도 됩니다) 그때까지는 정적 파일로 개발하겠습니다. 필요 역할까지 실려 있으면 **권한 없는 액션은 LLM이 제안조차 안 하도록** 막을 수 있어서 RBAC과도 맞물립니다.
>
> 참고로 이건 F12만의 요구는 아닙니다. 노드 편집기(F10)와 권한별 UI 차단도 이게 없으면 하드코딩이 됩니다.

**레지스트리 요청에 덧붙일 것**: entity·node·zone에 `display_name`과 `aliases[]`를 포함해 달라 (E8 음성 호칭 사전 — 별도 시스템이 아니라 필드 하나면 됨).

---

## §6. 추후 확정 (Tier 2)

- **STT 엔진·모델 선정** — REQ-1302로 추상화만 해두고 구현체는 Phase A 스파이크 결과로. 로컬 처리(NFR-010)를 만족하는 선에서 고름.
- **음성 원본 오디오 보존 여부** — 감사 보존 기간과 함께. 기본은 전사 텍스트만 보존.
- **신뢰도 임계값** — STT·의도 해석 각각 몇 이상이어야 실행할지. 실측 후.
- **관제실 음향 환경** — 소음·다중 화자·마이크 배치(요구사항정의서 §미해결에 이미 등재).
- **의도 해석 성공 판정 기준** — "노드를 생성했다"와 "의도에 맞다"는 다름(REQ-1203 리스크).

---

## §7. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 로컬 LLM이 태스크 분해를 못 함 | **차별화 축 소멸** — 프로젝트 포지셔닝 재수립 | 구현계획서 Phase A로 조기 검증, 정량 기준(10건 중 7건) 선고정 |
| STT 오인식으로 잘못된 제어 | 공공 서비스 사고 | 2단 확인(REQ-1303/1204) + 신뢰도 임계 + `expires_at` |
| 액션 카탈로그 부재 | LLM이 유효한 명령을 못 만듦 | 요청 B. 그때까지 정적 파일로 우회 |
| 음성 지연으로 상황 변화 후 실행 | 오작동 | `expires_at`으로 만료 명령 실행 금지 |

**LLM·STT가 다 안 되더라도 F10 이하 전 기능은 동작해야 합니다**(REQ-1206, NFR "LLM 비종속"). 이 층은 증강이지 전제가 아닙니다.
