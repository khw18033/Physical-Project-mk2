# 백엔드(대규)에게 물을 것 — 260819

> 기준 문서: `캡스톤MK2_요구사항_통합정립본_v1.docx` (2026.08.19, 이대규)
> 물어야 할 것 **3건**, 확인만 하면 되는 것 **2건**, 회의에 올릴 것 **1건**.

## 한눈에

| | 무엇 | 왜 급한가 | 등급 |
|---|---|---|---|
| **1** | 감사 기록을 누가 쓰나 | [확정]으로 박혔는데 방향이 위험함. 굳기 전에 | 🔴 물어야 함 |
| **2** | 상태를 `lifecycle`로 받나 3층으로 받나 | 상태 뱃지 구현이 통째로 걸림 | 🔴 물어야 함 |
| **3** | "지금 뭐가 있나" 목록을 어디서 받나 | 이게 없으면 미배포 표시가 구현 불가 | 🔴 물어야 함 |
| 4 | `completed`는 백엔드가 붙여주는 것 맞나 | 거의 확정. 한 줄 확인 | 🟡 확인 |
| 5 | WS 구독을 entity/node로 하나 | 그렇게 보임. 한 줄 확인 | 🟡 확인 |
| 6 | 로봇 state를 retained로 할지 백엔드 캐시로 할지 | 병현이 걸린 3자 건 | 🔵 회의 안건 |

---

# 🔴 1. 감사 기록을 **누가 쓰나**

## 지금 문서엔 이렇게 돼 있다

통합본이 이 부분의 주어를 채웠는데, 하필 위험한 쪽입니다.

- §2.3 표 → 감사 기록의 방향이 **"가시화→OTel"**, 태그는 **[확정]**
- §3 요약표 → 가시화가 OTel로 보내는 것: 감사 기록·자체 지표, 채널은 **"OTel 직접"**
- 부록 A-4 제목 → "제어 명령 감사 기록 **(가시화→OTel Log Record 속성)**"

즉 **내 브라우저가 OTel 서버에 직접 기록을 쓴다**는 그림입니다. 명령은 백엔드를 거쳐 나가는데(§3: WS 게이트웨이 → MQTT), 감사만 다른 문으로 나가는 셈이에요.

## 왜 곤란한가 — 쉽게 말하면

**감사 기록은 "누가 시켰는지"를 남기는 증거입니다.** 그런데 그 증거를 브라우저가 직접 쓰면, **증거를 남기는 주체가 감시 대상 본인**이 됩니다.

브라우저 코드는 사용자 손 안에 있어서 얼마든지 조작할 수 있습니다. `actor.id`에 남의 이름을 넣어 보내도 서버는 그대로 받아 적습니다. 대규 본인이 §2.4/⑥-3에서 **"화면 차단은 UX라 우회 가능하니 실제 강제는 백엔드"**라고 못박았는데, 같은 논리가 감사에도 그대로 적용돼야 합니다. 권한은 백엔드가 지키는데 감사는 브라우저가 쓰면, "권한 + 감사가 합쳐져야 책임이 성립한다"는 원칙이 반쪽이 됩니다.

그 외에 세 가지가 더 걸립니다.

- **시각**: 하드웨어엔 "NTP 동기 필수"가 걸려 있는데 브라우저엔 없습니다. 감사 시각이 사용자 PC 시계면 법적 추적성 주장이 약해집니다.
- **유실**: 명령은 백엔드로 가서 실행됐는데 감사는 별도 경로라, 탭이 닫히거나 네트워크가 끊기면 **"실행은 됐는데 기록이 없음"**이 발생합니다. 감사에선 최악의 실패입니다.
- **노출**: OTel 수집기를 외부에 열고 CORS·토큰을 붙여야 하는데, RBAC은 명령·조회 API에만 걸려 있습니다.

## 왜 이렇게 됐을까 (짐작)

§3 표에서 **`viz.*` 자체 지표와 감사 기록이 한 칸에 묶여** 있습니다. 자체 지표는 "내 렌더 지연이 몇 ms"라서 OTel 직접이 맞아요(②의 "자기 지표는 OTel 직접" 조항). 그런데 **감사는 성격이 완전히 다릅니다.** 지표는 위조돼도 그래프가 이상해질 뿐이지만, 감사는 위조되면 책임소재가 무너집니다. 둘이 한 칸에 들어가면서 지표 규칙이 감사에까지 딸려간 것으로 보입니다.

## 결정적 근거 — 브리지를 따라가면 답이 나온다

②에 따르면 브리지는 **"MQTT에 사는 것(센서값·명령)"을 OTel로 미러**합니다. 내 명령은 WS→백엔드→MQTT로 나가니까, **브리지가 이미 그 명령을 자동으로 OTel에 미러**하고 있습니다. 감사 레코드의 뼈대는 그렇게 생기는 셈이에요.

문제는 `actor`·`origin.path`·`voice.transcript`·`llm.userModified`입니다. 이 값들은 **브라우저와 백엔드 사이에만 존재**합니다. MQTT에 안 실리면 브리지 미러에 안 들어가요. 그렇다고 MQTT에 실으면 **음성 전사 내용과 LLM 메타데이터가 로봇·센서한테까지 흘러갑니다.**

→ 결국 **그 정보를 붙일 수 있는 건 백엔드뿐**입니다.

## 내가 원하는 답

> 가시화는 명령 요청에 `actor`·`origin.path`·`llm.*` 같은 **필드만 실어 보내고**, Log Record 작성은 백엔드 audit-writer가 한다.

이러면 OTel JS SDK를 안 붙여도 되고, `actor`는 토큰에서 백엔드가 뽑으니 위조가 불가능하고, 시각은 서버 시계가 되고, 명령 처리와 감사 기록이 한 덩어리라 유실이 없습니다.

**단, 한 가지 비용이 생깁니다.** 명령이 아닌 순수 화면 이벤트(나중에 노드 편집기의 `viz.config.applied`)는 실어 보낼 명령 API가 없습니다. 그래서 **이벤트 보고 경로 하나를 자리만 열어달라**고 함께 요청해야 합니다. 이건 대규가 O-5를 처리한 방식과 똑같은 논리라 설득이 쉽습니다.

## 이렇게 보낸다

> 통합본 §3에서 가시화의 감사 기록이 `viz.*` 지표와 같은 칸에 묶여 "OTel 직접"으로 돼 있는데, 이 둘은 성격이 좀 다른 것 같습니다. 지표는 제 자체 관측값이라 직접 발행이 맞는데, 감사는 `actor.id`가 브라우저에서 채워지면 클라이언트가 자기 자신을 증언하는 구조가 됩니다. ⑥-3에서 "화면 차단은 우회 가능하니 강제는 백엔드"라고 하신 것과 같은 문제로 보입니다.
>
> 그리고 `actor`·`origin.path`·`voice.transcript`는 브라우저↔백엔드 구간에만 있는 정보라, MQTT에 안 실리면 브리지 미러에도 안 들어갑니다. 그렇다고 MQTT에 실으면 음성 전사가 디바이스까지 가고요.
>
> **가시화는 명령 요청에 이 필드들만 실어 보내고 Log Record 작성은 audit-writer가 하는 쪽**이 맞지 않을까요? (`actor`는 토큰에서 뽑으면 위조 방지도 되고요.) 그렇게 가면 나중 O-5용으로 **명령이 아닌 이벤트 보고 경로**만 자리를 열어두면 될 것 같습니다.

---

# 🔴 2. 상태를 `lifecycle`로 받나, **3층으로 받나**

## 지금 문서엔 두 가지가 같이 있다

한 문서 안에 **상태를 표현하는 방식이 두 개** 들어 있는데, 둘의 관계가 어디에도 안 적혀 있습니다.

- **§1-4 (공통 계약)** → 상태 **3층**
  - `device.status` : ok / degraded / fault (기기 자기보고)
  - `availability` : online / **stale** / offline (서버 판정)
  - `deployment` : deployed / not_deployed (오케스트레이터)
- **부록 A-4 (나에게 오는 상태 메시지)** → **단일 필드**
  - `lifecycle` : running / not_deployed / unreachable

§2.3 개념설명도 lifecycle 어휘로 서술돼 있습니다.

## 왜 곤란한가

**lifecycle 3값으로는 §1-4가 표현하려던 것을 못 담습니다.**

대충 이렇게 대응은 됩니다.

| lifecycle | 3층으로 치면 |
|---|---|
| `running` | availability=online + deployment=deployed |
| `not_deployed` | deployment=not_deployed |
| `unreachable` | availability=offline |

그런데 **두 가지가 갈 곳이 없습니다.**

- `device.status: fault` — **연결은 멀쩡한데 기기가 고장난 상태.** lifecycle로는 `running`입니다. 화면에 정상으로 뜹니다.
- `availability: stale` — **연결은 살아있는데 값이 오래된 상태.** 이것도 lifecycle로는 `running`입니다.

정확히 §1-4가 **"하나로 뭉치면 '연결됐는데 기기 고장'을 표현 못 한다"**고 쓴 그 문제가, lifecycle 단일 필드에서 그대로 일어납니다. 그리고 `stale`은 제가 요청한 "판단 불가" 표시의 근거였는데 그것도 사라집니다.

## 내가 원하는 답

> 상태 메시지에 `lifecycle` 하나가 아니라 **3층을 각각 필드로** 실어 달라.

`{device_status, availability, deployment, last_seen}` 정도면 됩니다. 그러면 화면이 조합해서 4가지 표시(정상 / 장애 / 의도적 미배포 / 판단 불가)를 만들 수 있습니다. lifecycle은 이 조합에서 파생되는 표시용 값으로 두면 되고요.

같이 물을 것: **`stale` 판정은 백엔드가 하나요?** 제가 `last_seen`으로 직접 계산하면 사용자 PC 시계에 의존하게 됩니다. (임계값 자체는 Tier 2라도 판정 주체는 지금 정해져야 합니다.)

## 이렇게 보낸다

> 통합본에 상태 표현이 두 가지로 들어가 있는 것 같습니다. §1-4는 3층(device.status / availability / deployment)인데, 부록 A-4의 상태 메시지는 `lifecycle(running|not_deployed|unreachable)` 단일 필드입니다.
>
> lifecycle 3값으로는 **"연결은 됐는데 기기 fault"**와 **"값이 오래됨(stale)"**이 둘 다 `running`으로 뭉개집니다. §1-4에서 3층으로 나누신 이유가 그대로 사라지는 것 같아서요.
>
> 상태 메시지에 **3층을 각각 필드로**(`device_status`/`availability`/`deployment` + `last_seen`) 실어주시면 화면에서 조합해 4가지 표시를 만들 수 있습니다. 그리고 **`stale` 판정은 백엔드가 해주시는 게 맞을까요?** 제가 `last_seen`으로 계산하면 클라이언트 시계에 의존하게 됩니다.

---

# 🔴 3. "지금 뭐가 있나" 목록을 **어디서 받나**

## 지금 문서엔 빠져 있다

- §2.4에 **"로컬→글로벌 변환 레지스트리(Node 원점의 전역 배치 보유)"**가 백엔드 제공으로 [확정]돼 있습니다. 즉 **레지스트리는 존재합니다.**
- 그런데 §3의 **"백엔드 → 가시화" 행은 여전히 WS 게이트웨이 / 질의 프록시 / 감사 API 3경로뿐**입니다. **레지스트리를 조회하는 경로가 없습니다.**

## 왜 곤란한가 — 가장 명확한 근거

**§2.3이 `not_deployed`(의도적 미배포) 표시를 요구하는데, 미배포 대상은 값이 안 옵니다.**

상태 메시지는 "값을 보내는 대상"만 알려줍니다. 배포가 안 된 로봇은 아무것도 안 보내니까 상태 메시지에 영원히 안 나타나요. **화면에 "여기 로봇이 있어야 하는데 미배포 상태" 라고 그리려면, "존재해야 할 목록"이 따로 있어야 합니다.**

즉 이 경로가 없으면 **§2.3에 [확정]으로 들어간 요구사항이 구현 자체가 불가능합니다.** 이게 가장 설득력 있는 논거입니다.

그 외에도 3D 화면을 그리려면 필요합니다.

- 어떤 entity가 어느 node에 속하는지 (계층 뷰·구역 필터)
- Node 원점의 전역 배치 (구역 바닥·경계를 그리려면)
- Zone 트리 (추상화 축 L2/L1/L0의 뼈대)

## 내가 원하는 답

> 레지스트리 조회를 **계약 항목으로 추가**해 달라. (구체적 형태는 Tier 2여도 됨.)

그리고 **핸드오버로 entity의 node가 바뀌므로**, 변경 통지 자리도 함께 열어두면 좋겠다고 덧붙입니다. MK2가 다중 노드+핸드오버 구조라 결국 필요해집니다. 이것도 O-5를 "자리만 열고 미룬다"고 처리한 것과 같은 논리입니다.

**그때까지 내 우회책**: 정적 구성 파일을 프론트에 동봉해서 개발 진행. (블로킹되지 않음)

## 이렇게 보낸다

> §2.4에 레지스트리(Node 원점의 전역 배치)가 백엔드 제공으로 들어가 있는데, §3의 "백엔드→가시화" 행에는 조회 경로가 없어서 여쭙니다.
>
> §2.3의 `not_deployed`(의도적 미배포) 표시가 [확정]으로 들어가 있는데, **미배포 대상은 값을 안 보내니 상태 메시지에 안 나타납니다.** "여기 있어야 하는데 미배포" 를 그리려면 "존재해야 할 목록"이 따로 필요합니다. 3D 배치용으로 Node 원점·Zone 트리도 같은 소스에서 받으면 될 것 같고요.
>
> **레지스트리 조회를 계약 항목으로만 추가**해 주시면(형태는 Tier 2여도 됩니다) 그때까지는 정적 파일로 개발하겠습니다. 핸드오버로 entity의 node가 바뀌니 **변경 통지 자리**도 같이 열어두면 좋을 것 같습니다.

---

# 🟡 4. `completed`는 백엔드가 붙여주는 것 맞나 (확인)

**거의 답이 나와 있습니다.** 부록 A-4 마지막에 이렇게 적혀 있어요.

> 명령 결과(백엔드→가시화): `command_id` · `status(accepted|rejected|timeout|failed|completed)` · `reason` · `executed_by`

**백엔드가 나에게 `completed`를 포함한 상태를 보낸다**는 뜻입니다. 그리고 하드웨어 ack는 로봇이 `accepted|rejected`, 센서가 `applied|rejected|failed`뿐이라 **`completed`를 만들어낼 수 있는 자리는 백엔드밖에 없습니다.** 논리적으로 이미 확정입니다.

이게 맞으면 제가 원래 요청했던 **"액션별로 ack까지가 확정인지 state까지 봐야 하는지 목록"이 통째로 불필요해집니다.** 프론트는 진행중(`accepted`) / 확정(`completed`) / 실패(`rejected`·`timeout`·`failed`) 3상태만 그리면 끝이에요.

> 부록 A-4의 명령 결과에 `completed`가 있는 걸 보면, **백엔드가 state를 확인하고 `accepted`→`completed`로 승격해서 내려주시는 것**으로 이해했는데 맞을까요? 그렇다면 제가 요청드렸던 "액션별 확정 기준 목록"은 필요 없어지고, 프론트는 진행중/확정/실패 3상태만 그리면 됩니다. 타임아웃 기본값만 나중에 알려주시면 될 것 같습니다.
>
> (사소한 것: 센서 ack의 `applied`가 §1-4 공통 enum에 없는데, 백엔드가 `completed`로 번역하는 것으로 보면 될까요? §3 표에도 "accepted/applied/rejected"로 남아 있습니다.)

---

# 🟡 5. WS 구독은 entity/node로 하나 (확인)

부록 A-4의 상태 메시지 봉투가 **`zone`·`node`·`entity`·timestamp·lifecycle·values·quality·source_ref**입니다. MQTT 토픽 문자열이 아니라 **계약 축으로 말하는 그림**이라 이미 그쪽으로 기울어 있습니다.

제가 확인하고 싶은 건 **구독을 요청할 때도 같은 축으로 하냐**는 것뿐입니다. 메시지 포맷·재연결 같은 세부는 안 물어봅니다(이 결정에서 파생되니까).

> WS 구독 요청도 상태 메시지 봉투처럼 **`{entity, node, channel}` 축으로 하는 것**으로 보면 될까요? 아니면 MQTT 토픽 문자열을 그대로 쓰는 방식인가요? 전자면 토픽 규칙이 바뀌어도 프론트가 안 깨져서 그쪽이 좋을 것 같습니다.

---

# 🔵 6. 회의 안건 — 로봇 state를 retained로 할지 백엔드 캐시로 할지

이건 **나↔백엔드 양자가 아니라 병현이가 걸린 3자 건**이라, 개인 질문이 아니라 회의(§7.4 "3자 정합 확인")에 올려야 합니다.

**어긋난 지점**:

- §2.3 개념설명 → "라이프사이클·상태는 **retained 또는 백엔드 캐시**로 즉시 제공한다" ✅ (내 요구는 반영됨)
- §2.1 표 · 부록 A-1 → 로봇 상태는 여전히 **`QoS0 / retained=false`**

**왜 중요한가**: retained가 아니면 관제 화면을 새로 열거나 재접속했을 때 **다음 발행 주기까지 빈 화면**입니다. 시연에서 제일 티나는 결함이에요.

**어느 쪽이든 무방**합니다. 하드웨어가 retained=true를 켜든, 백엔드가 마지막 상태를 캐시하든 결과는 같습니다. 다만 **누가 담당인지가 안 정해졌습니다.**

> 개인적으로는 **백엔드 캐시 + 구독 시 최신값 1회 즉시 푸시**가 제일 깔끔합니다. 하드웨어 변경이 필요 없고, 프론트도 "구독하면 항상 현재값부터 온다"는 규칙 하나면 초기화 분기가 사라집니다.

---

# 보내는 방법

1. **1·2·3을 한 묶음으로** 먼저. 각각 제 추천안을 붙여서 **"이렇게 이해했는데 맞나요"** 형태로. 대규가 고를 부담이 줄어 답이 빨리 옵니다.
2. **4·5는 같은 메시지 끝에 가볍게.** "확인만 부탁드립니다" 정도로.
3. **6은 회의에서.** 병현이가 있어야 정해집니다.
4. **"문서가 틀렸다" 톤은 쓰지 않습니다.** 1번은 원래 제 v2 문서(§2.2 "가시화 → OTel Log Record")에서 비롯된 것이고, 대규는 그걸 승인한 것뿐일 수 있습니다. **"제가 이렇게 썼는데 다시 보니 이 부분이 걸린다"**가 정확한 프레임입니다.
5. 대규는 지금 3파트 취합 + 교수님 회의 + PM 이관이 겹쳐 있습니다. **목록이 길수록 답이 늦어집니다.**

# 답을 기다리는 동안 할 수 있는 것

- 정적 구성 파일 + 직접 만든 목(mock) 퍼블리셔로 개발 진행
- 상태 뱃지는 **3층 조합 구조로 먼저 설계** (lifecycle로 오면 어댑터 한 겹 씌우면 됨)
- 영상은 녹화 파일 + `frame_ref` 스틸 방식으로 먼저 구현
- 좌표: **AI가 native `{x,y}`로 내고 x-z 변환은 백엔드**로 바뀌었으니(§2.2 어제 결정 철회), 변환 코드는 아예 안 만들어도 됨
