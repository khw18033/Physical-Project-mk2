# 작업 지시: 추가 개선 4건 — 캔버스 높이 · 하드웨어 상태 · 연결 관리 · 배치 모드 정리

**선행**: 노드 캔버스 3단계 완료(`11609ec`) · 요구사항 **v1.2**(`VZ-C-07` 신설 · §7.10)
**대상 요구사항**: `VZ-C-07`(신설) · `VZ-D-07` · `VZ-N-04` · `VZ-U-03`
**기준 커밋**: 착수 시점의 `HEAD`
**날짜**: 2026-09-04

---

## 왜 하나

3단계까지 끝난 화면을 쓰다 걸린 것 넷이다. **넷 다 성격이 다르다.**

| # | 무엇 | 성격 |
|---|---|---|
| 1 | 캔버스 세로가 짧다 | 배치가 **폭만 알고 높이를 모른다** |
| 2 | 하드웨어 카드를 눌러도 상태를 못 본다 | `VZ-D-07` 의 **미구현분** |
| 3 | 접속 주소를 바꾸려면 다시 빌드해야 한다 | **`VZ-C-07` 신설** |
| 4 | DAG/트리 토글이 아무 일도 안 한다 | **회의록 미결이 닫혔다** (§7.10) |

**착수 순서는 1 → 2 → 3 → 4.** 1이 제일 작고 독립적이며, 4는 검사 재정의가 붙어 있어 마지막이다.
**단계마다 따로 커밋하고 보고서를 쓴다.**

---

## 1. 캔버스가 화면 높이를 모른다

### 지금

- `columnsPerBand(availableWidth)` — **폭만** 받는다
- `.graph-scroll { overflow-x:auto; overflow-y:hidden }` — 세로는 아예 스크롤하지 않는다
- 높이 바닥값이 `Math.max(390, 내용)` — 16:9 모니터에서 세로가 남는데 390px 에서 멈춘다

9/1에 「폭을 재서 접는다」를 넣을 때 **높이를 재지 않은 것**이 이제 드러났다.
남는 세로를 안 쓰면서 밴드는 필요 이상으로 접힌다.

### 할 것

1. 폭을 재는 그 `ResizeObserver` 에서 **높이도 같이 잰다.** 훅이 이미 있으니 추가 비용이 거의 없다.
2. `columnsPerBand(width, height)` — 세로가 남으면 열을 줄여 밴드를 늘리고(세로를 쓴다),
   세로가 모자라면 열을 늘려 밴드를 줄인다.
3. 캔버스에 **뷰포트 기반 최소 높이**를 준다(상단 바·머리줄·범례를 뺀 나머지).
   기준은 **16:9 PC** 다. 다만 잰 값 기반으로 짜면 반응형이 따라오므로 고정 픽셀로 못박지 마라.
4. `verify:layout` 을 **최대 y ≤ 잰 높이**까지 검사하도록 확장한다.

### 하지 말 것

- 자동 정렬·확대축소·미니맵을 만들지 마라. **첫 화면에 다 들어오기만 하면 된다**(기존 요건).
- 사용자가 옮긴 노드를 창 크기 변화로 되돌리지 마라 (`VZ-N-04`).

---

## 2. 하드웨어 카드 더블클릭 → 상태 조회

**새 기능이 아니라 `VZ-D-07`(대상 배정과 대상 상태 조회)의 미구현분이다.** 요구사항 신설 없음.

### 할 것

- 하드웨어 패널의 카드를 **더블클릭하면 그 대상의 상태**를 연다.
- **`ActionModal` 의 `device-strip` 을 재사용한다** — 배터리·RSSI·지연·IP·펌웨어·관절 온도·
  하트비트. 두 곳에 다른 상태 화면을 만들면 어휘가 갈린다.
- 확대 규칙은 `VZ-N-05` 그대로다: **오버레이 · 캔버스를 교체하지 않는다 · 동시 하나.**

### ⚠ 자리표시 규칙을 깨지 마라

대본(registry) 세계 장비의 실측값은 **지어내지 않는다**(8/31 결정). 더블클릭하면 뜨는 것은
**「무엇을 · 누구에게서 기다리는가」 카드**다. 그것이 이 화면의 요지다.

**카메라 연결 상태는 계약에 없다.** `Hardware` 타입에 필드가 없고 상대 파트도 미확인이다
(요구사항정의서 §7.10 「열림」). **자리만 만들고 「연결 예정」으로 둔다.** 값을 지어내거나
필드를 임의로 추가하지 마라 — 하드웨어 파트와 맞춘 뒤에 넣는다.

---

## 3. 연결 관리 (`VZ-C-07` · 신설)

### 지금

접속 주소가 **빌드 시점 환경변수**다.

```
transport/index.ts   VITE_GATEWAY_WS  ?? 'ws://127.0.0.1:8790'
                     VITE_GATEWAY_HTTP ?? 'http://127.0.0.1:8790'
stt/SttClient.ts     VITE_STT_URL     ?? 'http://127.0.0.1:8801'
```

바꾸려면 다시 빌드해야 한다. 현장에서 게이트웨이·제어 노드 IP가 바뀔 때마다 빌드할 수 없다.

### 할 것

상단 바 **「알림」 오른쪽에 `⇄ 연결 관리`** 버튼. 대상은 **넷**이다.

| 대상 | 지금 |
|---|---|
| 백엔드 WS 게이트웨이 (WS/HTTP) | 실제로 붙는다 |
| STT 서비스 | 실제로 붙는다 |
| **제어 노드** | **상대 없음 — 자리만, 「연결 예정」** |
| **디지털 트윈** | **상대 없음 — 자리만, 「연결 예정」** |

뒤 둘은 주소를 넣어도 붙을 곳이 없다. **없는 것을 있는 척하지 마라** — 다른 자리표시와 같은 규칙이다.

### 지켜야 하는 것 넷

1. **출입구는 여전히 하나다.** `GATEWAY` 상수를 런타임 스토어로 바꾸되 `getTransport()`
   싱글턴은 유지하고, 주소가 바뀌면 **끊고 다시 붙는다.** 여기저기서 URL 을 읽게 하면
   `verify:one-gateway` 가 무너진다. **이 검사가 이번 단계의 관문이다.**
2. **대상 목록을 한 곳에 둔다.** UI 는 그 목록을 그린다. 화면에 손으로 넷을 적지 마라.
3. **새로고침에 살아남는다.** `VZ-N-04` 의 localStorage 패턴을 재사용하되 **키는 앱 전역**이다
   (임무별이 아니다). 저장소가 막혔을 때는 `try/catch` 로 기본값으로 조용히 진행한다.
4. **되돌아올 길**을 둔다 — 기본값 복원 버튼. 틀린 주소를 넣으면 아무 데도 못 붙는다.

상단의 `conn` 배지는 **상태**, 이 버튼은 **설정**이다. 둘을 섞지 마라.

---

## 4. 배치 모드 토글을 없앤다 (§7.10)

### 왜

`260826_회의록.md` §10 미결 「그래프 형태 vs 트리 형태」가 **닫혔다.** 검토 결과는 그래프다 —
같은 회의 §4-3이 요구한 「여러 갈래가 다시 하나의 박스로 모이는 형태」(합류)를 트리는
표현할 수 없다. 대본 실측으로 뒷받침돼 있다(요구사항정의서 §7.10).

그리고 실제로 **2·3편은 두 배치가 구조적으로 완전히 같아** 토글이 아무 일도 하지 않는다.

### 할 것

| | 무엇 |
|---|---|
| 화면 | **배치 토글 제거** · `layoutMode` 상태 제거 |
| 머리줄 | 고정 문구를 **그 임무의 실측값**으로. 예: `태스크 DAG — 17노드 · 합류 1 · 되돌아감 1` / 합류가 없으면 `태스크 DAG — 16노드 · 일직선` |
| 코드 | **`treeLayout()` 을 지우지 마라.** 부르는 곳이 화면에서 `scripts/` 로 바뀐다 |
| 신설 | `scripts/measure-representation.mjs` + `npm run measure:representation` |

`verify-layout.mjs` 가 이미 `layout.ts` 를 Node 에서 직접 import 해 돌리고 있다. 같은 패턴이다.

측정 스크립트는 대본마다 **노드 수 · 합류 수 · 되돌아감 수 · 트리로 펼쳤을 때 노드 수(배수)**
를 계산해 표로 찍는다. 기대값은 아래와 같다(요구사항정의서 §7.10과 같아야 한다).

```
MSN-260826-01   7노드  합류 1  되돌아감 0   → 11 (×1.57)
MSN-260831-01  17노드  합류 1  되돌아감 0   → 31 (×1.82)
MSN-260831-02  18노드  합류 0  되돌아감 1   → 18 (×1.00)
MSN-260831-03  16노드  합류 0  되돌아감 0   → 16 (×1.00)
```

### ⚠ 화면에서 트리를 언급하지 마라

머리줄에도, `?` 설명서에도, 어디에도 「트리」라는 말을 넣지 마라. 없는 기능을 설명하면
오히려 "그게 뭔데?"가 생긴다. 화면은 **이 임무가 어떻게 생겼는지만** 말한다.
트리 비교는 측정 도구와 논문에서만 다룬다.

### ⚠ `verify:canvas-persist` 를 같이 지우지 마라

지금 이 검사에 **「DAG↔트리 전환이 뷰 노드를 지우지 않는가」**가 들어 있다. 토글이 없어지면
그 계기가 사라지지만 **규칙 자체는 살아야 한다.** 검사 대상을
**「창 폭 변경 · 보기 범위(마일스톤↔임무 전체) 전환」**으로 옮겨라.
검사를 지우면 폭이 바뀔 때 뷰 노드가 날아가는 회귀를 아무도 못 잡는다.

### 함께 고칠 문서

- `README.md` 16행 「태스크 그래프 (DAG/트리 · 밴드 접기)」
- `HANDOFF.md` — 전달본 화면에서도 토글이 없어진다

---

## 검증

| 검사 | 무엇을 |
|---|---|
| `verify:layout` (개정) | DAG 하나 · 최대 x **와 최대 y** 가 잰 폭·높이 안에 |
| `verify:canvas-persist` (**대상 재정의**) | 폭 변경 · 범위 전환에서 뷰 노드가 살아 있는가 |
| `verify:one-gateway` (**유지**) | 주소가 런타임으로 바뀌어도 출입구가 하나인가 |
| `verify:standalone` (**무개정 통과**) | 단독 번들에 `tabs/` 가 들어오면 실패 |
| `verify:no-tabs` (유지) | 확대가 캔버스를 교체하지 않는가 · 동시 하나 |
| `measure:representation` (신설) | 위 기대값과 일치하는가 |

기존 18종 + `typecheck` 전부 통과. `verify:standalone` 은 Windows 에서 실행한다.

---

## 건드리지 않는 것

- **공유 계층의 나머지 16건** — `VZ-C-07` 하나만 는다
- **마일스톤 목록 화면** — `VZ-U-05` 대체의 근거다
- `useTabsDataLayer()` — 앱 수명과 같아야 한다
- **뷰 노드를 `deps` 에 넣지 마라**
- `web-dashboard`(기준선) · `stt-lab` · `scenarios/*.json` 과 사이드카 · `stt/engines/*.py`
- `5tab-baseline` 태그 — 논문 대조군이다. 지우거나 옮기지 마라

---

## 보고서

단계마다 하나씩 — `reports/2026-09-04_추가개선_N단계.md`. 최소한 아래를 적어라.

- 1단계: 어느 창 크기에서 몇 밴드 × 몇 열이 됐는지, 세로를 얼마나 더 쓰게 됐는지 **실측**
- 2단계: 자리표시로 남긴 항목과 그 사유 (특히 카메라)
- 3단계: `verify:one-gateway` 를 어떻게 지켰는지 — 싱글턴 재연결 구조
- 4단계: `measure:representation` 출력 전문, `verify:canvas-persist` 를 어떻게 재정의했는지
- 지시서와 다르게 한 곳과 그 이유
