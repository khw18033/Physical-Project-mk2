# 가시화 담당(현우) 참고 정리 — 260819 (rev.4)

> 출처: `가시화/하드웨어/AI_담당_요구사항_정의서_통합정립본_v0.docx` + `캡스톤MK2_백엔드_결정종합_260819.md`
> 한 줄 요약: **v2 스킴 그대로 채택됨. 브라우저는 뭐든 백엔드를 경유하고, 좌표 변환·감사 저장·권한은 전부 백엔드가 해준다. 나는 "받아서 그리고, 명령은 백엔드로 던지고, 내 지표만 OTel로 직접 낸다."**

---

## 1. 내 요구사항 중 통과된 것 (그대로 진행)

| 항목 | 결과 |
|---|---|
| v2 스킴 (Entity/Node/Zone + 공간축·추상화축) | **채택** — 재작업 없음 |
| 식별자 | Entity = `agent_id`/`sensor_id`, Node = `site_id`, Zone = 선택(없으면 domain) |
| 감사 척추 (origin.path·actor·traceparent) | **채택**, 백엔드가 OTel Log Record → MySQL 영구 저장 |
| 감사 조회 API | **제공됨** (target·기간·actor·origin.path 필터) → "이 대상 마지막 조작자" 화면 표시 가능 |
| 브라우저 실시간 | **백엔드 WS 게이트웨이** (구독 + 명령 발행 양방향, 단명 토큰) |
| Prometheus 질의 | **백엔드 질의 프록시** (`POST /query`, instant/range) |
| 권한 | 백엔드 RBAC `viewer/operator/admin` + `GET /me/role` 제공 |
| Grafana | 내부 모니터링용. **대외 최종 화면은 내 자체 프론트**. 필요하면 패널 임베드 허용 |
| O-5 (구성 반영 감사) | 개념 채택, **발행은 노드 편집기 나올 때**. 지금은 `event.name` 자리만 열어둠 |

**이중 발행 우려 해소**: MQTT→OTel 브리지를 백엔드가 만든다. 나는 감사를 MQTT/OTel 양쪽에 낼 필요 없음.

---

## 2. 내가 지켜야 할 계약 (프론트 구현에 직결)

### 2-1. 접근 경로 — 브라우저는 직접 붙지 않는다
- 브로커·Prometheus·MySQL **직접 접속 금지**. 전부 백엔드 경유(자격증명 은닉·권한·rate limit).
- 실시간 = WS 게이트웨이 / 과거 지표 = 질의 프록시 / 감사 = 감사 조회 API. **세 개가 내 데이터 입구 전부.**
- 제어 명령도 WS 게이트웨이를 통해 백엔드로 → 백엔드가 토큰 역할 검증 후 MQTT 발행.

### 2-2. 라벨 — 화면 컴포넌트 매칭의 열쇠
- **최소 라벨셋 = `domain` + `entity` + `node`** (예: domain=capstone, entity=go1-01, node=zone-503).
  → 이 셋이 있어야 "이 지표가 어느 화면 요소 것인지" 판정 가능. (내 I-2 질문의 답)
- 물리 생산자(로봇·센서)는 `entity`가 **Resource**, SW 생산자(AI·가시화)는 `entity`가 **라벨**.
  → 내가 남(go1-01)을 처리하며 내는 지표엔 entity를 **라벨로** 붙인다.

### 2-3. 내 지표(`viz.*`)는 OTel 직접 발행
- 브리지 대상 아님. 내 컴포넌트 성능 지표(렌더 지연 등)는 내가 OTel로 직접.
- 이름 = `viz.*` + 점 표기, **단위는 이름에 붙이지 말고 `unit` 필드로**.

### 2-4. traceparent는 "복사만"
- span 생성 주체 = 백엔드 audit-writer. 가시화는 **traceparent를 만들거나 복사만** 하고 span은 만들지 않는다.
- 상관키 2종 병용: `traceparent`(인과 사슬) + `command_id`(명령↔결과 짝짓기). 감사엔 둘 다 실음.
- 주기 상태값(state)엔 trace 미부착 — metric으로만.

---

## 3. 화면 설계에 바로 반영해야 할 것

### 3-1. 상태 3층 → 내 "4가지 표시"가 여기서 파생
| 층 | 값 | 원천 |
|---|---|---|
| `device.status` | ok / degraded / fault | 기기 자기보고 |
| `availability` | online / **stale** / offline | 서버 파생(마지막 수신 + 임계) |
| `deployment` | deployed / not_deployed | 오케스트레이터 |

- 내가 요청한 4상태(정상 / 장애 / 의도적 미배포 / 판단 불가) = **이 3층의 조합으로 표현**.
- **"판단 불가" = `availability: stale`로 흡수** (연결은 살아있는데 값이 오래됨).
- 즉 UI는 단일 status 뱃지가 아니라 **3층을 조합해 렌더**해야 한다.

### 3-2. 공통 enum (화면 문구·필터에 그대로 사용)
- `command.result`: accepted / rejected / timeout / failed / completed
- `origin.path`: manual / voice / llm_suggestion / auto  ← 음성·LLM 배지 표시에 사용
- 도메인 특화 상태(수문 open/closed 등)는 **표준 아님** → 도메인별 어휘집. 공통 컴포넌트가 하드코딩하면 안 됨.

### 3-3. 좌표 — 변환은 백엔드가 한다
- 축 규약: **y = 위, 바닥 = x-z 평면, 1 unit = 1 m, 3D 유지(지상 로봇 y=0)**. "Unity"가 아니라 "디지털 트윈 가상환경 좌표계"로 표기.
- 각 개체는 **자기 Node 로컬 좌표**로 보고 + `frame_id`(어느 프레임) + `source`(odometry / perception / anchor_corrected).
- **로컬→글로벌 변환은 백엔드 레지스트리 담당.** 내가 원점 배치를 계산할 필요 없음.
- 다만 `source`는 화면에 의미가 있음: odometry(드리프트 있음) vs anchor_corrected(보정됨) vs perception(AI 추정) → **위치 신뢰도 표현에 쓸 수 있는 필드.**

---

## 4. 다른 파트 → 나에게 들어오는 데이터 변화

### 하드웨어(병현) 쪽 변경 — 내가 받는 형태가 바뀜
- `position {x,z}` → **`{x,y,z}` + `frame_id` + `source`** (지상 y=0)
- **로봇에 `site_id`(Node) 라벨 추가** → 로봇이 어느 구역인지 화면·핸드오버가 알 수 있게 됨
- 메트릭 단위 접미 제거 → `unit` 필드 (`robot.speed.mps` → `robot.speed` + unit=m/s)
  → **기존 프로토타입에서 이름으로 단위 파싱하던 코드가 있으면 수정 필요**
- `waypoints`는 `{x,z}` 2D 유지
- 유지: 토픽 규칙, QoS/retained/LWT, `frame_ref`, `seq`, command_id 멱등, **"성공은 ack가 아니라 state로 최종 확인"**

### AI(나영) 쪽 — 내가 요청한 4건(A-1~A-4) **전부 승인됨**
| | 내용 | 나에게 주는 이득 |
|---|---|---|
| A-1 | `frame_ref` 되돌리기 | 박스를 **맞는 프레임 위에** 겹칠 수 있음 |
| A-2 | bbox 좌표계 명시(정규화 여부·원점·기준 해상도) | 추론 해상도 ≠ 표시 해상도일 때 어긋남 방지 |
| A-3 | 추상화 수준 표기(원본 vs 집약) | risk·priority 같은 집약값 **재집약 오류 방지** |
| A-4 | `reason` 구조화 `[{factor,value}]` | AI 판단 근거를 **화면에서 파싱해 표시** 가능 |

- `waypoints {x,y}` → `{x,z}`, `position_world`는 y=위 + `frame_id` + `source=perception`
- ※ AI 장애물의 `frame_id`는 **로봇을 가리킴**(카메라 기준). 백엔드가 로봇→구역→전역으로 이어붙임.
- AI 산출물은 **계획·추천까지**. 로봇 제어 프로토콜 변환은 백엔드.

---

## 5. 백엔드(대규)에게 물을 것

> **전제**: 나에게 배정된 [확인 요망]은 없다(감사 보존 기간·영상 규격 모두 Tier 2로 이관). AI 쪽 변경은 나영이 전부 승인.
> **지금 단계 = 요구사항 취합·일관성 리뷰**(결정문 §7.1). 구현은 AI 에이전트가 계약에서 파생시키는 구조(§1.1)이므로,
> **물어볼 것은 "계약에 빠졌거나 서로 어긋난 것"까지**다. 엔드포인트·스키마·수치는 이 단계에서 요구하지 않는다.

### 5-1. 계약급 4건 — 지금 물을 것

**① 감사 발행의 주체 — 주어가 빠져 있음** (최우선)

*모순이 아니라 미규정이다. 그리고 이 문장의 출처는 내 v2 문서다 — 던질 때 주의.*

- **경로 원칙 쪽**: ①("브라우저에 브로커 자격증명 안 내림") · ⑤("브라우저 DB 직접 접근 금지") · ⑥-1("Prometheus도 프록시 경유") → 세 결정이 **"브라우저는 백엔드 뒤에만 선다"**를 반복.
- **감사 발행 쪽**: §5 "감사 = OTel Log Record로 **발행**", 통합정립본 "감사 기록은 OTel Log Record로 **냅니다**" → **주어 없음.** 문자 그대로 "가시화가 낸다"로 읽으면 브라우저가 OTLP에 직접 붙는 **네 번째 아웃바운드 경로**가 생겨 위 원칙과 어긋난다.
- **출처 주의**: 내 v2 §2.2가 "표준 출력 스키마 ① 제어 명령 감사 기록 — **가시화 → OTel Log Record**"로 시작한다. 대규는 이걸 승인한 것뿐일 수 있다. → **"결정문이 모순"이 아니라 "내가 이렇게 썼는데 ①과 같이 놓으면 주체가 어느 쪽인가"**로 물어야 한다.
- (v2 §2.3의 D-8은 "브리지 있으니 이중 발행 불필요"로 답이 왔지만, **이중 발행 여부와 누가 쓰느냐는 다른 질문**이라 주체는 여전히 미해소.)

**프론트가 직접 쓰면 걸리는 것**

| | 문제 |
|---|---|
| **⑥-3 논리와 충돌** (결정적) | ⑥-3은 "화면단 차단은 우회 가능, 실제 강제는 백엔드"라 했다. 브라우저가 감사를 직접 쓰면 `actor.id`가 **클라이언트 자기신고** → 위조 가능. ⑥-3의 근거인 "권한 + 감사가 합쳐져야 책임 성립"이 반쪽이 됨 |
| 시계 | 하드웨어엔 "NTP 동기 필수"가 걸려 있으나 브라우저엔 없음. `event.timestamp`가 클라 시계면 법적 추적성이 약해짐 |
| 유실 | 명령은 WS로 가서 실행됐는데 감사는 별도 경로 → 탭 닫힘·네트워크 끊김 시 "실행됐는데 기록 없음" |
| 노출 | collector를 외부에 열고 CORS·토큰 필요. ⑥-3 RBAC은 명령·조회 API에만 걸려 있음 |

**브리지를 따라가면 답이 나온다 (가장 단단한 근거)**
②에 따르면 브리지는 "MQTT에 사는 것(센서값·**명령**)"을 미러한다. 내 명령은 WS→백엔드→MQTT로 나가므로 **브리지가 이미 자동 미러**한다. 그런데 `actor`·`origin.path`·`voice.transcript`·`llm.userModified`는 **브라우저↔백엔드 구간에만 있는 정보**라 MQTT에 안 실리면 미러에 안 들어간다. 그렇다고 MQTT에 실으면 음성 전사·LLM 메타데이터가 로봇·디바이스까지 흘러간다. → **그 구간 정보를 붙일 수 있는 건 백엔드뿐.**

| | 선택지 | 평가 |
|---|---|---|
| **A안** ⭐ | 백엔드 대행 — 프론트는 명령 요청에 `actor`·`origin.path`·`llm.*`만 실음, Log Record 작성은 audit-writer | OTel JS SDK 불필요. `actor`를 토큰에서 뽑으면 위조 불가. 서버 시계. "span 생성 = 백엔드"와 일관 |
| B안 | 브라우저가 OTLP 직접 발행 | 위 4가지 문제를 전부 떠안음 |

**A안의 실제 비용**: 명령이 아닌 순수 UI 이벤트(O-5 `viz.config.applied`)는 실어 보낼 명령 API가 없다 → **이벤트 보고 경로가 별도로 필요.** 이건 ⑤가 O-5를 처리한 방식과 같은 논리("지금 자리 하나 여는 비용 << 나중에 계층 끼우는 비용" — 대규 본인 문장)로 요청하면 된다.

**→ 던질 문장**
> v2 §2.2에 "가시화 → OTel Log Record"라고 썼는데, ①의 "브라우저는 백엔드 경유" 원칙과 같이 놓으면 **가시화는 명령 요청에 `actor`·`origin.path`·`llm.*`만 실어 보내고 Log Record 작성은 audit-writer가 하는 것**으로 이해했습니다. 맞을까요? (`actor`는 토큰에서 백엔드가 뽑는 편이 위조 방지에 맞다고 봅니다.) 그리고 나중 O-5용으로 **명령이 아닌 이벤트 보고 경로**도 자리만 열어두면 좋겠습니다.

→ "네" 한 마디로 끝낼 수 있고, 내 쪽은 **OTel JS SDK 도입 여부**가 바로 결정된다.

**② 레지스트리 조회 — 내 '수신' 칸의 빈칸**
§3.1의 산출물이 "3파트 × 제공/수신 매트릭스"인데, 내 수신 칸에 **"지금 어떤 entity가 어느 node에 있고, node 원점이 전역 어디인가"** 항목이 없다. 3D 배치에 필수인데 계약에 안 잡혀 있음.
- **A안 ⭐** 레지스트리 조회 인터페이스를 계약에 항목으로 추가(형태는 Tier 2).
- B안 시연 한정으로 정적 구성 파일. ← 규격 나올 때까지 내 우회책.

→ 핸드오버로 entity의 node가 바뀌므로 **변경 통지 자리**도 함께 열어두자고 제안(O-5를 "자리만 열고 미룬" 것과 같은 논리).

**③ `completed` 판정 주체** (H-6 재프레이밍)
④-3에 "accepted→completed는 state 확정"이 있다. **백엔드가 state를 보고 승격해준다면** 내가 원래 요청한 "액션별 ack/state 판정 목록"은 불필요해진다.
- **A안 ⭐** 백엔드가 승격 발행 → 프론트는 진행중/확정/실패 **3상태 렌더로 끝**.
- B안 프론트가 state를 직접 판정 → 액션별 규칙 목록 필요.

→ 목록을 달라고 하지 말고 *"백엔드가 승격해주는 것 맞죠?"* 로 묻는다. A안이면 질문 하나가 통째로 사라진다.

**④ WS 게이트웨이의 구독 단위 — 방향만**
- **A안 ⭐ 논리 구독** `{entity, node, channel}` — v2 스킴과 정합. 토픽은 백엔드 내부 구현으로 감춤.
- B안 MQTT 토픽 문자열 그대로 노출 — 프론트가 토픽 문법에 묶임.

→ 메시지 포맷·재연결·백프레셔는 **묻지 않는다**(이 결정에서 파생됨).

### 5-2. Tier 2 우선순위로 올릴 것 (지금 답 요구 X)

결정문 §7.4에 이미 **"Tier 2를 언제 채울지"**가 안건으로 있다. 아래 셋을 **앞순위로** 올려달라고만 요청:

1. **영상 변환 주체** — RTSP를 브라우저용으로 바꾸는 지점이 백엔드 미디어 프록시인지. 규격 상세는 나중이어도 **주체 한 줄**이면 위젯 구조를 잡을 수 있음. (+ `stream_url` 협의 대상에 가시화 포함 — 하드웨어 문서 7장 협의표에 내가 빠져 있음)
2. **초기 상태 스냅샷**(H-3) — retained=false면 화면 새로 열 때 다음 주기까지 빈 화면. 하드웨어 retained / 백엔드 캐시 / 구독 시 1회 푸시 중 어느 쪽이 담당인지. 결정문이 이 건에 답을 안 함.
3. **레지스트리 조회 형태** — 5-1 ②의 후속.

### 5-3. 묻지 않을 것 (이미 답이 있거나 다음 단계)

| 항목 | 이유 |
|---|---|
| 질의 타임아웃·rate limit·보존·해상도 | ⑥-1이 "Tier 2"라고 이미 명시 |
| `stale` 임계값 | ④-3이 "SLA, Tier 2"라고 이미 명시 |
| 토큰 만료·갱신·401/403 | 권한 매트릭스와 같은 급 → Tier 2 |
| WS 메시지 스키마 파일(JSON Schema/AsyncAPI) | 계약에서 **AI가 파생**시킬 산출물(§1.1). 인간 PM에게 요구할 것 아님 |
| 개발용 docker-compose·목 데이터 | 회의 안건 아님. 필요하면 개인적으로 요청 |

### 5-4. 던지는 방식

- **4건을 한 묶음으로, 각각 내 추천안을 붙여 확인받는 형태**로. 대규가 고를 부담이 줄어 답이 빨리 온다.
- **"결정문이 틀렸다" 톤은 쓰지 않는다.** ①은 내 v2 문서에서 비롯된 미규정이고, ②③④도 "빠졌다"가 아니라 "이렇게 이해했는데 맞나"로 던진다.
- 전부 나↔백엔드 양자 건이라 **회의 안건(7.4 3자 정합)이 아니다.** 별도 트랙으로.
- 대규는 지금 3파트 취합 + 교수님 회의 + PM 이관이 겹쳐 있다. 목록이 길수록 답이 늦어진다.

---

## 6. 우선순위 체크리스트

**백엔드에 요청 (지금)**

1. 감사 발행 **주체** 확인 — 백엔드 대행안 + O-5용 이벤트 경로 자리 (5-1 ①)
2. 레지스트리 조회를 계약 항목으로 추가 (5-1 ②)
3. `completed` 승격 주체 확인 (5-1 ③)
4. WS 구독 단위 — 논리 구독 방향 확인 (5-1 ④)
5. Tier 2 앞순위 요청 3건 (5-2)

**내 코드에 반영 (규격 나오면)**

6. 데이터 접근을 **WS 게이트웨이 / 질의 프록시 / 감사 API 3경로**로 정리
7. 상태 뱃지를 **3층 조합 렌더**로 리팩터 (`stale` 추가)
8. 좌표 코드에서 **로컬→글로벌 변환 제거**(백엔드 담당), `frame_id`·`source` 표시 추가
9. 단위 파싱 로직 → `unit` 필드 읽기로 변경
10. 감사 패널: `actor` · `origin.path` · `command_id` · `command.result` 5값 기준

**병행 (블로킹 회피)**

11. 정적 레지스트리 파일 + 내가 만든 목 퍼블리셔로 개발 진행
12. 영상은 녹화 파일 + `frame_ref` 스틸 폴링으로 먼저 구현, 실시간 스트림은 나중에 교체
