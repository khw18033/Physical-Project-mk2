# 가시화 파트 구현 현황 — 2026-08-24

> 기준: 교수님께 제출한 요구사항 시트(`피지컬팀 프로젝트 mk2 요구사항 정의서.xlsx`) 김현우 시트 **33건**
> 대조 방법: 요구사항 ID를 코드에서 역추적해 데이터 레이어·화면 양쪽에 실체가 있는지 확인
> 2026-08-25 개정: Phase 2 잔여 표시 4건 반영. 웹 타입 검사·프로덕션 빌드·재접속 캐시 검증 통과.
> 2026-08-25 재개정: 대조(`reports/2026-08-25_1804_node-graph-doc-code-audit.md`)에서 드러난
> `VZ-U-04`의 근거 공백을 요건 단위로 다시 적고, 그 공백을 메운 작업
> (`reports/2026-08-25_1930_node-graph-contract-alignment.md`)의 결과를 반영했다.

## 한눈에

| 구분 | 건수 | 비율 |
|---|---|---|
| **동작하는 것** | 27 | 82% |
| **아직 없는 것** | 6 | 18% |

핵심은 숫자보다 **어느 27건인가**입니다. 웹의 데이터·제어·영상 뼈대뿐 아니라 위험 판단·실패 알림·계층 뷰·노드 편집기와 Unity 트윈 뷰어까지 계약 경계를 지켜 왕복합니다. 남은 6건은 음성·LLM과 타 파트 합의 대기 항목입니다.

---

# 1. 동작하는 것 (27건)

## 1-1. 데이터가 들어오는 길 — 입구 5개 전부 열림

| ID | 내용 | 확인 방법 |
|---|---|---|
| `VZ-I-01` | 실시간 상태 구독 (상태 3층) | 현황판이 갱신되는 것 |
| `VZ-I-02` | 재접속 시 현재값 1회 확보 | 새로고침해도 **빈 카드가 없음** |
| `VZ-I-03` | 레지스트리 조회 | `robot-03`이 값 없이도 카드로 보임 |
| `VZ-I-04` | 지표 질의 (요약/원본 두 경로) | 지표 탭의 "원본 보기" 전환 |
| `VZ-I-05` | 감사 이력 조회 | 제어 패널의 마지막 조작자 |

**입구가 다섯이라는 구조가 실제로 그렇게 구현돼 있습니다.** 브라우저가 브로커나 DB에 직접 붙는 경로는 코드에 없습니다.

## 1-2. 상태를 다루는 방식

| ID | 내용 | 왜 중요한가 |
|---|---|---|
| `VZ-U-01` | 상태 4종 구분 표시 | 3층(기기·서버·배포)을 각각 보관하고 표시값만 파생. **뭉쳐서 저장하지 않음** |
| `VZ-C-03` | 집약 계층 표기 | 요약값을 원본으로 오인해 재집약하는 것을 **차단**(경고가 아니라 계산 중단) |
| `VZ-C-04` | 권한 범위 | 담당 구역 밖 대상은 버튼 잠금 + 서버도 거부 |
| `VZ-C-01` | 권한·역할 확인 | 로그인 시 1회 조회, 주기 폴링 없음 |
| `VZ-C-02` | 외부 소스 부재 시 동작 | 레지스트리가 정적 파일이어도 화면이 돌아감 |
| `VZ-C-05` | 설계 규모 가정 | 상수로 명시 (구역 1 · 장치 20 · 사용자 1) |
| `VZ-I-11` | 구독 범위 한정 | **자리 확보 상태** — 필드가 실제로 왕복만 함 |

## 1-3. 명령을 보내고 결과를 받는 길

| ID | 내용 |
|---|---|
| `VZ-O-01` | 제어 명령 발행 — 요청 식별자는 가시화, **상관키는 백엔드 발급** |
| `VZ-O-02` | 명령 결과 4단계 수신 → 3종 표시 |
| `VZ-O-03` | 책임소재 필드 전달 (기록 작성은 백엔드) |
| `VZ-O-05` | 제어 잠금 상태와 사유 표시 |

## 1-4. 임무·영상·트윈

| ID | 내용 |
|---|---|
| `VZ-U-07` | 계획 승인 — **승인 전에는 실행되지 않음**, 근거를 펼쳐 봄 |
| `VZ-U-05` | 서브태스크 진행 — 어느 구간에서 왜 실패했는지 |
| `VZ-I-06` | 영상 스트림 표시 (합성 영상으로 대체) |
| `VZ-I-07` | 탐지 오버레이 정합 — **프레임 참조로 맞춤**. 온디바이스 최소 안전 판단과 엣지 정밀 인지를 출처별로 구분 |
| `VZ-I-09` | 객체 추적·궤적, 신뢰도 낮은 탐지 구분. 다중 관측 연계가 없으면 소스별 추적 유지 |
| `VZ-U-02` | Unity 디지털 트윈 위치 렌더링 — 웹과 같은 WS 봉투·레지스트리·상태 규약을 사용하고 좌표 보간·손잡이 진단 제공 |

## 1-5. 판단·알림·계층 뷰

| ID | 내용 |
|---|---|
| `VZ-I-08` | 위험도 평시·관찰·경보·복구와 점수·판단 근거·권고 조치 표시 |
| `VZ-I-10` | AI 실패 이벤트를 지표 폴링과 분리해 즉시 표시. 컴포넌트·모델·입력·오류 포함 |
| `VZ-O-04` | 가시화 자체 봉투 처리 지연을 60초 창으로 관측 수신 경로에 발행 |
| `VZ-U-03` | 같은 원본을 결심자·운영자·개발자 세 깊이로 표시하며 전환 시 재구독하지 않음 |

## 1-6. 노드 그래프 편집기

| ID | 내용 |
|---|---|
| `VZ-U-04` | **임무 관제 그래프가 기본** — 임무→Sub task 순서와 근거 노드 연결, 실시간 진행/실패 전파. 데이터 파이프라인 편집·검증·반영·관측은 보조 모드 |

`VZ-U-04`는 시트에서 한 줄이지만 요구사항 정의서로는 **REQ-1001~1007 일곱 건**이다.
한 줄로 적어 두면 어느 요건이 근거이고 어느 요건이 비어 있는지가 드러나지 않아, 실제로
2026-08-25 대조에서 세 건이 비어 있는 채로 완료로 읽히는 일이 있었다. 요건 단위로 적는다.

| 요건 | 상태 | 근거 |
|---|---|---|
| `REQ-1001` 노드 그래프 시각 편집 | 동작 | 캔버스 배치·포트 연결 (`src/views/NodeGraphView.tsx`) |
| `REQ-1002` 계약과의 무손실 왕복 | 동작 | 초안 = `{ pipeline: 계약, layout: 좌표 }`. 직렬화는 `shared/pipeline-contract.ts` 한 곳. `npm run verify:contract-roundtrip` |
| `REQ-1003` 노드 카탈로그 자동 구성 | 동작 | `contracts/examples/`의 디스크립터·렌더러·데이터소스에서 파생. `npm run verify:catalog-derived` |
| `REQ-1004` 연결 유효성 강제 | 동작 | 포트 타입을 카탈로그에서 끌어와 대조, 사유 표시. `npm run verify:pipeline` |
| `REQ-1005` 시험 실행 | 동작 (**목 실행기**) | 노드별 결과와 시험 토큰. 행 수·소요 시간은 지어낸 값이고 화면이 그 사실을 표기한다 |
| `REQ-1006` 반영·되돌리기 | 동작 | 시험 증명 없는 반영 거부, 직전 버전 복구, 감사 |
| `REQ-1007` 역방향 관측 | 동작 (**부분**) | 반영된 그래프의 노드별 최근 수신 시각·건수·마지막 값. source는 실수신, 하류는 목 실행기 전달이며 응답과 화면이 둘을 구분한다 |

**완료 27건 숫자는 그대로 둔다.** 시트 33건이 세는 단위는 `VZ-*` 한 줄이고 `VZ-U-04`는
그 전에도 후에도 한 줄이다. 이번 작업으로 바뀐 것은 건수가 아니라 **그 한 줄이 서 있는
근거의 범위**다 — 대조 시점에는 REQ-1001·1004·1005·1006 네 건이었고 지금은 일곱 건이다.

LLM 초안 생성(`VZ-L-04`)은 아직 붙이지 않았다. 사람이 만든 초안과 마찬가지로 추후 LLM 초안도 시험 실행과 명시적 반영을 우회할 수 없다.

---

# 2. 아직 없는 것 (6건)

## 2-1. 음성·LLM — 4건

| ID | 내용 |
|---|---|
| `VZ-L-01` | 음성 입력(STT) 및 텍스트 전달 |
| `VZ-L-02` | 해석 결과 표시 및 수락 |
| `VZ-L-03` | 실행 전 2단 확인 · 신뢰도 임계 |
| `VZ-L-04` | 화면 구성 보조 LLM |

STT를 가시화가 맡는 것으로 확정됐습니다. 목업은 있고 구현이 없습니다.

## 2-2. 다른 파트를 기다리는 것 — 2건

| ID | 내용 | 무엇을 기다리나 |
|---|---|---|
| `VZ-U-06` | 미확인 탐지 그룹 확인 | AI와 전달 형식을 맞춰야 함 |
| `VZ-C-06` | 원천 종류(실물·시뮬레이션) 표기 | **표기를 붙일 주체가 아직 없음** |

---

# 3. 남은 순서

| 순서 | 무엇 | 왜 이 순서인가 |
|---|---|---|
| **1** | 음성·LLM 4건 | 의도 해석 주체·출력 스키마·비-GenAI 경로 확정 후 |
| **2** | LLM 그래프 초안 생성 | 편집기는 완료. `VZ-L-04` 계약 확정 뒤 같은 검증·반영 절차 앞에 연결 |
| **3** | Unity 시뮬레이터 (2단계) | 뷰어 왕복은 검증 완료. 계약이 굳은 뒤 원천을 교체 |
| 대기 | `VZ-U-06` · `VZ-C-06` | 다른 파트 합의 후 |

---

# 4. 회의에서 정해야 할 것

| # | 안건 | 상대 | 정하지 않으면 |
|---|---|---|---|
| 1 | **미디어 경로의 뷰어용 출력 분기** | 3자 | `AI-C-08` 어댑터와 `AI-C-14` 미디어 평면은 생겼지만 뷰어 출력 주체가 없음 |
| 2 | **명령 접수 응답에 요청 식별자를 에코하는가** | 이대규 | 두 키 매핑이 성립하지 않아 진행 표시가 엉뚱한 카드에 붙음 |
| 3 | **원천 종류 표기의 발행 주체·형식** (`VZ-C-06`) | 3자 | 시뮬레이션 조작과 실물 조작이 감사에서 구분되지 않음 |
| 4 | **좌표계 손잡이(handedness) 규약** | 이대규 | Unity는 왼손 좌표계. 오른손 관례로 값이 오면 **좌우가 뒤집힘** |
| 5 | **설계 규모 가정** (`VZ-C-05`) | 전 파트 | 모든 주기 산정의 전제인데 문서에 숫자가 없음 |
| 6 | **capability 상태 전달** | 진나영 | 선택 기능의 미배포와 장애를 화면에서 구분할 수 없음 |
| 7 | **명령 의도 해석 주체·출력 스키마** | 진나영 | `AI-C-09`는 호출 어댑터일 뿐 기능 주체와 결과 계약이 비어 있음 |

`BE-T-03`의 특정 브라우저 결속은 **가시화 클라이언트**로 일반화되어 해소됐습니다. `BE-T-06` 캐시 범위는 회신 문서 작성과 목 게이트웨이 검증까지 끝났고 팀 전달만 남았습니다.

---

# 5. 현재 검증 상태

| 검증 | 결과 |
|---|---|
| `npm.cmd run typecheck` | 통과 |
| `npm.cmd run build` | 통과 — Vite 62 modules · JS 307.17 kB (gzip 96.09 kB) |
| `npm.cmd run verify:cache` | 통과 — 금지 채널 재생 0, 허용 밖 채널 0, 원본 `ts` 보존 |
| `npm.cmd run verify:pipeline` | 통과 — 카탈로그 14종, 검증 거부 5종, 시험 토큰·반영·되돌리기·감사·역방향 관측 |
| `npm.cmd run verify:mission-graph` | 통과 — Sub task 5개, 승인 전 진행 0건, 최종 `done → done → done → failed → skipped` |
| `npm.cmd run verify:contract-roundtrip` | 통과 — 초안↔계약 왕복, 좌표 격리, `pipeline.schema.json` 검증 (REQ-1002) |
| `npm.cmd run verify:catalog-derived` | 통과 — 예시 파일 1개 추가로 카탈로그 14→15종, `.ts` 수정 0건 (REQ-1003) |
| Unity | 계약 검증 **9개 항목 통과**(8/21 실측) · 웹과 상태 대조 **10대상 × 2시점 = 20건 일치**. 에디터 재검증 절차는 `unity-twin/README.md` 참고 |

> `frame_ref`가 버퍼에 없을 때 이를 정합 성공처럼 보이지 않게 표시하는 보완은 `09add2c`에 반영됐습니다. 기존 요구사항의 실패 상태 표현을 보강한 것이므로 완료 기능 수에는 별도 가산하지 않았습니다.
