# 작업 지시: 노드 그래프 계약 정합 및 누락 요건 보완

## 배경

이 저장소는 하이브리드 디지털 트윈 프레임워크의 **가시화 파트**다.
노드 그래프 탭(`VZ-U-04`)은 이미 구현돼 있고 임무 관제 그래프와 데이터 파이프라인
편집기가 동작한다. 검증 스크립트도 전부 통과한다.

**이번 작업은 새 기능 추가가 아니라 정합 복구다.** 대조 결과
(`reports/2026-08-25_1804_node-graph-doc-code-audit.md`) 문서가 완료라고 적은 `VZ-U-04`의
실제 근거는 REQ-1001·1004·1005·1006 네 건뿐이고, 다음 세 요건이 비어 있다.

- **REQ-1002 무손실 왕복** — 편집기 초안이 `pipeline.schema.json`으로 직렬화되지 않는다.
- **REQ-1003 카탈로그 자동 구성** — 노드 목록이 코드 상수 배열이다.
- **REQ-1007 역방향 관측** — 구현이 없고, 어느 문서에도 미구현으로 적혀 있지 않다.

## 먼저 읽을 것

1. `reports/2026-08-25_1804_node-graph-doc-code-audit.md` — 무엇이 왜 어긋났는지.
2. `요구사항정의서.md` §F10 — **REQ-1001~1007**. 특히 REQ-1002의
   "편집기 고유 정보(노드 좌표 등)는 계약을 오염시키지 않는 별도 영역에 둔다"와
   REQ-1003의 "개발자가 편집기 코드를 고쳐 노드를 추가하는 일이 없어야 한다".
3. `contracts/pipeline.schema.json` — `node`가 `additionalProperties: false`이고
   kind별 `source` / `transform` / `sink` 설정이 필수라는 점.
4. `contracts/component-descriptor.schema.json` · `renderer.schema.json` ·
   `datasource.schema.json`과 `contracts/examples/` 아래 예시들 — 카탈로그의 입력이 될 것들.
5. `web-dashboard/README.md`의 **"지켜야 하는 규칙"** 절 — 이 저장소의 코드 규율과 서술 톤.

## 제약 — 반드시 지킬 것

1. **동작하는 것을 깨지 않는다.** `verify:pipeline` · `verify:mission-graph` ·
   `verify:cache` · `typecheck` · `build`가 작업 전후로 모두 통과해야 한다.
   기존 검증 스크립트의 **기대값을 낮춰 통과시키지 않는다.**
2. **임무 관제 그래프는 이번 범위가 아니다.** `MissionGraph`는 5단계에서만 건드린다.
   1~4단계에서 `plan` · `plan_progress` 경로를 수정하지 않는다.
3. **계약 스키마 파일을 고치지 않는다.** `contracts/*.schema.json`은 이번 작업의
   **기준이지 대상이 아니다.** 코드가 스키마를 못 맞추면 코드를 고친다.
   스키마를 고쳐야만 풀리는 지점을 발견하면 **고치지 말고 보고서에 남긴다.**
4. **새 그래프 형식을 만들지 않는다.** 초안의 표준형은 `pipeline.schema.json`이고,
   편집기 전용 정보는 그 **바깥**에 둔다.
5. **목 구현임을 감추지 않는다.** 카탈로그·실행기는 여전히 목이다.
   화면과 문서에 목이라는 사실이 드러나야 한다.
6. **문서를 먼저 정정하고 코드를 고친다.** 3단계는 1·2단계 없이도 오늘 할 수 있다.

## 단계

### 1단계 — REQ-1002 직렬화 계층

편집기 초안과 F4 계약 사이의 왕복을 만든다.

- 초안 자료구조를 **계약 본문 + 편집기 레이아웃** 둘로 분리한다.
  좌표·선택 상태 같은 편집기 고유 정보는 계약 객체 안에 들어가지 않는다.
  (예: `{ pipeline: <계약 그대로>, layout: { [nodeId]: {x, y} } }`)
- 노드에 kind별 설정을 채운다. `source`는 `dataSourceRef` · `queryMode` · `query`,
  `transform`은 `operator` · `params`, `sink`는 `rendererType` · `componentRef` · `fieldRef`.
  카탈로그 항목이 이 값의 기본값을 제공해야 한다.
- **왕복 검사를 회귀 스크립트로 만든다.** 초안 → 계약 JSON → 초안으로 되돌렸을 때
  같은 그래프가 나오고, 중간 계약 JSON이 `contracts/validate_examples.py`와 같은
  검증기를 통과해야 한다. 좌표가 계약 JSON에 섞이면 **실패로 잡아야 한다.**
- 목 게이트웨이의 검증기(`validateGraph`)도 계약형 그래프를 받도록 맞춘다.

### 2단계 — REQ-1003 카탈로그 파생

- `NODE_CATALOG` 상수 배열을 **없앤다.**
- 카탈로그는 등록된 디스크립터·렌더러·데이터소스에서 생성한다. 지금 단계의 등록처는
  `contracts/examples/` 아래 파일들로 충분하다. 게이트웨이가 이를 읽어
  `GET /pipelines/catalog`로 내려준다.
- **판정 기준**: 예시 파일을 하나 추가하면 **어떤 `.ts`도 고치지 않고** 카탈로그에
  새 노드가 나타나야 한다. 이걸 회귀 스크립트로 확인한다.
- 포트 타입(`timeseries` · `table` · `video` · `scalar`)도 디스크립터·렌더러의
  선언에서 끌어온다. 코드에 타입 표를 다시 적지 않는다.

### 3단계 — 문서 정정

코드 작업과 독립적으로 진행할 수 있다.

- `가시화_구현현황_260824.md` — `VZ-U-04` 행에 완료 근거가 REQ-1001·1004·1005·1006임을
  명시하고, REQ-1002·1003·1007을 잔여로 드러낸다. **27/33 숫자를 바꿀지 말지는
  판단해서 결정하고, 어느 쪽이든 근거를 한 줄 남긴다.**
- 보고서 `2026-08-25_1525_node-graph-editor.md` — "기존 스키마 구조를 사용했다"는 서술을
  실제 상태에 맞게 정정한다. **기존 보고서를 지우지 말고** 정정 문단을 덧붙인다
  (그 보고서가 이미 `1547`에 대해 하고 있는 방식과 같게).
- `작업계획_260824.md` §3-2 ⑥행과 §4 체크리스트 — "편집·검증·반영은 완료"를
  요건 단위로 쪼갠다.
- `백엔드_회신_260824.md`의 `data_transport_architecture_v2.docx` 인용을
  `260825_데이터_전송_및_아키텍처v4.docx`로 정정한다.
- `docs/verify_docs.py`의 D 검사가 `npm.cmd run` 표기도 잡도록 패턴을 넓힌다.

### 4단계 — REQ-1007 역방향 관측

- 반영된(`active`) 그래프의 각 노드에 **지금 흐르고 있는 값**을 표시한다.
  최소 요건은 노드별 최근 수신 시각·건수·마지막 값 요약이다.
- 시험 실행 결과(`rows` · `elapsed_ms`)와 **시각적으로 구분한다.** 하나는 초안의
  모의 결과이고 하나는 운영 그래프의 실제 관측이다. 섞이면 요건이 깨진 것이다.
- 데이터 경로는 기존 게이트웨이 왕복을 쓴다. 편집기가 스토어 내부를 직접 읽는
  우회 경로를 만들지 않는다.

### 5단계 — `robot-01` 고정 해제

- `NodeGraphView.tsx`의 `entities.get('robot-01')`과 `EVIDENCE_CATALOG`의
  `robot-01` · `camera-02` · `sensor-01` · `actuator-01` 하드코딩을 제거한다.
- 임무 대상과 근거 노드 후보는 **레지스트리에서** 온다. 장치가 늘면 화면이 따라가야 한다.
- `MissionView.tsx:37`의 `PLAN_ENTITY`도 같은 문제다. 함께 정리한다.

### 위생 (독립 작업)

- `.gitattributes`를 추가해 줄바꿈을 고정하고, **정규화만 담은 커밋 1개**를 따로 만든다.
  기능 변경과 섞지 않는다. 지금 167개 파일이 CRLF↔LF 전환만으로 수정 상태다.

## 완료 기준

```text
npm run typecheck             통과
npm run build                 통과
npm run verify:cache          통과
npm run verify:pipeline       통과
npm run verify:mission-graph  통과
npm run verify:contract-roundtrip   신규 · 초안↔계약 왕복 + 좌표 격리 + 스키마 검증
npm run verify:catalog-derived      신규 · 예시 파일 추가만으로 노드 증가
python docs/verify_docs.py    발견 0건
git status                    줄바꿈 전용 변경 없음
```

화면에서 확인할 것:

- 파이프라인 초안을 계약 JSON으로 내보내면 좌표가 없다.
- 그 JSON을 다시 불러오면 좌표까지 포함해 같은 화면이 나온다.
- `contracts/examples/renderer/`에 예시를 하나 넣으면 카탈로그에 노드가 하나 는다.
- 반영된 그래프의 노드에서 실제 수신 상태가 보이고, 시험 결과와 구분된다.

## 하지 말 것

- 계약 스키마를 코드에 맞춰 완화하기.
- 검증 스크립트의 기대값을 낮춰 통과시키기.
- 임무 관제 그래프의 동작을 1~4단계에서 바꾸기.
- REQ-1002~1003이 안 풀렸는데 문서만 완료로 되돌리기.
- 목 실행기를 실제 실행기인 것처럼 서술하기.

## 보고

작업이 끝나면 `reports/` 에 기존 형식으로 보고서를 남긴다.
직전 보고서 · 기준 · 범위 · 결과 · 검증 · 남은 경계를 포함하고,
**스키마를 고쳐야만 풀리는 지점을 발견했다면 고치지 말고 거기 적는다.**
