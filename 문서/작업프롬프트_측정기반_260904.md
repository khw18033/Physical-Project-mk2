# 작업 지시: 측정 기반 열기 — 기록 고리와 자체 관측

**선행**: 추가 개선 4건 완료 (`9baa3b7`) · 요구사항 v1.2
**대상 요구사항**: `VZ-D-02` · `VZ-D-04` · `VZ-D-08`(기록 면) · `VZ-O-04`
**기준 커밋**: 착수 시점의 `HEAD`
**날짜**: 2026-09-04

---

## 왜 하나

화면은 거의 다 됐다. **남은 것은 화면 뒤에서 숫자가 나오게 하는 일**이고, 그중 제일 작으면서
**논문 측정축 D**를 여는 둘이 이것이다.

지금 상태를 코드로 확인한 결과는 이렇다.

| 확인한 것 | 결과 |
|---|---|
| `TraceStore` | 35줄 · `append()`·`snapshot()`뿐. **`snapshot()` 을 부르는 곳이 0** |
| `receiveTrace()` | `missionTrace.append()` 하지만 **`state.current.events` 에는 안 넣는다.** 재생 머리(`headSec`)만 민다 |
| `foldStatuses(second, view)` | 접는 대상이 **`view.events`** — 승인 시점에 읽은 **대본 JSON** |
| `humanTrace` | 배열에 넣고 `console.log`. 되감기에 안 들어간다 |
| `observability.ts` | 11줄 · `updateClientHealth`/`getClientHealth` **양쪽 다 부르는 곳이 0** |

즉 **받은 기록이 아니라 대본을 접고 있고, 계측은 아예 돌지 않는다.**
목 데이터에서는 대본이 곧 정답이라 티가 안 나지만, 실제 백엔드가 붙으면 화면은 여전히
대본을 접는다. REQ-1404·1405가 말하는 「덧붙이기 전용 기록을 접어 시점 복원」 고리가
닫혀 있지 않다.

**착수 순서는 1 → 2.** 둘 다 작고, 2는 1이 만든 기록 열을 지표로 쓴다.
**단계마다 따로 커밋하고 보고서를 쓴다.**

---

## 1. 기록 → 되감기 고리를 닫는다

### 목표

**화면이 접는 것을 「대본」에서 「받은 기록」으로 옮긴다.**

```
지금   대본 JSON(view.events) ──접기──▶ 화면
       게이트웨이 trace_event ──▶ headSec 만
       사람 조작(humanTrace)  ──▶ console.log

뒤     게이트웨이 trace_event ─┐
       로컬 재생기            ─┼─▶ TraceStore ──접기──▶ 화면
       사람 조작              ─┘
```

### 할 것

1. **입구를 하나로 모은다.** 게이트웨이(`missionBridge` → `receiveTrace`)와 **단독 빌드의
   로컬 재생기**(`localTimer`)가 **같은 append 경로**를 쓴다. 입구가 둘이면 단독 빌드와
   통합 빌드의 되감기가 달라지고, 그게 곧 논문 측정축 D의 오염이다.
2. **`foldStatuses` 의 접는 대상을 기록 열로 바꾼다.** `view.events` 는 이제
   **대본의 정의**이지 화면의 원천이 아니다 — 목 게이트웨이와 로컬 재생기가 그것을 읽어
   기록으로 흘려보내고, 화면은 흘러온 것만 접는다.
3. **`humanTrace` 를 같은 열에 합친다.** `recordHuman()` 이 만드는 사건도 되감기에 보여야
   한다 — `VZ-D-08` 의 「모든 조작은 `produced_by=human` 으로 기록된다」가 화면에서 확인
   가능해지는 지점이다. 지금은 `console.log` 가 끝이라 **기록이라고 부를 수 없다.**
4. **`TraceStore` 를 실제 저장소로 만든다.** 지금은 배열 래퍼다. 최소한 이것들이 필요하다.
   - `seq` 순서 보장 (수신 순서 ≠ seq 순서일 수 있다)
   - 같은 `seq` 재수신 시 **중복 무시** (재접속하면 다시 온다 — `VZ-I-02`)
   - `snapshot()` 은 지금처럼 복사본. **수정·삭제 경로를 만들지 마라** (REQ-1404)

### 지켜야 하는 것

- **정지 미리보기(t=0)에서는 기록 열이 비어 있어야 한다.** 승인 전에는 아무것도 재생되지
  않는다는 규칙(`VZ-U-07`)이 여기서도 그대로다.
- **`scenarios/*.json` 을 고치지 마라.** 특히 `MSN-260826-01`(HCI 전달본)은 무수정이고
  `verify:scenario` 가 그것을 검사한다.
- **접기 성능.** 20 Hz 수신에서 매 사건마다 전체를 접으면 렌더 예산을 넘긴다.
  기존 100 ms 병합 규칙을 유지하고, 필요하면 접기를 증분으로 만들어라 —
  다만 **증분과 전체 접기의 결과가 같아야 한다**(아래 `verify:replay` 가 그것을 본다).
- 되감기 중 명령 차단(REQ-1405)이 이미 있으면 그대로 유지한다.

### 검사 셋 신설

| 검사 | 무엇을 |
|---|---|
| `verify:trace-append` | 기록이 **덧붙이기만** 되는가. 수정·삭제 경로가 없는가. 같은 `seq` 재수신이 중복을 만들지 않는가 |
| `verify:replay` | **임의 시점 복원 결과가 순차 재생 결과와 일치하는가.** 증분 접기를 넣었다면 전체 접기와도 일치해야 한다 |
| `verify:human-trace` | 화면에서 나가는 **모든 명령**이 `produced_by=human` 으로 기록되고 **되감기에 보이는가**. 대조군 포함 — 기록을 무력화한 사본이 반드시 실패로 잡히는지까지 |

기술스택 §7이 「2·3단계 예정」으로 적어 둔 여섯 중 셋이 여기서 닫힌다.

---

## 2. 자체 관측을 실제로 돌린다 (`VZ-O-04`)

### 지금

`src/shared/observability.ts` 11줄. `ClientHealth` 세 필드(`connected`·`subscriptions`·
`lastError`)를 두고 갱신·조회 함수가 있는데 **양쪽 다 부르는 곳이 없다.**
계측이 없으니 **논문 측정축 D를 잴 수 없다.**

### 할 것

**측정은 우리 것, 발행은 남의 것**으로 나눈다.

| | 지금 한다 | 나중에 |
|---|---|---|
| 값을 잰다 | **○ 이번에** | |
| 화면에 보인다 (devpanel) | **○ 이번에** | |
| JSON 으로 내보낸다 | **○ 이번에** — 논문 측정에 바로 쓴다 | |
| 관측 스택으로 발행 | ✗ **상대가 없다** — 「연결 예정」 자리표시 | 관측 스택이 생기면 |

`VZ-O-04` 의 「60초 1회 집계 발행」은 발행 상대가 생겼을 때의 주기다. 지금은 **같은 주기로
집계해서 들고 있기**까지 한다.

### 무엇을 재나 — 논문 §3-4가 요구하는 것

| 지표 | 왜 |
|---|---|
| **기록 발생률** (건/초) | 축 D 본문. 1단계가 만든 기록 열에서 바로 나온다 |
| **기록 열 길이 · 추정 용량** (건 · KB) | 축 D 「로그 용량」 |
| **접기 시간** (ms) | 축 D 「임의 시점 복원 시간」 — 기술 문서 §5-2의 **16 ms 목표** |
| 수신 지연 (ms) | 추가 지연 |
| 재연결 횟수 | 안정성 |
| 브라우저 메모리 | `performance.memory` 가 있을 때만. **없으면 지어내지 마라** |

### 지켜야 하는 것

- **`shared/` 에 둔다.** 축 D는 **단독 빌드**에서 재야 하는데 `tabs/` 에 의존하면
  대시보드 데이터 계층이 딸려 들어와 부하가 섞인다. `verify:standalone` 이 관문이다.
- **계측 자체가 부하다.** 60초 집계이고 매 사건마다 계산하지 않는다.
  계측이 측정 대상을 흔들면 축 D의 숫자가 무의미해진다.
- **재는 값과 못 재는 값을 화면에서 가른다.** 메모리처럼 브라우저가 안 주는 것은
  「해당 없음」이고, 관측 스택 발행은 「연결 예정」이다. **빈칸에 0을 넣지 마라.**

---

## 검증

| 검사 | 무엇을 |
|---|---|
| **`verify:trace-append`** (신설) | 덧붙이기 전용 · 중복 무시 |
| **`verify:replay`** (신설) | 임의 시점 = 순차 재생 |
| **`verify:human-trace`** (신설) | 사람 조작이 기록되고 되감기에 보이는가 · 대조군 포함 |
| `verify:standalone` (**무개정 통과**) | 관측 계층이 `tabs/` 를 끌어오면 실패 |
| `verify:scenario` (유지) | `MSN-260826-01` 무수정 |
| `verify:canvas-persist`·`layout`·`one-gateway`·`no-tabs` 등 기존 18종 | 전부 통과 |

`verify:standalone` 은 Windows 에서 실행한다.

---

## 건드리지 않는 것

- `scenarios/*.json` 과 사이드카 — 특히 `MSN-260826-01`
- `web-dashboard`(기준선) · `stt-lab` · `stt/engines/*.py`
- `DataStore`(`tabs/data/store.ts`) — **기록 열과 최신값 저장소는 계속 둘이다.**
  합치면 둘 중 하나가 손해를 본다(기술스택 §10)
- 뷰 노드·캔버스 구조 — 이번 범위가 아니다
- `5tab-baseline` 태그

---

## 이 다음에 오는 것 (참고 · 이번 범위 아님)

| # | 무엇 | 논문 축 |
|---|---|---|
| 3 | **결함 주입** (REQ-1409) — 지금 코드가 0이다 | A |
| 4 | **격리 집합 계산** (`VZ-D-05`) — 지금은 렌더 때 흐리게만 | A |
| 5 | 생성 `VZ-G-02`~`05` (발화를 안 거치므로 §7.8 계약과 무관) | — |
| 6 | 생성 `VZ-G-01` (§7.8 계약 대기) | — |

**3·4는 짝이다** — 결함을 심어야 격리가 잡을 게 생기고, 격리가 있어야 심은 결함이
의미가 있다. 따로 하면 반쪽이다.

---

## 보고서

단계마다 하나 — `reports/2026-09-04_측정기반_N단계.md`. 최소한 아래를 적어라.

- 1단계: 입구를 어떻게 하나로 모았는지(게이트웨이·로컬 재생기·사람 조작),
  접기 성능 **실측**(사건 수 × 접기 시간), 증분 접기를 넣었다면 전체 접기와 일치하는 근거
- 2단계: 실제로 잰 값 예시와 **16 ms 목표 대비 접기 시간**, 못 재서 「해당 없음」으로 둔 것
- 지시서와 다르게 한 곳과 그 이유
