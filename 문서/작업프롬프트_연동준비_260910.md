# 작업 지시: 하드웨어·탐지 연동 준비 (2단계)

**날짜**: 2026-09-10 (하드웨어 회신 반영)
**선행**: 1단계 UI 수정 완료 후 착수
**범위**: 바깥과 말을 트는 것까지. 실제 로봇 합동 시험은 다음 단계

> **`구현계획_시연확정판_260909.md` 2절의 C·S 채널 JSON 안을 이 지시서가 대체한다.**
> 하드웨어 쪽이 MQTT + protobuf로 이미 서 있고, 그쪽이 사실이다.

---

## 0. 착수 전에 확인할 것

| 확인 | 무엇을 |
|---|---|
| `schema/physical_command.proto` | **`HW` 브랜치에만 있다.** 우리 쪽으로 가져오는 방법을 먼저 정한다 (2절) |
| `src/stt/SttClient.ts` · `src/generate/LlmClient.ts` | 경계 파일의 모양. **이번 것도 같은 패턴이다** |
| `CONNECTION_TARGETS` · `registerConnectionDefault` | 주소를 등록하는 자리 |
| `shared/registry.ts` | 장비 id가 무엇으로 적혀 있는가 — 하드웨어는 **`go1-001`**을 쓴다 |
| `verify:standalone` 현재 수치 | protobufjs·mqtt.js를 넣으면 번들이 는다. **착수 전 KB를 적어 두어라** |
| 1단계 결과물 | 뷰포인트 노드의 상태 넷과 그것을 갱신하는 함수 |

---

## 1. 하드웨어 채널 — 확정된 사실

하드웨어 담당이 실제로 쏴 보고 바이트까지 확인한 뒤, 우리 질문에 회신한 내용이다.
**추측이 아니라 관측과 합의다.**

| | |
|---|---|
| 브로커 | `pi7.local:1883` (TCP) · **`ws://pi7.local:9001`** (WebSocket) |
| 프로토콜 | MQTT 5, 인증 없음 |
| 발행 토픽 | `terminal/go1-001/downlink`, QoS 1 |
| **수신 토픽** | **`terminal/go1-001/uplink`, QoS 1** |
| 페이로드 | **protobuf 바이너리.** JSON 문자열이 아니다 |

**브라우저는 WebSocket 쪽을 쓴다.** 9001 포트다.
응답 세 종(`Acceptance` · `CommandStatus` · `CommandResult`)이 **모두 uplink 하나로** 온다.
종류는 봉투의 `oneof body`가 구분한다.

### 봉투 구조

```
Envelope { field 1 = Command { 1: command_id, 2: target, 3: action, 4: parameters } }
```

`parameters`는 `map<string, double>`이다. 문자열 파라미터를 넣을 자리가 없다.

### action 셋

| action | parameters | 비고 |
|---|---|---|
| `ping` | 없음 | 31바이트. 연결 확인용 |
| `scan_mission` | `steps=8`, `forward_m` | **`forward_m`은 스캔 뒤 전진까지 포함.** `0`이면 스캔만 |
| `move_forward` | `distance_m` (0.05~10), `vx` (생략 가능, 기본 0.15) | 전진만. 결과 `{distance_m, odo_m, duration_s}` |

### 웹에서 만드는 법

```js
const payload = Envelope.encode(Envelope.create({ command: {
  commandId : 'cmd-' + Date.now(),
  target    : 'go1-001',
  action    : 'scan_mission',
  parameters: { steps: 8, forward_m: 0 }
}})).finish()

client.publish('terminal/go1-001/downlink', payload, { qos: 1 })
```

---

## 2. 경계 파일 — STT·생성과 같은 패턴

`src/physical/PhysicalClient.ts` 하나가 **하드웨어를 보는 유일한 면**이다.
이 파일 밖에서 MQTT 클라이언트를 만들거나 토픽 문자열을 적지 마라.

| | STT | 생성 | **하드웨어** |
|---|---|---|---|
| 경계 파일 | `src/stt/SttClient.ts` | `src/generate/LlmClient.ts` | **`src/physical/PhysicalClient.ts`** |
| 연결 대상 | `stt` | `generate` | **`physical`** — 기본값 `ws://pi7.local:9001` |
| 꺼져 있으면 | 음성만 꺼짐 | 생성만 꺼짐 | **로봇 명령만 꺼짐. 대본 재생은 그대로 돈다** |
| 검사 | `verify:no-stt` | `verify:no-llm` | **`verify:no-physical`** (신설) |

### 주소 프리셋 셋

이름이 안 풀릴 때를 대비해 화면에서 고를 수 있게 한다.

| 프리셋 | 값 |
|---|---|
| 이름 | `ws://pi7.local:9001` |
| 랩 Wi-Fi | `ws://192.168.50.172:9001` — **고정 IP** |
| 직접 입력 | 사용자가 적는다 |

핫스팟은 DHCP라 고정이 없다. 발표장에서 핫스팟을 쓰게 되면 직접 입력으로 간다
(11절에 후속 조치를 적어 두었다).

### protobuf를 어떻게 넣을 것인가

하드웨어 쪽은 "브라우저면 protobufjs가 `.proto`를 런타임에 읽으므로 컴파일 불필요"라고 했다.
**우리 저장소에서는 정적 코드 생성 쪽이 낫다.** 이유 둘이고 둘 다 이 저장소의 기존 제약이다.

1. `verify:standalone`이 단독 빌드 오염을 KB 단위로 감시한다. 런타임 파싱은 protobufjs 전체가
   들어가지만 정적 생성은 `protobufjs/minimal`이면 된다
2. 런타임 파싱은 `.proto`를 정적 자산으로 서빙해야 한다. `dist/`를 그대로 뿌리는 배포 형태와
   맞물려 경로 문제가 생긴다

그러니 `pbjs`로 정적 모듈을 뽑아 커밋한다. **다만 먼저 숫자를 재라** — 착수 전 KB와 비교해
런타임 파싱이 한도 안에 들어온다면 하드웨어 쪽 방식을 따라도 된다. 보고서에 근거를 남긴다.

### `.proto`를 가져오는 방법

원본은 `HW` 브랜치의 `schema/physical_command.proto` 하나다.
우리 쪽 복사본은 **낡을 수 있다.** 복사본 머리에 출처와 가져온 날짜를 주석으로 적고,
재생성 스크립트에도 같은 URL을 남겨라.

```
https://github.com/khw18033/Physical-Project-mk2/blob/HW/schema/physical_command.proto
```

**스키마를 우리가 고치지 마라.** 고쳐야 할 일이 생기면 하드웨어 쪽에 요청한다.

---

## 3. 명령 매핑 — 어느 태스크가 무엇을 쏘는가

| 태스크 | action | parameters |
|---|---|---|
| `T-A3` 로봇이 1바퀴 돈다 | `scan_mission` | `steps=8`, **`forward_m=0`** |
| `T-B2` 전방으로 직진 | `move_forward` | `distance_m=n`, `vx` 생략 |
| 연결 확인 (화면 버튼) | `ping` | 없음 |

### 왜 `scan_mission`에 전진을 태우지 않는가

`scan_mission`은 `forward_m`으로 스캔과 전진을 한 번에 할 수 있다. 그런데 **순서도가 스캔과
접근을 다른 마일스톤으로 갈라 놓았다.** 명령 하나가 두 마일스톤에 걸치면 어느 노드를 언제
완료로 바꿀지 애매해진다.

`forward_m=0`으로 스캔만 시키고, 접근은 `move_forward`로 따로 쏜다.
**태스크 경계와 명령 경계를 맞추는 것**이 이 결정의 이유다.

부수 효과도 있다 — 스캔이 끝난 뒤 사람이 화면을 보고 접근을 이어 갈 수 있다.
발표에서 한 박자 쉬어 갈 자리가 생긴다.

### 연결 확인 버튼

`ping`을 쏘고 왕복이 도는지 보는 버튼을 화면 한구석에 둔다.
**발표 직전에 이걸 눌러서 초록이 뜨는지 보고 무대에 오른다.** 시연 중에는 안 쓴다.

---

## 4. 응답 매핑

| 응답 | 뜻 | 화면 |
|---|---|---|
| `Acceptance{accepted:true}` | 받았다 | 태스크 노드 실행 중 |
| `Acceptance{accepted:false}` | **거절** | 태스크 노드 실패. `rejection.code`와 `message`를 그대로 노출 |
| `CommandStatus` | 진행 보고 | **5절 — 이게 뷰포인트 노드를 채운다** |
| `CommandResult{SUCCEEDED}` | 끝 | 태스크 노드 완료 |
| `CommandResult{그 외}` | 실패 | 태스크 노드 실패 + 사유 |

### 거절을 숨기지 마라

`robot_state_dead`가 실제로 나온 응답이다. 로봇을 안 켜면 시연 당일에도 이게 뜬다.
**화면에 코드와 문구를 그대로 띄워라.** 조용히 실패하면 무대에서 원인을 못 찾는다.
논문의 「실패 경로」 주장과도 같은 방향이다.

---

## 5. `CommandStatus.detail` — 뷰포인트를 채우는 열쇠

**미결이었던 자리가 풀렸다.** `detail`이 JSON 문자열로 온다.

```json
{"ack":3,"of":10,"event":"scan_turn","step":3,"steps":8,"yaw_deg":131.0,"note":"ok"}
```

| 필드 | 내용 |
|---|---|
| `ack` / `of` | 이번 임무의 ACK 순번 / 총 ACK 수 |
| `event` | `scan_turn` · `door_turn` · `forward` · `aborted` |
| `step` / `steps` | 그 단계에서 몇 번째인가 — 회전 3/8의 3, 8 |
| `yaw_deg` | 그 시점 방위(도). **모를 수 있다 — `null` 처리** |
| `note` | `ok` · `turn_timeout` · `robot_state_lost` · `forward_timeout` |

### 지켜야 하는 것 넷

**㉠ `step`은 1부터 시작한다. 우리 노드 인덱스는 0부터다.**
`index = step - 1`. 이 한 줄을 틀리면 노드가 한 칸씩 밀린다. 이 작업에서 가장
흔하게 날 실수이고, 8절 검사가 이것부터 본다.

**㉡ `event`로 갈라라.** `scan_turn`만 뷰포인트 노드를 건드린다.
`forward`는 `T-B2`, `aborted`는 임무 중단이다. `door_turn`은 11절 확인 전까지 화면에
표시만 하고 노드 상태를 바꾸지 않는다.

**㉢ `note != "ok"`이면 그 노드에 경고를 남겨라.** `turn_timeout`은 그 각도를 제대로
못 봤다는 뜻이다. 조용히 정상으로 칠하면 안 된다.

**㉣ `yaw_deg`로 노드를 고르지 마라.** `step`이 기준이다. `yaw_deg`는 화면 표시용이고
`null`일 수 있다.

### 인덱스를 올리는 자리는 함수 하나

`detail`을 파싱해 인덱스를 내놓는 함수를 하나만 둔다. 노드 갱신 코드는 그 인덱스가
로봇에서 왔는지 대본에서 왔는지 몰라야 한다. 어제 지시서 6절과 같은 원칙이다.

### ACK 개수 읽기 — 확인 필요

예시의 `of:10`은 `steps=8`에 `door_turn` 하나, `forward` 하나를 더한 수로 읽힌다.
`forward_m=0`이면 `steps+1`이라 했으니 우리 경우 `of:9`가 될 것이다.
**이 읽기가 맞는지 11절에서 확인한다.** 진행률 표시를 `ack/of`로 만들 거면 특히.

---

## 6. 객체 탐지 — HTTP, 전역 뷰 노드 셋

마일스톤 A에 **전역 뷰 노드 셋**을 붙인다. 개별 태스크가 아니라 마일스톤에 매달린다 —
스캔이 도는 동안 계속 떠 있어야 하는 화면이기 때문이다.

| 뷰 노드 | 내용 |
|---|---|
| `VIEW-CAM` | 객체 탐지 영상 — 현재 프레임에 bbox를 입힌 것 |
| `VIEW-MAP` | 2D 맵 — **다른 담당의 산출물을 받아 띄우기만 한다** |
| `VIEW-REASON` | 탐지 근거 텍스트 — 현재 각도의 판단 문장 |

`VIEW-REASON`은 `T-A6` 근거 노드와 내용이 겹친다. **역할이 다르다** —
뷰 노드는 스캔 중 실시간으로 흐르고, `T-A6`는 최종 선정된 방향의 근거를 남긴다.
같은 컴포넌트를 쓰되 데이터 출처를 갈라라.

### 엔드포인트 — 아직 합의 전이다

HTTP로 가기로만 정해졌고 형식은 안 나왔다. **아래는 우리가 필요한 최소치이고, 이대로
목 서버를 만들어 작업한다.** 확정되면 갈아끼운다.

| | |
|---|---|
| `GET /detect/results` | 여덟 전부. `[{index, angle_deg, door, bbox, confidence, reason}, ...]` |
| `GET /detect/result?index=n` | 하나만 |
| `GET /detect/frame?index=n` | 해당 각도의 프레임 이미지 (bbox 입힌 것) |
| `GET /detect/current` | 지금 보고 있는 프레임 — `VIEW-CAM`용 |

**`index`는 0부터다.** 하드웨어의 `step`(1부터)과 다르다는 걸 탐지 쪽에도 알려야 한다.

**폴링으로 간다.** 스캔이 도는 동안 500ms 간격, 그 외에는 멈춘다.
WebSocket이나 SSE를 새로 깔지 마라 — 붙을 시간이 없다.

주소는 `physical`과 같은 방식으로 연결 관리에 올린다. 대상 이름은 `detect`.
탐지 서비스가 없으면 **뷰 노드 셋만 꺼지고 나머지는 그대로 돈다.**

### 이미지가 무거우면

프레임을 매번 받으면 느려진다. `VIEW-CAM`은 현재 프레임 하나만 갱신하고,
각도별 프레임은 **그 노드를 열었을 때만** 받아라.

---

## 7. 목과 폴백

이번 단계의 산출물은 **실제 연결이 아니라 연결할 수 있는 상태**다.
로봇도 탐지 서비스도 없이 화면이 끝까지 돌아야 한다.

| 상황 | 동작 |
|---|---|
| 브로커 없음 | 로봇 명령 경로만 꺼짐. 대본 재생으로 전 과정 재생 |
| 탐지 서비스 없음 | 뷰 노드 셋만 꺼짐. 대본의 고정 결과로 노드 채움 |
| 둘 다 있음 | 실제 값 |
| 브로커는 있는데 로봇이 꺼짐 | `robot_state_dead` 거절을 화면에 표시 |

목 탐지 서버를 `mock/` 아래에 하나 둔다. 위 네 엔드포인트를 흉내 내고
index 2만 `door: true`. 대본의 타임라인과 같은 속도로 결과를 내놓는다.

목 uplink도 하나 둔다. `CommandStatus`를 1초+1초 간격으로 `step` 1~8까지 흘리고
`note`를 한 번은 `turn_timeout`으로 내보내 경고 표시가 실제로 뜨는지 본다.

---

## 8. 검증

| 검사 | 무엇을 |
|---|---|
| **`verify:status-index`** (신설) | **`step`(1~8)이 노드 인덱스(0~7)로 옳게 옮는가** · `event`가 `scan_turn`이 아닌 것은 뷰포인트를 안 건드리는가 · `note != ok`가 경고로 남는가 · `yaw_deg: null`에서 깨지지 않는가 |
| **`verify:command-encode`** (신설) | `scan_mission{steps:8, forward_m:1.0}`을 인코딩하면 **자료의 79바이트 hex와 같은가** · `ping`이 31바이트인가 · `move_forward`가 스키마를 통과하는가 |
| **`verify:physical-port`** (신설) | MQTT 주소·토픽을 아는 면이 `src/physical/` 하나인가 |
| **`verify:no-physical`** (신설) | 브로커가 없어도 화면이 뜨고 **대본 재생·되감기·캔버스가 도는가** |
| **`verify:no-detect`** (신설) | 탐지 서비스가 없어도 뷰 노드 셋만 꺼지는가 |
| `verify:standalone` | **증가분을 숫자로 남긴다.** 한도를 넘으면 보고서에 적고 판단을 물어라 |
| `verify:viewpoint-fill` · `viewpoint-layout` | 1단계 결과 그대로 통과 |
| 기존 전종 | 통과 |

`verify:status-index`와 `verify:command-encode` 둘이 이 작업의 핵심 검사다.
앞엣것은 **한 칸 밀림**을 잡고, 뒤엣것은 **하드웨어가 알아듣는지**를 로봇 없이 증명한다.

---

## 9. 하지 않을 것

- 중앙 서버 경유 경로 — 시연은 온디바이스 직통으로 확정됐다
- 2D 맵 자체 구현 — 받아서 띄우기만 한다
- `.proto` 스키마 수정 — `HW` 브랜치 것이다
- `scan_mission`에 `forward_m`을 태워 두 마일스톤을 한 명령으로 묶는 것
- `door_turn`으로 노드 상태를 바꾸는 것 — 11절 확인 전까지
- WebSocket·SSE 탐지 채널 신설 — HTTP 폴링으로 간다
- 뷰포인트 노드의 배치·색 — 1단계에서 끝났다

---

## 10. 보고서

`reports/2026-09-10_연동준비.md`

- 0절 확인 결과 — `.proto`를 가져온 방법, `registry.ts`의 장비 id 대 `go1-001` 불일치
- `verify:command-encode` 결과 — 자료의 hex와 실제로 같았는가, 다르면 어디가
- **번들 증가분** — 착수 전 KB → 착수 후 KB, 한도 대비. 정적 생성과 런타임 파싱 중 무엇을 골랐고 왜인가
- `step → index` 변환을 어디서 하는가
- 목 uplink와 목 탐지 서버의 동작 범위
- 지시서와 다르게 한 곳과 그 이유

---

## 11. 남은 확인

하드웨어 회신으로 다섯 중 넷이 풀렸다. 남은 것과 새로 생긴 것.

| 상대 | 질문 |
|---|---|
| 하드웨어 | **`door_turn`은 로봇이 스스로 문 방향을 정해 도는 것인가.** 그렇다면 우리 탐지 AI가 고른 각도와 어긋날 수 있다. 우리가 방향을 지정할 방법이 있는가 |
| 하드웨어 | `forward_m=0`일 때 `of`가 9가 맞는가 (5절 읽기 확인) |
| 하드웨어 | 발표장 핫스팟 대역을 정해 주면 **정적 IP를 박아 준다** 했다. 대역을 정해 회신할 것 |
| 탐지 | HTTP 엔드포인트와 응답 형식. 6절 안으로 괜찮은가 |
| 탐지 | 프레임에 **bbox를 입혀서** 주는가, 아니면 원본과 좌표를 따로 주는가 |
| 탐지 | `reason` 문장을 만들어 주는가 |
| 탐지 | **`index`는 0부터**로 맞출 수 있는가 |

첫 줄이 이번에 새로 생긴 가장 무거운 질문이다. 로봇이 자기 판단으로 문 방향을 정해
돌아 버리면, 화면이 보여 준 「90도 선정」과 로봇의 실제 방향이 다를 수 있다.
**시연에서 그 어긋남이 보이면 설명이 곤란해진다.**
