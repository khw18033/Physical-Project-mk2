/**
 * gateway/i18n.en.ts (260919 신설 — 영문화 5단계 §1)
 *
 * **게이트웨이가 하는 말, 영어.**
 *
 * `GW_KO` 에 있는 키가 여기 없어도 된다 — 없으면 한국어로 떨어진다(`gwT`).
 * 부분 번역 상태에서도 화면이 정상이어야 한다.
 */
export const GW_EN: Record<string, string> = {
};
