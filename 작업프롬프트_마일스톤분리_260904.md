# 작업 지시: 마일스톤 분리와 태스크 생성 — 발화에서 임무 객체까지

**선행**: 측정기반 2단계 완료 (`482001b`) · 요구사항 v1.2
**대상 요구사항**: `VZ-G-01`(마일스톤 분리) · `VZ-G-02`(태스크 DAG 생성) · `VZ-G-03`(검증) 일부
**기준 커밋**: `482001b`
**날짜**: 2026-09-04

---

## 왜 하나

기록 고리가 닫혔고 계측이 돈다. **화면에 닿지 않는 것은 이제 생성 계열뿐이다.**
지금까지 임무 객체는 손으로 쓴 대본 JSON이 전부였고, 발화에서 임무를 만드는 경로는 비어 있다.

착수 전에 코드로 확인한 것은 이렇다.

| 확인한 것 | 결과 |
|---|---|
| `src/generate/` | **없다.** 기술스택 §에 「미착수」로만 적혀 있다 |
| `src/stt/SttClient.ts` | 살아 있다. 발화 텍스트를 얻는 경로는 이미 있다 |
| `contracts/mission.schema.json` | `utterance` + `milestones[]` 로 완성돼 있다 |
| `contracts/task.schema.json` | `deps` · `derived_from` · `evaluation` 있음. **`nodeKind` 가 없다** — 대본 JSON에만 있고 계약에는 안 올라갔다 |
| `scenarios/*.json` 4편 | 발화 · 마일스톤 · 태스크(deps · nodeKind · target)가 **한 파일에 다 있다** |

**마지막 줄이 이 작업의 출발점이다.** 정답셋을 새로 만들 필요가 없다.

---

## 배치 결정 — 브라우저 안이 아니다

STT 와 **같은 패턴**이다. 모델은 브라우저에 들어가지 않는다.

| | STT | 생성 |
|---|---|---|
| 프로세스 | `stt-lab` FastAPI · 8801 | **`gen-lab` FastAPI · 8802** |
| 경계 파일 | `src/stt/SttClient.ts` | **`src/generate/LlmClient.ts`** |
| 주소 | 연결 관리(`VZ-C-07`) + 환경변수 기본값 | 동일 — `CONNECTION_TARGETS` 에 `generate` 추가 |
| 꺼져 있으면 | 음성만 꺼짐 (`verify:no-stt`) | **생성만 꺼짐 (`verify:no-llm` 신설)** |

**브라우저 안(WebGPU)으로 가지 마라.** 이유 셋이고 셋 다 이 저장소의 기존 제약과 충돌한다.

1. `verify:standalone` 이 단독 빌드 오염을 **KB 단위**로 감시한다(탭 이식 때 229.99 → 235.63 kB).
   모델을 번들에 넣으면 이 검사가 의미를 잃는다. 사이드카면 `LlmClient` 는 fetch 래퍼라 몇 KB다.
2. 문법 강제 디코딩이 브라우저 스택에서 안정적이지 않다. **그것이 이 작업의 핵심 도구다**(3단계).
3. 배포에서 모델을 빼는 선택지가 사라진다 — 아래 배치 ①이 불가능해진다.

### 배치 셋 — 다른 기기에서 어떻게 도는가

| 배치 | 모델 위치 | 다른 기기에 필요한 것 | 쓰는 자리 |
|---|---|---|---|
| ① 생성 꺼짐 | 없음 | **`dist/` 만** | 심사·평가 배포, 대본 재생 |
| ② 한 대가 서버 | GPU PC 1대 | 연결 관리에서 주소만 | 발표·시연 |
| ③ 기기마다 사이드카 | 기기마다 | 설치 + GPU | 개발자 각자 |

**①이 기본형이다.** 정적 빌드(`base: './'`)를 뿌리고 생성 서비스가 없으면 생성만 꺼진다.
②는 연결 관리가 런타임 주소 변경과 `localStorage` 영속을 이미 지원하므로 추가 구현이 없다.

`dist/` 를 `file://` 로 열면 origin 이 `null` 이라 fetch 가 막힌다. **`gen-lab` 이 `dist/` 를
정적 서빙**하면 same-origin 이 되어 CORS 가 사라진다 — `stt-lab` 이 이미 쓰는 방식이다.

---

## 0. 선행 결정 둘

### 0-1. `utterance.confidence` 계약 (§7.8 · `VZ-G-01` 의 선행 조건)

문서 세 곳이 이것을 `VZ-G-01` 착수 조건으로 적어 두었다. 세 안은 STT 보고서에 있다.
**`VZ-G-02` 는 발화를 거치지 않으므로 이 결정에 막히지 않는다** — 0-1이 늦어지면 5단계를 먼저 해도 된다.

### 0-2. 문은 노드다

대본 1편의 `MS-B` 가 「문 열림 확인」이다. **문이 마일스톤을 만들어내는 대상**이므로
장소 그래프에서 문을 엣지 속성이 아니라 **노드**로 둔다. 엣지 속성으로 두면 `VZ-G-01` 이
이 마일스톤을 낼 근거가 사라진다.

---

## 1. 장소 계약과 맵 추출 (`contracts/place.schema.json`)

### 층을 나눈다 — 이것이 이 단계의 요점

Unity 맵에서 뽑는 것을 **셋으로 가른다.** 한 파일에 섞으면 생성기가 좌표를 보게 되고,
그 순간 모델이 503호 전용이 된다.

| 층 | 내용 | 파일 | 쓰는 곳 |
|---|---|---|---|
| **위상** | 장소 · 별칭 · 종류 · 층 · 인접 | `places.json` | **G-01 · G-02 그라운딩**, G-03 검증 |
| 기하 | 좌표 · 거리 · 정지 여유 | `places.geometry.json` | G-04 전개 · 시연 · 임무 `params` |
| 렌더 | 메시 · 머티리얼 | 뽑지 않는다 | Unity 만 |

**생성 서비스는 `places.json` 만 받는다.** 기하 파일을 읽는 경로를 만들지 마라.
이 분리가 논문 4-6 「범용성 장치」의 다섯째가 된다 — *계층마다 필요한 데이터가 다르고, 그것이
파일로 갈라져 있다.*

### `place.schema.json` 최소 항목

```
place_id    room-503 · corridor-5f · elevator-hall-5f · door-503
label       503호
aliases     [503, 503호실]          ← 발화 정규화용. STT 오인식 표기도 넣는다
kind        room | corridor | door | elevator | stair | zone
floor       5
adjacent    [place_id]              ← 무방향. 양쪽에 다 적고 정합성은 검사가 본다
zone_id     zone-503                ← shared/registry.ts 의 CURRENT_ZONE_ID 와 맞춘다
```

### 지켜야 하는 것

- **`shared/registry.ts` 와 같은 규칙이다.** 장소 목록을 아는 면은 하나여야 한다.
  화면·생성 서비스·검증기가 각자 장소 이름을 손으로 적으면 「두 곳이 갈라진다」가 그대로 난다.
- **대본에 나오는 장소가 전부 들어 있어야 한다.** 4편의 발화·마일스톤 제목에 등장하는
  장소를 뽑아 대조하는 것이 아래 `verify:places` 다.
- **없는 층·구역을 지어내지 마라.** 실측한 것은 503호와 5층 복도다. 415호는 옛 편
  (`MSN-260826-01`)의 발화에 나오므로 **자리는 두되 인접이 비어 있어도 된다** —
  「연결 예정」과 같은 원칙이다.

---

## 2. 정답셋과 채점기 (맵과 무관 · 병렬로 간다)

### 정답셋은 이미 저장소에 있다

| 편 | 발화 | 마일스톤 | 태스크 | 문법 라벨 | 합류 | refEdge |
|---|---|---|---|---|---|---|
| `MSN-260826-01` | 415호에서 503호로 이동해줘 | 7 | 7 | 없음 | 1 | 0 |
| `MSN-260831-01` | 503호에서 복도에 있는 엘리베이터 앞까지 이동해줘 | 7 | 17 | 5종 | 1 | 0 |
| `MSN-260831-02` | 503호 내에서 고정 카메라의 사각지대를 탐지해줘 | 6 | 18 | 5종 | 0 | 1 |
| `MSN-260831-03` | 월류방어벽 자동 개폐 시스템을 가동해 | 4 | 16 | 4종 | 0 | 0 |

이동 · 탐지 · 제어로 도메인이 갈라져 있다. **범용성 주장에 그대로 쓴다.**

### 할 것

1. **추출기** `scripts/extract-goldset.mjs` — 대본 4편에서 `(utterance.text → milestones[] → tasks[])`
   쌍을 뽑아 `gen-lab/goldset/` 에 쓴다. **대본을 고치지 않는다. 읽기만 한다.**
2. **발화 변형을 더한다.** 같은 임무에 대한 다른 말투를 편당 3~5개씩 손으로 적어
   `goldset/utterances.json` 에 둔다 — 「503호에서 엘리베이터까지 가줘」·「5층 복도 엘리베이터 앞으로」.
   **마일스톤 정답은 원본과 같다.** 이것이 G-01 의 표현 강건성을 재는 유일한 축이다.
3. **채점기** `scripts/score-generation.mjs` — 네 축을 따로 낸다. 합산 점수 하나로 뭉치지 마라.

| 축 | 무엇을 | G-01 | G-02 |
|---|---|---|---|
| 스키마 | `mission.schema.json` 을 통과하는가 | ○ | ○ |
| 마일스톤 | 개수 · 순서 · 장소 어휘가 정답과 맞는가 | ○ | — |
| 노드 문법 | `nodeKind` 라벨이 정답과 맞는가 | — | ○ |
| 그래프 | `deps` 가 만드는 DAG 가 정답과 동형인가 · 순환이 없는가 | — | ○ |

**축을 섞으면 실패 원인을 못 가른다.** 3단계에서 「형식 오류인가 내용 오류인가」를
가르는 것이 학습 판단의 근거이므로 여기서 갈라 두어야 한다.

---

## 3. 서비스 경계와 강제 디코딩

### 할 것

1. **`src/generate/LlmClient.ts`** — 생성 서비스를 보는 **유일한 면**. 이 파일 밖에서
   `fetch` 로 생성 서비스를 부르지 않는다. `SttClient.ts` 의 주석과 구조를 그대로 따른다.
2. **`registerConnectionDefault('generate', 'base', ...)`** — 기본값 `http://127.0.0.1:8802`.
   `CONNECTION_TARGETS` 에 `generate` 를 추가한다. **주소를 아는 면은 `src/generate/` 하나다.**
3. **`gen-lab/`** — `stt-lab` 과 같은 모양의 FastAPI. `python -m server.main`, 포트 8802,
   npm·Vite·node_modules 없음. 엔진 교체가 파일 하나가 되도록 `engines/` 를 나눈다.
4. **문법을 계약에서 뽑는다.** `mission.schema.json` → GBNF(또는 엔진의 스키마 지정)를
   **생성 시점에 변환**한다. 손으로 쓴 문법 파일을 두지 마라 —
   **계약이 원본이라는 원칙이 여기서 생성기까지 확장된다.**

### 지켜야 하는 것

- **서비스가 없어도 화면이 뜬다.** `availability.ts` 와 같은 순수 함수 하나로
  「무엇이 켜지고 무엇이 꺼지는가」를 뽑고, `verify:no-llm` 이 그 함수를 실제로 부른다.
  **대본 재생 · 되감기 · 캔버스는 생성 서비스와 무관하게 돈다.**
- **정지 미리보기 규칙은 그대로다.** 생성 결과는 **제안 상태**로만 뜬다.
  사람이 수락하기 전에는 아무것도 실행되지 않는다(`VZ-U-07`).
- **`LlmClient` 는 실패를 던지기만 하고 잡지 않는다.** 무엇을 끌지는 화면이 정한다.

---

## 4. `VZ-G-01` 베이스라인 — 학습 없이 먼저 잰다

### 할 것

강제 디코딩 + few-shot 만으로 발화 → 마일스톤을 낸다. **CLI 로 먼저 돌린다.**
화면에 붙이기 전에 숫자가 나와야 한다 — 붙인 뒤에 실패하면 모델 탓인지 붙이는 코드 탓인지
가를 수 없다. `VZ-G-03` 이 「생성기가 스스로를 검증하면 같은 오해를 두 번 반복한다」고
적어 둔 것과 같은 논리다.

### 프롬프트에 넣는 것

- `places.json` 의 **장소 목록과 인접 관계** — 여기 없는 장소를 만들면 실패다
- 대본 2~3편의 마일스톤 목록을 few-shot 예시로. **채점 대상인 편은 예시에서 뺀다**
- 마일스톤은 **추상**이라는 규칙 — 로봇 기종·좌표·속도가 나오면 실패다

### 측정

| 재는 것 | 왜 |
|---|---|
| 스키마 통과율 | 강제 디코딩이 실제로 듣는가 |
| 마일스톤 개수·순서 일치율 | G-01 본문 |
| **장소 어휘 위반 건수** | `places.json` 밖 장소를 지어낸 횟수. 그라운딩의 효과 |
| 추상 위반 건수 | 마일스톤에 기종·수치가 새어 들어온 횟수 |
| 응답 시간 | **실시간이 아니다.** 사람이 수락하는 단계이므로 5~10초는 허용된다 |

**모델 후보를 둘 이상 같은 정답셋으로 잰다.** 8B 급과 4B 급을 각각 재고 낙폭을 표로 남긴다 —
이것이 배포 크기 결정의 근거이자 논문 5장의 「경량화 대가」 숫자다.

---

## 5. `VZ-G-02` — `deps` 를 모델이 만들게 하지 마라

### 문제

대본 1편의 병렬 가지(`T-14b`·`T-14c`)는 **`worldTimeline` 34s/44s 의 `in_view` 변화가
근거**였다. 사이트개선 문서가 「지어낸 것이 아니다」라고 적은 그 자리다.
그런데 `VZ-G-02` 는 **실행 전**에 DAG 를 만든다. worldTimeline 이 없다.
근거 없는 상태에서 `deps` 배열을 직접 내게 하면 순환·고아 노드·엉뚱한 합류가 나온다.

### 두 단계로 나눈다

**(a) 모델은 노드 목록만 낸다** — 마일스톤별로 `title` · `nodeKind` · `target`, 일렬로.

**(b) `deps` 는 규칙으로 계산한다** — `src/generate/solveDeps.ts`, 순수 함수.

| 규칙 | 내용 |
|---|---|
| 자원 순차 | 같은 `target` 의 `act` 끼리는 순서를 지킨다 |
| 관측 선행 | `decide` 는 자기가 쓰는 `sense` 뒤에 온다 |
| 검증 후행 | `verify` 는 대응하는 `act` 뒤에 온다 |
| 마일스톤 경계 | 다음 마일스톤의 첫 노드는 이전 마일스톤의 **끝 노드**에 매달린다 |
| 그 외 | **제약이 없으면 매달지 않는다** |

마지막 줄이 요점이다. **병렬은 「만들어지는」 것이 아니라 「제약이 없어서 남는」 것이 된다.**
`T-14b`(robot-01 위치 발행)와 `T-14c`(camera-02 시야 이탈)가 갈라지는 이유가
「다른 자원이라 서로 기다릴 필요가 없다」로 설명되고, 이것은 실행 기록 없이 성립한다.

부수 효과 둘이 크다.

- **모델이 순환을 만들 수 없다.** `layout.ts` 의 `depths()` 무한 재귀 위험이 원천에서 사라진다.
- **이 규칙 세트가 그대로 `VZ-G-03` 검증기의 규칙이 된다.** 생성과 검증이 자연히 갈라진다.

### 되돌아가는 엣지

`refEdges` 는 **생성하지 않는다.** 2편에 하나 있지만 그것은 사람이 대본에 적은 것이고,
실행 전 계획에서 재탐색 루프를 지어내는 것은 「사용자가 준 단계를 더하지 않는다」 원칙 위반이다.

### 계약 보강

`task.schema.json` 에 **`node_kind` 를 선택 필드로 추가**한다. 지금은 대본 JSON 에만 있고
계약에 없어서, 생성 결과를 계약으로 검증할 수 없다. 선택 필드이므로 기존 예시는 무효가 되지 않는다.

---

## 6. 화면 적용 — 마지막이다

4·5단계가 정답셋에서 숫자를 낸 뒤에만 붙인다.

- 발화 입력(음성 또는 직접 입력) → 생성 서비스 → **제안 상태 임무 객체**
- 사람이 수락해야 캔버스에 실행 노드로 올라간다(`VZ-U-07`)
- **생성 근거를 기록에 남긴다** — `produced_by=ai`, 어느 모델·어느 프롬프트였는지.
  이것이 없으면 역추적이 맨 위까지 못 닿는다(`VZ-G-01` 의 「왜 별도 요구사항인가」)
- 서비스가 없으면 이 경로만 꺼지고 화면은 그대로다

---

## 학습 판단 관문 — 4단계 뒤에 선다

**여기까지 오기 전에 학습을 돌리지 마라.** 베이스라인이 없으면
「프롬프트만으로도 되는 것 아니냐」에 답할 숫자가 없다.

4단계의 실패를 유형으로 나눈 뒤 결정한다.

| 실패 유형 | 처방 | 학습이 필요한가 |
|---|---|---|
| 스키마 위반 | 강제 디코딩 | 아니다 |
| 없는 장소를 지어냄 | `places.json` 그라운딩 · G-03 검증 | 아니다 |
| 추상 위반(기종·수치 유입) | 프롬프트 규칙 · 검증 | 아니다 |
| **분할 입도가 정답과 다름** | — | **여기만 남으면 그때 학습** |

학습을 하게 되면 **목적을 「정확도」가 아니라 「형식 안정성」으로 잡는다.**
정답 4편으로 정확도를 올렸다는 주장은 방어되지 않지만, 작은 모델의 스키마 위반율을
낮췄다는 주장은 방어된다. 그 경우 `places.json` 인접 그래프에서 경로를 뽑아 만드는
합성 증강도 정당해진다 — **형식 학습이 목적이기 때문이다.**

---

## 검증

| 검사 | 무엇을 |
|---|---|
| **`verify:places`** (신설) | 대본 4편의 발화·마일스톤에 나오는 장소가 `places.json` 에 전부 있는가 · 인접이 양쪽에 적혀 있는가 · **기하 파일을 읽는 경로가 없는가** |
| **`verify:no-llm`** (신설) | 생성 서비스가 없어도 화면이 뜨고 **대본 재생·되감기·캔버스가 도는가** · 생성 경로만 꺼지는가 |
| **`verify:gen-port`** (신설) | 생성 주소를 아는 면이 `src/generate/` 하나인가 (`verify:stt-port` 와 같은 모양) |
| **`verify:dep-rules`** (신설) | `solveDeps()` 가 순환을 만들지 않는가 · **대본 4편의 노드 목록을 넣으면 원래 `deps` 가 복원되는가** |
| `verify:scenario` (유지) | `MSN-260826-01` 무수정 |
| `verify:standalone` (**무개정 통과**) | 생성 계층이 번들을 오염시키지 않는가 — 모델이 브라우저에 들어가면 여기서 잡힌다 |
| `verify:one-gateway`·`replay`·`trace-append` 등 기존 전부 | 통과 |

`verify:dep-rules` 가 이 작업에서 가장 중요한 검사다. **규칙이 사람이 손으로 만든 정답
DAG 를 복원하지 못하면 규칙이 틀린 것이고, 그때는 모델을 고칠 것이 아니라 규칙을 고쳐야 한다.**

---

## 건드리지 않는 것

- `scenarios/*.json` — **읽기만 한다.** 특히 `MSN-260826-01`(HCI 전달본)
- `web-dashboard`(기준선) · `stt-lab` · `stt/engines/*.py`
- 캔버스 구조 · 뷰 노드 · 되감기 — 이번 범위가 아니다
- `places.geometry.json` 을 생성 경로에서 읽는 것

---

## 이 다음에 오는 것 (참고 · 이번 범위 아님)

| # | 무엇 | 논문 축 |
|---|---|---|
| 1 | **결함 주입** (REQ-1409) | A |
| 2 | **격리 집합 계산** (`VZ-D-05`) | A |
| 3 | `VZ-G-03` 검증기 본체 · `VZ-G-04` 어댑터 전개 · `VZ-G-05` 평가 판정 | — |

**1·2 가 논문의 핵심 주장이고 아직 코드가 없다.** 이 작업이 그것을 밀어내지 않도록
단계마다 끊어서 커밋하고, 4단계 관문에서 학습으로 갈지 여기서 멈출지를 판단한다.

---

## 보고서

단계마다 하나 — `reports/2026-09-04_마일스톤분리_N단계.md`. 최소한 아래를 적어라.

- 1단계: 맵에서 뽑은 장소 수와 인접 수, **뽑지 않기로 한 것과 그 이유**, 415호처럼 자리만 둔 것
- 2단계: 정답셋 규모(임무 4 × 발화 변형 n), 네 축을 가른 근거
- 3단계: 계약에서 문법을 뽑는 방식, 서비스가 꺼졌을 때 실제로 꺼지는 것 목록
- 4단계: **모델별 네 축 숫자 표**(8B 대 4B 낙폭 포함), 실패 유형 분류, **학습 판단과 그 근거**
- 5단계: `solveDeps()` 규칙 표와 **대본 4편 복원 결과**, 복원이 안 된 자리와 그 이유
- 지시서와 다르게 한 곳과 그 이유
