# 작업 지시: 통합 — 셸과 공유 계층을 먼저, 탭 이식은 뒤에

**착수**: 지금 (0·1단계 완료 직후)
**선행**: 0단계(폴더 골격·이식·3계층 계약) · 1단계(화면 5종 목 데이터 동작) 완료
**대상 문서**: `요구사항정의서.md` **§3.1 통합 앱 구조** · `구현진행_가시화도구_260826.md` §6-6

---

## 배경

가시화 파트에는 두 개의 앱이 있다. 기존 프로토타입(`web-dashboard/`, 탭 7개)과
새로 만든 「임무 설계 및 디버깅」(`viz-debugger/`)이다.

**기존 것을 폐기하지 않고 합친다.** 폐기하면 잃는 것이 셋이다.

1. 다른 파트와의 수신·송신 계약이 끊긴다. `VZ-I-06`↔`AI-C-08`·`AI-C-14`,
   `VZ-I-07`↔`AI-E-01`, `VZ-I-09`↔`AI-S-01`~`03`, `VZ-U-02`↔트윈 정합이 상대를 잃는다.
   **AI 파트가 `AI-D-01`·`02`를 지웠을 때 가시화 요구사항 다섯 개가 상대를 잃었던 일**을
   이번에는 우리가 남에게 저지르게 된다.
2. 요구사항정의서 §1의 서비스 목적 둘 중 하나(자동 정합)가 사라진다.
3. 이미 동작하고 검증 5종이 통과하는 것을 버리게 된다.

그래서 **탭 7개 중 5개를 이식하고, 「임무 설계 및 디버깅」을 탭 하나로 넣어 탭 6개로 만든다.**
요구사항 46건이 하나도 죽지 않는다.

## 왜 지금인가 — 통합은 하나가 아니라 셋이다

| | 무엇 | 언제 |
|---|---|---|
| ① 셸 골격 | 탭 전환 · 상단 공통 바 · 빈 탭 5개 | **이번 작업** |
| ② 공유 계층 경계 | transport 하나 · 명령 출구 하나 · store 분리 · 단독 빌드 타깃 | **이번 작업** |
| ③ 탭 5개 실제 이식 | 구역현황판 · 제어 · 지표 · 영상 · 파이프라인 | 10월 |

**②를 미루면 반드시 비싸진다.** 다음 구간에서 기록 저장소와 명령 개입(정지·재실행)을 만드는데,
공유 계층 경계가 없으면 그것들이 **탭⑥ 안에** 만들어지고 나중에 뜯어내야 한다. 특히 셋이 위험하다.

- **명령 출구** — 탭⑥이 자기 발행 경로를 만들면 나중에 탭②를 붙일 때 출구가 둘이 된다.
- **상태 어휘** — 장치 4종과 태스크 8종을 나중에 가르는 것은 이미 섞인 타입을 푸는 일이다.
- **단독 빌드** — 나중에 만들면 이미 얽힌 import를 끊어야 한다. 논문 측정축 D가 여기 걸린다.

**①도 지금이 낫다.** HCI 팀원에게 보낼 때 "화면 하나"가 아니라 **"탭 6개 중 ⑥이 채워진 앱"**이
되어야 배치를 논할 수 있다. 이게 전체에서 어디쯤인지 모르면 화면 안쪽 이야기만 하게 된다.

**③은 급하지 않다.** 기존 코드가 이미 동작한다.

**그릇은 `viz-debugger/` 다.** 방향이 반대(기존 앱에 새 탭을 넣기)이면 기존 코드를 고치게 되고,
"원본 무수정 · 기존 검증 5종 통과"라는 안전장치가 무너진다.

---

## 먼저 읽을 것

1. **`요구사항정의서.md` §3.1** — 이번 작업의 명세다. 공유 계층 16건, 탭 6개 배치,
   기존 7탭에서 어떻게 옮겨졌는지, 46건 배치 확인표가 전부 여기 있다.
2. 엑셀 김현우 시트 **`통합 앱 배치`** 열 — 항목마다 어느 탭인지 적혀 있다.
3. `가시화_프로토타입_설명서.md` §2-1~2-7 — 기존 탭 7개가 각각 무엇을 하는 화면인지.
   **탭별 관련 요구사항이 절 머리에 적혀 있다.**
4. `기술스택_가시화도구_260826.md` §3 이식 판단표 · §4 폴더 구조.
5. 0·1단계 보고서 — 무엇이 바뀌었고 무엇이 미결로 넘어왔는지.

---

## 제약 — 반드시 지킬 것

1. **`web-dashboard/` 를 한 줄도 고치지 않는다.** 이식은 **복사해서 `viz-debugger/` 안에서
   고치는 것**이다. 작업 전후로 `web-dashboard`의 검증 5종(`verify:cache` · `verify:pipeline` ·
   `verify:mission-graph` · `verify:contract-roundtrip` · `verify:catalog-derived`)과
   `typecheck` · `build`가 모두 통과해야 한다.
2. **1단계 결과물을 깨지 않는다.** 화면 5종이 지금 동작하고 있다. 셸을 씌우는 것이지
   화면을 다시 만드는 것이 아니다. 작업 후에도 같은 화면이 같게 동작해야 한다.
3. **명령 출구는 하나다.** 지금은 탭⑥뿐이지만 **자리를 공유 계층에 만든다.**
   나중에 탭②가 붙을 때 같은 경로를 쓴다. `verify:single-egress` 로 검사한다.
4. **탭⑥ 단독 빌드를 유지한다.** 다른 탭 코드를 import 하지 않아야 한다.
   `verify:standalone` 으로 검사한다.
5. **상태 어휘를 분리한다.** 탭①의 **장치 상태 4종**(`VZ-U-01`)과 탭⑥의 **태스크 상태 8종**
   (`VZ-D-06`)은 다른 것이다. **타입 이름부터 가른다.** 같은 `Status` 타입을 공유하지 않는다.
   지금은 탭①이 비어 있지만 **타입 자리를 먼저 갈라 둔다.**
6. **빈 탭에 기능을 넣지 않는다.** ①~⑤는 placeholder다. 이번에 채우지 않는다.
7. **의존성을 늘리지 않는다.** 라우터 라이브러리도 넣지 않는다 — 탭 전환은 상태 하나면 된다.
8. **이식한 파일 맨 위에 출처를 남긴다.**
   ```
   // 이식: web-dashboard/src/views/DeviceGrid.tsx @ <커밋> — 탭①로 개조
   ```

---

## 단계

### 1단계 — 기준선 확인 (먼저 한다)

1. `web-dashboard`의 검증 7종을 돌리고 **결과를 기록**한다.
2. `viz-debugger`의 검증(0·1단계에서 만든 것 전부)을 돌리고 기록한다.
3. 1단계 화면 5종이 뜨는 것을 확인하고 스크린샷을 남긴다. **작업 후 비교 기준이다.**
4. `git status`가 깨끗한지 확인한다.

### 2단계 — 셸 골격 ①

`src/shell/` 에 만든다. 0단계에서 빈 폴더로 잡아 둔 자리다.

- **탭 전환** — 탭 6개. ⑥만 채워져 있고 ①~⑤는 placeholder다.
  placeholder에는 **"이식 예정 — 기존 프로토타입에서 동작 중"**을 적는다.
  비어 있는 것과 미구현인 것은 다르다.
- **탭을 옮겨도 구독이 끊기지 않아야 한다.** 기존 앱이 지키던 규칙이고
  (`가시화_프로토타입_설명서.md` §2 머리말), 돌아왔을 때 화면이 비면 안 된다.
  **표시만 바꾸고 구독은 유지한다.**
- **상단 공통 바** — 임무 제어(정지·재시작·중단) · 임무 이력 · 알림.
  1단계에서 탭⑥ 안에 만든 것을 셸로 **끌어올린다.** 알림에는 `VZ-I-10`(외부 AI 실패)과
  **탭⑥의 생성 실패**(F15)가 함께 올라온다.
- 라우터를 쓰지 않는다. 탭 전환은 상태 하나로 충분하다.

**판정 기준**: 탭 6개가 전환되고, ⑥에서 다른 탭에 갔다 돌아와도 화면이 그대로다.

### 3단계 — 공유 계층 경계 ②

요구사항정의서 §3.1의 16건이다. **탭⑥ 안에 있는 것을 꺼내 공유 계층으로 옮긴다.**

| 묶음 | 이번에 할 것 |
|---|---|
| 전송·구독 | `transport` 하나를 앱 전체가 공유한다. **탭마다 연결을 새로 열지 않는다** |
| 레지스트리 | 탭⑥의 배정 풀이 쓰는 원천을 공유 계층으로. 나중에 탭①이 같은 것을 쓴다 |
| 명령 발행·추적 | **여기가 핵심이다.** 탭⑥의 정지·재시작·재실행이 공유 계층의 발행 경로를 쓰게 한다 |
| 권한·표기·알림·자체 관측·기동 | 나머지 |

**store는 나누어 둔다.** 기존 5개 탭은 `store.ts`의 **채널별 최신값** 기반이고 탭⑥은
**기록 열** 기반이다. 지금은 후자만 있지만 **전자가 들어올 자리를 비워 둔다.**

권장 형태 — **`transport` 는 하나, `store` 는 둘.** 구독 관리만 공유 계층이 맡고,
수신한 봉투를 각 store가 각자 해석한다. **하나로 합치려 들지 말 것.** 합치면 둘 다 망가진다.

**판정 기준**: 탭⑥이 `transport`·명령 발행을 **직접 갖지 않고** 공유 계층을 통해서만 쓴다.

### 4단계 — 단독 빌드 타깃

탭⑥만 뜨는 빌드를 만든다. 논문 측정축 D(계측 오버헤드)를 잴 때 다른 탭의 메모리·렌더
부하가 섞이면 "설명 가능성의 대가"를 분리할 수 없다.

- `npm run build:standalone` 같은 별도 타깃으로 둔다.
- **탭⑥ 코드가 셸이나 다른 탭을 import 하지 않아야 한다.** 공유 계층은 써도 된다.
- 번들 크기를 기록해 둔다. 논문 측정의 참고값이 된다.

### 5단계 — 검증 스크립트 둘

기존 방식대로 **단독 실행 가능한 Node 스크립트**로 만든다.

- **`verify:single-egress`** — 명령 발행 경로가 하나뿐인지.
  **일부러 두 번째 출구를 만들면 실패로 잡히는지**까지 확인한다. 통과만 보고 끝내지 않는다.
- **`verify:standalone`** — 단독 빌드가 성립하는지. 탭⑥이 셸·다른 탭을 import 하지 않는지.

### 6단계 — HCI 전달본 갱신

1단계에서 만든 `viz-debugger/HANDOFF.md` 를 고친다.

- **맨 위에 "⑥번 탭만 봐주세요"를 적는다.** 나머지 다섯은 이식 예정이라 이번 논의 대상이 아니다.
  이 문장이 없으면 팀원이 빈 탭 다섯 개를 보고 무엇을 봐야 할지 몰라 시간을 쓴다.
- 탭 6개 구조를 한 줄 그림으로 넣어 **⑥이 전체에서 어디쯤인지** 보이게 한다.
- 논의 항목 7건은 그대로 둔다.

### 7단계 — 문서 정정

코드와 독립적으로 진행할 수 있다.

- 요구사항정의서 §3.1의 표와 **실제로 만든 것이 다르면 문서를 고친다.** 문서가 코드를 따라간다.
- 남은 것(탭 5개 이식)이 무엇인지 보고서에 목록으로 남긴다.

---

## 이번에 하지 않는 것 — 10월로 남긴다

탭 ①~⑤의 실제 이식이다. 순서와 주의점만 적어 둔다.

| 순서 | 탭 | 원본 | 주의 |
|---|---|---|---|
| ① | 구역 현황판 | `DeviceGrid.tsx` · `DeviceCard.tsx` | 장치 상태 4종. 탭⑥의 8상태와 타입을 섞지 말 것 |
| ② | 제어 패널 | `ControlPanel.tsx` | 명령은 공유 계층으로만 나간다 |
| ③ | 지표 조회 | `MetricsView.tsx` | `HW-C-05` 개정으로 하드웨어 지표 export가 60초다. 15초 질의에 같은 점이 반복되니 **지표별 원천 주기를 표기**한다 |
| ④ | 영상 오버레이 | `VideoOverlayView.tsx` | 미디어 평면은 별도 경로다(`AI-C-14`) |
| ⑤ | 파이프라인 편집기 | `NodeGraphView.tsx` 의 **데이터 파이프라인 모드만** | 임무 관제 모드는 가져오지 않는다 — 탭⑥이 대체한다. **동결이므로 기능 추가 금지** |

**⑤에 주의한다.** 원본은 모드가 둘이다. 임무 관제 모드를 함께 가져오면 탭⑥과 같은 화면이
두 개가 되어 사용자가 어느 것을 봐야 하는지 알 수 없게 된다.

이식하지 않는 것 — 구 **임무 승인·진행** 탭(탭⑥이 통폐합), 구 **판단·알림** 탭(해체:
계층 뷰는 탭⑥, 위험도는 탭①, AI 실패는 상단 알림, 자체 관측은 공유 계층).

---

## 완료 판정

이 여덟이 전부 참이어야 이번 작업이 끝난 것이다.

1. 탭 6개가 전환되고, ⑥에서 나갔다 돌아와도 화면이 그대로다.
2. ①~⑤ placeholder에 **"이식 예정"** 표시가 있다.
3. 상단 공통 바(임무 제어·이력·알림)가 **어느 탭에서나** 동작한다.
4. 탭⑥이 `transport`·명령 발행을 직접 갖지 않고 **공유 계층을 통해서만** 쓴다.
5. `npm run build:standalone` 이 성립한다 (`verify:standalone` 통과).
6. 명령 출구가 하나다 (`verify:single-egress` 통과).
7. `HANDOFF.md` 맨 위에 **"⑥번 탭만 봐주세요"**가 있다.
8. `web-dashboard` 의 검증 7종이 **작업 전과 동일하게** 통과하고,
   1단계 화면 5종이 **작업 전 스크린샷과 같게** 뜬다.

---

## 보고

`reports/` 아래에 기존 형식(`YYYY-MM-DD_HHMM_제목.md`)으로 남긴다. 담을 것.

- 기준선 검증 결과(작업 전)와 완료 후 결과
- **공유 계층 경계를 어디에 그었는지** — 이번 작업의 핵심 판단이므로 근거를 남긴다.
  특히 탭⑥에서 무엇을 꺼냈고 무엇을 남겼는지
- 탭⑥ 단독 빌드의 번들 크기 (논문 측정축 D 참고값)
- 남은 탭 5개 이식 시 예상되는 걸림돌
- 요구사항정의서 §3.1과 어긋난 지점

**막히면 임의로 정하지 말고 보고서에 남기고 멈춘다.** 특히 공유 계층의 경계와 명령 출구는
통합 전체가 걸린 결정이라 혼자 바꾸지 않는다.
