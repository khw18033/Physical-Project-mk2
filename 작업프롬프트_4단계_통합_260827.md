# 작업 지시: 4단계 — 기존 탭을 합쳐 하나의 앱으로

**기간**: 3단계 완료 후 (2026-10월 예정)
**선행**: 0~3단계 완료 — 「임무 설계 및 디버깅」이 단독으로 동작하고 결함 60건 자동 측정이 나온 상태
**대상 문서**: `요구사항정의서.md` **§3.1 통합 앱 구조** · `구현진행_가시화도구_260826.md` §6-6 · `기술스택_가시화도구_260826.md`

---

## 배경

가시화 파트에는 두 개의 앱이 있다. 기존 프로토타입(`web-dashboard/`, 탭 7개)과 8/26 회의 이후
새로 만든 「임무 설계 및 디버깅」(`viz-debugger/`)이다.

**기존 것을 폐기하지 않고 합친다.** 폐기하면 잃는 것이 셋이다.

1. 다른 파트와의 수신·송신 계약이 끊긴다. `VZ-I-06`↔`AI-C-08`·`AI-C-14`,
   `VZ-I-07`↔`AI-E-01`, `VZ-I-09`↔`AI-S-01`~`03`, `VZ-U-02`↔트윈 정합이 전부 상대를 잃는다.
   **AI 파트가 `AI-D-01`·`02`를 지웠을 때 가시화 요구사항 다섯 개가 상대를 잃었던 일**을
   이번에는 우리가 남에게 저지르게 된다.
2. 요구사항정의서 §1의 서비스 목적 둘 중 하나(자동 정합)가 사라진다.
3. 이미 동작하고 검증 5종이 통과하는 것을 버리게 된다.

그래서 **탭 7개 중 5개를 이식하고, 「임무 설계 및 디버깅」을 탭 하나로 넣어 탭 6개로 만든다.**
요구사항 46건이 하나도 죽지 않는다.

**그릇은 `viz-debugger/` 다.** 방향이 반대(기존 앱에 새 탭을 넣기)이면 기존 코드를 고치게 되고,
"원본 무수정 · 기존 검증 5종 통과"라는 안전장치가 무너진다. 문제가 생겼을 때 내가 깼는지
원래 그랬는지 가를 기준선이 사라진다.

---

## 먼저 읽을 것

1. **`요구사항정의서.md` §3.1** — 이번 작업의 명세다. 공유 계층 16건, 탭 6개 배치,
   기존 7탭에서 어떻게 옮겨졌는지, 46건 배치 확인표가 전부 여기 있다.
2. 엑셀 김현우 시트 **`통합 앱 배치`** 열 — 항목마다 어느 탭인지 적혀 있다.
3. `가시화_프로토타입_설명서.md` §2-1~2-7 — 기존 탭 7개가 각각 무엇을 하는 화면인지.
   **탭별 관련 요구사항이 절 머리에 적혀 있다.**
4. `기술스택_가시화도구_260826.md` §3 이식 판단표 · §4 폴더 구조.
5. 0~3단계 보고서 — 무엇이 바뀌었고 무엇이 미결로 넘어왔는지.

---

## 제약 — 반드시 지킬 것

1. **`web-dashboard/` 를 한 줄도 고치지 않는다.** 이식은 **복사해서 `viz-debugger/` 안에서
   고치는 것**이다. 작업 전후로 `web-dashboard`의 기존 검증 5종(`verify:cache` ·
   `verify:pipeline` · `verify:mission-graph` · `verify:contract-roundtrip` ·
   `verify:catalog-derived`)과 `typecheck` · `build`가 모두 통과해야 한다.
2. **이식한 파일 맨 위에 출처를 남긴다.**
   ```
   // 이식: web-dashboard/src/views/DeviceGrid.tsx @ <커밋> — 탭①로 개조
   ```
3. **명령 출구는 하나다.** 탭②의 수동 제어와 탭⑥의 재실행이 **같은 발행 경로**를 쓴다.
   두 벌이 되면 트레이스가 갈라지고 `produced_by=human` 기록이 한쪽에만 남는다.
   `verify:single-egress` 로 검사한다.
4. **탭⑥ 단독 빌드를 유지한다.** 다른 탭 코드를 import 하지 않아야 한다.
   논문 계측 측정(측정축 D)에 관제·영상 탭의 메모리·렌더 부하가 섞이면 안 된다.
   `verify:standalone` 으로 검사한다.
5. **상태 어휘를 분리한다.** 탭①의 **장치 상태 4종**(`VZ-U-01`)과 탭⑥의 **태스크 상태 8종**
   (`VZ-D-06`)은 다른 것이다. 둘 다 "상태"라 섞이기 쉬우니 **타입 이름부터 가른다.**
   같은 `Status` 타입을 공유하지 않는다.
6. **탭⑤ 파이프라인 편집기는 동결이다.** 이식만 하고 **기능을 추가하지 않는다.**
   동작하는 채로 옮기는 것이 목적이고, 화면에 동결 표시를 남긴다.
7. **의존성을 늘리지 않는다.** 이식 과정에서 라이브러리를 새로 넣지 않는다.
8. **목임을 감추지 않는다.** 목 게이트웨이·목 실행기라는 사실이 화면과 문서에 드러나야 한다.

---

## 단계

### 1단계 — 기준선 확인 (먼저 한다)

1. `web-dashboard`의 검증 7종을 돌리고 **결과를 기록**한다.
2. `viz-debugger`의 검증(0~3단계에서 만든 것 전부)을 돌리고 기록한다.
3. `git status`가 깨끗한지 확인한다.

이걸 먼저 하지 않으면 나중에 깨졌을 때 원인을 가릴 수 없다.

### 2단계 — 탭 셸과 상단 공통 바

`src/shell/` 에 만든다. 0단계에서 빈 폴더로 잡아 둔 자리다.

- **탭 전환** — 탭을 옮겨도 **구독이 끊기지 않아야 한다.** 기존 앱이 지키던 규칙이고
  (`가시화_프로토타입_설명서.md` §2 머리말), 돌아왔을 때 화면이 비면 안 된다.
  탭은 **표시만 바꾸고 구독은 유지**한다.
- **상단 공통 바** — 임무 제어(정지·재시작·중단) · 임무 이력 · 알림.
  탭⑥에서 만든 것을 셸로 끌어올린다. 알림에는 `VZ-I-10`(외부 AI 실패)과
  **탭⑥의 생성 실패**(F15)가 함께 올라온다.

**판정 기준**: 빈 탭 6개가 전환되고, 탭을 옮겼다 돌아와도 구독이 살아 있다.

### 3단계 — 공유 계층 정리

`src/shared/` (또는 0단계 구조에 맞는 위치)로 모은다. 요구사항정의서 §3.1의 16건이다.

| 묶음 | 내용 |
|---|---|
| 전송·구독 | `transport` 하나를 앱 전체가 공유한다. **탭마다 연결을 새로 열지 않는다** |
| 레지스트리 | 탭①의 장치 카드와 탭⑥의 배정 풀이 **같은 원천**을 쓴다 |
| 명령 발행·추적 | 제약 3 — 출구 하나 |
| 권한·표기·알림·자체 관측·기동 | 나머지 |

**여기가 이번 작업에서 가장 위험한 구간이다.** 기존 5개 탭은 `store.ts`의 **채널별 최신값**
기반이고 탭⑥은 **기록 열** 기반이다. 두 상태 모델이 한 앱에 공존해야 한다.

권장 형태 — **`transport` 는 하나, `store` 는 둘.** 구독 관리만 공유 계층이 맡고,
수신한 봉투를 각 store가 각자 해석한다. 하나로 합치려 들지 말 것. 합치면 둘 다 망가진다.

**판정 기준**: 게이트웨이 연결이 하나이고, 게이트웨이를 껐다 켜면 **모든 탭의 구독이
한 번에 복원**된다.

### 4단계 — 탭 이식 (의존성이 적은 것부터)

한 번에 하나씩. **탭 하나를 옮길 때마다 검증을 돌린다.**

| 순서 | 탭 | 원본 | 주의 |
|---|---|---|---|
| ① | 구역 현황판 | `DeviceGrid.tsx` · `DeviceCard.tsx` | 장치 상태 4종. **탭⑥의 8상태와 타입을 섞지 말 것**(제약 5) |
| ② | 제어 패널 | `ControlPanel.tsx` | 명령은 **공유 계층으로만** 나간다(제약 3) |
| ③ | 지표 조회 | `MetricsView.tsx` | `HW-C-05` 개정으로 하드웨어 지표 export가 60초다. 15초 질의에 같은 점이 반복되니 **지표별 원천 주기를 표기**한다 |
| ④ | 영상 오버레이 | `VideoOverlayView.tsx` | 미디어 평면은 별도 경로다. 브로커에 싣지 않는다(`AI-C-14`) |
| ⑤ | 파이프라인 편집기 | `NodeGraphView.tsx` 의 **데이터 파이프라인 모드만** | 임무 관제 모드는 **가져오지 않는다** — 탭⑥이 대체한다. 동결이므로 기능 추가 금지 |

**⑤에 주의한다.** 원본 `NodeGraphView.tsx` 는 모드가 둘이다. 임무 관제 모드를 함께 가져오면
탭⑥과 같은 화면이 두 개가 되고, 사용자가 어느 것을 봐야 하는지 알 수 없게 된다.

또 이식하지 않는 것 — 구 **임무 승인·진행** 탭(탭⑥이 통폐합), 구 **판단·알림** 탭(해체:
계층 뷰는 탭⑥, 위험도는 탭①, AI 실패는 상단 알림, 자체 관측은 공유 계층).

### 5단계 — 검증 스크립트 두 개

- **`verify:single-egress`** — 명령 발행 경로가 하나뿐인지. 탭②와 탭⑥이 같은 출구를 쓰는지.
  **일부러 두 번째 출구를 만들면 실패로 잡히는지**까지 확인한다.
- **`verify:standalone`** — 탭⑥만 뜨는 빌드가 성립하는지. 탭⑥ 코드가 다른 탭을 import
  하지 않는지. 번들 크기도 함께 기록해 둔다(논문 측정축 D의 참고값).

### 6단계 — 문서 정정

코드와 독립적으로 진행할 수 있다.

- `가시화_프로토타입_설명서.md` — 화면 7개 → **6개** 기준으로 다시 쓴다. 탭 이름과 순서,
  각 탭의 관련 요구사항을 §3.1과 맞춘다.
- `현재상황_읽기순서.txt` — 통합 후 실행 방법으로 갱신.
- 요구사항정의서 §3.1의 표와 **실제로 만든 것이 다르면 문서를 고친다.** 문서가 코드를 따라간다.

---

## 완료 판정

이 일곱이 전부 참이어야 4단계가 끝난 것이다.

1. 한 앱에서 **탭 6개가 전환**되고, 탭을 옮겼다 돌아와도 구독이 살아 있다.
2. 게이트웨이 연결이 **하나**이고, 재시작 시 모든 탭의 구독이 한 번에 복원된다.
3. 상단 공통 바의 임무 제어·이력·알림이 **어느 탭에서나** 동작한다.
4. **탭⑥ 단독 빌드가 그대로 뜬다** (`verify:standalone` 통과).
5. 명령 출구가 하나다 (`verify:single-egress` 통과).
6. `web-dashboard` 의 검증 7종이 **작업 전과 동일하게** 통과한다.
7. 탭⑤에 **동결 표시**가 있고 기능이 늘지 않았다.

---

## 보고

`reports/` 아래에 기존 형식(`YYYY-MM-DD_HHMM_제목.md`)으로 남긴다. 담을 것.

- 기준선 검증 결과(작업 전)와 완료 후 결과
- **두 상태 모델을 어떻게 공존시켰는지** — 이번 작업의 핵심 설계 판단이므로 근거를 남긴다
- 이식하면서 **원본과 다르게 만든 부분과 그 이유**
- 탭⑥ 단독 빌드의 번들 크기·메모리 (논문 측정축 D 참고값)
- 요구사항정의서 §3.1과 어긋난 지점

**막히면 임의로 정하지 말고 보고서에 남기고 멈춘다.** 특히 공유 계층의 경계와 명령 출구는
통합 전체가 걸린 결정이라 혼자 바꾸지 않는다.
