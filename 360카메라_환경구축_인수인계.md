# 360° 카메라 파이프라인 — 환경 구축 인수인계

**작성**: 이대규 / 2026-09-22
**받는 사람**: 360 카메라 거리 계산부(UniDepth 융합) 작업 담당
**같이 받는 것**: `env_report_<host>_<날짜>.txt` (서버에서 `collect_env.sh` 로 뽑은 실측 리포트)

> 이 문서는 **"어떻게 만드는가"** 만 다룬다.
> 코드가 무엇을 하는지는 `260922_구버전_현행_비전처리_비교_및_배포가이드.md`,
> 파일이 어디 있는지는 `260922_360카메라_파일_전수목록_및_보유현황.md` 를 볼 것.

---

# 0. 결론부터 — 세 갈래 중 하나를 고르면 된다

| | 방식 | 걸리는 시간 | 디스크 | 언제 |
|---|---|---|---|---|
| **안 1** | **`dg` 의 `venv_image` 를 읽기전용으로 같이 쓴다** | **5분** | **0** | ★ 기본값. 코드만 고칠 거면 이걸로 끝 |
| **안 2** | `requirements` 로 각자 새로 만든다 | 1~3시간 | 5~7 GB/인 | 서버가 다르거나, 패키지를 자기 맘대로 바꿔야 할 때 |
| **안 3** | venv 폴더를 복사하고 경로를 치환한다 | 20분 | 5~7 GB/인 | 안 1이 권한 때문에 막혔고 안 2가 귀찮을 때 |

**UniDepth 를 붙이는 작업이면 여기에 한 갈래가 더 붙는다** → §4.

---

# 1. 먼저 3분 안에 답이 나오는 질문 두 개

## Q1. `venv_image` 안에 UniDepth 가 이미 있는가?

```bash
. ~/capstone-db/venv_image/bin/activate
python -c "from unidepth.models import UniDepthV2; print('OK')"
```

| 결과 | 뜻 | 다음 |
|---|---|---|
| `OK` | **환경 작업 끝.** 바로 코드 작성 시작 | §2 건너뛰고 §5 로 |
| `ModuleNotFoundError: unidepth` | **venv_image 에는 없다.** UniDepth 는 conda 쪽에 있음 | §4 로 |

리포트의 **§6** 에 이 결과가 찍혀 있다.

## Q2. 작업할 사람이 `jny` 계정을 쓰는가?

`/home/jny/miniconda3/envs/unidepth` 에 UniDepth 환경이 있고,
`unidepth_dual_yolo_config.yaml` 도 **`jny` 소유**다.

→ **그 사람이 `jny` 라면 환경 구축이 아예 필요 없다.** 이미 다 갖고 있다.
360 스크립트 4개만 자기 홈에 복사하면 그날 바로 돌릴 수 있다.

---

# 2. 안 1 — 공용 venv 읽기전용 (★ 권장)

**venv 를 복사하지 않는다.** `dg` 것 하나를 모두가 activate 한다.
venv 활성화에는 **쓰기 권한이 필요 없다.**

## 2-1. `dg` 계정에서 딱 한 번

```bash
chmod o+x  /home/dg                                     # 통과 권한만 (홈 내용은 안 보임)
chmod -R o+rX /home/dg/capstone-db/venv_image           # 읽기 + 디렉터리 진입
chmod o+r  /home/dg/capstone-db/image_server/yolov8s-world.pt
chmod o+r  /home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml
chmod o+rX /home/dg/capstone-db/docx2026/weights        # CLIP 캐시
```

> `chmod o+x /home/dg` 는 "이 디렉터리를 통과해도 된다"만 주는 것이고,
> `ls /home/dg` 로 내용을 나열하는 권한(`r`)은 주지 않는다.

## 2-2. 팀원 계정에서

```bash
# (1) 환경 — 복사 안 함, 그냥 켠다
. /home/dg/capstone-db/venv_image/bin/activate
python -c "import torch,cv2,py360convert,ultralytics,yaml,fastapi,uvicorn; \
           print('OK', torch.__version__, torch.cuda.is_available())"

# (2) 개인 작업 디렉터리
mkdir -p ~/capstone-db/docx2026/weights/clip
mkdir -p ~/capstone-db/image_server/pano_captures/pro2_anchor

# (3) 스크립트 사본 (자기 것으로 고칠 거니까 복사)
cp /home/dg/capstone-db/docx2026/anchor360_live.py          ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/e2p.py                     ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/relay_loop.py              ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/detect_ground_3b1.py       ~/capstone-db/docx2026/
cp /home/dg/capstone-db/docx2026/detect_send_3b2.py         ~/capstone-db/docx2026/
cp /home/dg/capstone-db/image_server/pano_receiver.py       ~/capstone-db/image_server/

# (4) config 는 ★ 반드시 사본 ★ (원본은 jny 소유·777 이라 언제 바뀔지 모름)
cp /home/dg/capstone-db/image_server/unidepth_dual_yolo_config.yaml \
   ~/capstone-db/image_server/

# (5) CLIP 캐시 복사 — 안 하면 실행할 때마다 338MB 재다운로드
cp /home/dg/capstone-db/docx2026/weights/clip/ViT-B-32.pt \
   ~/capstone-db/docx2026/weights/clip/
```

## 2-3. 동작 확인 — 카메라 없이 (★ 여러 명이 동시에 가능)

```bash
cd ~/capstone-db/docx2026
python3 anchor360_live.py \
  --replay /home/dg/capstone-db/image_server/pano_captures/pro2_anchor/<폴더명>/equirect.jpg \
  --cfg    ~/capstone-db/image_server/unidepth_dual_yolo_config.yaml \
  --u 230 --v -36.35 --pitch -35 \
  --drop-clipped --max-dist 8 --debug-img \
  --out-dir ~/capstone-db/docx2026/live_out
```

기동 로그에서 **이 두 줄이 서로 달라야** 정상이다.
```
  슬라이스   : u=+230.0deg, v=-36.4deg, 720x720
  카메라     : 높이 1.85m, 하향 -35.0deg
```
같으면 `--pitch` 를 안 준 것이고, **바닥 계산이 1.35° 틀어진다.**

## 2-4. 주의 3가지

| | 내용 |
|---|---|
| 1 | **`--cfg` 를 반드시 자기 사본으로 준다.** 안 그러면 `jny` 가 원본을 바꿀 때 검출이 말없이 달라진다. 실제로 `box 0.18→0.12`, `fire extinguisher 0.22→0.12` 로 바뀐 전례가 있다 |
| 2 | 사본 config 안의 `weight:` 는 여전히 `/home/dg/.../yolov8s-world.pt` 절대경로다. §2-1 의 `chmod o+r` 이 돼 있으면 그대로 읽힌다. 안 되면 `--weight` 로 오버라이드 |
| 3 | `dg` 가 venv 에 패키지를 추가/갱신하면 **전원에게 즉시 반영된다.** 누가 언제 뭘 바꿨는지 기록할 것 |

---

# 3. 안 2 — requirements 로 각자 재구성

## 3-1. 재료 (리포트에서 잘라내기)

- `env_report_*.txt` 의 **§5** `BEGIN ~ END requirements-server.txt` 블록
  → 그대로 `requirements-server.txt` 로 저장
- 같은 리포트 **§7** 의 `▶ 재설치 명령` 줄 → 파일 맨 위에 주석으로

```
# 서버: <<리포트 §1 의 OS>>
# Python: <<리포트 §4 의 venv 파이썬 버전>>
# GPU/드라이버: <<리포트 §2 의 nvidia-smi 첫 줄>>
#
# ★ torch 는 이 줄을 먼저 실행할 것 (pip freeze 만으로는 CUDA 빌드가 안 맞는다)
#   <<리포트 §7 의 "▶ 재설치 명령" 그대로>>
#   예) pip install torch==2.4.0 torchvision --index-url https://download.pytorch.org/whl/cu121
```

## 3-2. 설치

```bash
# (0) 디스크 확인 — 1인당 5~7GB
df -h ~

# (1) 베이스 파이썬을 리포트 §4 와 같은 마이너 버전으로 맞출 것
python3 -m venv ~/capstone-db/venv_image
. ~/capstone-db/venv_image/bin/activate
pip install --upgrade pip wheel setuptools

# (2) ★ torch 부터 따로 (인덱스 URL 필수)
pip install torch==<버전> torchvision --index-url https://download.pytorch.org/whl/<cuXXX>

# (3) 나머지
pip install -r requirements-server.txt

# (4) 검증
python -c "import torch,cv2,py360convert,ultralytics,yaml,fastapi,uvicorn; \
           print('OK', torch.__version__, torch.version.cuda, torch.cuda.is_available())"
```

## 3-3. 빠르게 하고 싶으면 `uv`

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
uv venv ~/capstone-db/venv_image --python 3.<리포트 값>
. ~/capstone-db/venv_image/bin/activate
uv pip install torch==<버전> torchvision --index-url https://download.pytorch.org/whl/<cuXXX>
uv pip sync requirements-server.txt
```
pip 대비 10~30배 빠르다. 결과물은 동일한 venv 다.

## 3-4. 가중치는 대부분 자동으로 받아진다

**넘겨줄 필요가 없는 것들** (인터넷이 되면 첫 실행 때 자동 다운로드):

| 파일 | 어디서 | 크기 |
|---|---|---|
| `yolov8s-world.pt` | ultralytics 가 GitHub 릴리스에서 자동 | ~25 MB |
| CLIP `ViT-B-32.pt` | `set_classes()` 가 자동 | 338 MB |
| UniDepth 체크포인트 | `UniDepthV2.from_pretrained("lpiccinelli/unidepth-v2-vitb14")` 가 HF 에서 자동 | ~1 GB |

**단 3가지 조건:**
1. 서버에서 **바깥 인터넷이 나가야** 한다 (학교망 방화벽 확인)
2. CLIP 은 **CWD 아래 `weights/clip/`** 에 캐시된다. 스크립트가 자기 디렉터리로 `chdir` 하므로
   `~/capstone-db/docx2026/` 에서 띄우면 거기 받힌다. **다른 폴더에서 띄우면 또 받는다**
3. config 의 `weight:` / `model_name:` 이 **절대경로**라, 그 경로에 파일이 없으면
   ultralytics 는 그 경로로 받으려 시도하고 UniDepth 는 로컬 경로로 읽으려다 실패한다
   → **자동 다운로드에 기대려면 경로를 이름(`yolov8s-world.pt`) 또는 HF id 로 바꿔야 한다**

> 그래서 실무적으로는 **가중치도 같이 복사하는 게 속 편하다.**
> "환경 스펙만 있으면 된다"는 맞는 말이지만, 위 3조건 중 하나만 어긋나도 30분을 날린다.

---

# 4. UniDepth 환경 — ★ 여기가 진짜 관문

## 4-1. 왜 어려운가

**UniDepth 는 PyPI 에 없다.** `pip install unidepth` 로 안 깔린다.
GitHub 저장소(`lpiccinelli-eth/UniDepth`)를 클론해서 `pip install -e .` 로 깔아야 하고,
**torch / xformers / CUDA 버전이 서로 맞아야** 한다. 여기서 대부분의 시간이 나간다.

그래서 **처음부터 새로 만들지 말고, 이미 돌고 있는 환경을 복제하는 게 정답이다.**

## 4-2. 이미 있는 환경: `jny` 의 conda `unidepth`

7월 검출 명령이 전부 이렇게 시작한다:
```bash
source /home/jny/miniconda3/etc/profile.d/conda.sh && conda activate unidepth
```
→ **UniDepth 는 거기 있다.** 리포트 **§9** 에 그 환경의 `pip freeze` 가 통째로 들어 있다.

## 4-3. 복제 방법 — `conda-pack` (★ 이게 정답)

conda 환경은 venv 와 달리 **경로 독립 아카이브**로 만들 수 있다.

```bash
# jny 계정에서 (또는 그 환경에 쓰기 권한이 있는 사람이)
conda install -n base conda-pack
conda pack -n unidepth -o unidepth_env.tar.gz      # 수 GB

# 받는 쪽
mkdir -p ~/envs/unidepth
tar xzf unidepth_env.tar.gz -C ~/envs/unidepth
source ~/envs/unidepth/bin/activate
conda-unpack                                        # ★ 경로 재작성. 빼먹으면 깨진다
python -c "from unidepth.models import UniDepthV2; print('OK')"
```

`conda-pack` 을 못 쓰면 차선:
```bash
conda env export -n unidepth > unidepth_env.yml
# 받는 쪽
conda env create -f unidepth_env.yml -n unidepth
```
※ `env export` 는 빌드 해시까지 박제해서 다른 머신에서 실패할 수 있다.
같은 서버 안이면 대개 괜찮다. 실패하면 `--from-history` 로 다시 뽑는다.

## 4-4. 그런데 — 정말 UniDepth 를 `venv_image` 에 넣어야 하나?

**세 가지 길이 있고, 난이도와 위험이 많이 다르다.**

| | 방식 | 난이도 | 위험 |
|---|---|---|---|
| **A** | `venv_image` 에 UniDepth 를 추가 설치 | **높음** | ★ torch 버전 충돌로 **기존 360 파이프라인이 깨질 수 있다**. 공용 venv 라 전원 피해 |
| **B** | conda `unidepth` 환경에 360 패키지(py360convert 등)를 추가하고, **거기서 `anchor360_live.py` 를 돌린다** | 낮음 | 7월에 실제로 그렇게 돌렸다. `py360convert` 만 추가하면 될 가능성 |
| **C** | **깊이 추정만 별도 프로세스로 분리.** conda 환경에서 UniDepth 서비스를 띄우고, `anchor360_live.py` 는 HTTP/파일로 깊이만 받아온다 | 중간 | 환경 충돌 0. GO1 쪽이 이미 이 구조(Redis) |

> **A 는 권장하지 않는다.** `venv_image` 는 지금 사진·영상 두 파이프라인이 다 쓰는 공용 환경이고,
> 여기서 torch 를 올렸다 내렸다 하면 검증된 파이프라인이 통째로 멈출 수 있다.
> **먼저 B 를 시도하고, 안 되면 C 로 간다.**

**B 를 시도하는 법 (5분):**
```bash
source /home/jny/miniconda3/etc/profile.d/conda.sh && conda activate unidepth
python -c "import py360convert, ultralytics, yaml, cv2; print('전부 있음')"
# 빠진 게 있으면
pip install py360convert            # ← 순수 파이썬이라 torch 를 안 건드린다. 안전
# 그다음 그냥 돌려본다
cd ~/capstone-db/docx2026 && python3 anchor360_live.py --replay <경로> ... --once
```
`py360convert` 는 numpy 만 쓰는 순수 파이썬 패키지라 **torch 를 건드리지 않는다.**
이게 되면 **환경 작업은 그걸로 끝이다.**

---

# 5. 안 3 — venv 복사 + 경로 치환

venv 를 그냥 복사하면 깨지는 이유는 4가지다. 리포트 §4 에 실제 값이 찍혀 있다.

| # | 깨지는 곳 | 리포트 위치 |
|---|---|---|
| 1 | `pyvenv.cfg` 의 `home = ` 절대경로 | §4 `cat pyvenv.cfg` |
| 2 | `bin/activate` 의 `VIRTUAL_ENV="..."` 문자열 | §4 `grep VIRTUAL_ENV` |
| 3 | `bin/*` 콘솔 스크립트의 절대 shebang | §4 `head -1 bin/pip` |
| 4 | `.so` 확장모듈이 파이썬 마이너·glibc·CUDA 빌드에 묶임 | §1 §3 §7 |

1~3 은 고칠 수 있고, 4 는 **같은 서버 안이면 문제없다.**

```bash
cp -a /home/dg/capstone-db/venv_image ~/capstone-db/venv_image
cd ~/capstone-db/venv_image

# (2) activate 계열
sed -i "s#/home/dg/capstone-db/venv_image#$HOME/capstone-db/venv_image#g" \
    bin/activate bin/activate.csh bin/activate.fish 2>/dev/null

# (3) bin/* 의 절대 shebang (텍스트 파일만)
grep -rIl '^#!/home/dg/capstone-db/venv_image' bin/ 2>/dev/null | \
  xargs -r sed -i "1s#^#!/home/dg/capstone-db/venv_image#\#!$HOME/capstone-db/venv_image#"

# (1) pyvenv.cfg 는 보통 그대로 둬도 된다 (base 파이썬 경로라서)
cat pyvenv.cfg

# 검증
. bin/activate
python -c "import torch,cv2,py360convert,ultralytics; print('OK', torch.__version__, torch.cuda.is_available())"
pip -V     # 경로가 자기 홈으로 나오면 성공
```

검증이 실패하면 **미련 없이 안 2로 간다.**

> ⚠ `virtualenv --relocatable` 은 **폐기된 기능이다. 쓰지 말 것.**

---

# 6. PC(윈도우) 쪽 — 훨씬 간단하다

영상 파이프라인에 필요한 건 **패키지 2개**가 전부다.

```
python --version          :: 3.12 권장 (현재 3.12.10)
pip install requests opencv-python
```

| 용도 | 패키지 |
|---|---|
| RTMP 수신 + JPEG 인코딩 | `opencv-python` (wheel 에 FFmpeg 포함. 별도 설치 불필요) |
| 카메라 제어 / 서버 업로드 / 브릿지 | `requests` |

**사진 경로까지 쓸 거면** Insta360 Stitcher 를 기본 경로에 설치:
`C:\Program Files (x86)\Insta360Stitcher\tools\prostitcher\ProStitcher.exe`
그리고 `stitch_prostitcher.py` + `trt_cache\*.trt` 2개(84 MB)를 같이 둔다.
TRT 캐시가 없으면 첫 stitch 에 **약 19분**이 걸린다.

**각자 바꿔야 하는 값은 딱 3개** — 전부 CLI 인자로 뺄 수 있다.

| 무엇 | 지금 값 | 어떻게 |
|---|---|---|
| 카메라 IP | `192.168.100.124` | `stream_to_server.py --ip <IP>` |
| 서버 주소 | `210.110.250.33:7866` | `--upload http://<IP>:7866/upload_pano` / `--server http://<IP>:7866/anchor360_latest` |
| 유니티 PC IP | `192.168.50.244` | `anchor360_bridge.py --unity-ip <IP>` (유니티 PC 본인이면 `127.0.0.1`) |

⚠ `pro2_auto_bg.py` 만 예외로 **모듈 상수라 코드를 직접 고쳐야** 한다:
```python
CAMERA_IP = "192.168.100.124"
SERVER_IP = "210.110.250.33"
_TRT_BACKUP_DIR = r"C:\Users\asdfa\docx2026_code\trt_cache"   # ← 계정명 하드코딩
```

⚠ 유니티 PC 에서 **UDP 5010 인바운드 허용**. 이게 빠지면 브릿지에 `NEW` 는 뜨는데 유니티 `RX` 가 안 뜬다.

---

# 7. 동시에 못 쓰는 자원 (미리 합의할 것)

| 자원 | 제약 |
|---|---|
| **Insta360 Pro2** | 제어 세션 **1개만**. 2번째는 `already connected by another`. `pro2_auto_bg` / `stream_to_server` / `preview_probe` / Insta360 Pro 앱 전부 택일 |
| **서버 포트 7866** | `pano_receiver` 1 인스턴스만 |
| **`pano_captures/.../LIVE_STREAM/`** | 같은 폴더를 덮어쓴다. 2명이 올리면 섞인다 |
| **UDP 5010** | 유니티 수신기가 스냅샷 방식. 2명이 쏘면 큐브가 번갈아 뒤집힌다 |

**→ 실시간 구동은 한 번에 한 명. 대신 `--replay` 오프라인 작업은 몇 명이든 동시에 가능하다.**
알고리즘·파라미터 실험은 전부 `--replay` 로 충분하다.
`--out-dir` 과 `--capture-id` 만 사람마다 다르게 주면 서로 안 덮어쓴다.

---

# 8. 빈칸 채우기 — 리포트에서 옮겨적을 것

`env_report_*.txt` 를 열고 아래를 채워서 이 문서와 같이 보관.

| 항목 | 리포트 위치 | 값 |
|---|---|---|
| OS | §1 `/etc/os-release` | `<<                >>` |
| GPU / 드라이버 | §2 `nvidia-smi` 첫 줄 | `<<                >>` |
| venv 파이썬 버전 | §4 | `<<                >>` |
| venv 크기 | §12 `du -sh` | `<<                >>` |
| torch 버전 | §7 | `<<                >>` |
| torch CUDA 빌드 | §7 `▶ 재설치 명령` | `<<                >>` |
| **`UniDepthV2` import (venv)** | §6 | `<< OK / FAIL   >>` |
| conda `unidepth` 존재 | §9 | `<< 있음 / 없음  >>` |
| UniDepth 체크포인트 | §10 | `<< 있음 / 없음  >>` |
| `yolov8s-world.pt` md5 | §11-1 | `<<                >>` |
| 디스크 여유 | §12 | `<<                >>` |
| 인터넷 egress | 직접 확인 | `<< 됨 / 안 됨   >>` |

인터넷 확인:
```bash
curl -sI https://github.com | head -1
curl -sI https://huggingface.co | head -1
```
이게 안 되면 §3-4 의 자동 다운로드가 전부 막히므로 **가중치를 반드시 손으로 옮겨야 한다.**

---

# 9. 체크리스트

**보내는 쪽 (이대규)**
- [ ] 서버에서 `bash collect_env.sh` → `env_report_*.txt` 생성
- [ ] 리포트에 비밀번호/키가 남아 있지 않은지 눈으로 확인
- [ ] §5 블록 → `requirements-server.txt` 로 분리
- [ ] §9 블록(있으면) → `requirements-unidepth.txt` 로 분리
- [ ] 안 1로 갈 거면 §2-1 의 `chmod` 4줄 실행
- [ ] 이 문서 §8 빈칸 채우기
- [ ] 스크립트 묶음 전달 (파일 목록은 `260922_360카메라_파일_전수목록_및_보유현황.md`)

**받는 쪽**
- [ ] §1 Q1 실행 — `UniDepthV2` import 되는지
- [ ] §1 Q2 확인 — 내가 `jny` 인가
- [ ] 안 1/2/3 중 택일해서 환경 구성
- [ ] `--replay` 로 1장 돌려서 검출 나오는지 확인
- [ ] 기동 로그의 `슬라이스 v` 와 `카메라 하향` 이 **다른지** 확인 (같으면 `--pitch` 누락)
- [ ] `--cfg` 를 자기 사본으로 주고 있는지 확인

---

# 10. 자주 막히는 곳

| 증상 | 원인 | 조치 |
|---|---|---|
| `ModuleNotFoundError: unidepth` | venv_image 에는 없음 | §4. conda 환경을 쓰거나 conda-pack |
| `torch.cuda.is_available() == False` | CPU 빌드를 깔았음 | `--index-url https://download.pytorch.org/whl/cuXXX` 로 재설치 |
| 338 MB 를 매번 다운로드 | CLIP 캐시가 CWD 기준 | `~/capstone-db/docx2026/weights/clip/` 에 `ViT-B-32.pt` 배치 |
| `FileNotFoundError: yolov8s-world.pt` | config 의 절대경로가 `/home/dg/...` | `chmod o+r` 하거나 `--weight` 로 오버라이드 |
| `Permission denied` (venv) | `/home/dg` 통과 권한 없음 | `chmod o+x /home/dg` |
| `Address already in use` (7866) | 다른 사람이 `pano_receiver` 실행 중 | §7. 한 번에 한 명 |
| `already connected by another` | 카메라 제어 세션 점유 | 다른 창(`preview_probe` 등)부터 닫기 |
| `[live]` 가 초당 1번만 | `--poll` 기본값 1.0 | `--poll 0.1` |
| `[live]` 가 1.7초마다 | `wait_stable` 이 1.5초 소비 | `--no-wait-stable` |
| 검출 좌표가 전부 조금씩 틀림 | `--pitch` 누락 (`--v` 와 같아짐) | `--v -36.35 --pitch -35` 둘 다 줄 것 |
| `grep -c no_wait_stable` 가 1 | argparse 는 하이픈, 코드는 밑줄 | `grep -n -- "no.wait.stable"` 로 볼 것. 두 줄 나오면 정상 |
| 디스크 부족 | venv 1벌 5~7 GB × 인원 | 안 1(공용 venv)로 전환 |

---

*문서 끝. 2026-09-22.*
