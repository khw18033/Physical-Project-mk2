# 캡스톤 MK2 백엔드 — 관측 스택 인터페이스 계약 & 결정 종합

> **작성 맥락**: 이대규 (백엔드 / OpenTelemetry 관측 스택 담당, 이번 rotation PM).
> 260818 회의 이후 세 파트(AI·하드웨어·가시화) 요구사항 정의서를 받아, 백엔드가 정해야 할
> 여섯 결정(①~⑥)을 확정한 기록. **문서 말단(§7)에 내일 회의용 정리를 첨부.**
> 이 문서는 요약본이 아니라 이번 논의 전체를 한곳에 모은 상세 정리다.

---

## 목차
- §0 이 문서의 위치
- §1 배경 요지 (방향)
- §2 세 파트 요구사항 정의서 요지
- §3 핵심 개념 프레임 (3면 계약 · 2평면 · 2 정체성 축)
- §4 백엔드 결정 세트 (Tier 0 + ①~⑥)
- §5 팀원별 전달 사항 (변경 요청 · 답변)
- §6 미결 / Tier 2 항목
- §7 **회의용 정리 (내일 대비)** ← 말단 첨부

---

## §0. 이 문서의 위치

- **큰 프레임 = 범용 커먼 아키텍처** (도메인 무관, 구성요소 갈아끼움 가능). **구체 예시 = 캡스톤 MK2.** MK2가 우선순위지만, 항상 "범용 아키텍처의 한 인스턴스"라는 시선으로 본다.
- 교수님 사상(260814): OTel → Prometheus → Grafana 라는 **'핑크 기반(범용 관측 인프라)'** 위에, 도메인별 **'초록 계층(도메인 접착)'** 을 얹는다. 초록이 우리가 만드는 것. **MK2(로봇)와 하천(월류 방어벽)은 같은 아키텍처의 두 인스턴스** — 교수님이 260715에서 명시("로봇을 모터로 바꾸면 하천과 똑같다").
- **대규 = 요구사항의 시작과 끝(PM).** 이유: 모든 파트가 **OTel 기반 관측 평면**으로 주고받으므로 그 기준을 대규가 쥔다. 대규 담당 = **OTel 관측 스택 백엔드**(+ 디지털트윈 구성 AI, 360 앵커 자산 유지).
- 이 문서 = 그 백엔드 인터페이스 계약과 여섯 결정의 종합.

---

## §1. 배경 요지 (방향)

### 1.1 커먼 아키텍처 (260814 회의)
- **핑크 기반** = 범용 관측 인프라(OTel → Gateway → Prometheus → Grafana). 설치하면 있는 것, 도메인 무관.
- **초록 계층** = 도메인 접착. (a) 데이터가 어떤 규약으로 들어오나, (b) 백엔드에서 어떻게 계층적으로 저장되나, (c) 어떻게 해석·가시화되나, (d) 도메인 로직. 이게 우리가 만드는 것.
- 두 명제:
  - "**구성은 되는데 못써먹는다**" → 초록 계층이 그 간극을 메운다.
  - "**AI가 쓰는 도구**" → 사람은 요구사항·테스트케이스·설계, 구현은 AI 에이전트. 초록 계층은 AI가 헤매지 않고 쓸 수 있는 틀이어야 한다.
- 4파트: **하드웨어 / AI / 백엔드(최중요, 계층적 Edge-Cloud 관제) / 가상화·가시화.**

### 1.2 MK2 정체성 (260715·260716)
- 교수님 캡스톤1 비판: "분산 도시 인프라"라 했지만 실제론 **엣지 노드 1개짜리 특수 케이스**만 구현. PyGui는 개발자 도구지 관리 기능이 아님(없어져야 함). Unity가 ML-Agents로 경로 돌리는 건 틀림 → 로봇이 스스로, 인프라는 장애물 정보만.
- MK2 방향: **다중 중계(제어) 노드 + 핸드오버**, PyGui 제거·자동화, **2단계 디지털 트윈**(전체 저해상 + 로컬 상세), **DB 중심 + 명령 출처(책임소재)**, 로봇이 경로 주체, **360 앵커 위치 보정**.
- 원격 서버(대규) 역할 = **영속 저장 + 모니터링 + Unity 브리지.** 기존 fleet 코드(4브랜치, 로봇↔플랫폼 Redis 계약)는 초안, 성역 아님.

### 1.3 260818 재편 + PM
- 팀 재편: **대규**(OTel 백엔드 + 디지털트윈 AI) / **나영**(MLOps·마이크로서비스 백엔드 + 예측 AI) / **병현**(하드웨어: 말단 센서 + 로봇·장비) / **현우**(가시화 + 로컬 LLM 기반 동적 가시화 도구, 음성 NUI, 관리자 추상화 레벨). **시작 = 대규, 끝 = 현우(시연).**
- 방법: 대규가 otel 기준 확정 → 각자 요구사항 정의서 작성 → 대규 취합·일관성 리뷰 → AI로 파생 → 전체 리뷰.
- 책임소재: 인바운드/아웃바운드 명령·이벤트를 저장(수문 열어라 → 누가 의사결정했나).

---

## §2. 세 파트 요구사항 정의서 요지

### 2.1 AI (진나영)
- **'에이전트' 단위 추상화**, 도메인(캡스톤/군사/하천) 바뀌어도 AI 코드 그대로·설정만.
- 전송 = "백엔드가 정한 공통 브로커". 관측 지표 = otel (`ai.perception.latency_ms` 등, `agent_id`·`domain` 라벨).
- **capability over identity**(구두 확인): AI는 sensor_id가 아니라 "**어떤 기능이 필요한지**"로 모델을 선택. `agent_id`는 trace용으로 실어 나를 뿐, AI 로직이 라우팅에 쓰지 않음.
- 스키마: 인지 입력 `sensor_type`(camera_stereo|mono|lidar|thermal|…), `frame_ref`. 인지 출력 `detections[position_world{x,y,z}, bbox[x1,y1,x2,y2], distance_m, …]`, `model_version`, `processing_latency_ms`. 의사결정 입력 `current_position{x,y,z}`, `capability_flags`. 의사결정 출력 `waypoints[{x,y}]`, `plan_type`(move|hold|alert|…).
- 백엔드에 넘긴 것(8): 공통 브로커, frame_ref 저장, calibration 레지스트리, 하드웨어→AI 상태 라우팅, 실행 결과 피드백 루프, 계획→명령 번역, otel 네이밍 승인, k3s 배포.

### 2.2 하드웨어 (조병현)
- MQTT 토픽 `{domain}/{type}/{id}/{channel}`. QoS/retained/LWT. **traceparent(W3C Trace Context)**. **디바이스는 span 생성 안 함, traceparent 복사만 — span 생성·종료는 백엔드.** `frame_ref`(영상 참조만), `seq`(유실검출), `schema_version`, NTP 동기 필수, `received_at` 관측.
- 로봇: `position{x,z}`, `waypoints{x,z}`, metric `robot.position.x/z`. 접속 online/offline(LWT), 명령 결과 accepted/rejected.
- 센서: `water_level_m`, `flow_velocity_ms`, `levee_state`(open|closed|moving|fault), health(ok|degraded|fault). 명령 멱등(`command_id`), "성공 여부는 ack가 아니라 state의 levee_state로 최종 확인".
- 협의로 넘긴 것(8): 브로커 종류·버전, state 주기·SLA, 영상 스트림 규격, trace 샘플링·span 경계, risk_level 범위, **MQTT→OTel 브리지 주체**, keepalive·오프라인 판정, 로컬 버퍼링·재전송.

### 2.3 가시화 (김현우, v2)
- **대규(백엔드)에게 직접 쓴 인터페이스 계약서.** 2평면 확인. **감사 척추**를 구현: `origin.path`(manual|voice|llm_suggestion|auto), `actor{id,role}`, `expires_at`(지연 명령 실행 금지), `voice.transcript`·`llm.model`·`llm.userModified`, `traceparent`. **O-5**(노드 편집기 "반영" = 파이프라인 변경 → 제어 명령급 감사).
- **v2 핵심 변경**: 상대별 재구성(§2 OTel / §3 AI / §4 하드웨어) + **식별자·좌표계를 하드웨어로 이관**(v1 오귀속 수정). **두 축 명시**: 공간 축(Entity → Node → Zone) + 추상화 축(L2 결심자 → L1 운영자 → L0 개발자). **식별자 통일**: Entity = agent_id/sensor_id(개명·신설 없음), Node = site_id(로봇에 추가), Zone = 선택→domain.
- 백엔드에 넘긴 것(Q1~Q18): 브로커 실체, 브라우저 실시간 경로, 식별자 일원화, 감사 수신/조회/보존, Prometheus 질의 접근·인증, 권한 모델.

---

## §3. 핵심 개념 프레임

### 3.1 3면 계약 (3파트 아님, 3종류)
관측 스택 인터페이스 계약 = **세 종류**이며, 3명(파트)과 **직교**한다.
- **종류1 — 생산 텔레메트리 계약**: metric/log/trace를 어떤 이름·단위·라벨로 내나. (나영·병현이 냄)
- **종류2 — 질의·소비 인터페이스**: Prometheus/Grafana에서 받는 것 + 브라우저 실시간 경로. (현우가 받음)
- **종류3 — 명령·감사 척추**: 명령 전달(MQTT) + 기록(OTel). (현우가 냄 + 책임소재)

산출물 = **3파트(HW/AI/Viz) × 제공/수신 매트릭스**, 각 칸에 종류1/2/3이 섞임. 예: 현우 '제공' 칸 = 종류3(감사), '수신' 칸 = 종류2(질의). "정상 시 텔레메트리 계약"만으론 종류2·3을 놓친다.

### 3.2 2평면
- **데이터 평면(MQTT)**: 실제 payload. **관측 평면(OTel)**: 그 기록. 같은 사건이 둘에 나타나는 것은 중복이 아니라 역할 분리.
- 세 문서가 독립적으로 이 분리를 전제·확인함.

### 3.3 2 정체성 축 (③의 뿌리)
- **축 A — 관측 대상 정체성** (Entity / Node / Zone): "이 데이터가 **어떤 물리 대상**에 관한 것이고 **어디** 있나." 원천 = **하드웨어**(agent_id / sensor_id / site_id). MQTT 페이로드·OTel 라벨.
- **축 B — 서비스 인스턴스 정체성** (컨테이너 / `service.*`): "이 데이터를 **처리·서빙하는 소프트웨어**가 뭐고 어디서 도나." 원천 = **MLOps(나영)** (service.name / service.instance.id).
- **연결 = 병합이 아니라 라벨.** AI 서비스(축 B) 지표에 `entity=go1-01`(축 A) 라벨을 달아 "이 서비스가 대상 X를 처리 중"을 표현. ID 체계를 합치지 않는다.
- 나영의 "agent_id는 trace용, AI는 capability로 라우팅"이 이 구조를 뒷받침. entity 정체성 = 관측/trace용, capability = AI 라우팅용(별도 필드).

---

## §4. 백엔드 결정 세트

### Tier 0 — 이미 정해진 것 (확인만 하고 확정)
- **2평면 원칙**을 팀 공식 규칙으로 승격.
- **저장 배치**: metric → Prometheus, log → Loki, trace → Tempo, **감사 영구 저장 → MySQL**, frame_ref → Redis. (전부 docker-compose에 가동 중.) **MongoDB는 역할 불명 → 재검토.**
- **트레이스 필수** (책임소재 요구 때문, 옵션 아님).
- **로봇 직접 계측 안 함** → 백엔드 **audit-writer**가 Redis/ack 타임스탬프로 사후 span 재구성.

---

### ① 공통 브로커 + 브라우저 실시간 경로
**결정**: **Mosquitto + MQTT 5.0 + TLS + 브라우저는 백엔드 WebSocket 게이트웨이 경유(양방향: 데이터 구독 + 명령 발행) + 단명 토큰.**
- **근거**:
  - Mosquitto: 이미 가동·검증됐고 요구 기능(QoS·retained·LWT·MQTT5·WebSocket)을 전부 커버하면서 운영 부담이 최소.
  - MQTT 5.0: traceparent를 User Property로 실을 수 있고(병현), ack 규약(response topic + correlation data)이 5.0이라야 깔끔(현우). Mosquitto가 5.0 지원(v1.5부터).
  - WS 게이트웨이: 브라우저에 브로커 자격증명을 내리지 않고 인증·필터·rate limit을 백엔드에서 중앙 관리(현우 D-2, H-5와 일치).
- **구현 큰틀**: docker-compose Mosquitto 5.0 모드 + FastAPI 기반 WS 게이트웨이(구독 릴레이 + 명령 발행 프록시).
- **Mosquitto 역할**: 데이터 평면의 중앙 우체통(라우터). QoS(전달 보장) / retained(늦은 접속자 현재 상태) / LWT(끊김 감지) / WS 리스너(브라우저) / MQTT5 User Property(traceparent).

### ② 어댑터 / 브리지
**결정**:
- **어댑터 계약 = 백엔드(대규) 소유.**
- **제어·상태·명령·ack·이벤트 = MQTT** (Redis 대체). **이미지/프레임 스트리밍만 기존 Redis Stream 유지.**
- **로봇 어댑터(raspi)가 로봇 SDK를 직접 MQTT 계약으로 번역** (robot-image의 Redis 계약을 대체 — 성역 아님). Redis↔MQTT 중간 번역기 불필요.
- **MQTT → OTel 관측 브리지 = 백엔드 구현.** "MQTT에 사는 것"(센서값·명령)은 생산자가 MQTT에만 발행 → 브리지가 표준 OTel 시그널로 미러. 단, **생산자 자신의 성능 지표**(ai.*, viz.*)나 순수 관측값은 생산자가 **OTel 직접** 발행(브리지 대상 아님).
- **근거**:
  - 어댑터=백엔드 소유: 로봇·센서가 제각각인데 이종성을 표준으로 흡수하는 지점이 어댑터라, **받는 쪽이 계약을 쥐어야** 도메인·기기가 바뀌어도 표준 봉투가 안 흔들림.
  - 제어 Redis→MQTT: Redis는 로컬 IPC·이종 로봇 추상화 목적이었으나, 원격·다구역·핸드오버·관측/감사·팀 계약 일원화 요구에는 브로커 모델(QoS·retained·LWT·traceparent)이 맞음.
  - 브리지=백엔드: 생산자마다 이중 발행시키면 규격이 제각각 → 브리지가 일괄 미러로 관측 규격 통일 + 생산자 부담↓(현우 이중 발행 우려 해소).
- **어댑터의 3역할 구분**: (1) 기기→표준(왼쪽=병현 정의, 오른쪽=백엔드 정의) (2) MQTT→OTel 관측 브리지(백엔드) (3) 로봇 Redis↔MQTT(→ 로봇 어댑터가 SDK 직접 번역으로 흡수, 별도 번역기 소멸).

### ③ 식별자·계층 일원화 (= B) + 좌표계(H-2)
**식별자 (현우 v2 스킴 채택)**:
- **Entity** = 병현 `agent_id`(로봇) / `sensor_id`(센서). **개명·신설 없이 그대로 Entity ID로 사용.**
- **Node** = 병현 `site_id`. 센서엔 이미 있고 **로봇에 추가**. Node = **로컬 좌표계의 원점**.
- **Zone** = **선택**. 없으면 domain이 최상위(캡스톤은 domain 대체). 계약엔 남겨둠(하천 다지점 대비, 비용 비대칭).
- **토픽** = 병현 `{domain}/{type}/{id}/{channel}` **유지**(4세그먼트 강제 안 함). Node는 토픽이 아니라 **라벨**로.
- **서비스 정체성(축 B)** = OTel `service.*`, 나영이 부여, **라벨로 entity 연결**(병합 아님).

**Node ≠ Entity 정정**: Entity = 로봇/센서/탐지객체. Node = 그 개체가 속한 **구역/관측소**(좌표 원점). 로봇은 Entity로서 자기가 속한 Node의 프레임을 씀. 핸드오버 시 frame이 바뀜.

**좌표계 (H-2)**:
- **프레임 구조 = 로컬 + 레지스트리(하이브리드).** 각 개체는 자기 Node 로컬 프레임으로 보고. 백엔드 레지스트리가 **"Node 원점의 글로벌 배치(원점+회전)"** 보유. 소비자가 로컬 → 글로벌 변환. (레지스트리 = MK2 "구역 맵 ID ↔ 중계노드" 매핑 = 나영 calibration 레지스트리, 백엔드 것.)
- **축 규약 = Unity 규약.** **y = 위(높이), 바닥 = x-z 평면, 1 unit = 1 m, 3D 유지·지상 로봇 y=0.** (최종 렌더가 Unity 트윈이라 변환 지점을 하나로 모음. ROS(z-up) 로봇은 어댑터가 경계에서 변환.)
- **위치 출처 태그.** position에 **`frame_id`**(어느 Node/글로벌) + **`source`**(odometry | perception | anchor_corrected)를 실음.
  - (a) odometry = 로봇 자기 추정(드리프트). (b) perception `position_world` = AI가 인지한 **장애물** 위치. (c) anchor_corrected = 360 앵커로 (a)를 보정한 값.
  - **Node 원점 ≠ 앵커 위치.** 원점 = 그 구역 좌표계의 (0,0,0)이라는 약속된 기준점(레지스트리가 글로벌 배치 보유). 앵커 = 그 좌표계 안의 **알려진 참조점**으로, **로봇 보정(source=anchor_corrected)에 사용**. → **정합엔 노드 원점(레지스트리), 보정엔 앵커.**
- **계약 수준까지만**: position이 `frame_id`+`source`를 갖는다까지. **실제 앵커 보정 알고리즘 = 대규 디지털트윈 구성 AI 파트의 구현**(여기서 설계 안 함). (논문의 검증된 메커니즘을 캡스톤에 재사용 — 논문 → 캡스톤 방향.)

### ④ 시그널 네이밍 · 라벨 · enum · 상관키

**④-1. Resource / 라벨 규약** (현우 I-2 답)
- OTel엔 식별자 자리가 둘: **Resource 속성**(생산자 설명, 생산자당 한 번, 모든 시그널에 자동) / **메트릭 라벨**(측정 하나 설명, 포인트마다 가변).
- **핵심 규칙**: **물리 생산자(로봇/센서)는 `entity`가 Resource**(자기가 곧 그 개체). **소프트웨어 생산자(AI/가시화)는 `entity`가 라벨**(남을 처리).
- 생산자 정체성(Resource): `service.namespace`=domain, `service.name`=컴포넌트(robot-adapter|sensor-gateway|ai-perception|ai-decision|viz|…), `service.instance.id`=컨테이너/기기 ID(나영이 k3s로 부여), `deployment.environment`=dev|prod. 물리 생산자는 여기에 `zone`·`node`·`entity`·`entity_type`도 Resource로.
- **최소 라벨셋 = domain + entity + node.** → 현우 I-2(메트릭↔화면 컴포넌트 판정) + 나영 서비스↔entity 연결 동시 해결.
- **카디널리티 안전**: 무한값(타임스탬프·요청ID)은 **라벨 금지 → log/trace로.** 브리지가 라벨 규약 강제(생산자 실수 교정).

**④-2. 메트릭 네이밍 · 단위**
- 네임스페이스(도장): AI `ai.*`, 로봇 `robot.*`, 센서 `sensor.*`, 가시화 `viz.*`, 점 표기, `domain` 공통 라벨.
- **단위는 이름에 붙이지 않고 OTel `unit` 필드에만** (OTel semconv 권고). 기존 `*_ms`, `*.m`, `*.pct` 접미는 이름에서 떼고 unit으로. (Prometheus 자동 단위 접미는 exporter가 처리 — 우리가 손대지 않음.)

**④-3. enum 어휘집** (상태 3층 = 하천 검증 구조)
- `device.status`: `ok | degraded | fault` (기기 자기보고)
- `availability`: `online | stale | offline` (서버 파생: 마지막 수신 시각 + 임계)
- `deployment`: `deployed | not_deployed` (오케스트레이터 파생)
- `command.result`: `accepted | rejected | timeout | failed | completed` (병현·현우 통합. accepted→completed는 state 확정)
- `origin.path`: `manual | voice | llm_suggestion | auto`
- **도메인 상태**(levee open/closed 등) = **표준 아님, 도메인별 어휘집에 등록.** (커먼 아키텍처: 표준은 도메인 무관만.)
- **`stale` 정의**: 연결(LWT)은 살아 있으나 **마지막 수신 후 임계 시간 이상 새 값이 안 옴** = "값이 오래됨" 경고. online/offline 사이. 병현 `sensor.reading.staleness_s`가 판정 재료. (임계값 = SLA, Tier 2.)
- 현우 4가지 화면 표시(정상/장애/의도적 미배포/판단 불가)는 이 3층 조합에서 파생. "판단 불가" = `availability:stale`로 흡수.

**④-4. 상관키 (traceparent)**
- 표준 = **W3C Trace Context (`traceparent`)**. 데이터 평면 메시지가 **MQTT5 User Property**로 실음(+ 페이로드 JSON 필드로 백업).
- **span 생성 주체 = 백엔드 audit-writer.** 디바이스·가시화는 traceparent를 **생성하거나 복사만.**
- **두 상관키 병용**: `traceparent`(trace_id) = **인과 사슬**(전 여정), `command_id` = **명령↔결과 짝짓기**(업무 키). 감사 로그엔 둘 다.
- **주기 상태(state)엔 trace 미부착** (상태는 흐름 → metric으로; 명령·이벤트만 trace).

### ⑤ 감사 · 책임소재 척추
- **저장**: 감사 = **OTel Log Record**로 받아 **MySQL 감사 테이블**에 영구 저장 (Loki=운영 로그와 분리, 장기 보존). audit-writer가 적재.
- **조회**: 백엔드 **FastAPI에 감사 조회 엔드포인트** 추가 (예: `GET /audit/commands?target&from&to&actor&origin&page`, `GET /audit/entity/{entity}/last-command`). 내부: 필터 → 권한 확인 → 안전 SQL(파라미터 바인딩) → 페이징 반환. **브라우저가 DB 직접 접근 금지**(자격증명·부하·권한 통제). 계약 수준까지만(엔드포인트 상세는 Tier 2).
- **범위**: 제어 명령 + 명령 결과(command.result) + LLM/음성 관여(origin.path, llm.userModified) + **O-5(그래프 구성 반영)**.
  - **O-5 처리 방침**: **개념엔 포함, 구현은 보류.** 감사 = "actor가 시스템 상태·판단에 영향을 주는 행위"로 넓게 정의하고 `event.name`을 **개방형**으로 둠(`control.command.issued` 옆에 `viz.config.applied` 등을 나중에 추가). 지금 시연(대성고)엔 노드 편집기·구성 변경이 없어 이벤트가 안 생김 → **event.name 자리만 열고 실제 발행은 노드 편집기 나올 때.** ("지금 필드 하나 남기는 비용 << 나중에 계층 끼우는 비용".)
- 근거: 감사는 오래·정확히·구조화 질의(법적 추적성)라 저장을 분리. 조회 API 없으면 "마지막 조작자"를 화면에 못 띄움.

### ⑥ 질의 · 소비 인터페이스 (현우 수신부)
- **⑥-1 Prometheus 질의 = 백엔드 질의 프록시 경유** (감사 API 형제 엔드포인트, 예 `POST /query` → 내부에서 Prometheus HTTP API 호출). instant/range 지원. 세부(타임아웃·rate limit·보존·해상도) = Tier 2. 근거: ①②⑤와 같은 일관성(자격증명 은닉·쿼리 통제·권한).
- **⑥-2 Grafana = 내부 모니터링 전용 / 자체 프론트 = 대외 최종 화면.** Grafana를 관리자 최종 화면으로 쓰면 교수님이 지적한 "개발자 도구를 관리 기능인 척"이 됨 → 내부는 Grafana(빠른 관측·개발), 대외·시연은 자체 웹·TypeScript 프론트(동적/LLM 가시화·추상화 레벨). **자체 프론트에 Grafana 패널 임베드 가능성은 열어둠**(현우 판단 존중).
- **⑥-3 권한 = 백엔드 RBAC.** 역할 골격 `viewer`(열람) / `operator`(열람+제어) / `admin`(+구성). **화면단 사전 차단은 UX(우회 가능), 실제 강제는 백엔드**(명령·조회 API가 토큰 역할 확인). 흐름: 로그인 → 역할 담긴 단명 토큰 → 프론트 사전 차단 → 명령이 토큰과 함께 백엔드로(H-5, 가시화 명령=백엔드 경유) → 백엔드가 토큰 검증 + 권한 확인 → 허용/거부 → ⑤ 감사 기록. 구현 3덩어리: MySQL 역할 테이블 + 토큰 발급·검증(미들웨어) + 권한 게이트(엔드포인트 입구) + 현재 역할 조회 API(`GET /me/role`). 세부 권한 매트릭스 = Tier 2. 근거: 책임소재(⑤)와 짝 — 권한(자격 있었나) + 감사(실제 했나)가 합쳐져야 공공 서비스 책임 성립. 권한은 화면이 아니라 백엔드에서 강제해야 우회 불가.

---

## §5. 팀원별 전달 사항

### 병현 (하드웨어)
- **로봇에 `site_id`(Node) 라벨 추가** (센서엔 이미 있음).
- **`position {x,z}` → `{x,y,z}`**(지상 y=0) **+ `frame_id` + `source`** 추가.
- 토픽 `{domain}/{type}/{id}/{channel}` **유지**.
- 메트릭 **단위 이름 접미 제거 → `unit` 필드로** (`robot.position.x` 등은 이미 unit 필드 사용, `*.m`/`*.pct`/`*_ms` 정리).
- (기억만, 나중 논의: 미션 수준 명령·비상 정지 표현 / 어댑터 왼쪽(기기 날것 포맷)·게이트웨이 경계(아두이노·PLC) / 새 센서 종류 추가 방법(고정 스키마 vs 제네릭).)

### 나영 (AI / MLOps)
- **`waypoints {x,y}` → `{x,z}`**, `position_world`는 **y=위(Unity)** 로 선언 **+ `frame_id`**.
- **entity(agent_id)는 데이터 정체성 = trace용**(AI 라우팅은 capability로). entity를 정의·라우팅하지 않음.
- **MLOps/서비스 정체성**은 `service.name`/`service.instance.id`로 **본인이 부여**, entity는 **라벨로 연결**(병합 아님). 표준 = §4-1.
- 현우 요청 반영: detection 출력에 **`frame_ref` 되돌리기**(A-1), **bbox 좌표계 명시**(A-2: 픽셀 절대/0~1, 원점 좌상/좌하, 기준 해상도), **출력 추상화 수준 표기**(A-3: 원본 vs 집약), **`reason` 구조화**(A-4: 사람 문자열 → 기여 항목 구조, 설명가능성).
- (확인 1건: `service.instance.id` 부여 방식 — k3s 파드/컨테이너 ID를 어떻게 매길지.)

### 현우 (가시화)
- **v2 스킴 채택됨** (Entity/Node/Zone, 공간축+추상화축).
- 감사 = **OTel Log Record로 발행**, **감사 조회 API 제공됨**(target·기간·actor·origin.path 필터).
- 브라우저 실시간 = **WS 게이트웨이**, Prometheus 질의 = **프록시**.
- 권한 = **백엔드 RBAC 제공**, **현재 역할 조회 API 제공**(`GET /me/role`).
- **Grafana 임베드 가능성 열림** (자체 프론트가 필요시 패널 재사용).
- O-5(구성 반영 감사)는 **개념 채택, 발행은 노드 편집기 구현 시**.

---

## §6. 미결 / Tier 2 항목 (지금은 방향만, 나중 구체화)
- SLA · 발행 주기 · 버퍼링 · keepalive · 오프라인/`stale` 판정 임계
- `frame_ref` 키 규칙 · 보존 기간
- calibration(프레임/좌표) 레지스트리 상세 스키마
- 영상 스트림 규격 및 RTSP → WebRTC/HLS 변환 지점 (현우 H-4)
- 감사 로그 보존 기간, 권한 매트릭스(어느 액션이 어느 역할 이상) 상세
- 페이로드 스키마 레지스트리 · 버전 관리
- 앵커 보정 알고리즘 (대규 디지털트윈 AI 구현)
- 좌표계 원점 구체 배치(앵커에 둘지 등 — 구현 선택)
- 명령 확정 판단 기준: 액션별 "ack까지 확정" vs "state 확인까지 확정" 목록 (현우 H-6)
- MongoDB 존치/제거 여부

---
---

# §7. 회의용 정리 (내일 대비)

> 아래는 위 종합을 회의 한 장으로 압축한 것. 교수님 지시("가져올 수 있는 걸 가져와라, 이렇게 하기로 했다까지는") 대응.

## 7.1 회의 대비 — PM으로서 가져갈 것

**(1) 진행 방식(PM 프로세스)**
대규가 관측 표준 v0 확정 → 엑셀 시트/정의서로 각 파트 in/out 취합 → 대규 일관성 리뷰 → AI로 파생 요구사항 생성 → 전체 리뷰. (요구사항 정의서는 이미 3파트 회수 완료.)

**(2) 프레이밍 — 관측 스택 인터페이스 계약 = 3면**
"정상 시 텔레메트리 계약"만이 아니라 세 종류: ① 생산 텔레메트리(나영·병현) ② 질의·소비(현우 수신) ③ 명령·감사 척추(현우 생산 + 책임소재). 산출물 = 3파트 × 제공/수신 매트릭스.

**(3) 자발적 수렴 스토리 (근거로 제시)**
세 팀원이 독립적으로 **2평면 분리**와 **도메인 무관 추상화**(나영 '에이전트', 병현 `{domain}` 네임스페이스, 현우 Entity/Node/Zone)에 도달. 병현은 "디바이스 span 안 만들고 백엔드가"까지 우리 방식과 일치. → **팀이 이미 커먼 아키텍처 위에 정렬됨.**

**(4) "이렇게 하기로 했다" = 여섯 결정** (아래 7.2)

## 7.2 여섯 결정 한 장

| # | 항목 | 결정 (한 줄) |
|---|---|---|
| Tier0 | 기반 | 2평면(MQTT 데이터/OTel 관측). 저장: metric→Prometheus·log→Loki·trace→Tempo·감사→MySQL·frame→Redis(MongoDB 재검토). 트레이스 필수. 로봇 미계측→백엔드 audit-writer. |
| ① | 브로커·실시간 경로 | **Mosquitto + MQTT 5.0 + TLS + 브라우저는 백엔드 WS 게이트웨이 경유 + 단명 토큰.** |
| ② | 어댑터·브리지 | **어댑터 계약=백엔드 소유. 제어=MQTT(Redis 대체, 이미지만 Redis). 로봇 어댑터가 SDK→MQTT 번역. MQTT→OTel 브리지=백엔드.** |
| ③ | 식별자·좌표(=B) | **Entity=agent_id/sensor_id, Node=site_id(로봇 추가), Zone=선택/domain, 토픽=병현 유지. 좌표=로컬+레지스트리·Unity 규약(y=위,바닥x-z,3D)·position에 frame_id+source.** |
| ④ | 네이밍·enum·상관키 | **물리 생산자=entity가 Resource·SW 생산자=라벨. 단위=unit 필드. 상태 3층(device.status/availability(+stale)/deployment)·command.result 5값·origin.path. traceparent(MQTT5 UserProperty)+command_id, span=백엔드.** |
| ⑤ | 감사 척추 | **OTel Log Record→MySQL 감사 테이블. 백엔드 감사 조회 API. 범위=명령+결과+LLM/음성 + O-5(개념 포함·구현 보류).** |
| ⑥ | 질의·소비 | **Prometheus 질의=백엔드 프록시. Grafana=내부/자체 프론트=대외(임베드 열어둠). RBAC(viewer/operator/admin, 백엔드 강제).** |

## 7.3 회의 기록용 1줄 근거 (모음)

- **Mosquitto**: 이미 가동·검증됐고 요구 기능(QoS·retained·LWT·MQTT5·WebSocket)을 전부 커버, 운영 부담 최소.
- **MQTT 5.0**: traceparent를 User Property로 실을 수 있고 ack 규약이 5.0이라야 깔끔(병현·현우 요건).
- **백엔드 WS 게이트웨이**: 브라우저에 브로커 자격증명을 내리지 않고 인증·필터·rate limit을 백엔드에서 중앙 관리.
- **어댑터 계약=백엔드 소유**: 이종성(로봇·센서 제각각)을 표준으로 흡수하는 지점이라, 받는 쪽이 계약을 쥐어야 도메인·기기가 바뀌어도 표준 봉투가 안 흔들림.
- **제어 Redis→MQTT**: Redis는 로컬 IPC·이종 추상화 목적이었으나 원격·다구역·핸드오버·관측/감사·계약 일원화엔 브로커 모델(QoS·retained·LWT·traceparent)이 맞음. 이미지 스트리밍만 Redis 유지.
- **MQTT→OTel 브리지=백엔드**: 생산자마다 이중 발행시키면 규격 제각각 → 브리지가 일괄 미러로 관측 규격 통일·생산자 부담↓.
- **식별자 통일(현우 v2 채택)**: agent_id/sensor_id를 Entity로 재사용·site_id를 Node로 → 새 체계 없이 병현 변경 최소(로봇에 라벨 하나 추가).
- **좌표=로컬+레지스트리·Unity**: GPS 없이 단일 글로벌 원점 불가 + 최종 렌더가 Unity라, 각 개체는 Node 로컬로 보고하고 레지스트리가 글로벌 배치를 잇는 게 정직(핸드오버·앵커에 정합).
- **단위=unit 필드**: OTel semconv 권고. 단위 변경에 이름·쿼리가 안 깨지고 브리지가 파싱 없이 처리.
- **상태 3층 분리**: 하나로 뭉치면 "연결됐는데 기기 fault"를 표현 못 하고 원천(기기/서버/오케스트레이터)이 섞여 책임소재가 흐려짐. 층을 나눠 각 원천이 채우고 화면이 조합.
- **traceparent+command_id 병용**: trace_id=인과 사슬, command_id=명령↔결과 짝짓기. 역할이 달라 둘 다 필요.
- **감사=Log Record→MySQL 분리**: 감사는 오래·정확·구조화 질의(법적 추적성)라 일반 로그(Loki)와 저장 분리.
- **감사 조회 API 필수**: 없으면 "마지막 조작자"를 운영 화면에 못 띄움. 브라우저 DB 직접 접근은 자격증명·부하·권한 위험.
- **O-5 개념 포함·구현 보류**: 감사를 "제어 명령"으로 좁히면 확장 때 재작업 → "actor의 유의미한 행위"로 넓히되 지금 없는 기능은 event.name 자리만 열고 미룸.
- **Grafana 내부/자체 프론트 대외**: "개발자 도구 ≠ 관리 기능"(교수님) + 기존 관측 자산 활용의 절충.
- **RBAC 백엔드 강제**: 화면 차단은 우회 가능 → 실제 방어선은 백엔드. 권한(자격) + 감사(실제 행위)가 합쳐져야 공공 서비스 책임 성립.

## 7.4 회의 안건 · 미결 (교수님과 정할 것 / 팀에 던질 것)
- **3자 정합 확인**: 식별자(Entity/Node/Zone) + 좌표(Unity y-up, waypoints x-z) 를 병현·나영에게 "이 규약을 따르라"로 확정 전달.
- **Tier 2 우선순위**: SLA·frame_ref·스트림 변환 지점·권한 매트릭스·감사 보존기간을 언제 채울지.
- **나영 확인 1건**: service.instance.id 부여 방식.
- **MongoDB 존치 여부** 결정.
- **다음 rotation PM 이관** 시점·방식(교수님이 "돌아가며 PM"이라 함).

---

*본 문서는 260819 시점 결정 기록이며, Tier 2 항목은 실측·운영·팀 협의 후 확정한다.*
