# 백엔드 회신 — 캐시 범위 + 전송 아키텍처 대조

> 대상: 이대규
> 근거: `BE-T-06` [회의 확정 대기] · `data_transport_architecture_v2.docx`
> 이 문서는 **A절을 그대로 보내고**, 물어보면 B~D절을 보여주는 용도입니다.

---

# A. 그대로 보낼 메시지

전송 아키텍처 문서 봤어. 전송 도구 결정(MQTT/Kafka/OTLP)이랑 페더레이션 15초는 내 쪽 전제랑 다 맞아서 다시 계산할 게 없었어. 명령 경로 A안 확정도 내 요구사항 그대로 성립해.

**확인할 게 셋 있고, `BE-T-06` 캐시 범위 답변도 같이 보낼게.** 넷 다 같은 얘기라 한 번에 읽는 게 나을 것 같아.

---

**1. §7 시각화 종점에 실시간 구독 경로가 빠진 것 같아**

표 7이 전부 "저장소를 조회해서 그린다"로 되어 있고, 본문도 *"시각화의 소스는 항상 조회 가능한 저장소"* 라고 못 박았더라. 그런데 내 화면의 뼈대는 **WS 게이트웨이(`BE-T-03`) 구독 푸시**야.

**로봇 상태 50ms를 TSDB 조회로는 못 그려.** 초당 20번 폴링은 저장소를 두드리는 짓이고, 주기를 늦추면 트윈이 끊겨. 내 `VZ-I-01`이 "데이터는 전량 받되 화면 반영만 100ms로 묶는다"인데 이건 **받는 게 푸시일 때만** 성립해.

시트에는 `BE-T-03`·`BE-T-06`이 멀쩡히 있으니까 **이 문서가 전송 도구 관점이라 클라이언트 채널이 빠진 걸로** 보여. 근데 문서에 안 적히면 나중에 "화면은 TSDB 읽으면 되잖아"가 나올 수 있어서, 한 줄만 넣어주면 좋겠어.

> "실시간 상태·명령 결과·계획 진행은 저장소 조회가 아니라 WS 게이트웨이(`BE-T-03`) 구독으로 전달한다. 저장소 조회는 이력·지표·감사용이다."

---

**2. `BE-T-06` 캐시 범위 — 필드가 아니라 채널 단위로 갈라야 해**

"상태 전체(3층+`last_seen`)" 기준으로 물어봤는데, 필드 단위가 아니라 **채널마다 판단이 갈려.** 그리고 **전 채널을 다 캐시하면 오히려 화면이 거짓말을 해.**

**캐시해서 구독 즉시 내려줄 것 (7개)**
`state`(3층+`last_seen`+`stale_threshold_ms`) · `telemetry`(위치·배터리) · `actuator_state` · `control_lock` · `plan`(승인 대기) · `plan_progress` · `video_meta`

이유는 하나야 — **이 값들이 없으면 화면이 "모른다"를 그릴 수밖에 없는데 실제로는 서버가 알고 있어.** 센서는 평시 1분 주기라 캐시 없으면 재접속 후 최대 1분간 빈 화면이야.

**절대 캐시하면 안 되는 것 (3개)**

- `command_result` — 재접속하면 **지난 명령 결과가 방금 온 것처럼** 뜬다
- `video_frame` — 옛 프레임 한 장이 **정지 화면인데 현재처럼** 보인다
- `detections` — `frame_ref`가 지금 화면에 없는 프레임을 가리켜 **박스가 엉뚱한 데** 뜬다

**안 줘도 되는 것 (2개)**
`heartbeat`(서버가 이미 `availability`로 판정해 `state`에 실어주니 중복) · `metrics`(질의 프록시로 따로 조회)

**그리고 캐시된 값의 `ts`는 원래 발행 시각 그대로 줘.** 푸시 시점으로 다시 찍으면 `stale` 판정이 리셋돼서 오래된 값이 새 값처럼 보여. 화면은 `ts`만 보고 판단하니까 "캐시에서 왔다"는 별도 표시는 필요 없어.

> 참고 — 내 목 게이트웨이는 지금 전 채널을 캐시하고 있어서 위 3개가 그대로 재생되는 상태야. 내 쪽에서 고칠게.

---

**3. `availability`가 어느 평면 판정이야?**

§6-5에서 장치 생사를 넷으로 두면서, 업무 평면(MQTT 세션)과 관측 평면(Prometheus `up`)이 **서로 다른 각도로 본다**고 했잖아. 이중으로 두는 건 맞다고 봐.

근데 내 화면은 `availability` **하나**를 받아서 상태 4종(정상/장애/미배포/판단 불가)을 파생시켜. 둘이 갈릴 수 있어 —

| 상황 | 업무 평면 | 관측 평면 |
|---|---|---|
| MQTT 세션은 살아 있는데 지표 발신이 죽음 | online | `up=0` |
| 지표는 나오는데 MQTT 연결이 끊김 | offline (LWT) | `up=1` |

내 생각엔 **업무 평면 우선**이 맞을 것 같아. 관제사가 알아야 하는 건 "이 장치에 명령을 보낼 수 있나"고, 지표가 안 나오는 건 그다음 문제니까. 다만 **"명령은 가는데 지표가 없다"도 화면에 보여야** 해서 그건 따로 표기가 필요할 수도 있어.

`availability`가 어느 평면 판정인지, 갈릴 때 뭐가 우선인지 정해줘.

---

**4. 표 7 관측 지표 행에 관제 화면이 없어**

Grafana만 있는데, 내 `VZ-I-04`가 질의 프록시(`BE-Q-01`)로 지표를 받아 그리고 있고 `VZ-O-04`로 내 성능 지표를 발행도 해. **내가 발행한 걸 내가 못 보는** 모양이 됐어. 한 줄 추가해주면 좋겠어.

---

그리고 영상은 §5-9에서 "Redis 등 미디어 경로"라고 구현체가 나왔던데, §7 표에는 영상이 아예 없더라. **미디어 경로에서 뷰어로 나가는 분기**가 여전히 비어 있어 — 이건 3자 회의 안건으로 남겨둘게.

---

# B. 근거 — 캐시 범위 채널별 판단

| 채널 | 캐시 | 왜 |
|---|---|---|
| `state` | **필요** | 3층 + `last_seen` + `stale_threshold_ms`. 상태 4종이 전부 여기서 파생 |
| `telemetry` | **필요** | 카드의 값과 **트윈 초기 배치**. 위치를 모르면 로봇을 어디 놓을지 알 수 없다 |
| `actuator_state` | **필요** | 액추에이터는 표준 3층과 별개인 **자기 어휘**(대기·동작 중·완료·오류). 현재 상태 없이 버튼 판단 불가 |
| `control_lock` | **필요** | 잠금을 모르면 **안 눌리는 버튼을 계속 누른다**. `VZ-O-05`의 존재 이유 |
| `plan` | **필요** | 승인 대기 계획이 재접속으로 사라지면 **승인이 유실**된다 |
| `plan_progress` | **필요** | 진행 중 임무가 어느 구간까지 갔는지 |
| `video_meta` | **필요(경량)** | 패널 열 때 fps·해상도 규격 판단. 픽셀이 아니라 메타만 |
| `heartbeat` | 불필요 | `availability` 판정 근거인데 그 판정을 서버가 이미 해서 `state`에 실어준다 |
| `metrics` | 불필요 | 질의 프록시(`BE-Q-01`)로 조회하는 별도 경로가 있다 |
| `command_result` | **금지** | 지난 결과가 새 결과로 보인다. 마지막 조작 이력은 감사 조회(`VZ-I-05`)로 따로 받는다 |
| `video_frame` | **금지** | 정지 화면이 현재 영상처럼 보인다 |
| `detections` | **금지** | `frame_ref` 정합이 깨져 박스가 엉뚱한 위치에. `VZ-I-07`이 막으려는 문제 그 자체 |

## `ts`를 다시 찍으면 안 되는 이유

화면은 `stale` 판정을 **직접 하지 않습니다**(서버 판정을 따르는 것이 계약). 다만 값이 얼마나 묵었는지는 `ts`로 읽습니다.

캐시 푸시 시점으로 `ts`를 다시 찍으면 **1분 전 값이 방금 값으로 보이고**, 직후 서버가 `stale` 판정을 내리면 "방금 왔는데 판단 불가"라는 앞뒤 안 맞는 상태가 됩니다.

원래 발행 시각을 그대로 주면 화면이 스스로 판단할 필요 없이 정확히 그려집니다. **"캐시에서 왔음" 표시는 요청하지 않습니다** — `ts`로 충분하고, 필드를 하나라도 덜 만드는 쪽이 낫습니다.

---

# C. 근거 — 전송 아키텍처 문서 대조

## C-1. 확인된 것 — 내 근거가 단단해졌다

| 항목 | 문서 | 내 영향 |
|---|---|---|
| **명령 경로 확정** (§6-6) | 서버→엣지 Kafka, 엣지→말단 MQTT (A안) | `VZ-O-01`·`O-02` 전제 유효. 추상 action을 넘기면 백엔드가 번역해 내리는 구조 확인 |
| **상관키 발급 시점** (§5-6) | *"백엔드가 명령을 만들 때 발급해 MySQL에 기록"* | 두 키 구조(`client_request_id` → `command_id`)와 일치 |
| **페더레이션 15초·요약만** (§6-4) | 원본은 엣지, 백엔드는 구역 요약. 부하가 구역 수 비례 | `VZ-I-04`·`VZ-C-03` 근거 그대로. **재계산 없음** |
| **요약하지 않는 것** (§6-3·6-5) | log·trace 원본 전달, 장치 생사·치명 오류는 개별 전송 | `VZ-I-10`(AI 실패를 지표 경로가 아닌 별도 푸시)과 같은 방향 |

## C-2. 영상 — 조각은 늘었고 종점은 여전히 없다

| 구간 | 상태 |
|---|---|
| 카메라 → 엣지 | `HW-R-07`·`HW-S-06` — 있음 |
| 엣지 → 미디어 경로 | **Redis 등** (§5-9, 신규) |
| 원본 규격 흡수 | `AI-C-08` GStreamer 어댑터 — 경로가 **AI 모듈까지** |
| 미디어가 별도 평면 | `AI-C-14` — 있음 |
| **미디어 경로 → 뷰어** | **없음** |

§7 시각화 종점 표에 업무·관측·로그·trace·감사 다섯 줄이 있는데 **영상만 빠져 있습니다.**

## C-3. 영향 재검토 — 명령 만료 시각

문서가 Kafka 지연을 **10~50ms**로 밝히고 *"발신에서 디스크 기록·복제, 수신까지의 전체 파이프라인 시간"* 이라고 설명합니다. 여기에 브릿지 전환이 한 겹 더 붙고, 왕복이니 두 번 겪습니다.

**내 목 서버는 ACK를 즉시 줍니다.** 실제 경로에서는 100ms 안팎이 정상일 수 있고, 그러면 `expires_at` 기본값과 "ACK 없이 만료" 임계가 너무 빡빡할 수 있습니다.

→ 지금 고칠 것은 아니지만, **목 서버에 왕복 지연 주입 옵션**을 넣어 두면 실제 백엔드 붙이기 전에 확인됩니다. `ack-late` 시나리오 확장으로 됩니다.

## C-4. 이 문서로 해소된 안건 — 없음

이 문서는 **전송 도구**를 정한 것이고, 내 미결은 대부분 **필드·표기·경계**라 층이 다릅니다.

**여전히 남은 것** — ACK 요청 식별자 에코 · 원천 종류 표기 · 손잡이 규약 · 자세(orientation) · 레지스트리 변경 통지 · 설계 규모 가정 · 미디어 경로의 뷰어 분기

---

# D. 내 쪽 후속 작업

## D-1. 목 게이트웨이 캐시 버그

`mock-gateway/hub.ts`의 `publish()`가 **채널 구분 없이 전부 캐시**합니다.

```
this.cache.set(key, env);   // 227행 — video_frame·detections·command_result도 여기 걸린다
```

재접속하면 금지 채널 셋이 그대로 재생됩니다. **지금까지 못 본 이유는 재접속 검증을 현황판(`state`)에서만 했기 때문**입니다. 영상 탭을 연 채로 서버를 재시작하면 바로 드러납니다.

→ 캐시 대상을 화이트리스트로 바꾸고, **재접속 시 금지 채널이 안 오는지 확인하는 시나리오**를 추가합니다.

## D-2. `ack-late` 시나리오에 왕복 지연 주입

C-3의 확인 수단.
