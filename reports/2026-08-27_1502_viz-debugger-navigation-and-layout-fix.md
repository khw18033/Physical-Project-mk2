# 가시화 디버거 탐색·노드 배치 수정 보고

**작업일**: 2026-08-27
**기준 커밋**: `c79bdaf`

## 수정 내용

1. D-01~D-05 직접 전환 탭을 제거했다.
   - 임무 제목 클릭 → 마일스톤 복귀
   - 마일스톤 클릭 → 태스크 그래프
   - 일반 노드 더블클릭 → 액션 아이템 상세
   - 상단 임무 이력 클릭 → 리플레이
   - 리플레이에서 실패 노드 더블클릭 → 실패 수정
2. 노드가 왼쪽 위에 겹치던 문제를 수정했다.
   - 원인: 레이아웃의 `{x, y}`를 HTML 요소의 style에 그대로 넘겨 브라우저가 위치 속성으로 해석하지 못했다.
   - 수정: 계산 결과를 CSS의 `{left, top}`으로 명시적으로 변환한다.
3. 같은 회귀를 막기 위해 `verify:layout`을 추가했다.
   - DAG와 트리 각각 7개 노드 좌표가 모두 다른지 확인한다.
   - 계산 좌표가 카드의 `left/top`에 연결돼 있는지 확인한다.

## 검증

- `verify:layout`: 통과 — DAG/트리 각 7개 고유 좌표, `left/top` 적용 확인
- `verify:scenario`: 통과 — 15개 이벤트와 8상태 유지
- `typecheck`: 통과
- `build`: 통과

기존 데이터 계약, 게이트웨이, `web-dashboard` 파일은 수정하지 않았다.
