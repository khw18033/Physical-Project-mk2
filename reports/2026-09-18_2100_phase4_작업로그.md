# Phase 4 작업로그 — 미디어 경로 (2026-09-18 착수)

> 성격: **단계별 사실 기록**(최종 보고서 `reports/…_phase4_미디어경로.md`의 조립 재료). 형식은 사용자 지시
> (2026-09-19 「작업로그 프롬프트」)를 따른다 — 단계마다 헤딩 한 줄 + 해당하는 항목만(1 지시서와 다르게 한 것 /
> 2 발견한 것[증상·실측 원문·원인·조치] / 3 사용자가 결정해 준 것 / 4 남긴 것 / 5 검증하지 못한 것).
> 없는 항목은 줄을 쓰지 않고 헤딩 끝에 「그 밖 없음」. 앞 단계 기록은 고치지 않고 뒤에만 붙인다.
> 단계 0·1·2는 2026-09-19 소급 작성(대화·서버 출력 파일 기준 — 기억으로 채운 숫자 없음). 단계 3부터 즉시 작성.
> 비밀값·Tailscale 주소는 적지 않는다(Public 저장소). 실값은 `_serverinfo/`.

---

## 단계 0 — DoD 전건 통과(0-1·0-1b·0-1c·0-1d·0-2·0-3·0-4·0-4b·0-5·0-6 대조 완료)

1. 지시서와 다르게 한 것
   - 대조 블록에 **블록 11**(읽기 전용 — `docker network inspect capstone-db_default` 서브넷 + `SHOW GRANTS`)을 더했다. 완료 판정 72(브리지 대역 `172.18.0.0/16` 유지)의 착수 기준선을 잡기 위해서다. 결과 `172.18.0.0/16 172.18.0.1`, GRANT 9행(09-18 실측과 동일).

2. 발견한 것
   - **(a) `docker inspect <컨테이너> --format '{{index .RepoDigests 0}}'` 가 실패한다.**
     증상: 블록 1의 digest 조회 2건이 값 대신 오류.
     실측(원문): `template parsing error: template: :1:36: executing "" at <.RepoDigests>: map has no entry for key "RepoDigests"` ×2
     원인: `RepoDigests`는 컨테이너가 아니라 **이미지** 속성이다(지시서 블록 1의 명령 결함).
     조치: 단계 5-2 digest 읽기를 `docker image inspect $(docker inspect capstone_prometheus --format '{{.Image}}') --format '{{index .RepoDigests 0}}'` 로 한다. Collector digest는 `docker compose ps` 이미지 열에서 `@sha256:7087dcbb…` 확인.
   - **(b) Loki `/loki/api/v1/labels` 가 `start` 없이 빈 응답.**
     증상: 블록 5의 라벨 목록이 라벨 없이 옴.
     실측(원문): `{"status":"success"}` (data 없음). `start` 를 붙인 재조회(단계 2 결과 파일): `{"status":"success","data":["service_instance_id","service_name","service_namespace"]}`
     원인: 제약 31(기본 조회 창 6시간) — 지시서 블록 5의 그 한 줄이 `start` 없이 적혀 있었다.
     조치: 재조회로 **Loki 라벨 3종 기준선 확인**(0-1b). 이후 Loki 조회는 전부 `start`·`end` 부착.
   - **(c) Tempo 태그값 두 엔드포인트가 빈 배열.**
     실측(원문): `{"tagValues":[],"metrics":{}}` ×2 (v1·v2 둘 다)
     원인: 지시서 명령에 `start`/`end` 없음(추정 — 09-18 2차 실측은 `be-test-span-*` 4 + `hw-sensor-node` 를 봤다). 0-1b "Tempo에 우리 서비스가 없다"와 모순되지 않는다.
     조치: 단계 7-e 판정 때 `start`·`end` 를 붙여 조회. 착수 판정에는 영향 없음.
   - **(d) compose 백업 파일이 git 에 untracked 로 노출됐다.**
     증상: DoD 0-3 대조에서 `git status --short infra/` 출력에 백업이 보임.
     실측(원문): `?? infra/docker-compose.yml.bak_before_phase4_sync`
     원인: `.gitignore` 의 `*.bak` 은 `.bak_before_phase4_sync` 에 맞지 않고, `infra/docker-compose.yml` 패턴은 정확 일치다. 옛 작업본에는 평문 비밀번호가 있다.
     조치: `_serverinfo/260918_infra_docker-compose.yml.bak_before_phase4_sync` 로 이동(gitignore 확인: `.gitignore:4:_serverinfo/`).
   - **(e) DoD 0-6 — 서버·저장소 `mk2_mysql_schema.sql` 이 다르고, 저장소가 앞선 것이다.**
     실측(원문): 서버 `bce11cbcb0e151c7c43b0d0d7ad2f7a9  /home/dg/capstone-db/mk2_sql/mk2_mysql_schema.sql`(23,864B, 09-14 03:18) ↔ 저장소 `92C8ED1FB35CEFA2D60B46BE4267A259`(26,931B, 09-14 03:47). `git log -- infra/sql/mk2_mysql_schema.sql` = 커밋 1개(`8f1b08c`, 26,931B)뿐 — 23,864B 판본은 커밋된 적 없음.
     원인: 2026-09-14 정정(`mission_event.actor_kind NULL → NOT NULL`, 파일 282~286행 + `audit_log.actor_kind` 어휘 주석 232~238행)이 서버 사본 뒤에 저장소에만 반영됐다. 서버 DB에는 root ALTER 로 이미 적용(추적표 BE-S-08).
     조치: 단계 10 에서 ① `SHOW CREATE TABLE mk2.mission_event` 로 `actor_kind NOT NULL` 확인 ② 저장소 파일을 서버 `mk2_sql/` 에 덮어 복사(DDL 재실행 불필요 — `CREATE TABLE IF NOT EXISTS`) ③ 그 뒤 `mk2_media_capture.sql` → GRANT. 서버에만 있는 `registry-declared-vs-observed.sql`(5,184B)은 단계 10에서 md5 대조 1회.
   - **(f) 컴퓨터 venv(`..\venv_phase1`, Python 3.12)에 `websockets`·`confluent-kafka` 가 없다.**
     실측(원문): `ModuleNotFoundError: No module named 'websockets'` / pip list 에 둘 다 없음.
     원인: Phase 1·3 설치 목록에 없었다(지시서 7-2 는 `confluent-kafka` 만 언급).
     조치: 단계 2 에서 `websockets==17.1`(서버와 동일)·`confluent-kafka 2.15.1` 설치(단계 2 「남긴 것」).
   - **(g) 착수 시각차로 움직인 값** — 없음. 오프셋 200/140/127·telemetry 813 모두 09-18 실측과 동일(발행이 없었음). Loki·Tempo 컨테이너 `Up 3 days`(실측본 2 days) = 시각차.

3. 내가 결정해 준 것
   - 물음(내 지시): 서버 compose 를 `scp -P <포트> dg@<서버 주소>:… ` 로 내려받으라고 적었다. 답(사용자, 2026-09-18): *"앞으로 서버↔컴퓨터 파일 이동은 내가 직접 다운로드하고 손으로 옮길게 … 파일의 경로를 확실하게 말해 주면 이동은 내가 할게."* → 이후 전송 지시는 출발·도착 절대경로 표 + 확인 명령만 쓰고 접속 명령은 적지 않는다.

4. 이 단계가 서버·저장소에 남긴 것
   - 서버: 변경 없음(읽기 전용 블록 11개).
   - 저장소(gitignore): `infra/docker-compose.yml` = 서버본으로 덮음(md5 `2FA97CC75B123162B24E8108BEC02CBC`, CR 0). `_serverinfo/260918_phase4_단계0_착수대조.txt`(판정 기록) · `_serverinfo/260918_infra_docker-compose.yml.bak_before_phase4_sync`(옛 작업본 16A704A9…).
   - 사용자 보관: 서버 출력 원문 `260918 phase4 단계0.txt`.

5. 이번에 검증하지 못한 것
   - Tempo 태그값(위 (c)) — 조회 창 문제로 값을 못 봤다. 단계 7-e 로 이월.

---

## 단계 1 — DoD 전건 통과(1-1·1-1b·1-2·1-3·1-4)

1. 지시서와 다르게 한 것
   - **라벨 금지 8종 확장의 코드·테스트를 단계 5-3 이 아니라 여기서 README 와 함께 반영했다.** 지시서 5-3 이 "`FORBIDDEN_LABELS` + 테스트 + README 를 함께 고친다"고 묶어 둔 항목인데 README(1-4)만 먼저 고치면 "코드로 막는다"가 거짓인 커밋이 생긴다. 수집 수 증가(`test_observability_labels.py` 45 → 58 = parametrize +8, 신규 테스트 +5)는 이 시점에 발생. 9번째 「발화 원문」은 키 이름 대기(README 에 자리만).
   - 음성 fixture 를 지시서의 3 에 **1개 더했다**: `media-header-invalid-frame-ref-extra.json`(`frame_ref` 안쪽 추가 필드 거부 — README 「참조 규격 `additionalProperties:false` 예외」의 실측판).
   - `envelope.py` 에 지시서가 명시한 두 생성자 수정 외에 `contract_validator(filename)`(이름별 1회 생성 캐시 — 미디어 헤더 진입점)·`build_registry()`/`schema_registry()`·`reset_validators_for_tests()` 를 두었다.

2. 발견한 것
   - **(a) `.gitkeep` 을 실수로 지웠다.**
     실측(원문): `git status --short examples/` 에 ` D examples/.gitkeep`
     원인: fixture 작성 셸 명령에 `rm -f .gitkeep` 이 섞여 있었다(내 실수).
     조치: `git checkout -- contracts/common/examples/.gitkeep` 로 즉시 복원. 이후 status 에 없음.
   - **(b) 레지스트리에 없는 `$ref` 는 네트워크로 가지 않고 즉시 실패한다(결정 12 검증).**
     실측(원문): 등록 없이 검증 → 예외 타입 `_WrappedReferencingError`; 등록 후 `valid: True`, `+0900` → `"'2026-09-18T00:00:00+0900' is not a 'date-time'"`.
     조치: `tests/test_contract_media.py::test_unregistered_ref_fails_without_network` 로 못 박음.
   - (c) 수집 수 184 → 214(라벨 +13, `test_contract_media.py` 17). 컴퓨터 단위 파일 169 passed / 13 skipped(전부 `psycopg 미설치` — TSDB).

4. 이 단계가 서버·저장소에 남긴 것
   - 신설: `contracts/common/media-header.schema.json` · `contracts/common/detections.schema.json` · `contracts/common/examples/media-header-{valid,valid-unknown-encoding,invalid-missing-encoding,invalid-keyframe-type,invalid-timestamp-offset,invalid-frame-ref-extra}.json` · `examples/detections-draft-{valid,valid-no-alignment}.json` · `tests/test_contract_media.py`.
   - 수정: `contracts/common/frame-reference.schema.json`(description 2곳 — 구조 무변경: 필드 5·필수 3·`additionalProperties:false`) · `contracts/common/README.md`(파일 표 2행 + 예제 2행 + `$ref` 레지스트리 문단 + `frame_ref` 뜻·`additionalProperties` 예외 + 금지 라벨 표) · `backend/ingest/envelope.py` · `backend/observability.py`(`FORBIDDEN_LABELS` 8종 + docstring) · `tests/test_observability_labels.py`(N1-b 4 테스트).
   - 서버: 이 시점엔 복사하지 않음(단계 2-5 에서 함께).

---

## 단계 2 — DoD 전건 통과(2-1·2-2·2-3·2-4·2-5) — 2-2(4401)·2-3(첫 전송 keyframe)은 단계 4 서버 pytest 로 판정

1. 지시서와 다르게 한 것
   - **소켓 옵션 셋의 「경로별」은 `write_limit` 에만 적용했다.** `/state`·`/media` 가 8765 한 서버라 `compression`·`max_size` 는 서버 단위(둘 다 `compression=None`; 8765 `max_size=2**20` 명시, 8766 `max_size = 2 × MK2_MEDIA_MAX_FRAME_BYTES` 라이브러리 백스톱 + 우리 상한은 코드에서 프레임만 버림). `write_limit` 은 `process_request` 에서 `/media`·`/ingest` 8KB, `/state` 32KB.
   - **배출률 추정에 「정체 상한」을 얹었다** — EWMA(60표본 지평) 외에, 보내는 중인 프레임의 `in_flight / stalled_s` 를 상한으로. 근거는 단계 3 발견 (c).
   - **`MK2_MEDIA_INGEST_HOST`(기본 `127.0.0.1`) 를 부록 C 에 없던 변수로 추가** — 8766 은 단계 7 에서 tailscale IP 하나만, 8765 는 목록 둘 다라 별도 변수가 필요.
   - **`MK2_MEDIA_SNDBUF`(기본 65536 → Linux 실효 128KB) 추가 — 사용자 결정 A(단계 4 3번).** `/media` 소켓의 `SO_SNDBUF` 를 `process_request` 에서 못 박는다. 지연 바운드 문구를 `T_drop + write_limit + sndbuf` 로.
   - `websockets` 로거를 `main()` 에서 WARNING 으로 고정(요청 줄은 DEBUG 에서만 찍히지만 기대지 않음).
   - 뷰어 종료 로그에 회계 요약(`ViewerSlot.summary()` — `sent/dropped/gop_cut/max_buffered/t_drop_at_max/t_hard_at_max/max_frame/gop_seen`)을 남긴다 — M8 을 Loki 로 판정하기 위해(단계 4 1번).
   - `be.gateway.media_coldstart` 히스토그램 버킷 View 를 `observability.py` 에 추가(지시서 2-5 그대로 — 어댑터 밖에서 `opentelemetry` import 없음).

2. 발견한 것
   - **(a) websockets 17.1 요청 줄(쿼리 포함)은 DEBUG 에서만 찍힌다.**
     실측(원문, `server.py:626-629`): `if self.debug: self.logger.debug("< %s %s %s", request.method, request.path, request.protocol)`; INFO 는 `connection open`/`connection closed` 뿐.
     조치: 우리 로그는 `mask_token()` 으로 `token=***` 마스킹, `websockets` 로거 WARNING.
   - **(b) `serve(host=[...])` 는 목록의 모든 주소에 바인딩한다.**
     실측(원문): `["('127.0.0.1', 18799)", "('::1', 18799)"]`
     조치: `settings.ws_host() -> list[str]`(콤마 목록). 서버 `ss` 실측은 단계 7-i.
   - **(c) `process_request(connection, request)` 는 `protocol.accept()` 전에 불리고 `connection.transport` 가 있다**(`asyncio/server.py:140-160`) → 경로별 `set_write_buffer_limits`·`SO_SNDBUF` 가 공개 API 로 가능. `protocol.max_message_size`·`available_extensions` 는 내부 속성이라 쓰지 않음.
   - (d) 컴퓨터 로컬 관통(Kafka 없는 scratch 하네스, pytest 아님): 토큰 없음/오류 → `4401`, 중복 ingest → `4409`, 뷰어 수신 seq `[1, 2, 3, 4, 9]`(seq 0 P프레임은 WAIT_IDR 에서 버려짐, 6·7·8 = 헤더 불량·source_id 불일치·과대 프레임 거부 후 연결 유지), 첫 전송 keyframe True, 바이트 동일 `[True ×5]`.
   - (e) 실제 진입점 `python -m backend.gateway.ws_echo`(Kafka 미도달) 가 두 포트 READY 까지 감; `MK2_WS_HOST=127.0.0.1,0.0.0.0` + 토큰 없음 → exit 2(`MissingSetting`).

4. 이 단계가 서버·저장소에 남긴 것
   - 신설: `backend/gateway/media.py`. 수정: `backend/gateway/ws_echo.py`(파일명·유닛명 불변) · `backend/gateway/console.html`(판정 텍스트 + JPEG/WebCodecs 보조 + 결정 3 fail-safe 규칙 주석 — 오버레이 ⏭ 미구현) · `backend/settings.py`(부록 C + `MK2_MEDIA_INGEST_HOST`·`MK2_MEDIA_SNDBUF`, `ws_host()` 목록, `ws_url()` `/state`+토큰, `ws_media_url()`·`media_ingest_url()`) · `backend/observability.py`(coldstart View).
   - 서버(2026-09-18 15:15 UTC, DoD 2-5): 위 파일 + 단계 1 규격 + 단계 3·4 테스트를 `/home/dg/capstone-db/phase1_work/Physical-Project-mk2/` 에 복사(md5 6개 일치) → `sudo systemctl restart mk2-ingest mk2-storage-consumer mk2-ws-echo`(`envelope.py` 가 ingest 에도 실림) → `ss`: `127.0.0.1:8765`·`127.0.0.1:8766` python. 15:40 UTC 결정 A 반영 4파일 재복사(md5 4개 일치) → `sudo systemctl restart mk2-ws-echo`. READY 원문: `READY — WS 게이트웨이:ws://127.0.0.1:8765 (/state·/media, 토큰 있음, media write_limit=8192 sndbuf=65536)` · `READY — 엣지 입구: ws://127.0.0.1:8766/ingest (토큰 있음, max_frame=8388608, write_limit=8192)`.
   - 서버 `.env`(`/home/dg/capstone-db/.env`): `MK2_WS_TOKEN`·`MK2_EDGE_TOKEN` 2줄 추가(사용자, 영숫자 32자 — 값은 여기 적지 않음). 되돌릴 것: 없음(8766 은 loopback, 단계 8 에서 `MK2_MEDIA_INGEST_PORT=0`).
   - 컴퓨터 venv `..\venv_phase1`: `websockets 17.1` · `confluent-kafka 2.15.1` 설치.
   - scratch(저장소 밖): `harness_server.py`·`smoke_relay.py` — 개발용, 커밋·서버 반입 없음.

5. 이번에 검증하지 못한 것
   - **27-d 토큰이 로그에 남지 않는 것** — `journalctl`·Loki 실제 조회를 아직 하지 않았다(단계 5 서버 블록에서).
   - 8765 두 주소 LISTEN(7-i) — 단계 7.
   - `/state` 의 `write_limit` 32KB 가 실효인지 — 로컬 하네스 로그에 실패 경고(`주지 못했다`) 0건인 것까지만.

---

## 단계 3 — DoD 전건 통과(3-1·3-2·3-3 — 단계 4 서버 pytest `test_media_relay.py` 로 판정)

1. 지시서와 다르게 한 것
   - **ffmpeg 를 시스템에 설치하지 않고 컴퓨터 venv 의 `imageio-ffmpeg 0.6.0`(정적 ffmpeg 7.1, libx264·mjpeg·testsrc2 확인)로 생성 스크립트를 돌렸다**(`FFMPEG=… PYTHON=… bash tests/fixtures/make_h264_fixture.sh`). 스크립트 자체는 `ffmpeg` 기본값·bash 그대로라 서버·다른 컴퓨터에서 재생성 가능.
   - 스크립트가 **JPEG 프로파일 3장**(`synthetic_464x400_01~03.jpg`, `-q:v 2`)도 같이 만든다(지시서 3-2 "커밋된 JPEG 몇 장").
   - NAL 검산은 별도 코드가 아니라 `tests/media_publisher.py` 의 `split_annexb`/`group_access_units`/`count_nal_types` 를 그대로 쓴다(AU 규칙 한 벌).
   - **느린 뷰어 fixture(3-3)는 "수신 후 sleep" 하나가 아니라 둘**: ① 장마다 sleep(M7 용) ② **「한 장 받고 N초 정지 → 빠르게 소진」 + 수신 버퍼 32KB(`SO_RCVBUF`, connect 뒤) + `max_queue=1`**. 이유는 아래 (c).

2. 발견한 것
   - **(a) 컴퓨터에 ffmpeg 가 없다(Windows·WSL 둘 다).**
     실측(원문): PowerShell `ffmpeg 없음`; WSL `NO_FFMPEG` (`Linux DESKTOP-OFT3U6H 4.4.0-19041-Microsoft`).
     조치: 위 1번.
   - **(b) fixture 검산 결과(원문):** `NAL: SPS=8 PPS=8 IDR=8 nonIDR=112 SEI=1 AUD=0 | AU=120 keyframe AU=8 bytes=306532` · `검산: OK` · JPEG `23611`·`25021`·`25160` 바이트. `-x264-params repeat-headers=1` 재시도는 불필요했다(첫 인코딩에서 SPS·PPS 가 IDR 마다 반복).
   - **(c) loopback 에서 "수신 후 sleep" 뷰어로는 드롭이 나지 않는다 — 커널·클라이언트 버퍼가 삼킨다.**
     증상: 장마다 0.25초 자는 뷰어 8초(H.264 30fps) → 서버 드롭 0.
     실측(원문, 로컬 하네스): `/media 종료 source_id=mrt-90ecde65 sent=238 dropped=0 gop_cut=0` · JPEG 0.5초/장: 뷰어 수신 seq `[0, 1, …, 17]` 연속(드롭 없음).
     원인: 뷰어가 읽지 않아도 (클라이언트 `max_queue` 16장 + 커널 수신 버퍼 + 서버 커널 송신 버퍼)가 수백 KB~MB 를 흡수해 writer 의 `send()` 가 막히지 않는다 → 우리 `buffered` 가 늘지 않는다. 또 완료된 드레인 표본만 보는 EWMA 는 정체 시 60표본(수십 초) 동안 낡은 고속값을 유지해 T_drop 이 상한(1MB)에 붙는다.
     조치: ① 배출률에 정체 상한(`in_flight / stalled_s`) 추가(`DropOldPolicy.effective_drain_rate`, `tests/test_media_dropold.py::test_stall_cap_shrinks_t_drop_immediately`) ② 뷰어 fixture 를 정지-소진 패턴 + 수신 버퍼 고정으로. 그래도 남는 서버 커널 송신 버퍼는 단계 4 발견 (b) → 결정 A.
   - (d) 정체 상한 적용 뒤 로컬 H.264 정지 5초(원문): `sent=228 dropped=11 gop_cut=1 max_buffered=10540 t_drop_at_max=11702 t_hard_at_max=46807 max_frame=10540 gop_seen=True` — 큐가 T_drop 에 바운드, 재개 keyframe.
   - (e) **Windows loopback 은 `SO_SNDBUF` 를 그대로 존중하지 않는다** — 결정 A 뒤에도 JPEG 정지 4초(1.5MB)가 로컬에서 흡수됨(원문): `/media 종료 source_id=jpegchk-… sent=120 dropped=0 gop_cut=0 max_buffered=25374 t_drop_at_max=58210 t_hard_at_max=58210 max_frame=25374 gop_seen=False`(`t_hard == t_drop` GOP 없음 규칙은 작동). H.264 는 드롭(`sent=226 dropped=14 gop_cut=1`). Linux 서버에서는 둘 다 드롭(단계 4).

4. 이 단계가 서버·저장소에 남긴 것
   - 신설: `tests/fixtures/make_h264_fixture.sh` · `tests/fixtures/synthetic_464x400.h264`(306,532B, md5 `b6aba42ff04474b57ae858b0896dc750`) · `tests/fixtures/synthetic_464x400_01~03.jpg` · `tests/media_publisher.py`(홉2 참조 구현 — Annex-B→AU·frame_ref 부여·방식 B 송신·가짜 탐지 JSONL·CLI).
   - 수정: `.gitattributes` 에 `*.h264 binary`(fixture 커밋 전 추가 — 제약 26).
   - 컴퓨터 venv: `imageio-ffmpeg 0.6.0`.
   - 서버: fixture 폴더·`media_publisher.py` 는 단계 2-5 복사에 포함(h264 md5 일치).

5. 이번에 검증하지 못한 것
   - JPEG 프로파일의 drop-old 발동은 컴퓨터(Windows)에서 못 봤다 — 서버(Linux)에서 단계 4 로 판정.

---

## 단계 4 — DoD 전건 통과(4-1 서버 256 passed·skip 0 / 4-2 M1~M9 전부 실거부·유지 / 4-3 관측 차분 / 4-4 컴퓨터 단위 통과)

1. 지시서와 다르게 한 것
   - **M8(JPEG 큐 최대치 ≈ T_drop)은 뷰어 클라이언트가 아니라 Loki 의 게이트웨이 종료 로그로 판정한다** — 클라이언트는 TCP 버퍼 너머의 서버 큐를 볼 수 없다. 판정식: `gop_seen=False` · `t_hard_at_max == t_drop_at_max` · `max_buffered <= t_drop_at_max + max_frame` · `max_buffered < 4 × t_drop_at_max`. 계기 이름은 5종 그대로(늘리지 않음).
   - 관측 판정 3건(`test_media_metrics_increase_then_stop`·`test_rejected_metric_counts_stage`·M8)은 `prometheus_url`/`loki_url` fixture 로 없으면 skip; `ingest_url`(8766 TCP 도달) fixture 는 지시서대로 `conftest.py` 에.
   - 뷰어 fixture 가 `opened` 를 알리기 전 0.2초 여유(handshake 완료 → 서버 핸들러 슬롯 등록 사이 경쟁).

2. 발견한 것
   - **(a) 새 게이트웨이 프로세스의 「첫」 `/media` 스트림에서 빠른 뷰어인데 드롭이 났다(회계 결함).**
     증상: 서버 1차 전체 실행 `test_fast_viewer_receives_every_au_bytes_identical` 실패, 단독 재실행은 통과(컴퓨터 첫 실행도 같은 패턴).
     실측(원문, 컴퓨터 재현 — 새 하네스의 첫 접속): `AssertionError: 빠른 뷰어(loopback)인데 보낸 AU 수와 받은 수가 다르다: 40 vs 29` · 하네스 `/media 종료 source_id=mrt-aa33e5c3 sent=29 dropped=11 gop_cut=1 max_buffered=8786 t_drop_at_max=23399 t_hard_at_max=93596 max_frame=8786 gop_seen=True`. 서버 1차 원문: `3 failed, 251 passed, 2 skipped, 2 warnings in 274.13s (0:04:34)`.
     원인: 첫 프레임에서 `decode_frame` 이 `$ref` 레지스트리·검증기를 처음 만드는 동안(수십~수백 ms) 60fps 프레임이 소켓에 쌓이고, 그 뒤 `async for blob in websocket` 이 **await 없이** 연속 처리 → writer 태스크가 한 번도 돌지 못한 채 `buffered` 3장 누적 → 드레인 표본 없어 T_drop = 하한(2×AU 평균) → P프레임 1장 DROP + WAIT_IDR 로 GOP 나머지 버림.
     조치: ① `Gateway.__init__` 에서 `media.header_validator()` 선생성 ② ingest 루프가 프레임 하나 팬아웃 뒤 `await asyncio.sleep(0)` 로 writer 에 차례를 준다. 재현 하네스에서 첫 접속 `sent=40 dropped=0`(2회).
   - **(b) Linux 서버에서도 느린 뷰어 드롭 0 — 커널 송신 버퍼가 정지분을 삼킨다.**
     실측(원문, 서버 `-x` 재실행): `AssertionError: 느린 뷰어인데 드롭이 없다 (sent=240 got=240)` (H.264 5초 정지); 1차 전체 실행의 JPEG 도 `sent=120 got=120`.
     원인: 뷰어 수신 버퍼를 32KB 로 못 박아도 서버 `sk_sndbuf` 는 자동조정(loopback MSS 64KB × cwnd 10 ≈ 1.4MB, 상한 `tcp_wmem` 4MB)으로 커져 `send()` 가 안 막힌다. 지시서 바운드 `T_drop + write_limit` 에 이 세 번째 버퍼가 빠져 있었다.
     조치: 사용자 결정 A(3번) → `MK2_MEDIA_SNDBUF`.
   - **(c) `.env` 에 토큰이 안 들어간 채 재기동됐다.**
     실측(원문): `READY — WS 게이트웨이:ws://127.0.0.1:8765 (/state·/media, 토큰 없음(loopback 전용 기동))` · pytest `SKIPPED [1] tests/test_media_relay.py:169: MK2_WS_TOKEN 이 비어 있다 — loopback 무토큰 모드에서는 M1 을 판정할수 없다` ×2.
     원인: 토큰 값은 생성됐으나 `.env` 편집이 빠졌다(지시 전달 실수).
     조치: 사용자가 2줄 추가 → 재기동 뒤 `WS_TOKEN True EDGE_TOKEN True`, M1 2건 통과.
   - **(d) 결정 A 반영 뒤 서버 전체 실행(원문): `256 passed, 2 warnings in 269.14s (0:04:29)`** — skip 0. 남은 경고 2건은 `LoggingHandler` deprecation(`opentelemetry/sdk/_logs/_internal/__init__.py:580`) — 단계 5-3 #9 대상.
   - (e) 수집 수(원문): `256 tests collected` = N 184 + 라벨 13 + `test_contract_media` 17 + `test_media_frame` 12 + `test_media_dropold` 18 + `test_media_relay` 12.

3. 내가 결정해 준 것
   - 물음: 느린 뷰어 드롭이 안 나는 원인(커널 sndbuf)에 대해 **A** `/media` 소켓 `SO_SNDBUF` 상한(환경변수 `MK2_MEDIA_SNDBUF`, 기본 65536 → Linux 실효 128KB, 지연 바운드 `T_drop + write_limit + sndbuf`) vs **B** 서버 무변경·테스트만 강화(정지 도착량 > 4MB). 답(사용자, 2026-09-19): *"좋아. A로 가자. 기본값 128KB 그대로."* → setsockopt 값 65536(Linux 2배 → 실효 128KB)으로 구현.

4. 이 단계가 서버·저장소에 남긴 것
   - 신설: `tests/test_media_frame.py` · `tests/test_media_dropold.py` · `tests/test_media_relay.py`. 수정: `tests/conftest.py`(`ingest_url` skip fixture) · `backend/gateway/ws_echo.py`·`media.py`·`settings.py`(위 2번 (a)·(b) 조치).
   - 서버: 단계 2 「남긴 것」의 15:15·15:40 UTC 복사·재기동이 이 단계의 판정 근거. 최종 md5: `media.py 717fdde0…` · `ws_echo.py 22772b77…` · `settings.py afe942d1…` · `test_media_relay.py 3513c2a4…`.
   - 관측 평면에 남은 것: `be_gateway_media_*` 시계열(테스트 소스 `mrt-*`)·Loki `be-gateway` 로그(`/media 종료 source_id=mrt-…` 회계 줄) — 단계 5-8 기준선(28 → 35종) 확인 때 본다.
   - 사용자 보관: `260918 phase4 단계2.txt` · `260918 phase4 단계2보강.txt`.

5. 이번에 검증하지 못한 것
   - 27-d(토큰이 `journalctl`·Loki 에 없다) — 아직 조회 안 함(단계 5 서버 블록).
   - `be_gateway_media_gop_cut_total`·`media_coldstart` 두 이름의 Prometheus 출현 — pytest 는 `media_frames`·`media_ingress`·`media_rejected` 만 차분으로 봤다. DoD 5-8(28 → 35종)에서 확인.
   - 느린 뷰어 실측은 loopback + 합성 fixture 다. 터널 위(7-c)·실물 엣지·현장 회선은 범위 밖/단계 7.

---

## 단계 5 — DoD 전건 통과(5-1·5-2·5-4·5-5·5-6·5-7·5-7b·5-8 / 5-3 `edge_federate` up 은 단계 7 판정)

1. 지시서와 다르게 한 것
   - 5-3 #10(라벨 8종)은 단계 1에서 이미 반영했으므로 여기서는 확인만(서버 pytest 통과).
   - `LoggingHandler` 교체는 `LoggingInstrumentor().instrument()` 가 아니라 **핸들러 클래스 직접 사용** — 인스트루멘터는 루트 로거에 스스로 붙고 `logging.basicConfig` 를 감싸서 우리 `main()` 의 핸들러 배선과 겹친다. 없으면 SDK 핸들러로 폴백.
   - Collector 설정 파일(`otel-collector-config.yaml`)은 **바꾸지 않았다**(4316 은 compose `ports` 만) — `validate` 는 판정 수단으로만 돌렸다.
   - `infra/README.md` 에 §3 포트 표(8765·8766·8767·9095)·§6 행 4개(Prometheus digest·미고정 잔여 6·브리지 대역 미고정·ⓗ·LoggingHandler)를 이 단계에서 적었다(§8 갱신 대상의 일부 선반영).

2. 발견한 것
   - **(a) `opentelemetry-instrumentation-logging` 에는 최상위 `LoggingHandler` 가 없다 — `handler` 모듈에 있다.**
     실측(원문, 컴퓨터 0.65b0): `has LoggingHandler: False | exports: ['BaseInstrumentor', … 'LoggingInstrumentor', …]` · `./handler.py:114:class LoggingHandler(logging.Handler)` · 생성자 `(level=NOTSET, logger_provider=None, log_code_attributes=False)`.
     원인: SDK 1.44 deprecation 메시지가 "핸들러"라고만 적고 경로를 안 적었다(지시서 "API가 다를 수 있으므로 실측").
     조치: `observability._setup_logs` 가 `opentelemetry.instrumentation.logging.handler.LoggingHandler` 를 먼저 import. 컴퓨터 `-W error::DeprecationWarning` 통과; 서버 기동 로그 원문 `로그 경로 opentelemetry.sdk._logs + instrumentation.logging.handler`.
   - **(b) 27-d(토큰 로그 미노출) 확인.**
     실측(원문): `journalctl -u mk2-ws-echo --since '2026-09-18 15:00' … | grep -c "$MK2_WS_TOKEN"` → `0` · `grep -c 'token=\*\*\*'` → `30` · Loki `|= "$MK2_WS_TOKEN"` → `"result":[]`(`totalLinesProcessed":236`).
   - (c) 서버 값(원문): Prometheus digest `prom/prometheus@sha256:b5a5ad001253b37e72eb2a264c95e57ff60bb0fe45f9a110a7c3b269e0889112` · `promtool, version 3.9.1` · promtool `SUCCESS: /tmp/prometheus.new.yml is valid prometheus config file syntax` · `validate exit=0` · Collector `Everything is ready. Begin running and processing data.` · `ss`: `<서버 tailscale IP>:4316`·`127.0.0.1:4316` · targets 6 잡(`edge_federate` 포함, `robot_server` down 원래) · `scrape_interval: 1s` · ufw `[38] 4316/tcp ALLOW IN <컴퓨터 tailscale IP> # mk2 phase4 edge otlp (temporary, step8 removes)` · pytest `256 passed in 250.97s` 경고 없음 · `be_*` **35** · `be_gateway_*` 9 · Loki 라벨 3 · 재생성 `otel_collector`·`prometheus` 둘만(`Up 8 minutes`), 나머지 11 무변경.
   - (d) `docker compose` 가 매번 `the attribute 'version' is obsolete` 경고를 낸다 — 기존 파일 상태, 기능 영향 없음, 이번에 손대지 않음(분류 ③ 접촉 최소화).
   - (e) 컴퓨터 venv 에 `pyyaml` 이 없어 편집본 YAML 파싱은 서버 `promtool`·`compose config --quiet` 로만 판정했다(둘 다 통과).

4. 이 단계가 서버·저장소에 남긴 것
   - 서버 백업: `/home/dg/capstone-db/docker-compose.yml.bak_before_phase4_obs`(18,908B) · `/home/dg/capstone-db/config/prometheus.yml.bak_before_phase4`(5,992B). 대기 위치 `/home/dg/capstone-db/phase1_work/phase4_new/{docker-compose.yml,prometheus.yml}`.
   - 서버 compose(`md5 7be38ce0548fddc463ed481fcbc959d6`): `prometheus.image` → digest · `otel-collector.ports` 에 `"<서버 tailscale IP>:4316:4317"` 되살림(주석 2줄 추가). 서버 `config/prometheus.yml`(`947df55064d77c50903beb4d975664d1`): `edge_federate` 잡 주석 해제(타깃 컴퓨터 `:9090`, 15s·honor_labels·match[] 2줄 무변경).
   - 재생성 컨테이너: `capstone_prometheus`·`capstone_otel_collector`(2026-09-18 16:19 UTC). ufw `[38]` 4316 ← <컴퓨터 tailscale IP>/32.
   - 서버 venv: `opentelemetry-instrumentation-logging 0.65b0`(+`opentelemetry-instrumentation 0.65b0`·`wrapt 2.4.1`). 저장소 사본: `backend/observability.py`(`37339233…`)·`pyproject.toml`(`c7ccbd1e…`). 상주 3개 재기동 16:21 UTC.
   - 저장소: `backend/observability.py`(핸들러 교체) · `pyproject.toml`(의존성 + Phase 4 주석) · `infra/README.md`(§3·§6) · 작업본 `infra/docker-compose.yml`·`infra/config/prometheus.yml`(gitignore).
   - **되돌릴 것(단계 8):** compose 4316 Tailscale 줄 → 주석 · ufw `[38]` 삭제 · `edge_federate` 잡 → 주석. Prometheus digest·LoggingHandler 는 남긴다.

5. 이번에 검증하지 못한 것
   - `edge_federate` 잡 `up`·15s(DoD 5-3) — 엣지 Prometheus 가 단계 7에서 뜬다.
   - `/state` 의 `write_limit` 32KB 실효(단계 2 5번) — 여전히 미측정(경고 로그 0건까지만).

---

## 단계 6 — DoD 전건 통과(6-1·6-2·6-3·6-4)

1. 지시서와 다르게 한 것
   - compose 주석 2곳(6-b) 외에 `ports` 9095 줄 위·리스너 4종 설명에 Phase 4 주석을 더 적었다(단계 8 되돌림 위치 표시). `infra/README.md` §5 는 「3수정」 절 전체를 「EDGE 리스너」 절로 교체하고 정정 박스를 재정정했다(§8 갱신 대상 선반영).
   - 판정 명령에 "컨테이너 안에서 `--bootstrap-server <서버 tailscale IP>:9095`" 를 넣었는데 이 경로는 성립하지 않는다(아래 2(a)) — EDGE 도달 판정은 단계 7-d(컴퓨터)와 서버 호스트 python 확인으로 대신한다.

2. 발견한 것
   - **(a) 컨테이너 안에서 호스트 tailscale IP:9095 로 붙는 검사는 ufw 에 막힌다(브로커 결함 아님).**
     실측(원문): `Exception in thread "main" java.lang.RuntimeException: Request METADATA failed on brokers List(<서버 tailscale IP>:9095 (id: -1 rack: null))`
     원인: 컨테이너(172.18.0.x) → 호스트 주소 트래픽은 DNAT 를 타지 않고 `INPUT` 으로 가 ufw 가 적용된다(§5 정정의 첫 경로). 9095 허용 규칙은 엣지 `/32` 뿐이라 브리지 출발은 거부. 같은 명령의 `localhost:9092` 는 `localhost:9092 (id: 1 rack: null)` 로 정상.
     조치: 판정 수단을 바꾼다 — 서버 호스트 venv 의 `confluent_kafka.AdminClient("<서버 tailscale IP>:9095")`(lo 경유) + 단계 7-d 컴퓨터 왕복. 명령 결함으로 기록.
   - (b) 서버 값(원문): `capstone_kafka Up About a minute (healthy)` · `ss`: `<서버 tailscale IP>:9095`·`127.0.0.1:9092` · 토픽 4 · 그룹 2 LAG 0, 오프셋 `heartbeat 200 / status 152 / state 178`(단계 4·5 pytest 발행분 — 09-18 140/127 에서 증가, 지시서 예외) · 새 `CONSUMER-ID`(리밸런스) · 백엔드 `active`×3, `NRestarts=0`×3, `ActiveEnterTimestamp=Fri 2026-09-18 16:21:4x UTC`(단계 5 재기동 그대로) · `journalctl mk2-storage-consumer` 에 `%6|…|FAIL|rdkafka#consumer-1| [thrd:localhost:9092/bootstrap]: localhost:9092/bootstrap: Disconnected: connection reset by peer(after 0ms in state APIVERSION_QUERY)` 류 재연결 로그 14줄(정상 — 죽지 않음) · ufw `[39] 9095/tcp ALLOW IN <컴퓨터 tailscale IP> # mk2 phase4 edge kafka (temporary, step 8 removes)`.
   - (c) `ss -tnp | grep ':9092' | grep -c python` = **5**(지시서 기대 "셋"). 프로세스 3개가 librdkafka 연결을 bootstrap·broker·GroupCoordinator 로 여럿 열기 때문이며 프로세스 수·재기동 여부(NRestarts 0)로 판정했다.

4. 이 단계가 서버·저장소에 남긴 것
   - 서버 백업 `/home/dg/capstone-db/docker-compose.yml.bak_before_phase4_kafka`(단계 5 적용본 상태). 서버 compose(`md5 0c8ff5f262f2ab98b8a56c667dd3dc0f`): Kafka `ports` 에 `"<서버 tailscale IP>:9095:9095"`, `KAFKA_LISTENERS`·`ADVERTISED_LISTENERS`·`SECURITY_PROTOCOL_MAP` 에 EDGE, 주석 2곳 정정. 재생성 `capstone_kafka` 1회(2026-09-18 16:39 UTC, `kafka_data` bind mount 라 토픽·오프셋 유지). ufw `[39]` 9095 ← <컴퓨터 tailscale IP>/32.
   - 저장소: `infra/README.md` §5(리스너 표 3행·`ports` 행·「EDGE 리스너」 절·정정 박스 재정정·9092 ufw 낡은 문장 삭제). 작업본 `infra/docker-compose.yml`(gitignore).
   - **되돌릴 것(단계 8):** compose EDGE 세 자리 + `ports` 9095 줄 → 주석 · ufw `[39]` 삭제. 주석 정정 2곳은 남긴다.

5. 이번에 검증하지 못한 것
   - EDGE 리스너 실제 도달·advertised 주소(`<서버 tailscale IP>:9095`로 답하는지) — 컨테이너 안 검사가 성립하지 않아 단계 7 에서 판정(서버 호스트 python + 컴퓨터 7-d).

---

## 단계 7 — DoD 전건 통과(7-1 실측 9건 기록 · 7-1b 기록 · 7-2 재생성 없음 · 7-3 서버 256 passed) — 7-c 는 링크를 조인 뒤(사용자 결정 A) 통과

1. 지시서와 다르게 한 것
   - **7-c 를 앱 속도 뷰어로는 판정하지 못해 서버 `tailscale0` egress 를 `tc tbf rate 500kbit` 로 30초 조여 「느린 링크」를 만들었다**(사용자 결정 A, 3번). 지시서의 느린 뷰어 fixture(수신 후 sleep)는 Windows 뷰어에서 커널 수신 버퍼(자동조정)가 정지분·초과분을 전부 삼켜 서버 `send()` 가 막히지 않는다(2(a)).
   - 7-h 프로브(허용 목록 밖 tailnet 기기에서 `nc`)는 **실행 불가** — 접근 가능한 기기가 서버·컴퓨터뿐. 같은 사실을 nat `DOCKER` 규칙(소스 제한 없음)·`DOCKER-USER` 빈 것으로 기록하고, 🔴 공인 IP 프로브(컴퓨터 → <서버 공인 IP> 9095/8766/8765/4316 전부 False)를 더했다.
   - 7-3 pytest 에 `MK2_MEDIA_INGEST_URL=ws://<서버 tailscale IP>:8766/ingest` 를 줬다 — 8766 이 tailscale IP 에만 있어 기본값(loopback)으로는 `ingest_url` fixture 가 relay 12건을 skip 한다(단계 8 에서 8766 이 닫히면 skip 이 정상).
   - `tests/test_observability_pipeline.py::test_gateway_metrics_with_ws_client` 를 고쳤다(2(c)) — 지시서 범위 밖 파일이지만 DoD 7-3 전건 통과에 걸렸다.
   - 7-2 터미널에 가짜 B층(`edge_probe_publisher.py --interval 5 --rounds 6`)을 하나 더 뒀다(7-e·7-f 재료 — 지시서 7-2 목록에는 없었다). 7-c 실측용 `edge_slow_viewer.py`·7-d 용 `edge_kafka_check.py` 를 `_serverinfo/edge_probe_260919/` 에 만들었다(커밋 안 함).
   - `mk2-ws-echo.service` 에 `RestartSec 5 → 10`(지시서 "상향 검토"의 실행).

2. 발견한 것
   - **(a) Windows 뷰어는 `SO_RCVBUF` 를 무시해 앱이 느려도 서버에 백프레셔가 오지 않는다.**
     증상: 정지 5초(H.264)·정지 4초(JPEG 1.5MB)·60KB/s 앱 제한 — 세 번 다 서버 드롭 0.
     실측(원문): `sent=297 (keyframes 20)  got=297 (keyframes 20)` … `age_s all: p50=0.196 p90=4.093 max=5.158` / `sent=150 … got=150 … max=4.207` / `sent=596 (keyframes 40)  got=437 (keyframes 30)` … `age_s all: p50=3.305 p90=5.641 max=6.376 | second-half: p50=4.639 …` / 서버 Loki `/media 종료 source_id=edge-chk sent=596 dropped=0 gop_cut=0 max_buffered=10536 t_drop_at_max=51624 …`.
     원인: 초과분이 컴퓨터(Windows) 커널 수신 버퍼(자동조정, 수 MB)에 쌓여 서버 `send()` 가 막히지 않는다 → 서버 회계 `max_buffered` 가 한 장. 링크가 빠르고 **앱**이 느린 경우는 서버 drop-old 가 보호할 수 없다(브라우저도 같다 — VZ 통지에 적을 항목).
     조치: 링크를 조여 다시 실측(A). 앱-느림 경우는 「검증 범위의 한계」와 VZ 통지 항목으로.
   - **(b) 링크를 조이면 drop-old 가 설계대로 돈다 — 그리고 지연은 sndbuf 가 지배한다.**
     실측(원문): `sent=745 (keyframes 50)  got=538 (keyframes 50)` · `gaps=19 first_gaps=[(192, 195), (195, 210), (210, 225), (264, 270), (270, 285)]  resume_on_keyframe=True` · `age_s all: p50=1.968 p90=2.342 max=2.534` · 서버 `/media 종료 source_id=edge-tc sent=538 dropped=207 gop_cut=20 max_buffered=27917 t_drop_at_max=5716 t_hard_at_max=22864 max_frame=10535 gop_seen=True`.
     해석: 불연속 19개가 전부 IDR 위치(GOP 15 배수)에서 재개, IDR 50/50 전달(T_hard 가 IDR 여유를 준다), 바이트 동일. 서버 큐 몫은 T_drop 5.7KB ≈ 92ms @62KB/s 인데 관측 지연 ~2.0s 는 **커널 sndbuf 128KB(≈2.06s @62KB/s)** 몫이다 — 바운드 `(T_drop + write_limit + sndbuf)/링크율 ≈ 2.3s` 가 p90 2.34s 와 맞는다. "150ms" 는 서버 큐 몫에만 해당.
     조치: 기록. `MK2_MEDIA_SNDBUF` 를 낮추는 선택(예 16384 → 실효 32KB, 같은 링크에서 ≈0.7s, 대신 DERP 처리량 상한 444KB/s)은 **다음 결정 항목**으로 보고서에 올린다. 값은 이번에 바꾸지 않았다.
   - **(c) `test_gateway_metrics_with_ws_client` 가 외부 뷰어가 붙어 있으면 헛되이 실패한다(테스트 결함).**
     실측(원문): `FAILED tests/test_observability_pipeline.py::test_gateway_metrics_with_ws_client` / `1 failed, 255 passed in 242.09s (0:04:02)`; 수정 뒤 `1 passed in 18.09s` · 전건 `256 passed in 239.17s (0:03:59)`.
     원인: 브라우저 뷰어가 붙어 있어 `sum(be_gateway_clients) >= 1` 이 즉시 참 → `wait_until` 이 바로 끝나 `be_gateway_push_total` 차분을 export(15s)+scrape(5s) 전에 읽었다. 이전 판은 clients 대기가 그 시간을 대신 벌어 준다고 기대고 있었다.
     조치: push 차분도 `wait_until` 로(md5 `6b31ddba53887a76333384f10d29fc2e`). 서버 사본에 복사·전건 통과.
   - **(d) 7-f 즉시 조회는 비어 있었다 — 프로브 종료 15분 뒤라 stale.**
     실측(원문): `count by (__name__) ({agg_layer="edge"})` 출력 없음; `last_over_time(…[2h])` 로 재조회 → `hw_publish_count_total 2 / hw_publish_duration_milliseconds_bucket 16 / …_count 1 / …_sum 1 / system_cpu_utilization_ratio 1 / system_memory_utilization_ratio 1`, 음성 `result: []`.
     원인: 엣지 Collector exporter `metric_expiration` 5m + Prometheus staleness — 프로브(6주기, 02:07:34~59)가 끝난 뒤 조회.
     조치: 구간 질의로 판정. 이후 페더레이션 판정은 프로브 직후 5분 안에 하거나 `last_over_time` 을 쓴다.
   - (e) 값(원문): 7-a `pong … via <서버 공인 IP>:41641 in 2ms` / `via <컴퓨터 공인 IP>:1126 in 22ms` · 7-b 브라우저 `frames=2954 keyframes=197 bytes=8193446 seq_first=0 seq_last=2953 gaps=0 reversals=0`, `첫 프레임: keyframe=true seq=0`, `디코더: WebCodecs avc1.42C01E`; fast `sent=173 got=173 … age_s p50=0.007 p90=0.009 max=0.215` · 7-d `brokers(advertised): {1: '<서버 tailscale IP>:9095'}` `produce: ok offset=200` `consume: key=edge-kchk-b413fb28 value_identical=True` TSDB `edge-kchk-b413fb28|heartbeat|2026-09-18 17:20:05.969+00` · 7-e Loki 스트림 `hw-sensor-node`, Tempo `rootServiceName":"hw-sensor-node"` 3건·태그값 `["hw-sensor-node"]`, Loki 인덱스 라벨 3종 · 7-g `before=23` → `after= 25` · 7-h 공인 IP `9095/8766/8765/4316 public TcpTestSucceeded=False`, nat `-A DOCKER -d <서버 tailscale IP>/32 … --dport 9095 -j DNAT --to-destination 172.18.0.13:9095`, `-N DOCKER-USER` · 7-i `ss` `<서버 tailscale IP>:8766`·`<서버 tailscale IP>:8765`·`127.0.0.1:8765` · 재생성 없음.
   - (f) 첫 라운드에서 컴퓨터 터미널의 토큰이 자리표시자 그대로 실행돼(`$env:MK2_WS_TOKEN="<MK2_WS_TOKEN 값>"`) 서버가 `4401` 로 닫았다 — 지시 전달 결함(실값을 적지 않음). 이후 명령에는 실값을 적는다.

3. 내가 결정해 준 것
   - 물음: 7-c 가 앱 속도 뷰어로는 드롭 0 — **A** 서버 `tailscale0` egress 를 `tc` 로 30초 조여 실측(원복 한 줄) vs **B** 「미검증」 기록. 답(사용자, 2026-09-19): *"일단 7-c는 A로 가자."* → 실행, 원복 확인(`qdisc fq_codel 0: root`).

4. 이 단계가 서버·저장소에 남긴 것
   - 서버 `.env`: `MK2_WS_HOST=127.0.0.1,<서버 tailscale IP>` · `MK2_MEDIA_INGEST_HOST=<서버 tailscale IP>` (단계 8: `MK2_MEDIA_INGEST_PORT=0` 추가, `MK2_WS_HOST` 는 남긴다).
   - 서버 유닛 `/etc/systemd/system/mk2-ws-echo.service`(md5 `f77a9585…`, After/Wants tailscaled, RestartSec 10) — 남긴다. `mk2-ws-echo` 재기동 16:54 UTC.
   - ufw `[40]` 8765 ← <컴퓨터 tailscale IP>(남긴다) · `[41]` 8766 ← <컴퓨터 tailscale IP>(단계 8 삭제). `tc` 는 원복됨(변경 잔여 없음).
   - 서버 저장소 사본: `tests/test_observability_pipeline.py`(`6b31ddba…`).
   - 컴퓨터: `C:\Users\asdfa\physical mk2\edge_probe\`(실행 파일 2 + 설정·스크립트 4 + `data\`) — 단계 8 삭제 · Windows 방화벽 규칙 "MK2 edge Prometheus (phase4)" — 단계 8 삭제. 설정·스크립트 원본은 `_serverinfo/edge_probe_260919/`.
   - 저장소: `infra/systemd/mk2-ws-echo.service` · `tests/test_observability_pipeline.py`(push wait_until). 실측 기록 `_serverinfo/260919_phase4_단계7_실측.txt`.
   - 관측 평면 잔여: Prometheus `agg_layer="edge"` 시계열(갱신 중단, stale) · Loki/Tempo `hw-sensor-node` · TSDB `edge-kchk-b413fb28` 1행 · Loki `be-gateway` 회계 줄(`edge-chk`·`edge-tc`).
   - 추적표 BE-T-08 「현재 통제 수준」·7-h 결과 반영은 §8 문서 갱신(단계 9 전)에서 한다 — 아직 안 적었다.

5. 이번에 검증하지 못한 것
   - 허용 목록 밖 **tailnet 기기**에서의 9095 연결(7-h 원문 프로브) — 기기 없음. 규칙 검사·공인 IP 프로브로 대체.
   - 링크가 느릴 때의 지연은 `tc` 500kbit 한 조건뿐이다. 현장 회선·DERP 경유(7-a 는 직접 경로였다)·실물 엣지는 범위 밖.
   - 뷰어 앱이 느린 경우(브라우저 디코드 지연 등)의 지연은 서버가 바운드하지 못한다 — 실측 (a) 그대로.
   - 4318(브라우저 관측 입구)은 손대지 않았다(5-7b) — VZ 답 이후.

---

## 단계 8 — DoD 전건 통과(서버 8-1·8-2·8-3·8-4·8-6 · 컴퓨터 8-5 는 폴더 삭제 2차 시도에서 통과)

1. 지시서와 다르게 한 것
   - Prometheus 는 재생성하지 않고 `docker compose kill -s HUP prometheus` 로 리로드했다 — 바뀐 것이 bind-mount 된 설정 파일뿐이라 compose 정의가 같아 `up -d` 로는 반영되지 않는다. 로그 `Completed loading of configuration file … totalDuration=1.131943ms`. 재생성은 `otel_collector`·`kafka` 둘만(ports·environment 변경).
   - 적용 전 백업 2개(`…bak_before_phase4_step8`)와 적용 전 문법 판정(`compose --project-directory … config --quiet` → `COMPOSE_OK`, `promtool check config` → `SUCCESS`)을 넣었다(지시서에 없음, 단계 5·6 관행).
   - Kafka EDGE 「주석으로」는 **주석 판 3줄 + 활성 3줄(단계 6 이전 값)** 로 했다 — `KAFKA_LISTENERS` 등 env 키 자체는 지울 수 없으니 활성 값을 되돌리고 EDGE 판을 바로 위에 주석으로 남겼다. 되살릴 때 `ports` 한 줄 + env 세 줄 「넷이 한 묶음」 주석을 두 자리(ports·환경)에 적었다.
   - `.env` 의 `MK2_MEDIA_INGEST_HOST=<서버 tailscale IP>` 는 지시서 되돌림 목록에 없어 남겼다(`MK2_MEDIA_INGEST_PORT=0` 이면 읽히지 않는 값).
   - 컴퓨터 사전 확인을 내가 일반 권한 셸에서 돌려 `Get-NetFirewallRule` 이 `Access is denied` → 사용자 명령을 **관리자 PowerShell** 로 지정했다.
   - 컴퓨터 폴더 삭제 명령에 실행 위치를 적지 않아 삭제 대상 폴더 **안에서** 실행됐다 → 2(b). 지시 결함.

2. 발견한 것
   - **(a) 단계 7 브라우저 세션의 전체 회계가 게이트웨이 재기동 때 나왔다 — 3836장 송신, 드롭 0.**
     실측(원문): `/media 종료 source_id=go1-001_front sent=3836 dropped=0 gop_cut=0 max_buffered=10542 t_drop_at_max=82060 t_hard_at_max=328241 max_frame=10542 gop_seen=True`(구 pid 종료 직전).
     원인/해석: 단계 7-2 송신기 총량 「송신 끝: 3836 장」과 같다 — 직접 경로(2ms)·빠른 뷰어(브라우저)에서는 세션 전체에서 한 장도 버리지 않았다(`max_buffered` = 한 장). 7-c(a) 「앱이 느려도 드롭 0」과 같은 조건의 정상 쪽 값.
     조치: 값 기록(02-media-path 「실측」 절 재료).
   - **(b) 컴퓨터 `Remove-Item` 1차 실패(지시 결함).**
     실측(원문): `Remove-Item : 'C:\Users\asdfa\physical mk2\edge_probe'의 항목은 사용 중이므로 제거할 수 없습니다.` · `Test-Path … → True`.
     원인: 셸의 현재 위치가 삭제 대상 폴더 안(`PS C:\Users\asdfa\physical mk2\edge_probe>`) — 셸 자신이 디렉터리 핸들을 잡는다. 단계 7 터미널 1·2도 그 폴더에서 띄웠다.
     조치: 그 폴더가 프롬프트인 터미널을 닫고 `Set-Location "C:\Users\asdfa\physical mk2"` 뒤 재시도 → `Test-Path … → False`(2차 통과). 이후 삭제 지시에는 실행 위치를 먼저 적는다.
   - **(c) 이 작업로그 자체에 tailscale·공인 주소가 그대로 적혀 있었다**(단계 5~7 항목 — 머리말 「비밀값·Tailscale 주소는 적지 않는다」 위반, 제약 11).
     실측(원문): grep 결과 `<서버 tailscale IP>` 계열 9행·`<컴퓨터 tailscale IP>` 계열 5행·공인 IP 3행·`scp -P` 포트 1행.
     원인: 단계 5~7 항목을 서버 출력 원문을 그대로 옮겨 적으면서 치환을 빼먹었다(실측 원문 기록 규칙과 주소 금지 규칙이 충돌하는 자리).
     조치: 이번 단계에서 전부 자리표시자로 치환(`<서버 tailscale IP>`·`<컴퓨터 tailscale IP>`·`<서버 공인 IP>`·`<컴퓨터 공인 IP>`·`-P <포트>`). 실값은 `_serverinfo/` 파일에만. 🔴 **커밋된 지시서 `docs/be/tasks/작업지시_phase4_미디어경로.md` 233·245행에도 tailscale 주소 2개가 있다**(HEAD `cd3e6a2` 부터 — 설계방 산출물). 저장소 안 tailscale 주소는 그 파일뿐(`git grep` HEAD 기준). 사용자 결정 대기(4번).
   - (d) 값(원문): pytest `244 passed, 12 skipped in 123.88s (0:02:03)`, skip 12건 전부 `tests/test_media_relay.py`(136·170·182·206·230·251·301·329·368·383·412·442행) 사유 `엣지 입구(8766)에 닿지 않는다 — 미디어 중계 테스트를 건너뛴다(127.0.0.1:8766) — ConnectionRefusedError: [Errno 111] Connection refused` · `settings.media_ingest_port()` → `0`, `media_ingest_url()` → `ws://127.0.0.1:8766/ingest?token=…` · ufw 삭제 3건 `allow from <컴퓨터 tailscale IP> to any port 8766|9095|4316 proto tcp comment '…(temporary, step 8 removes)'` → `Rule deleted`, 남은 `[38] 8765/tcp ALLOW IN <컴퓨터 tailscale IP> # mk2 phase4 viewer (user PC) - stays`(번호 40→38), `grep -c` → `1` · `ss` `<서버 tailscale IP>:8765`·`127.0.0.1:8765`(pid 1417769)·`127.0.0.1:4316` 만 · `docker port` kafka `9092/tcp -> 127.0.0.1:9092`, collector `4317/tcp -> 127.0.0.1:4316` · 서버 compose 139·239행 `# - "…:4316:4317"`/`# - "…:9095:9095"`, 261~263행 EDGE 판 주석, 264~266행 활성 3줄 EDGE 없음 · prometheus.yml 74행 `# - job_name: 'edge_federate'`, scrapePool 5종 · `capstone_kafka Up About a minute (healthy)`·`capstone_otel_collector Up About a minute`·`capstone_prometheus Up 2 hours`(재생성 없음), 나머지 10개 무변경 · 토픽 4 · 상주 3개 `ActiveState=active NRestarts=0` · ws-echo `READY — WS 게이트웨이: ws://127.0.0.1,<서버 tailscale IP>:8765 (… sndbuf=65536)` + `엣지 입구 닫힘 — MK2_MEDIA_INGEST_PORT=0`, `READY — 엣지 입구` 줄 없음 · Kafka 재생성 순간 rdkafka `Disconnected … APIVERSION_QUERY`·`Connection refused` 7줄 → 재연결(단계 6 과 같은 정상 패턴). 컴퓨터 `Get-Process` 빈 출력 · `Get-NetFirewallRule` → `'MK2 edge Prometheus (phase4)'과(와) 같은 'DisplayName' 속성을 가진 MSFT_NetFirewallRule 개체가 없습니다`(= 삭제됨).

3. 내가 결정해 준 것 — 그 밖 없음(ufw 「큰 번호부터, 끝나고 `status numbered`」는 착수 지시로 받아 그대로 실행)

4. 이 단계가 서버·저장소에 남긴 것
   - 서버 `.env` 8행 `MK2_MEDIA_INGEST_PORT=0` 추가(6행 `MK2_WS_HOST=127.0.0.1,<서버 tailscale IP>`·7행 `MK2_MEDIA_INGEST_HOST` 유지). 서버 compose(`md5 c94ddd8301019ab499f72129f0f0b16e`)·`config/prometheus.yml`(`ab2410206a205a70b394e7c650226fc8`) = 컴퓨터 작업본. 백업 `docker-compose.yml.bak_before_phase4_step8`(22,792B, 단계 6 적용본)·`config/prometheus.yml.bak_before_phase4_step8`(6,386B, 단계 5 적용본). 대기 위치 `phase1_work/phase4_new/` 두 파일 = 적용본.
   - 재생성 `capstone_kafka`·`capstone_otel_collector`(2026-09-18 18:29 UTC, `kafka_data` bind mount 라 토픽 유지) · Prometheus HUP 리로드 · `mk2-ws-echo` 재기동 18:29:41 UTC(pid 1417769).
   - ufw: `[38]` 8765 ← 컴퓨터 `/32` 한 줄(엣지 `/32` 3줄 삭제). **남긴 것(의도):** 8765 두 주소 + `MK2_WS_TOKEN`/`MK2_EDGE_TOKEN` · ufw 8765 · 유닛 `After/Wants=tailscaled`(md5 `f77a9585…`) · `MK2_MEDIA_SNDBUF` 기본 65536 · Prometheus digest · `LoggingHandler` · compose 주석 정정 2곳.
   - 컴퓨터: Windows 방화벽 규칙 삭제됨 · `C:\Users\asdfa\physical mk2\edge_probe\`(≈1GB — exe 2·압축본 2·`data\` 134MB·`moved\`) 삭제됨(`Test-Path` False) · 단계 7 터미널 5개 전부 닫음(컴퓨터 상주물 없음) · 원본 4개는 `_serverinfo/edge_probe_260919/` · venv 추가 패키지 4종(websockets·confluent-kafka·imageio-ffmpeg·instrumentation-logging)은 개발 의존성이라 남긴다(되돌림 목록 밖).
   - 저장소: 작업본 `infra/docker-compose.yml`·`infra/config/prometheus.yml`(gitignore) · `_serverinfo/260919_phase4_단계8_되돌림.txt`(실값) · 이 작업로그의 주소 치환(2(c)).
   - §8 문서 갱신(단계 9 전)으로 넘긴 것: `infra/README.md` §3 포트 표의 8766·9095·`<서버 tailscale IP>:4316` 을 「닫힘/주석」으로, §5 「EDGE 리스너」 절에 현재 주석 상태 + 되살리기(넷이 한 묶음) 절차, §6 되돌림 행 · 추적표 BE-T-08「현재 통제 수준」·BE-T-02 등(단계 7 항목 4 그대로) · 02-media-path 「실측」 절에 (a)·단계 7 값.
   - 관측 잔여(되돌림 목록 밖, 손대지 않음): Prometheus `agg_layer="edge"` stale 시계열 · Loki/Tempo `hw-sensor-node` · TSDB `edge-kchk-…` 1행 · Loki `be-gateway` 회계 줄.
   - **결정 대기(사용자):** 커밋된 지시서 233·245행의 tailscale 주소 2개를 자리표시자로 바꿀지 — 제안: 단계 10 뒤 `_archive/` 이동 커밋에서 같이 치환(이력에는 남지만 현재 트리에서는 사라진다).

5. 이번에 검증하지 못한 것
   - 되돌린 뒤 미디어 중계 경로 자체는 다시 돌리지 않았다 — 8766 이 닫혀 relay 12건 skip 이 DoD 8-4 의 정의. 단계 7 이 마지막 실측.
   - Kafka 재생성 뒤 소비자 그룹 LAG 는 따로 보지 않았다 — pytest 파이프라인 왕복(244 passed) 통과로 갈음.

---

## §8 문서 갱신(단계 8 뒤 · 단계 9 전 — DoD 9-6 「02-media-path 커밋이 회신보다 먼저」의 선행) — 대상 8 + 확인 1 전부 처리

1. 지시서와 다르게 한 것
   - **plan 의 Phase 4 는 「✅ 완료」가 아니라 「🔄 진행 중(단계 0~8 완료)」로 적었다** — 단계 9(회신·통지 3건)·단계 10(4b)이 남아 있어 완료로 적으면 거짓이 된다. Phase 종료 시 ✅ 로 바꾼다. 같은 이유로 4b 관련 항목(`capture.py`·`mk2-capture`·8767·`MK2_CAPTURE_*`·`media_capture` DDL)은 CLAUDE.md·infra/README·추적표 BE-S-09·02-media-path §1-3-4 에 **「단계 10 예정」으로 자리만** 적었다(없는 파일을 있다고 쓰지 않는다). 단계 10 뒤 링크로 바꾼다.
   - plan §4 의 「Phase 4 가 새로 보내는 회신·통지」 행 3개(HW §8 · AI 통지 신설 · VZ 통지 → 회신 대기 5항목)를 **단계 9 발송 전에 미리** 만들었다(지시서 §8 plan 행 ②). 단계 9 에서 문서 링크만 붙인다.
   - 추적표 BE-T-08 은 지시서가 요구한 「7-h 결과·현재 통제 수준·Kafka 만 평문인 이유」에 더해 **BE-T-08 메모의 「브라우저는 tailnet 밖」을 결정 5 로 정정**했다(그 메모가 그대로면 통지와 어긋난다). BE-T-05 는 상태를 올리지 않고(미착수) MAC 명시 + Phase 4 물리 경로 실증만 적었다 — "사설 IP 중계"의 구현이라 부를 만한 코드가 없다.
   - 02-media-path 는 지시서 §8 행이 열거한 문장 12곳 외에 **§1-0 그림·핵심 성질·§0-3·§1-4-1 실측 박스·§1-4-8·§2 예상 질문·§3-3 체크리스트·§4 매핑표**도 같이 고쳤다 — 열거된 문장만 고치면 앞뒤가 서로 모순된다(그림은 JPEG·WSS 인데 본문은 native·ws). 고친 자리마다 **[Phase 4 정정]** 표시.
   - 00-architecture §8-5 의 BE-S-09 행을 **둘로 갈랐다**(스트림 저장·이벤트 캡처 = 유예 유지 / 촬영본 #15 = 유예 해제) — 한 행에 "유예"와 "해제"가 같이 있으면 표 규칙(발동 조건)이 서지 않는다. 「개발 프로파일 스트림 탭 카드」는 스트림 행에 넣었다.
   - Phase 1 지시서(`작업지시_phase1_얇은파이프라인관통.md` 358·422행)의 epoch ms 서술은 지우지 않고 **결정 2 결과를 인용 박스로 덧붙였다**(지시서는 당시 기록이라 본문을 고치지 않는다).

2. 발견한 것
   - **(a) plan 의 2026-09-10 정정 박스(*"실측이 반증했다 — ufw 를 열자 수집이 정상화됐다"*)가 infra/README §5 와 달리 아직 구판이었다.** 증상: 같은 사실을 두 문서가 반대로 적고 있었다(README 는 단계 6-b 에서 재정정, plan 은 그대로). 원인: 단계 6-b 갱신 대상이 README 뿐이었다. 조치: plan Phase 4 항목에 **재정정 박스**(두 경로 — 컨테이너發 `INPUT` / 외부發 `FORWARD`, 도커 포트 통제 = 바인딩 주소)를 넣고 README §5 를 가리키게 했다.
   - **(b) 추적표에 적으려던 테스트 이름 두 개가 실재하지 않았다** — `test_frame_ref_byte_identical`류·`test_mask_token`류. `grep '^def test_'` 로 대조해 실제 이름(`test_fast_viewer_receives_every_au_bytes_identical`·`test_frame_ref_bytes_survive_relay_path`)으로 바꾸고, **토큰 로그 마스킹은 pytest 가 없다**(journal·Loki 수동 대조뿐)고 명시했다. 없는 테스트를 근거로 쓰지 않는다(규율).
   - (c) 계수 정정: A층 계기는 9 + 미디어 5(`media_frames`·`media_gop_cut`·`media_coldstart`·`media_rejected`·`media_ingress`) = **14**(`grep -o 'be\.gateway\.[a-z_]+'` 로 확인 — `clients` 는 기존 것에 `endpoint` 라벨만 더해짐). 라벨 테스트는 45 → **58**. 요구사항 총계 **완료 3 · 부분 12 · 미착수 32**(BE-T-07·BE-C-03·BE-T-08 미착수 → 부분).
   - (d) 실주소 잔여: 커밋된 지시서 두 파일에 남아 있다 — `작업지시_phase4_미디어경로.md` 233·245행(tailscale 2개), `작업지시_phase3_관측파이프라인.md` 271행(컴퓨터 공인 IP·사설 IP). 이번 갱신 문서 8개에는 0건(`grep` 확인). `210.110.250.33`(서버 공인 IP, Mosquitto 기본값)은 `settings.py`·Phase 1 지시서·보고서에 이미 여러 번 커밋돼 있어 이번 범위에서 다루지 않았다.

3. 내가 결정해 준 것 — 그 밖 없음(결정 대기 1건은 4번)

4. 이 단계가 서버·저장소에 남긴 것
   - 서버: 변경 없음.
   - 저장소(전부 커밋 대상, `git check-ignore` 0건): `docs/be/02-media-path.md`(머리말 구현 상태 박스 · §1-1-1 신설 · §1-2-2/1-2-3/1-3-1/1-3-3(구현·실측 절)/1-3-4/1-5-0/1-5-2/1-5-3/1-5-4/1-6-1/1-6-2(예시·헤더 표)/1-6-3 · §2·§3·§4) · `docs/be/requirement-traceability.md`(요약 총계 · BE-C-03·T-02·T-03·T-05·T-06·T-07·T-08·S-02·S-03·S-06·S-09) · `docs/be/01-standalone-implementation-plan.md`(§1 Tier B · §2 한 줄 · Phase 4 절 전면 · Phase 5/6/7 「Phase 4 이월」 · §3 ⑥⑦ · §4 행 6개) · `CLAUDE.md`(명령 블록 미디어 fixture · 환경변수 · 폴더 블록 + **저장소↔서버 경로 대응표** · 원칙 3 프로파일 · 금지 라벨 15종 · enum 금지 규약) · `README.md`(총계·구조·그림·포트 3행·저장 결정·할 일·남은 검증) · `docs/be/00-architecture.md`(§2 그림·성질 · §7-4 `video_meta` 실체 · §7-5 정정 · §8-5 행 5개) · `infra/README.md`(§1 동기화 절차 7단계 + 되돌림 판 · §3 3행 + 현재 상태 · §5 EDGE 현재 상태·7-h 결과·§5-0 4행 · §6 2행 · 상주 유닛 행) · `docs/be/tasks/작업지시_phase1_얇은파이프라인관통.md`(결정 2 인용 2곳) · `contracts/common/README.md` 는 단계 1 에서 완료(확인만) · `설계_규칙.md` §3·`작업지시_템플릿.md` §8 「명세서 문서」 행 실재 확인(구현방 확인만).
   - 단계 9 로 넘긴 것: plan §4 회신 행 3개의 문서 링크 · 추적표 BE-C-03 gap ①(AI·VZ 회신) 상태 · `hw-envelope-conformance.md` §5-1 표 갱신(단계 9 DoD 9-2).
   - 단계 10 으로 넘긴 것: 4b 파일 링크(CLAUDE.md 폴더 블록·infra/README §3 8767 행·추적표 BE-S-09·02-media-path §1-3-4 끝 문장).
   - Phase 종료 시: plan Phase 4 「🔄 진행 중」→ ✅ · README「⑥ 진행 중」→ 완료 · 추적표 요약 날짜.
   - **결정 대기(사용자, 단계 8 에서 이월):** 커밋된 지시서 두 파일의 실주소(phase4 233·245행 tailscale 2개 · phase3 271행 컴퓨터 공인·사설 IP)를 자리표시자로 바꿀지 — 제안: 단계 10 뒤 `_archive/` 이동 커밋에서 같이.

5. 이번에 검증하지 못한 것
   - 문서 간 상호 링크(`../../backend/gateway/media.py` 등 상대 경로)는 파일 존재만 확인했고 렌더링은 보지 않았다.
   - 02-media-path §1-3-3 의 지연 예산 추정치(경로 A/B 표)는 손대지 않았다 — Phase 4 실측은 직접 경로 한 점(age p50 7ms)뿐이라 표를 실측으로 바꿀 재료가 못 된다.

---

## 단계 9 — DoD 전건 통과(9-1 §8 8-0~8-12 · 9-2 §5-0/§5-1 이동 + `BACKEND_AGENDA §…` 출처 · 9-3 VZ 13항목 + — 행 + §9 관측 답 · 9-4 실주소·토큰 0건 + `git status` 가시 · 9-5 AI 통지 + plan 진나영 행 연결 · 9-6 `02-media-path` 커밋 `7265281` 이 회신 문서보다 앞섬)

1. 지시서와 다르게 한 것
   - **인용한 `파일:줄` 을 지시서에서 그대로 옮기지 않고 HW 0914·VZ 0916 사본에서 `grep` 으로 전부 재확인**했다. 그 결과 위치가 다른 것 하나 — 부록 A 8-10 ⑤의 `started_at` before/after 코드는 `capture_upload.py` 가 아니라 **`pi/bench/go1_capture_teleop.py:250` · `go1_scan_capture.py:268`**(`capture_upload.py:75` 는 그 값을 읽기만 한다). 회신에는 실제 위치를 적었다.
   - HW §8 에 **8-12 「이 회신의 근거(실측)」 절**을 더했다(부록 A 골격에 없음 — §7-7 선례). 「HW가 할 일」은 부록 A 의 ①~⑧ 을 **15~24**로(⑧ 「최신 push」 뒤에 24 「SDD/SRS 문구 정정」을 하나 더). §5-1 에 **회신 대기 신설 3행**(`stream` 우회 수용 · imageai 촬영 시각 · IDR 간격) 추가. `<a id="s7-8">` 앵커가 없어서 추가.
   - VZ 통지의 「—」 요약행은 **§14** 로, 부록 B 에 없는 **§0 한 장 요약**을 앞에 두었다(다른 두 통지의 선례). AI 통지도 §0 + 「AI가 할 일」.
   - 부록 B 6번 ⑤ `alignment` 의 VZ 쪽 자리를 `resolveAlignment(buffer, aligned, frame)` 의 `aligned` 인자로 **"보인다"** 고만 적었다(확정 아님 — VZ 가 답할 몫).

2. 발견한 것
   - **(a) VZ `gateway/config.ts` 캐시 정책은 18채널이다** — 캐시 8(우리 7 + `coverage`) / 비캐시 10(우리 금지 3 + `heartbeat`·`metrics`·`risk_state`·`ai_failure`·`trace_event`·`robot_state`·`detection`).
     실측(원문): `config.ts:137` `// ── 캐시한다 (7)` · `:146` `// ── 캐시하지 않는다 — 재생되면 화면이 거짓말을 한다 (3)` · `:151` `(2)` · `:159` `coverage: { cache: true, …}` · `:163-164` `robot_state`·`detection` `cache: false`.
     해석: 우리 10종(`00-architecture.md` §7-4)은 **한 건도 어긋나지 않는다**. 헤더 주석 「7/3」은 낡은 숫자(실제 8/10). 통지 §14 에 그대로.
   - **(b) HW `edge/availability.py` 가 Prometheus 예약 지표 `up` 을 발행하는 것을 코드로 확인.** 실측(원문): `:161` `"# HELP up 말단 생사 — 엣지가 하트비트·LWT 로 파생 (push 전용 말단은 scrape 불가)"` · `:162` `"# TYPE up gauge"` · `:171` `lines.append(f"up{label} …")`. 조치: §8-9 ⑥ 개명 요청(`hw_entity_up`) — 근거로 HW `SRS.md` §9.8(`:715`) 인용.
   - **(c) `stream` 호출 불가는 코드 그대로다.** 실측(원문): `robot_node.py:196-199` `if action == "stream": a = params.get("action"); if a not in ("start", "stop"): raise CommandError("INVALID_ARGUMENT", "invalid_stream_action")` · `physical_command.proto:32` `map<string, double> parameters = 4;` · `:575-577` `"stream": _act_stream` / `PHYSICAL_ACTIONS = frozenset({…, "stream"})`. 우회 근거 `mission-command.md:59-62`·`robot_node.py:333`.
   - (d) VZ 쪽 근거(원문): `WsTransport.ts:178` `msg = JSON.parse(String(ev.data))` · `:179-181` `catch { return; }` · `vision.ts:84` `frame_ref: number;` · `:240-241` `frameAt(seq) … f.frame_seq === seq` · `:221` `if (tier !== 'device' && tier !== 'edge') return;` · `:47` `origin: 'top-left';` · `:277-278` `space.format === 'normalized'` · `gateway/vision.ts:142-143` *"정수 frame_seq로 가정한다"* · `presets.ts:10` *"전부 테일넷으로 붙는다"*. ⚠ VZ `presets.ts:30` 에 AI 데스크톱의 tailnet IP 가 들어 있다 — 우리 문서에 옮기지 않았다.
   - (e) 실주소·토큰: 세 문서 `grep` 0건. `git status` 에 두 신설 파일 보임(`check-ignore` 미해당). `02-media-path.md` 마지막 커밋 `7265281`, 회신 문서는 미커밋 → 커밋 순서가 DoD 9-6 대로 된다.

3. 내가 결정해 준 것 — 그 밖 없음

4. 이 단계가 서버·저장소에 남긴 것
   - 서버: 변경 없음.
   - 저장소(신설 2·수정 4): `docs/be/hw-envelope-conformance.md`(§8 8-0~8-12 · 머리말 표·📌 · §5-0 에 6행 이동 · §5-1 출처 접두사 + 신규 3행 · §3 함정 박스에 `stream` 추가 · `s7-8` 앵커) · **`docs/be/vz-media-interface.md`**(§0~§14 + 할 일 8) · **`docs/be/ai-detections-interface.md`**(§0~§6 + 할 일 6) · `01-standalone-implementation-plan.md` §4(HW·AI·VZ 3행 ✅ 발송 + 링크, Phase 4 절 「단계 9 ✅」) · `requirement-traceability.md` BE-C-03 gap ①(통지 링크) · 작업로그.
   - **회신 대기 목록(다음 지시서 재료):** HW 3(`stream_start/stop` 수용 · imageai 촬영 시각 · IDR 간격 조절) + HW 할 일 15~24 · VZ §10 5개(tailnet 주소 · CORS origin/exporter · https · `source_id` 확인 · 발화 원문 키) + §6 규격 이름 선택 + VZ-C-07 문면 · AI 2(§1 규격 초안 · §4 7864 세 물음).
   - 커밋 대기: 위 6개 파일(사용자가 커밋). 커밋 메시지 제안: `Phase 4 단계 9 — HW §8 회신 · VZ 미디어 통지 · AI 탐지 통지`.

5. 이번에 검증하지 못한 것
   - 상대 파트가 문서를 실제로 받았는지·읽었는지(전달은 사용자 몫). 마크다운 앵커(`#s8-2` 등) 렌더링은 보지 않았다.
   - VZ `resolveAlignment` 의 `aligned` 인자가 `alignment` 를 받는 자리인지는 추정(위 1번).

---

## 단계 10 — DoD 전건 통과(10-1 저장·행 1·매니페스트 원본 · 10-2 kind 보존 · 10-3 멱등 · 10-4 문서 · C1~C5 · 0-6 델타 · 서버 pytest 272 passed + 12 skip) — 4b 촬영본 저장소 개통(8767, `mk2-capture`)

1. 지시서와 다르게 한 것
   - **환경변수 하나 추가 — `MK2_CAPTURE_HOST`**(부록 C에 없음). 8766 의 `MK2_MEDIA_INGEST_HOST` 와 같은 이유(tailscale 단일 주소 바인딩을 `.env` 로). `MK2_CAPTURE_URL`(업로더·tests 용)도 부록 C 밖이지만 `MK2_MEDIA_INGEST_URL` 선례.
   - `MK2_CAPTURE_DIR` 기본값을 지시서의 서버 절대 경로가 아니라 **`media_capture`(실행 디렉터리 기준, gitignore)** 로 두고 서버는 `.env` 로 절대 경로를 준다 — 부록 C 「문서에 실제 경로를 적지 않는다」와 코드 기본값에 서버 경로를 박지 않는 규율.
   - 매니페스트 짝짓기를 **디스크 대기 파일**(`.incoming/<세션>.manifest.json`)로 했다 — 지시서는 "먼저 받아 파싱"만 적었는데, 프로세스가 재기동돼도 짝이 살아남게(메모리면 사라진다). 아카이브 임시 파일도 같은 디렉터리(루트와 같은 파일시스템이라 rename 이 원자적).
   - **실제 삭제 경로는 함수(`purge_sessions(dry_run=False)`)까지만 두고 CLI 에 켜지 않았다**(지시서 10-4 "dry-run 으로 선정만") — `mark_purged` SQL 은 있으나 이번엔 돌리지 않았다(아래 5번).
   - 테스트를 **프로세스 안 임시 서버**로 짰다(임의 포트, 임시 루트). MySQL 이 닿으면 실제 `media_capture`, 아니면 `FakeStore`(UNIQUE 만 흉내) — 파일 규칙은 어느 쪽에서도 같은 코드가 돈다. 실제 유닛(8767)은 서버에서 업로더 5회로 판정.
   - 인식 안내로 넘기려던 「깨진 매니페스트 재생성」(HW 규율 구멍)은 우리 수신단에선 **깨진 JSON 을 400 으로 거부**한다(재생성하지 않는다).
   - DoD 0-6 델타 절차에 `registry-declared-vs-observed.sql` md5 대조를 넣었다(서버 `d028325c…` 5,184B ≠ 저장소 `docs/be/queries/` `84631303…` 6,026B — 서버 것은 `mk2_sql/` 에 따로 있는 옛 사본, 기록만).

2. 발견한 것
   - **(a) MySQL `JSON` 칼럼은 값을 보존하지 수의 표기까지 보존하지 않는다 — 간헐 실패.**
     실측(원문): 단일 실행 `1 failed, 27 passed in 0.84s` — `test_10_1` 의 `json.loads(row["manifest"]) == manifest` 에서 `{'frames': {'uri': None, 'bytes': 147584, 'count': 6, 'naming': 'frame_%06d.jpg', ...}} != {'frames': {'count': 6, 'bytes': 147584, 'naming': 'frame_%06d.jpg', 't0_unix': 1789762836.2713063, ...}}`; 같은 테스트가 전건 실행에서는 통과(`272 passed, 12 skipped in 126.58s`).
     원인: `t0_unix = time.time()`(17자리 float)를 MySQL 이 double 로 저장 뒤 다시 찍으면 마지막 자리가 달라질 수 있다 — 회차마다 float 자릿수가 달라 간헐.
     조치: 테스트를 「키 전부 동일 + 값은 float 를 ms 까지」 비교로(`tests/test_capture_upload.py` md5 `d8adeeaeb8f90d80b854225b25bc45a0`, 컴퓨터 28 passed). 코드·DDL 무변경 — 원본 바이트가 필요해지면 별도 칼럼(문서 §1-3-4 에 적음). 서버 재복사 뒤 3회 통과 확인 대기.
   - **(b) 내 실측 명령 두 곳이 틀렸다(코드 결함 아님).**
     ① md5 비교: `053b19437be212dea37e0bdca24ed5fb /tmp/cap1/staged/20260919-cap1.tar.gz` ≠ `a41c581f1d0996ec77045508f9381a48 …/go1-001/20260919-cap1.tar.gz` — 업로더를 같은 `--out` 으로 두 번 돌려 **두 번째 실행이 staged 를 다시 묶어 덮어썼다**(두 번째 `archive_bytes 126820` ≠ 첫 번째 126839). 저장본 126839B = 첫 업로드 = 행 `archive_bytes`. 두 번째(다른 바이트)가 저장본을 **덮어쓰지 않은 것**이 10-3 의 증거. 바이트 동일 판정은 pytest 10-1 이 프로세스 안에서 했다.
     ② dry-run `--max-bytes 1000` 이 둘 다 선정: `total_bytes 295249` > 1000 이라 cap1(126839) 을 빼도 168410 > 1000 → 둘 다 고르는 것이 맞다. "cap1 만" 은 내 기대값 오류(`--max-bytes 170000` 이어야).
   - (c) 값(원문): DDL `media_capture | utf8mb4_bin` · `GRANT SELECT, INSERT, UPDATE ON \`mk2\`.\`media_capture\`` · `mission_event.actor_kind | NO | varchar(32)` · 테이블 8 → 9 · 유닛 `ActiveState=active NRestarts=0`, `READY — 촬영본 입구: http://<서버 tailscale IP>:8767/capture (토큰 있음, dir=…/media_capture, max_bytes=107374182400)` · `ss` `<서버 tailscale IP>:8767` · ufw `[39] 8767/tcp ALLOW IN <pi7 tailscale IP> # mk2 phase4 capture (pi7) - stays`(`tailscale status` 에서 pi7 = offline, last seen 14h) · cap1 `201 stored 126839` → 재전송 `200 already_stored` · cap2 `scan_capture_session 201 168410` · cap3 `409 manifest_required` · cap4 `401 unauthorized` · 저장 루트 `go1-001/20260919-cap1.tar.gz 126839`·`cap2.tar.gz 168410`, `.incoming/` 비어 있음 · MySQL 2행(cap1 `frames 6 started_at 2026-09-18 20:20:04.655178`, cap2 `frames 8 started_at NULL`, `purged_at NULL`) · journal 에 토큰·쿼리 미출력 · 전건 `272 passed, 12 skipped in 126.58s`(244 + 28).
   - (d) S3 의 `echo "생성된 토큰: $TOK"` 때문에 **토큰이 사용자 로그 파일에 찍혔다**(저장소 밖). `_serverinfo/` 에도 적지 않았다. 회전은 `.env` 12행 교체 + `restart mk2-capture` 한 줄 — 사용자 판단. 다음부터 토큰 생성 명령은 화면에 찍지 않는다.

3. 내가 결정해 준 것
   - 물음(내 제안): 지시서를 `docs/be/tasks/_archive/` 로 옮긴다(이월 목록에 있던 것). 답(사용자, 2026-09-19): *"`/_archive`로 옮기지 않을 거야. 그냥 그대로 위치 유지."* → 이동 안 함, 보고서 링크를 제자리로. 이어서 *"`_archive`로 옮긴다. 이런 규칙만 백지화할 거야"* → 규칙이 적힌 5곳 수정(`작업지시_템플릿.md` 머리말 · `설계방_진입가이드.md` 2곳 · phase1·phase2·phase4 지시서 머리말). reports 의 과거 기록은 그대로.
   - 물음: 커밋된 지시서의 실주소 3줄을 자리표시자로 바꿀지(단계 8 이월). 답(사용자): *"자리표시자 치환하자. 실주소 3줄."* → phase4 지시서 233·245행(서버·컴퓨터 tailscale) · phase3 지시서 271행(컴퓨터 공인 IP·사설 IP) 치환. (같은 파일 148행의 사설 IP 하나는 사용자가 **남기기로** 했다.)
   - 물음: 4b 토큰이 사용자 로그 파일에 찍힌 것. 답(사용자): 저장소 밖이라 push 되지 않으니 안 고친다 → 회전 없음.

4. 이 단계가 서버·저장소에 남긴 것
   - 서버: MySQL `mk2.media_capture`(DDL+GRANT root 1회) · `mk2_sql/mk2_media_capture.sql`·`mk2_grants_mysql.sql.example`(저장소 판) · **`mk2_sql/mk2_mysql_schema.sql` 을 저장소 판(`92c8ed1f…`)으로 교체**(백업 `.bak_before_phase4`, 09-14 판 `bce11cbc…`) — DDL 재실행 안 함 · `.env` 9~12행 `MK2_CAPTURE_HOST/PORT/TOKEN/DIR` · 저장 루트 `/home/dg/capstone-db/media_capture/`(cap1·cap2 아카이브 2개 + `.incoming/`) · `/etc/systemd/system/mk2-capture.service`(enabled, 20:18 UTC) · ufw `[39]` 8767 ← pi7 `/32`(**남긴다**) · 서버 저장소 사본: `backend/gateway/capture.py`·`backend/settings.py`·`tests/capture_uploader.py`·`tests/test_capture_upload.py`(수정본 재복사 대기)·`infra/systemd/mk2-capture.service` · `/tmp/cap1~4` 임시.
   - **남기는 것(의도, 되돌림 대상 아님 — 결정 9 ㄴ):** 8767 입구·유닛·ufw pi7 규칙·테이블·`.env` 4행.
   - 저장소: `backend/gateway/capture.py`·`infra/sql/mk2_media_capture.sql`·`infra/systemd/mk2-capture.service`·`tests/capture_uploader.py`·`tests/test_capture_upload.py`(신설 5) · `backend/settings.py`(4b 표·함수 6)·`infra/sql/mk2_grants_mysql.sql.example`(6절)·`.gitignore`(`media_capture/`) · 문서: `CLAUDE.md`(폴더·명령·환경변수·유닛 4개) · `infra/README.md`(§1 유닛 4·sql 행 · §2-0·§2-1 테이블 9 · §3 8767 행·현재 상태 · §6 유닛·`media_capture` 행) · 추적표(BE-S-09 미착수→**부분**, 총계 부분 13·미착수 31, BE-S-02 A층 15·`be-capture`) · `02-media-path.md` §1-3-4 구현 문단 · `00-architecture.md` §8-5 촬영본 행 · plan(Phase 4 ✅·단계 10 ✅·§3 ⑥ [x]·⑦ 다음) · `README.md`(총계·포트·MySQL 행·할 일) · `hw-envelope-conformance.md` §8-0/8-8/8-10/8-12(개통됨) · `_serverinfo/260919_phase4_단계10_실측.txt`.
   - 회신 대기에 더한 것 없음(HW 19·20 은 "첫 실물 업로드 전").

5. 이번에 검증하지 못한 것
   - **실물 HW 촬영본 0건** — 합성 업로더뿐(pi7 offline). URL·매니페스트가 HW 실물과 같은 모양인지는 `capture_upload.py` 0914 사본 대조로만.
   - **실제 삭제(`purge_sessions(dry_run=False)`·`mark_purged`)는 돌리지 않았다** — 지시서 범위(선정만). 운영 창에서 켤 때 첫 실행을 dry-run 과 대조한다.
   - pi7 → 서버 8767 도달(ufw `/32` 실효)은 pi7 이 offline 이라 못 봤다 — 서버 자신(lo 경유) 실측뿐.
   - 대용량(수백 MB) 아카이브의 청크 수신·`Content-Length` 불일치는 소형(126KB)·C4 절단 테스트로만.
   - ~~테스트 수정본(`d8adeeae…`) 서버 3회 통과 — 재복사 뒤 확인 대기~~ → ✅ 서버 `md5 d8adeeaeb8f90d80b854225b25bc45a0` · `28 passed in 0.84s / 0.82s / 0.81s`(2026-09-19). 간헐 실패 해소.


---

## Phase 종료 후 정정 (2026-09-19) — 산출물 전수 점검에서 나온 것

> 머리말 규칙(「앞 단계 기록은 고치지 않고 뒤에만 붙인다」)대로 앞 항목은 그대로 두고 여기에 적는다.
> 계기는 Phase 4 산출물 전수 대조(보고서·작업로그·회신 3종·정정 문서 9종 + `git grep`·링크 검사).

1. 지시서와 다르게 한 것 — 그 밖 없음

2. 발견한 것
   - **(a) 🔴 단계 8 「(d) 실주소 잔여」의 위치 기록이 틀렸다.**
     증상: 그 항목이 *"`작업지시_phase4_미디어경로.md` 233·245행(tailscale 2개)"* 이라 적었는데, 단계 10에서
     치환한 뒤 `git grep` 으로 다시 세어 보니 **그 두 줄에는 애초에 실주소가 없었다**(자리표시자였다).
     실측(원문, `git grep -nE '100\.(6[4-9]|[7-9][0-9]|1[0-1][0-9]|12[0-7])\.'`): 남아 있던 곳은
     `docs/be/tasks/작업지시_phase4_미디어경로.md:213`(`pi4`·`pi6` tailscale 2개) 과
     `reports/2026-09-18_1700_phase4_지시서_합본수정안.md:329`(같은 2개) **둘**이며, 뒤의 것은 단계 8·10 집계에
     **아예 들어 있지 않았다**.
     원인: 줄 번호를 실측이 아니라 기억으로 적었고, 치환 대상을 정할 때 `git grep` 으로 전수 대조하지 않았다.
     제약 11 의 판정 수단이 「목록 대조」였던 것이 문제다 — **판정 수단은 `git grep` 이어야 한다.**
     조치: 두 곳 모두 `(주소는 `_serverinfo/`)` 로 치환. 재조회 **0건**. 앞으로 실주소 판정은 목록이 아니라
     `git grep` 결과로 한다.
   - **(b) 단계 8 「(c) 계수 정정」의 A층 계기 14 는 단계 8 시점 값이다 — 최종은 15.**
     단계 10 에서 `be.gateway.capture{outcome}`(4b) 하나가 늘어 **A층 15종**이 됐다(추적표 BE-S-02·보고서
     「서버에 남은 것」과 같은 값). 지표 **이름** 수로는 `be_*` 35(`media_coldstart` 히스토그램이 `_bucket`·
     `_sum`·`_count` 로 갈린다). 보고서를 조립할 때 14 를 집어 쓰지 않도록 여기에 잇는다.
   - **(c) 나머지 정정 대상은 전부 「단계 8 이후에 단계 10 이 바꾼 사실이 안 내려간 것」이었다** —
     `02-media-path.md` 실측 박스(8765만 남김 → 8765·8767 둘) · `hw-envelope-conformance.md` §5 표 두 줄
     (4b 입구 「완료 후 통지」 → 개통) · 같은 문서 머리말의 「HW가 할 일 15~22」(실제 15~24) ·
     `infra/README.md`·plan 의 pytest 기준선 184 · `infra/README.md` 의 `be_*` 21종 ·
     `infra/README.md`·추적표가 4318 을 「도커 발행 포트」로 적은 것(발행하지 않았다) ·
     `contracts/common/README.md` A층 라벨의 `endpoint` 누락 · plan §4 의 「발송」(= 작성이지 전달이 아니다).
     **문서 간 링크 68건·요구사항 총계·금지 라벨 15종·라벨 테스트 58·포트 상태는 전부 일치했다.**

4. 이 정정이 저장소에 남긴 것
   - 고친 문서 9개: `docs/be/02-media-path.md` · `docs/be/hw-envelope-conformance.md`(§5-0·§5-1·머리말·근거·
     §8-10 항목 25 신설) · `docs/be/01-standalone-implementation-plan.md`(§4 세 행·pytest 기준선) ·
     `docs/be/requirement-traceability.md` · `docs/be/vz-media-interface.md`(근거 272) · `infra/README.md` ·
     `contracts/common/README.md` · `docs/be/설계방_진입가이드.md`(7-a 회신 목록 갱신 · **아침 메모 규칙 삭제**
     — 존재하지 않는 `설계_규칙.md` §7 을 가리키는 끊어진 참조였고 실제로 만들지 않고 있다) ·
     `reports/2026-09-19_0600_phase4_미디어경로.md`(발견 11·미결 표·TSDB 행).
   - 실주소 치환 2곳: `docs/be/tasks/작업지시_phase4_미디어경로.md:213` ·
     `reports/2026-09-18_1700_phase4_지시서_합본수정안.md:329`.
   - HW 회신 대기가 3건 → **4건**이 됐다: **실 스트림의 장당 바이트·초당 장수**(§8-10 항목 25) —
     `MK2_MEDIA_SNDBUF` 하향 여부(Phase 5 이월 결정)의 유일한 입력값인데 물어 둔 적이 없었다.
   - 서버: 변경 없음.

5. 이번에 검증하지 못한 것
   - **TSDB 행 수·채널별 분포**(보고서 「서버에 남은 것」 TSDB 행) — 여전히 미측정이다. 서버 조회가 필요해
     여기서 채우지 않았고, **Phase 5 단계 0 의 첫 조회 항목**으로 보고서에 명령까지 적어 두었다.
   - 상대 파트가 회신 3종을 실제로 받았는지 — 단계 9 그대로(전달은 사용자 몫).
