# 사이트 개선 4건 — 사용자가 어느 버튼을 눌러야 하는지 보이게

**작업 지시**: `작업프롬프트_사이트개선_260831.md`
**기준 커밋**: `6b07d13` (지시서 커밋 · 8/31 점검 반영분 `58db598` 포함. 지시서가 말한 미커밋
5파일·`_to_delete/`·`index.lock`은 착수 시점에 이미 커밋·정리돼 있었다 — 트리 깨끗)
**날짜**: 2026-08-31 · 착수 순서 4 → 2 → 3 → 1 (지시서 그대로)

---

## 요구 4 — 우상단 모드 표시·전환 ✅

- **`src/shell/ModeSwitch.tsx` 신설** — `모드 [일반] [시나리오 ▾] [목·개발]`. 일반 =
  `exitScenarioRender` + placeholder + 게이트웨이 `script_close`. 시나리오 ▾ = 대본 3편
  드롭다운 → **정지 미리보기**. 목·개발 = mock (붉은 배지 유지 — 끄는 경로 안 만듦).
  상태 표기: `시나리오 · 260831-03 정지 / 재생 중`.
- **승인 선을 우회하지 않는다** — `previewMission()`(`src/data/scenario.ts` · headSec 0 ·
  playing false · activatedBy 'preview')은 탭①을 전부 pending 으로 그리고, 게이트웨이
  **`script_preview`**(`ScriptEngine`)는 `worldTimeline` 의 **`atSec === 0` 프레임만** 반영한다.
  trace_event·commands 0건, 타이머 없음 — `verify:scenario-mode` 가 실왕복으로 강제한다.
  반영 경로는 재생과 같은 `applyWorldFrame()` 하나로 뽑았다(두 벌 방지).
- `ScenarioRender.playing` 추가 — 정지 화면을 「재생 중」이라고 적지 않는다.
  `DEFAULT_MODE = 'placeholder'` 무변경 (`verify:placeholder-default` 통과).
- 배너 3종(목·대본·구판)은 **한 줄로** 줄였다 — 상태는 세그먼트가 말한다. 목 배너의 경고만 남김.

## 요구 2 — 탭②~⑤ 시나리오 연계 ✅

- **「임시 데이터」 = 대본 t=0 스냅샷** — 지어내지 않는다. 정지 화면과 동영상의 관계다.
- **`src/scenarios/axes.ts` 신설** — 축을 **대본에서 유도**한다(손으로 적지 않는다):
  `position`/`speed`/`water`/`coverage`/`video` ← drive 키, `actuator`/`command` ← commands,
  `risk`/`observability` ← **어느 대본에도 없다**(AI 파트 VZ-I-08 · 평시 ObservabilityEmitter 몫).
- **`PendingSource` 넷째 상태** — `axis` 프롭. 시나리오 모드에서 대본이 그 축을 몰지 않으면
  「연결 예정」이 아니라 **「이 대본에는 해당 없음」+ 어느 편에서 보이는지**. 자리표시 렌더
  자체는 무변경(결정 5 — `pendingSources.ts` diff 0).
- 탭별: ② `zone-summary` 를 시나리오에서 **cast 기준 집계**(「대본 N · 자리표시 M」),
  `risk-state` → `axis="risk"`. ③ `TARGETS` 를 cast 로 좁히고 제어 자리들 `axis="actuator"/"command"`
  (1·2편 → 해당 없음, 3편은 현행 엔진 통과). ④ **시나리오 진입 시 기본 지표 자동 전환**
  (축 유도: 2편 커버리지 · 3편 수위 · 1편 로봇 속도), 무관 지표 흐리게(`btn--dim`),
  `metrics-push` → `axis="observability"`. ⑤ 1·2편 현행 경로 · 3편 → `axis="video"` 해당 없음.

## 요구 3 — 노드 분화 (대본 JSON) ✅

- **문법 5종**(`nodeKind`: sense·decide·act·verify·report)으로 마일스톤을 편다 — 지시서 표
  그대로 **1편 7→17 · 2편 7→18 · 3편 4→16 노드.** 단계를 더하거나 빼지 않았고, 실패 분기
  없고, `sensor-02` 이중화 노드 없음(결정 3). 노드 위에 문법 칩이 붙는다.
- **마일스톤 시작·끝 시각 보존** — 지시서 대조표(1편 2–20/22–32/34–44/46–90/92–98/100–104/
  106–110 등) 그대로 배치해 `statusesAt()` 접기·되감기 눈금·worldTimeline 정렬 불변.
  근거값은 원래 사건에서 지정된 노드로 이동(door_open→T-12b · distance_m→T-15a ·
  blind_cells→T-22b · last_scan_at_sec→T-24a/26a · rise/hold/fall→T-42b/43b/44b).
  3편 `commands[].taskId` → `T-43c`/`T-44c` (atSec 162·392 = 그 노드의 started 와 일치).
- **되돌아가는 엣지 = `refEdges`** (deps 아님) — 2편 `T-27c → T-23a` 재탐색 루프를
  점선 + `↺ 문구`로 그리기만 한다. `layout.depths()`에 **순환 방어선**(방문 스택) 추가 —
  데이터 실수 하나로 화면이 멎지 않는다. `verify:script-library` 에 refEdge(태스크 실재 ·
  label · **deps 중복 금지**)·nodeKind(5종) 검사 추가, 3편 근거값은 태스크 id 가 아니라
  **payload 키로** 찾게 바꿈(세분화에 견딤 — 대조군도 키 기반).
- **그래프 보기 범위** — DAG/트리 옆 `[이 마일스톤] [임무 전체]` 토글(기본 마일스톤 —
  전달본 흐름 유지). 마일스톤 밖으로 나가는 refEdge 는 그래프 아래 한 줄 + 「임무 전체로
  보기」 버튼. 캔버스 높이는 상수(390/690) 대신 **노드 위치에서 계산**한다.
- 지시서의 세부 판단 두 곳: 지시서 시간표에 「대기」가 없는 `T-11c`(1편)·`T-21b`(2편)는
  `evaluation` 을 선언하지 않았다(평가 태스크 = awaiting→done 전이 규칙과 일치).
  `T-21b` done 에는 video_meta 근거(`fps: 15`)를 실었다.

## 요구 1 — 설명문 → 우상단 `?` ✅

- **`src/shared/Explain.tsx` 신설** — 문구는 **원래 컴포넌트 자리에 그대로** 두고 그리는
  위치만 바꾼다. 통합 셸(ManualScope 제공)에서는 본문에 안 그리고 「지금 열려 있는 탭」의
  설명서로 등록(마운트 1회 · 내용은 ref로 최신). **단독 빌드(셸 없음)에서는 원래 문단
  그대로 그린다** — HCI 전달본 화면이 이 개편으로 바뀌지 않는다.
- **`src/shell/HelpOverlay.tsx` 신설** — `?` 버튼 → 현재 탭의 것만 모달(결정 4): 탭 제목 +
  한 줄 요약(손으로 씀) → 등록된 문단들 → 모드 3종 설명 → 개발 도구 표시 토글.
- 치환 **31곳**(`board__sub`·`note`·`note--dim`·`hint`·`stt-hint` — 탭① 포함 9파일).
  `global-bar` 안내 문단 2줄은 삭제하고 내용을 오버레이 공통 절로. 상태 의존
  문구(동적 className·notice)는 기능적 표시라 남겼다.
- **devpanel 6곳은 목·개발 모드에서만** — 셸 클래스(`app-shell--dev`) + CSS 규칙 하나로
  일괄 게이트(단독 빌드는 셸이 없어 무영향). `?` 오버레이 토글로 명시 on/off 가능.

## verify — 15종 전부 통과

| 갱신 | 내용 |
|---|---|
| `verify:placeholder-default` | 허용 목록 + `ModeSwitch.tsx` · 임시 디렉터리 정리를 EPERM 에 안전하게(try/catch) |
| `verify:scenario-mode` | 진입 둘(미리보기 정지/승인 재생 — `playing` 구분) · 끄는 경로 = 셸(닫기·모드 스위치) · **미리보기 실왕복**(t=0 프레임 반영 · trace 0건) · EPERM 안전 정리 |
| `verify:script-library` | refEdges(실재·label·deps 중복 금지) · nodeKind 5종 · 3편 근거를 payload 키로 |
| `verify:scenario` | 무수정 통과 — 옛 편은 손대지 않았다 |
| `verify:layout` | 통과 (노드 7개 검사 기준은 옛 편) |
| 신설 후보 `verify:no-cycle` | 별도 스크립트 대신 `verify:script-library` 의 refEdge-deps 중복 검사 + `depths()` 방어선으로 갈음 |

## 번들 크기

| | 착수 전(58db598 기준) | 완료 후 |
|---|---|---|
| 단독 JS | 279.86 kB | **299.09 kB (gzip 90.48)** |
| 통합 JS | 373.75 kB | **403.52 kB (gzip 122.64)** |

증분은 노드 분화된 대본 JSON(사건 54/72/53건) + ModeSwitch·HelpOverlay·Explain·axes.

## 이어받기 최종 점검

Claude Code 중단 후 현재 작업 트리에서 타입 검사와 검증 15종, 통합·단독 빌드를 다시 실행해
전부 통과시켰다. 실제 브라우저 렌더에서 공통 버튼 CSS가 모드 세그먼트 색상을 덮어 `일반` 글자가
보이지 않는 문제를 발견했고, `.global-bar .modeswitch__seg` 우선순위로 바로잡았다. 이식 CSS의
JavaScript식 `//` 주석도 표준 CSS 주석으로 바꿔 프로덕션 빌드 경고를 없앴다.

최종 화면: `reports/assets/site-improvement-260831-final.png`

## 건드리지 않은 것 (지시서 §건드리지 않는 것 대조)

`web-dashboard` diff 0 · `MSN-260826-01.json`+사이드카 무수정 · `pendingSources.ts` 무수정 ·
`DEFAULT_MODE='placeholder'` 무변경 · 단독 빌드 경계 유지(37파일 · 셸/탭 import 0건 —
미리보기 게이트웨이 연동은 셸 `ModeSwitch.tsx` 에만 있다).

## 판단이 들어간 곳 (지시서와 다르게 하거나 지시서가 비운 곳)

1. **devpanel 게이트를 CSS 한 규칙으로** — 여섯 파일에 조건부 렌더를 넣는 대신 셸 클래스
   하나로 가렸다. 단독 빌드(셸 없음)는 자동으로 무영향이라 전달본이 보존된다.
2. **Explain 의 단독 빌드 폴백** — 지시서는 단독 빌드를 언급하지 않았다. ManualScope 가
   없으면 원래 문단으로 그리게 해 전달본 화면 무변경을 지켰다.
3. 미리보기에서 대본 `initial`(3편 수문 100%)은 반영하지 않는다 — 지시서가 「t=0 프레임만」
   이라고 못 박았다. 미리보기의 수문 개도율은 평시값(0%)이다.
4. 상태 의존 문구(동적 className note·notice 계열)는 치환하지 않았다 — 설명이 아니라
   상태 표시라, 오버레이로 옮기면 상태와 분리된다.
