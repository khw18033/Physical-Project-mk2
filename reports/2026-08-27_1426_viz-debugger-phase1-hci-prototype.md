# 가시화 디버거 1단계 HCI 프로토타입 완료 보고

**작업일**: 2026-08-27
**기준 커밋**: `84554c2`
**범위**: 목 시나리오, 화면 D-01~D-05, DAG/트리 표현, 정적 전달본

## 완료 결과

- 임무 `MSN-260826-01` 하나에 마일스톤 7건, MS-C 태스크 T-31~T-37, T-31 액션 아이템 4건을 정의했다.
- 태스크 관계는 `T-31 → {T-32,T-33} → T-34 → T-35 → T-36 → T-37` 분기·합류 DAG다.
- 하드웨어 7대를 넣고 `arm-03`과 `drone-01`을 오프라인, `cam-5f`를 점검 상태로 표시했다.
- 게이트웨이에서 15개 trace 이벤트를 20배속으로 실제 발행해 시간순 수신과 8상태 전체를 확인했다.
- D-01~D-05 화면을 같은 시나리오와 공용 그래프 컴포넌트로 구현했다.
- 모든 사람 조작은 `recordHuman()`을 거쳐 메모리 배열과 콘솔에 `produced_by=human`으로 남는다.
- 빌드 자산 경로는 상대 경로이며 시나리오 JSON이 번들에 포함돼 게이트웨이 없이 화면이 채워진다.

## 화면별 동작

| 화면 | 구현된 동작 |
|---|---|
| D-01 | 발화 처리 단계, 마일스톤·하드웨어 목록, 드래그 배정, 마일스톤 클릭 하강 |
| D-02 | SVG 베지어 연결선, 계산된 depth 배치, 장비 연결 상태, 더블클릭 상세, DAG/트리 토글 |
| D-03 | 액션 표, Go1 상태 스트립, 평가 항목, 사람 조작 기록 버튼 |
| D-04 | D-02 공용 그래프에 시각 상태 주입, 슬라이더·단계 이동, 임무 이력, 상태 구간 타임라인 |
| D-05 | 실패 노드 강조, 비관련 노드 흐림, 수정 입력, 반사실/수정 재실행 기록 |

## 예상 구현 이미지와 다르게 만든 부분

- 이미지처럼 픽셀 단위로 맞추지 않고 정보 구조와 3열/그래프/팝업 관계만 따랐다. 시각 디자인을 HCI 논의 전에 확정하지 말라는 제약 때문이다.
- 상단에 D-01~D-05 이동 탭을 추가했다. 정적 전달본 한 주소에서 팀원이 모든 상태를 즉시 비교할 수 있게 하기 위한 초안용 탐색 장치다.
- D-05는 관련 없는 노드를 완전히 접지 않고 흐리게 했다. 실패 경로 격리는 2단계 범위이므로 1단계에서는 표현 논의를 위한 시각적 모사만 했다.
- 임무 이력은 현재 임무 외 항목을 표시용 목 목록으로만 둔다. 여러 임무 기록 저장과 실제 선택 재생은 2단계 기록 저장소 범위다.
- 로봇 상태 스트립 값은 `unitree_go1.json`의 항목명에 맞췄지만 시나리오 JSON의 목 값이다.

## 트리 표현 판단

사용자 확인을 받아 구현했다. `graph/layout.ts`에 DAG와 트리 배치를 분리하고, 노드·연결선·상태 표현은 `TaskGraph` 하나를 공유한다. 따라서 HCI 논의 결과는 두 표현에 동시에 반영된다.

## 검증 결과

- `verify:scenario`: 통과 — 7 마일스톤, 7 태스크, 7 하드웨어, 15 이벤트, 8상태, 분기·합류 확인
- 실제 게이트웨이 스트림: 15건 시간순 수신, 상태 8종 수신
- `verify:hierarchy`: 통과 — 하드웨어 어휘 주입 거부 포함
- `verify:adapter-swap`: 통과
- `verify:transport`: 통과 — 재연결·구독 복원
- 신규 `typecheck`, `build`: 통과
- 기존 `web-dashboard` 검증 7종: 모두 통과
- Edge 헤드리스 브라우저 1600×1000 캡처: D-01 정보 구조와 3열 배치 정상

## 2단계로 넘길 미결 사항

- 현재 `statusesAt()`은 내장 이벤트를 앞에서 접는 프로토타입이다. append-only 저장소, 스냅샷, 임의 seq 복원으로 교체해야 한다.
- D-05의 흐림 처리는 정적 역방향 의존 탐색이다. 근본 원인을 포함하는 실패 경로 격리 검증이 필요하다.
- 메모리의 사람 명령 기록을 실제 trace 저장소와 명령 상관관계에 연결해야 한다.
- 임무 이력의 실제 여러 임무 로딩과 사건 지점 점프를 구현해야 한다.
- 두 번째 어댑터 프로파일을 추가할 때 `verify:adapter-swap`의 Git 변경 0건 판정을 활성화해야 한다.

## HCI 논의 항목 변화

기존 7건을 그대로 유지했다. DAG/트리 비교는 계층 전환과 실패 경로 표현 논의에 쓸 수 있도록 구현했지만, 별도 질문을 추가하거나 기존 질문을 제거하지 않았다. 전체 문구는 `viz-debugger/HANDOFF.md`에 옮겼다.
