# 마일스톤 분리 4단계 — 장소 계약과 맵 추출 (§1)

**지시서**: `문서/작업프롬프트_마일스톤분리_260904.md` §1
**대상 요구사항**: `VZ-G-01` 그라운딩 · `VZ-G-03` 검증 재료
**날짜**: 2026-09-06

---

## 무엇을 뽑았나

원천은 `Unity_Map/Assets/Scenes/N45F_Map.unity` 하나다.
추출은 `viz-debugger/scripts/extract-places.mjs` — `npm run extract:places` 로 다시 돌릴 수 있다.

| | 수 |
|---|---|
| 장소 | **54** |
| 방 | 20 (501~520) + 415호 자리표시 1 |
| 문 | 26 |
| 복도 | 2 |
| 지물 | 5 (엘리베이터 · 화장실 · 계단 셋) |
| 인접 변 | 61 |

## 분류를 좌표에서 뽑은 이유

문은 네 줄로 서 있다 — z 가 같은 줄 둘(동서 복도의 양쪽)과 x 가 같은 줄 둘(남북 복도의 양쪽).
그래서 **어느 좌표를 이웃과 공유하는가**로 갈랐고, 복도 축은 마주 보는 두 줄의 가운데다.

| 복도 | 축 | 문 |
|---|---|---|
| `corridor-5f-main` 동서 | z = −44.57 | 16 (501~512) |
| `corridor-5f-wing` 남북 | x = −11.37 | 10 (513~520) |

**축 하나만 보고 임계값으로 자르면 안 된다.** 처음에 그렇게 짰더니 남북 복도 남쪽 끝의
`Door_520F`(z = −42.52)가 동서 복도로 넘어갔다. 복도 끝의 문은 반대 복도의 축에도 가깝다.

교차부의 지물(엘리베이터 · 화장실 · 동편 계단)은 **두 복도 모두에 붙였다.** 실제로 두 곳에서
닿기 때문이고, 하나로 강제하면 그 선택이 근거 없는 판단이 된다.

## 좌표의 출처와 정밀도 — 말을 정확히 해 둔다

이 데이터를 「실측」이라고 부를 수 있는 근거는 이렇다.

| | |
|---|---|
| 방법 | **줄자 실측** + 피난 안내도 사진을 Unity 밑그림으로 깔고 트레이싱 |
| 안내도 | `Unity_Map/Assets/N4_5F_Corrider/Images/N4_5F.jpg` (2026-01-09). 머티리얼로 감싸 밑그림으로 쓰고 지금은 어느 씬도 참조하지 않는다 |
| 정밀도 | **측량 등급이 아니다.** 줄자 오차가 있고 안내도에 축척 표시가 없다 |
| 높이(y) | **실측이 아니다.** 편의값이라 기하 파일에 넣지 않았다 |

**기준 좌표계를 `N45F_Map.unity` 의 월드 좌표로 정했다**(2026-09-06). 그것이
`site-global` 의 정의이고 단위는 m 다. 다른 좌표가 들어오면 이쪽으로 변환한다.

논문·보고서에서는 **「줄자 실측 기반」**까지가 정확하다. 「측량」이나 「도면 기반」은
근거를 넘어선다.

## 층을 나눈 결과

| 층 | 파일 | 내용 |
|---|---|---|
| 위상 | `places/places.json` | 장소 · 별칭 · 종류 · 층 · 인접. **좌표 없음** |
| 기하 | `places/places.geometry.json` | 문 26 · 지물 5 의 좌표, 복도 두 축과 구간 |

`verify:places` 의 4번 검사가 `src/generate/` 와 `gen-lab/server/` 를 훑어
**기하 파일을 읽는 경로가 없는지**를 본다. 지금 0건이다.

## 이름은 어디서 왔나

방 번호는 씬의 문 프리팹 이름(`Door_503`, `Door_520F` …)에서 그대로 나온다.
**사람이 넣은 이름은 하나도 없다.**

**방은 번호로만 부른다.** 기능 이름(강의실 · 산학협동실 …)은 안내도에는 있지만
씬에는 없는 정보이고, 방 용도는 바뀐다. 그때마다 추출 결과가 흔들리면 그것은 실측이 아니다.
Assets 안의 N4동 지상5층 피난 안내도는 추출 대조에만 쓰고 결과에는 넣지 않았다.

## 자리만 둔 것

`room-415` — 4층이고 이 씬에 없다. 그런데 옛 편 `MSN-260826-01` 의 발화가
「415호에서 503호로」라 자리가 없으면 그 대본의 장소를 못 푼다.
**인접을 비운 채로 두었다.** 「연결 예정」과 같은 원칙이고 `verify:places` 가 이 하나만
고아 예외로 인정한다.

## 뽑지 않은 것

| 뽑지 않음 | 이유 |
|---|---|
| 가구 (`Desk` · `Cabinet` · `Shelf` · `Trash_Can` · `Water_Purifier`) | 장소가 아니다. 임무 마일스톤을 만들어내지 않는다 |
| `AED` | 지물이지만 어느 대본도 쓰지 않는다 |
| `Corrider` 프리팹 · `pb_Mesh-1302` | 복도 메시. 위상은 문 위치에서 나오고 메시는 렌더 층이다 |
| 벽 (`Wall_*`) | 계단·엘리베이터 위치의 근거로만 쓰고 장소로 올리지 않았다 |

## 503호 실내 — 씬이 둘이고 프레임도 둘이다

503호는 복도와 다른 씬이다(`Unity_Map/Assets/XRI/lab.unity`, GameObject 336개).
벽 네 장으로 치수가 딱 떨어진다 — **7.4 m × 9.4 m**.

두 프레임을 잇는 앵커는 **문 하나**다. 실내의 `Door`(2.62, 9.29)가 복도의
`door-503`(−37.84, −45.97)이다. **배치는 씬 그대로가 맞고 회전은 없다**(2026-09-06 확인).
그래서 평행이동 하나로 끝난다 — 오프셋 **(−40.46, −55.26)**.

검산이 된다. 변환한 방의 x 범위가 **−40.46 ~ −33.06** 이고, 이웃 문
`504b`(−43.28)와 `502b`(−27.95) 사이에 겹침 없이 들어간다. 앵커 하나로 맞춘 변환이
다른 문 둘과도 어긋나지 않는다는 뜻이다.

실내에서 가져온 것은 **범위와 앵커 넷**이다 — `StartPoint` · `GoalPoint` · `go1` 스폰 ·
`Anchor360`. 씬에 이미 박혀 있던 임무 지점이고 지어낸 값이 아니다.

**y 는 넣지 않았다.** 씬의 벽 높이(6)는 z 축을 고려하지 않고 둔 편의값이며 실측이 아니다.
없는 것을 있는 척하지 않는다는 규칙이 여기에도 그대로다.

가구(책상 19개 · 책장 · 응접 셋)는 **위상에도 기하에도 넣지 않았다.** 장소가 아니고
어느 대본도 가구를 마일스톤으로 만들지 않는다. 장애물 회피가 들어오면 그때 판단한다.

## 발견 — 대본 1편의 엘리베이터 좌표가 이 맵과 다르다

`MSN-260831-01` 의 `params` 는 이렇게 적혀 있다.

```
elevator_position: { x: 51.6, y: 0, z: -4.2, frame: "site-global" }
```

실측 맵의 엘리베이터는 **(−7.36, 8.11, −45.29)** 다. 부호도 크기도 맞지 않는다.
`frame: "site-global"` 이라고 적혀 있지만 **이 씬의 월드 좌표계가 아니다.**

프레임을 세어 보면 셋이었다. 이번에 둘을 하나로 합쳤다.

| 프레임 | 상태 |
|---|---|
| `N45F_Map.unity` 월드 | **`site-global` 의 정의다** (2026-09-06 확정) — 층 전체를 담는다 |
| `XRI/lab.unity` 월드 (503호) | **합쳤다** — 문 앵커 평행이동 |
| 대본 `params.frame: "site-global"` | **미결** — 아래 |

지금은 아무것도 안 깨진다 — 대본 재생은 이 값을 표시만 하고 `VZ-G-01` 은 좌표를 안 쓴다.
**`VZ-G-04`(액션 아이템 전개)가 걸린다.** 전개가 실측 거리를 계산하는 순간 두 프레임이 만난다.

`worldTimeline` 까지 뜯어보면 어긋남이 좌표계 하나가 아니다. 로봇이 `(9.0, −6.5)` 에서
`(48.9, −4.2)` 까지 **x 축에 평행한 직선**으로 가고 방향 변화가 3.3° 뿐이다. 기준
좌표계에서 같은 임무는 방에서 `+z` 로 7.7 m 나온 뒤 복도에서 `+x` 로 30.5 m 가는
**83° 꺾인 경로**다. 평행이동으로도 겹쳐지지 않는다.

거리는 맞춰 두었다 — 대본 40.34 m 대 기준 좌표계 38.24 m, 차이 5%.
**거리 감각은 잡고 좌표계만 임의로 잡은 것이다.**

**대본은 고치지 않는다.** 4편은 HCI 전달본이자 §2 정답셋이고 좌표 때문에 건드리면
채점 기준이 같이 흔들린다. `places/README.md` 에 프레임 표를 두었고 `VZ-G-04` 는
그 표를 보고 대본의 `frame` 값을 믿지 않으면 된다.

## 검사

| 검사 | 결과 |
|---|---|
| `verify:places` (신설) | 계약 54건 · 인접 대칭 · 대본 4편 장소 전부 풀림 · 기하 참조 0 |
| `verify:scenario` | 통과 (`MSN-260826-01` 무수정) |
| `verify:dep-rules` | 통과 |

## 다음

§4 (`VZ-G-01` 베이스라인). 남은 선행 둘은 이렇다.

1. `utterance.confidence` 계약 (§7.8) — 3안 중 택1
2. 모델 결정 — `gen-lab` 엔진이 아직 스텁이다. 8B 급과 4B 급을 같은 정답셋으로 재고 낙폭을 남긴다
