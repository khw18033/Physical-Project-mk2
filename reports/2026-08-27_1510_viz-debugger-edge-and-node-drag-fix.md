# 가시화 디버거 연결선·노드 드래그 수정 보고

**작업일**: 2026-08-27
**기준 커밋**: `d5aff9e`

## 원인과 수정

- 화살표 분리 원인은 SVG의 `viewBox` 좌표가 화면 폭에 맞춰 확대되는 동안 HTML 노드는 CSS 픽셀 좌표를 사용해 두 좌표계가 달라진 것이었다.
- SVG의 크기를 그래프 캔버스와 동일한 픽셀 `width/height`로 지정해 연결선과 노드가 같은 좌표를 쓰게 했다.
- 노드는 포인터로 자유롭게 드래그할 수 있다. 이동 좌표는 컴포넌트 상태에 저장되며 모든 의존 연결선이 즉시 다시 계산된다.
- 카드가 좌우로 충분히 떨어져 있으면 좌·우 포트를 사용하고, 카드의 가로 영역이 겹치면 위·아래 포트로 자동 전환한다.
- DAG/트리 토글 시 수동 배치를 초기화하고 선택한 레이아웃의 계산 위치로 돌아간다.

## 검증

- `verify:layout`: 통과 — 고유 좌표, CSS `left/top`, SVG 공용 픽셀 좌표, 포인터 드래그 경로 확인
- `verify:scenario`: 통과
- `typecheck`: 통과
- Edge 헤드리스 브라우저에서 마일스톤 클릭 후 T-31을 100×45 px 드래그했다. 카드 이동과 의존 화살표 재연결을 캡처로 확인했다.

기존 계약과 `web-dashboard`는 수정하지 않았다.
