# 실측 작업로그 — 로봇의 영상과 업무 데이터를 엣지를 거쳐 뷰어까지

**근거 문서:** [`260919_실측준비_올인원_v2.md`](../../260919_실측준비_올인원_v2.md) (v2.2)
**기록 방식:** 실측준비 v2 §7 — 단계마다 **해당하는 것만** 쓴다. 없는 항목은 줄을 쓰지 않는다.

```
1 문서와 다르게 한 것
2 발견한 것 — 증상 / 실측 원문(그대로) / 원인 / 조치
3 결정한 것 — 물음·선택지·답
4 이 단계가 서버·장비에 남긴 것 (되돌릴 것 포함)
5 검증하지 못한 것
```

> 🔴 **앞 단계 기록은 고치지 않고 뒤에만 붙인다.** 원문을 잃었으면 지어내지 말고 「원문 유실」이라 쓴다.
> **기억으로 보고서를 쓰면 일어나지 않은 숫자가 들어간다 — 파일에 적힌 것만 보고서로 간다.**

---

## 🔴 서버에서 pytest 를 돌리는 법 — 네 줄. **이 순서가 규율이다**

```bash
cd /home/dg/capstone-db/phase1_work/Physical-Project-mk2
set -a; . /home/dg/capstone-db/.env; set +a
source /home/dg/capstone-db/phase1_work/venv_phase1/bin/activate
python -m pytest -q -rs 2>&1 | tail -20
```

**Phase 1~4 내내 이 네 줄로 했다.** 한 줄만 어긋나도 결과가 통째로 달라진다 — 2026-09-21 에
실제로 겪었다(단계 2-b 발견 ④).

| 줄 | 왜 빠뜨리면 안 되나 |
|---|---|
| `set -a; . .env; set +a` | 🔴 **`.env` 에 `export` 가 없다.** `source` 만으로는 **셸 변수**에 그쳐 자식 프로세스(python)가 못 본다. `set -a` 가 「이후 대입을 전부 export」다. 빠뜨리면 **토큰 없는 클라이언트 → 4401**, **DB 접속 정보 없음 → skip 30건** |
| `source …/venv_phase1/bin/activate` | 서버 venv. 전체 경로로 python 을 불러도 같지만 **활성화가 규율이다**(경로를 매번 적으면 하나가 빠진다) |
| `-rs` | **skip 사유를 찍는다.** skip 이 늘었을 때 원인을 바로 본다 — 없으면 숫자만 보고 헤맨다 |
| `tail -20` | 실패 요약과 마지막 집계만 본다 |

**기대값(2026-09-21 단계 C 시점):** `314 passed, 12 skipped`.
(단계 2-b 시점은 `311 passed` 였다 — 그 뒤 진짜 소켓 관통 1 + 라벨·규격 2 가 늘었다.)
skip 12 는 **엣지 입구 8766 이 닫혀 있을 때의 정상값**이다(relay 시험). 단계 1 에서 8766 을 열면 줄어든다.

> ⚠ **`.env` 를 읽는 파서가 셋이다** — 셸 `set -a` · compose dotenv · systemd `EnvironmentFile=`.
> 셋의 인용부호 규칙이 달라 **값에 특수문자를 넣지 않는다(영숫자만)**. `mk2-ingest.service` 주석과 같은 내용.

> ⚠ **서버 프로세스는 systemd 가 `.env` 를 읽어 이미 토큰을 갖고 있다.** 그래서 셸에서만 빠뜨리면
> **「서버는 토큰을 요구하는데 시험 클라이언트는 안 보내는」** 비대칭이 생긴다 — 그 증상이 4401 이다.

---

## 🔴 주소·토큰은 이 문서에 적지 않는다

**저장소가 Public 이다.** tailnet 주소는 `<…>` 자리표시자로 적고, 실값은 **`_serverinfo/`(gitignore)**
와 **별도 전달 파일**에만 둔다. 2026-09-21 에 실주소 13곳을 적었다가 push 전에 되돌렸다 —
**회신 문서·작업로그를 쓸 때 매번 확인한다.**

## 🔴 서버 MySQL 을 보는 법 — **`docker exec … mysql` 은 절대 안 붙는다**

```bash
# X  이렇게 하면 100% ERROR 1045 다
docker exec -i capstone_mysql mysql -umk2_app mk2 -e "…"

# O  백엔드 자신의 접속 경로를 쓴다
cd /home/dg/capstone-db/phase1_work/Physical-Project-mk2
set -a; . /home/dg/capstone-db/.env; set +a
source /home/dg/capstone-db/phase1_work/venv_phase1/bin/activate
python - <<'PY'
from backend.gateway.capture import CaptureStore   # 또는 필요한 Store
store = CaptureStore()
for row in store.list_for_retention():
    print(row)
store.close()
PY
```

| 왜 | |
|---|---|
| 계정이 하나뿐이다 | **`'mk2_app'@'172.18.%'`** — `@'localhost'` 도 `@'%'` 도 **없다**(`_serverinfo/260918_phase4_단계0_실측.txt:199`) |
| `docker exec` 는 | 컨테이너 **안의 유닉스 소켓**으로 붙어 MySQL 이 `@'localhost'` 로 본다 → **없는 계정 → 1045** |
| 백엔드는 | 호스트에서 **TCP `127.0.0.1:7858`** → 도커 포트 포워딩을 지나며 클라이언트가 **`172.18.0.1`** 이 되어 `@'172.18.%'` 에 맞는다 |
| root 는 | **`.env` 에 `MK2_MYSQL_ROOT_PASSWORD` 가 없다**(`using password: NO`). 비번은 운영 compose 의 `MYSQL_ROOT_PASSWORD` 안이다 |

> 🔴 **Phase 2 에서 이미 똑같이 겪었다** — `_serverinfo/phase2_transcript/phase2_대화원문.md:997` 에
> `ERROR 1045 … 'mk2_app'@'localhost'` 가 그대로 있다. **세 번째로 겪지 않으려고 여기 적는다.**
>
> ⚠ **`mk2_app` 에는 DELETE 가 없다 — 의도한 것이다.** 촬영본은 행을 지우지 않고 **아카이브를 purge**
> 한다(`archive_path=NULL` + `purged_at`). `capture.py:442` 독스트링이 그렇게 적고 있다.

---

## 단계 A — 착수 대조 (2026-09-21)

**서버를 만지지 않고 읽기만 했다.** 문서 §2 「서버가 지금 어떻게 서 있나」와 대조한다.

### 2 발견한 것

**① 🔴 설정 md5 가 어긋나 보였다 — 내가 틀린 파일을 재게 했다(명령 결함, 코드 결함 아님)**

- **증상:** A-4 가 기대값과 달랐다.
  ```
  6e7978726822c830d31d37268da37d54  infra/docker-compose.yml      (기대 c94ddd83…)
  f48e171353b079ec2d8425b8749d0e51  infra/config/prometheus.yml   (기대 ab241020…)
  ```
- **원인:** 내가 잰 것은 `phase1_work/Physical-Project-mk2/infra/` 의 **저장소 사본**인데,
  Phase 4 가 기록한 md5 는 **운영본**의 것이었다.
  `_serverinfo/260919_phase4_단계8_되돌림.txt:15` — *"서버 백업: **/home/dg/capstone-db/**docker-compose.yml.bak_before_phase4_step8"*.
  운영 compose 는 `.env` 와 같은 디렉터리에 있다(compose 가 같은 폴더의 `.env` 를 자동으로 읽기 때문).
- **조치:** 컨테이너 라벨로 운영 경로를 확정하고 다시 쟀다.
  ```
  /home/dg/capstone-db/docker-compose.yml
  c94ddd8301019ab499f72129f0f0b16e  /home/dg/capstone-db/docker-compose.yml
  ab2410206a205a70b394e7c650226fc8  /home/dg/capstone-db/config/prometheus.yml
  ```
  **기대값과 정확히 일치.** 백업 3종(`_obs`·`_kafka`·`_step8`)과 `phase1_work/phase4_new/` 대기본도 그대로 있다.
- **남길 것:** 앞으로 서버 설정 md5 를 잴 때는 **`/home/dg/capstone-db/` 쪽**이다.
  저장소 사본(`phase1_work/.../infra/`)은 배포 원본이 아니다.

**② 저장소 사본은 git 저장소가 아니다 — 정상이다**

`git log` 가 아무것도 찍지 않았다. `mk2-ws-echo.service` 주석 그대로다 —
*"코드를 고치면: 서버 사본에 **복사** → restart (**git pull 배포가 아니다**)"*. 조치 없음.

**③ 🔴 TSDB 기준선을 확보했다 — Phase 4 가 남긴 숙제를 닫았다**

Phase 4 보고서가 *"행 수·채널별 분포는 재지 않았다 — **Phase 5 단계 0의 첫 조회 항목**이다"* 로 남긴 것이다.

```
 count
-------
  2381

  channel  | count
-----------+-------
 state     |  1148
 heartbeat |   746
 status    |   487

     source_id      | count
--------------------+-------
 edge-kchk-b413fb28 |     1
```

- Phase 3 종료 시점 **479행** → 지금 **2,381행**. 차이 1,902행이 Phase 4 pytest 발행분이다.
- ⚠ **보고서와 다른 점:** 보고서는 *"`edge-*`·`test-*` 접두사"* 라 적었으나 **실제로는 `edge-kchk` 1행뿐**이다.
  pytest 가 다른 `source_id` 를 쓴다는 뜻이고, **정리할 인공물이 거의 없다.**
- **이 숫자가 실측 뒤 증가분을 가르는 기준선이다.**

### 4 이 단계가 남긴 것

**없다 — 읽기만 했다.** 되돌릴 것 없음.

### 대조 결과

| 항목 | 판정 |
|---|---|
| 상주 유닛 넷 active/enabled, NRestarts 0 | ✅ |
| 열린 입구 — 8765 두 주소 + 8767 / 8766·9095·서버:4316 없음 | ✅ |
| 컨테이너 13개, 발행 포트 loopback | ✅ |
| 설정 md5 (운영본) | ✅ (위 ①) |
| `.env` 12행 | ✅ |
| ufw Phase 4 규칙 2줄 | ✅ `[38]` 8765←`<이대규 PC tailnet 주소>` · `[39]` 8767←`<pi7 tailnet 주소>` |

**7/7 일치. 서버는 Phase 4 종료 상태 그대로다.**

---

## 단계 2-b — `/state` VZ 와이어 어댑터 (2026-09-21)

**실측대비를 위해 임시로 구현한 코드다**(실측준비 v2 §3-1-1). **서버에는 아직 아무것도 올리지 않았다.**

### 1 문서와 다르게 한 것

**① 시험 파일이 하나가 아니라 둘이다**

문서 §3-1-1 「시험」 표는 *"`tests/test_vz_wire.py` 신설 + 기존 관통 시험 파일에 1건 추가"* 였다. 실제로는:

| 파일 | 무엇 | 왜 나눴나 |
|---|---|---|
| `tests/test_vz_wire.py` (24건) | 매칭·변환·구독표 **순수 함수** | 표준 라이브러리만으로 돈다 |
| `tests/test_ws_state_wire.py` (15건) | **이중 형식 분기** — 가짜 소켓으로 실제 `_broadcast`·`state_session` | ← 아래 |

**이유:** 이중 형식 분기(구독표가 비었나로 갈리는 것)는 `_broadcast` 의 **동작**이라 순수 함수 시험으로는
못 잡는데, 기존 관통 시험(`test_pipeline.py`)은 **서버가 떠 있어야** 돌아서 지금 돌릴 수 없다.
가짜 소켓으로 **소켓 없이 도는** 시험을 따로 만들었다(`test_media_frame.py` 와 같은 결).
→ 서버 관통 1건은 **단계 D** 에서 따로 확인한다.

**② `vz_wire` 에 함수가 셋이다**

문서 표에는 `selector_matches` · `to_vz_envelope` 둘만 있었다. 구독표 갱신(`apply_subscription`)도
**소켓을 모르는 순수 로직**이라 같은 모듈에 뒀다 — `ws_echo` 쪽이 얇아지고 단위 시험이 붙는다.
`data_message()` 는 둘을 합치는 한 줄짜리 편의 함수다.

### 2 발견한 것

**① 🔴 깨진 선택자를 `{}` 로 정규화하면 전량 푸시가 된다 (설계 중 발견, 코드에 들어가기 전에 잡음)**

- **증상:** 초안의 `apply_subscription()` 이 dict 가 아닌 selector 를 `{}` 로 바꿔 저장했다.
- **원인:** `{}` 는 **모든 축이 와일드카드**다. 즉 *"선택자가 깨졌다"* 가 *"전부 보내라"* 가 된다.
  구독 계약이 깨졌는데 데이터를 쏟으면 원인이 묻힌다.
- **조치:** **정규화하지 않고 받은 그대로 저장**하고, `selector_matches()` 가 dict 가 아니면 `False`.
  **판정을 한 곳에만 둔다.** 시험 `test_깨진_선택자는_전량_푸시로_물러서지_않는다` 로 못 박았다.

**② `bool` 이 `int` 의 하위형이라 `sequence_id: True` 가 `seq=1` 로 새어 들어갔다**

- **조치:** `isinstance(seq, bool)` 을 먼저 막는다. 시험 `test_순번이_없거나_이상해도_0으로_간다`.

**③ 직렬화가 클라이언트별이 됐다 (성질 변화 — 결함 아님)**

전에는 `json.dumps` 를 **한 번** 하고 전원에게 같은 문자열을 보냈는데, `sub` id 가 클라이언트마다
달라 구독한 클라이언트는 따로 만든다. **지금 형식은 여전히 한 번만 만든다**(`raw_payload` 지연 생성).
`/state` 클라이언트가 관제 화면 한둘이면 문제 없다. **알고 바꾼 것이다.**

**④ 🔴 `source .env` 만으로는 환경변수가 자식 프로세스에 안 넘어간다 (실행 규율 위반 — 코드 결함 아님)**

- **증상:** 서버 회귀 1차 실행이 이렇게 나왔다.
  ```
  FAILED tests/test_capture_upload.py::test_c4_truncated_archive_leaves_nothing
  FAILED tests/test_observability_pipeline.py::test_gateway_metrics_with_ws_client
  FAILED tests/test_pipeline.py::test_ws_delivery
  3 failed, 278 passed, 42 skipped in 112.81s
  ```
  트레이스백: `websockets.exceptions.ConnectionClosedError: received 4401 (private use) token`
- **원인:** 실행 명령이 `source /home/dg/capstone-db/.env` 였다. `.env` 에 `export` 가 없어
  **셸 변수**에 그쳤고 pytest(자식 프로세스)가 못 봤다. 반면 `mk2-ws-echo` 는 systemd
  `EnvironmentFile=` 로 읽어 **토큰을 갖고 있었다** → 서버만 토큰을 요구하는 비대칭.
- **숫자가 정확히 떨어진다:**
  - 수집 `278 + 42 + 3 = 323` = Phase 4 의 284(272+12) + 신규 39 ✅ **수집은 정상이었다**
  - 통과 못 한 33건 = skip 증가 **30**(TSDB·MySQL 접속 정보 없음 → conftest 가 skip) + 4401 실패 **2** + C4 **1**
  - 검산 `272 − 33 + 39 = 278` ✅ 실제 passed 와 일치
- **조치:** `set -a; . .env; set +a` 로 되돌렸다 → **`311 passed, 12 skipped`**. 예측과 일치.
- **남길 것:** 🔴 **이 문서 머리말의 「pytest 네 줄」이 규율이다.** 유닛 주석이 이미 「파서가 셋」이라
  경고하고 있었는데 그것을 읽고도 어겼다. **다음 지시서·보고서에도 그 네 줄을 그대로 적는다.**

**⑤ `test_c4_truncated_archive_leaves_nothing` 은 부하 flake 였다 (코드 결함 아님)**

- **증상:** 위 1차 실행에서 C4 가 실패했다. ④ 로 설명되지 않는 한 건이었다 —
  `test_capture_upload.py` 는 **자기 서버를 임의 포트에 직접 띄우고 토큰도 스스로 만들어서**
  `.env` 를 전혀 읽지 않는다(그래서 28건 중 27건은 통과했다).
- **판정:** 단독 4회 재실행 **4/4 통과**(각 0.58s).
  ```
  ── 시도 1 ──  1 passed in 0.58s
  ── 시도 2 ──  1 passed in 0.58s
  ── 시도 3 ──  1 passed in 0.58s
  (단독 재확인)  1 passed in 0.58s
  ```
- **원인:** C4 는 원시 소켓으로 잘린 PUT 을 보내고 **서버가 끊김을 감지해 `.part` 를 지울 때까지
  최대 10초를 기다린다.** 112초짜리 전체 실행에서 다른 시험 3건이 실패·재시도하며 부하가 걸린 창이었다.
- **남길 것:** ⚠ **C4 는 시간에 민감한 시험이다.** 전체 실행에서 단독 실패하면 **먼저 단독 재실행**으로
  flake 를 가른다. 재현되지 않으면 부하 flake 로 기록하고 넘어간다.

### 3 결정한 것

| 물음 | 선택지 | 답 |
|---|---|---|
| 두 형식을 무엇으로 가르나 | ⓐ 구독표가 비었나 · ⓑ 경로를 따로(`/state2`) · ⓒ 쿼리 파라미터 | **ⓐ** — 경로·파라미터를 늘리면 VZ 가 코드를 고쳐야 한다. 구독은 VZ 가 **이미 접속 즉시 보낸다** |
| `status` 채널을 구독자에게 보내나 | 보낸다 / 거른다 | **거른다** — VZ `Channel` 타입에 없어 화면이 무엇을 할지 계약에 없다. 비구독자에게는 그대로 간다(Phase 1 echo 유지) |
| 계측(`be.gateway.push`) 단위 | 전송 1건 = 1 / 메시지×클라이언트 = 1 | **메시지×클라이언트 = 1** — 구독 둘이 맞아 두 번 보내도 1. Phase 1 의 뜻을 유지한다 |
| `subscribed`·`hello` 를 만드나 | 만든다 / 안 만든다 | **안 만든다** — VZ `switch` 에 **처리 가지 자체가 없다**(보내도 무시). 범위를 넓히지 않는다 |

### 4 이 단계가 남긴 것 — **로컬 저장소 작업본만. 서버는 무변경**

| 파일 | 무엇 |
|---|---|
| `backend/gateway/vz_wire.py` | **신설** (11.7KB) — `selector_matches` · `to_vz_envelope` · `data_message` · `apply_subscription` |
| `backend/gateway/ws_echo.py` | **수정 5곳** — 모듈 독스트링(`/state` 설명·tests 줄) · `typing` 에서 `Set` 제거 · `vz_wire` import · `_broadcast` 재작성 · `Gateway.__init__` 타입 · `state_session` 구독 읽기 |
| `tests/test_vz_wire.py` | **신설** — 24건 |
| `tests/test_ws_state_wire.py` | **신설** — 15건 |
| `tests/test_pipeline.py` | **수정** — `test_ws_delivery_vz_wire` 1건 추가(진짜 소켓으로 구독→`data` 수신) |

**되돌리는 법:** `state_session` 의 수신 루프를 `pass` 로 되돌리면 구독표가 비고, 구독표가 비면
`_broadcast` 가 「지금 형식」 가지로만 간다. `vz_wire.py` 는 아무도 부르지 않는 파일이 된다.

### 검증한 것

**① 로컬(컨테이너) 39건 전부 통과** — `24 + 15 passed`.

| 무엇 | 건수 |
|---|---|
| 매칭 양성(VZ 실제 선택자 둘 포함) | 5 |
| 🔴 매칭 음성 — **구역 `zone-503` → 0건** · `status` 거름 · 모르는 채널 · 깨진 선택자 · 타입 오류 | 5 |
| 봉투 변환 11칸 · payload 통째 보존 · scope 되돌림 | 3 |
| 견고성 — 빈 봉투·이상한 순번·타입 오류에도 **예외 없음** | 3 |
| 잠정값 셋 고정(`good`·`raw`·`null`) | 1 |
| 구독표 — 반영·무시·기본 scope·깨진 선택자 | 5 + 2 |
| **이중 형식** — 지금 형식 3 · VZ 형식 3 · 공존 1 · 음성 3 · 정리 1 · 세션 4 | 15 |

**② 🔴 서버 회귀 — `311 passed, 12 skipped in 114.72s`**

```
311 passed, 12 skipped in 114.72s (0:01:54)
```

- **Phase 4 기준선 272 + 신규 39 = 311.** 기대값과 정확히 일치한다.
- **기존 272건이 하나도 깨지지 않았다** — 이중 형식의 핵심 판정이 닫혔다.
  `test_pipeline.py::test_ws_delivery`(지금 형식)와 `test_observability_pipeline.py`가 그대로 통과했다.
- skip 12 = 엣지 입구 8766 이 닫혀 있을 때의 정상값(relay 시험). 단계 1 에서 8766 을 열면 줄어든다.
- 재기동 정상: `READY — WS 게이트웨이: ws://127.0.0.1,<서버 tailnet 주소>:8765 (/state·/media, 토큰 있음, media write_limit=8192 sndbuf=65536)`

⚠ **`test_ws_delivery_vz_wire`(진짜 소켓 + 구독)는 이 실행 뒤에 추가했다** — 아직 돌리지 않았다.
다음 실행의 기대값은 **312 passed** 다.

### 5 검증하지 못한 것

- ~~🔴 서버 회귀 272건~~ → ✅ **닫힘** — `311 passed, 12 skipped`(위 검증 ②).
- **진짜 소켓 관통 — 반만 닫혔다.**
  · **지금 형식 가지** ✅ `test_ws_delivery` 가 실제 WebSocket 으로 통과했다.
  · **VZ 형식 가지** ⏳ `test_ws_delivery_vz_wire` 를 신설했으나 **아직 돌리지 않았다.**
    서버의 `async for` 가 구독 메시지를 실제로 읽는지는 그 시험이 판정한다.
- **VZ 앱 실접속** — 구독 형식이 VZ 가 실제로 보내는 것과 같은지는 브랜치 코드를 읽어 맞췄을 뿐이다.
- **`console.html` 의 `/state` 탭** — 지금 형식 가지가 실제 브라우저에서도 그대로 찍히는지.
- **잠정값 셋을 VZ 화면이 실제로 쓰는지** — 쓰고 있으면 Phase 5 에서 먼저 맞춰야 할 자리다.
- **성능** — 클라이언트별 직렬화의 실제 비용을 재지 않았다(관제 화면 한둘 전제).

---
---

## 단계 C — 팀 회신 처리 · 결정 8건 · 코드 수정 ⓐⓑⓒ (2026-09-21)

**두 파트에서 회신이 왔다** — HW `백엔드_전달_260921.md`(431줄) · VZ `백엔드_답신_260921.md`(195줄).
둘 다 **전문**을 읽고 처리했다. 이 단계는 **읽기 → 결정 → 코드 → 회신 문서** 넷으로 이뤄졌다.

### 1 문서와 다르게 한 것

**① 회신 문서를 새 파일로 만들지 않고 기존 누적 문서에 절로 붙였다**

처음에는 파트별로 새 파일(`hw-실측회신-260921.md` 같은)을 만들 생각이었다. **안 만들었다** —
Phase 4 보고서에서 *"「HW가 할 일」이 세 곳에 나뉘어 있다"* 를 **문제로 지적해 놓고** 네 번째 파일을
만드는 것이 앞뒤가 안 맞기 때문이다.

| | |
|---|---|
| HW | `docs/be/hw-envelope-conformance.md` **§9** 로 추가 |
| VZ | `docs/be/vz-media-interface.md` **§15** 로 추가 |

**그리고 §9-1 에 「HW가 할 일」 1~25 전체를 한 표로 합쳤다.** 앞의 세 목록(§6 뒤 1~10 ·
§7-8 11~14 · §8-10 15~25)은 이력으로만 남기고, 머리말에 *"현황은 §9-1 을 본다"* 를 박았다.
**지적한 문제를 이번에 닫은 것이다.**

**② `/state` 통지가 뒤집혔다 — VZ 문서 §3 ↔ §15**

VZ 가 정확히 짚었다 — *"문서 둘이 `/state` 에 대해 반대로 말한다"*(§3「바꾸지 마라」 ↔
실측준비 v2.2 §3-3「이번에 함께 붙인다」). **나중 것이 맞다.** §0 요약표의 3행·6행에
취소선 + 「09-21 뒤집힘」을 달고, §15 가 §3 을 대체한다고 머리말에 적었다.

### 2 발견한 것

**① 🔴 ⓒ 수정이 반쪽이었다 — 그리고 원인은 「검증을 재구현한 것」이다**

- **증상:** 서버 pytest `1 failed, 313 passed, 12 skipped`. **두 번 돌려 같은 결과.**
  ```
  tests/test_contract_media.py:145: KeyError
  ```
- **실측 원문(그대로):**
  ```
  message = _load("detections-draft-valid.json")
  del message["coord"]          # ← 여기
  ```
- **원인:** ⓒ 에서 `coord` → `bbox_space` 로 옮기면서 **예시 파일도 같이 바꿨는데**,
  `tests/test_contract_media.py` 안의 `coord` 를 **한 군데만 고치고 두 번째를 놓쳤다.**
  `test_detections_draft_shape_and_vocab_not_enum` 은 고쳤고
  `test_detections_draft_rejects_bad_box`(:140·:145·:146)는 안 고쳤다.
- **🔴 진짜 원인 — 검증 방법이 틀렸다.** 컨테이너에서 확인할 때 **시험 파일을 실행하지 않고
  단언을 다시 구현**했다. 재구현한 단언은 전부 통과했으니 「초록」으로 보였다. **실제로 돌렸으면
  그 자리에서 죽었다.** 그리고 저장소 전체를 `coord` 로 훑지 않고 `README.md` 만 훑었다.
- **조치 둘:**
  - 세 줄을 `bbox_space` 로 고치고, **그 자리에 왜 깨졌는지를 독스트링으로 박았다**
    (*"이름을 또 바꾸면 규격·예시·이 시험 셋을 같이 고쳐야 한다"*).
  - **선생님 PC 저장소 사본에 venv 를 만들어 진짜 pytest 를 돌렸다** —
    `test_contract_media.py` **17 passed**, `test_vz_wire`+`test_ws_state_wire`+`test_observability_labels`
    **99 passed**, `tests/` 전체 **326 collected · 수집 오류 0**.
    326 = 서버의 313+1+12 과 정확히 같아 **다른 곳은 안 깨졌음이 확인됐다.**
- **남길 것:** 🔴 **「단언을 다시 구현해 놓고 통과했다고 하지 않는다. 시험 파일을 돌린다.」**
  그리고 **이름을 바꾸면 저장소 전체를 옛 이름으로 훑는다**(문서 하나가 아니라).

**② 🔴 HW 가 자기 규격 위반을 먼저 보고했고, 그 덫이 우리 참조 구현에도 있었다**

- **HW 원문(§3-3):** *"처음에는 **전송 시점**에 `sequence_id` 를 매겼습니다. 그랬더니 64장을
  버렸는데도 서버가 `순번 0~624 · 불연속 0회` 로 봤습니다 — **뷰어가 손실을 알 방법이
  없었습니다.** … 같은 함정이 참조 구현(`tests/media_publisher.py`)에도 있는지 한 번 봐 주시면
  좋겠습니다 — 합성 fixture 는 버릴 일이 없어 드러나지 않았을 수 있습니다."*
- **확인:** **있었다.** `iter_frames()` 가 읽는 시점이 아니라 `publish()` 의 송신 루프 카운터로
  순번을 매기고 있었다.
- **왜 안 드러났나:** 파일을 읽어 보내는 구현이라 **버릴 일이 자체가 없다.** 송신 순번과
  확정 순번이 항상 같아 30번을 돌려도 같은 결과다. **잠복 결함**이다.
- **조치:** 코드 모양은 그대로 두고(파일 재생에는 결함이 아니다) **경고를 세 곳에 박았다** —
  모듈 머리말 · `iter_frames()` · `publish()`.
- **남길 것:** **손실은 「버린 개수」가 아니라 「불연속」으로만 뷰어에 전달된다.** 순번을 전송
  시점에 매기면 손실이 **관측 불가능**해진다. 두 개는 다른 축이다.

**③ `edge/availability.py` 는 있다 — 양방향 브랜치 갈림이다**

- **HW 보고:** *"`edge/availability.py` 가 저장소에 없습니다. §8-9 ⑥이 인용하신 `:161-171` 도
  맞는 자리가 없습니다."*
- **확인 경로 둘로 같은 결과:**
  ```
  origin/HW   edge/availability.py · edge/media_gateway.py · edge/test_edge.py
  161: "# HELP up 말단 생사 — 엣지가 하트비트·LWT 로 파생 …"
  162: "# TYPE up gauge"
  171: lines.append(f"up{label} {UP if e.state == ONLINE else DOWN}")
  ```
  2026-09-17 HW 브랜치 사본에도 **같은 줄 번호**다. **인용이 맞았다.**
- 🔴 **반대 방향도 있다** — HW 가 §5-2 에서 인용한 `pi/robot/detect_sink.py` 는
  **09-17 사본에 없다.** 브랜치에만 있는 폴더와 작업 트리에만 있는 파일이 **둘 다** 있다.
- **남길 것:** *"브랜치를 HW 실체로 믿지 않는다"*(할 일 23)가 이번에 양방향으로 확인됐다.

**④ 🔴 `<HW 개발 PC 구 주소(개인 tailnet)>` 은 개인 tailnet 주소라 못 쓴다 — 방화벽 세 줄을 지웠다**

- HW 문서가 스스로 적었다 — *"이 PC의 tailscale이 **개인 계정**이라 팀 서버가 안 보입니다."*
- **tailnet 마다 주소를 따로 발급한다.** 팀 tailnet 에 합류하면 **다른 `100.x` 를 받는다.**
- **조치:** `[40][41][42]`(8766·9095·4316 ← `<HW 개발 PC 구 주소(개인 tailnet)>`)를 **큰 번호부터 삭제**했다.
  엉뚱한 주소를 열어 두는 것이 더 나쁘다. **절차·문법은 확인됐으니 주소만 갈아 끼운다.**
- **그리고 tailnet 초대는 우리 일이 아니다** — 기기 11대가 **팀 공용 구글 계정 하나** 아래
  있으므로 「초대」가 아니라 **그 PC 에서 팀 계정으로 로그인**하는 구조다.

**⑤ VZ 뷰어 기기 둘을 8765 에 넣었다**

```
[38] 8765/tcp ALLOW IN <이대규 PC tailnet 주소>   # 이대규 컴퓨터 (유지)
[40] 8765/tcp ALLOW IN <VZ 데스크톱 tailnet 주소>    # VZ desktop-oaujese
[41] 8765/tcp ALLOW IN <VZ 노트북 tailnet 주소>     # VZ laptop-isk6l1rq
```

**뷰어 토큰만 보내면 VZ 가 바로 붙을 수 있다**(데이터는 발견 ⑪ 뒤에 온다).

⚠ **`§10` 의 ①(관제 웹 주소)은 이것으로 닫혔다** — 회신 문서에는 `《…》` 로 비어 있었으나
**구두로 받았다.** 그러면 `②` CORS origin 넷(두 기기 × `:5174`·`:5175`)도 자동으로 정해진다.
**남은 것은 우리 쪽 `4318` ufw·CORS 설정**(단계 1)이고 VZ 에서 더 받을 것은 없다.

**⑥ 촬영본 두 세션의 차이가 **1바이트** 였다 — 같은 것이 맞다**

```
go1-001/20260910-134408.tar.gz   1,274,114 B
wl-001 /20260910-134408.tar.gz   1,274,115 B
```

매니페스트의 `"wl-001"`(6자) → `"go1-001"`(7자) **딱 1바이트** 차이다. HW §5-3 설명
(업로더가 `/etc/device_id` 를 읽어 수위센서 소유로 올라갔다)과 정확히 맞는다.
**다른 세션이 아니므로 삭제해도 된다.**

**⑦ `lag_s` 조회 — 창이 HW 가 말한 것보다 넓었고, 분당 건수가 일치했다**

```
처음 2026-09-21 07:26:09.727+00 → 마지막 07:47:12.801+00   1,036건   lag_없음 0
```

KST 로 **16:26:09 ~ 16:47:12, 21.05분**. HW 가 적은 창은 `16:26:59 ~ 16:38:24` **11.4분 / 557건**.

| | 건수 | 분 | 분당 |
|---|---:|---:|---:|
| HW 계수 | 557 | 11.4 | **48.9** |
| 우리 TSDB | 1,036 | 21.05 | **49.2** |

**분당 건수가 같다.** HW 가 센 창이 우리 것의 일부일 뿐이고, **브로커→TSDB 사이에 새는 구간이
없다.** `lag_없음 0` 이라 1,036건 전부 헤더가 제대로 실려 왔다.

**⑧ `clock_skew` 는 숫자가 아니라 불리언이다 (내 쿼리 결함)**

- **증상:** `ERROR: function avg(boolean) does not exist`
- **원인:** `writer.py:112` — `clock_skew` 는 `lag_s < 0` 인지를 담는 **플래그**다
  (*"버리지 않고 그대로 저장하되 플래그만 세운다"*). 값이 아니라 표시다.
- **조치:** `count(*) FILTER (WHERE clock_skew)` 로 고쳤다.
- **남길 것:** `lag_s` 는 **부호가 있는 값**이고 음수도 정상이다(말단 시계가 앞선 경우).
  그 부호를 `clock_skew` 가 보존한다.

**⑨ 드론이 etype 어휘 밖인데 **격리가 아니라 통과**다**

- **물음(VZ):** 브릿지가 `zoneA/+/+/state` 인지 `zoneA/robot/+/state` 인지.
- **확인:** **둘 다 아니다.** `bridge.py:61` 이 `+/+/+/{state,status,heartbeat}` 로
  **구역까지 와일드카드**다. `zoneA/drone/x500-001/state` 는 그대로 걸린다.
- 🔴 **진짜 답:** etype = `drone` 인데 `state.drone.schema.json` 이 없다.
  `envelope.py::validate_payload` 가 *"규격이 없는 조합은 **통과시키고 기록만** 한다 —
  새 노드 타입이 첫 메시지부터 격리되면 안 되기 때문"* 이라 **통과한다.**
  → **드론 상태는 이미 뷰어까지 흐른다. 다만 본문이 엉망이어도 안 걸린다.**
- **켜는 법:** 파일 하나. `known_entity_types()` 가 어휘를 **디렉터리에서 유도**하므로
  `contracts/common/payload/state.drone.schema.json` 을 넣으면 그 순간 켜진다(파이썬 변경 0).
- **브로커 둘:** `IngestBridge` 가 `mqtt.Client` 를 하나만 만든다 → **한 프로세스 = 브로커 하나.**
  둘 띄우면 되고 같은 Kafka 에서 합류한다. `<pi3 LAN 주소>` 는 **사설 LAN** 이라 서버에서 못 닿으니
  **pi3 와 같은 망**에서 돌아야 한다 — 이번 실측의 엣지 브릿지 배치와 같은 모양이다.

**⑩ 🔴 MySQL 이 `docker exec` 로는 안 붙는다 — Phase 2 에서 겪은 것과 같은 오류다**

- **증상:**
  ```
  ERROR 1045 (28000): Access denied for user 'mk2_app'@'localhost' (using password: YES)
  ERROR 1045 (28000): Access denied for user 'root'@'localhost' (using password: NO)
  ```
- **원인:** `mk2_app` 계정이 **`@'172.18.%'` 하나뿐**이다(`_serverinfo/260918_phase4_단계0_실측.txt:199` —
  *"`@'%'` 는 존재하지 않는다"*). `docker exec` 는 유닉스 소켓이라 MySQL 이 `@'localhost'` 로 보고,
  그런 계정이 없다. root 는 `.env` 에 비번 자체가 없다(`using password: NO`).
- **조치:** **이 문서 머리말에 「서버 MySQL 을 보는 법」을 규율로 박았다.** 백엔드 자신의 접속 경로
  (호스트 TCP `127.0.0.1:7858` → `172.18.0.1`)를 쓴다.
- 🔴 **그리고 내 명령이 애초에 틀렸다** — `mk2_app` 에 **DELETE 가 없다.** 의도한 설계다.
  촬영본은 **행을 지우지 않고 아카이브를 purge** 한다(`archive_path=NULL` + `purged_at`, 행은 묘비).
  `capture.py:442` 독스트링이 *"행은 지우지 않는다(`mk2_app` 에 DELETE 가 없다)"* 라고 적고 있었다.
- **남길 것:** **우리 서버를 조회할 때는 우리 코드의 접속 경로를 쓴다.** 컨테이너에 직접 들어가는 것은
  권한 모델을 우회하는 것이고, 그 모델이 곧 규율이다(append-only).

**⑪ 🔴 지금 서버로 오는 업무 데이터가 0이다 — 결함이 아니라 순서다**

HW 회신의 부록과 §8 을 맞춰 읽고 알았다. 회신 문서를 쓰다가 잡혔다.

| 근거 | |
|---|---|
| HW 부록 | `/etc/hw-node.env` — `HW_BROKER_HOST` → **엣지** |
| HW §8 | `[완료] 업무 평면 — 엣지 브로커 경유` |
| HW §8 | `6b-3 · 엣지 브릿지 → Kafka 9095` — 🔴 **tailnet + 백엔드 저장소 대기** |

**말단은 엣지 브로커로 발행하는데 엣지 브릿지가 아직 안 섰다.** 그 사이 서버 Mosquitto 는
아무것도 못 받는다. **실측준비 v2.2 §3-1-2 가 설계한 그대로**이고, 문제는 **순서**다.

- **증거:** 우리 TSDB 의 `go1-001`·`wl-001` 마지막 행이 **16:47:12** 다(발견 ⑦). HW 가
  *"검증을 위해 발행 대상을 일시적으로 서버로 돌렸고 끝난 뒤 다시 엣지로 되돌렸다"* 고 적은 그 시점이다.
- 🔴 **결과:** VZ 가 지금 붙으면 **「연결은 되는데 화면이 빔」**이 나온다. 원인이 어댑터가 아니라
  **위쪽이 아직 안 선 것**이라 찾는 데 시간이 든다.
- **조치:** HW §9-14 · VZ §15-2 양쪽에 **붙어 보는 시점을 엣지 브릿지 기동 뒤로 잡으라**고 적었다.
  그리고 **중간 수단**도 적었다 — 말단 하나만 `HW_BROKER_HOST` 를 서버로 잠시 되돌리면
  **관문 C-a 를 tailnet 과 분리해 미리 닫을 수 있다.**
- **남길 것:** **순서가 곧 관문이다.** `tailnet → 방화벽 → zip → 엣지 브릿지 → 그때 뷰어`.

**⑫ 회신 문서 자기대조에서 HW 쪽 넷이 빠져 있었다**

회신을 다 쓰고 **원본 문서의 절 목록과 한 줄씩 대조**했더니 §9 에 답이 없는 것이 넷이었다 —
§8-2b(IDR 간격 수락) · §3-1~3-3(실측 결과 셋) · §6 구형 retained · 위 ⑪. **VZ 는 열한 안건 전부
대응 절이 있었다.** 채워서 §9-11~9-14 로 넣었다.

**남길 것:** **회신을 쓴 뒤에는 원본의 목차를 뽑아 한 줄씩 대조한다.** 기억으로 훑으면
「숫자를 보내 줬는데 받았다는 말이 없는」 절이 남는다.

### 3 결정한 것 — 여덟

| # | 물음 | 선택지 | 답 |
|---|---|---|---|
| ① | `MK2_MEDIA_SNDBUF` 하향? | A 유지 · B 16KB 로 하향 | **A 유지.** 줄이면 무슨 일이 생기는지를 **이번 실측에서 실제로 보고** 그 결과로 값을 정한다. HW 경고대로 IDR **최대 14,876 B** 기준이면 32KB 는 2장분뿐이다 |
| ② | `otel_metrics.py` 15 vs 60초 | — | **15초. 결정이 아니라 사실 정리다** — `config.py:81` 이 이미 `15.0` 이고 HW `SRS.md` §9.11 이 *"60초 → 15초"* 로 개정했다. **주석과 SRS 세 곳만 낡았다** |
| ③ | VZ `alignment` **부재** 처리 | A 수용 · B 강제 · C 수용+기한 | **C.** 지금 실경로 탐지가 0건이라 위험이 0이다. **실물 탐지가 오는 시점부터 「부재 = unaligned」로 되돌린다** — 그때 부재는 옛 계약이 아니라 생산자 결함이다 |
| ④ | 관측 sidecar gRPC(4316) vs HTTP(4318) | A 셋 다 4318 · B 갈라 쓰기 | **A.** `4316` 은 엣지 전용으로 둔다 — 섞으면 장애 때 엣지 문제인지 VZ 문제인지 안 갈린다. 트래픽은 **60초에 숫자 여섯**이라 프로토콜 효율을 따질 양이 아니다 |
| ⑤ | `detect_sink.py:235` `device_id` | A 개명 · B 그대로 · C 개명+보류 | **C.** **일단 이름만 `source_id` 로 바꿔 둔다**(한 줄). 그 칸을 규격에 남길지는 **다음 구현 단계**에서 정한다 |
| ⑥ | 업로더 식별자 | A 환경변수 우회 유지 · B 매니페스트에 박기 · C 명시 인자 필수 | **C.** 매니페스트 `source_id` 는 **이미 권위**다 — 물음은 「박을지」가 아니라 「무엇으로 채우나」다. **`/etc/device_id` 로 조용히 폴백하는 것이 결함의 본체** |
| ⑦ | `format` 기본값 | A `normalized` · B `absolute` | **A.** 정규화면 기준 해상도가 어긋나도 비율은 맞다. `absolute` 인데 `reference` 가 틀리면 **박스가 통째로 밀린다** — 더 나쁜 실패다. **최종 확정은 AI 회신 뒤**지만 실측은 `normalized` 로 간다 |
| ⑧ | 백엔드 저장소 전달 | A zip · B git | **zip + git push 둘 다.** `_serverinfo/` 는 **뺀다**(실주소가 들어 있다) |

### 4 이 단계가 남긴 것

**서버에 남긴 것 — 코드 반영 + 재기동 (ⓐⓑⓒ + 단계 2-b 어댑터)**

| 파일 | 무엇 |
|---|---|
| `tests/media_publisher.py` | ⓐ — 순번 시점 경고 3곳(머리말·`iter_frames`·`publish`) · `coord` → `bbox_space` |
| `backend/observability.py` | ⓑ — `FORBIDDEN_LABELS` 에 **`utterance_text`** 추가(9종째, VZ 회신으로 이름 확정) |
| `contracts/common/detections.schema.json` | ⓒ — `coord{normalized,ref_*}` → **`bbox_space{format,origin,reference{width,height}}`** · `required` = `["bbox_space","boxes"]` |
| `contracts/common/examples/detections-draft-valid.json` · `…-no-alignment.json` | ⓒ 예시 둘 |
| `contracts/common/README.md` | ⓒ — 금지 라벨 표 9행째 · detections 행 |
| `tests/test_contract_media.py` | ⓒ — `bbox_space` 단언 + 🔴 **:140·:145·:146 뒤늦은 정정**(발견 ①) |
| `tests/test_observability_labels.py` | ⓑ — `utterance_text` 시험 |

**되돌리는 법:** ⓒ 는 초안 규격이라 어떤 검증 경로도 로드하지 않는다 — 이름을 되돌려도
동작에 영향이 없다(단, **규격·예시·시험 셋을 같이** 되돌려야 한다. 발견 ①이 그 증명이다).

**방화벽에 남긴 것**

| 규칙 | 상태 |
|---|---|
| `[40][41][42]` 8766·9095·4316 ← `<HW 개발 PC 구 주소(개인 tailnet)>` | 🔴 **삭제했다**(발견 ④) — 팀 tailnet 주소를 받으면 다시 넣는다 |
| `[40]` 8765 ← `<VZ 데스크톱 tailnet 주소>` · `[41]` 8765 ← `<VZ 노트북 tailnet 주소>` | ✅ **추가**(VZ 뷰어 둘) |
| `[38]` 8765 ← `<이대규 PC tailnet 주소>` · `[39]` 8767 ← `<pi7 tailnet 주소>` | 유지 |

**문서에 남긴 것**

| 파일 | 무엇 |
|---|---|
| `docs/be/hw-envelope-conformance.md` | **§9 신설** — 회신 + 🔴 **§9-1 「HW가 할 일」 1~25 통합표**(세 목록을 대체) |
| `docs/be/vz-media-interface.md` | **§15 신설** — 회신 + §3 뒤집기 + §0 요약표 3·6행 갱신 |

**촬영본 — 실행 완료**

```
아카이브 삭제: /home/dg/capstone-db/media_capture/wl-001/20260910-134408.tar.gz  (1,274,115 B 회수)
행 표시(purged_at): 1 행
rmdir: removing directory, '/home/dg/capstone-db/media_capture/wl-001/'
```

행은 **묘비로 남았다**(`archive_path=NULL`·`purged_at` — append-only). 남은 촬영본은 `go1-001/` 6세션.
**되돌릴 수 없다** — 대조 근거는 발견 ⑥(1바이트 차이).

**`coord` → `bbox_space` 개명 잔재 정리 — 7파일 18곳**

ⓒ 가 규격·예시·시험만 고치고 **참조를 안 고쳤다**(발견 ①의 뿌리). 저장소 전체를 훑어 닫았다.

| 파일 | 무엇 |
|---|---|
| `contracts/common/media-header.schema.json` | `description` 2줄 — `coord.ref_*` → `bbox_space.reference.*`. **규격이 없는 칸을 가리키고 있었다** |
| `backend/gateway/console.html` | 확인용 뷰어 주석 2줄 |
| 🔴 `docs/be/ai-detections-interface.md` | **가장 급했다** — AI 가 회신 전인데 *"이름을 VZ와 정하는 중"* 으로 남아 있었다. §0 에 개명 고지 행 신설 + 예시 JSON + 필드 행 |
| `docs/be/vz-media-interface.md` | §6 에 **「닫힘 — §15-4 를 본다」 배너** · 「가시화가 할 일」 1·4 취소선 |
| `docs/be/02-media-path.md` | 헤더 표 + 예시 JSON + 개명 고지 |
| `docs/be/requirement-traceability.md` | BE-C-03 규격 요약 · **gap ① 을 「VZ 닫힘 / AI 대기」로** |
| `docs/be/01-standalone-implementation-plan.md` | AI·VZ 행 현황 |

**안 고친 것 — 동결 이력:** `docs/be/tasks/작업지시_phase4_…` · `reports/2026-09-17…` ·
`reports/2026-09-18…` 둘. **그때 그렇게 물었다는 기록**이라 고치면 왜 이름이 바뀌었는지 근거가 사라진다.

### 검증한 것

**🔴 서버 회귀 — `314 passed, 12 skipped in 115.21s`**

```
314 passed, 12 skipped in 115.21s (0:01:55)
```

- **272(Phase 4 기준선) + 39(어댑터) + 1(진짜 소켓 관통) + 2(라벨·규격) = 314.** 기대값과 일치.
- **기존 272건이 하나도 안 깨졌다** — 이중 형식의 핵심 판정이 닫혔다.
- **`test_ws_delivery_vz_wire`(진짜 WebSocket + 구독)가 이번에 돌았다** — 단계 2-b 의
  「검증하지 못한 것」 중 **VZ 형식 가지**가 닫혔다.
- skip 12 는 전부 `test_media_relay.py` 이고 사유가 한 줄로 찍혔다 —
  *"엣지 입구(8766)에 닿지 않는다"*. **8766 을 여는 단계 1 전에는 정상값**이다.

**로컬(선생님 PC) 실제 pytest** — 재구현이 아니라 시험 파일 실행:
`test_contract_media.py` **17 passed** · `vz_wire`+`ws_state_wire`+`observability_labels` **99 passed** ·
`tests/` 전체 **326 collected, 수집 오류 0**.

### 5 검증하지 못한 것

- 🔴 **VZ 앱 실접속** — 어댑터가 올라갔고 방화벽도 열렸지만 **아직 붙어 보지 않았다.**
  구역 값 `zoneA` 가 VZ 연결 설정에 들어갔는지도 미확인이다(빈 화면의 1순위 원인).
- 🔴 **홉2 의 진짜 서버 도달** — HW 는 **모의 입구**로만 검증했다. tailnet 합류 뒤다.
- **`8766`·`9095`·`4316`** — 아직 안 열었다(단계 1). HW 새 주소 대기.
- **드론 `state` 본문 규격** — VZ 회신 대기. 그때까지 본문 검증은 **건너뛴 채로** 흐른다.
- **`stream_start` 의 중복 start 가드 이관** — HW 가 `stream` 을 쪼갰다고 했으나
  §8-11 ⓑ(가드)도 같이 옮겼는지 한 줄 확인이 필요하다(할 일 22).
- **`edge/` 폴더 갈림의 해소** — HW 가 `git checkout origin/HW -- edge/` 를 했는지.
- **AI(진나영) 회신** — `bbox_space`·`format` 기본값의 **생산자**가 AI 다. 아직 회신 전이다.
- **현장 회선** — HW 실측도 *"같은 방 안이라 바닥 기준선"* 이다. `SNDBUF` 판단의 입력이 아직 없다.
---

## 단계 D — 커밋 전 전수검사 (2026-09-21)

**`git add . && git commit && push` 직전에 멈추고 영향받은 파일을 전부 다시 봤다.**
**한 번에 확실하게 하자**는 판단이었고, 실제로 **파일을 또 고치는 것보다 싸게 끝났다.**

### 1 문서와 다르게 한 것

**검토를 내가 혼자 하지 않았다 — 검토자 셋을 따로 붙였다**

내가 쓴 회신을 내가 다시 읽으면 **쓸 때 한 착각을 읽을 때 그대로 반복한다.** 그래서 원본과
회신만 주고 「적대적으로 대조하라」고 시킨 검토자 셋을 병렬로 돌렸다 — HW 대조 · VZ 대조 ·
개명 정합. **셋이 합쳐 서른 건 넘게 찾았고, 그중 내가 혼자서는 못 찾았을 것이 여럿이다.**

### 2 발견한 것

**① 🔴 저장소에 서버 공인 IP 가 이미 커밋돼 있다 — Phase 1 부터**

```
backend/settings.py:151   DEFAULT_BROKER_HOST = "<서버 공인 IP>"  # 서버 Mosquitto (익명, 1883)
backend/settings.py:11    표에도 같은 값
                          ※ 실값은 그 파일에 있다 — 이 로그에 옮겨 적지 않는다
```

- **이미 `origin/ldg_BE` 에 있다**(`a043a86 phase 1 완료`). 즉 **지금 고쳐도 히스토리에 남는다.**
- 같은 값이 `작업지시_phase1_…`(7곳) · `reports/2026-09-07_…` · `tests/publisher.py` 에도 있다.
- 🔴 **주석이 「익명」이라고 적고 있다** — 인증 없는 MQTT 브로커의 **공인 주소**가 공개 저장소에 있다.
- **이번에 생긴 것이 아니다.** `reports/2026-09-18_2100_phase4_작업로그.md:358` 이 이미
  *"(그 IP) … 이미 여러 번 커밋돼 있어 **이번 범위에서 다루지 않았다**"* 로 미뤄 두었다.
- **조치: 이번에 고치지 않았다.** 값을 바꾸면 Phase 1 시험 기본값이 같이 움직이고,
  이미 공개된 것이라 **되돌리는 것과 앞으로 막는 것이 별개 작업**이다. → 아래 「5 검증하지 못한 것」.

**② 🔴 VZ 문서의 뒤집힘이 절반만 반영돼 있었다**

`/state` 통지(§3)를 §15 가 뒤집었는데, **§3 절 자체에는 아무 표시가 없었다.** 그런데
§15-1 이 *"[§3](#s3) 을 이 절이 뒤집는다"* 며 **그 절로 링크까지 걸어 두었다** — 링크를 타고 간
독자가 아무 경고 없이 「붙지 마라」를 읽는다.

| 자리 | 상태 |
|---|---|
| §0 요약표 3·6행 | ✅ 되어 있었다 |
| 「가시화가 할 일」 1·4 | ✅ 되어 있었다 |
| **§3 절** | 🔴 **없었다** |
| **머리말 우선순위 줄** | 🔴 **없었다**(문서의 첫 표다) |
| **§10 절 제목·§0 10행·44-45·239·269·324** | 🔴 **여섯 곳이 *"받기 전에는 열지 않는다"* 그대로** |
| **「가시화가 할 일」 2·3·5** | 🔴 **🔴 그대로**(§15-10 은 ✅ 라고 한다) |

**조치:** 여섯 자리 전부에 취소선 + 🔄 배너를 달았다. 상단 배너도 *"1·4·5"* → *"여덟 항목 전부"* 로 고쳤다.

**③ 🔴 내 문서가 스스로를 뒤집고 있었다 — 「지금 붙을 수 있다」 ↔ 「지금 붙으면 안 뜬다」**

§15-2 에 「상류가 안 섰으니 붙는 시점을 미뤄 달라」를 **나중에 끼워 넣으면서**,
§15-0 요약표와 문서 **맺음말**의 *"뷰어 토큰만 있으면 지금 붙을 수 있다"* 를 안 고쳤다.
**문서가 「바쁘면 요약만 봐도 된다」고 권하는데, 그 요약이 본문 경고를 무효화한다.**

- **조치:** 둘 다 고쳤다. 그리고 §15-2 의 「화면만 빔」 의심 순서에 **0️⃣ 상류가 섰는가**를
  맨 앞에 넣었다 — 그 전에는 구역 값이 1순위로 적혀 있어 **엉뚱한 곳을 붙들게** 돼 있었다.
- **남길 것:** **나중에 끼워 넣은 경고는 요약과 맺음말까지 따라가야 한다.** 본문만 고치면
  요약이 거짓말을 한다.

**④ 🔴 HW 가 재서 보낸 것 중 받지 않은 것이 있었다 — NAL 분포**

HW §2 가 NAL 분포 표를 주며 *"**AUD 892개 = 프레임 892장.** 즉 **로봇 WS 청크 1개 = AU 1개**가
실측으로 확인됐습니다 — §8-2 에서 지적하신 가정이 실제로 맞습니다"* 라고 적었다.

**§8-2 가 🔴 로 열어 둔 가정이 닫힌 것인데 §9 에 한 줄도 없었다.** 우리가 합성 fixture 로는
영원히 확인할 수 없는 자리다. SPS/PPS 가 IDR 마다 인밴드(60/60)인 것도 같이 왔다 —
**뷰어가 중간에 붙어도 첫 IDR 하나로 디코더를 세울 수 있다**는 근거다.

- **조치:** §9-12 ④ 를 신설했다.

**⑤ HW 숫자 안에 안 맞는 것이 있다 — 되묻는다**

§3-3 이 홉1 수신 **900** · 홉2 전송 **643** · 역압으로 버림 **64** 라고 적었다.
**900 − 643 = 257 인데 버린 것은 64 로 보고됐다. 193장의 행방이 표에 없다.**

- **조치:** 덮지 않고 §9-12 ③ 에 **되묻는 문단**으로 넣었다. 다른 자리에서 또 버렸는지,
  두 숫자가 서로 다른 구간을 잰 것인지 물었다.
- **남길 것:** **받은 숫자가 자기들끼리 안 맞으면 조용히 옮겨 적지 않는다.**

**⑥ 내 추론 하나가 거꾸로였다 — 1바이트의 부호**

`wl-001`(6자) → `go1-001`(7자) 이니 **긴 쪽이 커야** 하는데 실제로는 **짧은 쪽이 1바이트 크다.**
`.tar.gz` 라 ⓐ tar 가 512바이트 블록으로 패딩하고 ⓑ gzip 을 거치므로 **원본 1글자 차이가
결과에서 +1·−1·0 중 무엇이 될지 정해지지 않는다.**

- **판정 자체는 맞았다** — 근거가 **부호가 아니라 「1.27 MB 중 1바이트만 다르다」는 크기**이고,
  세션 이름이 같다는 것과 HW §5-3 설명이 더해진다. **논리만 틀렸다.**
- **조치:** §9-2-2 의 설명을 고쳤다. **purge 는 이미 실행했고, 판정은 유효하다.**
- **남길 것:** **압축 파일의 바이트 차이로 원본 글자 수를 역산하지 않는다.**

**⑦ HW 가 물은 둘에 답이 없었다**

§1-4 의 *"**브랜치 zip이 필요한지**"* 는 **HW 가 우리에게 보낼지**를 묻는 것인데,
§9-10 은 **우리가 HW 에게 보내는** 이야기만 했다. 방향을 반대로 읽었다.
*"백엔드 저장소를 **받을 경로**"* 도 넣을 것/뺄 것만 적고 **주소를 안 줬다.**

- **조치:** §9-10 에 표로 넣었다 — **zip 보내 달라(작업 트리 기준)** · **git `ldg_BE` 브랜치가 먼저**.

**⑧ §8 안에 옛 이름이 남아 있었다 — 한 문서가 자기모순**

`hw-envelope-conformance.md` 는 §8(09-19 회신)에 `coord.ref_*` 가 둘 있는데
§9-15 는 *"`coord` → `bbox_space` 로 바꿨다"* 고 적는다. **같은 파일 안에서 갈렸다.**
`coord` 정리(단계 C)에서 이 파일을 훑지 않았다 — **회신을 쓰는 파일이라 「문서」로 안 세었다.**

- **조치:** 둘 다 고치고 **「09-19 에는 `coord.ref_*` 로 적었다」**를 같이 적었다(이력 보존).

**⑨ `.claude/` 가 gitignore 에 없었다**

`git add .` 하면 `.claude/settings.local.json` 이 같이 올라간다. 비밀은 없었지만
**사람마다 다른 로컬 설정**이라 공유할 것이 아니다. → `.gitignore` 에 추가.

**⑩ `.git/index.lock` 이 09-19 부터 남아 있다**

`git add` 가 *"Another git process seems to be running"* 으로 죽는다. **2일 된 유령 잠금**이다.
→ 지우면 된다(아래 「4 남긴 것」).

**⑪ ⚠ 내가 검토자에게 옛 사본을 줬다 — 거짓 양성 셋**

검토자 보고 중 셋이 **틀렸다**: *"`observability.py` 에 `utterance_text` 가 없다"* ·
*"`02-media-path.md` 에 `bbox_space` 가 0건"* · *"`requirement-traceability.md` 가 아직 `coord`"*.
**실파일로 재확인하니 셋 다 이미 고쳐져 있었다.** 원인은 내가 검토자에게 넘긴 사본이
**고치기 전 시점**이었던 것이다.

- **남길 것:** 🔴 **검토자에게 파일을 넘길 때는 그 시점의 실파일을 다시 뜬다.**
  그리고 **검토 결과는 실파일로 한 번 더 확인하고 움직인다** — 안 그러면
  **이미 맞는 것을 고치다가 망가뜨린다.**

### 3 결정한 것

| 물음 | 답 |
|---|---|
| `DEFAULT_BROKER_HOST`(서버 공인 IP) 를 이번에 고치나 | **안 고친다.** 이미 공개됐고 시험 기본값이 같이 움직인다 — **별건으로 본다**(발견 ①) |
| §3·§10 같은 「옛 통지」를 고치나 지우나 | **고치지 않고 배너를 단다.** 무엇을 물었는지가 남아야 왜 바뀌었는지가 설명된다 |
| HW 숫자 불일치를 덮나 묻나 | **묻는다**(발견 ⑤) |
| `<pi3 LAN 주소>` | **자리표시자로.** 사설 대역이라 위험은 낮지만 **규율을 한 문서 안에서 깨지 않는다** |

### 4 이 단계가 남긴 것

**고친 파일 여섯 · 쉰 곳**

| 파일 | 곳 | 무엇 |
|---|---:|---|
| `docs/be/vz-media-interface.md` | 26 | 뒤집힘 반영 여섯 자리 · 요약↔본문 모순 · 의심 순서 · 코덱 문자열 셋 · 번호 체계 · 자리표시자 |
| `docs/be/hw-envelope-conformance.md` | 17 | §9-12 ④ **AUD 신설** · §9-10 두 물음 · IDR 최대 15,831 · 되묻기 · #23 정정 · 1바이트 논리 · §8 옛 이름 |
| `docs/be/ai-detections-interface.md` | 4 | 🔴 **`bbox_space.format` 회신 요청 추가** · 작성일 · 행 번호 |
| `docs/be/02-media-path.md` | 1 | 예시 **설명문**(`normalized:true`·`ref_width/height`) — 단계 C 에서 JSON 만 고쳤다 |
| `contracts/common/detections.schema.json` | 1 | `$comment` 에 `origin` 빠져 있었다 |
| `.gitignore` | 1 | `.claude/` |

**기계 검사 결과**

| 항목 | 결과 |
|---|---|
| 추적 파일 안의 `100.x` 실주소 | **0건** |
| 추적 파일 안의 `192.168.x` (시험 fixture 제외) | **0건** |
| 토큰 문자열 | **0건** |
| 마크다운 앵커 깨짐 | **0건**(HW 48개 · VZ 30개) |
| `_serverinfo/`·`.env`·`infra/docker-compose.yml`·`infra/config/`·`media_capture/` | **전부 IGNORED ✅** |
| `detections.schema.json` · `media-header.schema.json` | **JSON 유효 ✅** |

**사람이 할 것 둘**

```bash
# ① 유령 잠금 제거 (09-19 부터 남아 있다 — 이게 있으면 git add 가 죽는다)
rm -f .git/index.lock
```

② **`<서버 공인 IP>`** — 발견 ① 을 읽고 정한다. 급하면 서버 `1883` 이 인터넷에 열려 있는지부터 본다.

### 5 검증하지 못한 것

- 🔴 **서버 공인 IP(`settings.py:151`) 의 실제 노출 범위** — 서버 `1883` 이 tailnet 밖에서 닿는지 안 재 봤다.
  **닿는다면 인증 없는 브로커가 열려 있는 것**이고, 안 닿는다면 주소만 새어 있는 것이다. **먼저 이것부터 잰다.**
- **HW §6-13 ⑥ 확인 항목 ①②(격리 파일)** — TSDB 에 1,036건이 전부 들어온 것이 **간접 증거**이지만
  격리 파일을 직접 열어 보지 않았다. §9-8 에 그대로 적었다. **단계 1 에서 조회한다.**
- **HW §3-3 의 193장** — 되물었다. 답이 와야 「버리기가 어디서 일어나는가」가 닫힌다.
- **세 회신 문서를 받는 쪽이 실제로 읽었을 때** — 링크·번호가 맞는지는 기계로 확인했지만
  **읽는 사람이 헤매는지는 확인할 방법이 없다.**

---
