# 작업 지시: STT 이식 — 발화를 실제로 받는다

**선행**: 0·1단계 완료 · 통합 셸과 공유 계층 (`작업프롬프트_통합_260827.md`)
**대상 요구사항**: `VZ-L-01` · `VZ-L-03` · `REQ-1301`~`1306` · `REQ-305`
**참고 구현**: `stt-lab/` (2026-08-25 구축, `reports/2026-08-25_1506_stt-model-comparison-lab.md`)
**대상 문서**: `구현진행_가시화도구_260826.md` §3-0 · `기술스택_가시화도구_260826.md`

---

## 배경

1단계 마일스톤 화면 상단에는 발화 처리 진행 막대가 다섯 칸으로 그려져 있다.

```
음성 수신 → STT 변환 → 의도 분석 → 마일스톤 분리 → 태스크 생성
```

**지금은 다섯 칸 전부 목이다.** 이번 작업은 **앞의 두 칸을 진짜로 만드는 것**이다.
뒤의 세 칸(`VZ-G-01`·`VZ-G-02`)은 로컬 LLM이 필요하므로 다음 작업으로 넘긴다.
이 경계를 넘지 않는다 — 한 번에 다섯 칸을 다 하려 들면 "인식이 나쁜 건지 해석이 나쁜 건지"를
가를 수 없게 된다.

**바닥부터 만들지 않는다.** `stt-lab/`에 8/25에 이미 만들어 둔 것이 있다.

| stt-lab에 이미 있는 것 | 이번에 쓰는 방식 |
|---|---|
| `server/engines/base.py` — `SttEngine` 프로토콜, `TranscribeOptions`, `TranscribeResult` | **그대로 쓴다.** `REQ-1302` 추상화가 이미 이 파일에 있다 |
| `server/engines/faster_whisper.py` — CUDA→CPU 자동 폴백, 모델 캐시, 로드 시간 분리 | 그대로 쓴다 |
| `server/registry.py` — 레지스트리 `display_name`·`aliases[]` → hotwords 42개 | 그대로 쓴다. `REQ-305`가 이걸로 충족된다 |
| `POST /api/transcribe` — 오디오 한 건 → 텍스트 + 원본 수치 | 가시화 웹이 부를 엔드포인트 |
| `web/index.html`의 `MediaRecorder` 녹음 | **방식만 참고**한다. 코드를 복사하지 않는다 |
| `/api/score`, `/api/runs`, 프리셋 67건, CSV 내보내기 | **가져오지 않는다.** 실험 하네스 기능이다 |

---

## 먼저 읽을 것

1. **`stt-lab/server/engines/base.py`의 파일 머리 주석.** 엔진 추상화가 무엇을 약속하는지가 여기 있다.
   특히 `applied_options` — "요청했지만 안 먹은 옵션"이 응답에 드러나야 한다는 규칙.
2. `stt-lab/server/main.py`의 `api_transcribe` — 폼 파라미터와 응답 형태.
3. `reports/2026-08-25_1506_stt-model-comparison-lab.md` §주요 판단 — 특히
   **"신뢰도 임계값은 아직 정하지 않았다"**. 이번 작업이 그 결론 위에 선다.
4. 엑셀 김현우 시트 `VZ-L-01`·`VZ-L-03`의 요구사항 정의 원문.
5. `viz-debugger/src/shared/commandEgress.ts` — 명령 출구가 하나라는 제약과 감사 필드.

---

## 아키텍처 판단 — 먼저 정하고 시작한다

**결론: `stt-lab`을 고치지도 복사하지도 않는다. 있는 그대로 로컬 STT 서비스로 띄우고 HTTP로 부른다.**

근거 셋.

1. **복사하면 엔진 구현이 두 벌이 된다.** CUDA 폴백·모델 캐시처럼 손이 많이 간 코드가 갈라지면
   어느 쪽이 원본인지 관리해야 한다. 0단계에서 `contracts/`를 복사하지 않고 상대 경로로 참조한 것과 같은 이유다.
2. **개발 단계에서는 실험 기능이 함께 떠 있는 편이 낫다.** `VZ-L-03`의 신뢰도 임계값은 아직 미정이고,
   그 값을 정하려면 `/api/runs`의 누적 기록과 CER 계산이 필요하다. 지금 떼어내면 임계를 정할 도구를 잃는다.
3. **브라우저 내장 음성 인식(Web Speech API)은 쓰지 않는다.** 엔진 종속이라 `REQ-1302`에 어긋나고,
   구현체가 클라우드로 오디오를 보내므로 `NFR-010`(로컬 처리 원칙)과 충돌한다.

**운영 경계가 필요해지는 시점은 따로 온다.** `/api/score`·`/api/runs`가 운영 화면에서 보이면 안 되는
단계가 되면, 그때 `engines/`와 `registry.py`만 떼어 얇은 서비스로 만든다. **이번에는 하지 않는다.**
대신 가시화 쪽 호출을 **어댑터 한 파일 뒤에 가둬서**, 그때 그 파일만 바꾸면 되게 한다.

포트는 stt-lab 기본값 `8799`를 그대로 쓴다(가시화 5174, 목 게이트웨이와 겹치지 않는다).

---

## 제약 — 반드시 지킬 것

1. **`stt-lab/`을 한 줄도 고치지 않는다.** 읽기 전용이다. 부족한 것이 있으면 고치지 말고 보고한다.
2. **`web-dashboard/`도 한 줄도 고치지 않는다.** 기존 검증 7종이 계속 통과해야 한다.
3. **명령 출구는 여전히 하나다.** 음성으로 발행되는 명령도 `shared/commandEgress.ts`의
   `issueCommand()`를 거친다. 음성 전용 발행 경로를 만들면 `verify:single-egress`가 잡는다.
   달라지는 것은 **경로가 아니라 감사 필드**다(제약 6).
4. **STT가 없어도 화면이 뜬다.** `stt-lab` 서버가 꺼져 있거나 응답하지 않으면
   **그 기능만 비활성화하고 수동 텍스트 입력 경로를 남긴다**(`VZ-C-02`, `VZ-G-01` 명시).
   화면 전체가 죽거나 무한 로딩에 걸리면 안 된다. `verify:no-stt`로 검사한다.
5. **임계값을 지금 정하지 않는다.** `VZ-L-03`의 신뢰도 임계는 실측 후 확정(Tier 2)이다.
   **판정이 일어나는 지점을 만들고 값은 설정 한 곳으로 뺀다.** 코드 여기저기에 숫자를 박지 않는다.
   지금 넣는 값은 잠정값이며 화면과 설정 파일에 **"잠정 — 실측 미완"**이라고 적는다.
6. **감사 필드를 빠뜨리지 않는다**(`REQ-1305`). 음성으로 발행된 명령의 `audit`에는
   `input_modality: 'voice'` · 발화 원문 · 인식 신뢰도 수치 · 엔진/모델 식별자가 실려야 한다.
   **기록은 백엔드 audit-writer가 한다.** 브라우저는 전달만 하고 직접 쓰지 않는다.
7. **의존성을 늘리지 않는다.** 녹음은 브라우저 표준 `MediaRecorder`로 충분하다.
   오디오 라이브러리·음성 인식 SDK를 넣지 않는다.
8. **목임을 감추지 않는다.** 이번에 진짜가 되는 것은 앞 두 칸뿐이다.
   진행 막대의 뒤 세 칸에는 **아직 목이라는 표시가 남아야 한다.**
9. **하나의 confidence를 지어내지 않는다.** Whisper는 단일 신뢰도를 주지 않는다.
   `avg_logprob`·`no_speech_prob`·단어 확률을 **각각 그대로** 들고 다닌다.
   셋을 임의 가중합해 0~1 점수 하나로 뭉개면 임계를 실측할 근거가 사라진다.

---

## 단계

### 1단계 — 어댑터 경계부터 (화면보다 먼저)

`viz-debugger/src/stt/` 에 만든다.

| 파일 | 역할 |
|---|---|
| `SttClient.ts` | 가시화가 보는 유일한 STT 면. `transcribe(blob) → SttResult` 하나. **여기 밖에서 `fetch('/api/transcribe')`를 부르지 않는다** |
| `HttpSttClient.ts` | stt-lab HTTP 구현. 폼 조립·오류 변환·타임아웃. 출처 주석 필수 |
| `types.ts` | `SttResult` — `text` · `segments` · `words` · `avgLogprob` · `noSpeechProb` · `meanWordProb` · `engine` · `model` · `device` · `elapsedSec` · `appliedOptions` |
| `confidence.ts` | 임계 판정 한 곳. 잠정값과 "실측 미완" 주석 |

`HttpSttClient.ts` 머리에 출처를 남긴다.

```ts
// 참조: stt-lab/server/main.py @ <커밋> — POST /api/transcribe 폼 계약
// stt-lab은 읽기 전용이다. 이 파일이 계약 변화를 흡수하는 유일한 지점.
```

**판정 기준**: 화면 없이, 콘솔에서 `SttClient`로 `stt-lab/samples/`의 webm 한 건을 보내
텍스트와 세 수치가 돌아오는 것까지 확인한다.

### 2단계 — 녹음과 1차 확인 (`VZ-L-01`)

- 마일스톤 화면의 발화 패널에 **녹음 버튼**을 붙인다. `MediaRecorder` + `audio/webm;codecs=opus`.
  브라우저가 지원하지 않으면 파일 업로드로 떨어진다.
- 녹음 중에는 지금 그려진 가짜 파형 대신 **실제 입력 레벨**을 보여준다
  (`AnalyserNode` 하나면 된다. 라이브러리 금지).
- 전사가 끝나면 **인식된 문장을 그대로 표시하고 사용자가 고칠 수 있게 한다** — 이것이 1차 확인이다.
  고친 문장이 다음 단계의 입력이 되며, **원문과 수정본을 둘 다 보관한다.**
  (원문만 남기면 오인식을 놓치고, 수정본만 남기면 STT 성능을 평가할 수 없다.)
- 진행 막대의 `음성 수신`·`STT 변환` 두 칸이 실제 상태를 따라 움직인다.
  실패하면 실패한 칸에서 멈추고 이유를 보여준다.

**판정 기준**: 마이크로 "503 구역 로봇을 5층 복도로 이동시켜"를 말하면
그 문장이 화면에 뜨고, 수정 가능하며, 뒤 세 칸은 목 표시가 붙은 채 남아 있다.

### 3단계 — 레지스트리 어휘 (`REQ-305`)

`stt-lab`이 `use_hotwords=true`일 때 레지스트리 원본에서 hotwords를 매번 새로 뽑는다.
**가시화는 어휘 문자열을 만들어 보내지 않는다.** 플래그만 켠다.

- 화면에 **hotwords가 몇 개 적용됐는지**를 표시한다(응답의 `applied_options`에서 읽는다).
- hotwords를 끈 상태와 켠 상태를 나란히 시험할 수 있게 토글을 둔다.
  `VZ-L-03` 임계 실측 때 필요하다.

**여기서 흔한 실수** — 화면이 보내온 어휘 문자열을 믿으면 레지스트리가 바뀌었을 때
조용히 옛 어휘로 인식하게 된다. `stt-lab/server/main.py`의 해당 주석을 그대로 지킨다.

### 4단계 — 신뢰도 판정 지점 (`VZ-L-03` 1차 확인 쪽)

- `confidence.ts`에 판정 함수 하나. 입력은 세 수치, 출력은 `accept | confirm | reject`.
- 잠정 기준을 넣되 **숫자마다 근거를 주석으로 적는다.** 근거가 "감"이면 그렇게 적는다.
- 임계 미만이면 **다음 단계로 넘기지 않고 재확인을 요구한다.** 조용히 통과시키지 않는다.
- 화면에 세 수치를 **접어서 볼 수 있게** 둔다. 평소엔 감추고, 펴면 원본 값이 보인다.

**2차 확인(해석이 맞는가)은 이번 범위가 아니다.** `VZ-G-01`이 생긴 뒤에 붙는다.
지금은 **자리만 만들고 "해석 단계 미구현"으로 표시한다.**

### 5단계 — 감사 필드 (`REQ-1305`)

`commandEgress.ts`의 `audit`을 넓힌다. **출구는 그대로 하나다.**

```ts
audit: {
  produced_by: 'human',
  input_modality: 'pointer' | 'voice',
  voice?: { transcript, transcript_edited, avg_logprob, no_speech_prob, mean_word_prob, engine, model },
}
```

- `input_modality: 'voice'`인데 `voice`가 비어 있으면 **발행을 거부한다.** 타입만으로 막지 말고
  런타임에서도 막는다 — 나중에 다른 사람이 음성 경로를 추가할 때 조용히 빠지는 곳이다.
- 브라우저는 감사 **필드를 전달만** 한다. Log Record 작성은 백엔드다(`REQ-907`·`NFR-002`).

### 6단계 — 검증 스크립트 두 개

- **`verify:voice-audit`** — `input_modality: 'voice'` 경로가 `voice` 필드 없이 발행되면
  실패로 잡히는지. **일부러 필드를 빼는 음성 대조군까지 넣는다**(기존 두 verify와 같은 방식).
- **`verify:no-stt`** — `SttClient`가 전부 실패를 던지는 상태에서 화면이 뜨고
  **수동 텍스트 입력 경로가 살아 있는지.**

기존 `verify:single-egress`·`verify:standalone`이 계속 통과해야 한다.
특히 **`verify:standalone`** — `src/stt/`가 셸이나 다른 탭을 끌어오면 단독 빌드가 깨진다.

### 7단계 — 문서

- `기술스택_가시화도구_260826.md`에 **§STT 계층**을 추가한다. 위의 아키텍처 판단 셋과
  "언제 stt-lab에서 떼어내는가"의 조건을 적는다.
- `viz-debugger/README.md`에 실행 절차 — stt-lab을 함께 띄워야 한다는 것과, 안 띄웠을 때의 동작.
- `HANDOFF.md`에 녹음·확인 화면이 생겼음을 반영하고, **논의 항목에 한 줄 추가**:
  "인식 결과 수정을 어디서 하게 할 것인가 — 발화 패널 안인가, 별도 확인 단계인가".

---

## 완료 판정

1. 마이크로 말하면 **실제 텍스트가 뜬다.** 목이 아니다.
2. 인식 문장을 고칠 수 있고, **원문과 수정본이 둘 다 남는다.**
3. `avg_logprob`·`no_speech_prob`·평균 단어 확률 셋이 **각각** 화면에서 확인된다. 하나로 뭉개지지 않았다.
4. 임계 미만이면 재확인을 요구하고 다음으로 넘어가지 않는다.
5. **stt-lab 서버를 끈 채로도 화면이 뜨고 수동 입력이 된다** (`verify:no-stt` 통과).
6. 음성 명령이 `issueCommand()` 한 곳으로 나가고 `voice` 감사 필드가 실린다
   (`verify:single-egress` · `verify:voice-audit` 통과).
7. 진행 막대의 뒤 세 칸에 **아직 목이라는 표시**가 남아 있다.
8. `stt-lab/`·`web-dashboard/`에 diff가 없다.

---

## 보고

`reports/YYYY-MM-DD_HHMM_제목.md` 형식으로 남긴다. 담을 것.

- 실제 발화 몇 건의 **세 수치 실측값** — `VZ-L-03` 임계를 정하는 첫 자료다.
  정답 발화·오인식·무음·소음 각각으로 나눠 적는다.
- **잠정 임계로 무엇을 넣었고 근거가 무엇인지.** 근거가 약하면 약하다고 적는다.
- 녹음 형식(webm/opus)이 stt-lab에서 문제없이 디코딩됐는지, 브라우저별 차이
- stt-lab을 고치고 싶었던 지점 — **고치지 말고 여기 적는다.** 떼어낼 시점을 정하는 근거가 된다.
- 다음 작업(`VZ-G-01` 마일스톤 분리)으로 넘길 미결

**막히면 임의로 정하지 말고 보고서에 남기고 멈춘다.** 특히 신뢰도 임계와 감사 필드 스키마는
다른 파트와 걸린 결정이라 혼자 바꾸지 않는다.
