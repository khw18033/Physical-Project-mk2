.venv/
__pycache__/
*.pyc

# 실행 기록. 측정값은 **각자 장비에서 다시 뽑아야** 의미가 있고(같은 모델도 GPU·드라이버가
# 다르면 시간이 다르다), 원문에는 모델이 낸 문장이 통째로 들어 있다.
# 표는 보고서(reports/)에 남는다 — 숫자는 거기서 읽는다.
runs/

# GGUF 가중치. 셋이 12 GB 다. 받는 절차는 README 에 있다.
models/

# llama-server 바이너리와 CUDA 런타임. 압축 해제 후 ~1 GB 이고, 빌드 번호가 박힌
# 남의 배포물이라 저장소가 들 것이 아니다. 받는 절차는 README 에 있다.
vendor/
