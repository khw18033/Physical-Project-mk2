"""생성 엔진. **아직 하나도 없다.**

모델이 정해지지 않았다 (지시서 §4 의 모델 후보 비교 대기). 엔진을 붙일 때 파일 하나를
여기 더하고 `server/main.py` 의 `_load_engine()` 이 그것을 고르게 한다 —
**서버 파일에 엔진 이름이 나오면 안 된다** (`stt-lab` 과 같은 규칙 · REQ-1302).

`stt/engines/*.py` 처럼 원본에서 이식하는 것이 아니라 여기서 새로 쓴다.
그래서 `verify:stt-port` 같은 바이트 동일성 검사가 이 폴더에는 없다.
"""

def available() -> list[str]:
    """붙어 있는 엔진 이름. 지금은 빈 목록이고, 그 사실을 숨기지 않는다."""
    return []
