# 가시화 파트 작업 계획 — 2026-08-24

> 요구사항 시트 갱신(진나영 49행 · 조병현 전면 재작성) 반영 후 정리한 작업 계획.
> 기준 문서 — `현황정리_260824.md`(대조 결과) · `가시화_구현현황_260824.md`(구현 대조)

---

# 0. 지금 어디 서 있나

| | |
|---|---|
| 요구사항 | **33건** (수신 11 · 송신 5 · 화면 7 · LLM/음성 4 · 공통 6) |
| 동작하는 것 | **27건** — 데이터 뼈대 + 판단·알림·계층 뷰 + 노드 편집기 + 트윈 뷰어 |
| 남은 것 | **6건** — 음성·LLM과 합의 대기 |
| 클라이언트 | 웹 대시보드(화면 7) + Unity 트윈 뷰어. **두 화면이 같은 계약을 같게 해석함(검증 20건 일치)** |
| 마지막 기능·문서 커밋 | `09add2c` — 문서 최신화와 영상 참조 프레임 누락 표시 |
| 현재 작업 | 구판 산출물 아카이브와 요구사항정의서 v0.5 정합 |

**지난주와 달라진 것은 내 코드가 아니라 상대 계약입니다.** 내 시트는 아무도 안 건드렸는데, 내가 기대던 상대 요구사항의 뜻이 바뀌었습니다.

---

# 1. 요구사항 — 내가 해야 할 것

## 1-1. 이미 반영 완료 (오늘)

시트 **연관점 7행 · 정의 6행**을 고쳤습니다. 변경분은 파란 글씨.

| 무엇 | 왜 |
|---|---|
| `AI-D-03` 참조 5곳 제거 | 「명령 의도 해석」 → 「의사결정 보조 정보 요청」으로 **완전히 다른 요구사항이 됨** |
| `AI-E-02` → `AI-E-04` (`VZ-I-07`) | 「이동형 보조 인지」 → 「카메라 자동 캘리브레이션」으로 바뀜 |
| `AI-C-04` → `AI-C-05` (`VZ-L-04`) | 「AI 책임 범위 규약」 → 「외부 입력·실행 어댑터」로 바뀜 |
| `VZ-I-06` + `AI-C-08`·`AI-C-14` | 미디어 어댑터·미디어 경로 신설 |
| `VZ-I-07` 정의 | 온디바이스(최소 안전)와 엣지(정밀) **인지 급이 갈림** — 화면에서 출처 구분 |
| `VZ-I-09` 정의 | 다중 관측 연계가 **선택 기능**이 됨 — 없을 때 동작 명시 |
| `VZ-L-01`·`02`·`03` 정의 | 의도 해석 담당 부재를 **[확인 요망]으로 명시** |

> ⚠️ **8/21에 넣은 파란 표시가 전부 날아가 있었습니다.** Excel 재저장 과정에서 서식이 평문화된 것으로 보입니다. 팀에 넘기기 전 확인 필요.

## 1-2. 회의에 가져갈 것 — 우선순위 순

| # | 안건 | 상대 | 정하지 않으면 |
|---|---|---|---|
| **1** | **`BE-T-06` 캐시 범위 회신** | 이대규 | **상대가 나를 기다리는 유일한 항목.** 시트에 "[회의 확정 대기] 가시화 요구 확인 후 조정"으로 적혀 있음 |
| **2** | **명령 의도 해석 4문항** (아래) | 진나영 | `VZ-L-02`가 받을 것이 없는 요구사항으로 남음 |
| **3** | **미디어 경로의 뷰어용 출력 분기** | 3자 | 실제 카메라를 붙이는 순간 막힘 |
| **4** | **capability 상태가 가시화까지 오는가** | 진나영 | 오지 않으면 관련 요구사항을 만들어도 읽을 값이 없음 |
| **5** | **ID 체계 고정** | 3자 | 재편될 때마다 연관점 전수 수정. **이번에 실제 사례가 나옴** |
| 6 | ACK에 요청 식별자 에코 | 이대규 | 두 키 매핑의 전제 |
| 7 | 좌표계 손잡이 규약 | 이대규 | Unity 왼손 — 좌우가 뒤집힘 |
| 8 | 원천 종류 표기(`VZ-C-06`) 주체·형식 | 3자 | 시뮬레이션과 실물이 감사에서 구분 안 됨 |
| 9 | 자세(orientation)가 계약에 없음 | 이대규 | 트윈에서 로봇의 향을 못 그림 |
| 10 | 레지스트리 변경 통지 채널 | 이대규 | 뷰어가 최초 1회만 받음 |
| 11 | 설계 규모 가정(`VZ-C-05`) | 전 파트 | 모든 주기 산정의 전제 |

### 2번 상세 — 나영에게 물을 4문항

`AI-C-09`(생성형 AI 보조 실행)가 **통로인 것은 맞습니다.** 필수 입력에 `task_type` · `input text refs` · `expected_output_schema`가 있어 `intent_parse`로 태우면 구조적으로 맞습니다. **다만 어댑터이지 기능이 아닙니다.**

1. 의도 해석을 `AI-C-09`에 `task_type`으로 태우는 게 맞나? 맞다면 **그렇게 적힌 행이 하나** 필요하다
2. **출력 스키마는 누가 정하나?** `VZ-L-02`가 그걸 그린다
3. GenAI provider가 없을 때 음성 명령은? `AI-C-09`가 말하는 **"비-GenAI 경로"가 음성에는 정의돼 있지 않다**
4. 그 `DISABLED` 상태가 가시화까지 전달되나? (4번 안건과 같은 뿌리)

> 참고 — `AI-C-09`의 **담당이 「하드웨어, 백엔드」** 로 적혀 있습니다. Lemonade Server 붙이는 인프라 작업으로 보고 있다는 뜻이고, 그래서 **의도 해석이라는 기능 자체는 여전히 주인이 없습니다.**

### 3번 상세 — 질문이 두 단계 구체화됐다

| 시점 | 상태 |
|---|---|
| 8/21 | 카메라 원본을 뷰어 형식으로 바꿀 **주체가 세 파트 통틀어 없음** |
| 8/24 오전 | `AI-C-08` GStreamer 어댑터 등장 — 단 경로가 **AI 모듈까지** |
| 8/24 오후 | `AI-C-14`로 **미디어가 별도 평면으로 명명됨** |

이제 *"누가 만드나"* 가 아니라 **"미디어 경로에 뷰어용 출력 분기를 다느냐, 그 어댑터는 누구 것이냐"** 입니다. GStreamer가 이미 시스템 안에 있으므로 WebRTC·HLS 내보내기는 도구상 자연스럽습니다.

## 1-3. 답을 받은 뒤에 할 것

| 대상 | 조건 |
|---|---|
| `VZ-L-01`~`04` 연관점 확정 | 의도 해석 소재가 정해지면 |
| **capability 축 신설 검토** (`DISABLED`/`DEGRADED`) | 상태 전달 경로가 있다고 확인되면 |
| `VZ-C-06` 실사용 승격 | 원천 종류 표기 주체가 정해지면 |
| `VZ-I-06` 변환 지점 확정 | 미디어 출력 분기가 정해지면 |

### capability 축이 왜 필요한가

지금 상태 표시는 **정상 / 장애 / 미배포 / 판단 불가** 넷인데 담을 자리가 없는 것이 둘입니다.

| 새 상태 | 뜻 |
|---|---|
| `DISABLED` | 이 배치에는 그 기능이 **아예 없다** |
| `DEGRADED` | **성능 저하 모드**로 돌고 있다 |

**장애와 미설치를 섞으면 관제사가 고칠 수 없는 것을 고치려 듭니다.** `VZ-C-02`는 "내가 연결 못 함"이고 이건 "상대가 선언한 능력 상태"라 축이 다릅니다.

AI 시트가 `AI-C-10`(capability 등록) + `AI-C-11`(필수/선택 격리) + `AI-C-05`(핵심·선택 경계) + `AI-E-04`(선택형 보조 인지)로 이 방향을 굳혔습니다. 내 `VZ-I-09`·`VZ-U-06`이 이미 선택 기능에 의존합니다.

---

# 2. 개발 — 지금 있는 것에서 고칠 것

계약이 바뀌어서 **내 산출물이 낡은** 것들입니다. 코드보다 문서가 많습니다.

## 2-1. 문서·다이어그램 (우선)

| 대상 | 무엇이 낡았나 | 규모 |
|---|---|---|
| **`T3` 연결모드 다이어그램** | Mermaid 원본은 **`device_id` 반영 완료**. 조판 PNG 재생성 필요 | 조판 1장 |
| **데이터 흐름 구조도** (조판 PNG) | 2평면 구판은 아카이브 완료. `AI-C-14` 3평면 원본으로 재렌더 필요 | 재렌더 |
| **`DF-1` mermaid** (계통·평면) | **3평면 원본 반영 완료** | 검수 후 조판 |
| **`가시화_요구사항_쉬운설명.md`** | 2평면·영상 변환 서술 정정 완료. **PDF 구판은 아카이브** — 재조판 대기 | 재조판 |
| **`가시화_프로토타입_설명서.md`/`.pdf`** | 동일 | 2곳 |
| **`구현계획서.md`** | **v0.4 전면 개정 완료** — 현행 22/33, 3평면, Unity, 남은 순서 반영 | 완료 |

> `T3`는 방향이 오히려 맞았습니다 — 다이어그램에 *"IP는 도달성일 뿐 정체성은 논리 식별자로 유지"* 라고 적어둔 것이 이제 **계약으로 뒷받침**됩니다. 제목과 메커니즘만 `device_id`로 바꾸면 됩니다.
>
> ⚠️ `BE-T-05` 연관점이 아직 *"HW-C-07(**MAC 매핑**)"* 이라고 부릅니다. 하드웨어가 이름을 바꿨는데 백엔드 참조가 옛 이름 — 회의에서 한 줄.

## 2-2. 코드

| 대상 | 무엇 | 근거 |
|---|---|---|
| ~~**영상 오버레이 화면**~~ | 탐지 결과의 **출처 구분**(온디바이스 최소 안전 / 엣지 정밀) | **완료** — `5a409f0` |
| ~~**영상 오버레이 화면**~~ | 다중 관측 연계가 **없을 때**의 표시 — 소스별 추적 유지 | **완료** — `5a409f0` |
| ~~**목 게이트웨이**~~ | 위 둘을 재현할 시나리오 | **완료 — 4건**(`vision-edge-off/on` · `vision-link-off/on`). 캐시·왕복 지연 3건은 별건으로 추가 |
| ~~`web-dashboard/prototype.html`~~ | React 앱으로 이관돼 더는 안 씀 | **완료 — `_archive/prototype_260818.html`** |

**코드 수정은 크지 않습니다.** 데이터 레이어는 그대로고 표시 계층만 갈래가 늘어납니다.

## 2-3. 이번엔 안 고치는 것

| 대상 | 왜 |
|---|---|
| `VZ-L-*` 연관점 | 의도 해석 소재가 정해지기 전에 바꾸면 며칠 뒤 또 바꿔야 함 |
| capability 상태 표시 | 전달 경로 확인 전 |
| Unity 시뮬레이터(2단계) | 뷰어로 왕복을 검증했으니 순서는 맞으나, **계약이 흔들리는 지금 붙이면 두 번 일** |

---

# 3. 앞으로의 계획

## 3-1. 남은 요구사항 6건

| 묶음 | ID | 규모 |
|---|---|---|
| **음성·LLM 4건** | `VZ-L-01`~`04` | 상대 확정 대기 |
| **대기 2건** | `VZ-U-06` 미확인 탐지 · `VZ-C-06` 원천 종류 | 다른 파트 합의 |

## 3-2. 순서와 근거

| 순서 | 무엇 | 왜 이 순서 |
|---|---|---|
| **① 문서 정합 복구 (원본 완료)** | T3 · 데이터흐름 · 평면 서술 · 구현계획서 | 활성 Markdown 정합 완료. 생성 PNG·PDF 일부는 조판 대기 |
| **② 회의 안건 1·2·3 상신** | `BE-T-06` 회신 + 나영 4문항 + 미디어 분기 | 답이 와야 음성·영상이 풀림. **1번은 오늘 보낼 수 있음** |
| **③ 잔여 표시 4건 구현 (완료)** | `VZ-I-08`·`I-10`·`O-04`·`U-03` | 판단·알림 탭과 자체 지표 경로 구현 |
| **④ 영상 오버레이 정정** | 출처 구분 · 연계 부재 대응 | 계약이 이미 확정된 부분이라 지금 해도 안 뒤집힘 |
| **⑤ 음성·LLM 4건** | 2번 답을 받은 뒤 | 받을 것이 정해져야 만들 수 있음 |
| **⑥ 노드 편집기 + LLM 그래프 생성 대기** | `VZ-U-04` = `REQ-1001`~`1007` · `VZ-L-04` 대기 | 요건 단위 상태는 아래 표. LLM은 계약 확정 뒤 같은 절차에 연결 |

⑥은 시트에서 한 줄이지만 요구사항으로는 일곱 건이다. "편집·검증·반영은 완료"라는 한 줄
서술이 2026-08-25 대조에서 실제로 세 건의 공백을 가렸으므로 (`REQ-1002` 무손실 왕복,
`REQ-1003` 카탈로그 자동 구성, `REQ-1007` 역방향 관측), 요건 단위로 쪼개 적는다.

| 요건 | 상태 | 비고 |
|---|---|---|
| `REQ-1001` 시각 편집 | 완료 | |
| `REQ-1002` 계약 무손실 왕복 | 완료 | 초안 = 계약 + 좌표 분리 · `verify:contract-roundtrip` |
| `REQ-1003` 카탈로그 자동 구성 | 완료 | 등록처는 `contracts/examples/` · `verify:catalog-derived` |
| `REQ-1004` 연결 유효성 강제 | 완료 | 포트 타입을 카탈로그에서 끌어온다 |
| `REQ-1005` 시험 실행 | 완료 (**목 실행기**) | 행 수·소요 시간은 지어낸 값. 실물 실행기는 백엔드 계약 대기 |
| `REQ-1006` 반영·되돌리기 | 완료 | 감사는 서버 메모리에만 남는다 |
| `REQ-1007` 역방향 관측 | 완료 (**부분**) | source는 실수신, 하류는 목 전달. 필드 단위 관측은 실물 실행기 이후 |
| `VZ-L-04` LLM 초안 생성 | 대기 | 계약 확정 뒤 같은 시험·반영 절차 앞에 연결 |
| **⑦ Unity 시뮬레이터 2단계** | 목 장치를 Unity 시뮬로 교체 | 계약이 굳은 뒤. 지금 하면 두 번 일 |

**③이 이번 주의 실질 작업입니다.** 나머지는 답을 기다리거나 문서를 맞추는 일이라, 손이 비지 않으려면 여기를 잡는 게 맞습니다.

## 3-3. 판단 기준 하나

이번 주에 반복해서 나온 상황이 있습니다 — **상대 요구사항의 ID는 살아 있는데 뜻이 바뀌는 것.**

`AI-D-03`은 이름이 눈에 띄어서 잡혔지만, `AI-E-02`(보조 인지 → 캘리브레이션)는 **70개 ID를 전수 대조하지 않았으면 못 잡았습니다.** 자동 검사로는 안 걸립니다 — ID가 존재하니까요.

그래서 **시트가 갱신될 때마다 연관점이 부르는 ID의 소분류를 전수 대조**하는 것을 습관으로 둡니다. 5분이면 되고, 안 하면 틀린 근거 위에 코드를 쌓게 됩니다.

---

# 4. 이번 주 체크리스트

- [x] `현황정리_260824.md` · 갱신된 시트 커밋
- [x] **`BE-T-06` 캐시 범위 회신** 문서 작성 — `백엔드_회신_260824.md` (전달은 아직)
- [ ] **나영에게 의도 해석 4문항** 전달
- [x] `T3` 다이어그램 `device_id` 반영 — Mermaid 원본 (조판 PNG는 재생성 대기)
- [x] 데이터 흐름 구조도 **3평면** 반영 — Mermaid 원본 (조판 PNG는 재생성 대기)
- [x] 쉬운설명 · 프로토타입 설명서 평면/인지 출처/Unity 서술 정정 (PDF 재생성 대기)
- [x] `구현계획서.md` v0.4 전면 개정
- [x] 명백한 구판 문서·조판본 `_archive` 이동 및 아카이브 색인 갱신
- [x] 영상 오버레이 — 인지 출처 구분 · 연계 부재 대응 (목 서버 시나리오 4건 포함)
- [x] 목 게이트웨이 캐시 범위 정합 (`BE-T-06` 회신대로) + 재접속 검증 스크립트
- [x] **잔여 표시 4건 구현** (`VZ-I-08`·`I-10`·`O-04`·`U-03`)
- [x] **임무 관제 중심 노드 그래프 구현** (`REQ-1001`·`1004`·`1005`·`1006`) — Sub task 진행과 근거 연결. 데이터 파이프라인 편집은 보조 모드
- [x] **파이프라인 초안 ↔ F4 계약 무손실 왕복** (`REQ-1002`) — 좌표는 계약 밖 `layout`. `verify:contract-roundtrip`
- [x] **노드 카탈로그를 등록처에서 파생** (`REQ-1003`) — 코드 상수 배열 제거. `verify:catalog-derived`
- [x] **반영된 그래프의 역방향 관측** (`REQ-1007`) — 노드별 최근 수신·건수·마지막 값. 시험 결과와 분리 표시
- [x] **임무 대상·근거 노드의 레지스트리 파생** — `robot-01` 등 대상 id 하드코딩 제거
- [ ] 회의: 미디어 출력 분기 · capability 전달 경로 · ID 체계 고정
