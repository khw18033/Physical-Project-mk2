# 녹음·업로드 오디오. 운영 데이터가 아니라 모델 간 재현 비교용 시험 자료라
# 로컬에는 남기지만 저장소에는 올리지 않는다 (근거는 README 참조).
samples/

# 실행 기록. 사람 목소리와 발화 원문이 들어 있고, 측정값은 각자 장비에서
# 다시 뽑아야 의미가 있으므로 공유하지 않는다.
results/

# 모델 캐시 (STT_LAB_MODEL_DIR 기본값)
models/

# 파이썬
.venv/
venv/
__pycache__/
*.pyc
