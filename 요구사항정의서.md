# 개발 요구사항 정의서 (공통/범용)

**문서 버전**: v1.0
**작성일**: 2026-08-28 (v0.9 → v1.0 개정)
**개정 근거**: *(v1.0)* 탭 번호 재배치 — 「임무 설계 및 디버깅」이 **탭①**이 된다. STT 이식으로 드러난 `utterance.confidence` 계약 충돌을 7.8에 올림 / *(v0.9)* 두 도구를 **한 앱으로 통합**하기로 결정. `web-dashboard`의 7개 탭 중 5개를 새 앱으로 이식하고 **「임무 설계 및 디버깅」을 탭 하나로** 넣어 **탭 6개 + 공유 계층** 구조로 간다. 요구사항 46건이 하나도 죽지 않는다 / *(v0.8)* 요구사항 46건에 **적용 대상**을 표기해 신규 구현 범위를 분리함. 이 문서는 특정 탭 전용이 아니라 **가시화 파트 전체**를 덮는다 / *(v0.7)* 삭제된 AI 의사결정 요구사항의 인수 범위 확정 — 발화 해석·마일스톤 분리·태스크 생성·검증·액션 아이템 전개·평가 판정을 **가시화가 담당**하는 것으로 팀 합의(F15 신설). 파트 경계가 '받아서 그리는 레이어'에서 '임무를 만들고 그 실행을 되짚는 레이어'로 확대됨 / *(v0.6)* 요구사항 정의서 엑셀 신판에서 AI 파트의 의사결정(`AI-D-01`·`02`·`04`)과 학습(`AI-L-01`~`03`) 요구사항이 삭제되고 `AI-C-15`~`17`·`AI-O-04`·`AI-S-06`·`AI-N-03`·`AI-B-11`이 신설됨. 하드웨어 주기 다수 개정(`HW-A-01`·`A-04` 50 ms, `HW-C-04` 10초, `HW-C-05` 60초, `HW-S-05`·`S-07` 1 Hz 기준). 8/26 회의로 디버깅 가시화 축(F14) 신설 / *(v0.5)* `AI-C-14` 데이터 유형별 경로 분리, `HW-R-04` 온디바이스 최소 안전 판단/엣지 정밀 인지 분리, `HW-C-07` `device_id` 영구 식별, `BE-T-03` 가시화 클라이언트 일반화 / *(v0.4)* 트윈 화면을 별도 네이티브 앱(Unity)으로 구현하면서 드러난 뷰어 일반화와 원천 종류 축 / *(v0.3)* 당시 통합정립본 v3의 파트 간 계약 확정 / `260818_회의록.md`(v0.2)
**범위**: 본 문서는 **공통(범용) 요구사항**만을 다룬다. 분야별(캡스톤/하천/국방) 상세 요구사항은 별도 문서로 작성하며, 특정 분야에만 해당하는 내용은 요구사항이 아닌 **확장점(E1~E8)**으로 분리해 표시한다.

## v0.9 → v1.0 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **「임무 설계 및 디버깅」을 탭⑥ → 탭①로** | **이 앱의 본류이기 때문이다.** 유일하게 새로 만드는 탭이고, 논문의 대상이며, HCI 논의 대상도 이것 하나다. 본류가 맨 끝에 있으면 앱을 처음 여는 사람이 무엇을 보는 도구인지 알 수 없다 |
| 2 | 나머지 다섯이 한 칸씩 밀림 | 구역 현황판 ①→②, 제어 패널 ②→③, 지표 조회 ③→④, 영상 오버레이 ④→⑤, 파이프라인 편집기 ⑤→⑥ |
| 3 | 엑셀 `통합 앱 배치` 열 46건 전부 갱신 | 문서 번호와 코드 번호가 어긋나면 이식할 때 어느 자리에 넣을지가 흔들린다 |
| 4 | **`utterance.confidence` 계약 충돌을 미결 7.8로 신설** | STT를 실제로 붙여 보니 0단계에서 합의한 `0~1 숫자 하나`가 엔진 출력과 맞지 않는다. **다른 미결보다 먼저 정해야 한다** — `VZ-G-01`이 임무 객체를 만드는 순간 걸린다 |

**비용을 알고 택했다.** 엑셀 46건 · §3.1의 표 넷 · DF-7 데이터 흐름도 · 쉬운설명 md/pdf ·
구현·기술스택·논문 문서 · 통합 지시서를 함께 고쳐야 했다. 그래도 **번호가 곧 "무엇이 중심인가"를
말하기 때문에** 코드를 문서에 맞추는 쪽이 아니라 문서를 코드에 맞추는 쪽으로 갔다.

**손대지 않은 것** — `작업프롬프트_0단계_준비_260827.md`·`_1단계_초안_260827.md`.
이미 끝난 작업의 지시서라 당시 번호(탭⑥) 그대로 둔다. 읽을 때 §3.1을 기준으로 본다.

## v0.8 → v0.9 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **두 도구를 한 앱으로 통합** (3.1 전면 개정) | 폐기도 병행도 아니다. 기존 탭을 새 앱으로 이식하고 **「임무 설계 및 디버깅」을 탭 하나로** 넣는다. **다른 파트와의 수신·송신 계약이 하나도 죽지 않고**, 교수님 피드백 자리에서 구현물을 전부 보일 수 있다 |
| 2 | **그릇은 `viz-debugger/`** | 반대 방향(web-dashboard에 넣기)이면 기존 코드를 고치게 되어 "원본 무수정 · 기존 검증 5종 통과"라는 안전장치가 무너진다 |
| 3 | **탭 구성이 7개 → 6개** | 구 임무 승인·진행은 탭①으로 통폐합, 구 판단·알림은 해체해 세 곳으로, 구 노드 그래프는 모드별로 갈라 탭①과 탭⑥으로 |
| 4 | **공유 계층을 명시** (16건) | 수신·명령·권한·표기는 탭 전용이 아니라 앱 전체가 쓴다. 탭에 귀속시키면 같은 것을 여러 탭이 따로 구현하게 된다 |
| 5 | **탭⑥ 파이프라인 편집기를 '동결·시연용'으로 격하** | `VZ-U-04`(F10)는 8/18에 교수님이 예시로 보여준 것이라 지우지 않는다. 다만 신규 개발도 논문 반영도 하지 않는다 |
| 6 | **탭① 단독 빌드 타깃 유지** | 논문 측정축 D(계측 오버헤드)를 잴 때 다른 탭이 같이 뜨면 메모리·렌더 부하가 섞인다 |
| 7 | **통합 시점은 3단계 이후** | 1단계(9/7 HCI 전달본)는 탭① 단독으로 간다. 통합을 앞당기면 팀원 전달이 밀린다 |

## v0.7 → v0.8 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **요구사항마다 적용 대상을 표기** (3.1 신설) | 46건 중 **26건만 신규 구현 대상**이다. 나머지는 유지 중인 기존 프로토타입 몫이거나 공통이다. 0단계 착수 직전인데 "무엇까지 만들면 끝인가"가 문서에서 읽히지 않았다 |
| 2 | **`VZ-U-05`를 신규 구현 대상에서 제외** | 구 2계층(계획→구간) 판이다. 「임무 설계 및 디버깅」 탭에서는 `VZ-D-01`(3계층)과 `VZ-D-06`(8상태)이 이 역할을 대신하므로 같은 것을 두 번 만들 이유가 없다 |
| 3 | **엑셀 김현우 시트에 열 두 개 추가** | `적용 대상`·`범위 주석`. 다른 파트 시트는 건드리지 않았다 |

## v0.6 → v0.7 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **임무 생성 축 신설** (F15, `VZ-G-01`~`05`) | 삭제된 `AI-D-01`·`02`를 **가시화가 인수**한다. AI 팀원이 논문 작업을 병행하게 되어 텍스트→서브태스크 생성을 현우가 맡기로 팀에서 정했고, 검증·마일스톤 분리·평가 판정까지 함께 가져온다 |
| 2 | **v0.6의 최대 공백이 해소됨** | `VZ-D-01`·`VZ-U-05`·`VZ-U-07`·`VZ-L-02`·`VZ-L-03`의 `[확인 요망]`이 풀렸다. 이제 3계층을 만드는 주체가 있다 |
| 3 | **명령 의도 해석의 주인도 함께 정해짐** | 8/25부터 비어 있던 구멍이다. **발화 텍스트를 마일스톤으로 분리하는 `VZ-G-01`이 곧 의도 해석**이라 별도 요구사항 없이 메워졌다. 해석기가 두 파트에 나뉘지 않으므로 같은 발화가 서로 다른 결과를 내는 문제도 사라진다 |
| 4 | **파트 경계 확대** (3장) | "가시화는 데이터를 직접 만들지 않는다"는 전제가 깨졌다. **임무 계층은 만들고, 관측값은 여전히 만들지 않는다**로 갈라 적었다 |
| 5 | **`VZ-L-04`의 범위 제외 문장 삭제** | "로봇 제어 명령의 의도 해석은 범위에 포함하지 않는다"가 F15와 정면으로 모순된다. 같은 로컬 LLM 계층을 쓰되 **별개 기능으로 분리 관리**로 고쳤다 |
| 6 | **`VZ-C-02`를 운용 요구사항으로 격상** | 임무 생성이 로컬 LLM에 의존하게 되었다. LLM이 없으면 생성만 멈추고 실행·관측·디버깅은 계속되어야 한다 |
| 7 | **재생성 판단(구 `AI-D-04`)을 확장점 E9로 분류** | 사전조건이 깨졌을 때 다시 생성하는 판단은 **필수 동작이 아니라 확장 기능**이다. 미결이 아니라 확장점으로 옮겨, 본체를 다 만든 뒤 확장을 검토할 때 다시 본다(5장) |

## v0.5 → v0.6 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **디버깅 실행 추적 축 신설** (F14) | 8/26 회의에서 논문 주제가 "디버깅을 위한 가시화"로 확정. 마일스톤→태스크→액션 아이템 3계층과 실행 기록 되감기·실패 경로 격리가 기존 F1~F13 어디에도 없다 |
| 2 | **계획 생성·검증의 담당 파트가 사라짐** | 엑셀 신판에서 `AI-D-01`(서브태스크 생성)·`AI-D-02`(검증)·`AI-D-04`(재생성 판단)이 삭제되었다. 어느 파트 시트에도 대체 항목이 없어 **전 파트 미할당 상태**다. `VZ-U-05`·`VZ-U-07`·`VZ-L-02`·`VZ-L-03`·`VZ-D-01`의 상대가 비었다 |
| 3 | **학습 계열 삭제로 `VZ-U-06`의 전제가 바뀜** | `AI-L-01`~`03` 삭제. 미확인 객체 후보 관리(`AI-S-04`)는 남았으므로 **그룹 단위 확인 → 후보 개별 확인**으로, **학습 후보 반영 → 분류 상태 갱신 근거**로 축소 |
| 4 | **액추에이터 진행 보고 주기 상향 반영** | `HW-A-01`·`HW-A-04`가 200 ms(5 Hz)에서 50 ms(20 Hz)로 개정. 화면 반영은 F2와 같은 100 ms 병합 창을 유지 |
| 5 | **오프라인 판정에 구조적 지연이 있음을 명문화** | `HW-S-07`이 "즉시"에서 "1 Hz 하트비트 4회 미수신(약 4초) 후"로 개정. 화면은 판정 도달 즉시 반영하되 **지연의 존재를 감추지 않는다** |
| 6 | **장치 현황 요약 10초, 관측 export 60초로 정정** | `HW-C-04` 5초→10초, `HW-C-05` 15초→60초. 후자는 `BE-S-03`의 요약 pull 15초와 어긋나므로 미결로 올림(7.6) |
| 7 | **폐쇄망·오버레이 전제 추가 인지** | `AI-C-16`(폐쇄망 운용)·`AI-C-17`(보안 오버레이) 신설. 가시화의 외부 소스 부재 시 동작(`VZ-C-02`)이 예외가 아니라 **기본 전제**가 되었다 |

## v0.1 → v0.2 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **동적 가시화 축 신설** (F10 노드 조합 편집기) | v0.1은 개발자가 사전 선언한 대로만 흐르는 정적 구조였다. 회의 진단: "지금까지의 가시화는 정적이다 — 개발 분량은 많은데 임팩트가 낮다." |
| 2 | **로컬 LLM 계층 신설** (F12) | 차별화 포인트. "가시화 도구를 만들었습니다"는 임팩트가 없고, "온디바이스 AI를 활용하는 로컬 LLM 기반 가시화 도구"라야 성립한다. |
| 3 | **음성 인터페이스 신설** (F13) | 클릭 이벤트를 대체하는 트리거. 시연 효과 목적. |
| 4 | **추상화 계층 뷰 신설** (F11) | 군 지휘 비유 — 결심자는 빨강/초록, 더블클릭하면 구체 데이터. v0.1의 F3(공간 계층)과는 **직교하는 별개 축**인데 REQ-303 하나로 뭉뚱그려져 있었다. |
| 5 | **F2 범위 축소** (감지 → 수신·표시) | 오케스트레이터 이벤트 감지는 나영 파트(K8s/도커)로 이관. 3장 참조. |
| 6 | **타겟 사용자 재정의** (관리자용 → 개발자용 1차) | "개발자용을 상위로 추상화하면 관리자용이 되는 구조"로 정정. |
| 7 | **F9 우선순위 상향** | 회의 §4 수문 예시 — 제어 명령 미기록은 공공 서비스에서 책임 소재 추적 불가로 직결된다. |
| 8 | **F7에 Grafana 구현체 추가** | 조사 결과 그라파나는 대체 대상이 아니라 소비 대상으로 판명. `docs/research/2026-08-18_grafana-dynamic-datasource.md` 참조. |
| 9 | **공간 계층 정의 명문화, Zone을 선택으로 강등** *(v0.2.1)* | REQ-301이 "3단 계층을 제공한다"고만 하고 각 층의 의미를 정의하지 않아 다른 파트와 대조가 불가능했다. 또한 캡스톤에서 Zone은 단일 상수이므로 필수로 둘 근거가 없다. |

## v0.4 → v0.5 개정 요약

| # | 변경 | 사유 |
|---|---|---|
| 1 | **업무·관측·미디어 3평면 명문화** | `AI-C-14`가 영상 픽셀을 메시지 브로커·관측 경로에 싣지 않고 별도 미디어 경로로 보내도록 확정 |
| 2 | **인지 결과의 출처와 급 분리** | `HW-R-04`에 따라 온디바이스는 진행영역·접근 변화 등 최소 안전 판단, 엣지는 선택형 정밀 분류·추적을 담당 |
| 3 | **장치 영구 식별자를 `device_id`로 정정** | `HW-C-07`에서 MAC은 네트워크 인터페이스 정보일 뿐 영구 식별자로 단독 사용하지 않도록 변경 |
| 4 | **브라우저 결속 해소를 완료 처리** | `BE-T-03`이 특정 기술에 고정하지 않는 가시화 클라이언트 게이트웨이로 일반화됨 |

## v0.3 → v0.4 개정 요약

> 트윈 화면을 **브라우저가 아닌 별도 앱**으로 만들기로 하면서 두 가지가 드러났다.
> 둘 다 Unity라는 단어가 들어가지 않는 **범용성 축**이며, 특정 구현을 요구사항에 적는 것은 여전히 하지 않는다.

| # | 변경 | 사유 |
|---|---|---|
| 1 | **「브라우저」 결속을 「가시화 뷰어」로 일반화** (REQ-702, 4장 경계 서술) | 백엔드 경유의 근거가 *"브라우저는 브로커에 직접 붙을 수 없어서"* 라는 **기술 제약**으로 쓰여 있었다. 네이티브 뷰어는 붙을 수 있으므로 이 근거가 걸리지 않고, 그러면 네 개의 데이터 입구라는 구조 전체가 왜 필요한지가 무너진다. 근거를 **"권한·감사·집약 경계를 백엔드가 단독으로 강제해야 하므로"** 로 바꾸면 뷰어 종류와 무관하게 성립한다. |
| 2 | **원천 종류(실물/시뮬레이션/재생) 표기 신설** (REQ-206) | 시뮬레이터가 실제 원천을 대체·병행하면 가짜 값이 실물과 **동일한 봉투**로 올라온다. 화면 구분도 문제지만 본질은 **감사**다 — 제어 기록에 "그때 시뮬레이션 중이었다"가 남지 않으면 훈련과 실제 상황이 기록상 같아져 사후 조사가 성립하지 않는다. |

> **v0.3 이전 개정 요약의 「브라우저」 표현은 당시 서술 그대로 둔다.** 개정 이력을 소급 수정하면
> 무엇이 언제 바뀌었는지 대조가 불가능해진다. 현행 규칙은 위 표와 아래 본문이 우선한다.

## v0.2 → v0.3 개정 요약

> 통합정립본 v3에서 파트 간 계약이 확정되면서 **책임 주체가 이동한 항목**을 반영한다.
> 요약하면 세 가지다 — **감사는 백엔드가 쓴다 / 상태는 3층으로 온다 / 레지스트리 조회가 생겼다.** 나머지는 이 셋의 파생이다.

| # | 변경 | 사유 |
|---|---|---|
| 1 | **감사 기록의 작성 주체를 백엔드로 이관** (REQ-907, REQ-1305, NFR-002) | 브라우저가 직접 쓰면 `actor`가 클라이언트 자기신고가 되어 위조 가능하고, 시각이 사용자 PC 시계가 되며, 탭 종료 시 유실된다. 본 레이어는 **감사 필드만 전달**하고 Log Record 작성은 백엔드 audit-writer가 한다. |
| 2 | **상태 표시를 3층 조합으로 확장** (REQ-203, REQ-205 신설) | 미배포/장애 2가지 구분으로는 "연결됐는데 기기 fault"와 "값이 오래됨(stale)"을 표현할 수 없다. `device.status`/`availability`/`deployment` 3층을 조합해 4가지로 표시한다. |
| 3 | **구성(레지스트리) 조회 신설** (REQ-304) | F3에 계층을 **어디서 받는지**가 없었다. 또한 미배포 대상은 값을 발행하지 않으므로, 존재 목록 없이는 REQ-203의 미배포 표시가 구현 불가다. |
| 4 | **수신 경로를 백엔드 경유로 통일** (REQ-201, REQ-702, REQ-704) | 브라우저는 브로커·Prometheus·DB에 직접 접근하지 않는다. 실시간은 WS 게이트웨이(구독 축 `{entity,node,channel}`), 지표는 질의 프록시, 감사는 조회 API. |
| 5 | **명령 확정 판정을 백엔드 승격값으로** (REQ-903, REQ-909 신설) | 백엔드가 디바이스 ack를 공통 `command.result`로 승격(→`completed`)해 내려준다. 액션별 판정 규칙 목록이 불필요해졌다. `command_id`·`expires_at`을 명령 필수 필드로 승격. |
| 6 | **좌표 변환 책임을 백엔드로 명시** (REQ-302, E2) | "축 변환의 주인은 백엔드"가 공통 계약으로 확정. 본 레이어는 Node 원점의 전역 배치를 받아 배치만 한다. 엔진 비종속 표기로 "Unity" 표현을 정정. |
| 7 | **음성·LLM 계층 보강** (REQ-908, REQ-1208·1306 신설, REQ-1301·1303·1304 보강) | 발행 경로를 **입력 수단 × 판단 주체 두 축**으로 분리(단일 4값으로는 "음성으로 말했고 LLM이 해석했다"가 표현 불가). 제어 액션 카탈로그 소비와 신뢰도 임계를 명문화. |

---

## 1. 프로젝트 개요

**서비스 이름**
디지털 트윈 기반 하이브리드 아키텍처의 **로컬 LLM 연동 동적 가시화 레이어**

**서비스 목적**

두 가지를 동시에 만족시킨다.

1. **자동 정합** — 마이크로서비스로 분할 배포된 시스템에서, 가시화 계층이 실제 배포 상태와 자동으로 일치한다. 컨테이너를 붙이거나 떼는 것만으로 추가 개발 없이 대시보드가 생성·소멸된다.
2. **런타임 구성** — 데이터를 어떤 형태로 해석해 보여줄지를 개발자가 **사전에 코드로 고정하지 않고**, 사용 시점에 노드 그래프로 조합·중첩할 수 있다. 그 조합을 사람이 손으로 만들 수도, **음성 지시를 로컬 LLM이 분해해 자동 생성**할 수도 있다.

**해결하려는 문제**

| # | 문제 | 대응 |
|---|---|---|
| 1 | 서비스를 N등분해 일부만 배포해도 가시화는 통짜로 남아 데이터 없는 구간을 그대로 그린다. 배포 상태와 화면 상태가 어긋난다. | F2 |
| 2 | 새 센서·모듈을 추가할 때마다 대시보드를 수작업으로 구성해야 한다. | F1, F2 |
| 3 | 영상 스트림 등 비표준 표현이 텍스트 패널에 HTML을 끼워 넣는 임시방편으로 구현되어 재사용·확장이 불가능하다. | F5 |
| 4 | 특정 관측 스택(OTel / Prometheus / Grafana)에 구조가 종속되어 교체가 어렵다. | F7 |
| 5 | **가시화 구성이 정적이다.** 개발자가 사전에 선언한 경로대로만 데이터가 흐르므로, 사용자가 "이 두 소스를 겹쳐 보고 싶다"고 판단해도 개발 요청 외에는 방법이 없다. | **F10** |
| 6 | **원본 데이터와 판단 사이의 거리가 멀다.** 결심자에게 필요한 것은 "이길 수 있나"인데 화면은 전차 대수와 그래프를 그대로 보여준다. 반대로 근거를 확인하려 할 때 어떤 데이터에서 그 판단이 나왔는지 추적할 수 없다. | **F11, F12** |
| 7 | **조작이 전부 수동 클릭이다.** 관제 상황에서 손이 묶이고, 시연에서도 조작 과정이 그대로 노출된다. | **F13** |

**주요 타겟 사용자**

v0.1에서 "관리자용"으로 잡았던 것을 **개발자용 1차**로 정정한다. 개발자용 도구를
상위로 추상화한 결과가 관리자용이며, 그 추상화 자체가 F11의 요구사항이다.

| 계층 | 사용자 | 보는 것 | 관련 요구사항 |
|---|---|---|---|
| L0 (1차 대상) | **개발자·통합 엔지니어** | 노드 그래프, 원본 필드, 파이프라인 실행 결과 | F10, F4, F8 |
| L1 | 운영자·관제 요원 | 구성된 대시보드, 상태 배지, 제어 액션 | F2, F5, F9 |
| L2 | 결심자·관리자 | 집약된 판단 지표(예: 빨강/초록) | F11 |

> L2에서 더블클릭하면 L1으로, L1에서 더블클릭하면 L0으로 내려간다. 이 하향
> 경로가 곧 **설명가능성(explainability)** 이다 — 판단의 근거를 원본까지 되짚을 수 있다.

**설계 원칙**

- **도메인 중립**: 공통 계층에 로봇 / 하천 / 국방 등 도메인 용어가 등장하지 않는다.
- **확장점 기반**: 새 분야 적용 = 정의된 확장점(E1~E8)을 채우는 작업으로 환원된다.
- **인터페이스 + 스켈레톤 제공**: 지정된 함수를 구현하면 아키텍처가 동작한다.
- **관측 스택 비종속**: OTel / Grafana는 선택된 구현체이며 교체 가능해야 한다.
- **선언과 편집기의 분리**: 설정 명세(JSON Schema)와 편집기 구현을 분리해, 편집기 구현(데스크톱 / 웹)에 요구사항이 종속되지 않는다.
- **LLM 비종속**: 특정 모델·런타임에 종속되지 않는다. LLM은 F12 인터페이스 뒤에 은닉되며, 미연결 상태에서도 F10 이하 전 기능이 동작해야 한다. **LLM은 증강이지 전제가 아니다.**

**확장점 목록**

| ID | 확장점 | 비고 |
|---|---|---|
| E1 | Entity 타입 정의 | |
| E2 | 좌표·배경 어댑터 | |
| E3 | 전용 렌더러 | |
| E4 | 이벤트 규칙 세트 | |
| E5 | 파이프라인 프리셋 | |
| E6 | 제어 액션 세트 | |
| **E7** | **LLM 도구(tool) 세트** | v0.2 신설 — 분야별로 LLM이 호출 가능한 조작의 집합 |
| **E8** | **음성 명령 어휘 세트** | v0.2 신설 — 분야별 발화 표현 ↔ 액션 매핑 |

---

## 2. 기능 요구사항 (Functional Requirements)

### 2.1 F1. 컴포넌트 디스크립터

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-101 | 기능 | 디스크립터 스키마 정의 | 개발자는 각 컨테이너가 내보내는 데이터와 표현 방식을 선언적 스키마로 기술할 수 있어야 한다. 시스템은 이 스키마를 파싱해 이후 프로비저닝·렌더링 단계의 입력으로 사용한다. | High |
| REQ-102 | 기능 | 필수/선택 필드 구분 | 개발자는 디스크립터 내 각 필드를 필수/선택으로 지정할 수 있어야 한다. 시스템은 필수 필드 누락 시 등록을 거부하고 오류를 표시한다. | High |
| REQ-103 | 기능 | 시맨틱 메타데이터 기술 | 개발자는 필드별 단위, 값 범위, 샘플링 주기 등 시맨틱 메타데이터를 디스크립터에 포함할 수 있어야 한다. 시스템은 이를 렌더러·검증기에 전달한다. | Medium |
| REQ-104 | 기능 | 디스크립터 버전 정책 | 개발자는 디스크립터에 버전을 명시할 수 있어야 하며, 시스템은 버전 간 호환성 여부를 판별해 비호환 시 경고를 표시한다. | Medium |
| **REQ-105** | 기능 | **LLM 판독용 자연어 서술** | 개발자는 각 컴포넌트·필드에 자연어 설명을 포함할 수 있어야 하며, 시스템은 이를 F12의 LLM 컨텍스트로 제공해야 한다. LLM이 "온도가 높은 구역을 보여줘"를 특정 필드로 해석하려면 필드 의미가 기계 판독 가능해야 한다. | High |

> REQ-105는 v0.2 신설이다. REQ-103(단위·범위)만으로는 LLM이 필드를 선택할 수 없다.
> 프로토타입 검증에서 REQ-103 메타데이터만으로 **그럴듯한 값 합성**은 가능함이
> 확인됐으나, **의미 기반 선택**은 별개 문제다.

### 2.2 F2. 라이프사이클 상태 수신 및 자동 프로비저닝

> **v0.2 범위 변경**: 오케스트레이터 이벤트를 **직접 감지하는 책임은 본 레이어에서
> 제외**한다(나영 파트로 이관, 3장 참조). 본 레이어는 이벤트를 **수신**해
> 가시화 컴포넌트를 조정하는 것까지를 담당한다.

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-201 | 기능 | 라이프사이클 상태 수신 | 시스템은 오케스트레이터에서 파생된 `deployment` 상태를 **백엔드 WS 게이트웨이를 통해 상태 3층의 일부로** 수신해야 한다. 이벤트 소스(Docker / K3s)의 구현 차이는 백엔드 뒤에 은닉되며, **본 레이어는 오케스트레이터에 직접 접속하지 않는다.** | High |
| REQ-202 | 기능 | 가시화 컴포넌트 자동 생성·갱신·삭제 | 시스템은 수신한 등록/해제 이벤트에 따라 대응하는 가시화 컴포넌트를 자동으로 생성·갱신·삭제해야 한다. 운영자는 추가 개발 없이 대시보드 변화를 확인할 수 있다. | High |
| REQ-203 | 기능 | 상태 4종의 구분 표시 | 시스템은 **정상 / 장애 / 의도적 미배포 / 판단 불가(stale)** 를 구분해 화면에 다르게 표시해야 한다. 운영자는 네 상태를 시각적으로 즉시 구별할 수 있다. | High |
| **REQ-204** | 기능 | **이벤트 소스 부재 시 동작** | 시스템은 오케스트레이터 이벤트 소스가 연결되지 않은 상태에서도 정적 구성 파일로 컴포넌트 목록을 구성해 동작해야 한다. 다른 파트의 진척에 우리 파트가 블로킹되지 않기 위한 요건이다. | High |
| **REQ-206** | 기능 | **원천 종류 표기 수신·표시** *(v0.4 신설)* | 시스템은 값·이벤트·명령 결과에 그 원천이 **실물 장치 / 시뮬레이션 / 기록 재생** 중 무엇인지를 나타내는 표기를 함께 수신해 화면에서 구분해 표시해야 하며, 제어 명령 요청에도 당시 원천 종류를 실어 보내야 한다(REQ-908의 감사 필드와 함께 전달). 표기의 발행 주체와 필드 형식은 **전 파트 합의 사항**이며 본 레이어는 소비자다. | High |
| **REQ-205** | 기능 | **상태 3층 원본 보존** *(v0.3 신설)* | 시스템은 상태를 `device.status`(기기 자기보고) / `availability`(서버 판정) / `deployment`(오케스트레이터) **3층 필드와 `last_seen`으로 수신·보관**해야 하며, 단일 상태 값으로 뭉쳐 저장해서는 안 된다. 화면 표시값(REQ-203의 4종)은 이 3층에서 **파생**시킨다. | High |

> **REQ-203/205 근거**: 상태를 하나로 합치면 "연결은 됐는데 기기가 fault"와 "값이 오래됨"을
> 표현할 수 없고, 원천(기기/서버/오케스트레이터)이 섞여 책임 소재가 흐려진다. 세 층은 각각
> 다른 주체가 채우고 화면이 조합한다.
>
> **`stale` 판정은 백엔드가 서버 시각과 `last_seen`으로 수행한다.** 본 레이어는 클라이언트
> 시계로 판정하지 않는다(사용자 PC 시계에 의존하게 되므로). 임계값은 Tier 2.

### 2.3 F3. 공간 계층 모델

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-301 | 기능 | Entity/Node/Zone 계층 구조 | 시스템은 Entity / Node / Zone의 공간 계층과 각 계층의 고유 식별자 체계를 제공해야 한다. **Entity와 Node는 필수, Zone은 선택**이며, Zone이 지정되지 않으면 분야 식별자(domain)를 최상위 구역으로 간주한다. 개발자는 이 계층에 맞춰 데이터를 등록한다. | High |
| REQ-302 | 기능 | 로컬 단위 간 정합 결과 수신 | 시스템은 백엔드 레지스트리가 제공하는 **Node 원점의 전역 배치(원점·회전)** 를 받아 좌표를 배치해야 한다. **로컬→글로벌 변환과 축 변환(이미지 x,y → 트윈 x-z)은 백엔드가 수행하며, 본 레이어는 변환 로직을 구현하지 않는다.** | High |
| REQ-303 | 기능 | 공간 드릴다운 | 사용자는 최상위 뷰에서 특정 Node/Entity로 드릴다운해 상세 정보를 확인할 수 있어야 한다. 시스템은 계층 간 이동 시 컨텍스트(선택 상태, 필터)를 유지한다. | Medium |
| **REQ-304** | 기능 | **구성(레지스트리) 조회** *(v0.3 신설)* | 시스템은 백엔드 레지스트리 조회로 **존재해야 할 Entity 목록, Entity↔Node 소속, Node 원점의 전역 배치, Zone 트리**를 수신해야 한다. **값을 발행하지 않는 대상(미배포)도 이 목록을 통해 화면에 표시한다.** 핸드오버로 Entity의 소속 Node가 바뀌는 경우의 변경 통지 경로를 포함한다. | High |
| **REQ-305** | 기능 | **Entity 호칭 사전 수신** *(v0.3 신설)* | 시스템은 레지스트리로부터 Entity·Node·Zone의 `display_name`과 `aliases[]`를 함께 수신해야 한다. F13 음성 명령이 "503 구역 로봇" 같은 자연어 호칭을 식별자로 해석하는 데 사용한다(확장점 E8). | Medium |

**계층 정의** *(v0.2.1 신설)*

| 계층 | 정의 | 필수 | 캡스톤 | 하천 |
|---|---|---|---|---|
| **Entity** | 개별 관측·제어 대상. 상태를 갖고 값을 낸다 | 필수 | 로봇 1대, 탐지 객체 1개 | 게이트 1개, 수위 센서 1개 |
| **Node** | Entity가 묶이는 설치 단위이자 **로컬 좌표계의 원점** | 필수 | 엣지 노드와 붙은 센서들, 카메라 1대의 시야 | 관측소 1곳 |
| **Zone** | Node가 묶이는 운영 구역 | 선택 | 미사용 — domain으로 대체 | 하천 구간 |

> **Node는 대체할 수 없다.** 로컬 좌표계의 원점이므로, Node 식별자가 없으면 서로 다른
> 대상의 좌표를 한 화면에 겹칠 수 없다(REQ-302). 반면 Zone은 캡스톤에서 단일 상수이므로
> 필수로 두면 모든 페이로드에 고정값이 실리고 "빈 값 규칙"을 파트마다 정해야 한다.
> **그럼에도 계약에서 삭제하지 않는 이유는 비용이 비대칭이기 때문이다** — 지금 선택 필드로
> 남기는 비용은 필드 하나지만, 나중에 계층을 끼워 넣는 비용은 하드웨어 토픽·AI 출력·관측
> 라벨·화면까지 전 파트에 전파된다. 하천에서 방어벽 지점이 둘 이상 되면 즉시 필요해진다.

> **주의**: F3의 드릴다운은 **공간 축**(넓은 구역 → 좁은 구역)이다. F11의 드릴다운은
> **추상화 축**(판단 → 근거)이다. 두 축은 직교하며, 같은 Entity를 보면서도 추상화
> 수준을 바꿀 수 있어야 한다. 회의 §2의 군 지휘 비유는 **F11의 축**이며 F3와 다르다.

### 2.4 F4. 해석 파이프라인 엔진

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-401 | 기능 | 직접 해석 파이프라인 | 시스템은 원본 데이터를 별도 전처리 없이 표현 형태로 바로 변환하는 직접 해석 경로를 지원해야 한다. | High |
| REQ-402 | 기능 | 간접 해석 파이프라인(전처리 체인) | 개발자는 여러 전처리 단계를 경유하는 간접 해석 경로를 노드 그래프 형태로 정의할 수 있어야 한다. 시스템은 정의된 순서대로 각 단계를 실행한다. | High |
| REQ-403 | 기능 | source/transform/sink 계약 정의 | 시스템은 파이프라인의 각 노드(source / transform / sink)가 준수해야 할 입출력 계약과 직렬화 포맷을 정의하고 강제해야 한다. | High |
| REQ-404 | 기능 | 실행 위치 분기 | 개발자는 파이프라인 각 단계의 실행 위치(서버/클라이언트)를 지정할 수 있어야 한다. 시스템은 지정된 위치에서 해당 단계를 실행한다. | Medium |
| **REQ-405** | 기능 | **분기·합류 그래프 실행** | 시스템은 하나의 source가 여러 transform으로 분기하고, 여러 경로가 하나의 sink로 합류하는 DAG를 실행할 수 있어야 한다. 선형 체인만으로는 "여러 데이터 소스를 조합·중첩"(F10)이 성립하지 않는다. | High |
| **REQ-406** | 기능 | **부분 실행 및 중간 결과 확인** | 개발자는 파이프라인 전체를 완성하지 않은 상태에서 임의 노드까지만 실행해 중간 결과를 확인할 수 있어야 한다. 시스템은 노드 단위로 출력을 미리보기 형태로 제공한다. | High |
| **REQ-407** | 기능 | **미디어 데이터 타입 지원** | 시스템은 시계열·테이블 외에 프레임 단위 영상 데이터를 파이프라인의 1급 데이터 타입으로 취급해야 한다. (카메라 → 필터 → 디텍션 체인의 전제) | Medium |

> **전송 평면은 셋이다.** 상태·명령·계획은 업무 평면, metric·log·trace는 관측 평면,
> 영상 픽셀은 별도 미디어 평면을 사용한다. 픽셀을 MQTT·Kafka·OpenTelemetry에 직접 싣지
> 않는다. `AI-C-08` 미디어 어댑터의 AI 입력 경로와 별개로, 뷰어용 출력 분기의 형식과
> 소유 주체는 아직 합의가 필요하다.

> REQ-406은 회의록 §2 언리얼 블루프린트 비유의 "미리 돌려보고 테스트" 단계에
> 해당한다. 이것이 없으면 노드 에디터는 만들어도 쓸 수 없다.

### 2.5 F5. 렌더러 카탈로그

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-501 | 기능 | 표준 표현 타입 렌더러 제공 | 시스템은 텍스트·테이블·그래프·이미지 등 표준 표현 타입에 대한 기본 렌더러를 제공해야 한다. | High |
| REQ-502 | 기능 | 영상 스트림·음성 1급 렌더러 지원 | 시스템은 영상 스트림, 음성 등 비표준 표현을 텍스트 패널에 HTML을 끼워 넣는 임시방편이 아닌, 1급 컴포넌트로 승격된 렌더러로 지원해야 한다. | High |
| REQ-503 | 기능 | 신규 렌더러 등록 인터페이스 | 개발자는 정의된 인터페이스를 구현해 신규 렌더러를 카탈로그에 등록할 수 있어야 하며, 코어 아키텍처 변경 없이 이를 재사용할 수 있어야 한다. | High |
| **REQ-504** | 기능 | **중첩 렌더링** | 시스템은 서로 다른 파이프라인의 출력을 하나의 렌더러 위에 겹쳐 표시할 수 있어야 한다(예: 영상 위 디텍션 박스 오버레이, 한 그래프에 두 소스의 계열). 각 레이어의 표시/숨김과 순서는 사용자가 조정할 수 있다. | High |

### 2.6 F6. 이벤트 기반 뷰 전환 규칙

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-601 | 기능 | 조건→뷰 액션 규칙 엔진 | 시스템은 "조건 충족 시 특정 뷰 액션(대상 확대, 전용 뷰 전환 등)을 실행"하는 규칙을 평가하는 엔진을 공통으로 제공해야 한다. 규칙 엔진 자체는 도메인 중립이다. | High |
| REQ-602 | 기능 | 이벤트 규칙 세트 확장 인터페이스 | 개발자는 분야별 규칙 내용(구체적 조건·액션 정의, 확장점 E4)을 규칙 엔진에 플러그인 형태로 등록할 수 있어야 한다. | Medium |

### 2.7 F7. 데이터 소스 추상화

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-701 | 기능 | DataSource 인터페이스 정의 | 시스템은 데이터 소스에 독립적인 공통 DataSource 인터페이스를 제공해야 한다. 개발자는 이 인터페이스를 구현해 새로운 데이터 소스를 연동할 수 있다. | High |
| REQ-702 | 기능 | Prometheus 구현체 제공 | 시스템은 DataSource 인터페이스의 첫 구현체로 Prometheus 연동을 **백엔드 질의 프록시 경유**(instant/range)로 기본 제공해야 한다. **가시화 뷰어는 Prometheus·브로커·DB에 직접 접근하지 않는다**(브라우저·네이티브 앱 공통). | High |
| **REQ-703** | 기능 | **Grafana 구현체 제공** | 시스템은 기존 Grafana 대시보드/쿼리를 DataSource 구현체로 흡수해, 이미 구축된 관측 자산을 재작성 없이 소비할 수 있어야 한다. | Medium |
| **REQ-704** | 기능 | **스트림 소스 지원** | 시스템은 폴링 기반 질의뿐 아니라 푸시 기반 스트림(영상, 고빈도 텔레메트리)을 DataSource 인터페이스 안에서 다룰 수 있어야 한다. 실시간 상태 스트림은 **백엔드 WS 게이트웨이 구독**으로 다루며, 구독 요청은 **계약 축 `{entity, node, channel}`** 로 표현한다(MQTT 토픽 문자열을 노출하지 않는다). | High |

> REQ-703 근거: 조사 결과 그라파나는 다중 데이터소스 중첩(Mixed)을 이미 잘 처리한다.
> 대체 대상이 아니라 **소비 대상**으로 두는 것이 합리적이다.
> (`docs/research/2026-08-18_grafana-dynamic-datasource.md`)

### 2.8 F8. 적합성 검증 도구

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-801 | 기능 | 디스크립터 검증기 | 시스템은 컴포넌트 디스크립터가 정의된 규약(필수 필드, 타입, 값 범위 등)을 만족하는지 검사하는 validator를 제공해야 한다. 개발자는 등록 전 검증 결과를 확인할 수 있다. | Medium |
| REQ-802 | 기능 | 파이프라인 명세 검증기 | 시스템은 해석 파이프라인 명세가 source/transform/sink 계약을 만족하는지 검사하는 validator를 제공해야 한다. | Medium |
| **REQ-803** | 기능 | **계약 간 정합성 검증** | 시스템은 개별 스키마 준수뿐 아니라 계약 간 참조 무결성(렌더러 `acceptsFieldTypes` ↔ 디스크립터 `field.type`, 파이프라인 sink ↔ 렌더러 등)을 검사해야 한다. | High |
| **REQ-804** | 기능 | **편집 중 실시간 검증** | 시스템은 사용자가 노드 에디터(F10)에서 유효하지 않은 연결을 시도할 때 연결 전에 이를 차단하거나 경고해야 한다. 사용자가 직접 그래프를 구성하는 이상, 검증은 배치가 아니라 편집 시점에 동작해야 한다. | High |

> REQ-803은 프로토타입 작업에서 실제로 발생한 결함(`binary-stream` 타입 부재로
> 스트리밍 렌더러가 도달 불가능)에서 도출됐다. 각 스키마를 독립 검증하는 것만으로는
> 잡히지 않았다.

### 2.9 F9. 제어 액션 바인딩 (양방향 확장)

> 명령의 실제 실행·검증은 백엔드 계층의 책임이며, 본 레이어는 액션의 **선언·노출·결과 표시**까지를 범위로 한다.
> **v0.2에서 우선순위를 상향**했다 — 회의 §4의 책임 소재 추적 요건은 공공 서비스에서 선택 사항이 아니다.

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-901 | 기능 | 제어 액션 선언 규약 | 개발자는 가시화 컴포넌트에서 발행 가능한 제어 액션을 대상, 파라미터, 허용 범위를 포함해 선언적으로 정의할 수 있어야 한다. 본 레이어는 **추상 action + params까지** 선언·발행하며, **디바이스 명령으로의 번역은 백엔드가 도메인 어휘집으로 수행한다**(예: `open_gate` → 센서 `levee:open`). | High |
| REQ-902 | 기능 | 권한 및 확인 절차 | 시스템은 제어 액션 발행 전 사용자 권한을 확인하고, 필요 시 확인(confirm) 절차를 거치도록 강제해야 한다. | High |
| REQ-903 | 기능 | 명령 발행 후 상태 반영 방식 | 개발자는 명령 발행 후 화면 상태의 반영 시점을 액션별로 지정할 수 있어야 하며, **기본은 확인 후 반영**이다. 확정 판정은 백엔드가 디바이스 ack를 공통 `command.result`로 승격(→`completed`)해 내려주는 값을 따르며, 본 레이어는 **`accepted`(진행중) / `completed`(확정) / `rejected`·`timeout`·`failed`(실패)** 3상태로 표시한다. | High |
| REQ-904 | 기능 | 실패·타임아웃 처리 | 시스템은 명령이 실패하거나 타임아웃될 경우 이를 사용자에게 명확히 표시하고, 필요 시 이전 상태로 복원해야 한다. | High |
| REQ-905 | 기능 | 자동/수동 제어 모드 상태 가시화 | 시스템은 자동 제어(AI 기반)가 개입 중인지 여부와, 이때 수동 조작이 어떻게 처리되는지(우선순위 관계)를 화면에 표시해야 한다. | High |
| REQ-906 | 기능 | 레이어 책임 범위 한정 | 시스템(가시화 레이어)은 제어 명령의 실제 실행·검증을 직접 수행하지 않으며, 액션의 선언·노출·발행 및 결과 표시까지만 담당한다. 실행·검증은 백엔드 인터페이스 계약을 통해 위임한다. | High |
| **REQ-907** | 기능 | **책임소재 필드 전달** | 시스템은 발행하는 모든 제어 명령에 **책임소재 필드**(`origin.path`, `input.mode`, `decision.source`, `voice.*`, `llm.*`)를 실어 백엔드에 전달해야 한다. **OTel Log Record의 작성·저장은 백엔드 audit-writer가 수행**하며, `actor`는 토큰에서·시각은 서버 시각으로 백엔드가 주입한다. **본 레이어는 감사를 OTel에 직접 발행하지 않는다.** 인바운드 데이터만 저장되고 아웃바운드(제어 명령)가 저장되지 않으면 사후 책임 소재를 추적할 수 없다. | High |
| **REQ-908** | 기능 | **발행 경로 구분 기록** | REQ-907의 "어떤 방식으로"는 **입력 수단**(`input.mode`: click / voice / api)과 **판단 주체**(`decision.source`: human / llm_suggestion / auto)를 **두 축으로 분리해** 기록해야 한다. 기존 `origin.path` 4값은 이 두 필드에서 파생되는 값으로 유지한다. | High |
| **REQ-909** | 기능 | **명령 상관키 및 만료** *(v0.3 신설)* | 시스템은 발행하는 모든 제어 명령에 **`command_id`**(전 파트 단일 상관 키 — 명령↔결과↔감사를 하나로 잇는다)와 **`expires_at`**(만료 후 실행 금지)을 포함해야 한다. | High |

> REQ-908 근거: F12·F13을 도입하면 명령 발행 경로가 늘어난다. "누가 눌렀나"만으로는
> 부족하고 "사람이 직접 눌렀나, LLM 제안을 수락했나"가 구분되어야 감사 로그가 의미를 갖는다.
>
> **두 축으로 나누는 이유** *(v0.3)*: 단일 4값으로는 **"음성으로 말했고 LLM이 해석했다"**
> 가 표현되지 않는다. `voice`로 적으면 LLM 개입이, `llm_suggestion`으로 적으면 음성 입력이
> 기록에서 사라져, 사고 시 원인(STT 오인식 / LLM 오해석)을 구분할 수 없다. 네 값은 서로
> 배타적이지 않으므로 축을 나눈다. 조합 예: 음성→LLM 해석→수락 = `voice` × `llm_suggestion`.

### 2.10 F10. 노드 조합 편집기 *(v0.2 신설)*

> 회의록 §2 "요구 방향: 동적 가시화" / 언리얼 블루프린트 비유.
> **F4가 파이프라인의 실행 모델이라면, F10은 그 편집 인터페이스다.**

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-1001 | 기능 | 노드 그래프 시각 편집 | 사용자는 데이터 소스·변환·표현 노드를 캔버스에 배치하고 포트를 연결해 파이프라인을 구성할 수 있어야 한다. | High |
| REQ-1002 | 기능 | 계약과의 무손실 왕복 | 편집기가 만든 그래프는 F4 파이프라인 계약(JSON)으로 직렬화되고, 계약으로부터 동일한 그래프로 복원되어야 한다. 편집기 고유 정보(노드 좌표 등)는 계약을 오염시키지 않는 별도 영역에 둔다. | High |
| REQ-1003 | 기능 | 노드 카탈로그 | 시스템은 사용 가능한 노드 목록을 등록된 디스크립터·렌더러·데이터소스로부터 자동 구성해 제시해야 한다. 개발자가 편집기 코드를 고쳐 노드를 추가하는 일이 없어야 한다. | High |
| REQ-1004 | 기능 | 연결 유효성 강제 | 시스템은 출력 타입과 입력 타입이 맞지 않는 연결을 차단하고 사유를 표시해야 한다. (REQ-804와 동일 요건의 편집기 측면) | High |
| REQ-1005 | 기능 | 시험 실행 | 사용자는 구성 중인 그래프를 실제 대상에 반영하기 전에 시험 실행하고 각 노드의 출력을 확인할 수 있어야 한다. | High |
| REQ-1006 | 기능 | 반영(commit) 및 되돌리기 | 사용자는 시험 실행으로 검증한 그래프를 명시적 "반영" 조작으로만 운영 상태에 적용할 수 있어야 하며, 직전 상태로 되돌릴 수 있어야 한다. | High |
| REQ-1007 | 기능 | 역방향 관측 | 시스템은 반영된 그래프가 실제로 처리하고 있는 현실 데이터를 편집기 화면에서 확인할 수 있게 해야 한다. 편집기는 구성 도구인 동시에 관측 도구다. | Medium |

### 2.11 F11. 추상화 계층 뷰 및 설명가능성 *(v0.2 신설)*

> 회의록 §2 "계층적 가시화 (군 지휘 비유)".
> **F3(공간 축)과 직교하는 추상화 축이다.**

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-1101 | 기능 | 추상화 수준 선언 | 개발자는 하나의 데이터 집합에 대해 복수의 추상화 수준(집약 지표 / 구성 요소 / 원본)과 각 수준의 표현을 선언할 수 있어야 한다. | High |
| REQ-1102 | 기능 | 집약 규칙 정의 | 개발자는 하위 수준의 데이터로부터 상위 수준의 판단 지표를 산출하는 규칙을 선언적으로 정의할 수 있어야 한다. 규칙 내용은 도메인별(확장점), 규칙 **엔진**은 공통이다. | High |
| REQ-1103 | 기능 | 하향 드릴다운 | 사용자는 상위 수준의 지표에서 그 값을 만들어낸 하위 수준으로 이동할 수 있어야 한다. | High |
| REQ-1104 | 기능 | 근거 추적(설명가능성) | 시스템은 상위 지표에 대해 "이 값이 나온 이유"를 기여한 하위 데이터 항목의 목록·기여도 형태로 제시할 수 있어야 한다. | High |
| REQ-1105 | 기능 | 사용자 계층별 기본 뷰 | 시스템은 사용자 역할(L0/L1/L2)에 따라 진입 시 기본 추상화 수준을 다르게 제공해야 한다. | Medium |

> REQ-1104는 F12(LLM)를 쓰든 안 쓰든 성립해야 한다. **LLM이 만든 요약에
> 근거가 붙지 않으면 그것은 관제 시스템에서 쓸 수 없다.**

### 2.12 F12. 로컬 LLM 인터페이스 계층 *(v0.2 신설)*

> 회의록 §2 "차별화 포인트: 로컬 LLM 기반 가시화 도구".
> 프롬프트 창이 아니라 **MCP 기반 서버로 띄워 API처럼 사용**하는 것이 요점이다.

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-1201 | 기능 | LLM 런타임 추상화 | 시스템은 특정 모델·추론 런타임에 종속되지 않는 인터페이스 뒤에 LLM을 은닉해야 한다. 온디바이스 로컬 모델을 기본 구현체로 한다. | High |
| REQ-1202 | 기능 | 도구(tool) 노출 규약 | 시스템은 LLM이 호출할 수 있는 조작(노드 추가, 데이터 질의, 뷰 전환 등)을 도구 형태로 선언·노출해야 하며, 도구 집합은 확장점(E7)으로 분야별 확장 가능해야 한다. | High |
| REQ-1203 | 기능 | 태스크 분해 → 노드 생성 | 시스템은 사용자의 자연어 지시를 서브태스크로 분해하고, 각 서브태스크를 F10 캔버스 위의 노드로 자동 생성해야 한다. 사용자는 생성된 노드를 더블클릭해 개별 결과를 확인할 수 있다. | High |
| REQ-1204 | 기능 | 제안-검토-수락 절차 | 시스템은 LLM이 생성한 구성을 **제안 상태**로 표시하고, 사용자가 검토·수정한 뒤 명시적으로 수락해야 반영되도록 해야 한다. LLM 출력이 검토 없이 운영 상태에 적용되어서는 안 된다. | High |
| REQ-1205 | 기능 | 데이터 가공 계층 | 시스템은 원본 데이터를 렌더러에 넘기기 전에 LLM으로 한 번 가공(요약·이상 설명·자연어 레이블링)하는 경로를 파이프라인 노드 형태로 제공해야 한다. | Medium |
| REQ-1206 | 기능 | LLM 미가용 시 성능 저하 허용 | 시스템은 LLM이 연결되지 않았거나 응답하지 않을 때 관련 기능만 비활성화하고 나머지는 정상 동작해야 한다. | High |
| REQ-1207 | 기능 | LLM 관여 표시 | 시스템은 화면상의 어떤 내용이 LLM에 의해 생성·가공된 것인지 사용자가 구별할 수 있게 표시해야 한다. | High |
| **REQ-1208** | 기능 | **제어 액션 카탈로그 소비** *(v0.3 신설)* | 시스템은 백엔드로부터 **발행 가능한 제어 액션 목록**(action id · 적용 대상 타입 · 파라미터 스키마 · 필요 역할 · 확인 필요 여부)을 수신해 LLM 도구 컨텍스트로 제공해야 한다. **권한이 없는 액션은 제안조차 하지 않는다.** | High |

> **REQ-1208 근거** *(v0.3)*: 사람이 버튼을 누를 때는 개발자가 액션을 알고 있어 문제가 없지만,
> LLM이 발화에서 명령을 생성하려면 **런타임에 "무엇이 가능한지"를 알아야 한다.** 감사 스키마에
> `action.id`·`action.params`가 있으나 그 목록의 출처가 계약에 없었다. 제어 액션은 백엔드가
> MQTT로 발행하므로 카탈로그도 백엔드 소유다(REQ-901의 번역 분계점과 짝). 이 카탈로그는 F12만의
> 요구가 아니라 **F10 노드 카탈로그(REQ-1003)와 권한별 UI 차단(NFR-001)의 공통 입력**이기도 하다.

> REQ-1207 근거: F11의 설명가능성과 직결된다. 관측된 사실과 모델이 생성한 해석이
> 화면에서 구별되지 않으면 시스템 전체의 신뢰성이 무너진다.

### 2.13 F13. 음성 인터페이스 *(v0.2 신설)*

> 회의록 §3. 기술 난이도는 낮으나 시연 효과가 크고, F12와 결합해 "음성 → 태스크 분해 → 노드 생성" 경로를 완성한다.

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| REQ-1301 | 기능 | STT 입력 → 액션 트리거 | 시스템은 음성을 텍스트로 변환해 기존 클릭 이벤트와 동일한 액션 트리거로 처리해야 한다. 음성은 **추가 입력 수단**이며 기존 조작을 대체 강제하지 않는다. 적용 범위는 **뷰 전환 · 데이터 조회 · 제어 명령 · 구성(노드 그래프) 생성 전체**이며, 제어와 구성은 REQ-1303·REQ-1204의 확인 절차를 반드시 거친다. | High |
| REQ-1302 | 기능 | STT 엔진 추상화 | 시스템은 특정 STT 엔진에 종속되지 않는 인터페이스를 제공해야 한다. | Medium |
| REQ-1303 | 기능 | 인식 결과 확인 | 시스템은 인식된 발화를 사용자에게 표시하고(**1차 확인**), 해석된 액션을 제안 형태로 표시해 수락받아야 한다(**2차 확인**, REQ-1204). 특히 F9 제어 액션은 음성만으로 즉시 실행되어서는 안 된다. 되돌릴 수 있는 조회·뷰 전환은 1차 확인만으로 진행할 수 있다. | High |
| **REQ-1306** | 기능 | **신뢰도 임계 처리** *(v0.3 신설)* | 시스템은 STT 인식 신뢰도 또는 의도 해석 신뢰도가 임계 미만이면 액션을 실행하지 않고 재확인을 요구해야 한다. 임계값은 실측 후 확정한다(Tier 2). | High |
| REQ-1304 | 기능 | 명령 어휘 확장 | 개발자는 분야별 발화 표현과 액션의 매핑(확장점 E8)을 등록할 수 있어야 한다. **Entity 호칭 사전은 별도 시스템이 아니라 레지스트리의 `display_name`·`aliases[]`에서 수신한다**(REQ-305). | Medium |
| REQ-1305 | 기능 | 발화 원문 감사 전달 | 시스템은 음성으로 발행된 액션에 대해 인식된 발화 원문(`voice.transcript`)·신뢰도·엔진 식별자와 해석된 의도(`nlu.intent`)를 **명령 요청의 감사 필드로 백엔드에 전달해야** 한다. 기록은 audit-writer가 수행한다. (REQ-907/908 연계) | High |

> **음성 관련 자체 지표** *(v0.3)*: `viz.stt.latency` · `viz.stt.confidence` · `viz.nlu.latency` ·
> `viz.nlu.intent_unmatched`(해석 실패 — 시연 판정의 핵심 지표) · `viz.voice.command_issued`를
> `viz.*` 네임스페이스로 OTel에 직접 노출한다. 단위는 이름이 아니라 `unit` 필드에 둔다.
>
> **원본 오디오는 보존하지 않고 전사 텍스트만 남기는 것을 기본**으로 한다(개인정보). 확정은
> 감사 보존 기간과 함께 Tier 2.

---

### 2.14 F14. 디버깅 실행 추적 *(v0.6 신설)*

임무 실행을 계층적으로 추적해 **실패 원인에 도달하는 경로를 좁히는** 축이다. F11(추상화 계층 뷰)이
"같은 대상을 다른 깊이로 본다"라면 F14는 "같은 실행을 시간축으로 되짚는다"이며 서로 직교한다.

| 요구 | 내용 |
|---|---|
| REQ-1401 | 임무를 **마일스톤 → 태스크 → 액션 아이템** 3계층으로 표현한다. 마일스톤·태스크는 도메인 무관, 액션 아이템만 대상 기종에 의존한다 |
| REQ-1402 | 태스크는 `deps`로 분기·합류를 갖는 **DAG**다. 일렬 인덱스로 표현하지 않는다 |
| REQ-1403 | 태스크 종료는 **평가 통과로만** 성립한다. 실행 완료와 평가 완료를 구분한다 |
| REQ-1404 | 실행 사건을 **덧붙이기 전용 기록 열**로 보관한다. 각 항목은 순번·시각·계층·노드·종류·**산출 주체(AI/백엔드/사람)**를 갖는다 |
| REQ-1405 | 기록을 접어 **임의 시점의 계층 상태를 복원**한다. 되감기 중에는 대상으로 나가는 명령을 차단한다 |
| REQ-1406 | 실패 노드에서 의존·파생 관계를 **역방향 탐색**해 실패 경로만 남긴다. 격리 집합은 **근본 원인을 반드시 포함**해야 한다 |
| REQ-1407 | 실행 상태를 **8종**으로 구분한다. 특히 `건너뜀`(의도)과 `미수행`(선행 실패)을 분리한다 |
| REQ-1408 | 마일스톤·태스크 계약에 **하드웨어 어휘가 들어가지 못하게** 스키마와 회귀 검증으로 강제한다. 기종 의존 값은 어댑터 프로파일이 해석한다 |
| REQ-1409 | 화면에서 **정지·재시작·재실행·파라미터 수정·결함 주입**을 발행하며, 모두 산출 주체를 사람으로 기록한다 |

**계약 위험**: REQ-1401~1403의 생산 주체가 미정이다(7.6 참조). 계약이 확정되기 전까지 F14는 목 데이터로만 성립한다.

### 2.15 F15. 임무 생성 *(v0.7 신설)*

F14가 "실행된 것을 되짚는" 축이라면 F15는 **"실행할 것을 만드는"** 축이다.
삭제된 `AI-D-01`·`AI-D-02`를 인수하면서 생겼다.

| 요구 | 내용 |
|---|---|
| REQ-1501 | 발화 텍스트를 임무와 **마일스톤 목록으로 분리**한다. 이 단계가 명령 의도 해석에 해당한다 |
| REQ-1502 | 마일스톤마다 **태스크를 생성**한다. 태스크는 `deps`를 갖는 DAG이며 하드웨어 어휘를 포함하지 않는다 |
| REQ-1503 | 생성된 태스크를 실행 전에 **검증**한다. 검증은 생성과 **분리된 모듈**이며 동일 입력에서 재현 가능해야 한다 |
| REQ-1504 | 검증을 통과한 태스크를 **어댑터 프로파일에 따라 액션 아이템으로 전개**한다 |
| REQ-1505 | 태스크마다 **평가 기준을 생성하고 판정**한다. 근거가 도달하지 않으면 통과로 승격하지 않는다 |
| REQ-1506 | 생성 결과는 **제안 상태**로 표시하고 사용자 수락으로만 다음 단계로 넘어간다 |
| REQ-1507 | 생성에 쓴 **생성기 버전과 입력을 근거로 기록**해 실패 시 이 단계까지 역추적할 수 있게 한다 |
| REQ-1508 | 로컬 LLM이 없거나 무응답이면 **생성만 비활성화**하고 실행·관측·디버깅은 계속한다 |

**설계상 주의 — 생성기와 디버거가 같은 레이어에 있다.**
같은 사람이 만들더라도 생성(REQ-1501·1502)과 검증(REQ-1503)은 모듈을 분리한다.
생성기가 스스로를 검증하면 같은 오해를 두 번 반복할 뿐이고, 재현 가능한 검증이라야
실패했을 때 **생성 탓인지 검증 탓인지** 가를 수 있다. 같은 이유로 `VZ-U-07` 승인 화면은
생성 근거와 검증 결과를 요약하지 않고 그대로 펼친다.

---

## 3. 파트 경계 (본 레이어의 책임 범위)

> 회의록 §1 파트 재분담 및 §4 "최대 리스크: 각자 따로 만들면 통합하는 순간 안 맞는다"에 대응한다.

| 영역 | 담당 | 본 레이어와의 관계 |
|---|---|---|
| OTel 기반 관측 스택 | **대규** | 본 레이어는 **질의 프록시·WS 게이트웨이·감사 조회 API로 소비**하고, 제어 명령과 **감사 필드를 백엔드에 전달**한다. **OTel 직접 발행은 `viz.*` 자체 지표만이다.** |
| K8s/도커 마이크로서비스, MLOps | **나영** | 상태 3층 중 **`deployment` 층의 원천**이다. 본 레이어는 **백엔드가 조합한 상태 3층으로** 수신한다(직접 수신 아님). |
| 디지털 트윈 구성 AI(360도 앵커 정합) | 대규 | F3 좌표 정합(E2)의 입력을 제공한다. **축 변환과 로컬→글로벌 변환도 백엔드 책임**이다(REQ-302). |
| 예측 AI | 나영 | F12와 별개다. 예측 결과는 본 레이어에 데이터로 들어온다. |
| 오브젝트 디텍션 | AI 파트 | F4 파이프라인의 transform 노드로 호출된다(REQ-407). |
| 말단 센서 / 로봇·장비 | 하드웨어 2인 | 본 레이어는 직접 접촉하지 않는다. 관측 스택을 경유한다. |
| **가시화(본 문서 전체)** | **현우** | F1~F15 |
| **임무 생성(발화 해석·태스크·검증·평가)** | **현우** *(v0.7 인수)* | 원래 AI 파트였다. `AI-D-01`·`02` 삭제와 AI 팀원의 논문 병행으로 가시화가 가져왔다. **관측값은 여전히 만들지 않는다 — 만드는 것은 임무 계층뿐이다.** |

**경계에서 명시적으로 제외한 것**

| 제외 항목 | 사유 |
|---|---|
| 오케스트레이터 이벤트 직접 감지 | 나영 파트. 본 레이어는 계약으로 수신만 한다(REQ-201). |
| Prometheus/OTel 수집기 구축·운영 | 대규 파트. 본 레이어는 질의만 한다(REQ-702). |
| 제어 명령의 실제 실행·검증 **및 디바이스 명령 번역** | 백엔드. 본 레이어는 **추상 action 선언·노출·발행·표시**까지다(REQ-901, REQ-906). |
| 감사 로그의 **작성**·저장·조회 | 대규 파트(audit-writer). 본 레이어는 **감사 필드 제공과 조회 API 소비만** 한다(REQ-907). |
| 예측·디텍션 모델 자체 | AI 파트. 본 레이어는 결과를 노드로 소비한다. |

### 3.1 통합 앱 구조 — 무엇이 어디에 있는가 *(v0.9 전면 개정)*

**두 도구를 한 앱으로 합친다.** `web-dashboard`의 탭을 새 앱으로 이식하고,
지금 만드는 **「임무 설계 및 디버깅」을 탭 하나로** 넣는다. 폐기가 아니므로 **요구사항 46건이 하나도 죽지 않고**,
다른 파트의 수신·송신 상대도 그대로 유지된다.

```
통합 앱  (그릇은 viz-debugger/)
├─ 상단 공통 바 ── 임무 제어 · 임무 이력 · 알림
├─ 공유 계층 ───── 전송/구독 · 레지스트리 · 명령 발행·추적 · 권한 · 표기 규약   [16건]
└─ 탭 6개
   ① 임무 설계 및 디버깅  ← 본류      ② 구역 현황판  ③ 제어 패널
   ④ 지표 조회  ⑤ 영상 오버레이  ⑥ 파이프라인 편집기(동결)
```

#### 공유 계층 (16건) — 탭에 귀속되지 않는다

| 계층 | 요구사항 | 하는 일 |
|---|---|---|
| 전송·구독 | `VZ-I-01` `I-02` `I-11` | 계약 축 구독, 재접속 시 현재값 복원, 범위 한정 자리 |
| 레지스트리 | `VZ-I-03` | 대상 목록·소속·배치. 탭②의 장치 카드와 탭①의 배정 풀이 같은 원천 |
| 명령 발행·추적 | `VZ-O-01` `O-02` `O-03` `O-05` | 발행 → ACK → 진행 → 완료 4단계, 감사 필드, 제어 잠금 |
| 권한 | `VZ-C-01` `C-04` | 역할·범위. 잠금 판정이 한 곳에서만 나와야 한다 |
| 표기 규약 | `VZ-C-03` `C-06` | 집약 계층 · 원천 종류(실물/시뮬레이션) |
| 자체 관측 | `VZ-O-04` | `viz.*` 지표 |
| 기동 | `VZ-C-02` `C-05` | 외부 소스 부재 시 동작, 규모 가정 |
| 상단 알림 | `VZ-I-10` | 외부 AI 실패 + **탭①의 생성 실패(F15)** |

#### 탭 6개

| # | 탭 | 요구사항 | 상태 | 무엇을 보나 |
|---|---|---|---|---|
| ① | **임무 설계 및 디버깅** | **18건** | **신규 본류** | F14 디버깅 + F15 생성 |
| ② | 구역 현황판 | `VZ-U-01` `I-08` | 이식 | 장치 상태 4종 + 배포 층 + 환경 위험도 |
| ③ | 제어 패널 | `VZ-I-05` | 이식 | 수동 제어, 감사 이력 조회 |
| ④ | 지표 조회 | `VZ-I-04` | 이식 | 시계열 질의, 구역 요약 |
| ⑤ | 영상 오버레이 | `VZ-I-06` `I-07` `I-09` | 이식 | 스트림 + 탐지 박스 정합 + 추적·궤적 |
| ⑥ | 파이프라인 편집기 | `VZ-U-04` `L-04` | **동결·시연용** | source→transform→sink 조립, 계약 왕복, 시험 실행→반영 |

**탭① 내부 18건** — 디버깅 `VZ-D-01`~`08` (8) · 생성 `VZ-G-01`~`05` (5) ·
음성 `VZ-L-01`~`03` (3) · 흡수 `VZ-U-03` 계층 뷰 · `VZ-U-07` 계획 승인·거부 (2).

> *2026-08-28 이식 결과* — `VZ-U-07`(계획 승인·거부)은 탭① 안에 들어왔다.
> `VZ-U-03`(계층 뷰)은 **탭②에 남았다** — 위 이동표의 사유 참조. 배치 건수는 그대로이나
> 실제 위치가 다르므로 다음 개정 때 이 표를 한 번 더 맞춰야 한다.

#### 기존 7탭에서 어떻게 옮겨졌나

| 기존 탭 | 어디로 |
|---|---|
| 구역 현황판 | ② 그대로 + `VZ-I-08` 위험도 흡수 |
| 제어 패널 | ③ 그대로 |
| 임무 승인·진행 | **탭①으로 통폐합** — 승인·거부는 탭 안 버튼, `VZ-U-05`는 `VZ-D-01`·`D-06`이 대체 |
| 지표 조회 | ④ 그대로 |
| 영상 오버레이 | ⑤ 그대로 |
| 판단·알림 | **해체** — 위험도→②, AI 실패→상단 알림, 자체 관측→공유 계층. **계층 뷰(`VZ-U-03`)는 ①이 아니라 ②의 위험도 패널에 남았다** — 탭①의 표시 깊이 설계가 HCI 논의 미결(전달본 논의 항목 5)이라 지금 옮기면 탭① 전달본이 바뀐다 *(2026-08-28 이식 결과 반영)* |
| 노드 그래프 | **파이프라인 모드만 ⑥으로.** 임무 관제 모드는 가져오지 않고 지웠다 — 탭①이 같은 일(임무·태스크 그래프)을 하므로 둘을 다 두면 어느 쪽을 봐야 하는지 알 수 없다 *(2026-08-28 이식 결과 반영)* |

#### 46건 배치 확인

| 배치 | 건수 |
|---|---|
| 탭 ① 임무 설계 및 디버깅 | 18 |
| 공유 계층 | 16 |
| 탭 ②~⑤ | 7 |
| 탭 ⑥ (동결) | 2 |
| Unity 트윈 뷰어 (별도 앱) — `VZ-U-02` | 1 |
| 탭①이 대체 — `VZ-U-05` | 1 |
| 보류(미구현) — `VZ-U-06` | 1 |
| **합계** | **46** |

배치는 엑셀 김현우 시트의 **`통합 앱 배치`** 열에 항목마다 표기했다.

#### 구조상 지켜야 하는 것 셋

1. **상태 어휘를 분리한다.** 탭②의 `VZ-U-01`은 **장치 상태 4종**이고 탭①의 `VZ-D-06`은
   **태스크 상태 8종**이다. 둘 다 "상태"라 코드와 화면에서 섞이기 쉽다. 타입 이름부터 가른다.
2. **명령 출구는 하나다.** 탭③의 수동 제어와 탭①의 재실행이 같은 `VZ-O-01`을 쓴다.
   출구가 둘이 되면 트레이스가 갈라지고 `VZ-D-02`의 `produced_by=human` 기록이 한쪽에만 남는다.
3. **탭① 단독 빌드를 유지한다.** 논문 측정축 D를 잴 때 다른 탭이 같이 뜨면 부하가 섞인다.

#### 통합 시점 — 둘로 나눈다

통합은 하나가 아니라 셋이고, 그중 둘은 **1단계 직후 바로** 한다.

| | 무엇 | 언제 | 왜 |
|---|---|---|---|
| ① 셸 골격 | 탭 전환 · 상단 공통 바 · 빈 탭 5개 | **1단계 직후** | HCI 팀원이 **"이게 전체에서 어디쯤인지"** 를 알아야 배치를 논할 수 있다 |
| ② 공유 계층 경계 | transport 하나 · 명령 출구 하나 · store 분리 · 단독 빌드 타깃 | **1단계 직후** | 미루면 기록 저장소와 명령 개입이 **탭① 안에** 만들어지고 나중에 뜯어내야 한다 |
| ③ 탭 5개 실제 이식 | 구역현황판 · 제어 · 지표 · 영상 · 파이프라인 **+ 목 게이트웨이 합류** | ~~10월~~ **2026-08-28 완료** | 시연에서 사이트를 둘 띄우면 통합했다는 주장이 화면으로 증명되지 않는다. 그리고 `VZ-G-01`이 `utterance.confidence` 계약 결정(§7.8)에 막혀 있어 그 자리에 넣었다 |

**②를 미루면 반드시 비싸진다.** 특히 셋이 위험하다 — 탭①이 자기 발행 경로를 만들면 나중에
탭③을 붙일 때 **명령 출구가 둘**이 되고, 장치 4종과 태스크 8종을 나중에 가르는 것은
**이미 섞인 타입을 푸는 일**이 되며, 단독 빌드를 나중에 만들면 **이미 얽힌 import를 끊어야** 한다.

**③은 미뤄도 된다고 봤으나 앞당겼다.** 판단 자체는 맞았다 — ②에서 자리를 갈라 둔 덕분에
실제 이식이 그 자리에 끼우는 일이 됐다. 두 상태 모델은 **저장소만 둘, transport 는 하나**로 공존한다
(`DataStore` = 채널별 최신값, `TraceStore` = 기록 열).

**③에서 가장 큰 일은 화면이 아니라 목 게이트웨이였다.** 화면만 옮기면 탭②~⑥이 텅 빈 채로 뜬다.
`web-dashboard/mock-gateway/`(4,757줄)가 본체가 되고 옛 시나리오 재생기(10줄)가 그 안의
`trace_event` 채널로 접혔다. **게이트웨이는 하나이고 포트는 8790이다** — `web-dashboard`(8787)는
기준선으로 계속 살아 있어 둘을 동시에 띄워 비교할 수 있다.

작업 지시는 `작업프롬프트_통합_260827.md`(①②)와 `작업프롬프트_탭이식_260828.md`(③)에 있고,
결과는 `reports/2026-08-28_1900_탭이식.md` 에 있다.

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

| 요구사항 ID | 구분 | 요구사항 명 | 상세 설명 | 중요도 |
|---|---|---|---|---|
| NFR-001 | 보안 | 제어 액션 권한 관리 | 시스템은 F9 제어 액션 발행 권한을 역할 기반(RBAC)으로 관리해야 하며, 권한 없는 사용자의 명령 발행 시도는 차단하고 오류를 표시해야 한다. | High |
| NFR-002 | 보안 | 제어 명령 감사 로그 | 시스템은 모든 제어 명령의 대상, 파라미터, **발행 경로**를 감사 필드로 백엔드에 전달해야 하며, **기록·보존·조회는 백엔드가 수행한다**(발행자·시각은 백엔드가 주입). 본 레이어는 **감사 조회 API로 되읽어** 화면에 표시한다. | High |
| NFR-003 | 보안 | 명령 발행 전 검증 | 시스템은 제어 명령을 발행하기 전 파라미터가 선언된 허용 범위 내에 있는지 클라이언트 단에서 1차 검증해야 한다. (최종 검증은 백엔드 책임, REQ-906 참조) | High |
| NFR-004 | 성능 | 대시보드 반영 지연 시간 | 시스템은 라이프사이클 이벤트 수신 후 정해진 목표 시간 이내에 가시화 컴포넌트 생성·삭제를 완료해야 한다. (목표 수치는 분야별 문서에서 정의) | Medium |
| NFR-005 | 호환성 | 관측 스택 비종속성 | 시스템은 특정 관측 스택(OTel / Prometheus / Grafana) 구현체를 DataSource 인터페이스(F7) 뒤로 은닉해, 구현체 교체 시 상위 계층 코드 변경 없이 동작해야 한다. | High |
| NFR-006 | 확장성 | 코어 무변경 확장 | 신규 렌더러(F5), 신규 데이터 소스(F7), 신규 이벤트 규칙(F6), **신규 노드 타입(F10), 신규 LLM 도구(F12)** 를 추가할 때 코어 아키텍처 코드 수정 없이 인터페이스 구현만으로 통합되어야 한다. | High |
| NFR-007 | UI 호환성 | 편집기 비종속 명세 | 시스템의 설정 명세(디스크립터, 파이프라인, 규칙)는 특정 편집기 구현(데스크톱 / 웹)에 의존하지 않는 독립된 포맷으로 저장·교환되어야 한다. | Medium |
| **NFR-008** | 성능 | **편집기 응답성** | F10 노드 편집기는 노드 수가 늘어나도 조작(드래그·연결·선택)에 대한 화면 반응이 지연되지 않아야 한다. 목표 노드 규모는 분야별 문서에서 정의한다. | Medium |
| **NFR-009** | 성능 | **LLM 응답 시간 및 비차단** | F12 LLM 호출은 UI를 차단해서는 안 되며, 진행 상태를 표시하고 사용자가 취소할 수 있어야 한다. | High |
| **NFR-010** | 보안 | **로컬 처리 원칙** | LLM 추론은 로컬(온디바이스)에서 수행하는 것을 기본으로 하며, 관제 데이터가 외부 서비스로 전송되지 않아야 한다. 외부 모델을 사용하는 구성은 명시적 설정으로만 허용한다. | High |
| **NFR-011** | 신뢰성 | **관측 사실과 생성 내용의 구별** | 시스템은 측정된 데이터와 LLM이 생성·추정한 내용을 화면·로그 양쪽에서 구별 가능하게 유지해야 한다. | High |

---

## 5. 확장점(E1~E8) ↔ 분야 매핑표

> 아래 표는 공통 요구사항이 각 분야에서 어떤 확장점으로 구체화되는지를 나타내는 참조용 매핑이다. 분야별 상세 내용은 별도 문서에서 정의한다.

| 확장점 ID | 확장점 | 캡스톤(로봇 관제) | 하천(월류방어벽) | 국방(GOP 감시) |
|---|---|---|---|---|
| E1 | Entity 타입 정의 | 로봇, 탐지 객체 | 방어벽 게이트, 수위 센서 | 탐지 객체 |
| E2 | 좌표·배경 어댑터 | 디지털 트윈 가상환경 좌표계(y=위, 바닥 x-z, 1 unit=1 m) | 하천 지형/지도 기반 좌표계 | GOP 지도 기반 좌표계 |
| E3 | 전용 렌더러 | 고빈도 실시간 텔레메트리, 영상 스트림 | 수위 그래프, 임계 경보 패널 | 객체 탐지 박스 오버레이 |
| E4 | 이벤트 규칙 세트 | 이상 징후 시 관리자 긴급 제어 알림 | 임계 수위 초과 시 경보 및 뷰 전환 | 객체 탐지 시 경보 및 뷰 전환 |
| E5 | 파이프라인 프리셋 | 다구역 실시간 데이터 전처리 체인 | 수위 센서 전처리, AI 자동 제어 입력 체인 | 객체 탐지 결과 전처리 체인 |
| E6 | 제어 액션 세트 | 관리자 긴급 제어(정지/이동 등) | 방어벽 상승/하강 명령, AI 자동 제어 개입 | (미확정 — 참조용) |
| **E7** | **LLM 도구 세트** | "○○구역 로봇 상태 보여줘", 이상 로봇 자동 탐색 | "수위 급상승 구간 찾아줘", 경보 이력 요약 | (미확정 — 참조용) |
| **E8** | **음성 명령 어휘** | 구역명·로봇 호출명 사전 | 게이트명·하천 지점명 사전 | (미확정 — 참조용) |
| **E9** | **계획 재생성 판단** *(v0.7 신설)* | 엘리베이터 정지·경로 차단 시 남은 태스크 재생성 | 수위 급변으로 점검 순서가 무의미해질 때 재생성 | (미확정 — 참조용) |

**E9에 대한 주석.** 구 `AI-D-04`가 담당하던 것으로, 실행 도중 계획의 사전조건이 깨졌을 때
남은 부분을 다시 만드는 판단이다. **F15 본체에는 넣지 않는다.** 이유 셋.

1. **필수 동작이 아니다.** 전제가 깨지면 사용자가 임무를 중단하고 다시 발화하는 경로가 이미 있다(`VZ-D-08`).
2. **판단 규칙의 난이도가 본체와 맞지 않는다.** "실제로 깨졌을 때**만**"을 가르는 것 —
   실행 가능성에 영향을 주는 변화와 단순 관측 변화의 구분 — 이 그 자체로 별개 연구 주제다.
   과민하면 계획이 계속 다시 만들어져 로봇이 앞으로 못 나가고, 그 재생성 사건이 트레이스를 덮어
   **디버깅하려고 만든 기록이 노이즈가 된다.**
3. **측정에 변인을 더한다.** 실행 중 그래프가 자라면 탐색 공간 축소율의 **분모가 흔들린다.**

계약은 이미 열려 있다 — `task.derived_from`과 `TraceEvent.kind=derived`가 그 자리다.
나중에 붙여도 스키마는 바뀌지 않는다. **본체를 다 만들고 확장을 검토할 때 다시 본다.**

---

## 6. 외부 인터페이스 계약이 필요한 항목

> 회의록 §4의 "공통 인터페이스 문서 1개(모듈별 데이터 수신부 / 송신부 명세)"에 제출할 **가시화 모듈의 초안**이다.
>
> 아래 표는 **어떤 계약이 필요한지의 목록**이고, 각 계약의 필드 수준 명세는 상대 파트별
> 인터페이스 시트로 분리한다. 제출본: `docs/interfaces/2026-08-18_otel-interface.md`(대규).

### 6.1 받는 쪽 (인바운드)

| 요구사항 ID | 대상 파트 | 계약 내용 |
|---|---|---|
| REQ-201, 205 | 대규(백엔드 경유) | **상태 3층 필드 규격**(`device.status`/`availability`/`deployment` + `last_seen`) 및 WS 구독 규격(구독 축 `{entity,node,channel}`) — *v0.3: 나영 직접 수신에서 백엔드 경유로 변경* |
| **REQ-304, 305** | 대규(백엔드) | **레지스트리 조회** — 존재 목록·Entity↔Node 소속·Node 원점의 전역 배치·Zone 트리·`display_name`/`aliases[]`, 핸드오버 변경 통지 *(형태는 Tier 2)* |
| **REQ-901, 1208** | 대규(백엔드) | **제어 액션 카탈로그(도메인 어휘집)** — action id · 대상 타입 · 파라미터 스키마 · 필요 역할 · 확인 필요 여부 *(Tier 2)* |
| REQ-702 | 대규(관측) | **백엔드 질의 프록시** 규격(instant/range) 및 응답 스키마 — 직접 접근 금지 |
| REQ-703 | 대규(관측) | Grafana 패널 임베드 방식 *(v0.3: Grafana는 내부 모니터링 전용으로 확정 — 우선순위 하향)* |
| REQ-704 | 대규(관측) / 하드웨어 | 영상·고빈도 스트림의 전송 프로토콜 및 엔드포인트 규약 — **RTSP→WebRTC/HLS 변환 지점은 Tier 2** |
| REQ-407 | AI(디텍션) | 오브젝트 디텍션 결과 스키마(박스 좌표계, 클래스, 신뢰도, 프레임 동기화 키) — *A-1~A-4 반영 확정* |
| REQ-903, 909 | 대규(백엔드) | **명령 결과 규격** — `command.result` 5값 및 백엔드의 `completed` 승격 규칙, 타임아웃 기본값 *(Tier 2)* |
| REQ-1101 | 전 파트 | 각 파트가 제공하는 데이터의 추상화 수준 및 집약 가능성 |

### 6.2 주는 쪽 (아웃바운드)

| 요구사항 ID | 대상 파트 | 계약 내용 |
|---|---|---|
| REQ-901~906, 909 | 백엔드 | 제어 명령 발행 API 계약(요청/응답/상태 코드), **추상 action → 디바이스 cmd 번역 분계점**, `command_id`·`expires_at` |
| REQ-907, 908 | 대규(관측) | **명령 요청에 실을 감사 필드 규격** — `origin.path` / **`input.mode`** / **`decision.source`** / `voice.*` / `llm.*`. Log Record 작성·저장은 백엔드 *(v0.3: 본 레이어의 OTel 직접 발행에서 변경)* |
| REQ-1305 | 대규(관측) | 음성 발행 시 발화 원문·신뢰도·의도의 **전달 필드** |
| NFR-001, 002 | 대규(관측) | **확정됨** — RBAC(viewer/operator/admin) 백엔드 강제 + 현재 역할 조회 API + 감사 조회 API(target·기간·actor·origin.path 필터) |
| `viz.*` 지표 | 대규(관측) | 자체 지표는 **OTel 직접 노출**(감사와 경로가 다름). 단위는 `unit` 필드 |

### 6.3 계약 위험도

| 위험 | 내용 | 완화 |
|---|---|---|
| **높음** | **REQ-304 레지스트리 조회 형태 미정**(통합정립본 Tier 2) — F3 전체와 REQ-203의 미배포 표시가 여기 걸린다. 미배포 대상은 값을 발행하지 않으므로 존재 목록 없이는 화면에 그릴 수 없다 | 정적 구성 파일로 선행 개발(REQ-204). 회의에서 Tier 2 우선순위 상향 요청 |
| 중간 | **REQ-1208 액션 카탈로그 미정**(Tier 2) — F9 액션 선언과 F12 도구 세트가 하드코딩된다 | 정적 카탈로그로 선행, 형태 확정 시 교체 |
| 중간 | **스트림 변환 지점 미정**(Tier 2) — REQ-502·704의 영상 렌더러 구조가 결정되지 않는다 | 녹화 파일 + `frame_ref` 스틸 방식으로 선행 구현 |
| 중간 | REQ-407 프레임 동기화 — 영상 프레임과 디텍션 결과의 시각 정렬 기준 | A-1(`frame_ref` 되돌리기)로 해소 예정. AI 파트 반영 확인 |
| ~~높음~~ **해소** | ~~REQ-907 아웃바운드 이벤트 스키마~~ | v0.3에서 작성 주체(백엔드 audit-writer)와 전달 필드가 확정됨 |
| ~~높음~~ **해소** | ~~REQ-201 식별자 체계~~ | Entity=`agent_id`/`sensor_id`, Node=`site_id`로 확정 |

---

## 7. 미결 사항 및 가정

### 7.1 v0.1에서 이월

- 국방(GOP 감시) 분야의 요건은 확정되지 않았으며, 매핑표(5장)는 일반적인 감시 시나리오를 가정한 것이다.
- NFR-004의 구체적 목표 수치는 분야별 요구사항 문서에서 정의하는 것으로 가정했다.
- ~~REQ-903의 "낙관적 표시 vs 확인 후 반영" 선택 기준~~ → **해소**(v0.3). 기본은 확인 후 반영이며, 확정은 백엔드가 승격한 `command.result`를 따른다.
- REQ-905의 자동/수동 제어 간 우선순위 규칙은 분야별 정책에 따르며, 본 문서는 "상태 가시화"까지만 정의한다.
- 캡스톤·하천 두 분야 간 개발 순서는 정의하지 않았다.
- 디스크립터 버전 호환성 판단 로직(REQ-104)의 실제 구현 방식이 미정이다.

### 7.2 v0.2 신규

- **로컬 LLM의 모델·런타임이 미정이다.** REQ-1201로 추상화해 두었으나, 실제 온디바이스에서 REQ-1203(태스크 분해 → 노드 생성)을 수행할 만한 모델 크기와 하드웨어 요건은 검증되지 않았다. **본 문서 전체에서 가장 큰 기술 리스크다.**
- **REQ-1203의 성공 판정 기준이 없다.** "LLM이 노드를 생성했다"와 "생성된 노드가 의도에 맞다"는 다르다. 평가 방법이 필요하다.
- **F11 집약 규칙(REQ-1102)의 표현력 범위가 미정이다.** 단순 임계 판정만 지원할지, 가중합·조건 조합까지 지원할지에 따라 F6 규칙 엔진과의 통합 여부가 갈린다. (두 규칙 엔진을 따로 만드는 것은 피해야 한다)
- **F10과 F4의 실행 위치 관계가 미정이다.** REQ-404(서버/클라이언트 분기)와 REQ-1005(시험 실행)가 만나는 지점에서, 시험 실행을 클라이언트 단독으로 할 수 있는지 서버가 필요한지 결정되지 않았다.
- **음성 입력의 물리적 환경이 미정이다.** 관제실 소음, 다중 화자, 마이크 배치 등 STT 정확도를 좌우하는 조건이 정의되지 않았다.
- **REQ-1104 근거 추적의 깊이가 미정이다.** 한 단계 아래까지인지 원본까지 전 경로인지에 따라 구현 난이도가 크게 다르다.

### 7.3 v0.3 신규

- **`origin.path` 두 축 분리가 아직 공통 계약에 반영되지 않았다.** 통합정립본은 여전히 단일 4값(`manual`/`voice`/`llm_suggestion`/`auto`)이며, 이대로는 **"음성으로 말했고 LLM이 해석했다"**가 표현되지 않는다. REQ-908이 요구하는 `input.mode`·`decision.source` 분리를 백엔드에 요청 중이다. 기존 `origin.path`를 두 필드에서 **파생**시키면 기존 쿼리를 깨지 않고 조합만 추가할 수 있다.
- **REQ-304 레지스트리 조회와 REQ-1208 액션 카탈로그의 형태가 Tier 2다.** 그때까지 F3와 F9·F12는 정적 구성 파일에 의존한다(REQ-204의 연장).
- **STT 엔진·모델이 미정이다.** REQ-1302로 추상화만 해두고 구현체는 Phase A 스파이크 결과로 확정한다. NFR-010(로컬 처리 원칙)을 만족하는 범위에서 선정한다.
- **음성 원본 오디오의 보존 여부가 미정이다.** 기본은 전사 텍스트만 보존이며, 확정은 감사 보존 기간과 함께 Tier 2다.
- **REQ-1306의 신뢰도 임계값이 미정이다.** STT·의도 해석 각각의 실행 허용 기준을 실측 후 정한다.

### 7.4 v0.4 신규

- **REQ-206의 표기를 누가 붙이는지가 미정이다.** 표기를 발행하는 쪽은 원천(하드웨어·시뮬레이터)과 백엔드이고 본 레이어는 소비자다. 상대 파트 시트에 대응 항목이 생기지 않으면 이 요구사항은 읽을 값이 없어 성립하지 않는다. 필드 형식은 `VZ-C-03`(집약 계층 표기)과 같은 **값 동반 메타** 형태를 제안한다.
- ~~다른 파트 문서의 「브라우저」 표현 정렬~~ → **해소**(v0.5). `BE-T-03`이 특정 클라이언트 기술에 고정하지 않는 **가시화 클라이언트 실시간 채널 게이트웨이**로 일반화됐다.
- **뷰어가 둘 이상일 때의 표시 일관성 기준이 없다.** 같은 대상을 두 뷰어가 다르게 그리면 어느 쪽을 믿을지 정할 근거가 없다. 상태 판정을 서버가 단독으로 하는 원칙(REQ-205의 `availability`)이 이미 그 방향이지만, **뷰어 간 불일치를 발견했을 때의 처리**는 정의되어 있지 않다.
- **미디어 평면에서 뷰어로 나가는 출력 분기가 미정이다.** `AI-C-14`로 별도 미디어 경로가 확정되고 `AI-C-08` GStreamer 어댑터가 AI 입력까지 담당하지만, WebRTC·HLS 등 뷰어용 출력 형식과 소유 파트는 정해지지 않았다. 뷰어별로 원본 재생 능력에 의존하지 않고 공통 계약으로 정해야 한다.

### 7.8 v1.0 신규 — **가장 먼저 정해야 하는 것**

- **`contracts/mission.schema.json`의 `utterance.confidence`가 실제 STT 출력과 맞지 않는다.**
  0단계에서 `0~1 숫자 하나`로 합의했는데 Whisper는 그런 값을 주지 않는다. 나오는 것은
  `avg_logprob`(상한 0의 음수), `no_speech_prob`(확률), 평균 단어 확률(확률) 셋이고 축이 서로 다르다.
  게다가 `additionalProperties: false`라 필드를 더 넣을 수도 없다.

  **가중합으로 숫자 하나를 만들어 채우면 안 된다.** 2026-08-28 실측에서 **무음에 대한 반복 환각이
  세 수치를 전부 통과했다**(`avg_logprob -0.088`은 측정 중 최고값, 평균 단어 확률 `0.933`은
  정답 발화보다 높다). 가중합을 했다면 이 사실이 숫자 하나에 묻혔을 것이다.

  | 안 | 무엇을 포기하나 |
  |---|---|
  | ① `confidence`는 파생값, 세 수치는 `trace-event.payload`에 | **기록의 응집** — 임계 근거가 임무 기록 밖에 남아 조인이 필요하다 |
  | ② `utterance`를 열어 세 수치를 명시 필드로 | **계층 경계** — `additionalProperties: false`를 넣은 이유. 엔진 종속 어휘가 들어오면 `REQ-1302`와도 부딪친다 |
  | ③ `confidence`를 엔진 비종속 의미로 재정의 | **판정의 실질** — 위 환각이 최고 점수로 통과한다 |

  실측·근거는 `reports/2026-08-28_1620_STT이식.md`. **`VZ-G-01`(마일스톤 분리)이 임무 객체를
  만드는 순간 필요하므로 다음 작업 착수 전에 정한다.** 계약은 0단계 합의 사항이라 혼자 바꾸지 않는다.

- **`compression_ratio`를 네 번째 판정 축으로 넣을 것인가.** 위 환각을 실제로 거르는 유일한 수치다
  (해당 건 `12.42` · 엔진 문턱 `2.4`). 넣는다면 "어디에 실을 것인가"가 위 계약 결정과 같은 문제가 된다.

- **`VZ-L-03` 신뢰도 임계값이 여전히 미정이다.** 판정 지점과 설정 자리는 만들었고 화면에
  "잠정 — 실측 미완"이 붙어 있다. 거절선 둘은 엔진 기본 문턱을 빌려 왔고 **수락선 셋은 감이다.**
  정답 발화 30~50건과 잡음 환경 자료로 오거부율·오답 통과율 곡선을 그려야 확정된다.

- **녹음 보관 정책이 없다.** `utterance.audio_ref`가 요구해 음성 원본을 남기는데, 사람 목소리와
  발화 내용이라 보관 기간·삭제 주체를 정해야 한다. `NFR-010`(로컬 처리)만으로는 부족하다.
  `audio_ref`를 백엔드가 어떻게 해석할지도 합의가 필요하다.

### 7.7 v0.7 신규

- **생성 품질이 도구 평가에 섞일 수 있다.** 생성기와 디버거가 같은 레이어에 있으므로,
  "디버깅 도구가 좋다"와 "생성기가 좋다"가 분리되지 않을 위험이 있다.
  결함 주입 실험은 **근본 원인을 아는 합성 데이터**를 쓰므로 이 위험을 피해 가지만,
  사용자 실험에서는 두 변인을 분리해 설계해야 한다.
- **로컬 LLM 성능·자원 요건이 정해지지 않았다.** 생성이 로컬 LLM에 의존하게 되었는데
  어느 규모 모델을 어디서 돌릴지가 없다. `AI-C-16`(폐쇄망)이 기본 전제이므로 외부 API를
  기본 경로로 둘 수 없다. 연구실 PC 사양 기준 실측이 필요하다.
- **생성 실패의 표시 방법이 없다.** `VZ-I-10`(AI 실패 이벤트)은 외부 AI의 실패를 받는 경로다.
  이제 가시화 자신이 생성에 실패할 수 있으므로, 그 실패를 자체 관측(`VZ-O-04`)으로 낼지
  화면 상태로만 다룰지 정해야 한다.

### 7.6 v0.6 신규

- **계획 생성·검증의 담당 파트가 없다.** 엑셀 신판에서 `AI-D-01`·`AI-D-02`·`AI-D-04`가 삭제되었고
  다른 파트 시트에 대체 항목이 없다. 마일스톤·태스크·액션 아이템을 **누가 만드는가**가 정해지지 않으면
  F14 전체와 `VZ-U-05`·`VZ-U-07`·`VZ-L-02`·`VZ-L-03`이 받을 것이 없는 요구사항으로 남는다.
  **v0.6에서 가장 큰 공백이며 다른 미결보다 우선한다.**
- **학습 반영 경로가 사라졌다.** `AI-L-01`~`03` 삭제로 사용자가 확인한 명칭이 학습으로 이어지지 않는다.
  `AI-S-04`의 분류 상태 갱신까지가 끝이며, `VZ-U-06`의 목적을 그 범위로 좁혔다.
- **관측 export 주기와 요약 pull 주기가 어긋난다.** `HW-C-05`는 60초 export인데 `BE-S-03`은 15초 pull이다.
  하드웨어 계열 지표는 15초로 질의해도 같은 점이 네 번 반복된다. 어느 쪽을 맞출지 팀 합의가 필요하다.
- **오프라인 표시에 약 4초의 구조적 지연이 있다.** `HW-S-07` 개정으로 판정 자체가 4회 미수신을 요구한다.
  화면이 이 지연을 감추면 "방금까지 정상이었다"는 오해를 만든다. 표시 방식 합의가 필요하다.
- **폐쇄망이 기본 전제가 되었다.** `AI-C-16`·`AI-C-17` 신설로 외부 연결 없는 기동이 예외가 아니라 기본이다.
  `VZ-C-02`(외부 소스 부재 시 동작)를 "개발 편의"가 아니라 **운용 요구사항**으로 재분류해야 한다.

### 7.5 v0.5 신규

- **선택형 인지 capability 상태가 가시화까지 오는 경로가 없다.** 엣지 정밀 인지가 없는 배치와 장애로 결과가 끊긴 상태를 현재 화면 계약만으로 구분할 수 없다.
- **온디바이스와 엣지 인지 결과를 같은 신뢰도 축으로 표시할 수 없다.** 온디바이스에는 분류 신뢰도·추적 ID가 없을 수 있으므로 출처별 표현과 필드 해석을 유지해야 한다.
- **T3 핸드오버의 영구 식별자는 `device_id`다.** MAC·IP는 현재 도달성 정보이며 장치 정체성으로 단독 사용하지 않는다. 백엔드 문서의 옛 「MAC 매핑」 라벨은 정정이 필요하다.
