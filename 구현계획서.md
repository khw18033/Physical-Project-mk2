# 구현 계획서 (Implementation Plan)

**문서 버전**: v0.3.1
**작성일**: 2026-08-18 (v0.2 → v0.3 전면 개정, v0.3.1 협업 프로세스 보강)
**근거 문서**: `요구사항정의서.md` v0.2, `260818_회의록.md`, `docs/research/2026-08-18_grafana-dynamic-datasource.md`

## v0.2 → v0.3 개정 요약

회의 결과로 **파트 경계와 프로젝트의 차별화 축이 모두 바뀌었다.** 로드맵을 유지한 채
기능만 덧붙이는 것으로는 정합이 맞지 않아 전면 개정한다.

| # | 변경 | 사유 |
|---|---|---|
| 1 | **ADR-001(Go 코어) 폐기** → ADR-002(웹/TS) 확정 | Go를 택한 근거가 "컨테이너 이벤트 처리량"이었는데, 그 이벤트를 직접 감지하는 책임이 나영 파트로 이관됐다. 근거가 사라졌으므로 결정도 무효다. |
| 2 | **Phase 2(라이프사이클 감지)·Phase 3(Prometheus 수집) 삭제** | 각각 나영·대규 파트와 중복. 우리는 계약으로 수신·질의만 한다. |
| 3 | **노드 에디터(F10)를 최우선 마일스톤으로 승격** | 회의가 지적한 "정적이라 임팩트가 낮다"에 대한 직접적 대응이다. |
| 4 | **LLM 계층(F12)을 MVP 안으로 편입** | "LLM 기반"이 차별화의 전부라면 MVP 이후로 미룰 수 없다. 다만 리스크가 커서 별도 스파이크를 앞에 둔다. |
| 5 | **Phase -1(인터페이스 계약 협의) 신설** | 회의 §4의 최대 리스크(통합 시 불일치)에 대응. 코드보다 먼저다. |

---

## 0. 기술 스택 결정 기록

### ADR-001 — 코어 엔진 Go 채택 *(폐기 — Superseded by ADR-002)*

> **폐기 사유**: ADR-001은 "컨테이너 라이프사이클 감지(F2)와 고빈도 실시간 이벤트
> 처리량"을 결정 기준의 전부로 삼았다. 260818 회의에서 오케스트레이터 이벤트 감지가
> 나영 파트로 이관되면서(요구사항정의서 v0.2 §3), 본 레이어에 남은 것은 이벤트
> **수신**과 화면 반영이다. 처리량 요건이 우리 쪽에 없으므로 Go를 택할 근거가
> 소멸했다. 기록 목적으로만 남긴다.

### ADR-002 — 가시화 도구 스택: 웹(TypeScript) 확정

**맥락**: 회의록 §2는 파이썬(DearPyGui) / C#(.NET) / 웹 세 후보를 놓고 결론을
내리지 못했으며, "스택 자체로는 승부하기 어렵다. 로컬 LLM 기반이라는 포인트를
얹어야 한다"로 정리됐다. 즉 **스택 선택의 기준은 우열이 아니라 "차별화 요소에
쓸 시간을 얼마나 남겨주는가"** 다.

| 후보 | 노드 에디터(F10) | 영상·중첩(F5) | 계층 뷰(F11) | 로컬 LLM(F12) | 회의록 평가 |
|---|---|---|---|---|---|
| **웹 / TypeScript (채택)** | React Flow 등 성숙한 라이브러리 존재 | video/canvas/WebGL 오버레이가 기본기 | DOM 기반 계층 전환 자연스러움 | 로컬 LLM을 HTTP/MCP 서버로 두고 호출 — 언어 무관 | "수요 많지만 공급도 많고 노가다 비중 큼" |
| Python / DearPyGui | 참조 데모와 동일 스택, 재현 최속 | 즉시 렌더링 GUI라 영상은 유리하나 중첩 레이어는 직접 구현 | 직접 구현 | 프로세스 내 직접 호출 가능 | "취업 관점에서 좋은 선택지 아님" |
| C# / .NET | 노드 에디터 라이브러리 생태계 얕음 | 가능하나 구현 비용 큼 | 가능 | 외부 프로세스 호출 | "유니크하지만 운에 따라 갈리는 베팅" |

**결정**: **웹(TypeScript) + React Flow**로 확정한다.

**근거**

1. **F10이 최대 작업량인데, 웹만이 이를 라이브러리로 흡수한다.** 노드 에디터를 밑바닥부터 만들면 그 자체로 한 학기가 간다. 그 시간은 차별화(F12)에 써야 한다.
2. **F5 REQ-504 중첩 렌더링이 사실상 웹 전용 요건이다.** 영상 위 디텍션 박스 오버레이, 한 그래프에 두 소스 계열 — 브라우저의 합성 모델이 그대로 답이다.
3. **기존 자산이 있다.** `web-dashboard/prototype.html`이 이미 계약을 읽어 렌더러 4종을 그린다. 폐기 없이 이관 가능하다.
4. **LLM 종속성이 없다.** F12를 MCP/HTTP 서버로 분리하면 모델 런타임은 파이썬이든 무엇이든 무방하다. 스택 선택이 LLM 선택을 제약하지 않는다.
5. **회의록의 "노가다 비중 큼"은 사실이나 감수 가능하다.** 그 비판은 CRUD 화면 양산에 대한 것이고, 우리 작업은 그래프 편집기와 추론 계층이다.

**리스크와 대응**

| 리스크 | 대응 |
|---|---|
| 웹은 공급이 많아 차별화가 약하다(회의록 지적) | 차별화는 스택이 아니라 F12로 낸다. ADR-002는 그 판단의 실행이다. |
| 로컬 LLM 추론을 브라우저에서 못 돌린다 | 돌리지 않는다. 로컬 머신의 별도 프로세스(MCP 서버)로 두고 브라우저는 호출만 한다(REQ-1201). |
| 대용량 스트림 처리 시 브라우저 한계 | REQ-404 실행 위치 분기로 무거운 변환은 서버 측에 둔다. |

**프레임워크 세부**: React + TypeScript + Vite. 노드 에디터는 React Flow.
상태 관리·UI 킷은 Phase 1 착수 시 결정한다(현 시점 결정 불필요).

### ADR-003 — 그라파나: 대체하지 않고 흡수한다

**결정**: 그라파나를 경쟁 대상으로 두지 않는다. **F7 DataSource의 구현체(REQ-703)로
흡수**하고, 그라파나가 구조적으로 못 하는 영역만 우리가 만든다.

**근거** (`docs/research/2026-08-18_grafana-dynamic-datasource.md` 전문)

| 요구 | 그라파나 | 판단 |
|---|---|---|
| 여러 데이터소스 중첩 | `-- Mixed --` + transformation으로 **가능** | 다시 만들지 않는다 |
| 런타임 노드 조합 | **불가** — Node Graph는 시각화일 뿐 실행기가 아니고, transformation은 선형 체인이라 DAG를 표현하지 못한다 | **F10으로 우리가 만든다** |
| 미디어 파이프라인 | **불가** — 데이터 모델이 DataFrame이라 영상은 Text 패널 HTML 우회 (요구사항정의서 문제 #3과 동일 증상) | **F4 REQ-407로 우리가 만든다** |

부수 발견: 그라파나 Stat 등 단일값 패널은 다중 쿼리 지원이 제한적이다. 이는
F11의 최상위 계층("결심자에게는 빨강/초록")과 정확히 겹치는 지점이므로,
그라파나 위에 얹는 방식으로는 F11이 성립하지 않는다.

---

## 1. 아키텍처 개요 및 모듈 분해

언어 중립 계약(스키마)을 먼저 고정하는 계약 우선 설계를 유지한다. Phase 0에서
이미 5개 계약이 고정되었고 프로토타입이 그 실행 가능성을 입증했으므로, 이 접근
자체는 검증됐다.

| 모듈 | 대응 요구사항 | 역할 | 상태 |
|---|---|---|---|
| `contracts/` | F1, F4, F5, F6, F7 | 5개 JSON Schema. v0.2 신설 요건(REQ-405 DAG, REQ-407 미디어 타입, REQ-105 자연어 서술)만큼 **개정 필요** | 기존, 개정 대상 |
| `contracts/` (신규) | F10, F11, F12 | 노드 카탈로그, 추상화 계층, LLM 도구 선언 계약 추가 | 신규 |
| `validator/` | F8(REQ-801~804) | 스키마 검증 + **계약 간 정합성 검증**(REQ-803) | 기존 확장 |
| `web-dashboard/` | F1~F5, F9~F11, F13 | React+TS SPA. 렌더러·노드 에디터·계층 뷰·음성 입력 | 프로토타입 존재 |
| `llm-gateway/` | F12(REQ-1201~1207) | 로컬 LLM을 MCP/HTTP로 노출. 도구 세트 등록·태스크 분해 | 신규 |
| `adapters/` | F2(REQ-201), F7(REQ-702~704) | 라이프사이클 이벤트 수신, Prometheus/Grafana/스트림 DataSource 구현체 | 신규 |
| `docs/adr/`, `docs/research/` | — | 결정 기록 및 조사 결과 | 일부 존재 |

> `core-engine/`(Go), `renderers/`, `datasources/prometheus/`는 v0.2 모듈표에서
> 삭제한다. ADR-001 폐기 및 파트 경계 변경에 따른 결과다. F5 렌더러는 스키마상
> 대부분 `executionLocation: client`이므로 `web-dashboard/` 안에 둔다 — 프로토타입
> 작업에서 이미 확인된 사실이다.

---

## 2. 단계별 로드맵

### Phase -1 — 인터페이스 계약 협의 *(신설, 최우선)*

회의 §4의 최대 리스크는 "각자 따로 만들면 통합하는 순간 안 맞는다"이다. 코드보다
먼저다.

- **작업**: 요구사항정의서 v0.2 §6(인바운드/아웃바운드 표)을 공통 인터페이스 시트
  형식으로 제출. 위험도 "높음" 2건을 우선 확정한다.
  1. **REQ-907 아웃바운드 이벤트 스키마** (대규) — 제어 명령 OTel 이벤트의 필드 구성
  2. **REQ-201 식별자 체계** (나영) — 컨테이너 ID / 디스크립터 ID / Entity ID 대응 규칙
- **완료 기준**: 두 계약에 대해 양쪽 파트의 서면 합의. 나머지는 초안 제출까지.
- **비고**: 이 단계는 **다른 Phase를 블로킹하지 않는다.** REQ-204(이벤트 소스 부재 시
  정적 구성으로 동작)를 넣어둔 이유가 이것이다.

### Phase 0 — 계약 고정 *(완료, 개정 필요)*

- **완료분**: F1/F4/F5/F6/F7 계약 5종 + 예시 20건, validator 통과.
- **개정 작업**: v0.2 신설 요건 반영
  - `pipeline.schema.json` — 분기·합류 DAG 표현(REQ-405), 미디어 데이터 타입(REQ-407)
  - `component-descriptor.schema.json` — 자연어 서술 필드(REQ-105)
  - `renderer.schema.json` — 중첩/레이어 선언(REQ-504)
  - 신규: 노드 카탈로그 계약(REQ-1003), 추상화 계층 계약(REQ-1101), LLM 도구 계약(REQ-1202)
- **검증**: 개정 후 기존 예시 20건이 여전히 통과하는지 + 신규 요건 예시 추가.

### Phase 0.5 — 프런트엔드 프로토타입 *(완료)*

- `web-dashboard/prototype.html` — 무빌드 단일 HTML, 렌더러 4종, F1·F2·F4·F5·F7 구현.
- 성과: 계약만으로 대시보드 구성이 가능함을 확인. 계약 결함 2건 조기 발견.
- **v0.3에서의 위치**: Phase 1의 이관 대상. 폐기하지 않는다.

### Phase A — LLM 실현가능성 스파이크 *(신설, 조기 배치)*

> **가장 큰 미지수를 가장 먼저 친다.** F12가 불가능하다고 판명되면 프로젝트의 차별화
> 축 전체를 다시 잡아야 하는데, 그 사실은 늦게 알수록 비싸다.

- **작업**: 로컬 LLM을 MCP 서버로 띄우고, **미리 정의한 도구 3~5개**만 노출한 뒤
  "온도가 높은 구역을 보여줘" 수준의 지시가 도구 호출 시퀀스로 분해되는지 확인한다.
  UI 없이 콘솔 수준으로 충분하다.
- **완료 기준(정량)**: 준비한 지시문 10건 중 **7건 이상**에서 의도에 맞는 도구
  호출 시퀀스가 생성될 것. 지시문·정답 시퀀스는 사전에 작성해 고정한다.
- **산출물**: 모델·하드웨어 요건 보고서. 미달 시 대안(더 큰 모델 / 규칙 기반
  폴백 / 도구 단순화) 판단.
- **선행 조건**: 없음. Phase -1·0과 병렬 진행 가능.

### Phase 1 — 프로토타입 정식 이관

- **작업**: `prototype.html`을 React+TS+Vite 구조로 이관. 렌더러 4종
  (text/table/graph/video-stream)을 REQ-503 등록 인터페이스에 맞춰 재구성.
  라이프사이클 3상태 표시(REQ-203)와 정적 구성 동작(REQ-204) 유지.
- **검증**: 이관 전후 렌더링 결과가 동일할 것. 계약 파일을 직접 읽는 구조를 유지해
  드리프트를 막는다.

### Phase 2 — 노드 조합 편집기 *(핵심 마일스톤)*

- **작업**: React Flow 기반 캔버스(REQ-1001), 계약 무손실 왕복(REQ-1002),
  노드 카탈로그 자동 구성(REQ-1003), 연결 유효성 강제(REQ-1004/804),
  시험 실행 및 노드별 출력 미리보기(REQ-1005/406), 반영·되돌리기(REQ-1006).
- **검증(골든 시나리오)**: 두 개의 서로 다른 데이터 소스를 캔버스에서 연결해
  하나의 그래프에 중첩 표시하고, 그 구성을 파이프라인 JSON으로 저장한 뒤 다시
  불러와 동일한 그래프가 복원되는 것을 확인한다. **개발자가 코드를 한 줄도
  고치지 않고 새 조합을 만들 수 있어야 한다** — 이것이 "정적 → 동적"의 판정 기준이다.
- **이 Phase의 의미**: 회의 지적("정적이라 임팩트 낮음")에 대한 직접적 답이다.

### Phase 3 — 데이터소스 연결 및 미디어 파이프라인

- **작업**: Prometheus(REQ-702)·Grafana(REQ-703)·스트림(REQ-704) DataSource 구현체.
  프로토타입의 목 데이터를 실데이터로 교체. 카메라 → 필터 → 디텍션 체인(REQ-407)과
  영상 위 오버레이(REQ-504).
- **검증**: 실제 Prometheus 메트릭이 Phase 2 노드 에디터에서 구성한 경로를 통해
  그래프까지 end-to-end로 표시. 영상 스트림에 디텍션 박스가 겹쳐 표시.
- **선행 조건**: 대규 파트의 관측 스택. 미비 시 로컬 Prometheus로 대체 진행한다.

### Phase 4 — LLM 계층 통합

- **작업**: Phase A 결과를 `llm-gateway/`로 정식화. 태스크 분해 → 노드 자동
  생성(REQ-1203), 제안-검토-수락 절차(REQ-1204), LLM 관여 표시(REQ-1207),
  LLM 가공 노드(REQ-1205), 미가용 시 성능 저하 허용(REQ-1206).
- **검증**: 자연어 지시 하나로 Phase 2 캔버스에 노드가 생성되고, 사용자가
  더블클릭해 각 서브태스크 결과를 확인한 뒤 수락하면 반영된다. **거부하면 아무
  변화가 없어야 한다.**
- **선행 조건**: Phase A 통과, Phase 2 완료.

### Phase 5 — 추상화 계층 뷰 및 설명가능성

- **작업**: 추상화 수준 선언(REQ-1101), 집약 규칙(REQ-1102), 하향 드릴다운(REQ-1103),
  근거 추적(REQ-1104), 역할별 기본 뷰(REQ-1105). F6 이벤트 규칙 엔진(REQ-601)과
  통합 — **규칙 엔진을 두 개 만들지 않는다.**
- **검증**: 최상위 화면의 단일 판단 지표에서 더블클릭으로 구성 요소 → 원본까지
  내려가고, 각 단계에서 "이 값이 나온 이유"가 표시되는지 확인.

### Phase 6 — 제어 액션 및 감사 *(우선순위 상향)*

- **작업**: 제어 액션 선언(REQ-901), 권한·확인(REQ-902/NFR-001), 낙관적/확인
  반영(REQ-903), 실패·타임아웃(REQ-904), 자동/수동 모드 표시(REQ-905),
  **아웃바운드 OTel 이벤트 발행(REQ-907)과 발행 경로 구분 기록(REQ-908)**.
- **검증**: 제어 명령을 발행하면 OTel에 누가·언제·어디서·어떤 경로로가 기록되고,
  수동 클릭 / LLM 제안 수락 / 음성 발행이 로그에서 구별되는지 확인.
- **선행 조건**: Phase -1의 REQ-907 계약 합의.
- **비고**: v0.2에서 "2차(MVP 이후)"였으나, 회의 §4 수문 예시(제어 명령 미기록 시
  책임 소재 추적 불가)에 따라 MVP 안으로 끌어올렸다.

### Phase 7 — 음성 인터페이스

- **작업**: STT 입력 → 액션 트리거(REQ-1301), 엔진 추상화(REQ-1302), 인식 결과
  확인(REQ-1303), 어휘 확장(REQ-1304), 발화 원문 감사 기록(REQ-1305).
- **검증**: 음성만으로 뷰 전환이 되고, 제어 액션은 확인 절차를 반드시 거치며,
  발화 원문이 감사 로그에 남는지 확인.
- **비고**: 기술 난이도가 낮고 시연 효과가 크다(회의 §3). Phase 6 이후 배치했으나
  여력이 생기면 앞당길 수 있는 유일한 Phase다.
- **외부 일정 앵커**: 회의 §3이 지목한 시연 자리(내년 총장 관련 행사, 100주년 기념
  행사)가 확정되면 그 날짜가 이 Phase의 실질 마감이 된다. 날짜가 잡히는 즉시
  Phase 7을 앞으로 당기고, 시연 경로(음성 → 뷰 전환 → 제어 확인)만 우선 완성한다.

### Phase 8 — 확장점 실증 및 MVP 마감

- **작업**: 캡스톤(로봇 관제) 도메인으로 E1~E8을 실제로 채운다. NFR-005(관측 스택
  비종속)·NFR-006(코어 무변경 확장) 스모크 테스트 — 신규 렌더러·데이터소스·노드
  타입·LLM 도구를 코어 수정 없이 추가해본다. validator를 CI에 연결.
- **완료 기준**: 이 시점을 MVP 완료로 정의한다.

### 의존 관계 요약

```
Phase -1 (계약 협의) ─────────────────┬──────────────→ Phase 6 (제어·감사)
Phase A  (LLM 스파이크) ──────────────┼──────→ Phase 4 (LLM 통합)
Phase 0 개정 → Phase 1 → Phase 2 ────┴──→ Phase 3 → Phase 5 → Phase 7 → Phase 8
                        (노드 에디터)
```

Phase -1 · A · 0개정은 **서로 독립이며 동시에 착수할 수 있다.**

---

## 3. 협업 운영 프로세스

> 회의 §4(요구사항 정립 절차)와 §5(PM 역할)를 계획에 고정한다. Phase -1이 "무엇을
> 합의할지"라면, 이 장은 "어떤 순서와 형식으로 합의할지"다.

### 3.1 요구사항 정립 5단계 (회의 §4)

| 단계 | 내용 | 본 파트(가시화)의 산출물 | 상태 |
|---|---|---|---|
| 1 | 각자 요구사항 1차 정리 | `요구사항정의서.md` v0.2 | 완료 |
| 2 | 파트 간 오가는 정보 식별 | 요구사항정의서 §6 인바운드/아웃바운드 표 | 완료 |
| 3 | **공통 인터페이스 문서 1개**(모듈별 수신부/송신부 명세) 작성 | `docs/interfaces/` 파트별 인터페이스 시트 제출 | 진행 중 |
| 4 | 확정된 인터페이스를 기준으로 요구사항 재생성 | 계약 스키마(`contracts/`) 개정 = Phase 0 개정 | 대기 |
| 5 | 전체 리뷰 | PM 주관 회의에 제출 | 대기 |

**중요**: 4단계는 3단계의 결과물을 입력으로 받는다. 인터페이스가 확정되기 전에
계약 스키마를 고쳐두면 두 번 고치게 된다. Phase 0 개정 중 **외부 계약에 닿는
항목**(REQ-702/703/704, REQ-907/908)은 3단계 합의 이후로 미룬다. 외부에 닿지 않는
항목(REQ-405 DAG, REQ-105 자연어 서술, REQ-504 중첩)은 지금 진행해도 안전하다.

### 3.2 PM 체계 (회의 §5)

- 현 시점 PM은 **대규**다. OTel 기반 관측 스택이 모든 데이터의 경유지이므로 시작점을
  가진 사람이 중심을 잡는다.
- 따라서 본 파트는 **대규 파트의 스키마가 확정되기를 기다리는 쪽이 아니라, 필요한
  필드를 먼저 제시하는 쪽**으로 움직인다. 회의 §5의 주문("완벽하지 않아도 논의
  가능한 안을 가져오라")에 대한 실행이다.
- 제출 형식: 인터페이스 시트는 파트별 1문서로 쪼개 작성하되, 필드 표는 그대로
  통합 시트에 붙일 수 있는 표 구조를 유지한다.

### 3.3 제출 현황

| 상대 파트 | 문서 | 상태 |
|---|---|---|
| 대규 (OTel 관측 스택) | `docs/interfaces/2026-08-18_otel-interface.md` | 초안 제출 |
| 나영 (K8s/오케스트레이터) | 미작성 — REQ-201 식별자 체계 | 다음 차례 |
| AI (디텍션·예측) | 미작성 — REQ-407, REQ-905 | 후순위(회의 §6.3 "중간" 위험) |

---

## 4. 리스크 및 미결 사항

### 4.1 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **로컬 LLM이 태스크 분해를 못 한다** | 차별화 축 소멸. 프로젝트 포지셔닝 재수립 | Phase A로 조기 검증. 정량 기준(10건 중 7건)을 미리 고정해 판정이 흐려지지 않게 한다 |
| **다른 파트 진척에 블로킹** | 대규(관측)·나영(오케스트레이터) 산출물 없이 Phase 3·6 진행 불가 | REQ-204 정적 구성 동작, 로컬 Prometheus 대체 진행. 계약만 있으면 목으로 채운다 |
| **F10 작업량이 예상을 초과** | 핵심 마일스톤 지연 | React Flow로 그래프 편집 기반을 흡수. 자체 구현하지 않는다 |
| **F6과 F11의 규칙 엔진 이원화** | 유사 기능 중복 구현 | Phase 5에서 통합 설계. REQ-1102의 표현력 범위를 그 시점에 확정 |
| **계약 개정이 프로토타입을 깨뜨림** | Phase 1 이관 비용 증가 | Phase 0 개정 시 기존 예시 20건 통과를 회귀 기준으로 유지 |

### 4.2 미결

- 상태 관리 라이브러리·UI 킷 미정 (Phase 1 착수 시 결정).
- 로컬 LLM 모델·런타임 미정 (Phase A 산출물).
- REQ-1005 시험 실행을 클라이언트 단독으로 할 수 있는지 서버가 필요한지 미정.
- NFR-004(반영 지연)·NFR-008(편집기 노드 규모) 목표 수치 미정 — 분야별 문서 대기.
- Phase 간 일정(주 단위 배분) 미수립 — PM(대규)과의 전체 일정 조율 후 확정.

---

## 5. 폐기된 계획 (v0.2 대비)

추적을 위해 무엇을 왜 버렸는지 남긴다.

| 폐기 항목 | 사유 |
|---|---|
| ADR-001 Go 코어 엔진 | 결정 근거였던 이벤트 처리량 요건이 파트 이관으로 소멸 |
| `core-engine/` 모듈 | 위와 동일. 남은 책임은 `adapters/`와 `web-dashboard/`로 분산 |
| Phase 2(라이프사이클 자동화) — Docker 이벤트 직접 감지 | 나영 파트. 수신 측(REQ-201/202)만 유지 |
| Phase 3(파이프라인 엔진) 중 Prometheus 수집 구축 | 대규 파트. 질의 구현체(REQ-702)만 유지 |
| `renderers/` 별도 모듈 | 렌더러 대부분이 `executionLocation: client`. `web-dashboard/`로 통합 |
| "F9는 2차로 미룬다" | 회의 §4 책임 소재 추적 요건에 따라 MVP 편입 |
