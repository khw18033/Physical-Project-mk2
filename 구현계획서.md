# 구현 계획서 (Implementation Plan)

**문서 버전**: v0.2
**작성일**: 2026-08-18
**근거 문서**: `요구사항정의서.md` (REQ-101~906, NFR-001~007, 확장점 E1~E6)

## Context

`요구사항정의서.md`를 기반으로 실제 구현 로드맵을 수립한다. 저장소가 완전히
비어 있는(커밋 0개) 그린필드 상태이므로, 기술 스택부터 단계별 마일스톤까지
새로 정의해야 한다.

- **기술 스택**: 코어 엔진(백엔드)은 **Go로 확정**한다. TypeScript/Node.js·
  Python도 후보로 비교 검토했으나, 성능(컨테이너 이벤트 처리량, 동시성)을
  최우선 기준으로 두어 Go를 채택했다(ADR-001 참고). 계약(스키마)은 언어
  중립적으로 JSON Schema로 정의해, 프런트엔드(SPA)와는 API 계약으로만
  연동하는 **계약 우선(Contract-First)** 접근을 유지한다.
- **1차(MVP) 범위**: 코어 레이어만. F1~F8(디스크립터, 라이프사이클 프로비저닝,
  공간계층 모델, 해석 파이프라인, 렌더러 카탈로그, 이벤트 규칙 엔진, 데이터
  소스 추상화, 적합성 검증기)까지 구현한다. F9(제어 액션 바인딩)와 확장점
  (E1~E6)의 실제 구현은 2차로 미룬다.
- **프런트엔드**: 웹 기반 SPA로 대시보드 렌더링을 시작한다.

---

## 0. 기술 스택 결정 기록 (ADR-001) — 확정: Go

세 후보(TypeScript/Node.js, Go, Python)를 비교 검토한 결과, **코어 엔진(백엔드)은
Go로 확정**한다. 결정 기준은 편의성(언어 통일 등)이 아닌 **성능**을 최우선으로
한다.

| 후보 | 강점 | 약점 | 이 프로젝트에서의 적합도 |
|---|---|---|---|
| **Go (채택)** | Docker/K3s 오케스트레이터 생태계(client-go)와 궁합이 좋고, 컨테이너 라이프사이클 감지(F2)·동시성 처리에 강점. 고루틴 기반 동시성이 고빈도 실시간 이벤트 처리에 유리. 정적 타입으로 계약(JSON Schema) 위반을 컴파일 타임에 가깝게 잡기 쉬움. prometheus/client_golang 등 관측 스택 라이브러리도 1급 지원. | 프런트엔드(SPA)와 언어가 분리되어 별도 API 계약 관리가 필요. | F2(라이프사이클 감지)·F4(파이프라인) 등 실시간 이벤트 처리량이 핵심인 코어 엔진 전체에 적합. |
| TypeScript / Node.js | 선언적 스키마를 다루기 쉬움. SPA와 언어를 통일할 수 있어 개발 편의성이 높음. | 대용량 실시간 이벤트를 고빈도로 처리할 때 Go 대비 처리량이 낮음(싱글 이벤트루프). | 성능보다 개발 속도가 우선일 때 유리하나, 이번 결정 기준(성능)에는 부합하지 않음. |
| Python | AI 기반 자동 제어(F9)·데이터 전처리 파이프라인과의 연동이 쉬움. | 대규모 실시간 이벤트 처리 성능이 상대적으로 약함. 동적 타입으로 계약 위반이 런타임에야 드러날 위험. | 코어 엔진에는 부적합. F9/AI 연동 어댑터 등 특정 확장점 구현체로는 유효. |

**결정**: 코어 엔진(`core-engine/`, `contracts/`, `validator/`, `datasources/prometheus/`)은
Go로 구현한다. 프런트엔드(`web-dashboard/`)는 별도 언어(SPA, 통상 TS)를 사용하며,
둘 사이는 JSON Schema 기반 API 계약으로만 연동한다(REQ-701 DataSource 인터페이스,
F1 디스크립터 등은 이미 언어 중립 계약으로 정의되어 있어 영향 없음). F9 제어
액션이나 AI 연동 어댑터처럼 Go 외 언어가 유리한 특정 확장점은 후속 단계에서
개별 검토한다.

---

## 1. 아키텍처 개요 및 모듈 분해

언어 중립 계약(스키마)을 먼저 고정하는 계약 우선 설계를 따른다. 모듈 경계는
요구사항정의서의 기능(F1~F8)과 1:1로 대응시켜, 어떤 모듈이 어떤 REQ를
충족하는지 항상 추적 가능하게 한다.

| 모듈 | 대응 요구사항 | 역할 |
|---|---|---|
| `contracts/` | F1(REQ-101~104), F4(REQ-401~404), F5(REQ-501~503), F6(REQ-601~602), F7(REQ-701) | Component Descriptor, Pipeline 노드(source/transform/sink), Renderer, Event Rule, DataSource — 모두 JSON Schema로 정의. 모든 언어 구현체가 이 계약을 준수. |
| `core-engine/` | F2(REQ-201~203), F3(REQ-301~303), F4(REQ-401~404 실행), F6(REQ-601) | 라이프사이클 감지·자동 프로비저닝, 공간 계층(Zone/Node/Entity) 관리, 파이프라인 실행기, 이벤트 규칙 엔진 |
| `datasources/prometheus/` | F7(REQ-702) | DataSource 인터페이스의 첫 구현체 |
| `renderers/` | F5(REQ-501~502) | 텍스트·테이블·그래프 렌더러 (MVP는 3종만, 영상/음성은 2차) |
| `validator/` | F8(REQ-801~802) | 디스크립터·파이프라인 명세 적합성 검증기 |
| `web-dashboard/` | 프런트엔드 요구사항 전반 | SPA, 코어 엔진 API를 소비해 대시보드 렌더 |
| `docs/adr/` | — | ADR-001(스택) 등 결정 기록 누적 |

---

## 2. 단계별 로드맵

각 단계는 완료 기준(검증 방법)을 함께 명시한다.

### Phase 0 — 계약 고정
- **작업**: F1/F4/F5/F6/F7 계약을 JSON Schema로 확정 (`contracts/`).
- **검증**: 각 스키마에 대해 예시 인스턴스(샘플 디스크립터, 샘플 파이프라인)를
  작성해 validate 통과 확인.

### Phase 0.5 — 프런트엔드 프로토타입 (병렬 트랙, 완료)
- **배경**: Phase 0에서 계약이 고정되었으므로, 프런트엔드는 Go 코어를 기다릴
  필요 없이 계약 예시 인스턴스만으로 진행할 수 있다(계약 우선 설계의 목적).
- **작업**: `web-dashboard/prototype.html` — 무빌드 단일 HTML로 F1·F2·F4·F5·F7을
  실제 계약 파일을 읽어 구현. 렌더러 4종(text/table/graph/video-stream).
- **성과**: 계약만으로 대시보드 구성이 가능함을 확인했고, 그 과정에서 계약
  결함 2건(binary-stream 타입 누락, datasource description 누락)을 조기 발견했다.
- **비고**: F5 렌더러는 스키마상 대부분 `executionLocation: client`이므로,
  렌더러 구현의 상당 부분은 실질적으로 프런트엔드 작업이다.

### Phase 1 — 코어 스켈레톤
- **작업**: 디스크립터 등록·검증(F1, REQ-801), 정적 구성 기반 렌더러 3종
  (텍스트/테이블/그래프) 구현.
- **검증**: 샘플 디스크립터 2개를 등록해 대시보드에 수동으로 렌더링되는지 확인.

### Phase 2 — 라이프사이클 자동화
- **작업**: Docker 이벤트 감지 → 가시화 컴포넌트 자동 생성/갱신/삭제
  (F2, REQ-201~203), 의도적 미배포와 장애 상태를 구분 표시.
- **검증(골든 시나리오)**: docker-compose로 컨테이너 2개를 기동한 뒤 1개를
  중지 — 대시보드가 자동으로 컴포넌트를 반영하고, 중지된 컴포넌트가 "장애"
  상태로 표시되는지 확인.

### Phase 3 — 파이프라인 엔진
- **작업**: source/transform/sink 노드 그래프 실행기(F4, REQ-401~404),
  Prometheus DataSource 연동(F7, REQ-702).
- **검증**: Prometheus에서 수집한 메트릭이 그래프 렌더러까지 end-to-end로
  표시되는지 확인.

### Phase 4 — 공간 계층 & 이벤트 규칙
- **작업**: Zone/Node/Entity 드릴다운(F3, REQ-301~303), 조건→뷰 액션 규칙
  엔진(F6, REQ-601~602 — 규칙 "엔진"만, 규칙 "내용"은 분야별 확장으로 제외).
- **검증**: 더미 임계값 이벤트를 발생시켜 규칙 엔진이 정의된 뷰 전환 액션을
  트리거하는지 확인.

### Phase 5 — 검증기 완성 & MVP 마감
- **작업**: F8 validator를 CI 파이프라인에 연결. NFR-005(관측 스택
  비종속)·NFR-006(코어 무변경 확장) 요건을 점검(신규 렌더러/데이터소스를
  코어 수정 없이 추가해보는 스모크 테스트).
- **완료 기준**: 이 시점을 "코어 레이어 MVP 완료"로 정의한다.

### Phase 6 (2차, MVP 이후) — F9 제어 액션 + 확장점 실증
- **작업**: 제어 액션 바인딩(REQ-901~906)과 관련 비기능 요구사항
  (NFR-001~003: 권한 관리·감사 로그·명령 검증) 구현. 이후 캡스톤(로봇 관제)
  도메인을 대상으로 확장점 E1~E6을 실제로 채워 확장 가능성을 검증.

---

## 3. 리스크 및 미결 사항

- 코어 엔진(Go)과 프런트엔드(SPA, 통상 TS)의 언어가 분리됨에 따라, API 계약
  (OpenAPI 등)을 별도로 관리하고 양쪽 변경 시 동기화하는 프로세스가 필요하다.
- 실시간 이벤트 처리량 목표치(NFR-004의 구체 수치)가 없어 Phase 2/3에서
  성능 기준 없이 진행하게 된다. 목표 수치는 분야별 요구사항 문서에서
  정의될 예정이므로, 그 전까지는 정성적 확인(자동 반영 여부)에 그친다.
- 웹 SPA의 구체적 프레임워크(React/Vue 등) 선택은 본 계획서에서도 보류했다.
- F9/AI 연동 등 Go 외 언어가 유리할 수 있는 확장점의 구현 언어는 Phase 6
  착수 전 별도로 결정한다.
